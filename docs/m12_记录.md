# M12 里程碑记录（Web 内容编辑器 5a/5a2 + GM 指令权限 5b）

**收官日期**：2026-09-03
**全仓测试**：5810 passed（9 failed 均为既有基线；9 skipped）
**HEAD**：d4f85b3（见 git log）
**verify_m12**：7/7 PASS（scripts/verify/verify_m12.py，退出码 0；70 TC：PASS 59 / PARTIAL 3 / DELAYED 8）

## 〇、里程碑定位

**M12 = Web 内容编辑器壳（5a 六页 + 5a2 扩展页）+ GM 指令与权限系统（5b）**：
元数据驱动编辑器（editor.json 注册表 / 认证 / 15+ API 端点 / 原子写盘 / 快照回退 /
热重载联动 / 前端最小壳）+ GM 4 条新指令接线（备份/恢复/存档导出/封禁列表，含真实
zip 备份后端）+ 权限模型（admin_users + granted_commands + 静默语义）+ 审计
（audit_log 追加写不可删）+ field_meta 正式表注入（19 页元数据源）+ 事件通道收口。

**前置**：PvE 战斗击杀奖励结算断链修复（146081b，经验/金币/掉落/升级全链落账——
veinborn 阶段一拦路缺口，先补完再做 M12）。

## 一、批完成总览（6 批 × 3 路节奏 + 主 agent 收口）

| 批次 | 范围 | Commit 要点 | 用例 |
|---|---|---|---|
| 前置 | 战斗奖励结算断链 | 146081b：battle_reward.py 结算引擎 + context 装配闭包 + 升级渲染 + 掉落入包（Player replace 方案） | 14 |
| 批1 | 编辑器壳数据层 | 34776ac：editor_registry 页注册表 / web auth 认证 / pages_crud 六页 CRUD；3ef325e 收口（FieldMeta label + 硬编码登记） | 69 |
| 批2 | 保存链路+权限审计 | c3eddc3：atomic_store 原子写盘+快照回退 / schema 加 admin_users+audit_log / permission_store（revoke_gm P0 修复）/ audit_store | 101 |
| 批3 | API 总装 + GM 接线 + 事件通道 | 903abc7：web/api.py 15 端点（FastAPI 编辑器壳）；e33a7ce：GM 备份/恢复/存档导出/封禁列表接线 + GmBackend 真实 zip + event_bus read_event_log | 87 |
| 批4 | field_meta 注入 + 前端壳 + verify | 8488fb8：editor.html 最小壳 + /api/editor/pages；7d85a87：quest/shop/npc/checkin 字段 + 4 视图模块；d4f85b3：verify_m12 | 29 |

## 二、关键交付

1. **编辑器后端 15+ 端点**（web/api.py）：认证 4（setup/login/logout/me Bearer token
   互踢/5 次锁 15 分钟）+ meta/refs + 六页 CRUD+validate + reload/packs + editor/pages；
   统一包络 {ok, data}；写端点走 pages_crud → atomic_store 原子写盘流水线
2. **保存链路 SV-06/07**（atomic_store.py）：temp+rename 原子写、坏 JSON/路径穿越整体
   拒绝、reload 过 check_pack 失败回退旧快照 + 人话（复用热重载同管线）
3. **权限审计 5b**：admin_users/audit_log 两表（schema 9 表）；PermissionStore 三级
   角色 + per-command 下授 + 缓存即时失效；AuditStore 追加写不可删 + E1-E6 + 轮转
4. **GM 接线**：GM_COMMANDS 5→9 条；GmBackend.backup/restore 从「已声明未接线」变
   真实 zip 实现（0o700 + backups 登记 + 恢复先备份当前再原子覆盖）；parsers 白名单
   同步；无后端降级人话不崩
5. **field_meta 正式表注入**（P-07 数据源）：quest 17/shop 15/npc 14/checkin 7 字段全
   中文 label + ai/hidden/env_event/log_card 四视图模块 + settings env_event/log_card
   段；宽松登记（专项校验器全权）→ test_demo 加载零新增红拦
6. **事件通道收口**（3f D-01）：确认 5 结算点 bump_event 全接线 + event_log 环形 300
   满足 EV-01~09——不建第 10 表（避免双写冗余）；补 read_event_log 读取封装
7. **前端最小可用壳**（editor.html 单文件零依赖）：登录 → 侧边栏页清单 → 列表搜索
   分页 → /api/meta 驱动表单 → 保存（409 冲突提示）/删除；G0 合规（web 静态资源）

## 三、验收

- 全仓 pytest：5810 passed / 9 failed（9 个均为既有基线失败，M12 全程零新增；
  另修复 1 个基线过期测试 test_gm_prefix_required_wiring——免前缀裁决后断言更新）
- ruff/mypy 全绿；verify_m12 exit 0（7/7 脚本断言）
- 实机冒烟（TestClient 真 test_demo 包）：首页 200 / 设密登录 / 页清单 6 页 /
  怪物列表真数据 / meta 26 字段 / skill+quest 列表 全通

## 四、DELAYED 诚实登记（后续迭代）

| 项 | 说明 |
|---|---|
| GM G5 调试 / G6 测试 / G7 广播 / G9 玩家查询 / G11 解封 | 5b 14 条剩 5 条未接线（无常量无处理器） |
| 扩展页六页外 CRUD（npc/checkin/ai 等页） | pages_crud PAGE_MODULE 硬编码六页（已注释登记，归后续） |
| 前端完整交互（P-01~P-08 固定视口/抽屉/滑条等 7 类控件） | 4B 只做最小壳（登录/列表/meta 表单/保存） |
| 前端日历预览断签模拟 / NPC 10 标签渲染 | 字段已注入，UI 后续迭代 |
| /商店 列表 SM-04 余额行 / SM-06 紧凑格式 | 已有 SM-01~05 主链 |
| 存档导出 export_archive 后端 | G4 处理器已接，后端方法归批 5 装配注入 |
| /api/packs/active 切换 | 端点占位，装配层批 5 注入 watcher 后生效 |
| editor.json 12 页扩展登记 | test_demo editor.json 已建但 pages_crud 只认六页（同上） |

## 五、踩坑与决策（供后续参考）

1. **子 agent 50 轮上限半交付普遍**：4 路子 agent 全撞上限只勘察不写码（蓝图质量
   高），主 agent 按蓝图收口——M12 实际主 agent 写码占比高
2. **load_pack 第一参是目录非包名**（冒烟脚本踩坑）；Pack.registry 才是 Registry
3. **revoke_gm P0**：旧实现把被撤者重插成 owner（升级机主）——改整行删除
4. **quest unlock_chain 实为 str 非 list**（蓝图勘察误判，实测修正）
5. **event_log 已满足 EV 契约**：不建第 10 表（双写冗余违背用户铁律）
6. **GM 免前缀裁决**：DEFAULT_PREFIX_REQUIRED 清空（068bcd4）——gm_requires_prefix
   返回 GM_COMMANDS 是旧语义残留，既有测试已更新断言
7. **fastapi 运行时依赖**：requirements 加 fastapi/uvicorn（摸底 L357 计划内），
   惰性 import 保持（缺依赖核心层可导入）
8. **ruff F401/E501 门禁严格**：函数内 import 未用/行长都会被拦（GmBackend 内
   import os/time/gm_constants 清理）
