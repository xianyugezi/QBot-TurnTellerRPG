# M12.5 编辑器层硬编码审计清单（只读 · 2026-09-03）

类型：死映射=写死表/dict；写死键=硬编码键名；枚举写死=写死枚举；内存态=运行态数据；结构缺口=模型缺字段/空表。落点：内容包级（页面/字段/校验随包）→editor.json；全局/部署语义→settings.json。共 **36 点**：pages_crud 13、api.py 8、editor.html 7、editor_registry 5、field_meta 3。

## 1. qbot_rpg/web/pages_crud.py（697 行）13 点

| # | 位置 | 类型 | 现状 | 建议(落点) | 消费方风险 |
|---|---|---|---|---|---|
| 1 | L64-75 | 死映射 | PAGE_MODULE 六页写死 skill→skills/job→jobs/monster→enemies/map→maps/quest→quest(单数笔误)/shop→shop；注释自认批4应换 registry | registry 页表驱动；缺 editor.json 降六页兜底(editor.json) | 六页外全 404；B 类 npc/checkin 已登记页 CRUD 不可用 |
| 2 | L684 | 死映射 | PAGE_FILE={p:f"{m}.json"} 派生自 PAGE_MODULE | 随 #1 消除，文件名取页表 | 写盘层/测试引用需同步 |
| 3 | L82-89 | 死映射 | PAGE_ID_PREFIX 前缀=page_id 同名写死 | 页条目增 id_prefix 字段，缺省回退(editor.json) | ID 风格被 TC-01 钉死 |
| 4 | L92-94 | 死映射 | PAGE_ID_FIELD 全页="id" | 保留（主键恒 id） | 低 |
| 5 | L327-361 | 写死键 | _next_id 取常量前缀/模块；重扫上限 100 | 前缀随 #3；上限可配置(settings.json) | 扩展页自动 ID 脱节→重号/404 |
| 6 | L348/364-378 | 内存态 | 版本簿挂 ctx 内存键 `_page_versions`（state 长活）；注释自登记进程重启/换包即丢 | SV-08 持久化开关(settings.json)；ctx 须长活注入 | **最大风险**：重启/多进程版本归零→409 编辑锁失效、双人覆盖 |
| 7 | L139-146 | 死映射 | _ref_display 内嵌 kind→module 11 键 | 改 field_meta ref_target/模块表驱动 | 新 kind 中文名解析不到→回退原 id |
| 8 | L278-319 | 枚举写死 | 引用收集 11 组(字段,kind) + 嵌套容器页名特判 map/monster/shop/quest（monsters[]/items[]/reward[]） | 收集规则入字段元数据 ref_target+容器键(editor.json) | 新页引用芯片缺失、dungeon 等不覆盖 |
| 9 | L486-558 | 枚举写死 | 级联删除按页名分支：monster→maps.monsters/gate_guard+quest/npc conditions[].param 字符串 JSON 探针(L506-517)；map→exits/respawn/dungeon.maps；job→skills.job_restrict；shop/quest 空分支 | 级联规则表 from_page×ref_field×动作 入页条目(editor.json)；param 探针改结构化遍历 | **高**：扩展页删除零级联→悬空引用；字符串探针子串误伤+dumps 性能 |
| 10 | L569-573 | 写死键 | 注释承认删物品/技能级联未实现（物品非六页） | 随 #9 规则表覆盖 | 删 item/skill 悬空引用→重载红拦 |
| 11 | L604-624 | 枚举写死 | validate 数值键 16 个+引用键 8 组+min/max 配对写死 | 按 /api/meta 字段表驱动泛型校验 | 未列键零校验；新模块须改代码 |
| 12 | L629-648 | 死映射 | _ref_exists 第二份 kind→module 9 键（与 #7 漂移） | 与 #7 合并单源 | 校验口径分裂 |
| 13 | L220/262/387/415/470/593/665 | 写死键 | 7 处 `page not in PAGE_MODULE` 六页白名单守卫 | 改查页表（停用页→404） | 新页漏放行即 404 |

## 2. qbot_rpg/web/api.py（467 行）8 点

| # | 位置 | 类型 | 现状 | 建议(落点) | 消费方风险 |
|---|---|---|---|---|---|
| 14 | L98-106 | 内存态 | _ctx_of 版本簿挂 state 属性（单进程可用） | 同 #6(settings.json) | 多进程/重启丢锁 |
| 15 | L125 | 写死键 | _save_pipeline 查 pages_crud.PAGE_MODULE | 改 registry 页表 | B/C/D/E/F 全 404/写错文件 |
| 16 | L251-253 | 死映射 | meta_page 查 state.editor meta_source（registry 驱动，好）后再写死 {skill:skills,…,checkin:checkin} 8 键 dict | 删 dict：meta_source 命名空间→field_meta 模块键全量解析；页表增 module_key | **新模块漏改即 404/空表单**（m125 点名） |
| 17 | L276-282 | 死映射 | /api/refs/{target} kind→module 12 键 dict | registry/页表驱动全量 kind 别名 | 候选下拉空、与 pages_crud 别名不同步 |
| 18 | L301-309 | 写死键 | /api/editor/pages 手挑 9 键——漏 validator | EditorPage 序列化白名单(含 validator) | 前端拿不到 validator |
| 19 | L313-409 | 写死键 | CRUD 端点已泛化但被 pages_crud 六页守卫锁死；validate 未接 validator 钩子分派 | 守卫换页表；validate 接注册表钩子 | B 类登记页点开报错 |
| 20 | L380-390 | 写死键 | delete 仅把 cascades 出现模块一并原子写盘 | 级联规则入 editor.json 后 cascades 全量 | 缺模块→悬空残留 |
| 21 | L168 | 写死路径 | / 壳路径写死 static/editor.html | 可留（随包结构） | 低 |

## 3. qbot_rpg/web/static/editor.html（617 行）7 点

| # | 位置 | 类型 | 现状 | 建议(落点) | 消费方风险 |
|---|---|---|---|---|---|
| 22 | L327-341 | 写死文案 | 侧边栏分组标签写死「主页面/扩展页」(enabled 二分) | group 字段入页条目(editor.json) | npc/checkin 等全堆主页面 |
| 23 | L461-489 | 写死键 | 控件映射写死：enum→下拉、list&obj→JSON textarea、number/bool 特判、else text；obj 无 children 渲染 | 按 field_meta type/children/ref_target 驱动 | C/D/E/F 深结构只能原始 JSON |
| 24 | L502-506 | 写死行为 | ref_target 仅渲染提示文本，未调 /api/refs/{target}（端点零使用） | 接 refs 动态候选 | 引用 id 手填易错 |
| 25 | L449-452 | 写死文案 | 空字段表提示「field_meta 未注入」 | 前端保留，C 类空表注入后消除 | C/E 页仅原始 JSON 可编 |
| 26 | L510-548 | 写死键 | 原始 JSON 区 `__raw__` 数据集键写死 | 可留（高级编辑兜底） | 低 |
| 27 | L571 | 写死键 | 保存读 state.cur._version，后端返回体未含 version→恒 0 | 后端补 version 返回(update 已有计数) | 前端版本冲突保护缺失(配 #6) |
| 28 | L312-366 | 结构缺口 | 页面隐含 list 条目 CRUD；map/obj 型页无渲染分支 | entry_type 分支(map=键值/object=单对象表单)，page_kind 入 editor.json | stats/formula/settings/fishing/forge 形态错乱 |

## 4. qbot_rpg/content/editor_registry.py（352 行）5 点 — 能否驱动 pages_crud？

EditorPage(L133-157)：page_id/title/icon/module_file/meta_source/tabs/enabled/extends/validator；EditorRegistry(L160-212)：pages_by_id/get_page/enabled_pages/page_for_module(extends 宿主解析)/validator_names/has_validator；load(L262-296)读 modules_raw["editor"]，缺失→六页兜底(L59-121)；validate_validators(L303-339,known 外注)。

| # | 位置 | 类型 | 现状 | 建议(落点) | 消费方风险 |
|---|---|---|---|---|---|
| 29 | L59-121 | 死映射 | 六页兜底与 PAGE_MODULE 内容重复 | 保留兜底，与 field_meta/loader 映射同源 | 两处漂移→页/模块对不上 |
| 30 | L123-127 | 死映射 | DEFAULT_VALIDATORS 六钩子名——全仓零消费；api.py 未接线钩子分派 | validate 端点接钩子(名→函数表)+known 注入 | **validator 字段是死数据**：13 页登记名全不生效 |
| 31 | L133-157 | 结构缺口 | 无 id_prefix/group/page_kind/module_key；meta_source 自由串与 field_meta 键无强关联 | 扩字段(#3/#16/#22 落点)；meta_source 收口解析 | 模型不扩则驱动改造无落点 |
| 32 | L185-202 | — | page_for_module 按 module_file 取代表页(宿主优先) | 保留 | 低 |
| 33 | L262-296 | 内存态 | 每次调用重建页表(读 modules_raw)；写盘后重调才新 | 保留；写盘后刷新 | 页表与磁盘短暂脱节 |

**判定：能驱动。** page_id→module_file 覆盖 #1/#15；enabled/extends 支撑守卫 #13 与聚合；缺 editor.json 六页兜底保兼容。前置：① pages_crud 注入页表解析器而非 import 常量(state.registry 透传)；② EditorPage 扩 id_prefix/group/page_kind/module_key(#31)；③ validator 接线(#30)；④ tests/unit/test_pages_crud.py 大量引用 PAGE_MODULE→常量降兜底或测试改注入。

## 5. qbot_rpg/content/field_meta.py _module_table（L627-1074，实测 32 模块）3 点

字段数（只读 import 实测）非空：skills 25/jobs 11/enemies 26/maps 12/quest 17/shop 15/npc 14/checkin 7/ai 6/hidden 3/env_event 1/log_card 1/action 25/items 19/equipment 11/effects 12/statuses 10/marks 11/traits 12/recipe 18/proficiency 9/skill_chains 6/slots 2/manifest 5/settings 8/conditional 1。**空表 6 个**：C 类 dungeon(L1043)/achievements(L1055)/fishing(L1038 obj)/forge(L1034 obj) 专项全权防误拦；E 类 stats(L1044 map)/formula(L1020 map)。

| # | 位置 | 类型 | 现状 | 建议(落点) | 消费方风险 |
|---|---|---|---|---|---|
| 34 | L997-1074 | 死映射 | 模块登记=代码级 dict（schema 之家架构） | 维持；meta_source→本表键映射收口(#16/#31) | map/obj 形态缺失→C/E 无法表单化 |
| 35 | 空表 6 模块 | 结构缺口 | 防误拦 vs 可编辑矛盾 | m125§2.2 宽松注入(id required+宽 obj+soft_label)+page_kind；test_demo 把关零新增红拦 | 注入不当→加载红拦；不注入→空表单 |
| 36 | L45-72 | 死映射 | NAMESPACES 17 组写死 | 维持(校验语义层)，与 loader 断言对齐 | 新注册表漏登记→跨库 ID 唯一性失效 |

## 6. content/test_demo/editor.json（12 页登记）

L4-15：六页+npc/checkin/ai(extends=monster 共享 enemies.json)/hidden(maps.json)/env_event/log_card(settings.json)，含 tabs/validator；缺 id_prefix/group/page_kind(同 #31)。**缺口**：12 页全 enabled 但 pages_crud 只认六页→后 6 页 CRUD 全 404(B 类)；C/D/E/F 13+ 模块(dungeon/achievements/fishing/forge/stats/formula/items/equipment/action/effects/statuses/marks/traits/recipe/proficiency/skill_chains/slots/settings)无页登记；无「editor.json 缺失」用例→六页兜底未在真实包验证。

## 汇总

- 点数 **36**（死映射 14、写死键 12、枚举写死 2、内存态 2、写死文案/行为/结构 6）。
- **最大风险**：#6/#14 版本簿内存态（重启丢 409 编辑锁）；#9/#10 级联删除按页名写死（扩展页零级联→悬空引用）；次之 #16 meta dict（漏改即 404）。
- 主线（对齐 m125_启动包 §2.1）：registry 页表驱动替换 PAGE_MODULE/PAGE_FILE/PAGE_ID_PREFIX → EditorPage 扩 id_prefix/group/page_kind/module_key → 删 api.py meta/refs 写死 dict → C/E 空表宽松注入+前端 entry_type 分支 → validator 钩子接线。
