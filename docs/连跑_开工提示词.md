# 开工提示词（新对话第一条消息 · 连跑全自动版）

按《系统实施规划_指令缺口补全.md》实施指令缺口补全——**连跑模式：批 1 → 批 2 → 批 3 全自动顺序执行，每批完成只简短汇报进度不停下等确认，全部完成后再统一详细汇报**。用户出门中，全程无人值守。

## 背景
- 仓库：/root/QBot-TurnTellerRPG（git main，HEAD=f4071f8）
- 规划文档：docs/系统实施规划_指令缺口补全.md（已审核通过——六要素：指令/指令功能/变量可配置项/变量功能/事件/事件功能）——先完整读它
- 契约文档（实施依据，逐系统读）：
  - 强化：docs/细化/细化_2c3b_强化流程契约.md + 细化_2c3a_强化数值曲线.md
  - 木桩：RPG回合制框架设计文档.md L1280 + 怪物模块 §十五 + 细化_1g1a
  - 查看目标：定稿 7.6 + 细化_4f §2.6（战斗会话数据细化_1g1c）
  - 种植/委托/品评：细化_2c5c_种植品评代工.md + 细化_2c5b_委托板与声望.md
  - 赠送：定稿 L1286/L953 + 细化_4f B5
- 环境：仓库测试一律用 .venv/bin/python（Python 3.14，pytest/ruff/mypy 已配）；模拟器验证：
  QBotRPG_DB_PATH=/tmp/xxx.db .venv/bin/python scripts/qq_group_sim.py --file cmds.txt 2>/dev/null（每次先 rm DB）

## 分批与三路并行
- **批 1**：
  - 路 1：装备强化系统（强化/强化信息/强化保护/强化转移）
  - 路 2：木桩训练（木桩/调整木桩）
  - 路 3：查看目标（战斗中只读目标面板）
- **批 2**：
  - 路 1：赠送（交易引擎——玩家寻址 + 原子转移；勘察确认无现成引擎需新建）
  - 路 2：种植核心（温室/种植/收获——crops 表 + 生长计时）
  - 路 3：委托板 + 声望
- **批 3**：
  - 路 1：雇工/收取（代工助手后台产出）
  - 路 2：投稿 + 排行榜（品评会周赛）

每批：为每路建 git worktree（/tmp/dev_enhance、/tmp/dev_dummy、/tmp/dev_target 等），派 3 个子 agent 并行开发。给每路子 agent 的规格必须包含：改动文件清单方向、数据源/现成 helper 位置（先勘察好写进规格）、验收命令、已知坑（参考本仓 git log 历史里的坑：ctx 装配缺表、模板白名单双逗号、ALL_REGISTERED 锚点、router_setup stub 移除、白名单缺词静默等）。

## 硬性规则（用户铁律）
- 内容包纯配置：veinborn 数据只在 content/veinborn/*.json 改；框架机制代码在 qbot_rpg/
- 模板配置化：所有玩家可见文案走 tpl_of + 对应模板文件（enhance_tpl.py 等新建）+ PLACEHOLDER_WHITELIST 登记（注意别写双逗号）
- 新指令名必须同步：parsers.DEFAULT_WHITELIST、router_setup 对应 stub 移除（强化/调合 的 stub；种植 stub 转真）、tests/unit/test_assembly_router.py 的 ALL_REGISTERED、帮助组动态生成（注册即出现）
- 零 emoji；禁止裸静默（任何指令都有回执）
- 指令形态以规划文档/契约为准，不发明新指令
- 全仓 pytest 基线 10 failed（g0/battle_render×2/codex/e2e_m4×4/emoji/investigate）——不得新增红
- ruff/mypy 只查自己改的文件（context.py E402/E501、battle_reward.py mypy 是预存不算）
- commit 前隐私扫描（ghp_|sk-[A-Za-z0-9]{10}|BEGIN (RSA|OPENSSH|PRIVATE) 零命中）才提交
- commit 分路提交（feat(enhance) 等），每批收口后 merge 进 main + 重启服务（systemctl restart nonebot2.service）+ 隐私扫描

## 子 agent 管理（关键教训）
- 子 agent（deepseek-v4-flash）常撞 50 调用上限只勘察不写码、或自报完成但实际没落盘——**规格里明确要求先写码后验证**，且**主代理必须亲自复验每个子 agent 结果**：查 git status 确认改动真实存在 + 模拟器实测 + 相关测试；子 agent 报告与 worktree 实际状态不符时以实际为准，主代理直接补实现
- 每路完成标准：worktree 有真实改动 + 契约测试用例过 + 模拟器实测通 + 该路相关测试全绿

## 每路验收门禁
- 对应契约的验收用例全过（新建测试，2c3b/2c3a 有 TC-01~25 可直接落地）
- 模拟器实测：注册 阿伟 脊剑士 → 走通该路全部指令（强化需 veinborn 新建最小 enhance.json 并过 build_pack 零红拦；种植需 profession.json 最小配置）
- 全仓 pytest 无新增红

## 汇报节奏
- 每批收口：简短进度汇报（改了什么、测试状态），不停顿直接进下一批
- 全部三批完成：统一详细汇报（每系统：改动文件框架/内容包分层 + 模拟器实测 + 测试结果 + commit 列表），等用户回来后审核
