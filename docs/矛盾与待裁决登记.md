# 矛盾与待裁决登记（编辑器框架）

> **来源**：用户 2026-09-16 拍板落地 CakeGame《表写入规范》可借鉴项 **E**——「文档矛盾登记（⚠️ 只登记不裁决）」正式化；
> 归入本仓既有散落项（`docs/能力缺漏审计_四方对照.md` §五 U1–U10、台账「待确认」区、各文档「待确认 / 待裁决 / 存疑 / 矛盾」标记）。
> **互链**：`docs/编辑器修改意见0915_台账与方案.md` §十三（批次入口）· `docs/能力缺漏审计_四方对照.md` §五（未确认区）。
>
> **纪律（写死，本表唯一规则）**：
> 1. **只登记，不裁决**——本表不替用户拍板；未裁决项一律保留双方证据。
> 2. 每条含三段：**编号 / 一句话争点 / 双方证据（文件:行 或 文档行号）** + **状态**。
> 3. 状态取值只三档：**待实测**（含「待排期 / 待专批 / 待收敛」等实测性质）｜**待用户裁决**｜**已裁决-日期 + 结论与依据**。
> 4. 已裁决项**必须**写清裁决日期与依据（拍板记录 / commit / 契约文档行号）；文档间的矛盾**不自行压平**。
> 5. 新矛盾**追加**，旧条不删——只把状态从「待…」改为「已裁决-…」。

---

## 〇、采集口径与来源清单

**grep 口径**（本仓自有文档，排除冻结策划书与历史审计报告）：

```
grep -rn "待确认|待裁决|存疑|矛盾|待定|待收敛|未定案|未登记待确认" docs/ --include=*.md
  # 排除 docs/审查参考/（冻结策划书原件）与 docs/审查/幻觉审查*（历史审计报告）
```

原始命中 349 行；按「是否构成编辑器/框架的**独立**未决项」去重收敛为下表 **24 条**（其余命中多为 M0–M13 历史里程碑报告的「未实现」状态标签，不构成独立争点）。

| 来源文件 | 条目 |
|---|---|
| `docs/能力缺漏审计_四方对照.md` §五（:312-325） | U1–U10 |
| `docs/编辑器修改意见0915_台账与方案.md`（§四 :100-108、§十一 :296-329） | U1–U7 裁决依据；X1 / X3 / X4 / X5 |
| `docs/编辑器重写_需求与约束.md` | U5 / U6 边界；X4 / X5 契约 |
| `docs/消息模板重构/02_遗留登记.md` | X6 / X7 / X8 / X9 |
| `docs/m12_UX架构方案.md:467`（原 :460，本批顶部加说明后下移） | X10 |
| `docs/指令清单_全量.md:19` | X11 |
| `docs/审查/幻觉审查_1a.md:18` | X14（唯一保留的历史审计独立未决项） |
| `qbot_rpg/**`（代码实测） | U1 / U2 / U7 / X2 / X3 的实现事实 |

---

## 一、登记表

### A · 已裁决（有用户拍板记录 / 契约 / commit）

| 编号 | 一句话争点 | 双方证据 | 状态 |
|---|---|---|---|
| U1 | **采集 / 挖掘**在编辑器里是独立模块、独立生活技能，还是 `maps` 子段？ | 策划书：`docs/能力缺漏审计_四方对照.md:316`「总纲:387 把采集作为 maps 字段；总纲:1285 是独立 `/采集` 指令」；实现：同卷「`forge_deadlock.py:91` 归口名『采集/挖掘』；`explore_commands.py:6-7` 说『采集点在地图/副本探索流程内处理』」 | **已裁决-2026-09-15**：数据落点 = `maps.gather_points`（补 `name/weather_mods` 登记）+ 目录条目 `gathering`（标「未实现」）；引擎部分另列待排期。依据 `docs/编辑器修改意见0915_台账与方案.md:102`；代码 `qbot_rpg/content/module_catalog.py:121-122`（`implemented=False`） |
| U2 | **时间天气**编辑器归属：挂「通用设置」卡片，还是独立一级模块？ | `docs/能力缺漏审计_四方对照.md:317`「时间天气定稿:296 明写『通用设置 → 【时间天气】卡片』；但同定稿:407 登记要求落在 `settings.json time_cycle` 段」 | **已裁决-2026-09-15**：挂「通用设置 → 【时间天气】卡片」，**不**新增一级模块。依据 `docs/编辑器修改意见0915_台账与方案.md:103`；`module_catalog.py` 无 `time_cycle` 一级条目；`settings.time_cycle` 已登记 |
| U5 | `profession.json` 分层冲突如何收敛？ | `docs/能力缺漏审计_四方对照.md:320`「策划书：总纲:400 要求 `profession.json` 独立文件…实现：拆进 `settings.alchemy.farming / quest_board / contest / assistant / fishing / fishing.json`」 | **已裁决-2026-09-15**：用「生活」虚拟聚合视图 `life` 收敛，**不改策划书**。依据 `docs/编辑器修改意见0915_台账与方案.md:105`；契约 `docs/编辑器重写_需求与约束.md:181-183` |
| U6 | `message_prefix` 与 `templates` 职责边界，是否合并为「消息」模块？ | `docs/能力缺漏审计_四方对照.md:321`「消息前缀定稿管『回复首行身份标识』；`templates.json` 管全部消息模板文案，二者都落在『首行渲染』」 | **已裁决-2026-09-15**：**不合并**（前缀管首行外壳、templates 管正文文案）。依据 `docs/编辑器修改意见0915_台账与方案.md:106`；边界 `docs/编辑器重写_需求与约束.md:201-203` |
| U7 | **怪物触发类型枚举双源冲突**（13 类命名不一致），以哪份为准？ | `docs/能力缺漏审计_四方对照.md:322`「怪物模块定稿:298（`phase_below/broken/revive/enter_phase…`）vs 怪物行动AI定稿:63-75（`hp_below/pv_broken/…`）命名不一致」 | **已裁决-2026-09-15**：以**校验器现行枚举为准**（实现即事实）；另一套命名登记为「待收敛项」，不擅自改校验。依据 `docs/编辑器修改意见0915_台账与方案.md:107`；代码 `qbot_rpg/content/validator.py:219-224`（权威 15 类）、`:226-231`（别名归一）、`:232-235`（legacy） |
| X1 | `#9 公式未登记键`：`formula` 是否仍有「已消费但未登记」的标准键？ | 台账 `docs/编辑器修改意见0915_台账与方案.md:305`（批19 段C 按 `core/formula_loader.py` 实际消费面登记）；同卷 `:324-325` 逐字「**未登记待确认**：无（`formula_engine` 本身只做表达式求值、不按名消费键…）」 | **已裁决-2026-09-16（批19）**：清单闭合，状态 =「未登记待确认：无」。依据 `docs/编辑器修改意见0915_台账与方案.md:324-325` |
| X3 | **`slots` 模块 ↔ `settings.slot_defs` 功能重叠**：编辑器归口哪处、如何提示？ | 声明：`qbot_rpg/content/module_catalog.py:85-96`（`slots` 的 `overlap_with="settings.slot_defs"` + `overlap_note`）；实测：`content/veinborn/slots.json=[]`、`settings.json slot_defs` 8 部位；提示实现 `qbot_rpg/web/api.py:765-788` `_module_overlap_hints()` | **已裁决-2026-09-16（批19 #5）**：黄提示、不硬拦（声明驱动）。依据 `docs/编辑器修改意见0915_台账与方案.md:307`；契约 `docs/编辑器重写_需求与约束.md:335-336` |
| X4 | 生活系统归口：挂 `jobs` 父模块下，还是独立「生活」？ | 临时口径 `docs/编辑器修改意见0915_台账与方案.md:40`「挂到包内既有父模块「职业」`jobs` 下」；定案同卷 `:88`「用 `entry_merge` 虚拟聚合视图 `life`…批12 挂 `jobs` 下的临时做法已取消」 | **已裁决-2026-09-15**：`life` 虚拟聚合视图，不改策划书、不新增数据文件。依据 `docs/编辑器修改意见0915_台账与方案.md:88`；契约 `docs/编辑器重写_需求与约束.md:181-183` |
| X5 | 台账批4 表头「待确认」三条（#7 中文 ID 规则 / #10 概率覆盖 / #11 预设归属） | `docs/编辑器修改意见0915_台账与方案.md:61-63`（三处「待确认」原文） | **已裁决-2026-09-15**：#7 默认「前缀+序号」、拼音可选附加；#10「使用处 > 定义处 > 缺省」；#11 框架默认六预设 ∪ 包声明。依据同卷 `:83-87` 销项 + 契约 `docs/编辑器重写_需求与约束.md:207-262` |
| U3 | `覆盖审计_F` 缺失-1（锻造防具套装 P1）是否仍成立？ | `docs/能力缺漏审计_四方对照.md:318`「F 基线 2026-08-18 称『2c2d 不存在』；实测 `docs/细化/细化_2c2d_锻造套装与客制.md` **已存在**」；同卷 `:414`「该判据在 2026-09-15 不再成立…只代表承接文档已补」 | **已裁决-已失效（基线过期）2026-09-16（批30）**：依据 `docs/细化/细化_2c2d_锻造套装与客制.md` **已存在**（批30 实测 `ls docs/细化/细化_2c2d_锻造套装与客制.md`）；F 基线「2c2d 不存在」判据失效，不代表套装/客制已实现。台账 `docs/编辑器修改意见0915_台账与方案.md:104`「基线过期项」至此销项 |
| U4 | 伤害构成统计 / `dummy_log` 是否已实现？ | 战斗数值层定稿 §八（`docs/审查参考/战斗数值层设计定稿.md:319-342` per-action schema / `:353-360` dummy_log / `:362-372` `formula.stats_collector` 落点）；实现实测（批30） | **已裁决-已有（部分）2026-09-16（批30）**：①§8.1 per-action 收集器**已实现**——`qbot_rpg/core/battle.py:1024` 每段伤害写 `_snap["stats_collector"]["per_action"][]`，字段 source/seg/ch_phys/ch_elem/crit/blocked/pierce/weak_type/weak_elem/final **10/11**（缺 `penetrate`），且 `seg` 实取全局行动序号 `len(action_record)`（**非**定稿「连段段数」语义）；快照初始化 `:2218`；`qbot_rpg/data/battle.py:15` 有声明。②**未实现**：§8.1 按来源聚合/占比、§8.2 木桩战报展示、§8.3 `dummy_log` 环形缓冲与 `/木桩 记录` 查询、§8.5 校验器/编辑器（`stats_collector` 仅登记配置元数据 `field_meta.py:1004-1007`，全仓无消费点；`dummy_log` 全仓仅注释/文档，无生产读写）。结论：核心采集骨架已在，聚合/展示/存档层缓做 |
| X8 | 炼金两处换行 / 去头口径存疑 2 项 | `docs/消息模板重构/02_遗留登记.md:38`；规范 `docs/消息模板重构/00_方案与规范_v1.md:39-55`；实测（批30） | **已裁决-实测无误 2026-09-16（批30）**：与规范逐条比对——①`alchemy_feed_chain_effect` = `效果等级 {effect_level}`（`template_table.json:305`）：去掉与上一行 `alchemy_feed_chain`「连锁 {segments} 段」（`:300`）重复的头部，实测渲染 `火+12\n连锁 3 段\n效果等级 2`（连锁段数不丢，占位符 `segments` 仍由 `alchemy_commands.py:735-737` 传入且上一行已渲染），行长 **23 半角 ≤ 预算 28**，符合规范「拆行优先序③冗余词精简」（`:46`）与「结构化行 ≤ B」（`:40`）；②`alchemy_inherit_gold_slot` = `\n第 4 位金色：{name}`（`:323`）：前导换行为承接 `alchemy_inherit_slot_used`「特性位 {normal_used} 普通」的**有意换行**，实测渲染 `特性位 2 普通\n第 4 位金色：灼烧强化·精`（对齐 `alchemy_commands.py:1226-1231` 面板注释），行长 **19 半角 ≤ 28**，符合规范「少 ｜ 多换行」（`:43-44`）；全表同类前导换行键 12 个，`scripts/check_template_width.py` **FAIL 0**。两处**不改** |
| X10 | `docs/m12_UX架构方案.md` 附录 B 的 9 项【待定】是否仍成立 / 整档是否作废？ | `docs/m12_UX架构方案.md:467-477`（附录 B 九条；本批顶部加说明后由 :460 下移）；取代判定证据（批30 核查） | **已裁决-整档作废 2026-09-16（批30）**：判定=**已被取代**。依据：① m12 援引的权威契约 `docs/细化/细化_5a_编辑器契约.md`、`docs/细化/细化_5a2_编辑器扩展页.md` 与 `docs/m12_编辑器摸底.md` **已不存在**（2026-09-13 随旧编辑器删除，`docs/消息模板重构/02_遗留登记.md:237` #86）；② 重写版编辑器以 `docs/编辑器重写_需求与约束.md`（需求侧唯一来源，`:3`）+ `docs/编辑器重写_实现方案.md`（实施侧唯一来源，`:5`）为准，UX/API/`editor.json`/FieldMeta 机制全部重定义；③ 唯一横切项 GM 指令权限由仍在仓的 `docs/细化/细化_5b_GM指令契约.md` 承载，非 m12 独有。动作：m12 顶部加醒目取代说明（2026-09-16），附录 B 九项【待定】随之作废（git 历史可追溯） |
| X14 | 怪物防御率：定稿有因子但全文无定义 | `docs/审查/幻觉审查_1a.md:18`（历史审计，对象=归档 v1.1.1 375 行版）；本仓 `docs/审查参考/战斗数值层设计定稿.md` 实测（批30） | **已裁决-不清理（登记前提不成立）2026-09-16（批30）**：批30 实测本仓定稿 **L42 已有完整定义**「怪物防御率配置化（monster_def_rate · R-09 回写）：…每怪物可配字段…默认 1.0…R-09 拍板（2026-08-18）」；L29/L34 两处因子均指向该定义（幻觉审查所指 `L27/L32` 系归档 375 行版行号，本仓 383 行版已补 R-09 段，行号平移 +2）。因子另有实现消费（`core/damage.py:530-584` 乘入双通道、`core/formula_loader.py:164` 装配、`core/battle.py:4561` 取目标 combatant）与编辑器登记（`field_meta.py:1047`）。故**不删**：删除已定义且已消费的因子＝擅自改设计；奥卡姆只清「未定义/空壳」，本项不属。**未从定稿删任何文本**（只删不补、补定义属设计权，均无必要） |
| X9 | GM「编辑」指令：链接源 `settings.editor_url` 已清空，指令删除 / 改造？ | `docs/消息模板重构/02_遗留登记.md:244`（#87，原文曾引 `:238`/ #86 上下文）「G13 `/编辑` 只输出『编辑器链接 + 权限级提示』…链接源 `settings.editor_url` 已随之清空…本批未改行为（GM 指令清单属 5b 契约）」；批30 删除清单见下 | **已裁决-已删除（2026-09-16 · 批30 · 主 agent 依用户授权）**：按奥卡姆删除空壳。**删除清单**：常量 `gm_constants.py`（`__all__`:20 / `GM_CMD_EDIT`:35 / `GM_COMMANDS`:45 / `GM_COMMAND_INDEX`:54）；接线 `gm_commands.py`（`__all__`:130 / 导出:141 / import:164 / `GM_COMMAND_LEVEL`:198 / `GmBackend.editor_link`:596-608 / `cmd_gm_edit`:827-841 / `_HANDLERS`:1001 / docstring 多处）；白名单 `parsers.py:180,207`；权限集 `permission_store.py:76,84`；帮助组 `basic_commands.py:387`；测试 5 文件（含两条 G13 测试改为 `test_gm_edit_removed`）＋新增回归 `tests/unit/test_batch30_gm_edit_removed.py`；脚本 `verify_m4.py:97` / `verify_m12.py:61`（→DELAYED）/ `gen_command_list.py:76`；文档 `指令清单_全量.md:184`（→设计已删）/ `细化_5b_GM指令契约.md`（**契约条目 G13 已删除**，顶部契约变更 2026-09-16）/ `02_遗留登记.md` #87。**5b 契约相应条目已删除（2026-09-16）** |
| U8 | 商店字段新旧混用：编辑器下拉应出 `scope/limit/period` 还是 `per_player`？ | 商店定稿 `docs/审查参考/商店系统设计定稿.md:173-177`（字段表）/`:183`（旧字段自动映射）；实现 `qbot_rpg/content/field_meta.py`（编辑器）/ `content/shop_models.py`（校验）/ `core/shop.py:536-549`（引擎兼容读）；批30 实测 | **已裁决-已收敛（定稿键）2026-09-16（批30）**：**写入用定稿键**——`SHOP_ITEM_ENTRY_CHILDREN` 收敛为定稿 12 字段，下拉枚举与 `shop_models.SCOPES/LIMIT_PERIODS/REFRESH_MODES/SHOP_TYPES` 同源（修正旧元数据：`period` obj「上架时段」→ str day/week/month；`refresh.mode` manual/fixed → once/none；`type` general/black/quest → npc/blackmarket；`price_fluctuation` obj → int）。**兼容读取**：旧 `per_player`→limit、`per_player_period`→period（`core/shop.py:536-549`，不静默丢弃；校验器不红拦只 Y-6 黄提示迁移）。**内容包**：`content/*/shop.json` 实测无旧键（无需数据迁移，`veinborn` 亦无）；包展示名 `content/{veinborn,test_demo}/field_meta.json` 同步。回归 `tests/unit/test_batch30_shop_field_convergence.py`；字段元数据对拍按门禁语义重定基线（本批有意改 label/help、删 `interval`，理由见基线脚本注释） |
| X15 | §6.12-01 的「**版本快照**」部分（每 10 分钟 / 每次发布自动存一版、回退「30 分钟前的版本」）是否立项？ | 策划书：`docs/审查参考/RPG回合制框架设计文档.md:998`「版本快照：每 10 分钟 / 每次发布自动存一版，可回退『30 分钟前的版本』」；我方：`docs/编辑器重写_实现方案.md:733`「不做富文本编辑、不做多人协同、**不做版本 diff 视图**」；已有**单级备份回退** `qbot_rpg/web/editor_ops.py:1220 module_backup` / `:1229 rollback_module` + `.bak`（`qbot_rpg/content/atomic_store.py:380 backup_status` / `:402 backup_modules` / `:444 restore_modules_from_backup`）；`/root/deliverables/U9_编辑器进阶能力_逐条核对.md` §四 D4 | **已裁决-不做（2026-09-17 · 批33）**：①「版本 diff 视图」已被《编辑器重写_实现方案》排除；②单级 `.bak` +「回退到上一份备份」已覆盖「救错」场景；③定时 / 发布自动多版本快照需常驻调度与快照存储，与既有「零 apscheduler · 懒清理」口径冲突且无强需求。**撤销/重做（草稿级）另由批33 落地**（`qbot_rpg/web/static/index.html` 的 `EDITOR_HISTORY_BEGIN/END` + 工具栏「撤销/重做」+ `Ctrl+Z` / `Ctrl+Shift+Z` / `Ctrl+Y`），只动内存草稿、不写盘；见台账 §二十二 |
| X16 | 总纲 §6.12-05「CSV 导入顺序与验证」与 §6.12-20「CSV 双向防公式注入」是否落地？ | 策划书：`docs/审查参考/RPG回合制框架设计文档.md:1023-1028`（导入保持行序：追加/插入 + 逐一引用验证）、`:1155`（`= + - @` 前缀单元格转义或拒绝）；缺口登记：`docs/能力缺漏审计_四方对照.md:252`（②-5）、`/root/deliverables/U9_编辑器进阶能力_逐条核对.md` §6.12-05 判③ / §6.12-20 判②；实现：`qbot_rpg/content/csv_codec.py`（纯编解码 + 注入中和）、`qbot_rpg/web/csv_ops.py`（行序 / 逐行引用 / 冲突 / 落盘）、`scripts/editor_host.py`（`…/export_csv`、`…/import_csv` 路由）、`qbot_rpg/web/static/index.html`（模块级「导出 CSV / 导入 CSV」） | **已裁决-已落地（2026-09-17 · 批34）**：①导出列头 = 模块框架元数据字段（未配置也出列），UTF-8 带 BOM，只读不写包；②导入保持 CSV 行序（追加到末尾 / 插入指定位置，`§6.12-05`）；引用型字段逐行校验，缺失 → 逐行报错（行号 + 字段 + 期望目标）且**默认整批拒绝**（零写入、零备份）；③冲突默认跳过并在报告列出，明确选「覆盖」才原地替换（不静默覆盖）；④`= + - @` 前缀文本单元格导出前置 `'`、导入裸危险文本中和后写包（§6.12-20；恶意样例回归 `tests/unit/test_batch34_csv_ops.py`）。边界如实登记：**单对象（object）模块不支持 CSV**（无「每行一条条目」概念，如实提示）；认不出的列忽略并给人话提示 |

### B · 待用户裁决（保留双方证据，不自行拍板）

| 编号 | 一句话争点 | 双方证据 | 状态 |
|---|---|---|---|
| U9 | 总纲 §6.12 编辑器进阶能力（L993-1198，约 200 行）是否需补专项逐条核对？ | `docs/能力缺漏审计_四方对照.md:324`「报告篇幅所限未逐条对照」 | **待用户裁决**：台账 `docs/编辑器修改意见0915_台账与方案.md:108` 保留待确认 |
| U10 | `enemies.active_time` 双份 schema：刷怪行字段还是怪物字段？ | `docs/能力缺漏审计_四方对照.md:325`「`field_meta.py:452` 在 `DUNGEON_MAP_ELEM_CHILDREN` 登记了 `active_time`，但 `maps` 模块实测不生效（见 B6）；`enemies` 模块无该字段」 | **待用户裁决**：台账 `docs/编辑器修改意见0915_台账与方案.md:108` 保留待确认 |

### C · 待实测（含待排期 / 待专批 / 待收敛）

| 编号 | 一句话争点 | 双方证据 | 状态 |
|---|---|---|---|
| X2 | **采集 / 挖掘引擎未实现**（独立 `/采集` 与「挖掘」通道均无引擎） | 策划书：`docs/能力缺漏审计_四方对照.md:248`「总纲:1285 `/采集`」、`:249`「锻造定稿 L151-158『素材获取五路：**挖掘**/采集/垂钓/怪物掉落/商店』」；实现自承：`qbot_rpg/commands/explore_commands.py:6-7`（登记 DELAYED）、`qbot_rpg/assembly/router_setup.py:245`（stub） | **待实测（引擎未实现·待排期）**：目录侧已列 `gathering`（`module_catalog.py:121-122`，`implemented=False` 不可启用）；见 U1 裁决 |
| X6 | 引擎 / 跨模块渲染文案是否迁入新式模板表？ | `docs/消息模板重构/02_遗留登记.md:6`「## A. 引擎/跨模块渲染文案（待专批）」；`:45`（#16）「引擎层直出文案（专项批）：`qbot_rpg/core/battle.py` 的 `outcome.message` 直出串」 | **待实测（待专批·未闭环）** |
| X7 | 「表外旧键 214 条」待收敛 | `docs/消息模板重构/02_遗留登记.md:52`「test_demo 镜像同步…剩余『表外旧键 214 条』待收敛批」 | **待实测（待收敛批）** |
| X11 | 全量指令清单「设计未实现」约 36 条 | `docs/指令清单_全量.md:19`「\| 设计未实现 \| 约 36 条（强化系/种植温室系/时间天气/木桩/GM 运维/PVP 决斗等，按系统开发）\|」 | **待实测（待排期）** |
| X12 | 战后恢复 `post_battle_recovery` 未实现 | `docs/能力缺漏审计_四方对照.md:147`（F3）「`post_battle_recovery` **全仓无消费点**（grep 0 命中）」；`:256`（②-9）同 | **待实测（引擎未实现·待排期）** |
| X13 | NPC `repair` / 装备耐久未实现（策划书自承降级） | `docs/能力缺漏审计_四方对照.md:102`（C10）「NPC定稿:139『repair：依赖装备耐久系统（**框架未实现**——items 无 durability 字段、装备引擎无磨损）』」 | **待实测（策划书已标注降级·待排期）** |

---

## 二、汇总

| 状态 | 条数 | 编号 |
|---|---|---|
| 已裁决 | 18 | U1 / U2 / U3 / U4 / U5 / U6 / U7 / U8 / X1 / X3 / X4 / X5 / X8 / X9 / X10 / X14 / X15 / X16 |
| 待用户裁决 | 2 | U9 / U10 |
| 待实测 | 6 | X2 / X6 / X7 / X11 / X12 / X13 |

> 注：批30（2026-09-16 对账清账）新增裁决 7 条：U3（失效·基线过期）· U4（已有·部分）·
> U8（已收敛·定稿键）· X8（实测无误）· X9（已删除）· X10（整档作废）· X14（不清理·前提不成立）。
> 原「U4 兼具待用户裁决与待实测两性」的双登记随裁决取消。
> 独立条目共 **26 条**（U1–U10 + X1–X16）。
> 批33（2026-09-17 撤销/重做）新增裁决 1 条：X15（§6.12-01「版本快照」= 不做；
> 撤销/重做草稿级已落地，见台账 §二十二）。
> 批34（2026-09-17 通用 CSV）新增裁决 1 条：X16（§6.12-05 导入顺序与验证 / §6.12-20 公式注入防护 = 已落地；
> 单对象模块不支持为如实登记的边界）。

## 三、维护约定

- 本表由**批次收尾**时更新：新增矛盾追加新 `X<n>`；已裁决条目改状态并写日期 + 依据（引用拍板记录行号 / commit）。
- **不得**因「本表存在」就跳过实测：`待实测` 项要在对应批次给出原始证据后再改状态。
- 与 `docs/能力缺漏审计_四方对照.md` §五 的关系：§五 是**审计原始未确认区**（U1–U10），本表是**正式登记 + 状态跟踪**；两处编号一致，§五 不删。


---

## 追加：CakeGame 借鉴条目落地进度与用户裁决（2026-09-16）

> 来源：`/root/deliverables/CakeGame字段差距分析.md`、`CakeGame字段差距_补漏.md`。用户拍板「一二档全做」，补漏 α/D 组按下列口径处理。

### 已落地
| 批 | 范围 | 状态 |
|---|---|---|
| 批22 | 装备职业限制 `job_restrict`（复用 skills 同名键）· 使用等级 `use_level` · 战斗词条族（吸血/免伤/物穿值·比/法穿值·比，只加 `data/gear_stats.py` 注册表）· 无视护盾/免疫反弹（并入 effects）· 怪物侧复用同族键 | ✅ 并入 main（`3937213`），全量 7762 passed |

### 已裁决 · 明确不做（用户 2026-09-16）
| 条目 | 裁决 |
|---|---|
| **咏唱**（α8，装备附加 Re 内的咏唱机制） | **不要**（用户明确） |
| **同伴 / 好感系统**（补漏 H4 / D-3） | **不要**（用户明确） |
| G2 提交时消耗 vs 仅校验持有 | **不做**——补漏已判"已有等价"：我们 `quest.consume`（deliver/collect 两态）同义 |

### D 组已裁决（2026-09-16 · 主 agent 依用户授权判断）
> 本区原列「待裁决（保留，等用户）」；用户 2026-09-16 授权「你判断就行」后，D-1/D-3/D-5/D-6/D-7
> 五条全部收口，逐条状态与**可核对依据**见下（另见 §追加 D 组裁决表）。
| # | 事项 | 状态与依据 |
|---|---|---|
| D-1 | **转职属性继承开关** 是否推翻 `core/levelup.py:262`（2026-09-09 拍板：职业成长跟随职业，不保留旧职业成长） | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）**：依据 `core/levelup.py:262` 的 2026-09-09 拍板（保留旧职业成长会数值叠加膨胀）；开开关=把已定数值口径重新变成可选项，风险大于收益 |
| D-3 | 排行榜 / 玩家寄售商店（同伴好感用户已否） | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）**：系统级、成本最高；QQ 群场景优先级低于内容与编辑器 |
| D-5 | `Lost_target`（丢失目标机制）我们是否已有等价 | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）**：批28 核查=战斗链**无**仇恨/隐式寻敌等价物，属 CTB 显式目标模型差异（证据见 §追加 D 组裁决 D-5 行） |
| D-6 | `transfer_allowed`（强化转移）：**键在、校验在、`core/` 无消费点**（半个空壳） | **已裁决-已执行-删除（2026-09-16 · 主 agent 依用户授权判断）**：批28 删除框架子结构 + V5 校验 + 缺省键；批29（用户拍板「12 做」）清理内容包两处残留，全仓该键只剩设计文档历史引述与删除说明（详见 §追加 D 组裁决 D-6 行） |
| D-7 | `Basic_User` 34 项属性是否要在我们侧做"可配初值表" | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）**：新手锚点已由 K1（注册礼包 + 初始等级，台账 §十七 K1）覆盖；再压一层"属性初值表"违背"少而深"，运营侧无此需求 |
| — | **α 组其余条目**（α1 装备赋予技能【P0】· α2 物品最大获取数量【P0】· α3 装备增幅技能 · α4 技能条件进阶 · α5 替换普攻 · α6 替换职业） | **用户未否**，排入后续批次（拟批26） |

### 批次队列（串行，因均改 `field_meta.py`）
- 批23 技能/职业（`hp_cost` · 转职前置 `advance` 子对象 · `is_basic`）— 进行中
- 批24 地图/副本/任务（隐藏禁传送 · 进图消耗/门票 · 主动遭遇概率+参战数 · 副本层间推进 · 任务重置周期；**G2 已剔除**）
- 批25 NPC/成就/全局设置（功能次数上限 · 收费多通道 · 成就计入总数/自动领取 · 注册礼包 · 按状态禁用指令 · **指令限流 K3** · 消息分段上限）
- 批26 α 组（除咏唱/好感）

---

## 追加：D 组裁决（主 agent 判断，用户授权）· α 组全落地（2026-09-16）

### D 组裁决（用户授权"你判断就行"）
| # | 事项 | 裁决 | 理由 |
|---|---|---|---|
| D-1 | 转职属性继承开关 | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）**（保持 `core/levelup.py:262` 2026-09-09 拍板：职业成长跟随职业，不保留旧职业成长） | ①那是刻意拍板（保留旧成长会导致数值叠加膨胀，与他们定稿的"套装/成长数值膨胀对策"同向）；②开开关=把已定的数值口径重新变成可选项，风险大于收益；③将来真有玩法需要，再单开专项 |
| D-3 | 排行榜 / 玩家寄售商店 | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）** | 系统级、成本最高；QQ 群场景优先级低于内容与编辑器（好感一项目用户已否） |
| D-5 | `Lost_target`（丢失目标）是否有等价 | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）** | 我们的战斗是 CTB 行动条 + 显式目标，不存在"实体丢失目标"的隐式寻敌模型 → 不是缺口，是**模型差异**。**批28 核查证据**：①战斗为 1v1 显式对侧，`qbot_rpg/core/battle.py:853` `_opposite()`；②无仇恨/威胁表：`grep -rniE "aggro\|hate\|threat\|仇恨" qbot_rpg/` 全仓唯一命中 `qbot_rpg/core/battle.py:3412`「完整仇恨值（来源/衰减/远程系数）依赖多目标选择，属组队里程碑前置」（即未实现）；③唯一"丢失"构造是世界层 F-08 `lost_pending`（`qbot_rpg/world/battle_boundary.py:47-201` `TargetRef/build_lost_pending/decide_lost`；快照字段 `qbot_rpg/data/battle.py:75`、`qbot_rpg/core/battle.py:2211-2213` 仅置 None），语义=目标实例在世界/会话边界消失（被他人击败/刷怪删除/时段结束），**且全仓无生产调用点**（`decide_lost`/`build_lost_pending`/`apply_lost_pending`/`clear_lost_pending` 仅 `battle_boundary.py` 自身 + `tests/unit/test_battle_boundary.py`）；④`qbot_rpg/world/chase_resume.py` 为换区追击续战，与"丢失目标"不等价。故**不做** |
| D-6 | `transfer_allowed`（键在、校验在、`core/` 无消费点） | **已裁决-已执行-删除（2026-09-16 · 主 agent 依用户授权判断）** | 遵循奥卡姆：多余实体直接删除，不留"看着像功能其实没接线"的字段；将来要做"强化转移"再按需求设计。**执行证据（批28）**：①框架子结构 `qbot_rpg/content/field_meta_child_structure.py`（`enhance.settings`）；②V5 布尔校验循环 `qbot_rpg/content/enhance_models.py:308-311`；③缺省锚点 `DEFAULT_ENHANCE["settings"]`——三处已删；回归 `tests/unit/test_batch28_d6_transfer_removed.py`。**内容包残留已清理（批29，2026-09-17；用户 2026-09-16 拍板「12 做」）**：`content/veinborn/enhance.json` 数据键 + `content/veinborn/field_meta.json` 展示名两处已删；清理后 `check_pack` 仍 0 error / 332 warning（与删前一致，无红拦）；回归 `tests/unit/test_batch29_transfer_residue_cleaned.py`；迁移对拍基线按门禁语义重定（删除内容包展示名会让基线侧出现「仅迁移前有（删除）」硬差异 → 重定到本批清理提交） |
| D-7 | 玩家属性初值表可配 | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）** | 新手锚点已由 K1（注册礼包+初始等级，台账 §十七 K1）覆盖；再压一层"属性初值表"违背"少而深"，且运营侧并无此需求 |

### α 组：全部落地（用户"阿尔法组全要"；**咏唱已明确不要**）
| 条目 | 内容 | 归属 |
|---|---|---|
| α1 | **装备赋予技能**（穿戴获得 / 卸下收回） | equipment |
| α2 | **物品最大获取数量**（0 不限 / 1 唯一 / -1 禁止获取） | items/equipment |
| α3 | 装备增幅技能（伤害/冷却/熟练 三类增减 + 上下限） | equipment |
| α4 | 技能按条件进阶/退阶（24+ 条件分支） | skills |
| α5 | 替换普攻（替换技能 + 触发概率） | equipment |
| α6 | 替换职业（替换职业列表 / 等级重置 / 替换名称） | equipment |
| α7 | 技能附加的「对方」减益与持续制式（时间制/回合制、可叠加） | skills/effects |
| ~~α8~~ | ~~咏唱~~ | **用户明确不要** |
| β1 | 每技能战斗播报模板 | templates/skills（随批26 评估） |
| β4 | 技能复活标记（复活技能，非副本复活点） | skills |

### 批27 落地结论（α组技能侧 · 2026-09-17）
| 条目 | 落地 | 说明 |
|---|---|---|
| α4 | **既有机制已覆盖（形态替换）→ 缺口已补齐（条件集）** | 「同技按条件换形态」由 `skill_chains` 步骤 + `mode:"replace"`（`core/combo._resolve_derivation` 路径 c）等价表达：释放基础技前求 `condition`，命中即自动替换为进阶技。用法见 `tests/unit/test_batch27_alpha4_conditional_advance.py`。**批29（2026-09-17）补齐条件集缺口**：新增 `level`（`{eq\|min\|max}`，阈值 int ≥ 1）/ `job`（`{in\|not_in: [job_id…]}`，引用 jobs）/ `quest`（`{quest_id: {state: in_progress\|completed\|not_started}}`，引用 quest）三类，**沿用同一条件对象形态与同一求值入口**（`core/combo.evaluate_condition` + `ConditionCtx`），编辑器主体声明在 `content/field_meta.py::CHAIN_CONDITION_SUBJECTS`，红拦校验在 `content/skill_validator.py::validate_skill_chains`，端到端见 `tests/unit/test_batch29_level_job_quest_conditions.py`。仍存缺口：「退阶」用 `from` 侧条件取反表达；无「取消冷却/取消覆盖」 |
| α7 | **既有字段已可表达 → 不新增字段** | 对方属性减益走 `stat_modifier`（即时）或 status + `status_apply`（持续、到期自动回滚）；`duration.turns` = 回合制；`stack_frame:"dual"` 可叠加。用法见 `tests/unit/test_batch27_alpha7_enemy_debuff.py`。缺口：时间制（秒）在本 CTB 回合框架无对应物（模型差异）；战斗内属性为快照键，无基础/特殊/技能/熟练/物品分层 |
| β1 | **落地 `skills[].message_key`**（只存模板键，不存文案） | 渲染层按最终 skill_id 取键；编辑器下拉 = 全量模板表 key；未配置逐字段一致。边界：内容包不得新增表外模板键（既有门禁不放松），技能独有文案需框架表登记 |
| β4 | **落地 `skills[].revive`**（bool，缺省 false） | `core/battle.revive_side` 沿用既有 `revive` 事件链路（清死亡标记 + 恢复 HP + 去虚弱 + 回收胜负标记）。缺口：CTB `mark_dead` 无反向接口，只做战斗态复活、不回插调度队列；1v1 无双侧友军，落为「复活死亡态目标」 |

---

## 追加：批30 · 对账清账（7 项）· 2026-09-16（主 agent 依用户授权）

> 来源：用户 2026-09-16「按你建议」。范围 = 登记表 §B/§C 的 7 条未决项（U3 / U4 / U8 / X8 / X9 / X10 / X14）。
> 逐项动作与可核对证据见 §一 登记表对应行；本节只作索引与口径备注。

| 项 | 类型 | 结论 | 关键证据 |
|---|---|---|---|
| U3 · 文档 | 文档 | **已裁决-已失效（基线过期）** | `docs/细化/细化_2c2d_锻造套装与客制.md` 已存在 |
| U4 · 实测 | 实测 | **已裁决-已有（部分）** | per-action 收集器 `core/battle.py:1024`；聚合/展示/`dummy_log` 未实现 |
| U8 · 代码+文档 | 代码+文档 | **已裁决-已收敛（定稿键）** | `field_meta.py` 12 字段；`core/shop.py:536-549` 兼容读；回归 `test_batch30_shop_field_convergence.py` |
| X8 · 文档+实测 | 文档+实测 | **已裁决-实测无误** | 两键实测渲染 + 宽度 23/19 ≤ 28；规范 §三 |
| X9 · 代码+文档 | 代码+文档 | **已裁决-已删除** | 5b 契约 G13 删除（2026-09-16）；回归 `test_batch30_gm_edit_removed.py` |
| X10 · 文档 | 文档 | **已裁决-整档作废** | 5a/5a2/摸底已删；重写文档为唯一来源 |
| X14 · 文档 | 文档 | **已裁决-不清理（前提不成立）** | 定稿 L42 已有 `monster_def_rate` 定义（R-09 2026-08-18）+ 实现消费 |

**口径备注**：
- **U8 兼容映射**：读侧 = `limit` 缺省回落 `per_player`、`period` 缺省回落 `per_player_period`（`core/shop.py:536-549`），`scope` 生效侧由 `shop_models.ShopItemDef.effective_scope` 推断（显式 `scope` > 旧 `per_player` → personal > 缺省 global）；写侧编辑器只出定稿键。迁移影响面：现有 4 个内容包 `shop.json` **均无旧键**（实测遍历 items+pool），无需数据迁移。
- **X14 未执行「删除」的原因**：任务书前提（定稿全文无定义）在**本仓 383 行版不成立**——归档 375 行版才有此问题；删除已定义且已被引擎消费的因子属擅自改设计，故保留并在登记表写明。


