# 2026-09-10

## 仓库拉取

- 将 `https://github.com/xianyugezi/QBot-TurnTellerRPG` 全量克隆到工作区根目录 `D:\1V1TXTRPG\TTR`（即 TTR 本身作为仓库根，非子目录）。
- 仓库概况：1226 个受版本管理文件，约 32MB，分支仅 `main`，HEAD = `4ae3eaa`。
- 主要目录：`qbot_rpg/`（平台无关核心包，七层 core/world/storage/content/data + commands/web）、`qbot_rpg_bridge/`、`content/`（数据驱动内容）、`docs/`（细化契约 76 份 + 规划 + 审查，381 文件）、`tests/`（测试金字塔，353 文件）、`scripts/`（里程碑验收 verify_m0~m6 + check_architecture 架构门禁）。
- 入口文档：`README.md` → `docs/实现层启动手册.md` → `docs/细化规划总索引.md`；进度跟踪见根目录 `记录.md`。

## 注意事项

- 首次全量克隆在前台超时被 SIGTERM 中断，残留了不完整的 `.git`，已清理后改用后台任务重跑成功。经验：该仓库全量克隆约需 3 分钟，应直接后台执行（`run_in_background`）。
- `.workbuddy/`（本记忆目录）位于仓库根内，未在仓库 `.gitignore` 中，会显示为未跟踪目录，提交前注意排除。
