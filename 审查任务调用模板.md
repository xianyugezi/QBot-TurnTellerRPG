# 审查任务调用模板（dsh 固定对话使用）

> 用法：每次审查任务，把【角色加载】+【审查任务】拼成一条 dshx 命令。
> 固定对话的正确性 = 每次都从同一角色卡启动 + 报告落盘到同一路径 + 主 Agent 回执。

## 命令模板

```bash
cd /root/QBot-TurnTellerRPG && timeout 280 dshx --profile headless --patch /root/.dsh/pro-patch.yml "$(cat 审查角色_初始化.md)

【审查任务】
<在此填写本次审查对象>"
```

## 审查任务写法示例

【审查对象】：请审查以下文件是否严格对照 `细化_3b_玩家属性三层.md` 与《玩家属性方案定稿.md》：
- /root/QBot-TurnTellerRPG/qbot_rpg/core/stats.py
- /root/QBot-TurnTellerRPG/qbot_rpg/data/stats.py

【重点】：三层结构（白值/加成/临时）计算管线顺序、百分比只乘一次、条件加成防叠乘、stats.json schema 合规。输出报告到 `/root/QBot-TurnTellerRPG/审查报告/审查_stats_20260818.md`。

## 固定对话会话 ID 管理

- 单次审查用 headless（无状态）
- 若需要连续上下文（同一功能多轮审查），用 workdir 固定 + 审查报告路径固定，主 Agent 把上一轮报告 attach 进本轮任务文本
- 会话历史：dsh sessions 按 workdir 累积于 `/root/.dsh/sessions/--root-QBot-TurnTellerRPG--/`

## 回执约定

- 审查报告落盘后，主 Agent read_file 核对真实落盘（dsh 自报不可信）
- 主 Agent 把审查结论合并进里程碑汇报给用户拍板
