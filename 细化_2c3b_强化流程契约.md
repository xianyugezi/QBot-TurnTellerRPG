# 细化 · 2c3b 强化流程契约（/强化 <装备>+N 原子一条消息 · /强化信息 · /强化保护 · 防双扣 · 指令解析 · 验收用例）

> 依据（严格引用）：`/root/docs_archive/RPG框架项目/强化系统设计定稿.md`（下称【强化定稿】，v1.0 · 235 行，全篇已读）；联动 `细化_2c3a_强化数值曲线.md`（下称【2c3a】，356 行，成功率/消耗/上限/幸运/碎装备的数据字段级定义，本文只接线不重述）
> 对应任务：规划_路2c3b_强化交互流程.md（/强化 <装备>+N 的 + 等级标记解析 / 原子一条指令出结果防双扣 / /强化信息 <装备> +N 成功率上限 / /强化保护 <装备> 保护石 / 禁批量 * / 指令参数解析 / 验收用例）
> 定位：**本细化只定义强化交互流程契约（指令 / 守卫 / 出结果模板 / 保护石流程 / 原子防双扣 / 参数解析 / 验收用例）**；数值侧（成功率曲线、消耗计价、幸运修正公式、上限表、碎装备概率、校验器、编辑器）完整定义归 2c3a，本文对数值只落**指令可见的接线面**（显示拆分、消耗预览的字段来源）
> 编号规则：流程规则以 F-x、守卫以 GU-x、解析规则以 P-x 编号；验收用例 TC-01 起连续编号
> 文档总览：主契约指令 **3** 条（/强化、/强化信息、/强化保护），验收用例 **23** 例（TC-01 ~ TC-23）

## 0. 一句话定位（可追溯）

| 维度 | 定稿要求 | 引用 |
|---|---|---|
| 强化 = 数值堆叠 +N | 强化 = 给装备堆数值（+N）：消耗强化石+金币，按成功率曲线逐级提升；**概率成功**（与锻造 100% 随机性不同源） | 【强化定稿】L16 / L22 / L27 |
| 原子一条消息 | 原子操作：一条指令出结果（零会话，免疫热重载/双扣）；成功提示数值变化，失败提示惩罚类型 | L44 / L45 |
| + 为等级标记 | /强化 铁剑+5 的 + 为等级标记（保持）；**强化禁批量 \***（成功率随机，批量=连点爆装风险） | L10 |
| 失败分级 | 低等级段（+0 → 默认 +3）失败=装备不变；高等级段（默认 +4 起）失败=强化等级 -1；fail_tier_split 可配 | L53-55 / L155 |
| 保护石 | 强化失败不掉级（消耗 1 颗，失败时消耗）；来源 炼金产出/商店高价/活动奖励 | L68-69 / L188 |
| 上限按品质 | 强化上限=装备品质（普通+5 精良+8 史诗+10 传说+12）；到达上限 /强化 提示「已达强化上限」 | L113-115 / L154 |
| 指令面 | /强化 <装备>（原子，需确认材料后直接结算）/ /强化保护 <装备> / /强化信息 <装备>（当前 +N / 成功率 / 上限） | L214-216 |
| 刷屏防护 | 刷强化刷屏 → 原子一条消息 + 频率控制（框架 7 章） | L230 |

---

## 一、/强化 <装备>[+N]（原子一条指令出结果）

> 依据：【强化定稿】§二 L32-45（流程示例与原子铁律）、§三 L49-71（失败惩罚）、L10（等级标记/禁批量）、L214（指令定义：`/强化 <装备>  # 强化 +1（原子，需确认材料后直接结算）`）。

### 1.1 触发语义

- **基础形式** `/强化 <装备>`：强化 +1（L214）——从当前等级 +N 强化到 +N+1；材料足够即**直接结算，零会话、零确认**（L214“需确认材料后直接结算”= 系统按材料足额与否自行判定，玩家无需二次确认）。
- **带标形式** `/强化 <装备>+N`：`+N` 为**等级标记**（L10），N = 玩家声明的当前强化等级；语义仍为“从当前等级强化到下一级”，标记仅作**校验锚点**（GU-04），用于防止过期指令/连点误触造成等级认知错位。
- **禁批量**：`*` 在该系统**禁用**（L10）——成功率为随机，批量=连点爆装风险；与锻造 `*数量` 批量语义（2c2b P-05 / 【锻造】L12）对照禁止。
- **零会话**：强化不建立任何会话/待确认上下文（L44 零会话；与《指令分隔符统一规范》L10 同源约定，锻造 2c2b §1.2 同款结论）。

### 1.2 前置守卫（GU-01 ~ GU-05）

| # | 守卫 | 判定 | 失败行为 | 引用 |
|---|---|---|---|---|
| GU-01 | 强化系统启用 | 内容包 enhance.json 有效（2c3a §7 校验通过：曲线单调不增 / 档位覆盖 / 上限键=items.json 四档品质枚举） | 提示系统未启用 | 【强化定稿】L196-205 |
| GU-02 | 参数可解析 | `/<前缀> + <装备名>[+N]`；名称禁空格（P-01）、禁 `*`（P-03）、`+N` 须非负整数（P-02） | 参数错误 / 候选列表（§五） | L10 / L214 |
| GU-03 | 装备存在 & 持有 | 玩家背包实例命中（精确→唯一前缀→歧义列表，P-05）；未持有或已销毁 → 未找到空态 | 未找到 / 候选列表 | L214（<装备> 语义） |
| GU-04 | 等级匹配 & 未达顶 | ① 带 +N 时：实例当前等级 == N（不符 → 拒绝并报实际等级）；② 当前等级 < max_by_rarity（达顶 → 「已达强化上限」模板，**零消耗**） | 等级不符报错 / 达顶提示 | L10 / L115 / L113-114 / L154 |
| GU-05 | 材料足够 | 强化石（目标等级档位，数量 = 目标等级 × stones_per_level）+ 金币（= 目标等级 × coin_per_level）双足额（2c3a §1.1/§1.2）；缺项分列、先石后币 | 材料不足报错（§1.5） | L103-105 / L176-177 / 2c3a §1.4 |

> 守卫顺序固定 GU-02→03→04→05：先解析、再存在性、再等级/达顶、最后材料——报错永远指向「距可强化最近的那个缺口」，与 /强化信息 查询路径一致（§二）。

### 1.3 成功路径（原子提交，一条消息出结果）

```
[玩家] /强化 铁剑
[机器人] ⚙️ 铁剑 → 铁剑+1（成功率 90%｜幸运修正 +2）
        ✅ 成功！攻击 12 → 13
```
—— 定稿原文 L35-37 逐字段接线：

1. **事务内复核**：BEGIN IMMEDIATE 后重读背包/等级/材料（GU-03~05 复核），防事务外读到的过期快照（§四 F-01）。
2. **成功率计算**：基础率 = success_curve 中目标等级 `to=N+1` 的 `rate`（L79-81 / L163-174）；实际率 = 基础率 +（√我方幸运 − √对方幸运）%（对方=0 即 √我方幸运%）（L87，luck_affects=false 时修正恒 0，L89/L159）；结果 clamp 到 [0,100]。
3. **单次随机判定**：一次 roll 定成败（与锻造 100% 不同源，L27）；同一指令内不重 roll（L44 原子语义）。
4. **成功分支写入**（同一事务）：扣强化石（目标档位，2c3a §1.1）+ 扣金币（2c3a §1.2）→ 实例等级 +1 → 属性变更（武器攻击/防具防御按 2c3a §8 values 表，`{"type":"flat","value":…}` L184-187）→ 写 enhance_log（message_id 幂等记录，§四）→ COMMIT。
5. **输出模板**：`⚙️ <名> → <名>+N+1（成功率 X%｜幸运修正 ±Y）`（L36/L41 格式）+ `✅ 成功！<属性> <旧值> → <新值>`（L37，数值变化必显）；成功率显示拆分对齐 L90：`成功率 X%（基础 A% + 幸运 B%）`。
6. **结果回写存档**：实例等级字段落库，供 /强化信息（§二）与装备卡 +N 显示（L210）读取。

### 1.4 失败路径（分级惩罚，材料照扣）

| 段 | 判定（按**目标等级**） | 惩罚 | 结果模板 | 引用 |
|---|---|---|---|---|
| 低等级段 | 目标等级 ≤ fail_tier_split（默认 +3） | 装备不变 | `❌ 强化失败，装备保持不变` | L38 / L53 / L155 |
| 高等级段 | 目标等级 > fail_tier_split（默认 +4 起） | 强化等级 -1 | `❌ 失败！强化等级 -1（铁剑+5 → +4）` | L41-42 / L54 / L155 |

- **材料照扣**：失败与成功同样消耗石+金币（L103-105 “每次强化消耗”，无成功/失败区分；失败消耗即经济消耗设计 L226）——**扣除与惩罚在同一事务内原子完成**（不是错误路径）。
- **碎装备**：默认关（shatter_mode=false，我们不用，L60-63 / L156-158）；内容包开启时，碎判定挂在“高等级段失败”分支之后（碎率 shatter_chance，2c3a §5），本文只预留钩子、不展开（我们自己的数据包不配）。
- **保护石模式**：/强化保护 进入该分支时按 §三 规则消耗保护石免掉级。
- **铁律**：守卫失败（GU-02~05）= 零副作用可重试（§1.5）；流程判定失败（roll 失败）= 原子提交的“消耗+惩罚”结果，不可重试回滚（随机结果已定，防刷号）。

### 1.5 输出消息模板汇总（消息格式契约）

| 场景 | 输出模板 | 引用 |
|---|---|---|
| 成功 | `⚙️ 铁剑 → 铁剑+1（成功率 90%｜幸运修正 +2）` + `✅ 成功！攻击 12 → 13` | L36-37 |
| 低段失败 | `⚙️ …（成功率 …）` + `❌ 强化失败，装备保持不变` | L38 |
| 高段失败 | `⚙️ 铁剑+5 → +6（成功率 50%｜幸运修正 -1）` + `❌ 失败！强化等级 -1（铁剑+5 → +4）` | L41-42 |
| 已达上限 | `已达强化上限（+12）` | L115 |
| 材料不足 | `❌ 材料不足：需要 中级强化石 ×6 + 金币 600；缺：中级强化石 ×4（来源：炼金合成 / 商店）` | L95-106 / 2c3a §1 |
| 等级不符（带标） | `铁剑 当前 +3，请确认强化目标（+N 为等级标记）` | L10（标记语义） |
| 参数错误 / 歧义 / 未找到 | §五 5.3 | L10 / L214 |

---

## 二、/强化信息 <装备>（+N / 成功率 / 上限 / 消耗预览）

> 依据：【强化定稿】L216（`/强化信息 <装备>  # 查看当前 +N / 成功率 / 上限`）、L90（成功率显示拆分）、L113-114（上限按品质）、L103-105（消耗）、2c3a §1.3（默认消耗参考表）。

### 2.1 触发与门槛

- `/强化信息 <装备>`：**只读查询指令**——不扣材料、不扣金币、不 roll、不建会话；任意频次可用（频率控制仍按框架 7 章兜底 L230）。
- 装备匹配走 GU-03 同款算法（精确→唯一前缀→歧义候选，P-05）；未持有 → 空态 `未找到「<名>」`；/强化信息 无等级标记参数（只查当前状态，不需要声明等级）。

### 2.2 输出结构（对齐定稿 L216 三要素 + 消耗预览扩展）

```
📖 铁剑+3（品质：精良｜强化上限 +8）
→ +4 成功率：70%（基础 70% + 幸运 0%｜幸运修正开）
→ +4 消耗：中级强化石 ×4 + 金币 ×400 ｜ 持有：中级强化石 12 / 金币 2400
距离上限：5 级
```

| 段 | 内容 | 数据源 | 引用 |
|---|---|---|---|
| 标题 | 装备名 + 当前 `+N`（实例等级） + 品质 + 强化上限 | 存档实例等级字段 + items.json 品质枚举 + max_by_rarity | 【强化定稿】L216 / L113-114 / L154 |
| 成功率行 | 目标等级（当前+1）基础率 + 幸运修正拆分，格式对齐 L90 `成功率 X%（基础 A% + 幸运 B%）`；luck_affects=false 显示 `幸运修正已关` | success_curve + 幸运公式（L87-90）+ 角色 luck 属性（L88） | L87-90 / L159 / L216 |
| 消耗行 | 目标等级所需 石（数量=目标等级×stones_per_level + 档位 stone_tiers）+ 金币（=目标等级×coin_per_level）+ 背包实时持有量 | 2c3a §1.1/§1.2/§1.3 + 背包 | L103-105 / L176-182 |
| 上限态 | 已达上限时替换：`已达强化上限（+12）`，省略成功率/消耗行 | max_by_rarity | L115 |

### 2.3 达顶态与联动

- 当前等级 == max_by_rarity → 成功率行与消耗行合并替换为 `已达强化上限（+N）`（L115）；文案与 /强化 达顶提示**同一常量源**（保证 TC-08 断言同文案）。
- 装备卡联动：/强化信息 数据源即装备卡“当前 +N 与上限”字段（L210），两处共用同一读取接口，无二次定义。

---

## 三、/强化保护 <装备>[+N]（保护石：失败有惩罚时扣 1 颗免掉级）

> 依据：【强化定稿】§3.3 L66-71（保护石定义）、L215（指令：`/强化保护 <装备>  # 用保护石强化（可选）`）、L188（`"protect_stone": "item_protect_stone"` 物品 ID，可空）、L69（来源三渠道）。

### 3.1 触发语义

- `/强化保护 <装备>[+N]`：与 /强化 **完全相同的流程**（守卫 GU-01~05 全量复用、成功率/消耗/幸运修正/达顶判定一致），唯一差异 = 追加**保护标记**：高等级段失败时消费保护石抵消掉级。
- **前置校验**（GU-03 之后追加 GU-06）：事务内复核背包持有保护石（item_protect_stone，L188）≥ 1；不足 → 拒绝并给来源提示（L69：炼金产出 / 商店高价 / 活动奖励）；不配保护石（L188 可空）→ 指令整体不可用（GU-01 扩展：`强化保护 未启用`）。
- 保护石**不在事务外预检**（防并发双吃不安全），存在性在事务内与结算同窗口复核（§四 F-01 串行化保证）。

### 3.2 保护石消耗时机（细化决策，定版）

> 【强化定稿】L68：`保护石：强化失败不掉级（消耗 1 颗，失败时消耗）`。

| 结果 | 消耗 | 等级 | 说明 |
|---|---|---|---|
| 成功 | **不消耗** | +1 | 保护石只保失败，成功无“免”可言 |
| 失败·低等级段（目标 ≤ fail_tier_split，无惩罚） | **不消耗** | 不变 | 无掉级惩罚，无消耗对象（免掉级的前提是“会掉级”） |
| 失败·高等级段（目标 > fail_tier_split，有惩罚） | **扣 1 颗** | **不变（免掉级）** | 保护石抵消掉级；材料/金币照扣（L103-105） |

> 定版依据：任务定版“保护石：失败**有惩罚时**扣 1 颗免掉级”——低段失败本无惩罚，消耗保护石无意义且挫败（对齐 L70 保护石定位=高等级段堆装备的保险）。

### 3.3 输出模板

```
[玩家] /强化保护 铁剑+5
[机器人] ⚙️ 铁剑+5 → +6（成功率 50%｜幸运修正 -1｜保护石生效）
        ❌ 失败！保护石抵消掉级（消耗 1 颗，铁剑+5 保持不变）
```
—— 定稿 L41-42 格式 + 保护行扩展：高段失败模板尾部追加 `｜保护石抵消掉级（消耗 1 颗，装备保持不变）`；成功路径模板追加 `｜保护石生效` 前缀标记（可选展示），成功结果为 §1.3 同款。

### 3.4 与普通强化一致性（验收锚点）

| 维度 | /强化 | /强化保护 |
|---|---|---|
| 成功率 / 消耗 / 幸运 / 达顶 | 同 | **完全一致**（同一结算函数，仅失败分支差异） |
| 低段失败 | 不变，材料照扣 | 不变，材料照扣，保护石不耗（§3.2） |
| 高段失败 | 等级 -1，材料照扣 | 等级不变，材料照扣，保护石 -1 |
| 保护石校验 | 无 | 事务内足额校验（不足拒绝+来源提示） |
| 守卫 | GU-01~05 | GU-01~06（追加 GU-06） |

---

## 四、原子防双扣（SQLite 事务 + message_id 幂等 + 热重载免疫）

> 依据：【强化定稿】L44（`原子操作：一条指令出结果（零会话，免疫热重载/双扣）`）、L10（禁批量=切断连点爆装）、L230（刷屏 → 原子一条消息 + 频率控制（框架 7 章））。

### 4.1 威胁模型（防什么）

| # | 场景 | 不加防护的后果 |
|---|---|---|
| T-1 | QQ 网关重试 / 消息重发（同一 message_id 被投递两次） | 两次扣材料、两次升级（双扣双升） |
| T-2 | 处理中途热重载/进程被杀（旧代码已扣材料、未写等级，或反之） | 幽灵扣减 / 半升级（等级与库存失配） |
| T-3 | 玩家连点 / 脚本连发（批量 `*` 已禁，但多指令仍可能） | 多事务并发读同一背包，材料透支 |
| T-4 | 多 worker / 插件重载窗口内请求并发 | 同 T-3 |

### 4.2 SQLite 事务（单写者串行化，F-01 ~ F-02）

| # | 规则 | 内容 |
|---|---|---|
| F-01 | 单事务结算 | 强化结算整体处于一个事务：`BEGIN IMMEDIATE`（立即取写锁，串行化并发写者）→ **事务内重读** 背包/材料/等级/保护石（防过期断言）→ 判定（GU-03~06 复核）→ roll → 扣减+等级/属性变更+写 enhance_log → `COMMIT`。 |
| F-02 | 原子回滚 | 任一步失败/断言不通过 → `ROLLBACK`（零副作用，等价守卫失败模板 §1.5）；**扣除与变更永远同生共死**——不存在“扣了材料没升级”或“升级了没扣材料”的窗口。 |

### 4.3 message_id 幂等（F-03 ~ F-05）

| # | 规则 | 内容 |
|---|---|---|
| F-03 | 幂等键 | enhance_log 表 `message_id` 建 UNIQUE 索引；每条 QQ 消息的全局唯一 message_id 作为请求标识。 |
| F-04 | 先查后写 | 事务内先查 message_id：**命中** → 不重复扣减、不重复 roll，直接返回该条已缓存的结果消息（原样重发，用户看到同一结果）；**未命中** → 正常结算。 |
| F-05 | 同事务写幂等 | 幂等记录与结算写入**同一事务提交**（F-01 内）——崩溃回滚时幂等记录一并消失，重发后重新执行是安全的（消除“已结算但没记幂等”的死角）；T-1 由 F-03/F-05 闭环。 |

### 4.4 热重载免疫（F-06 ~ F-07）

| # | 规则 | 内容 |
|---|---|---|
| F-06 | 零跨消息状态 | 强化零会话（L44）：无内存态/无待确认上下文，热重载不丢任何流程状态——已提交的结果落库，未提交的事务被 WAL/回滚机制还原，重发即可重跑（T-2 闭环）。 |
| F-07 | 状态全落库 | 强化等级/属性/材料/金币全部为存档持久字段；重载只影响后续指令入口，绝不回放或补扣历史消息。 |

### 4.5 防连点兜底（三层，T-3/T-4 闭环）

1. **禁批量 `*`**（L10）：批量语义在解析层即被拒绝（P-03），切断“一条指令连爆”的入口。
2. **等级标记锚定**（L10 / GU-04）：带 +N 的指令以声明等级校验——连点中第二次指令因实际等级已变被拒（§1.5 等级不符模板），杜绝“同一目标的两次结算认知错位”。
3. **频率控制**（框架 7 章，L230）：同玩家强化类指令按频率表限流，超频直接拦截（与 /锻造 共用同一频率通道）。

### 4.6 与 2c2b 一致性

锻造与强化共用同一“原子事务 + message_id 幂等”结算底座（2c2b §1.2 同款结论，forge_log/enhance_log 同表族结构）；差异仅在裁决源：锻造=确定性素材判定（无 roll），强化=单次 roll 概率裁决（L27 随机性不同源）——roll 调用点固定在事务内、成功/失败分支各只写一次。

---

## 五、指令参数解析（装备名 / 等级标记 +N / 名称禁空格 / 禁批量 *）

> 依据：【强化定稿】L10（`/强化 铁剑+5 的 + 为等级标记（保持）；强化禁批量 *`——遵循《指令分隔符统一规范》）、指令表 L214-216、示例语法 L36-42（`/强化 铁剑`、`/强化 铁剑+5`）。

### 5.1 词法规则（P-01 ~ P-06）

| # | 规则 | 行为 | 引用 |
|---|---|---|---|
| P-01 | **装备名禁空格** | 首个参数内不允许空白字符（`铁剑 +2` 视为参数错误：`装备名不含空格`）；`/强化 铁剑+2` 合法——`+N` 为**附着后缀**，不是独立参数 | 【强化定稿】L10（分隔规范） |
| P-02 | 等级标记 `+N` | 按 `^(.+?)\+(\d+)$` 取**最后一个 `+数字` 段**为等级标记；N 须为非负整数且 ≤ 该实例上限（超上限按达顶/等级不符处理）；`铁剑+`、`铁剑+abc`、`铁剑++2` → `等级标记须为非负整数`；**装备名内禁 `+`**（+ 仅在标记位出现，L10“保持”标记语义） | L10 / L36-42 |
| P-03 | 禁批量 `*` | 参数含 `*` → `强化不支持批量强化（成功率随机，批量=连点爆装风险）`；与锻造 `*数量` 批量允许（2c2b P-05）对照——随机性不同源（L27） | L10 / L27 |
| P-04 | 前缀路由 | `/强化` `/强化信息` `/强化保护` 三前缀独立注册（指令表 L214-216）；最长前缀匹配，防交叉越权（/强化信息 前缀不会被 /强化 吞） | L214-216 |
| P-05 | 装备匹配 | 精确匹配 → 唯一前缀 → 歧义候选列表（每候选附 当前+N/上限 1 行摘要，不默选不自动强化）；全半角/大小写规范化后匹配；名称含 `·`/`Ⅱ`/`【】` 按普通字符参与（沿用装备名体系，2c2b P-02/P-03 同款约定） | L214（<装备> 语义） |
| P-06 | 参数数量 | 无参数 → 用法提示（`/强化 <装备>[+N]`）；多余参数（`/强化 铁剑 1`）→ 参数错误（名称禁空格 ⇒ 单参数天然界定，P-01 保证） | L214-216 |

### 5.2 各指令参数表（登记 3 条全契约指令）

| 指令 | 参数 | 解析要点 | 结算要点 | 引用 |
|---|---|---|---|---|
| /强化 <装备>[+N] | 装备名 + 可选等级标记 | P-01/P-02/P-03（名称禁空格/标记校验/禁批量） | 原子一条消息：成功 数值变更；失败 分级惩罚（低不变/高掉级） | 【强化定稿】L214 / L36-42 |
| /强化信息 <装备> | 装备名（无标记） | P-01/P-05 | 只读：当前 +N / 成功率（含幸运拆分）/ 上限 / 消耗预览 | L216 / L90 / L113-114 |
| /强化保护 <装备>[+N] | 装备名 + 可选等级标记 | 同 /强化 + GU-06 保护石足额校验 | 高段失败扣 1 颗免掉级；成功/低段失败不耗 | L215 / L68 / L188 |

> 全契约指令 = **/强化、/强化信息、/强化保护** 共 **3** 条；无其他强化指令（转移不用 L127-129、碎装备不开 L62、等级门槛默认关 L141——均无指令入口）。`+` 等级标记遵循《指令分隔符统一规范》（L10）。

### 5.3 解析失败输出汇总

| 失败态 | 输出 | 引用 |
|---|---|---|
| 名称含空格 | `参数错误：装备名不含空格` | P-01 / L10 |
| 标记非法 | `参数错误：等级标记须为非负整数（如 铁剑+5）` | P-02 / L10 |
| 批量 | `强化不支持批量强化（成功率随机）` | P-03 / L10 |
| 歧义前缀 | 候选列表（每候选 1 行）+ 用法 | P-05 |
| 未持有 | `未找到「<名>」` | P-05 / L214 |
| 无参/多参 | 用法提示 / `参数错误` | P-06 |

---

## 六、验收测试用例

> 编号 TC-01 起连续；断言全部自动化可判定（固定随机种子 + 模拟背包/存档/消息网关）。覆盖矩阵：A 强化成功/失败 6 例、B /强化信息 3 例、C /强化保护 4 例、D 达顶 2 例、E 原子防双扣 4 例、F 指令参数解析 4 例，共 **23** 例。

### 覆盖矩阵

| 需求类别 | 定稿章节 | 用例 | 条数 |
|---|---|---|---|
| A 强化成功/失败（原子 + 分级） | §二 L34-45 / §三.1 L52-56 | TC-01 ~ TC-06 | 6 |
| B /强化信息 | L216 / L90 / L113-114 / L103-105 | TC-07 ~ TC-09 | 3 |
| C /强化保护 | §3.3 L66-71 / L215 / L188 / L69 | TC-10 ~ TC-13 | 4 |
| D 强化达顶 | §六 L113-115 | TC-14 ~ TC-15 | 2 |
| E 原子防双扣 | L44 / L10 / L230 | TC-16 ~ TC-19 | 4 |
| F 指令参数解析 | L10 / L214-216 | TC-20 ~ TC-23 | 4 |
| **合计** | — | TC-01 ~ TC-23 | **23** |

### A 强化成功 / 失败（原子一条消息 + 分级惩罚）

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-01 | `/强化 铁剑`（+0 新装备，材料齐，roll 种子→成功） | **一条指令即时出结果**（无会话/无确认）：`⚙️ 铁剑 → 铁剑+1（成功率 90%｜幸运修正 +2）` + `✅ 成功！攻击 12 → 13`；低级强化石 −1、金币 −100、等级 +1 | 【强化定稿】L34-37 / L44 / L214 / L103-105 |
| TC-02 | `/强化 铁剑+5`（持有 6 石+600 币，roll 成功） | 消耗按**目标等级 +6**：中级强化石 ×6 + 金币 600（2c3a §1.3 表）；`⚙️ 铁剑+5 → +6（成功率 50%｜幸运修正 -1）` + `✅ 成功！` + 数值变化行 | L40-41 / L37 / L103-105 / 2c3a §1.1 |
| TC-03 | 低等级段失败（`/强化 铁剑+2`，roll 失败） | `❌ 强化失败，装备保持不变`；等级仍 +2；**材料照扣**（低级石 3 + 金币 300） | L38 / L53 / L155 |
| TC-04 | 高等级段失败（`/强化 铁剑+5`，roll 失败） | `❌ 失败！强化等级 -1（铁剑+5 → +4）`；等级降到 +4；材料照扣（中级石 6 + 金币 600） | L41-42 / L54 / L155 |
| TC-05 | luck_affects=true，幸运 25（√=5），基础 55% | 实际率 60% → 显示 `成功率 60%（基础 55% + 幸运 5%）`（L90 拆分格式，含正/负修正与“幸运修正已关”两态各验一次） | L87-90 / L159 |
| TC-06 | 结果消息双提示 | 成功分支必含数值变化行（`攻击 12 → 13`，delta 按 2c3a §8 values）；失败分支必含惩罚类型文案（`装备保持不变` / `强化等级 -1`） | L45 / L37 / L38 / L42 |

### B /强化信息

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-07 | `/强化信息 铁剑+3`（精良） | 📖 输出：`铁剑+3（品质：精良｜强化上限 +8）` + `→ +4 成功率：70%（基础 70% + 幸运 0%）` + `→ +4 消耗：中级强化石 ×4 + 金币 ×400 ｜ 持有：…`；**零扣除**（金币/石/背包差分 = 0） | L216 / L90 / L113-114 / 2c3a §1.3 |
| TC-08 | `/强化信息 传说装备`（已达 +12） | 成功率/消耗行替换为 `已达强化上限（+12）`；文案与 /强化 达顶同源 | L115 / L113-114 |
| TC-09 | `/强化信息` 未持有装备 / 模糊名 | `未找到「<名>」` 或候选列表；只读零副作用（无任何写入） | L216（语法）/ P-05 |

### C /强化保护

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-10 | `/强化保护 铁剑+5`（持保护石 ≥1，roll 成功） | 流程与 /强化 一致（同成功率/消耗）；**成功不消耗保护石**；石/币按 +6 扣、等级 +1 | L68 / L215 |
| TC-11 | `/强化保护 铁剑+5`（持保护石 ≥1，roll 失败=高段） | `❌ 失败！保护石抵消掉级（消耗 1 颗，铁剑+5 保持不变）`；保护石 −1、等级不变、石/币照扣（中级石 6 + 金币 600） | L68 / L215 / L54 |
| TC-12 | `/强化保护 铁剑+2`（低等级段 roll 失败） | 无惩罚 → **不消耗保护石**；等级不变；材料照扣；模板同普通失败（无保护追加行） | L53 / L68 / 定版（有惩罚才扣） |
| TC-13 | `/强化保护 铁剑`（无保护石 / 保护石未配置） | 拒绝：`需要 保护石 ×1（来源：炼金产出 / 商店 / 活动奖励）`（未配置 → `强化保护 未启用`）；零扣除 | L68-69 / L188 / L215 |

### D 强化达顶

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-14 | `/强化 传说+12` | `已达强化上限（+12）`；**零消耗**（石 0/金币 0/等级不变）；不 roll、不结算、不写日志 | L115 / L113-114 |
| TC-15 | `/强化 精良+8` 与 `/强化信息` 联动 | 两指令共用 max_by_rarity 同一来源：/强化 拦截达顶、/强化信息 同文案显示上限；上限键=品质四档枚举（normal/fine/epic/legendary） | L113-115 / L154 / L204 |

### E 原子防双扣

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-16 | 同一 message_id 重发（网关重试投递 2 次） | 幂等命中：第 2 次命中返回第 1 次缓存结果（原样重发）；enhance_log 仅 1 条结算记录；石/币/等级差分 = 单次 | L44（防双扣）/ F-03~05 |
| TC-17 | 结算事务中途热重载（扣材料与写等级之间 kill 进程） | 未提交事务回滚 → 无幽灵扣减/无半升级；重发后重新执行，最终状态 = 完整单次结算（扣一次升一级或按新 roll） | L44（免疫热重载）/ F-01/02 |
| TC-18 | 连点 `/强化 铁剑` 3 次（无标） | 三次按序独立事务各结算一次（+0→+1→+2→+3）；每次扣减与等级一致；同窗第 4 条被框架 7 章频率控制拦截 | L230 / L44 |
| TC-19 | 过期的带标指令（实际 +3 时发 `/强化 铁剑+2`） | GU-04 拒绝：`铁剑 当前 +3，请确认强化目标`；零扣除；等级标记锚定防误触/防双扣认知错位 | L10（标记语义）/ L44 |

### F 指令参数解析

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-20 | `/强化 铁剑+2`（合法标记） vs `/强化 铁剑`（无标记） | 两者均正常结算；带标仅作等级校验（实际等级≠2 拒绝），**目标恒为当前+1**，标记不覆盖目标 | L10 / L214 / P-02 |
| TC-21 | `/强化 铁剑*2` | `强化不支持批量强化（成功率随机）`；对照 `/锻造 铁剑*2` 批量允许——随机性不同源 | L10 / L27 |
| TC-22 | 非法参数族：`/强化 铁剑 Ⅱ`（空格）｜`铁剑+`｜`铁剑+abc`｜`铁剑++2` | `装备名不含空格` / `等级标记须为非负整数`；全部零副作用 | P-01 / P-02 / L10 |
| TC-23 | 歧义前缀（`铁剑Ⅰ` / `铁剑Ⅱ` 同前缀 `铁剑`） | 输出候选列表（每候选附 当前+N/上限 1 行）+ 用法；**不自动选择、不自动强化** | P-05 / L214 |

---

## 七、登记与对接（落点确认）

| 落点 | 内容 | 引用 |
|---|---|---|
| /强化 指令 | §一 流程（守卫 GU-01~05 + 成功/失败分级 + §1.5 消息模板）＋ §四 原子防双扣；数据读 enhance.json + 2c3a 数值 | 【强化定稿】L214 / L34-45 |
| /强化信息 指令 | §二 只读查询（+N/成功率/上限/消耗预览）；数据源=存档实例 + enhance.json + 2c3a 消耗表 | L216 / L90 / L113-114 |
| /强化保护 指令 | §三 保护石流程（GU-06 足额校验 + 有惩罚才扣免掉级 + 模板） | L215 / L68 / L188 |
| 玩家存档 | 物品实例等级字段（装备卡 +N L210 同源）/ enhance_log 结算日志（message_id UNIQUE 幂等）/ 属性快照（2c3a §8 values） | L44 / L210 / L184-187 |
| 结算底座 | SQLite BEGIN IMMEDIATE 单事务 + message_id 幂等（F-01~07，与锻造 2c2b §1 共用） | L44 / L230 |
| 框架 7 章 | 频率控制（强化/锻造共用通道） | L230 |
| 数据层 | 成功率曲线/消耗/上限/幸运公式/碎装备/校验器/编辑器 → **2c3a**（本文只接线显示与校验入口） | L196-210 |
| 不展开项 | 转移（transfer_allowed=false 无指令 L127-129）、碎装备（shatter_mode=false L60-63）、等级门槛（level_gated=false L139-142）——流程侧无入口 | L127-142 |

---

*设计依据：强化系统设计定稿 v1.0（235 行，全篇已读；§二流程 L32-45、§三失败分级 L49-71、§4.4 幸运 L85-91、§六上限 L110-120、指令表 L212-217 为直接依据）+ 细化 2c3a（数值字段级定义，本文流程侧接线）+ 框架 v2.8（7 章频率控制 / SQLite 结算底座）+ 用户拍板 5 点（L6）+ 编辑器/校验器一致性（L196-210）——本文对应 2c3b 交互流程层；数值展开见 2c3a*