# 细化规划 · 1g2：回合时序与拦截链挂载

> 引用约定：【数值层】= 战斗数值层设计定稿.md；【效果】= 效果系统设计定稿.md；行号以两份定稿原文为准。
> 依据：【数值层】§一 判定顺序与公式总览（L14-68）；【效果】§3.4 伤害拦截链总顺序（L133-148）+ §3.1 冷却/时点（L94-97）+ §4.1 持续双维结算时点（L174-177）。

---

## 一、回合时序（回合状态机，写死不可配）

> 回合状态机边界在本层写死（【数值层】L40-42），对齐技能派生定稿 §十二 回合时序「回合开始 dot/控制结算 → 玩家行动 → 怪物反击 → 回合结束 tick」（【数值层】L41），并与效果系统 §3.4 拦截链「⑦扣血 → ⑧死亡判定」衔接（【数值层】L42）。

### 1.1 先手 / 后手定义

| 项 | 定义 | 引用 |
|---|---|---|
| 先手 | 玩家行动（含连段/多段） | 【数值层】L41「玩家行动 → 怪物反击」、L49「先手行动结算」 |
| 后手 | 怪物反击 | 【数值层】L41、L54-57「后手行动判定」 |
| 特例 | 被先手击杀的怪物本身不执行反击（写死） | 【数值层】L57 |

### 1.2 行动顺序（一回合完整时序）

```
① 回合开始：dot/持续伤害结算（拦截链扣血后即查）＋ 控制结算   【数值层】L45
② 死亡判定（回合开始即死）                                  【数值层】L46-48
   · 玩家死 → 战斗失败标记（结算时机见 ⑨）
   · 怪物死 → 战斗胜利标记
③ 行动阶段：先手行动结算（含连段/多段）                      【数值层】L49
④ 击杀判定（每次扣血后立即检查，非等整回合）                 【数值层】L50-53
   · 连段套中击杀 → 后续段数照常执行（鞭尸浪费，套内稳定哲学）；套完结后目标退出战场，
     下一回合自动消失（不结算其行动）                        L51-52
   · BOSS/最后目标死亡 → 战斗立刻结束（⑨），不清算后续段数   L53
⑤ 后手行动判定                                             【数值层】L54-57
   · 目标未死 → 正常执行                                    L55
   · 目标已死 → 后手行动仍推进但无目标：默认普攻兜底（打向剩余目标；无剩余目标则空挥/待机；
     可配 skip=直接跳过）                                   L56-57
⑥ 回合结束 tick（顺序见 1.3）                               【数值层】L58
⑦ 同回合互杀判定（双方死亡标记同轮并存）                     【数值层】L59-63
   · 结果默认 draw=平局，可配 player_loss=玩家败              L59
   · 判定基准二选一：order「先判定后手」（默认）/ hp_ratio「剩余 HP 比」 L60-63
⑧ 战斗结束判定：胜负/奖励/掉落/快照清理统一在当轮事件末尾结算一次 【数值层】L64-65
   · BOSS/最后目标死亡 → 立刻结束（未执行的后续事件作废）      L65
⑨ 中断快照只落回合边界（回合开始前 / 回合结束 tick 后）        【数值层】L66-67
   · 回合内不落快照 → 死亡结算天然先于快照写入，无「恢复在死亡判定前/后」歧义
```

行动阶段内部顺序补充：控制状态裁决（混乱=行动变随机普攻、沉默、睡眠，【效果】L195）；先手行动每段扣血后立即查击杀判定（L50）再进下一段/后手。

### 1.3 回合结束 tick 顺序

> 总纲：【数值层】L58「回合结束 tick：持续回合扣减/冷却递减/效果衰减（效果系统 §3.1）」；
> 精口径（效果系统时点）：衰减=携带者行动结算后（非 tick 内），持续回合=回合结束 tick 扣减，次数=触发时扣减（【效果】L174-177、L243、L245）。

| # | tick 项 | 时机/规则 | 引用 |
|---|---|---|---|
| 1 | 效果衰减 | 携带者每次行动结算后衰减一次（halve/decrement/trigger 三型），非双方都衰减；与回合时序对齐 | 【效果】L177、L243、L245 |
| 2 | 持续回合扣减 | 回合结束 tick 统一扣减；-1 维不被安全区清除 | 【效果】L176、L173 |
| 3 | 冷却递减 | 技能/特效冷却为回合单位，剩余回合入快照 effect_cooldowns | 【数值层】L58；【效果】L94-97 |
| 4 | 伤害吸收回补 | 回合末按记录实伤回复（含护盾挡的伤害） | 【效果】L129、L139 |
| 5 | 再生/回合末回复 | tpl_regen 时点=回合末；回蓝 on_turn_end | 【效果】L360、L364 |
| 6 | dot（如配 tick=turn_end） | dot tick 可配回合开始/结束，默认按回合开始结算（1.2 ①） | 【效果】L286、L421；【数值层】L45 |

### 1.4 边界规则速查

- 回合开始即死 → 跳过其本回合行动（【数值层】L46-48）
- 击杀判定：每次扣血后立即（【数值层】L50）；互杀基准 order/hp_ratio（L60-63）
- 战斗结束统一在当轮事件末尾结算（L64-65）；快照仅落回合边界（L66-67）

---

## 二、拦截链 8 阶段挂载点

### 2.1 拦截链整体挂载位置

- 挂载于伤害结算管线第 6 步（判定顺序写死）：命中 → 会心 → 格挡 → 双通道（物理/元素独立取整后相加）→ 防御率 → **伤害拦截链** → 扣血（【数值层】L16）
- 输入 = 总伤害⑥：`max(1, floor((物理+元素)×格挡×防御指令×乱数))`，乱数∈[0.9,1.1]（【数值层】L34-35）
- 链序以效果系统 §3.4 为准（【数值层】L36）；链序本身可配数组，但「死亡判定在扣血前」等异常顺序仅警告（【效果】L135）
- 第二个链入口：回合开始 dot 结算复用同一拦截链（拦截链扣血后即查死亡）（【数值层】L45）

### 2.2 8 阶段挂载明细（每阶段：调用时机 / 输入 / 输出）

| 阶段 | 调用时机 | 输入 | 输出 | 引用 |
|---|---|---|---|---|
| ① 减伤/减免（mitigation：普攻/技能/异常/暴击/属性） | 链第一步，总伤害⑥ 之后 | 总伤害（格挡/防御指令/乱数已结算） | 减伤后伤害（tpl_mitigation 模板，scope 可参数化） | 【效果】L136、L358；【数值层】L36 |
| ② 护盾先扣（shield 吸收量） | ① 之后 | 减伤后伤害 | 盾吸收后的剩余伤害（护盾挡的伤害计入④记录） | 【效果】L137、L357、L129 |
| ③ 反弹 | ② 之后 | 实伤 | 按 % 减伤并反弹等值伤害给对方；反弹伤害不触发反弹；反弹=派生伤害，不触发对侧反击/反弹、不进 on_attack | 【效果】L138、L155-157 |
| ④ 伤害吸收 | ③ 之后 | 反弹后实伤 | 记录受伤害量 → 回合末回复（即 1.3 tick#4） | 【效果】L139、L129 |
| ⑤ 致命免疫/非致命免疫 | ④ 之后 | 当前伤害 | 全额免伤（伤害清零），消耗 triggers 计数；与续行并存时免疫先判；免死类默认每场 1-3 次（可配） | 【效果】L140、L148、L74 |
| ⑥ 战斗续行 | ⑤ 之后 | 当前伤害 | 锁 1 血（触发瞬间回复，可配）；不死续行=无敌（是否挡 debuff 可配） | 【效果】L141、L130 |
| ⑦ 扣血 | ⑥ 之后 | 最终伤害值 | HP 实际扣减 → 随后写入伤害构成统计（per-action，每段一条） | 【效果】L142；【数值层】L314 |
| ⑧ 死亡判定 | ⑦ 之后立即 | 扣血后 HP | 死亡标记（跳过其行动 / 战斗结束标记）→ 衔接回合状态机②④⑧ | 【效果】L143；【数值层】L42、L46-48、L50-53 |

### 2.3 挂载点汇总（8 链内 + 3 衔接 = 11）

| 类型 | 挂载点 | 引用 |
|---|---|---|
| 链内 ×8 | ①减伤 ②护盾 ③反弹 ④吸收 ⑤免疫 ⑥续行 ⑦扣血 ⑧死亡判定（各 1 个挂载点） | 【效果】L136-144 |
| 衔接 ×3 | A. 常规伤害链入口=管线第 6 步（总伤害⑥之后） | 【数值层】L16、L36 |
| | B. 回合开始 dot 结算复用链入口（扣血后即查） | 【数值层】L45 |
| | C. ⑦扣血后伤害构成统计收集器（每段一条写入） | 【数值层】L314 |
| 链尾联动 | ⑧死亡判定同时为回合状态机衔接点（击杀判定/战斗结束） | 【数值层】L42、L50 |

---

## 三、伤害结算流程时序图（命中→会心→格挡→乘区→拦截链→扣血）

```
攻击动作结算（先手/后手行动内，连段每段独立走全管线）
  │
  ▼
① 命中判定 ──────────────── 【数值层】L16、L21-22
   命中率 = clamp(专注/(专注+K_hit×对方敏捷), 10%, 95%)，K_hit=0.2
   miss → 伤害 0（提示"未命中"）→ 本段结束
   hit ↓
② 会心判定 ──────────────── 【数值层】L16、L23-24
   P=√幸运÷2；r≤P 高级×2.2 / r≤P×3 中级×1.7 / 其余低级×1.3
   倍率 +0.05×超会心Lv；物理+元素共用一次判定
   ↓
③ 格挡判定 ──────────────── 【数值层】L16、L25
   格挡率 = min(40%, 专注/(专注+150))；成功 → 总伤害 ×0.5；魔攻击无视格挡
   ↓
④ 乘区（双通道独立取整后相加；防御率在本步）── 【数值层】L16、L26-33
   物理通道 = max(1, floor(攻击值×技能倍率×类型克制×会心倍率×防御系数×怪物防御率))
             攻击值=力量（斩/打/突）｜智力（魔）；防御系数=K_def/(有效体质+K_def)，K_def=100；
             有效体质=体质×(1−破防%)（打类型内置破防 20%）
   元素通道 = max(1, floor(装备元素攻击×技能元素倍率×元素弱点×会心倍率×元素减伤系数×怪物防御率))
             元素减伤系数=K_def/(元素抗性+K_def)
   通道末 floor（保留双通道灵魂，deep_floor 开关保留）L38
   ↓ 相加
⑤ 总伤害 ──────────────── 【数值层】L34-35
   max(1, floor((物理+元素)×格挡(×0.5)×防御指令(×0.5)×乱数))
   乱数∈[0.9,1.1]（一次判定，双通道共用）
   ↓
⑥ 伤害拦截链 ────────────── 【数值层】L36；【效果】L133-148
   ①减伤 → ②护盾 → ③反弹 → ④吸收 → ⑤免疫 → ⑥续行 → ⑦扣血 → ⑧死亡判定
   （顺序以效果系统 §3.4 为准；链序可配数组，异常顺序仅警告 L135）
   ↓
⑦ 扣血 ──────────────── 【效果】L142；【数值层】L314
   HP 实际扣减；伤害构成统计按"段"写入（source/通道值/会心档/格挡/破防/穿透/弱点/最终值）
   ↓
⑧ 死亡判定（每次扣血后立即检查）── 【效果】L143；【数值层】L50-53
   未死 → 下一段 / 后手行动 / 回合结束 tick（L58）
   连段套中死亡 → 后续段数照常（鞭尸）→ 套完结目标退出战场，下一回合自动消失（L51-52）
   BOSS/最后目标死亡 → 战斗立刻结束，不清算后续段数（L53、L65）
```

> 备注：整条管线判定顺序写死不可配（【数值层】L14、L42；校验器规则 7「管线顺序不可改」【数值层】L262）；拦截链内部顺序为可配数组（【效果】L135）。
