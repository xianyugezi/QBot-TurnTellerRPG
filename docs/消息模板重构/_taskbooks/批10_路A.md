# 任务书 · 批10·路A（探索/地图/时间天气 16 键）

> 2026-09-12 · 消息模板重构战役 · 执行者：子 agent（本任务书 + `docs/消息模板重构/00_方案与规范_v1.md` 为唯一指令源）
> **先读** `<worktree>/docs/消息模板重构/00_方案与规范_v1.md`（结构化行 ≤14 全角 / 免斜杠 / 少｜多换行 /
> 错误行 ❌+原因(+下一步) / 拆行优先序 / 大数值独立行 / 零装饰 emoji / 介绍类可豁免）再开工。

## 1. 范围（本路 16 键，全部在 `qbot_rpg/core/templates/explore_tpl.py`）

explore_monster_header, explore_monster_empty, explore_rest_ok, explore_rest_cooldown,
explore_rest_fail, explore_rest_fail_reason, explore_rest_extra_arg, explore_rest_not_wired,
explore_rest_engine_error, explore_map_title, explore_map_row, explore_map_tail, explore_map_empty,
worldtime_now, weather_now, worldtime_disabled

**每键必做**：读旧文案拿到「信息要素 + 触发场景 + 占位符集合」，然后**按规范全新撰写**（旧文案不作参考）。
占位符沿用；`tpl_of(` 调用点的键名与参数名**不得改**；风味/介绍类文本允许自然折行 → 在 fragment
`prose_keys` 逐条登记理由。**只动本路这 16 键**，explore_tpl.py 里其他键（属别的批）不要碰。

## 2. 工作清单（每步都落盘）

1. **第一件事**：写 `<worktree>/docs/消息模板重构/_fragments/b10a.json`
   （`{"rail":"b10a","templates":{…}}`）→ commit。
2. 合表：`python3 scripts/tpl_merge_fragments.py docs/消息模板重构/_fragments/b10a.json`
3. 清分区：从 `explore_tpl.py` 删除已迁 16 键（其余键保留）；`content/test_demo/templates.json` 同 key 同步（缺则补）。
4. 代码/测试同步：`grep -rn "<key>" qbot_rpg/ tests/ scripts/` → 常量别名/旧文案断言/docstring 逐处更新；
   **不得**为过测试改回旧文案。
5. 自校验：checker（本路键 0 FAIL）+ `test_template_table` + `test_emoji_discipline` + 定向本模块测试
   + **最后跑一次全量** `pytest tests/ -q`（报计数）。
6. `.venv/bin/ruff check <改动文件>` 干净；commit（`feat(模板·路A): …`）。**禁止 push / 禁止动 main**。

## 3. 报告（回复正文**内联**）
- fragment 路径 + 16 键新文案（JSON 全量内联）；合表结果；改动文件清单；checker 输出；
  定向+全量测试计数原文；未完成/存疑；commit hash
- 预算纪律：临近上限优先 (a) fragment 落盘 (b) 代码 commit (c) 如实列未完成；禁止谎报/半成品。
