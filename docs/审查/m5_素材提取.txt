===== 细化_3d_消息模板规范.md =====

【模板注册表】
  | TPL-01 | 前缀-默认 | `Lv[等级].[玩家名] -[称号]-` | 默认格式，格式模板占位符自由组合 | 【前缀】L22/L43/L57 |
  | TPL-02 | 前缀-无称号 | `Lv[等级].[玩家名] - -` | 无称号默认：`empty_title_text` 默认 "-" | 【前缀】L35/L90/L61 |
  | TPL-03 | 前缀-空称号整段省略 | `Lv[等级].[玩家名]` | `hide_when_empty:true`：省略 [称号] 及其相邻装饰符（`-`），尾随空格清理 | 【前缀】L35/L46/L91 |
  | TPL-04 | 前缀-空称号仅隐本体 | `Lv[等级].[玩家名] - -` | `empty_title_text:""`（不带 hide_when_empty）：仅隐藏占位符本体，装饰符保留 | 【前缀】L47/L92 |
  | TPL-05 | 前缀-作者可配范例① | `[职业] Lv[等级].[玩家名]` | 职业版示例；占位符任意顺序/重复/单用 | 【前缀】L130/L74 |
  | TPL-06 | 前缀-作者可配范例② | `【[群名]】[玩家名]` | 群名片版示例；[群名] 私聊输出 "私聊" | 【前缀】L133/L84 |
  | TPL-07 | 列表-条目行 | `{序号}. {条目名} {关键数值}` | 列表类回复的条目骨架（如 `1. 铁剑 攻击+12`） | 本规范 D-02 |
  | TPL-08 | 列表-页脚 | `— 第 {X}/{Y} 页 · 共 {N} 条 · 输入 /{指令} {页码} 翻页 —` | 多页列表的页脚格式；单页不输出 | 本规范 D-02 |
  | TPL-09 | 折叠-省略行 | `…（其余 {N} 条已折叠，输入 /{指令} {页码} 查看）` | 正文超长时的尾部折叠行，保留页码指引 | 本规范 D-03 |
  | TPL-10 | 标记-成功 | `✅ {动作} 成功：{结果}` | 功能成功结果行首标记；如 `✅ 锻造成功：铁剑 ×1` | 本规范 D-01 |
  | TPL-11 | 标记-失败 | `❌ {动作} 失败：{原因}` | 功能失败结果行首标记 | 本规范 D-01 |
  | TPL-12 | 错误-指令出错 | `❌ 指令不正确：{原指令片段}。输入 /帮助 查看可用指令。` | 未识别指令/参数非法统一文案 | 本规范 D-04（对齐【前缀】L114 人话模板） |
  | TPL-13 | 错误-条件不满足 | `❌ 条件不满足：{条件名}（当前 {当前值}，需要 {需求值}）` | 等级/前置任务/时段等条件类失败统一文案 | 本规范 D-04 |
  | TPL-14 | 错误-资源不足 | `❌ 资源不足：需要 {资源}{数量}，当前 {当前值}` | 金币/物品/体力等资源类失败统一文案，**不扣款无副作用** | 本规范 D-04（失败无副作用 = 补白收敛，见 §5.3） |
  | TPL-02 |
| `hide_when_empty:true` | `Lv35.阿伟` | 整段省略 [称号] 及其**相邻装饰符**，尾随空格清理 |
  | TPL-04 |

- **相邻装饰符定义**：紧贴 [称号] 占位符、无空格分隔的纯符号 token（默认格式中的左右 `-`）；整段省略时一并移除（【前缀】L91）。
- **尾随空格清理**：整段省略后若行尾残留空格（如 `Lv3
  | TPL-10 |
| ❌ | 功能**失败** | 结果行首；三态失败（指令出错/条件不满足/资源不足）优先走错误模板（§五） |

【TC 清单】
  TC-01 | 玩家阿伟 35 级佩戴「斩龙者」（成就奖励），触发战斗回复 | 回复首行 = `Lv35.阿伟 -斩龙者-`（TPL-01）；正文从第二行开始；一轮战斗仍 1 条消息
  TC-02 | 同玩家卸下称号（无称号，默认配置） | 首行 = `Lv35.阿伟 - -`（TPL-02，empty_title_text 默认 "-"）
  TC-03 | 内容包设 `hide_when_empty:true`，玩家无称号 | 首行 = `Lv35.阿伟`（TPL-03：整段省略 [称号] 及相邻 `-`，尾随空格清理）
  TC-04 | 内容包设 `empty_title_text:""`（不带 hide_when_ | 首行 = `Lv35.阿伟 - -`（TPL-04：仅隐占位符本体，装饰符保留）
  TC-05 | 作者改 format 为 `[职业] Lv[等级].[玩家名]` 或 `【[群名 | 按新格式渲染占位符（TPL-05/TPL-06）；私聊下 [群名] 输出 "私聊"；任意组合/顺序/单占位符均生效
  TC-06 | format 包含未知占位符 `[未知X]` | 原样输出 `[未知X]` + 校验器黄提示「模板里有不认识的东西」，**不拦截加载**，其余占位符正常渲染
  TC-07 | 商店条目 12 条，`/商店` | 第 1 页渲染 TPL-07 条目 1-5 + 页脚 TPL-08（第 1/3 页 · 共 12 条）；**单页不超 5
  TC-08 | `/商店 2` | 渲染第 2 页条目 6-10 + 页脚（第 2/3 页）；不重渲染前缀以外旧页内容
  TC-09 | `/商店 4`（超总页数 3） | 夹取到最后一页：渲染第 3 页 + 提示「已到最后一页」（对齐 M4 裁决「页码越界夹取」，4f RUL-16）
  TC-10 | `/商店 0` / `/商店 abc` | 0/负数/非数字 → 输出 TPL-12 统一报错，不崩溃不静默
  TC-11 | 条目 3 条（≤5），`/商店` | 渲染 3 条 + **无页脚**（单页省略页脚，防刷屏）
  TC-12 | 页脚文本逐字校验 | 页脚 = `— 第 {X}/{Y} 页 · 共 {N} 条 · 输入 /{指令} 页码 翻页 —`（TPL-08 原样，
  TC-13 | 前缀渲染超长：默认 40 字符限制（称号/群名很长） | 前缀截断至 `prefix_max_len` + 黄提示「前缀过长已截断」（归属发起指令所在群）；**正文不受影响照常输
  TC-14 | 战斗结算流水 20 条（未分页场景） | 正文渲染前 5 条 + 折叠行 TPL-09：`…（其余 15 条已折叠，输入 /战斗记录 2 查看）`；折叠内容可在后
  TC-15 | 正文长度判定：前缀 38 字符 + 正文 200 字符 | 防刷屏判定只计正文（200 ≤ 正文上限）；**前缀不计入**长度限制
  TC-16 | 锻造成功 | 结果行首 = `✅ 锻造成功：铁剑 ×1`（TPL-10），其后纯文本无装饰 emoji
  TC-17 | 锻造失败（材料不足以外原因，如成功率为 0 失败） | 结果行首 = `❌ 锻造失败：…`（TPL-11）；资源类失败走 TPL-14
  TC-18 | **emoji 全量扫描**：渲染管线输出样本 grep 禁用清单（🔥🟢💥⚔️🛡 | 命中数为 0；**含定稿示例 L28-31 的 🔥/🟢 不出现于任何实现渲染输出**（降级为纯文本/✅❌）；唯二允许的 
  TC-19 | 前缀区扫描：全体 TPL-01~06 渲染样本 | 前缀不包含任何 emoji（含 ✅/❌），纯文本身份标识
  TC-20 | 输入未注册指令 `/攻撃` | 输出 TPL-12：`❌ 指令不正确：/攻撃。输入 /帮助 查看可用指令。`（首行带前缀，正文限 3 行内）
  TC-21 | 锻造等级不足（2 级需 5 级） | 输出 TPL-13：`❌ 条件不满足：锻造等级（当前 2 级，需要 5 级）`；条件名为可读中文
  TC-22 | 商店购买金币不足（500 需 120） | 输出 TPL-14：`❌ 资源不足：需要 金币 500，当前 120`；**不扣款不消耗不写存档**（余额不变，无副作用
  TC-23 | 战斗轮内多行回复（行动+反击+状态行） | 前缀只出现在该消息**首行**；后续每行均无前缀
  TC-24 | 系统公告/群广播（show_on_system 默认 false） | 默认**不加前缀**；设为 true 后同样加前缀
  TC-25 | per_channel 配置 group / private / all | 渠道限定正确：group 仅群聊生效、private 仅私聊生效、all 都生效
  TC-26 | 玩家发送 `Lv35.阿伟 -斩龙者-` 同款文本（含前缀样式）作为指令 | 指令解析用原文，**前缀样式文本不影响解析**；渲染层产物不进解析器/条件键/存档


===== 细化_5e_战斗战报格式.md =====

【BREP 注册表】
  | BREP-01 | 战报骨架 | 第 1 行前缀 + 第 2 行起正文 + 末行操作提示/状态行 | 单回合单条消息三段式骨架（骨架示例见 §1.5） | 【前缀】L34/L82/L31 |
  | BREP-02 | 玩家-攻击命中 | `✅ 你{攻击动作}{目标}，造成 {伤害} 伤害（{目标} {剩余HP}/{最大HP}）` | 普攻/技能攻击命中通用；`{目标}` **可选**（省略时输出 `你{攻击动作}，造成 …`，对齐
  | BREP-03 | 玩家-未命中 | `❌ 未命中：{目标} 闪过了你的{攻击动作}（{目标} {HP}）` | miss → 伤害 0，提示「未命中」 | 【数值层】L24 |
  | BREP-04 | 玩家-会心/格挡附注 | `（会心·{档} {倍率}）` / `（被格挡，伤害减半）` | 附注行拼接到 BREP-02 行尾；会心档 high/mid/low ×2.2/1.7/1.3；格挡成功 ×0.5 | 【数
  | BREP-05 | 玩家-进入防御 | `✅ 你进入防御姿态（本回合受到伤害减半）` | 防御指令 ×0.5（总伤害公式环节） | 【数值层】L36 |
  | BREP-06 | 玩家-防御受击 | `✅ 你防御了{目标}的{攻击动作}，受到 {伤害} 伤害（HP {剩余}/{最大}）` | 防御指令生效的受击行（玩家视角成功免半） | 【数值层】L36/L58-59 |
  | BREP-07 | 玩家-技能释放 | `✅ 你施放{技能}：{效果描述}（{资源变化}）` | 非攻击类技能（治疗/增益/控制/护盾）；示例：`✅ 你施放治疗术：回复 30 点 HP（MP 22/60）`（MP 消耗 8，经【数值层】
  | BREP-08 | 玩家-状态与资源行 | `{状态项} {旧值}→{新值}` 差分行（MP/印记/连段/护盾等） | 默认只渲染**实际变化**的轴（D-5D）；状态数默认前 5 个，超出追加「还有 N 个状态」（开发规则 L509）
  | BREP-09 | 操作提示行 | `你 {HP}/{最大} | {目标} {HP}/{最大} → /攻击[技能] /道具 /防御 /逃跑` | 战报末行；多怪时目标取战场第一个存活怪 |
  | BREP-10 | 怪物-反击命中 | `❌ {怪物}{攻击动作}，你受到 {伤害} 伤害（HP {剩余}/{最大}）` | 后手行动命中；示例：`❌ 史莱姆反击，你受到 4 伤害（HP 21/30）` | 【前缀】L30（降级）；【数
  | BREP-11 | 怪物-攻击未命中 | `✅ {怪物}的攻击被你躲开（HP {剩余}/{最大}）` | 后手 miss 对玩家是成功 → ✅；伤害 0 | 【数值层】L24/L58-59 |
  | BREP-12 | 怪物-意图预告 | `{怪物} 蓄力中（下回合发动「{招名}」）` | 蓄力/读招预告固定句式（D-5E）；玩家据此决策打断技/霸体派生 | 【数值层】L153/L292 |
  | BREP-13 | 怪物-特殊行动 | `{怪物} {特殊行动}（{效果变化}）` | 狂暴/召唤/印记等 HP 阈值触发行为；示例：`史莱姆王 进入狂暴状态（攻击提升）` | 【数值层】L149/L292-294 |
  | BREP-14 | 拦截链效果行 | `{盾} 吸收了 {n} 点伤害` / `反弹 {n} 伤害给{目标}` / `免疫了{效果}` | 拦截链（减伤→护盾→反弹→吸收→免疫→续行→扣血）各环节触发的战报行 | 【数值层】L38/L2
  | BREP-15 | 结算-击杀 | `✅ 你击败了{怪物}！` | 每次扣血后立即查击杀（非等整回合）；击杀行紧跟伤害行 | 【数值层】L54 |
  | BREP-16 | 结算-玩家死亡 | `❌ 你倒下了…` | 玩家死亡标记 → 战斗失败标记 | 【数值层】L50-51 |
  | BREP-17 | 结算-胜利 | `✅ 战斗胜利！` | 怪物死 → 战斗胜利标记；当轮事件末尾统一结算 | 【数值层】L52/L68 |
  | BREP-18 | 结算-失败 | `❌ 战斗失败：你被{怪物}击败了` | 玩家死 → 失败；当轮事件末尾统一结算 | 【数值层】L51/L68 |
  | BREP-19 | 结算-互杀平局 | `双方同归于尽，战斗以平局结束` | 同回合互杀默认 draw=平局（可配 player_loss=玩家败） | 【数值层】L63 |
  | BREP-20 | 结算-经验与掉落 | `✅ 获得 经验 {n}、金币 {n}、{素材}×{n}` | 只在战斗结束消息输出一次；金币=怪物等级×基准 | 【数值层】L68/L171 |
  | BREP-21 | 连段-段行 | `第 {N} 段：{动作} 造成 {伤害} 伤害（{目标} {剩余HP}/{最大HP}）` | 多段每段独立一行（D-5C），每段独立取整；段号 N 即收集器 seg 字段 | 【数值层】L53/L1
  | BREP-22 | 连段-结算行 | `连段 {N} 段已结算（{备注}）` | 套完结/鞭尸（目标已倒下后续段照常）/BOSS 提前结束（不清算后续段数）/派生倍率封顶附注（≤1.5×） | 【数值层】L55-57/L69/L133 
  | BREP-23 | 战斗开始 | `与{怪物}的战斗开始！{怪物} {HP}/{最大HP}` + （可用）意图/弱点情报行 | 首行动作前输出；弱点情报可选（类型/元素弱点 ×1.3） | 【数值层】L106-107/L149 |
  | BREP-24 | 战斗结束-汇总 | `战斗结束：{胜负结果}｜回合数 {N}｜{明细入口指令}` | 与 BREP-17/18/19/20 合并输出；明细入口指向 BREP-25 | 【数值层】L68/L147 |
  | BREP-25 | 战报详情/木桩明细块 | 条目行 `{来源} {总伤害}（{占比}%）` + 页脚 TPL-08 / 折叠行 TPL-09 | 木桩战后明细默认展示；普通战斗默认不展示明细（收集器仍在跑）；复用 5 条/页分页规则 

【TC 清单】
  TC-14 | ---

## 一、战报总则

### 1.1 追溯总览（子系统 / 定稿落点  | 子系统
  TC-01 | 阿伟 35 级佩戴「斩龙者」攻击史莱姆（命中+被反击） | 单回合输出**1 条消息**：第 1 行 = `Lv35.阿伟 -斩龙者-`（前缀），其后为攻击行+反击行+操作提示行；
  TC-02 | 前缀渲染三态回归 | 有称号=`Lv35.阿伟 -斩龙者-`；无称号默认=`Lv35.阿伟 - -`；hide_when_empty:true
  TC-03 | 前缀长度：称号+群名超长令前缀 >40 字符 | 前缀截断至 prefix_max_len + 黄提示「前缀过长已截断」；**正文战报行照常完整输出**；防刷屏判定只计正
  TC-04 | **emoji 全量扫描**：战报渲染样本 grep 禁用清单（🔥🟢💥⚔️🛡️✨ | 命中数 = 0；【前缀】L29 的 🔥 与 L30 的 🟢 **不出现在任何实现层战报输出**（降级为 ✅/❌ 纯文本）
  TC-05 | 排版符号扫描：竖线 `\ | `、ASCII 箭头 `→`、`×`、`/`
  TC-06 | BOSS 战（史莱姆王）全程消息流 45 回合 | 总消息数 = 45（单回合单条）；单条消息渲染行数 ≤16 行（超限折叠）；状态行只渲染变化的资源轴
  TC-07 | `/攻击 火球术` 命中（史莱姆 25→7） | 首行后输出 `✅ 你施放火球术，造成 18 伤害（史莱姆 7/25）`（BREP-02，`{目标}` 省略同 3d D-
  TC-08 | 攻击 miss | 输出 `❌ 未命中：史莱姆 闪过了你的攻击（史莱姆 25/25）`（BREP-03），伤害 0 不扣血
  TC-09 | 会心/格挡分支 | 会心跳过 → 行尾 `（会心·高阶 ×2.2）`/`（会心·中阶 ×1.7）`/`（会心·低阶 ×1.3）`；被格挡 →
  TC-10 | `/防御` 后受击 | 输出 `✅ 你进入防御姿态…`（BREP-05）+ `✅ 你防御了史莱姆的撞击，受到 2 伤害（HP 19/30）`（B
  TC-11 | 施放治疗术（MP 30→22，HP 0→30） | `✅ 你施放治疗术：回复 30 点 HP（MP 22/60）`（BREP-07）+ 资源差分行 `MP 30→22`（B
  TC-12 | 怪物反击命中玩家 | 输出 `❌ 史莱姆反击，你受到 4 伤害（HP 21/30）`（BREP-10），与先手行动同条消息
  TC-13 | 玩家先手击杀史莱姆（剩余多怪场景） | **不输出**被击杀怪的 反击行（写死，L61）；后手按兜底转向剩余目标或空挥行（L60）
  TC-14 | 史莱姆王进入蓄力（读招） | 输出 `史莱姆王 蓄力中（下回合发动「毒雾吐息」）`（BREP-12，固定句式 D-5E）；打断成功输出 `✅ 打断成功
  TC-15 | 史莱姆王 HP 触发阈值 / 召唤 / 叠印记 | 输出 BREP-13 特殊行动行（`进入狂暴状态（攻击提升）`/`召唤了 史莱姆 ×2`/`叠印记 2 层（当前 3/5
  TC-16 | 普攻击杀普通怪 | 伤害行后**紧跟** `✅ 你击败了史莱姆！`（BREP-15，扣血后立即查）；当轮事件末尾输出胜利+掉落
  TC-17 | 回合开始 dot 杀死玩家 | 输出 `❌ 你倒下了…`（BREP-16）→ 战斗失败标记；**玩家本回合行动不渲染**（行动跳过）
  TC-18 | 战斗胜利完整消息 | 同一消息含 `✅ 战斗胜利！`（BREP-17）+ 汇总行 `战斗结束：胜利｜回合数 5｜…`（BREP-24）+ `✅
  TC-19 | 同回合互杀（普通怪 + 反伤） | 输出 `双方同归于尽，战斗以平局结束`（BREP-19，默认 draw）；配置 player_loss 时改输出 BRE
  TC-20 | 玩家死亡（非互杀） | 输出 `❌ 战斗失败：你被史莱姆击败了`（BREP-18）；胜利/掉落行不出现
  TC-21 | 4 段连段全命中 | 每段独立一行 `第 {N} 段：…`（BREP-21，段号连续 1-4）；每段伤害独立取整；段行与伤害构成统计 seg 
  TC-22 | 连段第 3 段击杀普通怪（4 段套） | 第 3 段伤害行 → BREP-15 击杀行 → 第 4 段**照常渲染**（鞭尸）→ `连段 4 段已结算（目标已倒下
  TC-23 | BOSS 连段中死亡 / 派生封顶 | 连段击杀 BOSS → 击杀行后**立即进入结束结算，后续段数作废不渲染**（BREP-22 `（BOSS 已倒下，战斗
  TC-24 | `/攻击 史莱姆`（战斗开始） | 独立消息 `与史莱姆的战斗开始！史莱姆 25/25`（BREP-23）+ 弱点情报行 `弱点：火（×1.3）`；开始消息
  TC-25 | BOSS 战胜利结束 | 汇总行含回合数（对照 40-60 回合基准区间）与明细入口指令；结果横幅/掉落与汇总同消息输出
  TC-26 | `/木桩` 战后明细（来源 8 项） | 输出 `摘要：总伤害 …` 一行 + 前 5 条来源条目 + 页脚 TPL-08（第 1/2 页）；第 2 页 3 条单
  TC-27 | 普通战斗（非木桩）明细开关 | 默认**不展示**伤害构成明细（收集器仍在跑，供战报分享卡/平衡教练）；作者开战后明细时按 BREP-25 分页块渲染


===== 已实现渲染模块接口 =====


--- prefix_render.py (139 行) ---
  _TITLE_DECOR = r"[-=/~]+"
  def _worn(title: object) -> str:
  class PrefixResult:
  def render_prefix_result(
  def render_prefix(
  def _collapse_space(s: str) -> str:

--- list_render.py (201 行) ---
  class PageResolution:
  class ListPage:
  def _coerce_int(value: object) -> Optional[int]:
  def resolve_page(raw_page: object, total: int, per_page: int = DEFAULT_PAGE_SIZE) -> PageResolution:
  def page_items(
  def render_item_line(index: int, name: str, value: str = "") -> str:
  def render_footer(page: int, total_pages: int, total: int, command: str) -> str:
  def _default_item_line(index: int, item: Any) -> str:
  def render_list_page(
  def render_list_page_text(

--- panel_render.py (114 行) ---
  def _worn_title(player: Player) -> Optional[str]:
  def render_stats_line(attributes: PlayerAttributes) -> str:
  def render_panel(player: Player, *, include_prefix: bool = True) -> str:
  def paginate(

--- battle_render.py (50 行) ---
  _NOT_IMPL_MSG = "M1 实装：战斗渲染（细化_1g / 细化_1a / 细化_5e / 细化_3d §三）"
  def render_battle_start(
  def render_battle_round(round_result: Any) -> str:
  def render_battle_end(

--- sender.py (192 行) ---
  class SenderSendError(RuntimeError):
  def cq_escape(text: str) -> str:
  def segment_by_length(
  def format_tpl12(fragment: str) -> str:
  def format_tpl13(name: str, current: object, required: object) -> str:
  def format_tpl14(resource: str, amount: object, current: object) -> str:
  def page_error_tpl12(fragment: str, command: str, total_pages: int, total: int) -> str:
  class Sender:

--- battle.py 渲染/ActionOutcome 相关 (1841 行) ---
  "ActionOutcome",
  class ActionOutcome:
  outcomes: Tuple[ActionOutcome, ...] = ()
  def do_action(self, attacker: str, action_dict: Mapping[str, Any]) -> ActionOutcome:
  返回 ActionOutcome（含 rating/damage/battle_ended）。
  def _do_action_inner(self, attacker: str, action_dict: Mapping[str, Any]) -> ActionOutcome:
  def _guard_actor(self, attacker: str) -> ActionOutcome:
  return ActionOutcome(True, seq, attacker, "guard", target, True, "low", False,
  def _skip_turn(self, attacker: str) -> ActionOutcome:
  return ActionOutcome(False, seq, attacker, "skip", target, False, "low", False,
  def _flee_actor(self, attacker: str) -> ActionOutcome:
  return ActionOutcome(False, seq, attacker, "flee", target, False, "low", False,
  return ActionOutcome(False, seq, attacker, "flee", target, False, "low", False,
  return ActionOutcome(True, seq, attacker, "flee", target, True, "low", False,
  def _resolve_item_action(self, attacker: str, action: Dict[str, Any]) -> ActionOutcome:
  return ActionOutcome(True, seq, attacker, "item", target, True, "low", False,
  def _resolve_combo_action(self, attacker: str, action: Dict[str, Any]) -> ActionOutcome:
  msg = result.messages[0] if result.messages else f"指令被拒（{result.reject_reason}）"
  return ActionOutcome(False, seq, attacker, str(action.get("type", "skill")), target,
  out = ActionOutcome(
  tuple(hit_effects) + out.side_effects, out.message,
  def _resolve_damage_action(self, attacker: str, action: Dict[str, Any]) -> ActionOutcome:
  ) -> ActionOutcome:
  return ActionOutcome(
  message=f"{attacker} 对 {target} 造成 {damage.get('final', 0)} 伤害",
  def enemy_act(self, action_dict: Optional[Mapping[str, Any]] = None) -> Optional[ActionOutcome]:
  outcomes: List[ActionOutcome] = []

--- basic_commands.py /角色 相关 (1109 行) ---
  VIEW_CMD = "角色"
  def attr_line(idx: int, attr_id: str, stat_name: str, final: int,
  def cmd_view(parsed: Any, ctx: MutableMapping[str, Any]) -> str: