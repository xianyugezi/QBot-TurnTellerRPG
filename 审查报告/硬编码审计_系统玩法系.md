# 硬编码审计 · 系统/玩法系定稿（M/R/S 分级清单一稿）

> 审查角色：QBot-TurnTellerRPG 代码审查 Agent（固定人格·《审查角色_初始化.md》）
> 审查模式：**静态审查（静态推导）**——本环境无 bash 沙箱，全程仅使用 read/glob/grep 文件检索，未运行任何命令/脚本；凡涉及「引擎会如何实现」的判断一律标注【静态推导】
> 审计目标：16 份定稿（/root/docs_archive/RPG框架项目/）：资源轴机制 / 时间天气 / 任务 / NPC / 副本 / 商店 / 炼金 / 锻造 / 强化 / 钓鱼 / 签到 / 职业熟练度与生活 / 单机向体验包 / 开发规则 / 指令分隔符统一规范 / 消息前缀
> 审计维度：① 魔法数字/写死常量（次数/冷却/概率/时长/费用/倍率）② 本可配却写死的默认值（0=无上限 原则）③ 机制写死（具体系统数值规则写成框架硬编码）④ 通用化阻碍（固定天数/固定档位/固定台本，阻碍内容包复用）
> 日期：2026-08-16（本报告）

---

## 〇、结论摘要

**发现总数：28 处（M=4 / R=9 / S=15）**

- **M（必修·阻塞通用化）4 处**：见 §一。
- **R（应改·高价值可配化）9 处**：见 §二。
- **S（建议）15 处**：见 §三。
- **最有价值 3 处**：M1（品质档位唯一注册表化，影响面最广）＞ M3（时间季节/时段档位化，时间维度最大固定档位）＞ M2（职业熟练度 7 级去硬锚，生活职业通用机制）。速赢候选：R2（珠同名递减倍率配置化，改动最小见效最快）。

**正面确认（用户重点项，无需改）**：
- **每日次数/回春/冷却类上限**：副本 /休息「每日 3 次」「冷却 −3 回合」「HP/MP 20%」全部标注可配/0=不限；珠特效 ≤3 次/场、战斗即时调合 1 次/场、鱼王窗口 2、任务板 5/10、快捷表 20、别名 50、签到补签月上限 3 等均落入 settings/per-entity 配置——**这批「每日 3 次/冷却-3」类上限基本都已是配置驱动**，本审计未发现需要必修的写死项。
- **货币汇率**：商店出售比率 30%、黑市浮动率、炼金 gem 经济表、声望 −10% 惩罚全部可配；货币键 coins/gem 走 settings 货币键空间统一。无写死汇率。
- **系统间硬联接线**：reward 解析器唯一实现（任务/签到/NPC/怪物掉落共用）、分解回收率炼金→锻造复用、日界 05:00 三表同刻统一——这些收敛做得很好，予以确认。**未收敛的重叠集中在 M4（素材稀有度档位分裂）与 R7（30 天回收周期散落）**。

---

## 一、M 级（必修 · 阻塞通用化）——4 处

| # | 文件·节·行 | 原文（节选） | 问题维度 | 建议改法 |
|---|---|---|---|---|
| **M1** | 强化系统 §10.1#7（L204）；§六（L113-114）；炼金 §10.6（L411）；锻造 §12（L246） | 「max_by_rarity 键须为 items.json 四档品质枚举（normal/fine/epic/legendary，**不得出现整数/R 口径等其他编码**）」；「品质=items.json 四档唯一注册表」 | ④固定档位：品质档位在**校验器层硬拦死锁为 4 档**，非四档内容包（如 5 档或 3 档品质）会被拒绝加载；而 quality_tiers 表本身是 "0-100 分数计算口径（可配）"——档位数却不可配，自相矛盾 | 档位数/档界由 `quality_tiers`（settings/内容包注册表）**决定**，校验器改为「引用注册表键」而非硬编码 4 枚举；items.quality 键空间开放注册（编辑器新增/删档位）；分档字段（强化上限表、炼金品质系数、锻造 rarity、采集稀有档）全部从该注册表派生。0=无上限原则不变 |
| **M2** | 职业熟练度与生活 §2.1（L27-34）/§5.1（L169）/§六（L191）；NPC §4.3（L269）；任务系统 §一（L56）；炼金 §2.1（L55-61）；锻造 §10（L212）；钓鱼 M9（L23） | 「7 级默认名 + 内容包可改名」；校验器硬拦「解锁项引用的等级『大师』**不在 7 级称号表**」；NPC/任务互译表注释「prof_level 熟练等级（**7 级体系**）」 | ④固定档位：职业熟练度「7 级体系」被写死为框架硬锚，且横跨至少 6 份文档互引；proficiency 既然定位「通用机制，任何生活职业共用」，档位计数固定 7 即阻塞非 7 档职业（如 5 档学徒制、10 档职阶）内容包 | 以 `tier_names` 数组**长度即级数**（内容包可配 3~N 级）；删除全框架「7 级」话术，校验器改「不在 tier_names 注册表」；prof_level 条件变量、SP 面板、能量条 max_by_tier、job_tier_map 均按注册表长度自动展开 |
| **M3** | 时间天气系统 §1.2（L44）/§1.3（L53-54）/§9.2 | 「季节/时段枚举**固定写死**（用户原话定义，不做成可配枚举，防碎片化）」；`season_idx = … % 4`、`period_idx = … % 5` | ④固定档位/固定天数：四季/五时段个数与顺序写死进取模公式与条件变量枚举，阻塞潮汐/节气/无季节等时间向内容包复用（**注：为用户原话拍板，是否放开需上报用户仲裁**） | 建议 `season_count`/`period_count` 与枚举列表进 `time_cycle` 配置段（默认 4/5，顺序可配），取模与条件键、编辑器下拉、校验器 V6 全部由注册表驱动；如用户坚持固定，至少将「4/5」提升为显式配置常量并保留扩展声明 |
| **M4** | 锻造 §五/2.1 决策（L53、L142-149）；炼金 §10.3（L381）；时间天气 §4.2（L203） | 锻造「**两档素材**（普通/稀有）」；炼金 items 扩展「rarity(普通/稀有/**金色**)」；采集「稀有度档位（普通/稀有/金色/**✨**，clamp 到 4 档）」 | ④系统间硬联接线/重叠：同一「素材稀有度」在三份定稿各写一套档位（2/3/4 档），无唯一注册表，内容包作者无法得知素材档位的权威口径；是典型的"重复造轮子但口径分裂" | items.json 增加**素材稀有度唯一注册表**（如 rarity 枚举可配，默认 普通/稀有/金色），锻造两档、炼金三档、采集四档统一降级/映射到该注册表（校验器硬拦未注册档位，黄色提示跨系统档位不一致） |

---

## 二、R 级（应改 · 高价值可配化）——9 处

| # | 文件·节·行 | 原文（节选） | 问题维度 | 建议改法 |
|---|---|---|---|---|
| R1 | NPC 系统 §三对话结构（L316/L324）/§4.5（L446） | 「简单对话树（**≤2 层硬约束**防爆炸）」「对话树死循环（>2 层环）硬拦」 | ④固定台本：对话深度写死 2 层，阻塞多分支剧情内容包（用户拍板：单轮+≤2 层；放开需拍板） | 对话树深度改 `settings max_dialog_depth`（默认 2，0=不限），校验器由「>2 层环」改「>max_dialog_depth 提示不拦截/软拦」 |
| R2 | 炼金系统 §6.3（L272） | 「同名递减：**乘法叠加（第 2 颗 ×50%，第 3 颗 ×25%）**」 | ①魔法数字：珠同名递减倍率 50%/25% 写死，§10.6 settings 段无对应字段 | 新增 `settings.alchemy.gem_diminish = [{n:2,mult:0.5},{n:3,mult:0.25}]`（或递减表），默认值不变、可改 0=无递减 |
| R3 | 炼金系统 §2.2（L68-73）；职业熟练度 §2.2（L42-47） | SP 解锁项「品质上限 **+10**」「投入次数 **+1**」「采集量 **+1**」「连锁上限 **+1**」 | ①魔法数字：SP 面板解锁项数值写死在机制表，未标可配、无配置落点（§10.6 仅有 sp_per_level，无解锁项数值） | `sp_panel[]` 每条解锁项带 value 字段（默认 +10/+1），sp_panel 表驱动编辑器与结算；内容包可改数值/可多次次数 |
| R4 | 强化系统 §4.2（L87） | 「实际成功率 = 基础成功率 +（**√我方幸运 − √对方幸运**）%」 | ③机制写死：幸运修正公式（√ 演算）写死进引擎，luck_affects 仅开关不参数化 | 幸运修正进 enhance.json/settings：`luck_formula`（√/线性/关）+ `luck_scale` 系数，内容包可换公式不改引擎 |
| R5 | 钓鱼玩法 §一 M2（L16）/M5（L19）/§四（L94） | 「对口饵稀有 **+8%**/金 **+2%**」「满力收杆（升级 roll：+稀有度/冠级概率）」；「满配（对口饵+满力收杆）54/37/9」 | ①魔法数字：鱼饵对口加成与满力收杆 roll 加成数值写死，§三 profession.json 仅有 bait_ids 无加成字段 | profession.json fishing 段新增 `bait_bonus`（对口味稀有/金色倍率）+ `rod_full_bonus`（满力收杆加成），默认 8%/2% 不变，改配即生效 |
| R6 | 单机向体验包 §三（L45）/§八（L86） | 「**90% desc 软锚**：90% 档成就描述…唯一明示例外」vs「数值（可配）…阶梯间隔 25/50/75/90/100%」 | ①魔法数字/④固定档位：阶梯比例已可配，但「90% 软锚」的 90% 写死为特殊分支，两处冲突，移动里程碑后软锚失效 | 软锚挂到里程碑条目（`milestone.items[i].desc_anchor`），跟随配置；删除对 90% 的硬编码特殊逻辑 |
| R7 | 开发规则文档 §7.4（L478）/§7.5（L488-489）；另 NPC §2.3（L88）、炼金 §4.6（L183）、签到 §六（L120）引用同源 | 「30 天无操作回收（僵尸战斗防内存泄漏）」「备份保留默认 30 天」「回收站 30 天自动清理」 | ①魔法数字/④系统间硬联接线：**30 天**清理/回收周期散落 5+ 处独立写死，无统一键，繁忙服/轻量包无法按需调 | settings 统一 `lifecycle.recycle_days`（会话/僵尸回收）与 `lifecycle.backup_retention_days`（备份/回收站），全部引用同源键，删各地魔数 |
| R8 | 副本系统 §五（L241-244）；兑换衔接 §二 zone_change（L143） | 「阶段机制（血量阈值）：阶段 1（100-60%）…阶段 2（60-30%）…阶段 3（30-0%）」 | ③机制写死【静态推导】：若引擎按 3 段阈值轴实现，则阻塞 2/4 阶段 BOSS 内容包（换区 trigger 已每 BOSS 可配，阶段轴未声明可配） | BOSS 阶段阈值表进 enemies.json per-entity：`phases:[{hp_from,hp_to,behavior}]`（op type=phase_hp），默认 3 段示例值，档数不定死 |
| R9 | 商店系统 §3.1（L138）/§七（L282）/§十一（L430） | 「refresh 默认 **daily/4 点**」vs「默认 05:00，与 quest_daily 同刻对齐」vs「refresh=daily **4 点**」，但 §3.2 示例 hour:5 | ②写死的默认值/系统间硬联接线：刷新默认时刻「4 点」与「05:00」多处互相矛盾（商店文档内部即分裂；与签到/任务日界同刻的宣称被 4 点表述破坏） | 统一为单一配置键 `refresh_time`（默认 05:00，可配）并全文档引用；校验器/编辑器下拉同源；删除「4 点」表述（属文档修正+配置收敛） |

---

## 三、S 级（建议）——15 处

| # | 文件·节·行 | 问题 | 建议改法 |
|---|---|---|---|
| S1 | 时间天气 §1.3（L50）/§9.1（L356） | 世界锚点 2000-01-01 硬编码 | `time_cycle.epoch` 可配（内容包可换日历起点），默认不变 |
| S2 | NPC §3.0（L108-113） | 交互菜单 ≤6 折叠写死 | `menu_max_options` 可配（默认 6），与分页同源 |
| S3 | 炼金 §3.2（L115） | 合成数量 ≤99 未标可配（商店/指令分隔 §铁律1 已标「各系统配置·可配」） | 补「可配 0=不限」，引用公共批量上限配置键 |
| S4 | 炼金 §5.2 #1（L199） | 连锁奖励解锁条件「链式投料 ≥3 段」写死（chain_map 可配但解锁阈值未标） | 解锁阈值并入 chain_map/解锁配置（per-system） |
| S5 | 锻造 §12.3（L330/L332） | 客制配置示例仍含已砍 aug_heal「回复/吸血5%」（§七已标砍除） | 清理死配置示例，防止实现误建吸血渠道 |
| S6 | 强化 §六（L114）/§九（L154） | 「上限值不变」表述与可配表（max_by_rarity JSON）自相矛盾，作者易误以为不可改 | 措辞改「默认值」，明确表内条目可配 |
| S7 | 钓鱼 §2.2（L43） | 冠级判定顺序「（写死）」——重叠区间优先级 | 可接受（结构依赖）；建议文档化优先级规则，如需自定义再升级为可配 |
| S8 | 职业熟练度 §3.6（L135） | 佩戴槽固定 1 个 | `title_slots` 可配（默认 1，0=不支持佩戴） |
| S9 | 职业熟练度 §2.4（L70） | 能量「每 30 分钟回 1 格」此处未标可配（炼金 §10.6 energy_regen_sec 已可配） | 统一标注，引用同一配置键 |
| S10 | 单机向 §四（L53-54） | 「传记 50 段」与「300 条环形（可配）」容量不统一标注 | 传记段数并入日志容量配置同源 |
| S11 | 开发规则 §7.3（L466） | 登录失败 5 次锁定 15 分钟写死 | 安全策略项入 settings（默认不变），可配 |
| S12 | 开发规则 §8.2（L509） | 战斗状态行「默认显示前 5 个」未标可配 | 与列表分页同源 `page_size` 配置 |
| S13 | 指令分隔符 §四（L71） | 固定子词表（追加/预览/自动…）为解析器内置常量 | 子词表登记或可配，防与内容包物品名/别名冲突时留仪器 |
| S14 | 消息前缀 §四占位符体系 | 占位符固定 5 种，内容包无法扩展自定义占位符 | 占位符注册机制（x_ 扩展或引用变量体系注册表） |
| S15 | 任务系统 §3.4（L181） | 板槽位枚举固定 daily/weekly/event | 槽位可扩展注册（默认三型不变），0=不限原则 |

> 任务系统 / 签到系统 / 资源轴机制三份文档：未发现 M/R 级硬编码。任务板 5/10 上限、声望 5 级阈值表、签到 reward 日历、资源轴 base/max/reset 均已是配置驱动（0=无上限 原则落实良好），仅列 S15 一条低优先项，特此说明。

---

## 四、用户重点项逐项核对

1. **每日次数/回春/冷却上限** → ✅ 已基本全部可配（0=不限）：/休息 3次、冷却−3 回合、HP/MP 20%、珠特效≤3 次/场、即时调合 1 次/场、鱼王日窗 2、任务板 5/10、快捷 20、别名 50。未发现需必修的写死。
2. **货币汇率** → ✅ 已验证全部配置驱动：出售比率 30%（settings）、黑市 ±%（per-shop）、gem 经济表（settings.alchemy.gem）、声望 −10%（可配）。货币键 settings 键空间统一。无写死汇率。
3. **系统间硬联接线 / 重叠**：
   - ✅ 已收敛：reward 解析器唯一实现（任务/签到/NPC/掉落共用）、分解回收率（炼金→锻造复用）、品质四档注册表（档位可配问题见 M1）、刷新日界 05:00 三表同刻（文档表述分裂见 R9）。
   - 🔴 未收敛：**M4** 素材稀有度档位分裂（2/3/4 档各写各的）、**R7** 30 天回收周期散落 5+ 处、M2 熟练度「7 级体系」跨 6 文档互引。

## 五、建议落地路线（静态推导）

1. **先解 M1（品质档位注册表化）+ M2（熟练度 tier_names 数组即级数）**：两处都是「注册表驱动 + 校验器硬拦解除」，改动集中在 validator/编辑器/字段元数据，收益是档位类通用化的统一底座。
2. **M3（季节/时段档位）需用户拍板**：与「用户原话·防碎片化」直接冲突，按审查人格规则上报不裁决，仅在用户同意放开时实施。
3. **R2/R5 等纯倍率项**（珠递减/饵加成/幸运公式）：
   改动最小、最快落进 settings，作为「数值驱动」方向的第一批示范。
4. **R7（30 天回收）/R9（刷新时刻 4 点 vs 05:00）** 属运维与文档一致性快修，可并行处理。

---

*审计说明：以上行号均取自 16 份定稿原文实读（read 工具按行返回），引用真实存在，未运行任何命令/脚本；「引擎按 3 段阶段轴实现」「校验器硬拦 4 枚举」等实现层判断标注【静态推导】。*
