# 任务书 · 批7·路T（炼金 #5：即时/种植/收获/帮手/SP/图鉴/助剂 39 键）

> 2026-09-12 · 消息模板重构战役 · 执行者：子 agent（本任务书 + `docs/消息模板重构/00_方案与规范_v1.md` 为唯一指令源）
> **先读** `<worktree>/docs/消息模板重构/00_方案与规范_v1.md`（全量口径：14 全角/免斜杠/少｜多换行/❌+原因/拆行优先序/大数值/零 emoji/prose 豁免），再开工。

## 1. 范围（本路 39 键，全部在 `qbot_rpg/core/templates/alchemy_tpl.py`）

即时：`alchemy_instant_bag` `alchemy_instant_damage` `alchemy_instant_fail` `alchemy_instant_limit`
`alchemy_instant_not_battle` `alchemy_instant_used`
种植收获：`alchemy_plant_ok` `alchemy_plant_fail` `alchemy_plant_level` `alchemy_harvest_ok`
`alchemy_harvest_fail` `alchemy_harvest_level` `alchemy_collect_ok` `alchemy_collect_empty`
帮手/助剂：`alchemy_helper_assign_ok` `alchemy_helper_assign_fail` `alchemy_helper_level`
`alchemy_helper_task_invalid` `alchemy_assist_ok` `alchemy_assist_materials_missing`
`alchemy_assist_not_same_group`
SP 面板：`alchemy_sp_panel` `alchemy_sp_panel_empty` `alchemy_sp_panel_unavailable`
`alchemy_sp_unavailable` `alchemy_sp_not_found` `alchemy_sp_insufficient` `alchemy_sp_unlock_fail`
`alchemy_sp_item_used` `alchemy_sp_item_not_found` `alchemy_sp_max_repeat` `alchemy_sp_not_repeatable`
图鉴：`alchemy_codex_line` `alchemy_codex_unavailable` `alchemy_codex_king_hint`
`alchemy_codex_king_granted` `alchemy_codex_reward` `alchemy_codex_reward_exp` `alchemy_codex_reward_recipes`

**每键必做**：读旧文案拿到「信息要素 + 触发场景 + 占位符集合」，然后**按规范全新撰写**（旧文案不作参考）。
占位符沿用（`{name}` `{level}` `{count}` …）；`tpl_of(` 调用点的键名与参数名**不得改**。

## 2. 工作清单（每步都落盘）

1. **第一件事**：写 `<worktree>/docs/消息模板重构/_fragments/b7t.json`（39 键新文案，先落盘再动代码），
   commit（`docs(模板·路T): 批7 fragment 先落盘…`）。
2. 合表：`python3 scripts/tpl_merge_fragments.py <fragment>`（表 = `qbot_rpg/core/templates/template_table.json`）。
3. 清分区：从 `alchemy_tpl.py` 删除已迁 39 键（文件其余键保留）；`content/test_demo/templates.json` 里同 key 同步为新文案。
4. 代码/测试同步：`grep -rn "<key>" qbot_rpg/ tests/ scripts/` —— 模块级常量别名（`X: str = "…"` → 改读表）、
   断言旧文案、docstring 引用逐处更新；**不得**为了过测试而改回旧文案。
5. 自校验：`.venv/bin/python scripts/check_template_width.py`（本路键 0 FAIL）；
   `PYTHONPATH= <repo>/.venv/bin/python -m pytest tests/unit/test_template_table.py tests/unit/test_emoji_discipline.py -q`；
   定向跑改动涉及测试文件；**最后跑一次全量** `.venv/bin/python -m pytest tests/ -q`（只报计数）。
6. `.venv/bin/ruff check <改动文件>` 干净；commit（`feat(模板·路T): …`）。**禁止 push / 禁止动 main**。

## 3. 报告（回复正文内联，勿只写摘要）
- fragment 路径 + 39 键新文案（JSON 字段全量内联）；合表结果（新增 N/冲突 0/宽度 FAIL-WARN）
- 改动文件清单；checker 输出；定向+全量测试计数原文；未完成/存疑项；commit hash
- **预算纪律**：若临近工具/时间上限，优先 (a) fragment 已落盘 (b) 已改代码已 commit (c) 如实列未完成项。
  绝不允许谎报完成或留下与旧文案混用的半成品。
