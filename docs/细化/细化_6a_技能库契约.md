# 细化_6a：技能库契约（skills.json 全字段 Schema + skill_actions.json 行动库 + 校验器 + 魔法穿透裁决）

> 版本：v1.0（细化交付 · 第二轮补缺）· 2026-08-18
> 依据：《技能库行动库设计定稿 v1.0》（下文简称**定稿**，行号以 `/root/docs_archive/RPG框架项目/技能库行动库设计定稿.md` 为准；辅助引用：《效果系统设计定稿 v1.1》=**[效]**、《战斗数值层设计定稿 v1.1.1》=**[数]**、《实现层规划文档》=**[规]**）
> 覆盖声明：本文件收口《覆盖审计_B》缺失清单 P1 #1/#2/#3（技能库整库 schema + action 库 schema + 校验器缺 4 条），并对 P1 #4（魔法穿透"无视精神"语义丢失）做显式 ADR 裁决；承接 [规 T24] ActionCore 同构双库、[规 T25] 玩家侧技能扩展、[规 T27] 技能位与指令解析、[规 T19] interrupt 唯一归口。
> 标注约定：**定稿既有** = 定稿原文明确；**细化定型** = 定稿仅给方向/区间/载体缺位，本表取点定型（裁决边界透明，可改）；**承接** = 字段/规则已落于既有细化（1a/1b/1c2/1d/1e/1f），本表只登记接口不重复实现。
> 引用约定：`[Lxx]` = 定稿行号；`[效 Lxx]` = 效果系统设计定稿行号；`[数 Lxx]` = 战斗数值层设计定稿行号；`[规 Txx]` = 实现层规划文档任务号。

---

## 0 承接与衔接（先读本节）

| 既有落点 | 承接内容 | 本文件职责 |
|---|---|---|
| 1c2 §1.3/§1.5 | tag 五标签语义、chain_refs 派生链引用 | 全字段表登记（不重复设计 tag 语义） |
| 1d §一/P-1 | consume_marks 印记引用与消耗平衡 | 校验 V-3 判定细节 |
| 1a M10/§2.1 | power 倍率预算、attack_type → weapon_type_mult/type_affinity、8 元素注册表 | element/attack_type 枚举约束 |
| 1b 全契约 | effects.json 唯一数据源 + overrides + L0 词汇表 | effects 字段结构 + 校验 V-1 |
| 1e A01-A03 / 1f | 行动侧 AI 字段（weight/probability/intent/chain/condition/hungry） | 行动库 schema 登记接口 + 校验 V-9 |
| 1g1a A3 / 1g2 ⑤ | 后手无目标兜底（fallback/skip） | 目标解析管线引用 |
| 3e2 | 热重载：保存即生效 | 校验时机（§3.3） |
| 1g1c / 1g3 | 冷却/装配/触发计数存档 | 存档影响（§7） |

**三铁律对齐**（[L20-25]）：① 种类收敛——所有技能字段按"何时执行（type 四类）/做什么事（kind 五类 + 原子动作）"二分；② 字段最少——核心块 ≤7 字段 [L29]、扩展字段全部默认值兜底，漏配 = 合理默认不是报错 [L23/L189]；③ 不做数值预警——校验器只查结构/引用/死配置 [L24]，power 999 是作者自由。

---

## ① skills.json 字段级 Schema（全字段 24 个）

### 1.1 库级结构

- **文件形态**：`skills.json` = 技能条目**数组**（与定稿示例 L217-225 的扁平对象写法一致；loader 亦兼容 id 键对象，校验按 id 唯一，见 V-10）。
- 顶层可选元数据（细化定型）：`{"version": "…", "defaults": {…}}` —— `defaults` 为库级默认值覆盖（trigger_limit 全局默认的覆盖载体，见 F20），不承载字段定义（字段定义唯一来源 = ActionCore 元数据单点 [L243]）。
- 技能条目的字段语义由 `type` 决定：basic/active 占行动位，passive/trigger 占装配槽 [L62-65]；**同一执行器与 action.json 共用 ActionCore** [L7/L15-16]。

### 1.2 全字段表（24 字段 = A 共用核心 7 + B 玩家扩展 11 + C 全库补充 2 + D 细化定型 4）

#### A. ActionCore 共用核心块（7 字段，[L29-54] 字段表 L43-51）

| # | 字段 | 类型 | 必填 | 默认值 | 约束 / 枚举 | 编辑器控件 | 来源 |
|---|---|---|---|---|---|---|---|
| F01 | `id` | string | 必填 | 自动生成 | 全库唯一（V-10）；建议 snake_case；非空 | 自动 | [L33/L45/L210] |
| F02 | `name` | string | 必填 | — | 显示名；非空 | 文本框 | [L34/L46] |
| F03 | `kind` | enum | 选填（可自动推断） | 自动推断 | `damage`/`heal`/`status`/`control`/`utility` [L35/L47]；推断规则见 1.3 | 下拉（伤害/治疗/状态/控制/功能） | [L35/L47] |
| F04 | `power` | number | 选填 | `100` | 倍率（滑条 10-500% [L48]）；派生链累计默认 ≤1.5×（黄提示，V-6）[L206]；不拦数值（三铁律③） | **滑条** | [L36/L48] |
| F05 | `attack_type` | enum | 选填 | 按武器 | `slash`/`blunt`/`pierce`/`magic`/`none` [L49]；→ [数] weapon_type_mult / type_affinity [L142] | 下拉（斩/打/突/魔/无） | [L37/L49/L142] |
| F06 | `element` | string \| null | 选填 | `null` | ∈ 8 元素注册表（earth 地/fire 火/water 水/wind 风/thunder 雷/crystal 晶/moon 月/void 无 [数 L220-221]）[L141]；V-4 | 下拉（8 元素） | [L38/L50/L141] |
| F07 | `effects` | array | 选填 | `[]` | 效果引用（effects.json 唯一数据源 + overrides + x_ 例外）[L119-132]；结构见 1.3；V-1 | **效果模板库**（一键插入） | [L39/L51] |

#### B. 玩家侧扩展字段（11 字段，[L67-82]）

| # | 字段 | 类型 | 必填 | 默认值 | 约束 / 枚举 | 编辑器控件 | 来源 |
|---|---|---|---|---|---|---|---|
| F08 | `type` | enum | 必填 | — | `basic`/`active`/`passive`/`trigger` [L60-65] 四类时机，语义见 §1.4 | 下拉（普攻/主动/被动/触发） | [L60-65] |
| F09 | `mp_cost` | number | 选填 | `0` | ≥0；basic=0（不消耗）[L62]；→ stats.json 默认 MP，自定义资源轴可映射 [L143] | 滑条 | [L71/L143] |
| F10 | `cooldown` | number | 选填 | `0` | ≥0 整数；basic=0（无冷却）[L62]；冷却计数由引擎 1g2 管理（回合末 -1），/休息 减冷却（[效] 3.1） | 滑条 | [L72] |
| F11 | `tag` | enum | 选填 | `none` | `none`/`combo`/`combo_preserve`/`combo_push`/`interrupt`/`armor` [L73]；**tag 仅展示/派生语义、不承载效果**；与 `armor`/`interrupt` 布尔、effects 三处语义必须一致，不一致时以布尔/effects 为准（裁决见 1.3-t1） | 下拉（无/连段/保留/推进/打断/霸体） | [L73/L75] |
| F12 | `armor` | boolean | 选填 | `false` | 霸体（开关）[L74]；执行语义快键，效果归口 effects.json | 开关 | [L74/P4] |
| F13 | `interrupt` | boolean | 选填 | `false` | 打断——框架 L0 原子动作 interrupt 的快捷字段 [L75]；**唯一实现归口** = 效果系统词汇表 interrupt 动作（[规 T19]，三大入口汇聚同一实现） | 开关 | [L75/P4] |
| F14 | `chain_refs` | string[] | 选填 | `[]` | 派生链引用 skill_chains.json ID [L76/L139]；引用必须存在且可达（V-2）；派生语义/后台由 1c2 承接 | 下拉（多选） | [L76/L139] |
| F15 | `consume_marks` | object | 选填 | `{}` | `{mark_id: count}` 消耗印记 [L77/L140]；键 ∈ marks.json、值 ≥1 且 ≤ 印记上限（V-3）；平衡口径 1d §4.2/§6 承接 | 下拉（印记 + 数量） | [L77/L140/L203] |
| F16 | `job_restrict` | string[] | 选填 | `[]` | 职业限制 [L78]；引用 jobs.json id，必须存在（V-5）；空 = 通用（所有职业可见）；绑定规则见 §4 | 下拉（多选） | [L78/L205] |
| F17 | `job_form` | string \| null | 选填 | `null` | 形态技：职业变换后可用 [L79]；引用职业变换 transform 形态名，必须存在（V-5 扩展判定）；装配时形态替换对应技能位 [L88] | 下拉（可空） | [L79/L88] |
| F18 | `level` | object | 选填 | 无（不升级） | `{"max": N, "growth": […]}` [L80]；growth 数组长度 = max 且 growth[0] = 1 级倍率基准；升级状态落存档（§7） | 滑条（max + growth 表格） | [L80] |

#### C. 全库补充（2 字段：定稿提及但未进 schema 代码块，本文件补载体）

| # | 字段 | 类型 | 必填 | 默认值 | 约束 / 枚举 | 来源 |
|---|---|---|---|---|---|---|
| F19 | `hits` | number | 选填 | `1` | 多段次数：主动技能 1 轮 1 行动，多段走 hits 配置、不开放 `*` 数量 [L11]；**每段独立结算**（命中/会心/伤害分别判定，段间走拦截链）（[规 T25] 验收） | [L11] |
| F20 | `trigger_limit` | object | 选填 | `{per_round: 10, per_battle: 99}` | 触发技能触发上限（可配）[L208]：每秒触发行数上限/每场上限；`0` = 不限（防刷屏靠消息折叠兜底 [L208/L244]）；**技能级覆盖 > 库级 defaults > settings.json 全局默认**；引擎强制，超出不执行且计数不越界（TC-09） | [L208/L244] |

#### D. 细化定型（4 字段：定稿未列字段，本文件按数值层/展示需求取点定型）

| # | 字段 | 类型 | 必填 | 默认值 | 约束 / 枚举 | 来源（裁决依据） |
|---|---|---|---|---|---|---|
| F21 | `desc` | string | 选填 | 无 | 一句话说明（技能卡/战报/编辑器悬浮提示用）；非空建议（V-13 黄提示） | 细化定型；对齐动作卡"名称"文本框扩展 [L154-165] |
| F22 | `hit_mod` | number | 选填 | `1.0` | 命中率修正（乘数）→ 命中公式 [数 L21-22]（K_hit=0.2、clamp 10-95%）；>0 | 细化定型；判定顺序"命中→会心→格挡"[数 L14-17] 的命中段扩展 |
| F23 | `crit_mod` | number | 选填 | `1.0` | 会心判定修正（乘数）→ 会心 P=√幸运÷2、三档 2.2/1.7/1.3 [数 L23] | 细化定型；同上会心段扩展 |
| F24 | `block_mode` | enum | 选填 | `auto` | `auto`（默认：按 attack_type 规则——魔攻击无视格挡 [数 L25]）/ `normal`（参与格挡判定 min(40%, 专注/(专注+150)) ×0.5 [数 L25]）/ `ignore`（无视格挡） | 细化定型；覆盖 [数 L25] 全局规则的技能级例外开关 |

### 1.3 字段规则细节（接缝裁决）

- **f1 kind 自动推断**：power>0 且无状态类效果 → `damage`；有 heal/dot 效果 → `heal`/`status`；control 效果 → `control`；其余 → `utility`。推断值仅作默认，作者可下拉改（自动推断或下拉 [L35]）。
- **f2 effects 条目两种形态**（[L123-127]）：
  - 效果引用：`{"effect": "<effects.json ID>", "overrides": {…}}`——overrides 覆盖参数（示例 `{"power": 50}`）；
  - 原子动作（L0 词汇表成员）：`{"type": "mark_add", "target": "self", "mark": "fire_mark", "count": 1}`（
示例 [L125]；x_ 前缀自定义例外，编辑器隐藏 [L131]）。
  - `effects[].target` 为**效果级寻址**（self/敌方…），与行动级 `target` 字段（§2.4）分层，不混淆。
- **f3 tag / 布尔 / effects 三方一致性**（裁决，闭合定稿 L73/L75 双写表述）：`armor`/`interrupt` 布尔 = 执行语义；`tag` = 展示/派生语义；effects 中的 L0 动作（如 interrupt）= 效果归口。三处配置冲突时：**以 effects 为准 → 次以布尔字段为准 → tag 仅作展示**；校验器对 tag 与布尔不一致发黄提示（不拦截）。
- **f4 attack_type 默认按武器**：技能未配 attack_type 时取当前武器类型（定稿默认"按武器" [L37/L49]）；`none` 用于纯功能/治疗。
- **f5 element 与 attack_type 正交**：element 决定伤害的元素通道归属 [数 L31-33]；attack_type=magic 且 element=null 时按武器元素或 0 元素攻击处理（1a 承接）。

### 1.4 四类时机（type）执行语义（[L59-66]）

| type | 中文 | 执行时机 | 消耗 | 技能位 | 门禁（指令被拒） |
|---|---|---|---|---|---|
| `basic` | 普攻 | 玩家回合默认出手 [L62] | 0 MP / 0 冷却 [L62] | 占 1 位（**固定第 1 位**）[L62/L86] | 无（默认出手兜底） |
| `active` | 主动技能 | 玩家回合选择施放（`/攻击 <序号>`）[L63] | MP + 冷却 [L63] | 占 1 位（可拖拽排序）[L86] | **MP 不足 / 冷却中 / 条件不满足 → 不耗回合、不改连段、可反复尝试**（[规 T27]；裁决定型） |
| `passive` | 被动技能 | 常驻（进入战斗即生效）[L64] | — | 不占行动位（装配槽）[L64] | 无（装配即生效） |
| `trigger` | 触发技能 | 条件触发（受击/暴击/行动后…）[L65] | 触发上限 [L65] | 不占行动位（装配槽）[L65] | 触发上限（F20）：超出不执行、不进入结算链 |

- 指令分隔：`/攻击 <序号>` 序号不带 `*`；主动技能 1 轮 1 行动，不开放 `*` 数量（多段走 hits）[L11]。
- 触发条件的 13 类枚举与特殊行动共用（怪物模块 7.1 触发类，1e/1f 承接）[L111]——玩家 trigger 技能的条件判定复用同一触发引擎（同构双库推论，裁决定型）。

### 1.5 技能位与装配（[L84-91]，编辑器可视化）

- 技能列表 = 普攻（固定第 1 位）+ 主动技能 N 个（可拖拽排序）[L86]。
- 被动槽/触发槽：装配位（不占行动位）[L87]；装配结果**落玩家存档**（每次进战斗按装配快照生成可用技能列表，1g1c/1g3 承接）。
- 形态技能：职业变换后替换对应技能位 [L88]（TC-06）。
- 装备技：装备附带，使用后自动加入技能列表 [L89]（4b 装备侧承接）。
- 编辑器：拖拽排序 + 槽位图 [L90]；一卡编辑（技能页/行动页同卡）[L152-166]。

---

## ② skill_actions.json 行动库 Schema（ActionCore 共用块 + 目标解析 + 四时机对位）

### 2.1 库级结构

- **文件形态**：`action.json` = 行动条目**数组**（定稿示例 L228-232 扁平对象）；**与 skills.json 共用 ActionCore，引擎同一执行器，一卡编辑** [L7/L15-16]。
- 行动条目 = ActionCore 7 字段（F01-F07 完全相同）+ 目标字段 + 怪物侧扩展 + AI 登记字段；ActionCore 元数据单点定义（两库同一份），任何库新增字段必须登记元数据，否则被校验器拒绝（V-11）[L243]。

### 2.2 ActionCore 共用块（7 字段）

与 §1.2-A 的 F01-F07 **逐字段同构、逐约束同源**（id/name/kind/power/attack_type/element/effects）；仅两处语义差异：

| 差异点 | skills.json | action.json | 来源 |
|---|---|---|---|
| `attack_type` 默认 | 按玩家武器 | 按怪物模板/攻击部位（enemies.json 承接，1e） | [L37] + 1e |
| `effects` 引用 | 玩家技能效果 | 共用 effects.json，x_ 例外同开放 [L131] | [L119-132] |

### 2.3 怪物侧扩展字段（[L97-106]）+ AI 登记字段

| # | 字段 | 类型 | 默认 | 约束 / 枚举 | 来源 |
|---|---|---|---|---|---|
| G01 | `weight` | number | `0` | ≥0；随机权重（归一化 = 概率）[L100]；仅 probability=1 时参与随机池；weight 全 0 且无链/条件 → 提示"纯脚本怪？"（不拦截，V-9）[L209] | [L100/L209] |
| G02 | `probability` | enum | `0` | `0`=锚点行动（只被链/条件/状态机触发，不随机入表）/ `1`=参与随机池（数值不参与概率计算）[L101/L112]；**必须 ∈ {0,1}** | [L101/L112] |
| G03 | `intent` | enum | `damage` | `damage`/`defense`/`charge`/`heal`/`control`/`buff`/`debuff`/`mark`/`utility`（意图预告）[L102] | [L102] |
| G04 | `chain` | string[] | `[]` | 连招（xx 行动后触发，行动 ID 下拉）[L103]；**历史写法，读兼容**；新配置连招一律走 enemies.json 顶层 `chains` 表（1e S2 裁决，校验器提示迁移） | [L103] + 1e S2 |
| G05 | `cooldown` | number | `0` | 冷却回合 [L104] | [L104] |
| G06 | `target` | enum | `enemy_single` | 行动级目标（细化定型，见 §2.4）：`enemy_single`/`enemy_all`/`ally_single`/`ally_all`/`self`/`random_enemy` | 细化定型（定稿效果级 target L125 的上提） |
| G07 | `trigger_limit` | object | 同 F20 | 怪物触发行动同样适用触发上限（防刷屏，复用 L208 语义，细化定型载体） | [L208] 扩展 |
| G08-G16 | AI 登记字段 | — | — | `condition`/`hungry`/`armor`/`interrupt`/`tags`/`charge_*`/`preview`/`preview_chain`/`reveal_condition`——**登记接口不展开，结构/语义以 1e/1f 为准**（[规 T26] 已登记） | [规 T26] + 1e/1f |

> 玩家侧不适用 G01/G02：玩家出手由玩家选择决定，无随机入表 [L114]。

### 2.4 目标解析（行动级 target + 运行时决议管线）

1. **schema 层**：行动级 `target` 字段（G06，细化定型）声明目标形状；`effects[].target`（[L125]）只负责效果寻址（如印记加给谁），二者分层。
2. **运行时决议管线**（本文件只登记接口，实现落 1g1a/1f）：
   - 玩家 active/basic：`/攻击 <序号>` → 站位/目标解析（1g1a 回合时序承接）；
   - 怪物行动：AI 状态机目标选取（1f 承接）；
   - **后手无目标兜底**：目标已死亡/不存在且无可选目标 → `fallback`/`skip`（[数 L56-57]；1g1a A3；1g2 ⑤）。
3. 多目标（`enemy_all` 等）由 G06 承载，命中/会心/格挡按目标逐一判定（与 hits 多段正交：hits = 同一目标多段，target 多目标 = 多目标各结算一次）。

### 2.5 行动库四时机对位表（同构双库推论，[L15-16]）

| 玩家侧 type | 行动库对位路径 | 依据 |
|---|---|---|
| `basic` 普攻（默认出手） | 随机池普攻路径：`probability:1` + `weight` 入池条目（常态普攻行动） | [L101/L112/L114] |
| `active` 主动技能（显式施放） | 锚点行动/链调用：`probability:0` + `chain` 或 enemies 顶层 `chains` 显式触发 | [L101/L103] + 1e S2 |
| `trigger` 触发技能（条件触发） | 特殊行动：`special_actions` 13 类触发（hp_below/after_action/enter_phase…） | [L111] + 1e A04-A08 |
| `passive` 被动技能（常驻修正） | 无敌对字段：怪物侧由 AI 状态机/意图常驻规则承载（intent/condition 长效修正） | [L102] + 1f |

---

## ③ 技能校验器（定稿 10 条全量 + 细化增补 3 条 = 13 条）

### 3.1 定稿校验 10 条全量（[L200-211]，逐条落地判定细节）

| # | 规则 | 定稿行号 | 级别 | 承接 / 判定细节（本文件裁决） |
|---|---|---|---|---|
| V-1 | 效果引用存在（effects.json） | [L201] | 红拦 | 承接 1b；判定：`effects[].effect` ∈ effects.json ID，或 `effects[].type` ∈ L0 词汇表动作名（[规 T19]）；x_ 前缀放行 [L131] |
| V-2 | 派生链引用存在 + 无死配置（可达性） | [L202] | 红拦（引用缺失）/ 黄提示（死链） | 承接 1c2；判定：`chain_refs` ∈ skill_chains.json ID；链可达性检查（1c2 校验器） |
| V-3 | 印记引用存在 + 消耗 ≤ 上限 | [L203] | 红拦 | 承接 1d；判定：`consume_marks` 键 ∈ marks.json；值 ≥1 且 ≤ 印记上限字段 |
| V-4 | 元素 ∈ 注册表 | [L204] | 红拦 | 承接 1a；判定：element ∈ 8 元素注册表 [数 L220-221] |
| V-5 | 职业引用存在（jobs.json） | [L205] | 红拦 | **本文件 §4.3 落地**：`job_restrict[]` ⊆ jobs.json 职业 id；`job_form` ∈ 职业变换 transform 形态名（扩展判定，裁决定型） |
| V-6 | 派生倍率累计默认 ≤1.5×（可配） | [L206] | 黄提示（"注意数值膨胀"） | 承接 1a/1c2；判定：沿 chain_refs 可达链累计 power，超配仅在编辑器提示 |
| V-7 | 普攻技能：每职业恰 1 个 | [L207] | 红拦 | **本文件 §4.3 落地**：计数口径 = 按职业视角（无 job_restrict 的 basic 计入**所有**职业）；某职业可见 basic ≠ 1 → 拦截 |
| V-8 | 触发技能触发上限可配（默认每回合 10 / 每场 99，0=不限） | [L208] | 引擎强制（非配置错误） | **本文件 F20 落地**：技能级覆盖 > 库级 defaults > 全局默认；引擎执行期强制 |
| V-9 | 行动随机入表：probability 0=锚点 / 1=入池；weight 全 0 且无链/条件 → 提示"纯脚本怪？" | [L209] | 红拦（probability 非 0/1）/ 黄提示（纯脚本怪） | 承接 1e A02/A03；判定：probability ∈ {0,1}；随机池非空（至少一条 probability=1 且 weight>0，1e A03） |
| V-10 | 技能/行动 ID 唯一 | [L210] | 红拦（库内）/ 黄提示（跨库） | **本文件落地**：库内唯一 = 红拦；跨库（skills × action）重名 = 黄提示（同构单执行器按 ID 解析，跨库重名有解析歧义风险） |

### 3.2 细化增补 3 条

| # | 规则 | 级别 | 依据 | 判定细节 |
|---|---|---|---|---|
| V-11 | 未登记字段拒绝（防 schema 漂移） | 红拦 | [L243]（风险表"共用 schema 漂移 → ActionCore 元数据单点定义 + 校验器拒绝未登记字段"） | 字段必须 ∈ 元数据注册表（本文件 F01-F24 + G01-G16）；新增字段先登记元数据 |
| V-12 | 物理/魔法穿透对位校验 | 黄提示 | [效 L120-121/L325]；审计_B #4 建议 | `attack_type=magic` 或携带 element 的技能配 `pierce.target="def"` → 提示应改 `"spr"`；`pierce.target` 必须 ∈ {def, spr}（见 §5） |
| V-13 | 基础门禁 | 红拦 | 定稿三铁律②③ [L23-24] 派生 | id/name 非空；type ∈ 四枚举；kind ∈ 五枚举；数值字段（power/mp_cost/cooldown/hits/hit_mod/crit_mod）类型与取值范围正确；submit 前基础校验 |

### 3.3 校验时机

- 加载时：全库全量校验（loader 接线 3e 承接），失败拒绝加载并输出人话提示 [L191]。
- 热重载：保存即生效，增量校验（3e2 承接）。
- 编辑器：保存即校验 + "确认是故意设计？"式黄提示（不拦截）[L191]。

---

## ④ 职业技能绑定（jobs.json ↔ skills.json）

### 4.1 jobs.json 侧承接字段（本文件只登记接口，字段定义以 覆盖审计_H / 实现层规划 B5 为准）

| 字段 | 用途 | 绑定对象 |
|---|---|---|
| `id` | 职业唯一标识（skills 层 `job_restrict` 引用目标） | F16/F17 |
| `name` / `difficulty` / `playstyle` / `recommended_newbie` / `resource_axes` / `growth` / `weapon_types` / `mechanic_tags` | 职业卡（[审计_H] F-01；[规] B5） | 编辑器职业限制下拉数据源 |
| `transform`（职业变换段：形态定义 + 规则 3.3 + 平衡 3.4） | 形态技载体（`job_form` 引用目标） | F17；[审计_H] F-03/H-04 |

### 4.2 skills 侧绑定字段

- `job_restrict`（F16）：职业限制数组 [L78]，空 = 通用技能；编辑器下拉数据源 = jobs.json id [L164 动作卡"职业限制"行]。
- `job_form`（F17）：形态技 [L79]，值 = 职业变换 transform 形态名；形态激活时代替对应技能位 [L88]。

### 4.3 绑定规则（裁决定型，闭合 V-5/V-7 判定）

1. **引用存在性（V-5）**：`job_restrict[]` 每个值 ∈ jobs.json 职业 id；`job_form` ∈ 对应职业 transform 形态名——缺失即红拦 [L205]。
2. **普攻恰 1 个的计数口径（V-7）**：
   - 无 `job_restrict` 的 basic 技能（通用普攻）计入**所有**职业的可见集；
   - 带 `job_restrict` 的 basic 技能只计入所列职业；
   - 对每个职业：可见 basic 技能**恰 1 个**，否则红拦 [L207]；
   - 典型错误：通用普攻 + 某职业专属普攻 → 该职业可见 2 个 basic → 拦截。
3. **装配过滤**：技能列表按当前职业过滤（职业限制自动过滤 [L78]；通用技能全职业可见）；形态激活时 `job_form` 匹配技能替换对应技能位 [L88]。
4. **触发/被动装配**：passive/trigger 装配槽内容同样受职业过滤；职业变换时按新职业重算装配有效集（3b/3e2 存档承接）。

---

## ⑤ 魔法穿透语义裁决（ADR-6a-01：穿透 vs 减抗 vs 减防）

> 问题来源：《覆盖审计_B》P1 #4：1b 将 pierce 三修正器合并为单一"无视目标防御 X%"，**未承接"无视对方一定百分比精神"** [效 L121]。

### 5.1 定稿原文（证据）

| 条目 | 原文 | 行号 |
|---|---|---|
| 物理穿透 | 伤害结算时，无视对方一定百分比**防御** | [效 L120] |
| 魔法穿透 | 伤害结算时，无视对方一定百分比**精神** | [效 L121] |
| pierce 结算修正器 | 本次伤害无视目标防御 X%（value=30 · pct=true） | [效 L325] |
| 物理通道 | 攻击值 / 防御系数（K=100 / 有效体质 / 破防） | [数 L26-30] |
| 元素通道 | 装备元素攻击 / 元素减伤 | [数 L31-33] |
| 格挡 | min(40%, 专注/(专注+150))、×0.5、**魔攻击无视** | [数 L25] |

### 5.2 裁决 1：修正器参数化（不合并为单一"无视防御"）

- 效果系统 L0 结算修正器 `pierce` 保留单键，但**增加参数 `target`**（选填，缺省 `"def"`——幻觉审查_6a 修正：原「必填」与「缺省=def」自相矛盾，按 R-03 缺省 def 兼容既有登记值）：

```
pierce := { target: "def" | "spr", pct: 0-100 }
```

| target | 中文语义 | 作用通道 | 生效对象 |
|---|---|---|---|
| `def` | 物理穿透：无视目标防御 X% | 物理通道 [数 L26-30] | 防御系数 / 有效体质 段 |
| `spr` | 魔法穿透：无视目标精神 X% | 元素通道 [数 L31-33] | 元素减伤 / 精神对应抵抗段 |

- 默认：`target` 缺省 = `def`（兼容 [效 L325] 现有登记值，pct 默认 30 的示例不变）。
- 1b 修正器表应补记 `magic_pierce` 别名（`pierce{target:"spr"}`）条目，或直接以参数化 `pierce` 收敛——**两者等价，本裁决选参数化单键**（对齐审计 #4 建议二选一；避免双键漂移风险 [L243]）。

### 5.3 裁决 2：穿透 / 减抗 / 减防 三语义区分（不许混用）

| 机制 | 定义 | 是否改变目标状态 | 载体 |
|---|---|---|---|
| **穿透 pierce** | 结算时跳过目标防御/精神 X% 直接计算 | 否（只跳过百分比，不写入目标） | 攻击方修正器 [效 L325] |
| **减抗** | 降低结算中的抵抗因子（元素减伤系数 / 抗性门槛） | 否（作用于本次结算参数，目标抗性数据不变） | formula.json 元素减伤 [数 L31-33]；effects 修正器 |
| **减防（debuff）** | 降低目标属性值（stat_modifier） | 是（写目标属性，跨回合持续） | effects.json stat_modifier（1b 承接） |

- 判据：穿透**不读取、不修改**目标 resistance/抗性字段；减抗只降本次结算因子；减防持久改属性。三者叠加时按 判定顺序（穿透 → 减抗 → 扣血）[数 L14-17] 逐段生效。
- 怪物「元素减伤」以 effects 减抗或 enemies 抗性字段表达（1e W03/W04 承接），**禁止用 pierce 负数模拟减抗**（V-12 语义对位，黄提示）。

### 5.4 裁决 3：魔法穿透 × 魔攻击无视格挡（独立维度，可叠加）

- 魔攻击无视格挡 [数 L25] 是**格挡段**规则（格挡判定被跳过）；
- 魔法穿透 [效 L121] 是**元素通道结算段**规则（精神抵抗被跳过）；
- 两者位于不同判定段（段序：命中 → 会心 → 格挡 → 通道结算 [数 L14-17]），互不干扰，可同时生效：某魔法技可从"无视格挡"且"无视精神 30%"两个维度同时突破（TC-22）。
- 物理穿透与格挡：格挡判定在物理通道结算之前，物理穿透只影响防御系数段，不绕过格挡（格挡 ×0.5 仍生效）——若需要"穿透格挡"，配 `block_mode:"ignore"`（F24），与 `pierce.target="def"` 叠加（TC-20 断言边界）。

### 5.5 校验器落地

- V-12（黄提示）：pierce.target 对齐攻击类型（magic/带元素 → spr；slash/blunt/pierce → def）；pierce.target ∉ {def, spr} 红拦。
- 数值验证口径：魔法穿透验证用例（TC-19/TC-23）以"有效减伤系数 = 原系数 × (1 − pct/100)"为断言基准，实现细节随 1a 元素通道公式。

---

## ⑥ 验收测试用例（23 条：Schema 6 / 校验 7 / 引用 5 / 穿透 5）

> 编号 TC-01..TC-23；TC 与校验规则、解析规则逐条对应；断言口径对齐 [规 T24/T25/T27] 验收标准。

### Schema 组（6 条）

| 编号 | 用例 | 前置 / 输入 | 步骤 / 断言 | 依据 |
|---|---|---|---|---|
| TC-01 | 示例数据加载 | 定稿 §九 玩家 3 技能 + 怪物 3 行动（L217-232） | 全部加载通过：字段齐全、effect 引用样式两形态（对象引用 + type 原子动作）均解析 | [L217-232] |
| TC-02 | 缺省兜底 | 仅 `{"id","name","power"}` 的技能 | 加载成功：kind 自动推断 damage、attack_type 按武器、element null、effects []、mp_cost 0、cooldown 0、type 兜底 active（裁决定型：仅核心字段时按 active 处理，basic 必须显式） | [L23-24/L53] |
| TC-03 | hits 多段独立结算 | `hits:3` 的主动技 | 命中/会心/伤害逐段独立判定；连段按施放次数计（1 次施放 = 1 次连段计数）；不开放 `*` 数量 | [L11] + [规 T25] |
| TC-04 | 四类时机门禁与技能位 | 4 类技能各 1 个装配 | basic 固定第 1 位且仅 1 位；active 可排序；passive/trigger 进装配槽不占行动位 | [L60-65/L84-91] |
| TC-05 | 指令被拒无副作用 | 主动技 MP 不足 / 冷却中 / 条件不满足各一 | `/攻击 <序号>` 被拒：不耗回合、不改连段、MP 不变；可反复尝试 | [L63] + [规 T27] |
| TC-06 | 形态技替换技能位 | 职业变换激活形态 | job_form 匹配技能自动替换对应技能位；变换回原职业恢复原技能列表 | [L79/L88] + [规 T27] |

### 校验组（7 条）

| 编号 | 用例 | 输入 | 断言 | 依据 |
|---|---|---|---|---|
| TC-07 | V-10 ID 唯一 | 库内两条同 id 技能 | 红拦拒绝加载 | [L210] |
| TC-08 | V-7 普攻恰 1 计数口径 | ① 通用 basic + 职业 A 专属 basic ② 某职业 0 basic | ① 职业 A 可见 2 个 → 红拦；② 该职业可见 0 个 → 红拦；通用 basic 单独存在时全职业通过 | [L207] |
| TC-09 | V-8 触发上限强制 | 装配触发技，触发条件连续满足 | 默认每回合第 11 次触发不执行、计数不越界、消息折叠；`trigger_limit: {per_round:0}` 时不限 | [L208/L244] |
| TC-10 | V-4 元素注册表 | `element:"thunder"`（注册表内）vs `element:"ice"`（未注册） | 前者通过；后者红拦（8 元素 [数 L220-221]） | [L204] |
| TC-11 | V-6 倍率累计提示不拦截 | 派生链累计 1.8× | 加载通过，黄提示"注意数值膨胀" | [L206] |
| TC-12 | V-9 概率语义 + 纯脚本怪 | 行动 `probability:2`；行动 weight 全 0 且无 chain/条件 | probability:2 红拦；全 0 黄提示"纯脚本怪？"不拦截；随机池空 → 红拦（1e A03 派生） | [L209] + 1e A03 |
| TC-13 | V-11 未登记字段拒绝 | 技能带 `foo` 字段 | 红拦"未登记字段：foo"（防 schema 漂移） | [L243] |

### 引用组（5 条）

| 编号 | 用例 | 输入 | 断言 | 依据 |
|---|---|---|---|---|
| TC-14 | V-1 效果引用存在 | effects 引用不存在的效果 ID | 红拦；overrides 参数合并正确 | [L201/L123-127] |
| TC-15 | V-2 派生链引用 / 死配置 | chain_refs 指向不存在链；指向死链（不可达） | 前者红拦；后者黄提示（可达性检查） | [L202] |
| TC-16 | V-3 印记引用与消耗上限 | consume_marks 键不在 marks.json；值超上限 | 红拦（两种均拒） | [L203] |
| TC-17 | V-5 职业/形态引用 | job_restrict 引用不存在职业；job_form 引用不存在形态 | 红拦（job_restrict 任一值缺失即拒） | [L205] + §4.3 |
| TC-18 | 多引用联动加载 | 火球术式技能：element + effects(burn + mark_add) + chain_refs + consume_marks 全配 | 加载通过；引用分辨率全命中；引擎同一执行器入口（探针断言技能与行动走同一函数，[规 T24]） | [L135-146] + [规 T24] |

### 穿透组（5 条）

| 编号 | 用例 | 输入 | 断言 | 依据 |
|---|---|---|---|---|
| TC-19 | 魔法穿透无视精神 | 魔法技：`pierce{target:"spr", pct:30}` | 元素通道结算时有效精神抵抗系数 ×(1−30%)；pierce 只跳结算段，目标 spr 属性值不变 | [效 L121/L325] + §5.3 |
| TC-20 | 物理/魔法对位 | 物理技 target:"spr"；魔法技 target:"def" | 各作用于对应通道：physical 触发式只减物理防御系数段、不影响元素通道；反之亦然；`block_mode:"ignore"` 是格挡段规则不与穿透混淆 | [效 L120-121] + §5.4 |
| TC-21 | 穿透 vs 减抗区分 | 同目标：pierce 30% vs 元素减抗 30 | 穿透：目标 resistance/抗性字段不变，仅跳过 30% 结算；减抗：本次结算系数降低；两者数值路径分离（断言目标数据未被穿透写入） | [效 L120-121] + [数 L31-33] + §5.3 |
| TC-22 | 魔法穿透 × 魔攻击无视格挡 | 魔法技（attack_type=magic）+ pierce{target:"spr"} | 格挡段被跳过（魔攻击无视 [数 L25]）+ 精神段被穿透 30%，两段独立叠加生效 | [数 L25] + [效 L121] + §5.4 |
| TC-23 | V-12 对位校验 | 带 element 的魔法技配 pierce{target:"def"} | 加载通过 + 黄提示"魔法通道建议 target=spr"；pierce{target:"hp"} 红拦（target 枚举外） | [效 L120-121] + §5.5 |

---

## ⑦ 存档 / 热重载影响（登记接口）

| 事项 | 落点 | 说明 |
|---|---|---|
| 装配结果（被动/触发槽、技能排序） | 玩家存档（1g1c/1g3） | 进战斗按装配快照生成技能列表 |
| 冷却计数 / 触发计数 | 战斗状态数据（1g1c） | 冷却回合数、触发上限计数随战斗状态存档/快照 |
| level 升级状态 | 玩家存档（熟练度/属性层 3b 承接） | growth 倍率按已升级数生效 |
| 热重载 | 3e2 | 技能/行动库改动保存即生效，增量校验（§3.3） |
| 编辑器元数据注册表 | 5a | 全字段（F01-F24、G01-G16）须登记于动作字段注册表（字段名/中文名/类型/默认值/范围/引用目标）后方可在表单中出现（V-11 依此判定）[规 T19] |

---

## 附：本文件字段/规则/用例计数速览

- skills.json 全字段 **24 个**（A 共用核心 7 + B 玩家扩展 11 + C 全库补充 2 + D 细化定型 4）
- 校验规则 **13 条**（定稿 10 条全量 V-1..V-10 + 细化增补 V-11..V-13）
- 验收测试用例 **23 条**（Schema 6 / 校验 7 / 引用 5 / 穿透 5）
- 裁决 3 项：tag/布尔/effects 三方一致性（1.3-f3）、普攻恰 1 计数口径（§4.3-2）、魔法穿透参数化 pierce{target}（§5.2）