# 任务书 · 批8·路B（调查/线索 18 键）

> 2026-09-12 · 消息模板重构战役 · 执行者：子 agent（本任务书 + `docs/消息模板重构/00_方案与规范_v1.md` 为唯一指令源）
> **先读** `<worktree>/docs/消息模板重构/00_方案与规范_v1.md`（全量口径：结构化行 ≤14 全角 / 免斜杠 /
> 少｜多换行 / 错误行 ❌+原因(+下一步) / 拆行优先序 / 大数值独立行 / 零装饰 emoji）再开工。

## 1. 范围（本路 18 键，全部在 `qbot_rpg/core/templates/investigate_tpl.py`）

investigate_ambient_1, investigate_ambient_2, investigate_ambient_3, investigate_ambient_4,
investigate_ambient_5, investigate_codex_ref, investigate_discover_card, investigate_discover_label_boss,
investigate_discover_label_map, investigate_eggshell_done, investigate_env_header, investigate_hidden_map_done,
investigate_hidden_map_text, investigate_hunt_done, investigate_hunt_intro, investigate_hunt_signal,
investigate_no_map, investigate_register_gate

**每键必做**：读旧文案拿到「信息要素 + 触发场景 + 占位符集合」，然后**按规范全新撰写**（旧文案不作参考）。
占位符沿用（`{name}` `{count}` …）；`tpl_of(` 调用点的键名与参数名**不得改**。

## 2. 工作清单（每步都落盘）

1. **第一件事**：写 `<worktree>/docs/消息模板重构/_fragments/b8b.json`
   （`{"rail":"b8b","templates":{…}}`，18 键新文案，先落盘再动代码）→ commit。
2. 合表：`python3 scripts/tpl_merge_fragments.py docs/消息模板重构/_fragments/b8b.json`
   （表 = `qbot_rpg/core/templates/template_table.json`）。
3. 清分区：从 `investigate_tpl.py` 删除已迁 18 键（文件其余内容保留）；`content/test_demo/templates.json`
   同 key 同步为新文案（没有的键补上）。
4. 代码/测试同步：`grep -rn "<key>" qbot_rpg/ tests/ scripts/` → 模块级常量别名、断言旧文案、
   docstring 引用逐处更新；**不得**为过测试改回旧文案。
5. 自校验：`.venv/bin/python scripts/check_template_width.py`（本路键 0 FAIL）；
   `pytest tests/unit/test_template_table.py tests/unit/test_emoji_discipline.py -q`；
   定向跑本模块测试；**最后跑一次全量** `pytest tests/ -q`（只报计数）。
   （工作树无 venv：`cd <worktree> && PYTHONPATH= /root/QBot-TurnTellerRPG/.venv/bin/python -m pytest …`）
6. `.venv/bin/ruff check <改动文件>` 干净；commit（`feat(模板·路B): …`）。**禁止 push / 禁止动 main**。

## 3. 报告（回复正文**内联**）
- fragment 路径 + 18 键新文案（JSON 全量内联，勿只写摘要）；合表结果（新增 N/冲突/宽度 FAIL-WARN）
- 改动文件清单；checker 输出；定向+全量测试计数原文；未完成/存疑项；commit hash
- **预算纪律**：临近工具/时间上限时优先 (a) fragment 已落盘 (b) 已改代码已 commit (c) 如实列未完成项。
  绝不允许谎报完成、编造输出、留半成品。
