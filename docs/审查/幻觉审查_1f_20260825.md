# 幻觉审查报告（复审）：《细化_1f_怪物AI状态机.md》

## 头部

- **审查对象**：`/root/QBot-TurnTellerRPG/docs/细化/细化_1f_怪物AI状态机.md`（266 行，v1.0）
- **性质**：08-18 首审（幻觉审查_1f.md，1 P0/1 P1/5 P2）后的**实现前最终复审**——重点确认首审 P1/P2 是否修复闭环 + 新增实现视角缺口检查
- **核对基准**（全部 read_file 实测）：
  - 《怪物行动AI设计定稿.md》v1.0.1（322 行，下文《定稿》）
  - 《怪物模块设计定稿.md》v1.0.3（**398 行**，首审时为 396 行——行号漂移重点区）
  - 兄弟细化：细化_1e_怪物八段schema.md（v1.1，279 行，已复审修订）、细化_1g4_战斗世界边界.md（v1.0，334 行）
  - 首审报告：幻觉审查_1f.md（83 行）
- **方法**：通读 1f 全文 → 收集全部出处声明（§〇-§十 章节级引用，无行号级引用）→ 回《定稿》逐条内容匹配核验 → 与 1e v1.1 逐字段交叉比对 → 实现视角逐项推演（运行结论均为静态推导，本环境无 bash 沙箱）
- **四查口诀执行**：①行号引对 ②内容一致 ③补白显式标注 ④数值有依据

---

## 总体结论

**P0 = 0｜P1 = 1｜P2 = 10｜评分：B**

一句话：无内容级新幻觉、行号漂移零影响、与 1e 接口基本对齐，但**首审 P1-1 与 P2-1~5 共 6 项全部未闭环（仅 P0-1 已修）**，另新增 5 项实现视角/交叉一致性缺口，建议修补后再进入实现。

评分依据：P0=0（原 P0-1 已闭环，无新编造/实质冲突）；P1=1（未闭环）未触发"P1≥2 → B 至多"上限，但 P2=10 超出 B+ 的 P2≤5 阈值，故定 B。

---

## P0（编造 / 实质冲突）

### 无

首审 P0-1（随机池归一化示例"火球 probability=1"与定稿§九"火球=锚点"实质冲突，波及 L152/203/229）**已闭环修复**——详见"核对亮点"节。

---

## P1（出处引错 / 补白漏标 / 数值无依据）

### P1-1（复检，未闭环）：硬约束出处仍标《定稿》〇，实际位于定稿头部引言

- **位置**：1f 行 6「硬约束（《定稿》〇）：1v1 回合制 / QQ 文字 / 一轮一条消息 / 碎片化 / 小白优先 / 只建议不限制」。
- **核对**：《定稿》行 7「硬约束：1v1 回合制 / QQ 文字 / 一轮一条消息 / 碎片化 / 小白优先 / 只建议不限制」逐字一致，但该行位于文件头部（标题/版本之后）；§〇（行 11-30）为"核心规则 1-8"，不含此硬约束列表。"只建议不限制"虽与核心规则 8（行 29"校验器只提示不拦截"）同义，但整条列表出自行 7。
- **证据**：《定稿》行 3-7（头部版本/定位/一句话/硬约束）、行 11-30（§〇 核心规则）。
- **定性**：内容真实、章节标注偏差（"〇"应为"文件引言"）。语序与首审 L26-30 完全一致，**未修复**。
- **建议**：改为「硬约束（《定稿》文件引言 L7）」或「硬约束（《定稿》头部）」。

---

## P2（轻微）

### P2-1（复检，未闭环）：TC-20 命名"验收四判据"与实际项数、定稿判据数仍不符

- **位置**：1f 行 246（TC-20）。
- **核对**：用例名仍为"验收四判据"，期望列 6 项（可读性/差异性/应对面/博弈深度/先教后考/QQ 适配）；《定稿》§十（行 311-317）共 7 条判据，TC-20 未覆盖"中断恢复 10 秒进入状态（含套进度）——碎片化"（行 316；该判据实际由 TC-12 承接）。命名（四）与实际（6 项）、《定稿》（7 项）三者仍不一致。
- **建议**：改名"验收判据（承 §十）"并在期望补第 7 项"中断恢复 10 秒进入状态"或注明由 TC-12 承接。

### P2-2（复检，未闭环）：L1-L7"逐级短路"补白仍未标【细化】

- **位置**：1f 行 86「L1-L7 逐级短路：高优先级级有产出即停，不落入低优先级级【§四 结构】」。
- **核对**：《定稿》§四（行 130-143）按序列出步骤 0-6 + 步骤 6"兜底"（行 141），顺序结构可推出短路语义，但全文无"有产出即停"显式声明；属合理推断性补白，未按行 5 引用纪律标【细化】。
- **附加微瑕**：行 86"有产出即停"对 L1 字面不成立——L1 状态机切换的产出（enter_action）是**流入 L2 强制队列**而非停止（行 75 产出/去向列已正确表达）。建议措辞改为"L2-L7 逐级短路（L1 的切换产出入 L2 队列）"或保留 L1 例外说明。
- **建议**：标【细化】+ 修正 L1 例外表述。

### P2-3（复检，未闭环）：行为态 patrol 行"battle_start 后转战斗"补白仍未标【细化】

- **位置**：1f 行 22（patrol 巡逻行）。
- **核对**：《定稿》行 100 仅列状态序列"巡逻 → 战斗 → 狂暴 → 濒死"；"战斗前/默认；battle_start 后转战斗"系结合 §二④（行 66 battle_start）的推断；表格标题"进入条件（示例）"（行 20）仍只作半显式标注，未按行 5 纪律逐条标【细化】。行内"待机"效果亦为补白（定稿未定义 patrol 效果）。
- **建议**：patrol 行补【细化】标注（或整表加"进入条件为示例级补白【细化】"注）。

### P2-4（复检，未闭环）：执行态行级标注颗粒度仍不一致

- **位置**：1f 行 35-42（执行态表）。
- **核对**：行 12 节首已标"两级状态模型【细化分层】"、行 16 说明执行态为细化拆分（快照 ai_state 承载【§八 A.5】）；downed 行（行 42）单独标"被击倒类效果【细化】"，而 idle（行 39）/in_chain（行 40）/charging（行 41）行未逐个标注——颗粒度不一致仍在。定稿无"执行态"概念（§八 A.5 行 275 仅登记快照字段），执行态整体确属细化分层。
- **建议**：执行态表表头加"整体为【细化分层】"或逐行标注，与 downed 行口径统一。

### P2-5（复检，未闭环，微扩展）：残血狂暴 60 vs 50 张力仍未提示；TC-04"阈值单源"存在驱动口径依赖

- **位置**：1f 行 63；关联行 205（TC-04）。
- **核对**：
  - 行 63「血量跨 phases 阈值（§六.1 阶段2 狂暴期）→ 套间联动切换 enraged」——《定稿》§六.1 阶段 2 为 60%-30%（行 172），状态机示例 transitions 条件为 hp_below 50（行 120）；§九行 295 亦将"狂暴状态"挂于阶段 2（60 起）。60（阶段）与 50（状态机）落差未提示（该张力系定稿自身残留，非细化新造，首审已判）。
  - **扩展发现**：TC-04（行 205）期望"修改 phases 60→55 后……阶段表与状态机联动阈值同步变化（状态机不重复声明血量阈值）"——该期望仅当状态机转换由 **phase_changed 联动驱动**（§三 边界声明行 97 口径）时成立；若按 §三 示例 JSON（行 119-122）的 **hp_below 直接阈值转换**配置，改 phases 阈值不会联动。两套驱动机制（联动 vs 直读）在《定稿》内部并存，1f 未裁决，实现者照 TC-04 写测试可能失败。
- **建议**：行 63 加注"阶段阈值 60 与示例转换条件 50 的落差为定稿既有，配置时以 phases/transitions 实际值为准"；TC-04 前置补明驱动机制裁决（推荐：状态机转换一律走 phase_changed 联动，示例 JSON 的 hp_below 视为演示写法并提示迁移）。

### P2-6（新增，交叉一致性）：hungry 默认值口径"无"与 1e/AI 定稿"0=关闭"不齐

- **位置**：1f 行 144（4.1 字段表 hungry 行，默认列="无"）。
- **核对**：1e v1.1 A03d「hungry …… 默认 0=关闭（可配 settings/enemies.json 覆盖）」；《定稿》行 43「示例值；默认建议 0=关闭饥饿保底」。《定稿》给出了明确默认 0，1f 写"无"虽不冲突（语义=无保底），但字段默认值口径应与 1e/定稿统一为 **0**（四查④数值有依据）。
- **建议**：默认列改"0（关闭）"。

### P2-7（新增，交叉/实现）：intent.category 与 action.json `intent` 字段的同名碰撞与绑定缺失

- **位置**：1f 行 94-104（3.1 intent 运行时结构）+ 行 129（3.4 绑定规则）。
- **核对**：1f 定义 intent.category ∈ {charge 蓄力 / gather 积蓄能量 / stance 攻击架势}（依《定稿》§五行 151 L1 类别三值，正确）；但《怪物模块设计定稿》§六行动数据库的 action.json 也有 `intent` 字段（行 132："intent": "蓄力", // 意图预告（伤害/防御/蓄力/治疗））——**两个同名 intent、两套值域**（3 类预告类别 vs 4 类行动意图属性）。1f 3.1 称 intent 为"运行时快照【§八 A.5】"结构，3.4 绑定规则只覆盖 preview/preview_chain/reveal_condition 三个预告字段，**未声明 intent.category 的运行时来源**（由 action.json 的哪个字段/如何映射而来；伤害/防御/治疗类行动是否有 L1 预告、归属哪个 category 均未定义）。实现者误用 action.json.intent 直填 category 将产生枚举值域错误。
- **建议**：在 3.1/3.4 补一句映射裁决（如"L1 category 由 action.json 预告字段/行动类型推导，与 action.json intent 字段（伤害/防御/蓄力/治疗）的映射表见动作侧细化"或声明值域收敛为 §五 三值），消除同名字段歧义。

### P2-8（新增，实现视角）：执行态迁移表缺失；downed 未入决策管线总图

- **位置**：1f 行 35-42（执行态表）、行 46-56（1.2 决策管线总图）。
- **核对**：执行态 4 态给出"含义/行为"，但 idle↔in_chain↔charging↔downed 的完整迁移（何时入/何时出）未成表——in_chain→idle（套结算完）、charging→idle（蓄力完成/被打断后去向）、idle→downed（被击倒）、downed→idle（起身完成）仅散见于行 60-64 与 TC-05/TC-18。1.2 总图"回合开始 → 套内门（在途链/蓄力中？否→套间评估）"**未画 downed 分支**：downed 状态下 BOSS 行动槽做什么（跳过行动？起身演出占用回合？）无显式规定，实现者需自行推断。
- **建议**：补一张执行态迁移表（入/出条件+去向），并在 1.2 总图加 downed 分支注"起身演出占用行动槽，完成后进入套间评估（get_up 由 L3 评估）"。

### P2-9（新增，实现视角）：虚弱窗口提示的触发机制未定义且无 TC 覆盖

- **位置**：1f 行 125（3.3 表"虚弱窗口提示"行）。
- **核对**：「灭世龙息被打断后的虚弱窗口【细化：承接 §九 玩家博弈】」——《定稿》行 303 仅作为"喘息"点提及"灭世龙息被打断后的虚弱窗口"，无机制定义（何时出现/持续几回合/效果）。1f 将其列为预告类型（分级"公开"），但：a) 窗口本身何时成立未定义；b) TC-17（行 238 打断=套完结）期望仅"打断演出 → 下一回合 BOSS 进入随机流程"，**不含虚弱窗口**——该预告行无任何用例覆盖；c) 3.3 属预告产出规则，却暗含一个未定义的战斗效果事件源。
- **建议**：3.3 该行加注"虚弱窗口的时长/效果由效果系统侧细化定义（此处仅登记预告）"，或补一条 TC（打断蓄力后出现虚弱窗口提示）。

### P2-10（新增，实现/接口）：AI 决策管线与战斗回合状态机的挂接点未声明；charge_* 结构依赖外侧细化

- **位置**：1f 行 48（"回合开始 → 套内门……"）、行 62（"action.json charge_* 字段（登记于 4.2.1）"）。
- **核对**：
  - 框架固定玩家先手（1g4 TIME-02 引【框架】L114"玩家行动 → 怪物反击"），怪物 AI 评估/行动发生在**怪物反击槽**而非"回合开始"；1f 未声明与 1g1a/1g1b/1g2 战斗状态机及 1g4 世界层边界的挂接点（快照续玩由 1g3 承载、死亡/丢失由 1g4 承接），"回合开始"措辞易被实现者误读为回合最先执行。1f 全篇未引用任何战斗状态机兄弟细化。
  - charge_* 字段（蓄力回合数/进度）仅《定稿》行 271 登记键名前缀，结构未定义；1f TC-18（行 239"蓄力 2 回合"）与 3.1 progress("1/2") 依赖该字段——需 action.json 侧细化（1d 系）先落地，否则蓄力跨回合无法实现。
- **建议**：1.2 前加一行挂接声明（"本管线在怪物行动阶段（玩家行动后）执行；战斗主循环状态机归 1g1a/1g1b，世界层边界归 1g4"）；登记 charge_* 结构为跨细化待补项（实现依赖前置）。

---

## 核对亮点（首审闭环情况与通过项）

### 首审闭环逐项
- **P0-1 已闭环 ✓**：行 152 已改为"火球为**锚点**（probability=0，仅链触发，不入随机池）；随机池 = 爪击 40 / 尾扫 20 / 吼叫 10（probability=1）→ 归一化 ≈ 57.1% / 28.6% / 14.3%"，狂暴重算"爪击 60 / 尾扫 30 / 吼叫 5 → ≈ 63.2% / 31.6% / 5.3%"（40×1.5=60 ✓、20×1.5=30 ✓、10×0.5=5 ✓，Σ=95，60/95=63.16% ✓、30/95=31.58% ✓、5/95=5.26% ✓，数学自洽）；TC-02（行 203）、TC-13（行 229）同步修正；与 TC-14（火球 probability=0 永不直接出池）、TC-16（火球为链起点）**矛盾消除**。tag 归属（吼叫=防御类）已标【细化】。
- **P1-1、P2-1~P2-5 均未闭环 ✗**：逐条核对见上文 P1-1、P2-1~P2-5——6 项原样保留。其中 P2-5 实际还新增了 TC-04 驱动口径问题（见 P2-5 扩展）。

### 行号漂移全量核验（本次重点②）
- **结论：1f 全篇无行号级引用**（逐行扫描仅见章节级 §〇-§十 / 核心规则 N / 内部 L0-L7 分级标记），《怪物模块设计定稿》396→398 行漂移对 1f **零影响**；也无需做"按偏移猜"的校正。
- 1f 全部章节引用已回《定稿》（322 行）内容匹配实测：§一 L34-55（字段 L39-44）、§二 L59-91（13 类触发 L63-75、JSON L78-89）、§三 L95-124（边界声明 L97、状态序列 L100-106、JSON L108-124）、§四 L128-143（套内门+0-6）、§五 L147-163（L1-L3 分级 L151-154、消息模板 L157-162）、§六 L167-211（6.1 L169-176 / 6.2 L178-185 / 6.3 L187-195 / 6.4 / 6.5）、§七 L215-226、§八 L230-284（chains 载体唯一化 L248-266、登记 L268-278〔A.5=L275、4.2.1=L271〕、校验器 L280-284）、§九 L288-304、§十 L308-318（7 判据 L311-317）、§〇 核心规则 1-8 L11-30——**全部指向有实义文本的正确章节**，无空靶、无张冠李戴。
- 1f 引用的"快照 ai_state（state/phase/chain_pos/chain_queue/charge/trigger_cooldowns/intent）"（行 16/35/61/92/223）对应《定稿》行 275 登记字段——逐字段一致 ✓。

### 交叉一致性（本次最重点③，与 1e v1.1 对齐情况）
- ① 4.1 字段表 vs 1e A01-A03d：action/weight/condition/cooldown/probability **五字段名、默认值、语义完全对齐**（probability 默认 0、1=入池、其他正值等价 1 ⇔ 1e A02 ⇔《定稿》L25/L44/L51，逐字一致）；唯一措辞差异 = hungry 默认值（P2-6）。
- ② L2/L3 条件行动字段：1f 行 77 引用 once / max_triggers / trigger_cooldown 过滤 ⇔ 1e A10/A12/A13（同源《定稿》L81/L84-85）——**已引用到**，字段名一致，无遗漏；13 类枚举（行 256 注明"权威枚举=AI 定稿 §二（细化_0 R-01，1e 已同步）"）与 1e S3 裁决一致 ✓。
- ③ chains：1f §五（顶层 chains 唯一载体、节点 {action, chance, role, armor}、chain_ref 引用链 ID、chain_queue 只由顶层 chains 驱动）⇔ 1e F14/1.4 chains 行（{id, actions[]}、chain_ref→chains.id、AI L248-266）——结构一致；唯一粒度差异：armor 在 1f 为 4 字段固定项（依《定稿》L260 原文）、1e 标"armor?"选填（依 L254 样本泛化），方向相反但不构成实现冲突（两侧校验均未将 armor 列为必检），建议顺手统一。
- ④ intent 字段 vs 快照 ai_state（A.5）：1f 3.1 将 intent 置于 ai_state（A.5）内 ✓ 与《定稿》L275 一致；1e A.5 侧不展开 intent 结构，无冲突；但 intent.category 与 action.json `intent` 字段（怪物模块 L132）的绑定缺失见 **P2-7**。

### 其他通过项（抽样复核无误）
- 1.1 边界声明转述（行 14）↔《定稿》L97：逐意一致（含"60/30 双阈值已收敛"）；行为态 4 态与 JSON（行 22-25）↔ L100-122 全部吻合（normal weight_mod {}、enraged {attack:1.5, defense:0.5}+roar+[fire_burst]、dying {attack:2.0}+death_rattle+[final_strike]、transitions hp_below 50/15）。
- L0-L7 映射（行 74-81）↔ §四 步骤 0-6 + 套内门：8 决策点、顺序、名称、出处全部对应（L1=步骤0 … L7=步骤6；套内"跳过 L1-L7"与定稿"跳过 0-5，结束"效果一致）。
- 3.3 预告类型表 7 行 <-> L159-162/L174/L192/L303 全部有据；4.2 归一化 7 条与核心规则 4/6/8、§八 校验器（L282-283 硬拦/黄提示分类）一致；5.1-5.5 与 §八 L260-264、§二 L91、核心规则 1/2/3/7、§六.2 L180-183 一致。
- TC-01/03/05/06-12/14-19 前置与期望均锚定真实条款（TC-12"10 秒恢复"L316 ✓、TC-19"PV 恢复一半"L193 ✓、"熔岩坑道/矿洞"L298 ✓、TC-18 蓄力 2 回合 L297 ✓）。
- 内部自洽：TC 与正文规则一致（TC-01↔L63/L24、TC-02↔L152、TC-05↔L42、TC-08↔L74、TC-09↔L81、TC-17↔L183）；状态数 8（4+4）与 20 例 6 组（5/4/3/3/3/2=20）计数正确；行为态无死状态（dying→战斗结束；downed 有 get_up 出口）、无歧义迁移（切换仅套间）；13 类枚举、L0-L7 表、3.2 分级三表无自相矛盾。
- 首审报告"核对通过的主要声明"11 项在本轮复检中全部继续成立。
- 轻微引用目标微瑕（不单列）：行 61"中途不被条件打断"引【核心规则2】，该短语原文在核心规则 1（L14-15），核心规则 2 覆盖"确定性执行"——建议并引"规则1、2"顺手修正。

---

## 实现视角缺口清单（本次新增维度，逐项踩坑判断）

| # | 视角 | 现状 | 实现者按此落地会不会踩坑 |
|---|---|---|---|
| G1 | 两级状态模型（行为态 4 + 执行态 4） | 行为态：表+规则+示例 JSON 齐备，可直接编码 ✓；执行态：4 态语义可理解但**无迁移表**，downed 未入 1.2 总图 | **踩坑概率中**：downed 期间行动槽行为、charging 被打断后的状态去向需自行推断（P2-8）；建议补执行态迁移表后编码 |
| G2 | 套间评估 8 级优先级/短路 | L0-L7 表内"优先级+产出/去向"列完整、可编码 ✓；L86 短路总则未标【细化】且对 L1 字面不成立（产出流入 L2 非停止） | **踩坑概率低-中**：按表即可，但短路措辞需按"产出/去向"列理解；建议修 L86（P2-2） |
| G3 | intent 运行时结构与 action.json 预告字段绑定 | preview/preview_chain/reveal_condition 来源明确（§八 4.2.1）✓；**intent.category 运行时来源未绑定**，且与 action.json `intent` 字段（怪物模块 L132，伤害/防御/蓄力/治疗）同名碰撞 | **踩坑概率中高**：误用 action.json.intent 直填 category 将产生值域错误；伤害/防御/治疗类行动有无 L1 预告未定义（P2-7）——实现前须裁决映射 |
| G4 | 与战斗状态机（1g1 系）/1g4 交互边界 | phases vs ai.states 分工已声明（定稿内）✓；**与 1g1a/1g1b/1g2 回合时序挂接点未声明**（"回合开始"措辞 vs 框架 L114 玩家先手→怪物反击）；快照续玩/丢失/死亡边界 1f 未引用 1g3/1g4 | **踩坑概率中**：需自行对齐玩家先手时序与战斗主循环挂接；建议加挂接声明（P2-10） |
| G5 | 前置依赖 | charge_* 字段结构（蓄力回合数/进度）未定义，1f TC-18/progress 依赖之 | **踩坑概率中**：需 action.json 侧细化（1d 系）先行，否则蓄力跨回合用例无法实现（P2-10）；虚弱窗口机制同缺（P2-9） |

---

## 结尾核对基准说明

- 本次复审基准均经 read_file 全量实测：AI 定稿 322 行（v1.0.1，2026-08-16）、怪物模块定稿 **398 行**（v1.0.3，较首审的 396 行新增 pv_half_ratio ③b 等 2 行——已核实不影响 1f，因其无行号级引用）、1e v1.1（279 行）、1g4 v1.0（334 行）、首审报告（83 行）。
- 本环境无 bash 沙箱，未执行任何命令/脚本；所有运行/校验结论（归一化数学、迁移可达性、TC 期望可行性）均为**静态推导**，推导过程已在本报告内给出可复核算式与行号证据。
- 复审结论：**P0 = 0 / P1 = 1 / P2 = 10，评分 B，落盘 2026-08-25**。建议：修 P1-1 出处标注（1 分钟级）；P2-5 的 TC-04 驱动口径、P2-7 的 intent 映射、P2-8 的执行态迁移表、P2-10 的时序挂接 4 项为**实现前必须裁决**事项，其余为标注纪律项。