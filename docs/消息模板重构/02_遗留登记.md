# 02 · 遗留登记（跨批清扫清单）

> 消息模板重构战役 · 各批报告中「越批/待清扫」项汇总。终批（清扫批）逐项处理：迁表或按豁免结论登记。
> 更新：2026-09-12（批1/批2 收口）

## A. 引擎/跨模块渲染文案（待专批）

1. `qbot_rpg/core/checkin.py` 引擎侧中文串约 15 处（如 `今天已签到（重复指令，未重复发放）`、
   `连签天数…｜进度…`、`[连签里程碑达成]…`、补签族 `❌ 当前未开启补签 / 补签已达上限 / 需要补签卡…`）——
   经 `message` 透传直达玩家；批2·路E 报告 §7 有全清单。
2. `qbot_rpg/commands/gm_commands.py`：`_EMPTY_LOG`（与表内 `log_sys_empty` 同文）；`render_log_line` 族
   （GM 视图；含 `/` 前缀属管理类允许，宽度由数据决定）。
3. `qbot_rpg/commands/sender.py`：`format_tpl12`（`❌ 指令不正确：{片段}。输入 /帮助 查看可用指令。`）——
   跨模块格式错误 helper，影响面大，需专批统一新式（免斜杠、拆行）。
4. `qbot_rpg/world/movement.py`：`_REASON_NO_EXIT = "此方向没有通道"` 等世界层原因串（经 `❌ {reason}` 透传）。
5. 各模块 `_TAIL_TIP` 旧风格 Tip：`checkin_commands._TAIL_TIP`（`发送'签到 补签'即可补签`）等——
   待跨模块 Tip 统一批（对齐 `Tip:发 …` 新式）。
6. 数据层中文标签（品质/槽位/属性名映射、`_ATTR_CN` 类）——批1 起已按「数据映射豁免」处理；
   清扫批仅复核，不迁表。

### 批3 新增（2026-09-12）

7. `qbot_rpg/core/alchemy*.py`（引擎侧兜底文案：材料不足/配方不存在等；「温室」族文案；
   `_ASSIST_BONUS_POOL` 值片段）——批3·路H 报告 §5 有清单。
8. `qbot_rpg/core/forge_king.py`（`未获铸造王`，经 forge_king_gate 透传）、`core/forge_material.py`
   （2 条合成拒绝文案）、`core/forge_progress.py`（`需先锻造/缺/来源` 拼接串）——批3·路I 报告 §5。
9. `qbot_rpg/core/templates/forge_tpl.py:75`（`forge_sets_empty` 长行）E501 **存量基线**；
   forge#2 重做 sets/augments 段时随重写消除。
10. 战斗 HUD 大数值边际：`battle_start_line`/`battle_player_hit` 等在「目标名 10 字＋HP 8/8 位」极端组合下
    估算 >28 半角（已按数值紧贴最小化，无进一步压缩空间）——批3·路G 报告 §issues，属可接受边际。

### 批4 新增（2026-09-12）

11. `battle_no_item_arg` / `battle_no_item`：**疑似孤键**（无玩家入口——`道具` 指令不存在、战斗道具入口已删）——
    批4·路J 报告 §7；清扫批裁决（保留 or 删除）。
12. `使出{name}` 拼装动作标签（battle_render 侧，玩家可见但属数据标签）——battle 尾批处理。
13. `m_stat_parts` 词条行内含 ｜、`HELP_GROUPS` 目录描述 → 待「装备详情」/「帮助目录」批。
14. `alchemy_feed_chain_effect` 去头口径、`alchemy_inherit_gold_slot` 换行口径：批4·路K 存疑 2 项（已按新式落表，
    如需回跳单点改）。
15. 战斗「结果行不重复血量」口径为全战斗统一（批4 拍板）：敌我受击/未命中行均已去 HP 括注（血量只在 HUD 两行）；
    若后续批发现残余重复行（如 `battle_enemy_*`），按同一口径清理。

### 批5 新增（2026-09-12）

16. **引擎层直出文案（专项批）**：`qbot_rpg/core/battle.py` 的 `outcome.message` 直出串（防御指令说明/被控跳过/
    逃跑成功/道具使用成功/疲劳自摔/方位未命中原因）；`alchemy_deep.py` / `alchemy_meta.py _DEFAULT_TUTORIALS`
    （数据文案，含裸斜杠示例）；`alchemy_materials.py` 兜底（材料不足/配方不存在）；`core/equipment.py` 旧硬编码
    原因串（超宽；建议让引擎只回 reason 走 reason 键）。
17. `alchemy_buff_fail` / `alchemy_buff_fail_mark` 文案相同 → 合并候选（清扫批判定）。
18. `basic_equip_wear_fail` 死键已清除（仅 test_demo 定义、代码零引用）；`_REGISTER_GUIDE` 常量删除（零引用祛硬编码）。
19. e2e smoke 的「pack 校验未通过 12 error(s)」为**预存现象**（HEAD 复现，非本战役引入）——终验时复核。
20. test_demo 镜像同步：批1-4 迁移键的陈旧覆盖已同步至当前表值（本轮 11 键）；剩余「表外旧键 214 条」待收敛批。

### 批6 新增（2026-09-12）

21. **明细块 16 行折叠预算按物理行计（已修）**：`battle_render.render_battle_end` 的
    `_render_summary_block(overhead=len(lines))` 原按**元素数**计，BREP-24 三行化后（一个元素含 3 行）
    预算虚高 2 行 → 战斗结束消息 18 行超限（TC-06 红）。改为
    `overhead=sum(max(1, len(str(ln).splitlines())) for ln in lines)`。**后续凡新增多行模板元素，
    折叠预算/行数判断一律走物理行。**
22. 引擎层直出文案（沿用 #16，专项批执行）：本批已确认现状实况行的 `（HP x/y）` 尾注来自渲染层旧组装
    （`battle_render` 敌方行动行），HUD 重构专项（批7 路S）一并处理。
23. `battle_settle_levelup*` 3 键 test_demo 未镜像 → 批6 收口补齐（镜像一致性）。
24. 路T/R 报告的「范围外硬编码」候选（温室/种植/收获内联文案、加成名列表、`能量 -{qty}`、`宝石`、
    装配层 RuntimeError 开发提示）→ 归入终验「引擎层硬编码专项」。


### 批7 新增（2026-09-12 · 战斗 HUD v2 专项）

25. **战斗 HUD v2 已落地**（用户样稿）：分项资源行+百分比、缺资源不显示、怪物无朝向、
    行动行整句化（`❌ {怪}使出{招}，你受到 N 伤害。` / `✅ 你发动技能 {技能}，造成 N 伤害。`）。
    - 引擎：`TurnReport.effect_events`（行动收尾 DOT / 行动开始 DOT / 状态到期）+
      `effects.tick_turns` 返回被移除状态（失效事件）+ DOT 实例带展示名；
    - 接线：`EnrichedTurnReport` HUD 字段 + `battle_hud_payload()`；
    - 渲染：`render_action_hint` 重写为分项块（`_fmt_pct` / 按需显示 / 站位去【】）+
      `_render_effect_lines`；
    - 模板：新增 `battle_hud_*` / `battle_effect_*` / `battle_action_skill_verb` /
      `battle_hud_tail` / `battle_hud_state_*`；**删除被替代键 `battle_action_hint`**。
26. **投影透传坑（已修）**：`_without_npc_outcomes` / `_without_player_outcomes` 用固定字段
    清单重建 SimpleNamespace → 新增字段（HUD 数据）会被静默丢弃（现象：法力行不出）。
    后续凡给 EnrichedTurnReport 加字段，**必须同步这两个投影**。
27. **行动叙述行登记 prose**：`battle_player_hit` / `battle_enemy_hit` 按用户样稿整句
    （含「，」「。」）→ 归入 `meta.prose_keys`（窄屏自然折行，属叙述类）；结构化资源行仍 ≤14 全角。
28. **「怪物状态」超宽折行**：用户样稿三态整行 = 17 全角 > 14 → 首行带前缀、续行只出状态串
    （`battle_hud_enemy_status_cont`，已去空格对齐）。
29. **合并器升级**：`scripts/tpl_merge_fragments.py` 支持片段 `overwrite_keys`（改值片段：
    如行动行整句化覆盖旧值）与 `removed_keys`（删除被替代键）——改值/删键不再是硬冲突。
30. 敌方法力不显示（敌方 combatant 无真实 `max_mp`，引擎默认兜 100 → 不出行）；
    护盾剩余行动数缺失时只出数值不出空括号。
31. 模拟器观察到「偶发两条回复内容相同」（同一行动重复渲染）——旧 main 同现象 = **预存**，
    另案排查（非 HUD v2 引入）。
32. 批7 其余两路：任务链 25 键（路U）、炼金 #5 39 键（路T）已入表。

### 技能列表/详情重构（2026-09-12 用户拍板）

33. **技能列表**：普攻（type=basic）不入列表；每行 = `{序号}. {名称}（{类型}）` + 简述行 + 分隔线；
    头部改「【技能列表】」、去掉「技能 N 项」；序号/分页口径不变（`技能详情 <序号>`、`攻击 <序号>` 不受影响）。
34. **简述/详情 = 内容作者自由文本**（用户：「标签是文本类型，后续让用户自定义」「编辑器搞两个文本框，
    一个简述，一个详情」）→ 技能 def 新字段 `brief`（列表简述行）/`detail`（详情面板尾部文本，
    空则回落 `desc`）；已注册 skills_fields（FieldMeta label=简述/详情）→ 编辑器表单自动出两个文本框。
    `brief` 为空时按机制兜底生成标签串（伤害/消耗/连段/派生/跃空/闪反/防反/机动/部位/多段/霸体/打断）。
35. **技能详情 CTB 重写**：类型/标签/消耗（灵能·精力·印记·冷却 N 次行动）/效果（威力·段数·破坏值·
    霸体·打断）/**行动恢复（recovery）**/派生/详情文本；删除旧回合式「每次行动限 N 次」措辞。
36. 御剑师/脊剑士 18 个技能已按用户样稿风格填 `brief`（如 回收=【机动】【回收】、突=【伤害】【消耗】
    【连段】【派生】、腾空=【伤害】【跃空】【闪反】）；其余职业暂由机制兜底，待内容作者/编辑器补。
37. 待办：技能详情的「派生」行含 9 个派生名（约 50 字）会折行——若要求 ≤14 全角需另定拆行口径。

### 批8 新增（2026-09-12：PVP 20 / 调查 18 / 成就 16）

38. **引擎层中性文本池未纳管**（路B 报告）：`core/investigate.py` L93 `DEFAULT_GENERIC_TEXT`
    （「四周一片寂静，并没有特别的发现。」）、`core/environment_lore.py` L88 `DEFAULT_AMBIENT_TEXT`
    + L102-106 `_NEUTRAL_POOL`（4 条旧措辞）——玩家可见路径来自引擎兜底而非模板表 →
    归入「引擎层直出文案专项」。
39. **成就模块残余硬编码**（路C 报告）：`achievement_commands.py:145` 尾行
    `tip="输入 /成就信息 <N> 查看详情"`（仍带斜杠；`Tip:` 前缀硬编码在 `list_render.py`）、
    `:179` `"状态："+…`；`:41-42` `_DEF_*` 两常量已无调用点（待批18 裁决删除）。
40. **PVP 死模板**（路A 报告）：`pvp_lock_no_target` / `pvp_not_registered` 全仓无消费方
    （docs/审查报告/审查_M11_A3_jspace.md P2-5 已记录）——本轮按信息要素等价重做落表，
    是否删除留待批18 死键清扫统一裁决（避免边做边删导致回滚困难）。
41. 批8 并表后：表 **595** 键（541 → +18 调查 → +16 成就 → +20 PVP）；checker 0 FAIL / WARN 17；
    剩余未迁 **94 键**（dummy16 / fish15 / codex14 / explore13 / gift13 / shop13 / job8 / shortcut6 / use6 / dialog3）。

### 批9 新增（2026-09-12：木桩 16 / 钓鱼 15 / 图鉴 14）

42. **木桩死键候选**（路A）：`dummy_adjust_usage` 全仓无消费方 → 留批18 死键清扫统一裁决。
43. **图鉴死键候选（既有行为）**（路C）：`codex_unknown_category` 有 `tpl_of` 调用点
    （codex_commands L113），但指令壳在分册别名解析失败时回落总览，实机触发需引擎侧 `codex_view ok=False`
    异常路径 → 非本路引入，留表未删。
44. **钓鱼氛围句登记 prose**（路B）：`fish_bite_waiting` / `fish_bite_triggered` / `fish_reel_timeout`
    为叙事演出句（允许自然折行）；其余 12 键结构化行 ≤14 全角。
45. 批9 并表后：表 **640** 键；checker 0 FAIL / WARN 17；剩余未迁 **49 键**
    （explore13 / gift13 / shop13 / job8 / shortcut6 / use6 / dialog3 → 批10/批11）。

### 批10 新增（2026-09-12：探索/地图/时间天气 16 · 赠礼 13 · 商店 13）

46. **赠礼死键候选**（路B）：`gift_receive` / `gift_system_disabled` 全仓无渲染调用点（旧文本亦无）
    → 待接线或批18 死键清扫裁决。
47. **商店一览行壳层拼接超宽**（路C）：一览整行 = `shop_overview_row_prefix` + 类型徽标 + 描述 + 门槛标记
    在 `_shop_row` 拼接，实渲染可达 34 半角（如「1. 杂货铺 [普通商店] 新手村杂货铺」）——
    属壳层拼接（非模板），留批18 终扫或商店壳层专项处理。
48. 批10 并表后：表 **682** 键；checker 0 FAIL / WARN 17；剩余未迁 **23 键**
    （job 8 / shortcut 6 / use 6 / dialog 3 → 批11）。
49. **收口脚本坑**：`br[6:]` 取路名对 `tpl-b10b` 得到 `0b` → fragment 路径写错、表键漏并；
    已用 `fix(模板): 补批10 路B/路C 表键` 补回（26 键）。后续收口脚本一律显式写路名（如 `b10b`）。

### 专项·引擎文案1（2026-09-12：引擎层直出文案第一批 · 中性文本池 / 成就尾行）

50. **[已收口] #38 引擎层中性文本池纳管**：`core/investigate.py` `DEFAULT_GENERIC_TEXT`
    （L93）、`core/investigate.py` `DEFAULT_CONFIRM_TEXT`（L96）→ 表键 `env_investigate_generic` /
    `env_investigate_confirm`；`core/environment_lore.py` `DEFAULT_AMBIENT_TEXT`（L88）+
    `_NEUTRAL_POOL`（L101-106，4 条）→ `env_ambient_default` + `env_neutral_pool_1..4`
    （池化「多条随机」语义与 rng 确定性不变）。主路径 `tpl_of`，兜底常量降级为
    「ctx 无 templates / 表缺键」后备；内容包注入 `ctx["templates"]` 可覆盖同键。
51. **[已收口] #39 成就尾行硬编码**：`achievement_commands.py` L153 `tip="输入 /成就信息 <N> 查看详情"`
    → 新键 `ach_list_tail_tip`（免斜杠「发 成就信息 <序号> 查看详情」）。`_DEF_*` 两常量
    （`:41-42`）/ `ach_list_tail`（被 `ach_list_tail_tip` 语义替代）/ `ach_reveal_card` 仍无调用点，
    留批18 死键清扫统一裁决。
52. **同批发现的残留硬编码（未在本批范围，登记待后续）**：
    a) `core/investigate.py` `_map_reveal_outcome` 兜底句 `f"发现了一条隐藏通道，通往「{ref}」。"`、
       `_hunt_outcome` 兜底句 `f"你察觉到了「{boss}」出没的迹象。"`——玩家可见、含 f-string 占位
       （无 cand.desc / row.desc），同为引擎直出文案；
    b) `commands/achievement_commands.py` `cmd_achievement_info` 行 `"状态：" + ("✅ 已达成" if … else "未达成")`
       （#39 原登记 `:179` 未含在本批任务书范围）；
    c) `core/hidden_trigger.py` `DEFAULT_CONFIRM_TEXT`（L107，「这里没有新的发现。」）——
       R-11 去重口径的兄弟模块副本（`check_*` 返回 `card`）。
53. 专项·引擎文案1 并表后：表 **690** 键（682 → +8）；checker 0 FAIL / WARN 17；全量 pytest
    6560 passed / 9 skipped（新增 4 条表驱动契约测试）。

### 批11 + 引擎文案专项第一批（2026-09-12）**迁移完成**

50. **分区迁移归零**：`*_tpl.py` 未迁键 = **0**（表 713 键；会话起点 388 → 713）。批11 完成 23 键
    （job 8 / dialog 3 / shortcut 6 / use 6）；专项批完成 8 键（env 中性池 4 + ambient 兜底 +
    investigate 泛化/确认 + 成就尾行 Tip）。
51. **引擎层直出文案第一批已清**：`environment_lore._NEUTRAL_POOL`/`DEFAULT_AMBIENT_TEXT`、
    `investigate.DEFAULT_GENERIC_TEXT`/`DEFAULT_CONFIRM_TEXT` → 表驱动（池化随机语义不变，
    函数加 ctx 形参供调用方注入覆盖）；`achievement_commands` 尾行 Tip 改免斜杠表键。
52. **专项余项（待后续批）**：`investigate` map_reveal/hunt 的 f-string 兜底句、
    `achievement_commands` `"状态："+…` 行、`hidden_trigger.DEFAULT_CONFIRM_TEXT`；
    炼金引擎 message（`alchemy_battle.py`/`alchemy_harvest.py`/`alchemy_helper.py`）、
    商店一览壳层拼接超宽（#47）、成就残余（#39）。
53. **死键候选累积清单（批18 统一裁决）**：`pvp_lock_no_target`、`pvp_not_registered`、
    `dummy_adjust_usage`、`codex_unknown_category`、`gift_receive`、`gift_system_disabled`、
    `ach_list_tail`（被 `ach_list_tail_tip` 语义替代）。
54. 收口坑复现一次：合并脚本 fragment 文件名取错路名（已改显式写死）；另注意 `git commit --no-edit`
    在没有 MERGE_MSG 时会以空消息中止——收口提交一律显式 `-m`。

### 终验（2026-09-12）**终态达成**

55. **分区文件全删**：21 个迁移期 `*_tpl.py` 空壳删除；`core/templates/` 终态 = `__init__.py`（加载器）
    + `base.py`（核心 8 类）+ `template_table.json`（**唯一存储**）；`__init__.py` 收敛为 base + 全量表聚合。
56. **死键清扫**：删除 7 键（`battle_hud_enemy_status_cont` 无引用、`pvp_lock_no_target`、
    `pvp_not_registered`、`dummy_adjust_usage`、`gift_receive`、`gift_system_disabled`、`ach_list_tail`
    被 `ach_list_tail_tip` 替代）；表 713 → **706**。
57. **test_demo 收敛**：删除 21 个表外旧键（内容包 key 必须存在于表内，否则永远不生效）→ 619 键。
58. **守卫测试合并**：14 条迁移期「分区守卫」测试作废 → 新增
    `tests/unit/test_template_table_final.py`（5 条终态门禁：分区已删 / 表占位符 ⊆ 白名单 /
    聚合含全表键 / base 核心键在 / 内容包无表外键 + 键数下限）。
59. **引擎文案专项第二批（进行中）**：炼金引擎 message（`alchemy_battle`/`alchemy_harvest`/`alchemy_helper`/
    `alchemy_materials`）+ `investigate` f-string 兜底 + 成就「状态：」行 + `hidden_trigger.DEFAULT_CONFIRM_TEXT`。
60. **尾行 Tip 专项（进行中）**：`basic_commands`/`checkin_commands`/`quest_commands`/`core/shop.py`/
    `templates/base.py` 的尾行 Tip 常量（旧式「发送'…'」）→ 全量表键 + 免斜杠「发 <指令> <参数>」。
61. 实机抽查（终态）：注册/角色/状态/背包/技能/技能详情/技能派生/位置/地图/进入/怪物/锁定开战/攻击/
    任务/任务信息/商店列表/图鉴/成就/称号/帮助 全部正常；唯二待清 = #59/#60 两类硬编码文案。

## B. 已确认豁免（不再处理）

- 数据键/映射值（品质标签、字段条件键如「连续天数」）、kv 键名（`条数`）、
  内部 ValueError/RuntimeError 开发文案、指令匹配关键字（日志/传记/系统）、
  `MakeupCard` 物品 id（`补签卡`）。

## C. 分区文件与死键清扫（清扫批执行）

- 空壳 `*_tpl.py`（DEFAULT_TEMPLATES/PLACEHOLDER_WHITELIST 为空的过渡壳）统一删除：
  批2 后当前空壳 = `enhance_tpl.py`、`checkin_tpl.py`、`log_tpl.py`（其余待后续批清空）。
- 全量死键复扫（动态 key 复核）+ `templates/__init__.py` 归一化（表为唯一来源后清理分区加载逻辑）。
- `content/test_demo/templates.json` 收敛为「仅有意差异」（当前为逐批最小同步模式）。
