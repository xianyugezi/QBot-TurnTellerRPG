# M12.5 编辑器全模块扩展 · 里程碑记录

> 生成：2026-09-03（M12.5 收官）｜ 基线 0e947d4 → HEAD（7 批 + 收口）
> 依据：docs/m125_启动包.md（8 批计划）｜ 全仓 pytest 9 failed 基线保持（零新增）｜ verify_m125 exit 0

## 一、一句话定位

M12.5 = 把 content/test_demo **全部 30 页 JSON 模块接入编辑器**（页面登记 → field_meta 字段注入 → CRUD 泛化 → 前端三形态渲染），**同时清理编辑器/事件/功能三面硬编码**（用户点名「功能硬编码、事件硬编码」统一处理，2026-09-03 授权自动推进）。

## 二、批次交付链（7 批 + 收口，commit 全链）

| 批 | commit | 交付 |
|---|---|---|
| 批0 审计 | eb63755 | 3 文档落盘：编辑器硬编码 36 点清单 / 事件键 14 类注册表 / 模块字段摸底 18 模块 |
| 批1 泛化核心 | f893191 | pages_crud PAGE_MODULE 死映射 → editor_registry 动态页表（_page_info 优先 editor.json/enabled/id_prefix，兜底常量兼容）；api meta 8 键 dict + refs 12 键 dict → REFS_TARGET_ALIASES 单源 + field_meta 表键直查；EditorPage 扩 id_prefix/group/page_kind；field_meta dungeon/achievements 空表宽松注入（DUNGEON_FIELDS 9/ACHIEVEMENT_FIELDS 7） |
| 批2 D 类页 | 31bc087+b596b0a | editor.json 22 页登记 + editor 模块进 manifest/field_meta（**M12 遗留：editor.json 从未进运行时**——loader 只加载 manifest 声明模块，补交后动态页表才真正生效）；删物品级联（enemies.drops/shop.items/quest.reward/recipe.materials+output + 条件引用阻止）；/api/refs 契约 {target,kind,items,total}；skill_chains 补 4 键 |
| 批3 复杂模块 | c94bd59+b74a958 | forge/fishing obj 段级字段表（FORGE_EDITOR_TOP_FIELDS 5 段/FISHING_TOP_FIELD_DEFS 3 段，详细版保留导出）；fishing 4 专属端点（schema/csv validate/crown_preview/simulate 复用 fishing_editor_service 纯函数层）；dungeon/achievements children 细化（achievements conditions.value 实测恒 int 闭合修正） |
| 批4 obj/map+前端 | 8b10aa9+3c166b3 | pages_crud obj/map 形态读写（module_shape/get_whole_module/update_whole_module/map 键 CRUD）；api /pages/{page}/whole + /pages/{page}/map/{key} 6 端点（路由前置防 /{item_id} 吞）；editor.json 30 页全登记；**editor.html 三形态渲染**（object 整表单/map 键值编辑/侧边栏 group 分组 + meta 竞态清理）；SETTINGS_FIELDS 补 default_map/world_name（浏览器实机验证抓到） |
| 批5 事件可配置 | a049d0e+74f2cf9 | event_bus 事件键注册中心（EVENT_KEY_DEFAULTS 13 类 + resolve_event_key：settings.events name 可配/完整键直通/缺省回退/防呆）；7 处写侧字面量改读解析中心；adventure_log 6 类 + 环境事件写读键收口（常量保留=缺省值）；读侧审计（前缀匹配天然兼容，EVENT_PRESETS 仅黄提示）；settings.json events 段配置示例 |
| 批6 verify | 9e41fd8+修复链 | verify_m125.py：30 页可达矩阵 + 形态分流 + field_meta 全模块 + 事件可配置 + 硬编码清零 + 动态页表 6 项检查全 PASS |
| 批7 收口 | （本记录） | 全仓回归 9 failed 基线保持；真包 API 点验三形态读写落盘（/tmp 副本） |

## 三、关键架构决策（修正启动包认知）

1. **C 类真空表仅 dungeon/achievements**（forge/fishing 已有 obj module_meta；stats/formula 是 map+value_meta）——启动包 §2.2 原以为 4 空表
2. **editor.json 从未进运行时**：manifest.modules 无 editor → loader 不加载 → editor_registry 恒六页兜底。补交：field_meta editor 宽松表 + manifest 声明
3. pages_crud 动态页表 _page_info(ctx,page)：editor.json 页表优先（module_file 去 .json/enabled/id_prefix），缺失回退 PAGE_MODULE 常量（旧包兼容）
4. obj/map 形态判定：editor.json page_kind 优先 → field_meta entry_type → 顶层容器
5. 事件键 = 写侧字面/常量 + 读侧前缀匹配双面协议：`[事件:` 外壳与 `:target]` 拼装是路由语法**硬编码保留**，只可配 name 段；内容包条件字面量存量不动
6. 子代理 50 轮半交付常态化 → 派工单收敛单文件+测试，主 agent 收口（批1 路1B 全丢重做、批3 forge 详细版降级恢复等）

## 四、DELAYED 登记（需用户裁决）

- **版本簿跨重启持久化**（0A #6/#14）：编辑锁计数挂 ctx 内存，进程重启/多进程丢失 → 409 冲突保护失效。单进程 uvicorn 常驻下语义可接受（SV-08）；多进程部署需换共享存储。**待用户裁决是否加 settings 开关**
- 级联删除规则表化（0A #9 完整版）：monster/map/job/items 分支已实现，规则表驱动（editor.json 声明 from_page×ref_field）属锦上添花——现分支式已覆盖全部登记页
- validate 数值键/引用键字段表驱动（0A #11/12）：现写死 16 数值键 + 8 引用组，按 /api/meta 字段表驱动是增强项（未列键零校验是宽松语义，非缺陷）
- 内容包事件条件存量（[事件:副本通关] 等 JSON 字面量）：配置改名后存量条件不匹配——内容包作者需同步更新条件（只建议不限制）

## 五、验收门禁对照（启动包 §五）

1. ✅ 全仓 pytest 9 failed 基线保持（battle_render 2/codex 1/e2e_m4 4/emoji 1/investigate 1，零新增）+ ruff 改动文件绿
2. ✅ 30 页全模块编辑器可达（list CRUD / map 键值 / obj 段表单）+ verify_m125 断言
3. ✅ field_meta 注入后 test_demo 加载零红拦（最强回归：dungeon/achievements/forge/fishing/settings 全过）
4. ✅ events 段配键名全链生效（resolve_event_key 改名/完整键/缺省回退 + wiring 测试）；缺省回退零破坏
5. ✅ 硬编码清单 36 点逐项处理（主项批1-5 解决，余项 DELAYED 登记）
6. ✅ verify_m125 exit 0（6/6 PASS）

## 六、浏览器实机验证（agent-browser + 真包副本）

- map 页（stats）：键列表渲染 + 单键 JSON 编辑 + 保存落盘（hp base=150）✓
- obj 页（settings）：整对象表单渲染 + world_name 编辑保存落盘 + default_map 保留 ✓
- 抓真缺陷：**SETTINGS_FIELDS 缺 default_map/world_name**（obj 表单缺 str 字段不可编）→ 补登记
- 真包 API 点验（副本 8127）：items list 真实条目 / forge whole obj + 合并写（trees 保留）/ formula map 键写 crit_rate 落盘 ✓

## 七、踩坑沉淀（复用）

1. 子代理 50 轮半交付常态化：验收必须 git status + 实测，改动可能未落盘（git diff 0 但测试文件残留）
2. task 可能把「详细字段表降级成宽容器」（forge 89 行 children 被删）——主 agent 恢复 + 两全方案
3. children 细化必须闭合真实数据 + load_pack 验证（task-2 的 achievements value 标 str 实际 int → 8 红拦被收口修复）
4. FastAPI 路由顺序：/pages/{page}/whole 必须在 /pages/{page}/{item_id} 前声明
5. 前端 meta 竞态：切页清 state.meta 防旧 meta 误配新页表单
6. 测试文件 ref 中 `***` 是 read_file 显示 mask——patch old_string 用了显示值会把真文件改坏（改后必须 ast.parse 验证）
7. settings 是常驻模块宽松放行未知段（events 段零破坏加入）
