# M11 钓鱼收官 · 实现启动包（新对话开工依据）

> 生成：2026-09-01 · 用途：新对话读本文件 + 依序开工 M11
> 前置：M10 钓鱼全部 8 批完成（commit `4c1e61d`，12 个 commit，全仓 4482 passed / 9 skipped，ruff/mypy/G0/M43 全绿）
> 场景标准：agent-prompts 43 场景模板为通用标准（S0 诊断 → S5 PRD → S9 架构 → S21 编码 → S22 测试 → S23 审查 → S31 部署）

## 〇、M11 一句话定位

**M11 = M10 钓鱼收官**：实机冒烟通过 + 钓点地图可达性 + 部署实机验证 + 钓鱼内容补全。M10 代码全部完成且经 dsh 审查（A1-A5 五批抓出 3 个部署级 P0 已修），**唯一遗留 = 实机冒烟未跑通**（verify_m10_smoke 超时 + 钓点在地图深处玩家出生地无钓点）。

## 一、M10 遗留问题（开工首日必处理）

| # | 遗留 | 状态 | 处理 |
|---|---|---|---|
| 1 | **实机冒烟超时** | scripts/verify_m10_smoke.py 跑到 `/钓鱼 月光草甸` 后超时 | 见 §二 排查步骤 |
| 2 | **钓点地图可达性** | 玩家出生 start_village 无钓点；gp_moon_grass 在 gloom_forest（幽暗森林）——玩家需 `/进入` 到 gloom_forest 才能钓 | 冒烟脚本先 `/进入 幽暗森林` 再钓；或考虑出生图加钓点（待用户拍板） |
| 3 | **部署实机验证** | NoneBot2 服务（nonebot2.service）运行中，实机包=test_demo（bot.py L17），钓鱼数据已在包内 | 冒烟通过后重启 nonebot2 实测 |

## 二、冒烟超时排查步骤（开工第一步）

1. 读 `scripts/verify_m10_smoke.py`（钓鱼全链路：注册→/钓鱼→抛竿→鱼讯→收杆→结算→图鉴）
2. 已知问题：`/钓鱼` 在 start_village 返回「本图暂无可钓鱼点」（无钓点）；`/钓鱼 月光草甸` 返回「钓点不存在」（spot id 是 gp_moon_grass 非中文名）
3. **修复方向**：冒烟 FLOW 加 `/进入 幽暗森林`（gloom_forest）→ `/钓鱼`（此时应有 gp_moon_grass 钓点）→ `/钓鱼 gp_moon_grass` 或 `/钓鱼 月光草甸`（确认 spot 名解析）→ 后续
4. 超时点可能在 `/鱼讯`（等待期 wait_sec 300-900 秒——冒烟需注入 now 推进或 wait_sec=0 配置）
5. **关键**：跑冒烟前先看 `/tmp/fishingflow.txt`（上次跑到哪一步），用 `timeout 60` 分段跑，不要整脚本 180s

## 三、M11 批次派工单（建议 3 批）

### 批0 · 实机冒烟修复（2 路）
- **路0A 冒烟脚本修复**：verify_m10_smoke.py 补 `/进入 幽暗森林` + 钓点可达 + wait_sec 推进；跑通全链路（注册→进图→钓鱼→抛竿→鱼讯→收杆→结算→图鉴→对账）
- **路0B 钓点数据完善**：确认 test_demo 钓点分布（哪个图有哪些 gp_* 采集点可作钓点）；若玩家可达性差，加 1-2 个出生图钓点（待用户拍板：改 maps.json 加采集点 vs 鱼种 spots 指向既有采集点）

### 批1 · 部署实机验证（2 路）
- **路1A 部署验证**：重启 nonebot2 → 真实 QQ 群发指令实测钓鱼全链路（或 build_app_deps + runner 直测）→ 记录回复
- **路1B 回归收口**：全仓回归 + ruff/mypy/G0/M43 + 补冒烟发现的缺口测试

### 批2 · 钓鱼内容补全（2-3 路，可选，待用户定方向）
- **路2A 鱼种扩充**：test_demo fishing.json 从 1 种扩到 4-6 种（不同稀有度/时段/钓点分布），走 CSV 导入（编辑器服务已就绪）
- **路2B 鱼王 BOSS 战接线**：金闪 → 实际开战（enemies 引用 lake_leech → BattleEngine）——目前只做到金闪标记，BOSS 战未真正接线
- **路2C 四出口实机闭环**：商店出售鱼获 / 委托交付 / 品评投稿 / 炼金造饵 实机验证

## 四、关键接口（M11 开工前确认）

1. **verify_m10_smoke.py**：现脚本 FLOW 无 `/进入`，起点 start_village 无钓点——改造点
2. **spot 名解析**：`/钓鱼 月光草甸` 报「钓点不存在」——确认 spot 匹配逻辑（id vs name；gp_moon_grass 的 name 字段？maps.json L64-71 无 name → 显示回落 id）
3. **wait_sec 推进**：冒烟测等待期需注入 now（ctx["now"]）或临时 wait_sec=0 配置；不能真等 300-900 秒
4. **鱼王 BOSS 战**：king_event_available 已接线（A5 P1-2），但开战（BattleEngine）未接——路2B 范围

## 五、验收门禁（沿用 M10）

1. **全仓门禁**：`pytest tests/ -q -p no:cacheprovider --ignore=tests/e2e` 全绿 + `ruff check .` 绿 + `mypy .` 绿
2. **冒烟**：verify_m10_smoke 全链路通过（钓点→抛竿→鱼讯→收杆→出鱼→图鉴）
3. **emoji 门禁**：仅 ✅/❌ 功能性标记 + 排版符号
4. **模板门禁**：新增消息模板必须 tpl_of（fishing_tpl 分区）

## 六、坑位提醒（M8/M9/M10 教训）

1. **部署级 P0 三模式**（M10 批8 审查抓到）：指令未注册 Router/白名单 / 装配 hook 签名错 / 结算零消费方——改装配必查
2. **dsh 审查报告落盘 docs/审查报告/**（非工作区根），exit 2 是 ls 找不到根路径非审查失败
3. **dsh 端点**：~/.dsh/settings.yaml 已切 commandcode.ai（2026-09-01）；派审查前 curl 验证
4. **实机包=test_demo**（bot.py L17），改数据后重启 nonebot2 生效
5. **冒烟超时**：整脚本 180s 不够（build_pack 加载慢）——分段 timeout 60 跑，先看 /tmp/fishingflow.txt
6. **用户拍板不可擅改**：钓点是否加出生图 / 鱼种扩充范围——先问用户

## 七、M10 成果速览（新对话上下文）

- **代码**：12 个 commit（c3a45c2→4c1e61d），核心模块 core/fishing*.py（13 个）+ commands/fishing*.py（3 个）+ editor/fishing_editor_service.py
- **审查**：dsh A1-A5 五批，抓出 3 P0（指令注册/扣饵 hook/结算零消费）+ 15 P1，全部修复
- **全仓**：4482 passed / 9 skipped，ruff/mypy（431 文件）/G0/M43 全绿
- **设计**：定稿 /root/docs_archive/RPG框架项目/钓鱼玩法设计定稿.md v1.0.1 + 细化 2c1a/2c1b/2c1c + 规划_路2c1（T01-T19）
- **dsh**：已切 commandcode.ai 端点（与 Hermes 同配额）

## 八、启动第一步（新对话流程）

1. 读本文件 + `git log --oneline -8` 确认基线在 `4c1e61d`（M10 收官，工作区干净）
2. 读 `scripts/verify_m10_smoke.py` + `/tmp/fishingflow.txt`（上次冒烟断点）
3. 按 §二 排查冒烟（先 `/进入 幽暗森林` 再钓）
4. 派批0（冒烟修复）→ 批1（部署验证）→ 批2（内容补全，待用户定方向）
5. 全部完成后 dsh 审查 → 部署实机 → M10 正式收官
