# 矛盾与待裁决登记（编辑器框架）

> **来源**：用户 2026-09-16 拍板落地 CakeGame《表写入规范》可借鉴项 **E**——「文档矛盾登记（⚠️ 只登记不裁决）」正式化；
> 归入本仓既有散落项（`docs/能力缺漏审计_四方对照.md` §五 U1–U10、台账「待确认」区、各文档「待确认 / 待裁决 / 存疑 / 矛盾」标记）。
> **互链**：`docs/编辑器修改意见0915_台账与方案.md` §十三（批次入口）· `docs/能力缺漏审计_四方对照.md` §五（未确认区）。
>
> **纪律（写死，本表唯一规则）**：
> 1. **只登记，不裁决**——本表不替用户拍板；未裁决项一律保留双方证据。
> 2. 每条含三段：**编号 / 一句话争点 / 双方证据（文件:行 或 文档行号）** + **状态**。
> 3. 状态取值只三档：**待实测**（含「待排期 / 待专批 / 待收敛」等实测性质）｜**待用户裁决**｜**已裁决-日期 + 结论与依据**。
> 4. 已裁决项**必须**写清裁决日期与依据（拍板记录 / commit / 契约文档行号）；文档间的矛盾**不自行压平**。
> 5. 新矛盾**追加**，旧条不删——只把状态从「待…」改为「已裁决-…」。

---

## 〇、采集口径与来源清单

**grep 口径**（本仓自有文档，排除冻结策划书与历史审计报告）：

```
grep -rn "待确认|待裁决|存疑|矛盾|待定|待收敛|未定案|未登记待确认" docs/ --include=*.md
  # 排除 docs/审查参考/（冻结策划书原件）与 docs/审查/幻觉审查*（历史审计报告）
```

原始命中 349 行；按「是否构成编辑器/框架的**独立**未决项」去重收敛为下表 **24 条**（其余命中多为 M0–M13 历史里程碑报告的「未实现」状态标签，不构成独立争点）。

| 来源文件 | 条目 |
|---|---|
| `docs/能力缺漏审计_四方对照.md` §五（:312-325） | U1–U10 |
| `docs/编辑器修改意见0915_台账与方案.md`（§四 :100-108、§十一 :296-329） | U1–U7 裁决依据；X1 / X3 / X4 / X5 |
| `docs/编辑器重写_需求与约束.md` | U5 / U6 边界；X4 / X5 契约 |
| `docs/消息模板重构/02_遗留登记.md` | X6 / X7 / X8 / X9 |
| `docs/m12_UX架构方案.md:460` | X10 |
| `docs/指令清单_全量.md:19` | X11 |
| `docs/审查/幻觉审查_1a.md:18` | X14（唯一保留的历史审计独立未决项） |
| `qbot_rpg/**`（代码实测） | U1 / U2 / U7 / X2 / X3 的实现事实 |

---

## 一、登记表

### A · 已裁决（有用户拍板记录 / 契约 / commit）

| 编号 | 一句话争点 | 双方证据 | 状态 |
|---|---|---|---|
| U1 | **采集 / 挖掘**在编辑器里是独立模块、独立生活技能，还是 `maps` 子段？ | 策划书：`docs/能力缺漏审计_四方对照.md:316`「总纲:387 把采集作为 maps 字段；总纲:1285 是独立 `/采集` 指令」；实现：同卷「`forge_deadlock.py:91` 归口名『采集/挖掘』；`explore_commands.py:6-7` 说『采集点在地图/副本探索流程内处理』」 | **已裁决-2026-09-15**：数据落点 = `maps.gather_points`（补 `name/weather_mods` 登记）+ 目录条目 `gathering`（标「未实现」）；引擎部分另列待排期。依据 `docs/编辑器修改意见0915_台账与方案.md:102`；代码 `qbot_rpg/content/module_catalog.py:121-122`（`implemented=False`） |
| U2 | **时间天气**编辑器归属：挂「通用设置」卡片，还是独立一级模块？ | `docs/能力缺漏审计_四方对照.md:317`「时间天气定稿:296 明写『通用设置 → 【时间天气】卡片』；但同定稿:407 登记要求落在 `settings.json time_cycle` 段」 | **已裁决-2026-09-15**：挂「通用设置 → 【时间天气】卡片」，**不**新增一级模块。依据 `docs/编辑器修改意见0915_台账与方案.md:103`；`module_catalog.py` 无 `time_cycle` 一级条目；`settings.time_cycle` 已登记 |
| U5 | `profession.json` 分层冲突如何收敛？ | `docs/能力缺漏审计_四方对照.md:320`「策划书：总纲:400 要求 `profession.json` 独立文件…实现：拆进 `settings.alchemy.farming / quest_board / contest / assistant / fishing / fishing.json`」 | **已裁决-2026-09-15**：用「生活」虚拟聚合视图 `life` 收敛，**不改策划书**。依据 `docs/编辑器修改意见0915_台账与方案.md:105`；契约 `docs/编辑器重写_需求与约束.md:181-183` |
| U6 | `message_prefix` 与 `templates` 职责边界，是否合并为「消息」模块？ | `docs/能力缺漏审计_四方对照.md:321`「消息前缀定稿管『回复首行身份标识』；`templates.json` 管全部消息模板文案，二者都落在『首行渲染』」 | **已裁决-2026-09-15**：**不合并**（前缀管首行外壳、templates 管正文文案）。依据 `docs/编辑器修改意见0915_台账与方案.md:106`；边界 `docs/编辑器重写_需求与约束.md:201-203` |
| U7 | **怪物触发类型枚举双源冲突**（13 类命名不一致），以哪份为准？ | `docs/能力缺漏审计_四方对照.md:322`「怪物模块定稿:298（`phase_below/broken/revive/enter_phase…`）vs 怪物行动AI定稿:63-75（`hp_below/pv_broken/…`）命名不一致」 | **已裁决-2026-09-15**：以**校验器现行枚举为准**（实现即事实）；另一套命名登记为「待收敛项」，不擅自改校验。依据 `docs/编辑器修改意见0915_台账与方案.md:107`；代码 `qbot_rpg/content/validator.py:219-224`（权威 15 类）、`:226-231`（别名归一）、`:232-235`（legacy） |
| X1 | `#9 公式未登记键`：`formula` 是否仍有「已消费但未登记」的标准键？ | 台账 `docs/编辑器修改意见0915_台账与方案.md:305`（批19 段C 按 `core/formula_loader.py` 实际消费面登记）；同卷 `:324-325` 逐字「**未登记待确认**：无（`formula_engine` 本身只做表达式求值、不按名消费键…）」 | **已裁决-2026-09-16（批19）**：清单闭合，状态 =「未登记待确认：无」。依据 `docs/编辑器修改意见0915_台账与方案.md:324-325` |
| X3 | **`slots` 模块 ↔ `settings.slot_defs` 功能重叠**：编辑器归口哪处、如何提示？ | 声明：`qbot_rpg/content/module_catalog.py:85-96`（`slots` 的 `overlap_with="settings.slot_defs"` + `overlap_note`）；实测：`content/veinborn/slots.json=[]`、`settings.json slot_defs` 8 部位；提示实现 `qbot_rpg/web/api.py:765-788` `_module_overlap_hints()` | **已裁决-2026-09-16（批19 #5）**：黄提示、不硬拦（声明驱动）。依据 `docs/编辑器修改意见0915_台账与方案.md:307`；契约 `docs/编辑器重写_需求与约束.md:335-336` |
| X4 | 生活系统归口：挂 `jobs` 父模块下，还是独立「生活」？ | 临时口径 `docs/编辑器修改意见0915_台账与方案.md:40`「挂到包内既有父模块「职业」`jobs` 下」；定案同卷 `:88`「用 `entry_merge` 虚拟聚合视图 `life`…批12 挂 `jobs` 下的临时做法已取消」 | **已裁决-2026-09-15**：`life` 虚拟聚合视图，不改策划书、不新增数据文件。依据 `docs/编辑器修改意见0915_台账与方案.md:88`；契约 `docs/编辑器重写_需求与约束.md:181-183` |
| X5 | 台账批4 表头「待确认」三条（#7 中文 ID 规则 / #10 概率覆盖 / #11 预设归属） | `docs/编辑器修改意见0915_台账与方案.md:61-63`（三处「待确认」原文） | **已裁决-2026-09-15**：#7 默认「前缀+序号」、拼音可选附加；#10「使用处 > 定义处 > 缺省」；#11 框架默认六预设 ∪ 包声明。依据同卷 `:83-87` 销项 + 契约 `docs/编辑器重写_需求与约束.md:207-262` |

### B · 待用户裁决（保留双方证据，不自行拍板）

| 编号 | 一句话争点 | 双方证据 | 状态 |
|---|---|---|---|
| U3 | `覆盖审计_F` 缺失-1（锻造防具套装 P1）是否仍成立？ | `docs/能力缺漏审计_四方对照.md:318`「F 基线 2026-08-18 称『2c2d 不存在』；实测 `docs/细化/细化_2c2d_锻造套装与客制.md` **已存在**」；同卷 `:414`「该判据在 2026-09-15 不再成立…只代表承接文档已补」 | **待用户裁决**：台账 `docs/编辑器修改意见0915_台账与方案.md:104`「基线过期项，未在本批处理，保留在审计 §五 待确认」 |
| U4 | 伤害构成统计 / `dummy_log` 是否已实现？ | `docs/能力缺漏审计_四方对照.md:319`「战斗数值层定稿:322/356 有 schema 要求；`core/damage.py` 只算分，未定位独立收集器」；同卷 `:258`（②-11）「存疑…待复核」 | **待用户裁决 / 待实测**：台账 `docs/编辑器修改意见0915_台账与方案.md:108`（U4 本批仅登记 `formula.stats_collector` 配置字段，未实现引擎） |
| U8 | 商店字段新旧混用：编辑器下拉应出 `scope/limit/period` 还是 `per_player`？ | `docs/能力缺漏审计_四方对照.md:323`「商店定稿字段表用 `scope/limit/period`（:173-177），示例/校验器仍用旧 `per_player`（:415/:450-453）；:183 仅声明自动映射」 | **待用户裁决**：台账 `docs/编辑器修改意见0915_台账与方案.md:108` 保留待确认 |
| U9 | 总纲 §6.12 编辑器进阶能力（L993-1198，约 200 行）是否需补专项逐条核对？ | `docs/能力缺漏审计_四方对照.md:324`「报告篇幅所限未逐条对照」 | **待用户裁决**：台账 `docs/编辑器修改意见0915_台账与方案.md:108` 保留待确认 |
| U10 | `enemies.active_time` 双份 schema：刷怪行字段还是怪物字段？ | `docs/能力缺漏审计_四方对照.md:325`「`field_meta.py:452` 在 `DUNGEON_MAP_ELEM_CHILDREN` 登记了 `active_time`，但 `maps` 模块实测不生效（见 B6）；`enemies` 模块无该字段」 | **待用户裁决**：台账 `docs/编辑器修改意见0915_台账与方案.md:108` 保留待确认 |
| X9 | GM「编辑」指令：链接源 `settings.editor_url` 已清空，指令删除 / 改造？ | `docs/消息模板重构/02_遗留登记.md:238`「G13 `/编辑` 只输出『编辑器链接 + 权限级提示』…链接源 `settings.editor_url` 已随之清空…**本批未改行为**（GM 指令清单属 5b 契约），留待用户拍板」 | **待用户裁决** |
| X14 | 怪物防御率：定稿有因子但全文无定义 | `docs/审查/幻觉审查_1a.md:18`「O1 怪物防御率：定稿 L27/L32 确有该因子但全文无定义（grep 全文档仅此 2 处），细化正确标为『未定义（开放项）』并注明待裁决、禁止自行猜测接入 ✅ 诚实」 | **待用户裁决**（属战斗数值定稿层，超出编辑器字段范畴） |

### C · 待实测（含待排期 / 待专批 / 待收敛）

| 编号 | 一句话争点 | 双方证据 | 状态 |
|---|---|---|---|
| X2 | **采集 / 挖掘引擎未实现**（独立 `/采集` 与「挖掘」通道均无引擎） | 策划书：`docs/能力缺漏审计_四方对照.md:248`「总纲:1285 `/采集`」、`:249`「锻造定稿 L151-158『素材获取五路：**挖掘**/采集/垂钓/怪物掉落/商店』」；实现自承：`qbot_rpg/commands/explore_commands.py:6-7`（登记 DELAYED）、`qbot_rpg/assembly/router_setup.py:245`（stub） | **待实测（引擎未实现·待排期）**：目录侧已列 `gathering`（`module_catalog.py:121-122`，`implemented=False` 不可启用）；见 U1 裁决 |
| X6 | 引擎 / 跨模块渲染文案是否迁入新式模板表？ | `docs/消息模板重构/02_遗留登记.md:6`「## A. 引擎/跨模块渲染文案（待专批）」；`:45`（#16）「引擎层直出文案（专项批）：`qbot_rpg/core/battle.py` 的 `outcome.message` 直出串」 | **待实测（待专批·未闭环）** |
| X7 | 「表外旧键 214 条」待收敛 | `docs/消息模板重构/02_遗留登记.md:52`「test_demo 镜像同步…剩余『表外旧键 214 条』待收敛批」 | **待实测（待收敛批）** |
| X8 | 炼金两处换行 / 去头口径存疑 2 项 | `docs/消息模板重构/02_遗留登记.md:38`「`alchemy_feed_chain_effect` 去头口径、`alchemy_inherit_gold_slot` 换行口径：批4·路K 存疑 2 项（已按新式落表，如需回跳单点改）」 | **待实测（存疑·已按新式落表）** |
| X10 | `docs/m12_UX架构方案.md` 附录 B 的 9 项【待定】是否仍成立 / 整档是否作废？ | `docs/m12_UX架构方案.md:460`「## 附录 B：【待定】清单汇总」；`:462-469` 九条编号项（版本号落点 / 会话持久化 / 撤销栈…） | **待实测**（需先确认 m12 是否已被《编辑器重写_需求与约束》取代，再决定逐项销项或整档作废） |
| X11 | 全量指令清单「设计未实现」约 36 条 | `docs/指令清单_全量.md:19`「\| 设计未实现 \| 约 36 条（强化系/种植温室系/时间天气/木桩/GM 运维/PVP 决斗等，按系统开发）\|」 | **待实测（待排期）** |
| X12 | 战后恢复 `post_battle_recovery` 未实现 | `docs/能力缺漏审计_四方对照.md:147`（F3）「`post_battle_recovery` **全仓无消费点**（grep 0 命中）」；`:256`（②-9）同 | **待实测（引擎未实现·待排期）** |
| X13 | NPC `repair` / 装备耐久未实现（策划书自承降级） | `docs/能力缺漏审计_四方对照.md:102`（C10）「NPC定稿:139『repair：依赖装备耐久系统（**框架未实现**——items 无 durability 字段、装备引擎无磨损）』」 | **待实测（策划书已标注降级·待排期）** |

---

## 二、汇总

| 状态 | 条数 | 编号 |
|---|---|---|
| 已裁决 | 9 | U1 / U2 / U5 / U6 / U7 / X1 / X3 / X4 / X5 |
| 待用户裁决 | 7 | U3 / U4 / U8 / U9 / U10 / X9 / X14 |
| 待实测 | 8 | X2 / X6 / X7 / X8 / X10 / X11 / X12 / X13（另 U4 并登记） |

> 注：U4 同时具「待用户裁决」与「待实测」两性（需先复核是否有其它落点，再裁决是否排期），故两处都登记；
> 独立条目共 **24 条**（U1–U10 + X1–X14）。

## 三、维护约定

- 本表由**批次收尾**时更新：新增矛盾追加新 `X<n>`；已裁决条目改状态并写日期 + 依据（引用拍板记录行号 / commit）。
- **不得**因「本表存在」就跳过实测：`待实测` 项要在对应批次给出原始证据后再改状态。
- 与 `docs/能力缺漏审计_四方对照.md` §五 的关系：§五 是**审计原始未确认区**（U1–U10），本表是**正式登记 + 状态跟踪**；两处编号一致，§五 不删。


---

## 追加：CakeGame 借鉴条目落地进度与用户裁决（2026-09-16）

> 来源：`/root/deliverables/CakeGame字段差距分析.md`、`CakeGame字段差距_补漏.md`。用户拍板「一二档全做」，补漏 α/D 组按下列口径处理。

### 已落地
| 批 | 范围 | 状态 |
|---|---|---|
| 批22 | 装备职业限制 `job_restrict`（复用 skills 同名键）· 使用等级 `use_level` · 战斗词条族（吸血/免伤/物穿值·比/法穿值·比，只加 `data/gear_stats.py` 注册表）· 无视护盾/免疫反弹（并入 effects）· 怪物侧复用同族键 | ✅ 并入 main（`3937213`），全量 7762 passed |

### 已裁决 · 明确不做（用户 2026-09-16）
| 条目 | 裁决 |
|---|---|
| **咏唱**（α8，装备附加 Re 内的咏唱机制） | **不要**（用户明确） |
| **同伴 / 好感系统**（补漏 H4 / D-3） | **不要**（用户明确） |
| G2 提交时消耗 vs 仅校验持有 | **不做**——补漏已判"已有等价"：我们 `quest.consume`（deliver/collect 两态）同义 |

### 待裁决（保留，等用户）
| # | 事项 | 说明 |
|---|---|---|
| D-1 | **转职属性继承开关** 是否推翻 `core/levelup.py:262`（2026-09-09 拍板：职业成长跟随职业，不保留旧职业成长） | 已拍板项，改动需用户明确点头；批23 暂不做 |
| D-3 | 排行榜 / 玩家寄售商店（同伴好感用户已否） | 系统级，成本最高，未决 |
| D-5 | `Lost_target`（丢失目标机制）我们是否已有等价 | 未坐实（grep 零命中，但战斗链可能有隐含实现） |
| D-6 | `transfer_allowed`（强化转移）：**键在、校验在、`core/` 无消费点**（半个空壳） | 是否实装未决 |
| D-7 | `Basic_User` 34 项属性是否要在我们侧做"可配初值表" | 取决运营需求；新手锚点已由 K1（注册礼包）覆盖 |
| — | **α 组其余条目**（α1 装备赋予技能【P0】· α2 物品最大获取数量【P0】· α3 装备增幅技能 · α4 技能条件进阶 · α5 替换普攻 · α6 替换职业） | **用户未否**，排入后续批次（拟批26） |

### 批次队列（串行，因均改 `field_meta.py`）
- 批23 技能/职业（`hp_cost` · 转职前置 `advance` 子对象 · `is_basic`）— 进行中
- 批24 地图/副本/任务（隐藏禁传送 · 进图消耗/门票 · 主动遭遇概率+参战数 · 副本层间推进 · 任务重置周期；**G2 已剔除**）
- 批25 NPC/成就/全局设置（功能次数上限 · 收费多通道 · 成就计入总数/自动领取 · 注册礼包 · 按状态禁用指令 · **指令限流 K3** · 消息分段上限）
- 批26 α 组（除咏唱/好感）
