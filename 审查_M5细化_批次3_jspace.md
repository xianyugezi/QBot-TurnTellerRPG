# 审查报告：M5 细化 批次3——D5 战斗/状态消息模板 + /角色 三层展示（j-space）

> 审查类型：**纯静态审查**（仅 read/grep 读取，未运行任何命令/脚本/验证——本环境无 bash 沙箱，按任务要求禁验）。
> 凡涉及运行行为（验收是否失败、运行时字段读取等）的结论均标注「**静态推导**」。
> 审查依据：docs/m5_shared_contract.md（唯一契约）· docs/m5_batch_plan.md · docs/细化/细化_5e_战斗战报格式.md（433 行）· docs/细化/细化_3d_消息模板规范.md（372 行）· docs/细化/细化_4f_基础指令组契约.md · docs/细化/细化_3b_玩家属性三层.md · docs/审查参考/战斗数值层设计定稿.md（383 行）· docs/审查参考/消息前缀功能设计定稿.md（181 行）· qbot_rpg/core/battle.py（1841 行，ActionOutcome L218-236 / TurnReport L240-250）· qbot_rpg/core/message_format/battle_render.py（50 行骨架）· qbot_rpg/core/message_format/panel_render.py（114 行）· qbot_rpg/commands/basic_commands.py（1109 行，cmd_view/attr_line/view_header）。
> 门控：j-space **full 档**（按任务指定；因跨 8 文件+5 维度实际按 loop 运行，台账见文末）。

---

## 结论摘要

- **P0 × 2**（契约模板文本与 5e 逐字冲突，直接导致逐字验收失败）
- **P1 × 4**（契约签名/引擎字段描述失真 ×2、契约缺登记军规/折叠锚点 ×2）
- **P2 × 5**（行号漂移、示例数值不自洽、验收映射不完整、明细格式未承载等）

Top 问题：① BREP-02 契约丢 HP 后缀（§5.2 与 TC-07 断言自相矛盾）；② BREP-09 操作提示行丢 `/最大` 分母（契约+batch 同错）；③ ActionOutcome 字段清单编造（rating/damage/outcomes 并不存在）。

---

## 维度① 缺漏（点名「契约应覆盖 X 但未覆盖」）

1. **契约应覆盖 5e 军规5「结算一次性」但未覆盖**（P1）
   - 5e 军规5：胜负/奖励/掉落/快照清理统一当轮事件末尾结算一次；经验/掉落只在战斗结束消息输出一次，不逐怪逐段刷；BOSS/最后目标死亡→立刻结束、未执行后续事件作废不渲染（【数值层】L68/L57/L69）。
   - 契约 10 条铁律无此条；§5.2 BREP-20 仅列「20 经验与掉落」，无「只在结束输出一次」约束；BREP-22 亦无「BOSS 提前结束不清算后续段数」。
   - 仅 batch M5-04 内容句提及「（只在战斗结束消息输出一次）」，契约（唯一依据）层面缺失。
   - 修复：契约铁律补第 11 条或 §5.2 BREP-20/22 备注补「只在战斗结束消息输出一次 / BOSS 死立即结束，后续段数作废」。

2. **契约应覆盖「单条消息 ≤16 行折叠上限」但未覆盖**（P1，另见维度⑤）
   - 锚点在 3d §3.2 L184（真实存在：「一条消息的总渲染行数上限（含前缀/正文/页脚）默认 16 行」）与 5e TC-06；契约铁律/§5.2/§5.4 均无 16 行上限，BREP-25 只写「5 条/页分页」。
   - 修复：契约 §五 增加折叠锚点条目（如铁律补「单条消息 ≤16 行折叠上限，超限按正文尾部→中间过程行优先折叠 TPL-09」），并注明 3d §3.2 L184 / 5e TC-06。

3. **契约应覆盖 TC-06 折叠断言的实现承接锚点但未覆盖**（P1/P2，并入上条）
   - 5e TC-06（BOSS 45 回合：总消息数=45；单条 ≤16 行；状态行差分）在 batch 中无验收归属：M5-03 验收仅列 TC-07~11、M5-04 列 TC-12~20、M5-05 列 TC-21~27，TC-01~06 均未显式映射（TC-06 的 ≤16 行折叠断言无任何 M5 验收项）。

4. **batch M5-03/04/05 任务划分覆盖完整性**（确认无漏，仅验收映射不完整）
   - BREP-01~25 分块完整无重叠：M5-03=BREP-01~09、M5-04=BREP-10~20、M5-05=BREP-21~25；M5-09=81 TC 门禁。✓
   - 漏点：TC-01（单条+前缀首行+操作提示行）、TC-02（前缀三态）未列入任何 M5 验收清单（隐式覆盖：M5-01/06/08）；TC-06 折叠无锚点（见上）。建议 batch 计划补一张「TC-01~27 ↔ M5-0x 验收」映射表（P2）。

5. **契约 BREP-25 未承载摘要行/条目行格式**（P2）
   - 5e §6.3 有摘要行 `总伤害 {N}｜最大单段 {M}｜会心 {K} 次｜格挡 {G} 次` 与条目行 `{来源} {总伤害}（{占比}%）`；契约 §5.2 只写「25 木桩明细（5 条/页分页）」。契约自称「实现层唯一依据」，建议补两行格式（或明确引用 5e §6.3）。

---

## 维度② 错误（已知疑点 + 新发现）

### P0-1：BREP-02 契约文本丢 HP 后缀（疑点 a 确认）
- **契约 §5.2（m5_shared_contract.md L144）**：`✅ 你{攻击动作}{目标}，造成 {伤害} 伤害`（丢 `（{目标} {剩余HP}/{最大HP}）`）。
- **batch M5-03（m5_batch_plan.md L26）**：`✅ 你{攻击动作}{目标}，造成 {伤害} 伤害（{目标} {HP}）`（缩写为 `{HP}`，丢失 `{剩余HP}/{最大HP}` 双值语义）。
- **5e 原文（细化_5e §1.4 BREP-02 / §2.1 / TC-07）**：`✅ 你{攻击动作}{目标}，造成 {伤害} 伤害（{目标} {剩余HP}/{最大HP}）`；TC-07 逐字断言 `✅ 你施放火球术，造成 18 伤害（史莱姆 7/25）`（HP 后缀仍在）。
- **判定**：契约 §5.2 与 5e/自身 TC 矩阵（TC-07 断言含 HP 后缀）自相矛盾；按契约实装将逐字验收失败（静态推导）。
- **正确文本**：`✅ 你{攻击动作}{目标}，造成 {伤害} 伤害（{目标} {剩余HP}/{最大HP}）`。注意 5e 的 `{目标}` 可选仅指「你{攻击动作}」动作短语（3d D-01 降级口径 → `你施放火球术，造成 …`），**HP 后缀必须保留**；契约「，{目标} 可选」注与此混淆，需一并澄清。
- 修复：m5_shared_contract.md §5.2 L144 与 m5_batch_plan.md M5-03 L26 两处文本改为上述正确文本。

### P0-2：BREP-09 操作提示行丢 `/最大` 分母（新发现，同类传播性错误）
- **契约 §5.2（L143）与 batch M5-03（L26）**：`你 {HP} | {目标} {HP} → /攻击[技能] /道具 /防御 /逃跑`。
- **5e 原文（§1.4 BREP-09 / §1.5 骨架 L130）与【前缀】L31 / 3d §四降级示例 L232**：`你 {HP}/{最大} | {目标} {HP}/{最大} → /攻击[技能] /道具 /防御 /逃跑`（示例 `你 21/30 | 史莱姆 7/25 → …`）。
- **判定**：契约两处均丢 `/最大` 分母，与 5e/前缀/3d 处处原文冲突；按契约实装将导致 5e TC-01/TC-05（排版符号与操作提示行逐字一致断言）验收失败（静态推导）。
- **正确文本**：`你 {HP}/{最大} | {目标} {HP}/{最大} → /攻击[技能] /道具 /防御 /逃跑`。
- 修复：m5_shared_contract.md §5.2 L143 与 m5_batch_plan.md M5-03 L26 两处文本改为上述正确文本。

### P1-1：IF30/IF32 契约签名与骨架不符（疑点 b 确认，给对齐建议）
- 契约 §5.1：`IF30 render_battle_start(ctx)`、`IF32 render_battle_end(ctx)`。
- 骨架 battle_render.py 真实签名（L29-50）：`render_battle_start(party, enemy, hint=None)`、`render_battle_end(player, enemy, winner, summary=None)`。
- **对齐建议（以骨架为准回写契约）**：骨架是已提交代码，且文件头 M1 契约约束 S1~S5 已定义；batch M5-03 又明确「实装 render_battle_start/round/end 骨架」——契约应按骨架签名改写：
  - `IF30 render_battle_start(party, enemy, hint=None) -> str`（`hint` 承载意图/弱点情报行，映射 BREP-23）
  - `IF32 render_battle_end(player, enemy, winner, summary=None) -> str`（`winner` 承载胜负结果、`summary` 承载 BREP-24/25 汇总与明细）
  - M5-03 实装时禁止改骨架签名（batch 计划 M5-03 应补一句「按现有骨架签名实现」）。
  - 另建议契约在 §5.1 注明 `hint`/`summary` 的可选语义，避免实现歧义。

### P1-2：ActionOutcome 字段清单编造（疑点③核对，幻觉维度，见维度③ #1）

### P1-3 / P1-4：见维度① #1（军规5）、#2（16 行折叠锚点）

---

## 维度③ 幻觉核对

### 1. （P1）「ActionOutcome 含 rating/damage/battle_ended/message/outcomes」与 battle.py 实际结构不符（编造字段）
- 契约 §5.1（L138）与 batch M5-03（L28）均写：`battle.py 引擎 ActionOutcome（含 rating/damage/battle_ended/message/outcomes）`。
- **battle.py 实际（L218-236 ActionOutcome dataclass）**：字段 = `ok / seq / actor / action_type / target / hit / crit / blocked / raw_damage / final_damage / target_hp / side_effects / message / battle_ended / status`。
- 逐项核对：
  - `rating` —— **不存在**（引擎方法内是局部 dict，L596/609 等，经 `_action_outcome` 摊开为各字段，不构成 ActionOutcome 成员）；
  - `damage` —— **不存在**，实际为 `raw_damage`（拦截链前）与 `final_damage`（拦截链后实际扣血）；
  - `battle_ended` —— ✓ 存在（L235）；
  - `message` —— ✓ 存在（L234）；
  - `outcomes` —— **不在 ActionOutcome**，在 `TurnReport.outcomes: Tuple[ActionOutcome, ...]`（L250）。
- 修复建议：契约 §5.1 改写为「引擎输出源：`TurnReport`（turn/phases/player/enemy HP/ended/status/outcomes 流水）+ `ActionOutcome`（ok/hit/crit/blocked/final_damage/raw_damage/target_hp/target/side_effects/message/battle_ended/status）」；并注明战报取数口径：伤害= `final_damage`（拦截链后实际扣血，对齐 5e §2.1）、目标 HP= `target_hp`（扣血后即时值）。

### 2. 行号真实性（核心疑点③）
- **【数值层】L44-72 回合死亡判定顺序：真实存在**，且 5e 军规4 引用的子行序（L49 dot / L50-52 即死 / L53 先手 / L54-57 击杀 / L58-61 后手 / L62 tick / L63-67 互杀 / L68-69 结算）逐条吻合；5e 头注「由 v1.1 的 L40-68 整体 +4 下移至 L44-72」与现行 383 行定稿一致。
- 5e 其余【数值层】行号抽查（L14-17/24-27/36/38/50-52/55-57/60-61/64-67/70-71/106-107/129-133/139-141/147-153/168/171/240/292-294/314/319/340/344-351）：**全部真实且语义吻合**。
- 5e/3d 对【前缀】定稿行号抽查（L22/L28-31/L34/L80/L82/L83/L170/L17/L97-101/L100/L41-49/L64-76/L90-92/L112-121/L143-155/L103-108/L151/L114/L130/L133/L35）：**全部真实**。
- **唯一行号漂移（P2）**：5e §1.3/TC-06/§6.3 引用「【模板规范】D-03/L171」指 16 行折叠上限，但 3d 的 16 行上限实际在 **L184**（L171 是「战斗一轮 1 条」承接表行）。建议 5e 三处改引「3d §3.2 L184」。

### 3. 无编造模板 / 无编造 TC
- 5e BREP-01~25 全表与 §2~§6 各章节模板自洽；TC-01~27 与覆盖矩阵（6 类 27 条）一一对应，无凭空编号。
- 契约 §5.2 的 25 条 BREP 与 5e 一一对应，无新增/遗漏 ID（BREP-01~25 全覆盖，维度①确认）。
- 3d/4f TC 矩阵与契约 §六 汇总数字自洽：26+27+28=81，算术正确。

---

## 维度④ 跨文档一致性

### 1. 契约 BREP 模板 vs 5e 逐字抽查（BREP-01/02/09/10/12/15/20/23/24/25）
| BREP | 契约 §5.2 | 5e §1.4 | 判定 |
|---|---|---|---|
| 01 | 「前缀行归 D1」 | 三段式骨架（前缀+正文+末行操作提示/状态行） | 语义一致（前缀委托 D1，铁律2/§5.2 骨架兜底）✓ |
| 02 | `✅ 你{攻击动作}{目标}，造成 {伤害} 伤害`（丢 HP 后缀） | `…（{目标} {剩余HP}/{最大HP}）` | **P0-1 冲突** |
| 09 | `你 {HP} | {目标} {HP} → …`（丢 /最大） | `你 {HP}/{最大} | {目标} {HP}/{最大} → …` | **P0-2 冲突** |
| 10 | 无模板文本 | `❌ {怪物}{攻击动作}，你受到 {伤害} 伤害（HP {剩余}/{最大}）` | 无冲突（契约未展开）✓ |
| 12 | `{怪物} 蓄力中（下回合发动「{招名}」）` | 同左（D-5E） | **逐字一致** ✓ |
| 15 | 无模板文本（注明紧跟伤害行） | `✅ 你击败了{怪物}！` | 无冲突 ✓ |
| 20 | 「20 经验与掉落」无一次性约束 | `✅ 获得 经验 {n}、金币 {n}、{素材}×{n}` + 只在结束输出一次 | 缺「一次性」约束（维度①#1） |
| 23 | IF30 语义：`与{怪物}的战斗开始！+HP+意图/弱点情报行` | `与{怪物}的战斗开始！{怪物} {HP}/{最大HP}` + 情报行 | 语义一致 ✓ |
| 24 | 「24 汇总」 | `战斗结束：{胜负结果}｜回合数 {N}｜输入 /战斗记录 查看明细` | 语义一致（batch M5-05 展开式同）✓ |
| 25 | 「25 木桩明细（5 条/页分页）」 | 条目 `{来源} {总伤害}（{占比}%）` + 摘要行 + TPL-08/TPL-09 | 缺摘要/条目行格式（P2）+ 缺折叠锚点（P1） |

### 2. /角色 三层行：4f §2.5 / 3b / basic_commands.py / 契约 §5.3
- 4f RUL-37 LV 行 `【角色】Lv3.阿伟（战士） ｜ 经验 320/1000`（满级 `｜ 【已满级】`）；RUL-38 三层行 `{序号}. 【{属性名}】{最终值}（白值 {base} ｜ 加成 +5·+10% ｜ 临时 +3·+20%）`；resource 型 `1. 【生命】21/30（…）`。
- basic_commands.py：`view_header`（L311-324）与 `attr_line`（L380-393）/`_render_attr_page`（L396-433）实现与 4f RUL-37/38 **逐字段一致**（含 resource 型、5 条/页、TPL-08、裁决②夹取、未注册 TPL_REGISTER_GATE）。
- 契约 §5.3 与 4f/实现一致（摘要性引用）。✓
- 3b §一/§4.4 三层模型（白值/加成 flat+pct/临时 pct+flat）与 calc_all_final_attributes 管线真实存在。✓
- **唯一问题（P2-2）**：示例数值 `1. 【力量】26（白值 15 ｜ 加成 +5·+10% ｜ 临时 +3·+20%）`——按 3b TC-03 管线（15+5→20×1.10→22×1.20→26.4+临时flat3→29.4→**29**）应为 29 而非 26（26 对应无临时 flat 的 3b TC-01）。该示例在 4f RUL-38 / 契约 §5.3 / basic_commands.py L382 文档串三处共用。运行期不受影响（实现用 calc_all_final_attributes 计算），仅示例数值与 3b 口径不符，建议统一改为 29。

### 3. panel_render.py（/状态）
- 114 行，纯文本无装饰 emoji，前缀首行 + 多行正文，与契约 §5.4「✅」及 3d §3.1 一致，**无问题**。

---

## 维度⑤ 16 行折叠锚点（3d §3.2 L184 / 5e TC-06）

- **结论：契约未登记「单条消息 ≤16 行折叠上限」**（铁律 10 条与 §五 均无）。
- 锚点真实性：3d §3.2 L184 真实（「一条消息的总渲染行数上限（含前缀/正文/页脚）默认 16 行；超限按正文尾部→中间过程行优先折叠 TPL-09」）；5e TC-06 断言「单条消息渲染行数 ≤16 行」；5e §6.3 BREP-25 折叠明细亦依赖该上限。
- **M5-05 实装需要**：BREP-25 木桩明细块（一行摘要+折叠明细+5 条/页）与 5e TC-06/TC-26 的折叠断言均需该锚点；batch M5-05 只写「折叠明细」未给 16 行阈值。
- 修复建议：契约 §五 增加折叠锚点条目（建议并入铁律，如「单条消息 ≤16 行折叠上限，超限折叠 TPL-09（3d §3.2 L184 / 5e TC-06）；与 sender 分两条为分层关系（内容层按行折叠、传输层按字符分条）」），并在 §5.4 BREP-25 行注明「分页 + ≤16 行折叠」；batch M5-05 验收补「TC-06 ≤16 行折叠」断言。

---

## 无问题维度确认

- 疑点 c：契约 IF31 `render_battle_round(round_result)` 与骨架签名 `round_result` **一致，无问题**。
- 疑点③ 行号：定稿 L44-72 与 5e/3d 对【前缀】的引用**全部真实**，无编造行号/模板/TC（仅 5e→3d L171 一处漂移，P2）。
- 维度④：BREP-12 契约与 5e 逐字一致；BREP-01/10/15/23/24 无文本冲突；/角色 三层行 4f↔代码↔契约一致（除示例数值 P2）；panel_render.py 无问题。
- 铁律覆盖：5e 军规 1/2/3/4（前缀首行/少 emoji/单回合单条/渲染顺序）均被契约铁律 1/3/2/9 覆盖；「每段一行」被 §5.2 BREP-21 覆盖；「状态行前 5 + 还有 N 个状态」被铁律 10 覆盖；意图预告/拦截链/连段段行被 §5.2 覆盖。
- 契约 §六 TC 矩阵汇总：3d 26 + 5e 27 + 4f 28 = 81，与各细化覆盖矩阵自洽。

---

## 分级清单总表

| 级别 | 编号 | 问题 | 文件位置 | 修复建议 |
|---|---|---|---|---|
| P0 | 1 | BREP-02 丢 HP 后缀 | m5_shared_contract.md §5.2 L144；m5_batch_plan.md M5-03 L26 | 改为 `✅ 你{攻击动作}{目标}，造成 {伤害} 伤害（{目标} {剩余HP}/{最大HP}）`，澄清 `{目标}` 可选仅指动作短语 |
| P0 | 2 | BREP-09 丢 `/最大` 分母 | m5_shared_contract.md §5.2 L143；m5_batch_plan.md M5-03 L26 | 改为 `你 {HP}/{最大} | {目标} {HP}/{最大} → /攻击[技能] /道具 /防御 /逃跑` |
| P1 | 1 | IF30/IF32 契约签名与骨架不符 | m5_shared_contract.md §5.1 L135/L137 | 契约回写为 `render_battle_start(party, enemy, hint=None)` / `render_battle_end(player, enemy, winner, summary=None)`，注明 hint/summary 语义 |
| P1 | 2 | ActionOutcome 字段清单编造 | m5_shared_contract.md §5.1 L138；m5_batch_plan.md M5-03 L28 | 改为真实字段（final_damage/raw_damage/target_hp/hit/crit/blocked/side_effects/status…；outcomes 归 TurnReport），注明 final_damage/target_hp 取数口径 |
| P1 | 3 | 5e 军规5「结算一次性」未登记 | m5_shared_contract.md 铁律/§5.2 BREP-20/22 | 补「经验/掉落只在战斗结束消息输出一次；BOSS 死立即结束、后续事件作废」 |
| P1 | 4 | 16 行折叠上限未登记 | m5_shared_contract.md 铁律/§5.4 BREP-25；m5_batch_plan.md M5-05 | 补「单条消息 ≤16 行折叠上限（3d §3.2 L184 / 5e TC-06）」锚点；M5-05 验收补 TC-06 折叠断言 |
| P2 | 1 | 5e 引用 3d L171 指 16 行上限（实际 L184） | 细化_5e §1.3/TC-06/§6.3 | 改引「3d §3.2 L184」 |
| P2 | 2 | /角色 示例数值 26 vs 管线 29（临时 flat +3） | 4f RUL-38 / 契约 §5.3 / basic_commands.py L382 | 示例统一改为 29（或去掉临时 flat +3） |
| P2 | 3 | TC-01/TC-02 未列入 M5 验收清单（隐式覆盖） | m5_batch_plan.md 批1 | 补 TC-01~06 ↔ M5-0x 验收映射表 |
| P2 | 4 | 契约 BREP-25 未承载摘要/条目行格式 | m5_shared_contract.md §5.2 | 补 `摘要：总伤害 {N}｜…` 与 `{来源} {总伤害}（{占比}%）`（或明确引用 5e §6.3） |
| P2 | 5 | 契约 §5.3 未含 resource 型三层行变体/完整模板 | m5_shared_contract.md §5.3 | 补 `1. 【生命】21/30（…）` 变体或直接引用 4f RUL-38 |

---

## 台账（j-space ledger · 内部）

- goal：静态审查 M5 细化批次3 并落盘分级报告（P0×2 / P1×4 / P2×5）。
- verified：5e BREP-02/09 原文、契约两处偏差、ActionOutcome 真实字段、定稿 L44-72、3d L184、/角色 三层一致、【前缀】行号全真。
- open：无（全部维度已核对）。
- next：ship 检查后交付最终回复。
