# 任务书 · 批7·路U（任务链 25 键）

> 2026-09-12 · 消息模板重构战役 · 执行者：子 agent（本任务书 + `docs/消息模板重构/00_方案与规范_v1.md` 为唯一指令源）
> **先读** `<worktree>/docs/消息模板重构/00_方案与规范_v1.md`（全量口径：14 全角/免斜杠/少｜多换行/❌+原因/拆行优先序/大数值/零 emoji/prose 豁免），再开工。

## 1. 范围（本路 25 键，全部在 `qbot_rpg/core/templates/quest_tpl.py`）

任务板：`quest_board_line` `quest_board_section_header` `quest_board_progress`
`quest_board_main_prefix` `quest_board_marked_suffix` `quest_board_active_full_note`
`quest_board_no_accept_note`
任务信息/进度：`quest_info_header` `quest_info_line` `quest_info_met` `quest_info_not_met`
`quest_info_standalone_usage` `quest_progress_base` `quest_progress_base_no_target`
`quest_progress_current` `quest_progress_param`
接取/放弃/交付：`quest_accept_failed` `quest_abandon_failed` `quest_deliver_failed`
`quest_deliver_skipped` `quest_deliver_skipped_plain` `quest_deliver_seq_shift_note`
空态/门禁：`quest_no_board` `quest_no_quest` `quest_empty_board`

**每键必做**：读旧文案拿到「信息要素 + 触发场景 + 占位符集合」，然后**按规范全新撰写**（旧文案不作参考）。
占位符沿用；`tpl_of(` 键名/参数名**不得改**。任务板/进度行属结构化行（≤14 全角）；
任务介绍/风味文本属介绍类（可自然折行，必要时登记 `meta.prose_keys`）。

## 2. 工作清单（每步都落盘）

1. **第一件事**：写 `<worktree>/docs/消息模板重构/_fragments/b7u.json`（25 键新文案，先落盘再动代码），
   commit（`docs(模板·路U): 批7 fragment 先落盘…`）。
2. 合表：`python3 scripts/tpl_merge_fragments.py <fragment>`（表 = `qbot_rpg/core/templates/template_table.json`）。
3. 清分区：从 `quest_tpl.py` 删除已迁 25 键；`content/test_demo/templates.json` 同 key 同步新文案。
4. 代码/测试同步：`grep -rn "<key>" qbot_rpg/ tests/ scripts/` —— 模块级常量别名、断言旧文案、
   docstring 引用逐处更新；**不得**为过测试改回旧文案。
5. 自校验：`.venv/bin/python scripts/check_template_width.py`（本路键 0 FAIL）；
   `pytest tests/unit/test_template_table.py tests/unit/test_emoji_discipline.py -q`；
   定向跑任务相关测试（`tests/unit/test_quest*.py`、`test_quest_board*.py` 等）；**最后跑一次全量**。
6. `.venv/bin/ruff check <改动文件>` 干净；commit（`feat(模板·路U): …`）。**禁止 push / 禁止动 main**。

## 3. 报告（回复正文内联）
- fragment 路径 + 25 键新文案（JSON 全量内联）；合表结果（新增 N/冲突 0/宽度 FAIL-WARN）
- 改动文件清单；checker 输出；定向+全量计数原文；未完成/存疑项；commit hash
- **预算纪律**：临近上限时优先 (a) fragment 落盘 (b) 代码已 commit (c) 如实列未完成；禁止谎报/半成品。
