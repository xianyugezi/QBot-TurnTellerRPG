# 幻觉审查报告 · 细化_2c3b_强化流程契约.md

- **审查对象**：`/root/QBot-TurnTellerRPG/docs/细化/细化_2c3b_强化流程契约.md`（334 行）
- **依据定稿**：`/root/docs_archive/RPG框架项目/强化系统设计定稿.md`（v1.0，235 行，全文逐行核对）
- **核对方法**：四查口诀（①行号引对 ②内容一致 ③补白显式标注 ④数值有依据）+ 全文抽取声明 → 定稿逐条回查 → 兄弟文档 grep 实证。
- **核对范围**：头部定位表 8 行（L13-20）、指令表 3 条、守卫 GU-01~06、解析规则 P-01~06、F-01~07 / T-1~4、消息模板汇总 8 行、§1.5 模板 8 行、§2.2 输出结构 4 行、§3.2 消耗时机表、§3.4 一致性表、§4.6 2c2b 一致性、§5.2 指令参数表、§5.3 解析失败汇总、验收用例 TC-01~23 全量、覆盖矩阵、§七 登记 8 行——逐条核对。
- **外部引用实证对象**：规划_路2c3_强化.md（181 行，任务定版/C1 解析语义/C3 保护石）、细化_2c3a_强化数值曲线.md（356 行全文）、细化_2c2b_锻造流程契约.md（P-02/P-03/P-05/§1.2/幂等表述）、《指令分隔符统一规范.md》（246 行，L10/L19/L34）、锻造系统设计定稿.md（L12）、覆盖审计_F_生活生产.md（交叉佐证惯例）。

---

## 总体结论

**P0 = 0 条 / P1 = 7 条 / P2 = 5 条**

**评分：B**（定稿引用密度高、指令示例逐字对照零差错、行号绝大多数精确命中；但补白纪律系统性缺失——全文 0 处【补白/工程补白】标注——且存在 4 处兄弟文档/规划引用不实或章节错位，修订后可升 A）

**一句话**：无编造定稿内容、无指令示例改写，但 7 处 P1 集中在「自创规则/工程机制未标补白（GU-04 校验锚点、F 系列事务幂等底座）、推导冒充原文（L226 经济消耗、任务定版引据）、兄弟章节错位（2c3a §8 values ×3、2c3a §7 校验）及与 2c3a 显示口径冲突（幸运 0/关闭态）」。

---

## P0（严重幻觉：虚构定稿内容 / 方向性错误引用）— 0 条

逐条核对结论（关键项）：

- **指令示例逐字核对全过**：`⚙️ 铁剑 → 铁剑+1（成功率 90%｜幸运修正 +2）`（L36）✓ / `✅ 成功！攻击 12 → 13`（L37）✓ / `⚙️ 铁剑+5 → +6（成功率 50%｜幸运修正 -1）`（L41）✓ / `❌ 失败！强化等级 -1（铁剑+5 → +4）`（L42）✓——细化 L51-52/L79-81/TC-01/TC-02/TC-04 中五处复现与定稿逐字一致，无改字/加词/少词。
- **§0 定位表 8 行**：L13（L16/L22/L27）L14（L44/L45）L15（L10）L16（L53-55/L155）L17（L68-69/L188）L18（L113-115/L154）L19（L214-216）L20（L230）——所有引用行均为有实义文本且内容支持声明。
- **守卫表 GU-01~05**：GU-01（L196-205 校验器节）✓ / GU-02（L10/L214）✓ / GU-03（L214 <装备> 语义）✓ / GU-05（L103-105/L176-177 + 2c3a §1.1/§1.2/§1.4——2c3a §1.4 确有「先扣强化石再扣金币」（L78），「先石后币」成立）✓。
- **成功/失败路径**：材料照扣（L103"每次强化消耗"）✓ / 分级阈值（L53-55/L155，目标 ≤3 不变、>3 掉级）✓ / 碎装备（L60-63/L156-158）✓ / 保护石定义（L68/L188/L69）✓ / 上限（L113-115/L154）✓。
- **§五 解析**：P-03 禁批量（定稿 L10+L27 随机性不同源）✓ / P-04 三前缀（L214-216）✓ / P-05 沿用 2c2b P-02/P-03（2c2b L199/L200 实存：允许字符集/罗马数字按普通字符）✓ / 转移/碎/门槛无指令入口（L127-129/L62/L141）✓。
- **验收用例 23 例**：覆盖矩阵 A6+B3+C4+D2+E4+F4=23 ✓，TC 编号连续 TC-01~TC-23 ✓；各例数值（+6 中级石×6+600 币、+3 低级石×3+300 币、√25=5→+60%、达顶（+12）零消耗、精良+8/传说+12）全部与 2c3a 默认表和定稿上限表一致；TC 依据列行号逐条回查无落空。
- **§七 登记 8 行**：L214/L34-45、L216/L90/L113-114、L215/L68/L188、L44/L210/L184-187、L44/L230、L230、L196-210、L127-142——全部真实行号、内容支撑。
- **头部自检**：自称 235 行 = wc 235 ✓；自称 356 行（2c3a）= wc 356 ✓；自称 334 行 = wc 334 ✓；规则/用例数量与正文加总一致 ✓。

---

## P1（明显问题：引用错误 / 补白漏标 / 兄弟口径冲突）— 7 条

### P1-1　GU-04「等级匹配」校验锚点语义为自创规则，引用列仅挂定稿 L10，未标补白（语义实出于规划 C1）
- **位置**：细化 L31（带标形式）、L42（GU-04 行）、L202（4.5 第二条）、TC-19（L307）、TC-20（L313）
- **声明**：`+N` 为等级标记且「N = 玩家声明的当前强化等级，标记仅作校验锚点（GU-04）」「带 +N 时：实例当前等级 == N（不符 → 拒绝并报实际等级）」——引用列仅 `L10 / L115 / L113-114 / L154`
- **核对**：定稿 L10 只规定「+ 为等级标记（保持）」，没有任何「校验当前等级」语义；「带 +N 即声明当前等级、不符即拒」是细化新增的防连点规则。该语义事实上出自规划_路2c3 C1（L106「`/强化 铁剑+5` → 物品名+现等级 5（目标 6）」），但细化既未引用规划 C1、也未标【补白】，引用列挂 L10 暗示「定稿原文如此」。
- **证据**：定稿 L10 `> 指令分隔：遵循《指令分隔符统一规范》——/强化 铁剑+5 的 + 为等级标记（保持）；强化禁批量 *…`——无校验语义；规划 C1 L106 为唯一语义出处。
- **建议**：GU-04 ①②与 L31/L202/TC-19/TC-20 引用列补 `规划 C1`，或将「校验锚点」单列规则并标【工程补白（溯源：规划 C1）】。

### P1-2　L70「失败消耗即经济消耗设计 L226」——推导冒充原文，L226 实为资源获取对策
- **位置**：细化 L70
- **声明**：`失败与成功同样消耗石+金币（L103-105「每次强化消耗」，无成功/失败区分；失败消耗即经济消耗设计 L226）`
- **核对**：定稿 L226 = `| 强化石卡资源 | 炼金可量产 + 商店基础石 + 掉落补充（三渠道） |`——是资源获取三渠道对策行，不含「失败消耗=经济消耗」任何表述；「失败消耗作为经济出口」的正当性依据应为 L130（装备淘汰=重新强化 → 强化石消耗循环 = 经济出口），且即便 L130 也讲的是换装淘汰而非失败消耗，属细化自己的设计推断。
- **证据**：定稿 L226（见上，原文为风险-对策表中「强化石卡资源」行）；L130 `→ 装备淘汰 = 重新强化（强化石消耗循环 = 经济出口）`。
- **建议**：删除「（失败消耗即经济消耗设计 L226）」或改为「（细化设计推断：失败照扣使强化石成为持续消耗品，定稿未明示；经济出口依据 L130）」+ 标补白。

### P1-3　「2c3a §8 values 表」引用错位 ×3——2c3a §8 是行号回查表，values 定义在 §3.4/§5.1
- **位置**：细化 L59（§1.3 第 4 步）、L274（TC-06 依据列）、L327（§七 玩家存档行）
- **声明**：`属性变更（武器攻击/防具防御按 2c3a §8 values 表，{"type":"flat","value":…} L184-187）` / `delta 按 2c3a §8 values` / `属性快照（2c3a §8 values）`
- **核对**：2c3a §8 标题为「行号回查表」（L341）；values 字段级定义在 2c3a §3.4（每级数值 L179-189）与 §5.1 字段表 D-11/D-12（L251-252）。「§8 values」会引导实现者翻到回查表找数值定义。
- **证据**：2c3a L341 `## 8. 行号回查表`；L184-187 为 values 对象、L186-187 为 flat 5/flat 3 默认值——L184-187 引用本身正确，错在「§8」前缀。
- **建议**：三处改「2c3a §3.4（values 定义）」。

### P1-4　「任务定版『保护石：失败有惩罚时扣 1 颗免掉级』」引据不实——无该定版记录，实为规划 C3 的「实现默认语义」
- **位置**：细化 L141（§3.2 定版依据）、L290（TC-12 依据列）
- **声明**：`> 定版依据：任务定版"保护石：失败**有惩罚时**扣 1 颗免掉级"`；TC-12 依据列 `L53 / L68 / 定版（有惩罚才扣）`
- **核对**：规划_路2c3 C3（L122）原文为「消耗时机（**实现默认语义**）：失败且该失败将产生惩罚（掉级/碎）时消耗 1 颗…成功或低段无惩罚失败不消耗」——标注为「实现默认语义」而非用户拍板/任务定版，且逐字引文「保护石：失败有惩罚时扣 1 颗免掉级」在规划中并不存在。定稿 L68 面文为「消耗 1 颗，**失败时消耗**」——按字面任何失败都消耗；「有惩罚才扣」是规划层对 L68 的收敛细化，与定稿面文有出入，属应显式标注的取舍。
- **证据**：规划 C3 L122（见上）；定稿 L68 `- 保护石：强化失败不掉级（消耗 1 颗，失败时消耗）`；定稿 L70 `作用：高等级段堆装备的保险`（细化引用 L70 支持「只保高段」方向，此点成立）。
- **建议**：L141 改称「定版依据：规划 C3『实现默认语义』（失败有惩罚才扣；定稿 L68『失败时消耗』按字面含低段，此处收敛为高段惩罚场景，对齐 L70 保险定位）」并标【细化取舍】；TC-12 依据列同步改「规划 C3」。

### P1-5　与 2c3a 显示口径冲突：幸运修正为 0 / 开关关闭的显示格式两文档互相矛盾
- **位置**：细化 L102（§2.2 输出示例）、L110（成功率行数据源）、L280（TC-07 期望断言）
- **声明**：幸运修正为 0 时显示 `→ +4 成功率：70%（基础 70% + 幸运 0%｜幸运修正开）`；luck_affects=false 时显示 `幸运修正已关`
- **核对**：2c3a §2.3（L132）明确定版显示规则：`幸运修正为 0 或开关关闭时，仅显示「成功率 X%（基础）」`（TC-10 亦按此断言）。2c3b 的「+ 幸运 0%」与「幸运修正已关」两种形态与 2c3a 定义的口径直接冲突——同一场景两文档给出不同的消息文本，TC-07 若按 2c3a 实现必然失败。任务要求「凡数值声明需与 2c3a 一致」，本文未标注该取舍。
- **证据**：2c3a L131 `强化回执中成功率 = 基础 X% ± 幸运 Y% 三段展示…`；L132 `幸运修正为 0 或开关关闭时，仅显示「成功率 X%（基础）」`；2c3b L102/L280 显示 `+ 幸运 0%`。
- **建议**：二选一并回标：依 2c3a（0/关闭 → 只显示「成功率 X%（基础）」）改 L102/L110/L280，或显式声明「2c3b 流程契约扩展显示形态（幸运 0 也展示拆分项），覆盖 2c3a §2.3」并删除 TC-07 的「+ 幸运 0%」断言冲突。

### P1-6　4.6/L328「与锻造 2c2b §1.2 同款结论（原子事务 + message_id 幂等底座）」引用不实——2c2b 全文无事务/幂等表述，底座实出于实现层规划文档 V11
- **位置**：细化 L207（§4.6 第一句）、L328（§七 结算底座行）
- **声明**：`锻造与强化共用同一"原子事务 + message_id 幂等"结算底座（2c2b §1.2 同款结论，forge_log/enhance_log 同表族结构）`；`SQLite BEGIN IMMEDIATE 单事务 + message_id 幂等（F-01~07，与锻造 2c2b §1 共用）`
- **核对**：2c2b 全文 grep `幂等|message_id|事务|SQLite|BEGIN` 命中 **0** 处；2c2b §1.2 只讲「零会话原子、一条指令出结果」结论（L55），没有事务机制与幂等键设计。message_id 幂等/事务底座的实际出处是 规划_路2c3 B4（L92 引用实现层规划文档 V11「购买/强化/奖励结算防双扣：SQLite 事务 + message_id 幂等 + version」）。「forge_log/enhance_log 同表族结构」亦为本文自创描述。
- **证据**：2c2b grep 0 命中；规划 B4 L92 `实现层规划文档 V11（购买/强化/奖励结算防双扣：SQLite 事务 + message_id 幂等 + version）…+ 路2b 规划 C5 同款「事务 + 幂等键」模式`。
- **建议**：4.6/L328 引用改「规划_路2c3 B4 / 实现层规划文档 V11（购买/强化/奖励统一结算底座）」，删除「2c2b §1.2 同款结论」表述；同表族结构标注【工程补白】。

### P1-7　全文 0 处【补白/工程补白】标注——自创规则/工程机制/平台假设大量以定稿行号为引用列，系统性纪律缺失
- **位置**：全文（grep `补白` 命中 0；代表性位置：L31/L42、L45 守卫顺序、L56/L181 F-01、L188-190 F-03~05、L172 T-1、L219 P-01、L220 P-02 正则、L222 P-04 最长前缀、L224 P-06 无参/多参、L83 材料不足模板、L84 等级不符模板、L101-105 §2.2 输出结构、L116 同文案常量源、L128 GU-06、L203 共用频率通道）
- **声明**：`（示例）BEGIN IMMEDIATE 后重读背包/等级/材料…` / `enhance_log 表 message_id 建 UNIQUE 索引；每条 QQ 消息的全局唯一 message_id 作为请求标识` / `同一 message_id 被投递两次（QQ 网关重试/消息重发）` / `守卫顺序固定 GU-02→03→04→05` / `名称禁空格 P-01` / `^(.+?)\+(\d+)$` 等
- **核对**：这些均为本文自创的实现默认/新规则/平台假设，§四 F 规则表虽未伪造定稿原文（表格本身无引用列，结构上未直接冒充），但全档自创内容零标注、多处小行内引用列（GU 表/P 表）只挂定稿行号（L10/L214 等），在四查③口径下构成系统性补白漏标；其中 T-1「QQ 网关按同一 message_id 重投两次」是对网关行为的假设，全项目内未见出处（规划 B4 仅说「重发同 id 返回已结算结果」，未断言网关重投行为）。
- **证据**：grep `补白` 于细化_2c3b 命中 0；对照 2c2d 细化同类自创（king_only 等）均显式标【补白】的惯例。
- **建议**：在 §四 开头加一行全局声明「§四 F-01~07、T-1~4 及 §五 P-01~06 解析细节、各输出模板文案为本文工程实现契约，均属【工程补白】，依据见 规划 B4/实现层规划 V11」；GU-04/P-01/P-02 等规则行引用列补「【工程补白】」标注；T-1 标注「平台假设，未证实」。

---

## P2（轻微问题：行号偏移 / 引用过弱 / 措辞瑕疵）— 5 条

### P2-1　L13 定位表引用 L16 应 L15（「强化 = 给装备堆数值（+N）」原文在 L15）
- **位置**：细化 L13（§0 定位表首行）
- **声明**：`强化 = 给装备堆数值 +N` —— 引用 `L16 / L22 / L27`
- **核对**：定稿 L15 = `> 强化 = 给装备堆数值（+N）的系统：消耗强化石+金币，按成功率曲线逐级提升`；L16 是「低等级失败无惩罚…数值是'加数字'」行，「堆数值（+N）」主表述在 L15；L16/L22/L27 对「概率成功（随机性不同源）」部分支撑成立。建议引用改 `L15 / L22 / L27`。

### P2-2　《指令分隔符统一规范》L10 引用错（L33/L234 两处）——L10 为章节标题行，等级标记在 L19、禁批量在 L34
- **位置**：细化 L33（零会话注释）、L234（§5.2 注）
- **声明**：`与《指令分隔符统一规范》L10 同源约定` / `+ 等级标记遵循《指令分隔符统一规范》（L10）`
- **核对**：规范 L10 = `## 一、分隔符总表`（章节标题，无强化内容）；该规范中「+ 等级/数值标记」行为 L19、「/强化 禁止 *（成功率随机，批量=连点爆装风险）；+N 是等级标记不是数量」为 L34；且规范全文 grep「零会话」命中 0——L33「同源约定」所指约定在该文件不存在（零会话实为强化定稿 L44/锻造定稿 L239/2c2b L55 的表述）。L213 处引用的是【强化定稿】L10（正确），不受影响。
- **建议**：L33 改为「强化定稿 L44；锻造同款见 2c2b §1.2」，删除「规范 L10 同源」；L234 改「规范 L19/L34」。

### P2-3　GU-01「2c3a §7 校验通过」章节错位——校验器映射实为 2c3a §5.2
- **位置**：细化 L39（GU-01 行）
- **声明**：`内容包 enhance.json 有效（2c3a §7 校验通过：曲线单调不增 / 档位覆盖 / 上限键=items.json 四档品质枚举）`
- **核对**：2c3a §7 为「接缝审计（S1-S4）」，该校验三项对应 2c3a §5.2 校验器映射表 V1（L262）/V2（L263）/V7（L268）。§7 无校验通过语义。
- **建议**：改「2c3a §5.2 V1/V2/V7」。

### P2-4　区间引用起止点含代码围栏行（```）：§二 L34-45、§三.1 L52-56、TC-01 L34-37、TC-04 L41-42 边界
- **位置**：细化 L257（A 行区间）、L269（TC-01 依据）、L272（TC-04 依据）
- **声明**：`§二 L34-45` / `§三.1 L52-56` / `L34-37` / `L41-42`
- **核对**：定稿 L34/L43/L52/L56 均为 ``` 围栏行（L35-37/L40-42/L53-55 为实义内容）；L34-37、L41-42 这类起止落在围栏的引用虽内容正确（实质行全在区间内），但按「单行/小区间引用精确定位到有实义行」的纪律应写 L35-37/L40-41/L53-55。属区间边界偏移类。
- **建议**：改为 L35-37（成功示例）、L40-41（高段失败示例）、L53-55（分级规则）。

### P2-5　§3.3 L150「成功路径模板追加 `｜保护石生效` 前缀标记」措辞自相矛盾（示例为后缀段）
- **位置**：细化 L150
- **声明**：`成功路径模板追加 ｜保护石生效 前缀标记（可选展示）`
- **核对**：本行下方示例 L147 展示为尾缀：`⚙️ 铁剑+5 → +6（成功率 50%｜幸运修正 -1｜保护石生效）`——「保护石生效」位于成功率和幸运修正**之后**（后缀段），与「前缀标记」表述矛盾；且同一行上文「高段失败模板尾部追加」用的是「尾部追加」。应为「尾段追加」。
- **建议**：改「尾段标记」或注明「（示例展示于尾部）」。

---

## 核对亮点（加分项）

1. **指令示例逐字零差错**：定稿 L35-37/L40-42 两段示例在细化中复现 5 处以上（L50-53/L79-81/TC-01/TC-02/TC-04），全角竖线、括号、数值、箭头全部逐字一致；§3.3 保护石模板继承 L41-42 格式并显式声明「定稿 L41-42 格式 + 保护行扩展」（L150），扩展处自报家门，是全文唯一显式标注改写的段落。
2. **行号引用密度高且几乎全真**：全文约 90 处定稿行号引用，逐条回查仅发现 1 处实质内容不符（L226）、1 处轻微的推导性引用（L16 应 L15）、2 处区间围栏边界——其余全部命中「有实义文本的正确行」，且各数值（90%/50%/-1/70%/6 石/600 币/3 石/300 币/+12/+8 上限）全部与定稿默认表及 2c3a 表格一致，无一处杜撰数字。
3. **TC 用例与数值自洽**：23 例断言中的消耗数（目标等级×1/×100）、档位（+3 低级/+6 中级）、幸运算术（√25=5）、达顶零消耗、TC-18 三次连点 +0→+1→+2→+3 状态演化——全部可被 2c3a 默认表验证通过。
4. **2c2b/规划引用抽查基本属实**：2c2b P-05（L202 批量）、P-02/P-03（L199/L200 字符集）、§1.2（L55 零会话）、锻造定稿 L12（数量 `*` 批量）、规划 C1/C3 内容——均真实存在且口径相符。
5. **守卫/规则编号内部一致**：GU-01~06、P-01~06、F-01~07、T-1~4 定义-使用-登记三处闭环，无编号重复或语义错位（对比 2c2d 的 W3/W4 冲突，本文无此类问题）。

---

## 结尾核对基准说明

- 定稿 235 行全文逐行核对（read_file 全量）；细化 334 行全量通读抽查（read_file 全量）。
- 兄弟文档：细化_2c3a 356 行全文读毕；细化_2c2b、规划_路2c3（181 行）、《指令分隔符统一规范》（246 行）、锻造系统设计定稿（410 行 L12 抽查）按引用点 grep/定向阅读实证。
- 全文 `补白` 词频、2c2b `幂等|message_id|事务|SQLite|BEGIN`、规范 `零会话|原子` 均以 grep 实测确认（非目测）。
- 本报告 P1 判定从严（引用列挂定稿行号暗示原文=漏标补白/引据不实一律计 P1），P2 为边界/措辞类；无 P0 级编造或与定稿实质冲突。

---

## 补遗（2026-08-18 12:20 · 轮 13 合并记录）

1. **P0 级新增（父复核发现，本报告原判 P0=0 不成立）**：§1.4 段判定表（L65-68）与 §3.2 保护石消耗表（L138-139）按「目标等级」分段，与定稿 L53-54（按当前等级分段：低段 +0~+3 失败不变 / 高段 +4 起 −1）+ L42 示例（铁剑+5→+6 失败 → 当前−1=+4）实质冲突。边界场景「当前 +3（目标 +4）失败」按原表会被判高段掉级（→+2），按定稿应为低段不变。与 2c3a P0-1（已修）同根同源。
2. **处置（已当场修）**：L65（表头）/L67/L68（段判定两行）/L138/L139（保护石消耗两行）改为「当前等级」口径（V4A patch，git diff 可查）。2c3c 全线同口径修正见其补遗。
3. **并行审查（deleg_975a3112）对本档判 P0=0/P1=0/P2=9/A，不成立**：其「补白决策均有显式标注」结论与实测不符（本文档全文 grep「补白」0 命中，视为系统性补白缺失方为事实）；本报告 P1=7 从严判定成立，评分维持 **B**。
4. **并行审查 P2×9 查重**：其 9 条（跨文档章节号/行号标注瑕疵：2c3a §8 values、L226 引用、任务文件名等）与本报告 P1-2/P1-3/P2-1/P2-3 重叠，无实质性新发现；轮 30 统一修按本报告 7 P1 + 5 P2 清单执行。