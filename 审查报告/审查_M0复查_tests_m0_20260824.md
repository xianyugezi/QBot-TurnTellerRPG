# 审查报告：M0 复查批3-路3（M0 测试体系）

> 批次：M0 复查批3-路3 · 日期：2026-08-24 · 方式：**纯静态代码审查**（本环境无 bash 沙箱，禁止运行任何命令；所有运行行为结论均标注「静态推导」）
>
> 审查文件：
> - `scripts/verify/verify_m0.py`
> - `tests/conftest.py`
> - `tests/unit/test_coredata_regress.py`
> - `tests/contract/test_message_format.py`
> - `tests/contract/test_g0_architecture.py`
> - `tests/fixtures/packs/legal/README.md` + 8 个 json
>
> 对照基准：
> - `细化_5d_测试体系总纲.md`（重点：G0~G7 门禁、TC 具名断言、机制可用性测试、反向用例要求；TC-5d-01~36）
> - `细化_3d_消息模板规范.md`（TC-01~26，TPL-01~14）
>
> 审查维度：① 错误（空洞断言恒真 / or True / 只测字段存在不测机制 / 循环内 pass / docstring 过度声明）
> ② 缺漏（验收点未覆盖 / verify 门禁盲区）
> ③ 幻觉（注释声称覆盖但断言未达 / 冒充已验收）

---

## 一、总体结论

| 级别 | 数量 | 摘要 |
|---|---|---|
| **P0** | 1 | `test_message_format.py:24` 空洞断言恒真（`or True`） |
| **P1** | 4 | verify_m0 门禁盲区 ×2（87 TC 无校验、覆盖率不核算）+ 契约层前置无断言 + 3d 覆盖声明虚高 |
| **P2** | 5 | paginate 只测条数不测切片/越界、e2e 段重复执行与层目录缺失、legal 包 emoji 违禁与 README 漏列、门禁无最小用例数下限 |

分级口径：P0=断言恒真导致审查点完全失效；P1=门禁/覆盖声明与基准不符、静默降级验收口径；P2=覆盖偏弱、文档漂移、可观测性瑕疵。

---

## 二、P0（阻断级）

### P0-1　空洞断言恒真：`assert ... or True` 使「无 at 占位」检查永远通过
- **文件/行号**：`tests/contract/test_message_format.py:24`
- **代码**：
  ```python
  assert "@" not in s.split("\n")[0] or True  # at 依赖壳层构造，不深究字面
  ```
- **问题**：`X or True` 恒真，无论 `s` 首行是否含 `@` 都通过。模块 docstring（L3）声明「断言：渲染结果恒 str、无 [CQ:、无 at/图片占位、无装饰 emoji、5 条页上限」，其中「无 at/图片占位」这一声明**完全依赖此恒真断言**，属"注释声称覆盖但断言未达"（维度③）与"空洞断言恒真"（维度①）双重命中。
- **修复建议**：删除该行占位，改为对壳层依赖做真实断言；若 at 占位确实由壳层构造、M0 不可测，则从 docstring 与断言清单中显式移除「无 at/图片占位」表述（对齐 3f 的"显式声明不落地"做法），禁止用 `or True` 凑数。**运行行为结论「静态推导」：该行不可能失败。**

---

## 三、P1（高优先级）

### P1-1　verify_m0 门禁盲区：87 条 TC 口径只打印不校验，G1「全绿」系冒充
- **文件/行号**：`scripts/verify/verify_m0.py:26-31`（TARGETS 硬编码）、`:85-87`（仅 print）、`:124-127`（门禁判定）
- **问题**：
  - 细化_5d §2.2 骨架第 3 步要求"逐用例执行，按 `细化_XX.md#TC-NN` 逐条跑并回传结果"；TC-5d-08 要求"脚本内声明的 细化文档+TC 区间条数 与目标文档实际 TC 行数相等"。verify_m0 的 `TARGETS` 是**写死的 22/18/26/21**，不扫描文档、不采集 TC、不做条数与覆盖映射，第【1】段对每条直接打印 `✓ {doc}：{n} 条`——**无论实际覆盖与否都打印 ✓**（维度③：冒充已覆盖）。
  - 门禁判定（L124-127）只要求 `total_f == 0 and total_p > 0`，即"pytest 无失败 + 至少 1 个用例通过"。**删除一半测试文件、甚至删到只剩 1 条用例，门禁依然"通过（G1 全绿）"**——与 G1"87 条 TC 全绿"不符（维度②）。
  - 3d 的 26 条实际未达：对照 `细化_3d` 覆盖矩阵，本批可查的断言只覆盖 TC-01~03（test_core.py）、TC-05 部分（P1-2 泄漏）、TC-07/08/11 部分（仅条数）、TC-13 部分（仅 truncated 标志，无「前缀过长已截断」黄提示文本）、TC-18/19 部分（4 个输出样本）；TC-12（页脚逐字 TPL-08）、TC-14（折叠 TPL-09）、TC-15（前缀不计入长度）、TC-16/17（✅/❌ 模板 TPL-10/11）、TC-20~22（错误模板 TPL-12/13/14）、TC-23~26（综合联动）在 tests/ 全树**零断言**（grep 无「指令不正确/条件不满足/资源不足/页脚/折叠」命中）。`panel_render.py` docstring 也自认 TPL-08 页脚与 TPL-12 报错由"壳层"拼装——即 3d 相当部分 TC 与 3f TC-01~20 一样属"依赖后里程碑/壳层"，但 verify_m0 对 3d **未做任何 3f 式的显式 defer 声明**，却计入 87 条通过口径。
- **修复建议**：① 给 TARGETS 增加"已落地/后续覆盖"双态（对齐 F3_LANDED/F3_DEFERRED），把 3d 未达 TC 显式归入 defer 并从 M0 通过门槛中扣除，重算 M0 口径；② 实现 TC-5d-08 式条数核对（扫描各细化文档 `| TC-` 行数与脚本声明数比对）；③ 门禁增加"通过用例数 ≥ 声明落地 TC 数"的下限断言。

### P1-2　verify_m0 覆盖率门禁降级为纯提示，且"细化_5d 允许估算口径"系无据声明
- **文件/行号**：`scripts/verify/verify_m0.py:116-121`
- **问题**：细化_5d §6.1 G1 = "verify_m0.py 全绿（87 条 TC）+ **覆盖率核算 ≥80%**"（L333 / TC-5d-15 / TC-5d-17 均为硬性）；verify_m0 第【5】段只打印"coverage 已装/未装"，**从未实际核算 engine/+content/ 行覆盖**，也未把覆盖率纳入退出码。其注释"以 pytest 全绿 + 冒烟断言作为 M0 门禁简版（**细化_5d 允许的估算口径**）"——全篇检索细化_5d（317 行）**无任何"估算口径"或降级许可条款**，属伪托基准的静默偏离（维度③），且违反 §7.4"禁止静默改语义，偏离必须入决策记录"。
- **修复建议**：删除伪托表述；M0 时点若确无法核算，按 §7.4 在 5d 决策记录 D5 显式登记"M0 覆盖率以简版口径验收，M1 恢复 ≥80% 硬门禁"，并在 verify 输出明确标注"覆盖率未核算（简版）"而非暗示已达标；否则接入 `coverage` 实测并将 <80% 置为失败。

### P1-3　契约层前置 `_validate_fixtures` 无任何断言，恒打印 ✓；且 old_schema 期望与基准矛盾
- **文件/行号**：`scripts/verify/verify_m0.py:56-72`、`:89-91`
- **问题**：
  - 该函数对四包（legal/badref/missing_mod/old_schema）无论 `load_pack` 成功**还是**抛异常，一律打印 `✓`（L65-69 两分支都是 ✓ 前缀），**没有任何 `assert`**；失败只体现在输出文本，门禁判定（L124-127）完全不受其影响。即细化_5d §2.2 第 2 步"范围涉及的内容包全量 validator（L178 阻断式抛错）"被实现成"不崩就行"的冒烟打印（维度① 只测不崩不测机制）。
  - L68 注释将 old_schema 归类为"预期被拦"，与细化_5d §5.1/4.3（L302-303）"**旧 schema 包不拦截加载而是走迁移**（字段级缺补默认/多忽略、降级不抛异常）"直接矛盾——即便 M0 未实现迁移，也不应把"被拦"写成预期语义（维度③）。
- **修复建议**：改为有断言版本——legal 必须 `rep.ok` 且 0 errors；badref/missing_mod 必须抛 `PackLoadError` 且含 R-4/Y-6 相应 kind；old_schema 若 M0 不迁移则显式声明"迁移链 M6 覆盖"而非"预期被拦"；任一不满足即返回失败并计入退出码。

### P1-4　test_message_format docstring 声称 TC-12~19 覆盖，实际仅到 TC-18/19
- **文件/行号**：`tests/contract/test_message_format.py:1-4`（docstring）、`:39-47`（分页）、`:27-35`（emoji）
- **问题**：docstring"细化_3d#TC-12~19"与 L38 注释"TC-07/08/11：5 条/页分页 + 页脚语义"均**过度声明**（维度③）：本文件未断言 TC-12 页脚逐字（TPL-08 原文）、未断言 TC-13（截断黄提示）、未断言 TC-14（折叠行 TPL-09）、未断言 TC-15（前缀不计入长度）、未断言 TC-16/17（✅/❌ 结果行模板）；`test_paginate_five_per_page` 只校验 `len()`，`test_panel_no_banned_emoji` 仅扫 4 个输出样本，够不上 TC-18"渲染管线输出样本全量扫描 + 定稿示例 🔥/🟢 不出现"。
- **修复建议**：docstring 改为逐条列明真实断言覆盖（如"TC-07/08/11 条数语义、TC-18/19 emoji 扫描（M0 样本集）"）；未达 TC 参照 P1-1 归入 defer 清单。

---

## 四、P2（中优先级）

### P2-1　paginate 只测条数不测切片内容，越界页机制存在但零用例
- **文件/行号**：`tests/contract/test_message_format.py:39-47`
- **问题**：`test_paginate_five_per_page` 仅断言 `len(page1)==5 / len(page2)==5 / len(page3)==2`，**未断言第 2 页含 items[6:11]、第 3 页含 items[11:12]**——一个恒返回首 5 条的错误实现同样通过（维度① 只测字段存在不测机制）。同时 `panel_render.py:101-104` 的越界页 `IndexError`（TC-09/10 语义：非法页码经壳层转 TPL-12）**无任何用例**（维度② 机制存在但未测）。
- **修复建议**：断言各页内容切片（`page2 == items[5:10]` 等）；补 `page=0 / page=4（越界）` 触发 `IndexError` 的反向用例。

### P2-2　"e2e" 段重复执行 contract 目录内文件；tests/e2e/、tests/integration/ 目录缺失
- **文件/行号**：`scripts/verify/verify_m0.py:37-41`（SEGMENT_CMDS）、`:100-101`（执行循环）
- **问题**：`SEGMENT_CMDS["contract"] = ["tests/contract"]` 会收集 `test_e2e_smoke.py`、`test_3f_patch.py`；`SEGMENT_CMDS["e2e"]` 又显式重复这两文件——全量跑时**同一文件被执行两遍**（耗时翻倍、潜在 flaky 双计数）。且 `tests/e2e/` 与 `tests/integration/` 目录**不存在**（glob 无结果），细化_5d §1.1/TC-5d-01"四个目录齐全"未落实，L4 冒烟被塞进 contract 目录冒充（维度②）。
- **修复建议**：contract 段改为显式排除 e2e 两文件，或 e2e 段移除（与 contract 合并为单段）；后续里程碑补齐 tests/e2e/、tests/integration/ 目录并在 verify 中做目录存在性断言。

### P2-3　legal 合法包 marks.json 含禁用装饰 emoji，README 漏列第二条 mark
- **文件/行号**：`tests/fixtures/packs/legal/marks.json:5`（`"icon": "🔥"`）、`:17`（`"icon": "🩸"`）；`tests/fixtures/packs/legal/README.md:10`
- **问题**：`🔥` 在细化_3d §4.2 禁用清单（D-01/TC-18）中**明确禁用**（"含正文，全局"）。legal 包自称"validator 全绿基线/无破坏点"（README L5-6），但 marks.icon 若经渲染进入消息即违反 TC-18；即便 M0 不渲染 mark 图标，把违禁 emoji 放进"合法基线"也埋下漂移隐患。另 README L10 仅列 `fire_mark`，漏列实际存在的 `curse_mark`（文档漂移）。
- **修复建议**：① 若 mark 图标会进消息渲染，改用纯文本/合法符号，或在 3d 禁用清单显式豁免 marks.icon 数据字段；② README 补齐 curse_mark 条目。

### P2-4　门禁无最小用例数下限，防不住"用例被删仍通过"
- **文件/行号**：`scripts/verify/verify_m0.py:124-127`
- **问题**：`total_p > 0` 是唯一的用例数约束，配合 P1-1 的 TC 口径不校验，删测试可导致 M0 在断言面缩水后仍"G1 全绿"。
- **修复建议**：与 P1-1 修复合并——门禁校验"通过用例数 ≥ 声明落地 TC 数"，并对 unit/contract/e2e 各段设最低用例数基线。

### P2-5　test_coredata_regress 弱断言残留（Y-1 只验 kind 不验 rule）
- **文件/行号**：`tests/unit/test_coredata_regress.py:104-110`
- **问题**：`assert any(w.kind in ("Y-1",) ...)` 只验提示类别，未验 `w.rule`/定位路径是否确指 stats.hp.base——若 Y-1 由别处误报，该用例仍绿。属轻微强度不足（不构成 P1，机制本身已正确触发）。
- **修复建议**：补 `w.rule == "out_of_common_range"` 或 `w.path` 含 `stats.hp.base` 的断言。

---

## 五、无问题维度确认

- **循环内 `pass` / 空函数体**：全范围（verify_m0、conftest、三测试文件）**未发现**任何 `pass`、空 except、裸 `continue`——该维度确认无问题（「静态推导」基于全文逐行阅读）。
- **test_g0_architecture.py**：docstring（TC-01~03 + TC-5d-33）与断言**逐条对应**（R1/R2/R3 零 nonebot/依赖方向 + subprocess ARCH-OK + finder 对 ImportFrom 形态的反向用例），机制真实可达，无 or True、无过度声明——该文件维度①③确认无问题。
- **test_coredata_regress.py**：P1-1~P1-4 四组修复各有真实断言（环 R-5、自环 R-5、未知键 R-4、占位符泄漏、截断标志、负数黄提示、calc 兜底），docstring 声明与断言**匹配**（除 P2-5 弱断言外）——该文件维度①③确认基本无问题。
- **conftest.py**：无 `from nonebot` 顶层 import（对齐 L52），`make_player` 全字段构造与 4a#TC-12/3d 渲染基准声明一致，REQUIRED_PACKS 被 test_e2e_smoke 消费（存在性校验）——确认无问题。
- **legal 包 json 数据引用链**：manifest 8 模块 ↔ 磁盘 8 json 一一对应；effects↔statuses（rage_up→berserk、regen→heal_small）、items→heal_small/power_slash、equipment excludes 单向、stats 九键、formula 合法——README 声明与数据一致（除 P2-3 漏列项）。

---

## 六、TC 覆盖核对表（3d TC-01~26，M0 时点静态推导）

| TC | 断言落点 | 状态 |
|---|---|---|
| TC-01~03 前缀三态 | test_core.py（段内） | 已覆盖 |
| TC-04 空称号仅隐本体 | 无（prefix_render 三态仅在默认态断言） | 未达 |
| TC-05 作者格式/占位符组合 | test_coredata_regress P1-2（防泄漏） | 部分 |
| TC-06 未知占位符原样输出+黄提示 | 无 | 未达 |
| TC-07/08/11 分页/翻页/无页脚 | test_message_format（仅条数） | 部分 |
| TC-09/10 非法页码→TPL-12 | 无（IndexError 机制未测） | 未达 |
| TC-12 页脚逐字 TPL-08 | 无（footer 由壳层拼装，M0 无产出） | 未达（defer） |
| TC-13 前缀截断+黄提示 | test_coredata_regress P1-3（仅 truncated 标志） | 部分 |
| TC-14 折叠 TPL-09 | 无 | 未达（defer） |
| TC-15 前缀不计入长度 | 无 | 未达 |
| TC-16/17 ✅/❌ 模板 | 无（仅禁 emoji 反向扫描） | 未达 |
| TC-18 emoji 全量扫描 | test_message_format（4 样本） | 部分 |
| TC-19 前缀区无 emoji | test_message_format | 已覆盖 |
| TC-20~22 错误模板 TPL-12/13/14 | 无（M0 无错误模板模块） | 未达（defer） |
| TC-23~26 综合联动 | 无 | 未达（defer） |

---

## 七、修复优先级建议

1. **先清 P0**：删除 `or True` 恒真断言，改真实断言或显式摘除「无 at/图片占位」声明。
2. **再堵门禁**：verify_m0 补 TC 条数核对 + 覆盖率核算（或按 §7.4 登记显式降级）+ `_validate_fixtures` 加真实断言 + 3d/3f 统一 defer 声明并重算 87 口径。
3. **最后补测**：paginate 切片/越界反向用例、e2e 段去重、legal 包 emoji 与 README 修正。
