# 编辑器修改意见 0915 · 台账与分批方案

> 来源：用户 2026-09-15 交来的 13 条修改意见（`编辑器修改意见0915.md`）。
> 本文件 = **逐条落地口径 + 分批**；每条完成后在此标 ✅ 并写实现锚点（沿用既有「台账逐条销项」机制）。
> 需求侧总纲仍是 `docs/编辑器重写_需求与约束.md`（第〇节「通用工具/元数据驱动」优先，框架内不得出现内容包业务名）。

## 〇、主 agent 的现状核查（先坐实事实，再谈改法）

| 事实（实测） | 依据 |
|---|---|
| `settings` 现有 13 个段：`battle(21)` / `default_map` / `level_cap` / `exp_curve(35)` / `currencies(2)` / `death_penalty(4)` / `slot_defs(8)` / `command_aliases` / `world_name` / `alchemy` / `quest_board` / `assistant` / `contest` / `forge` | `content/veinborn/settings.json` |
| `exp_curve` 形态 = `{"1": 100, "2": 480, …}`（等级→所需经验，35 项） | 同上 |
| `death_penalty` = `{weak_duration_sec, drop_currency:[], drop_exp:{enabled,percent}, drop_items:{enabled,count}}` ← **嵌套两层** | 同上 |
| `slot_defs` = `{weapon:{name,max}, head:…, body:…}`（8 部位） | 同上 |
| `action.json` 条目含 **`probability` / `weight`**（固定概率写死在行动定义里） | `content/veinborn/action.json` 首条键 |
| `marks.json` 条目：`appliable_to/desc/duration/icon/id/max_stack/name/polarity/type`（**无概率键**） | `content/veinborn/marks.json` |

## 〇·补、一号原则（用户 2026-09-15 补充，**所有批次的验收口径**）
> 用户原话：「编辑器是**框架的**编辑器，不是数据包的专属编辑器，比如蚀脉猎手可能用不到的功能，但**框架本身实现的功能是需要显示出来的**。」

**口径（写死，后续每条意见都按它验收）**：
1. **显示全集 = 框架能力集**：编辑器展示的模块/字段/分组/机制，以**框架元数据**（`qbot_rpg/content/field_meta.py` 的类型与字段登记、`qbot_rpg/content/module_catalog.py` 的模块目录）为**全集来源**；
   **即使当前内容包没用到该功能（数据里没有该键、该模块没启用），只要框架实现了，也要在编辑器里显示出来**（空值/未启用状态 + 可填写），让作者知道"框架支持这个"。
2. **包声明只做"展示层补充"，不做"能力删减"**：包内 `field_meta.json` / `manifest.json` 的声明用于**中文名、说明、分组显示名、模块层级、默认值、包自有新增字段**；
   **不得**因为包没声明而让框架已有能力在编辑器里"消失"。
3. **空白包同样成立**：空白包打开编辑器时，左栏应按框架模块目录给出**可启用清单**（⚙ 面板已有），字段区应能显示框架登记的字段（哪怕数据为空）。
4. **例外通道（将来可选，非本批）**：若确有包想隐藏某框架能力，须走**显式声明**（如 `hidden: true`），默认为"显示"——不得默认隐藏。

**落地要求（并入既有批次）**：
- **批 2（#4 该能编辑的不能编辑）** 按本原则做：**以框架元数据遍历渲染字段**（含嵌套对象子字段、包未使用的框架字段），而非"只渲染数据里出现的键"；
- **批 3（#8/#9 页面化/表格化）**：页面与表格同样以"能力全集"为准组织，不因包未用而省略；
- 新增验收断言：**同一框架元数据下，用一个"几乎空白"的包打开编辑器，模块/字段显示集合应与功能齐全的包一致（差异仅体现在"有没有值/是否启用"）**。

## 一、逐条口径与批次

### 批 1 · 结构声明类（便宜、零风险，先做）
| # | 意见 | 落点与做法 |
|---|---|---|
| 1 | 设置模块改名「基础」，属性/公式**条目**并入（**非子模块层级**；用户 2026-09-15 澄清「放到设置里面去，和战斗参数的条目一起」） | **包声明**：`content/veinborn/field_meta.json` 的 `module_labels.settings` → 「基础」+ 新增展示层聚合键 `entry_merge: {settings: [stats, formula]}`；**框架通用实现**（`web/api.py::_entry_merge_map` 按声明聚合，不写死模块名）。`stats.json` / `formula.json` **文件、模块 id、加载与校验路径全不动**——中栏按来源分小节，编辑/保存路由回各自模块。契约见《需求与约束》§〇·补六 |
| 12 | 生活系统并入 | **包内无独立「生活」模块且不得凭空造模块** → 挂到包内既有父模块「职业」`jobs` 下：`proficiency / enhance / forge / recipe`（`module_tree`，按包现有模块实际列出，不写死）；采集/挖掘当前无对应模块 → 如实写明（存疑清单） |
| 3 | 固定选项（如死亡惩罚的货币）改下拉 | `death_penalty.drop_currency[].currency` 声明**展示层引用** `options_ref="settings.currencies"`（框架通用维度：字段 type/校验口径不变，只换控件）→ 编辑器自动出下拉，候选来自 `settings.currencies` 的货币 id |
| 13 | 字号三档（小/中/大，现为小号） | 新增**字号令牌档位** `html[data-fs="small|medium|large"]` 只覆盖 `--fs-*`，🎨 面板内「字号：小/中/大」单选，`localStorage`（`qbot.editor.font`）记忆；**只改字号令牌，不改间距/布局结构**；与三套主题正交 |

### 批 2 · 可编辑性 + 校验人话（用户最痛的两条）
| # | 意见 | 落点与做法 |
|---|---|---|
| 4 | 该能编辑的不能编辑（例：死亡掉落的经验/物品） | **已落地（批14）**：根因 = `_EDIT_BY_WIDGET["obj"]=("readonly",False)` 把对象型子字段整体只读 → 改 `objform` 可编辑 + 按框架元数据**递归**渲染子字段（含登记未配置键 + 未登记键兜底控件），动态键空间可增删；`drop_exp{enabled,percent}`/`drop_items{enabled,count}`/`slot_defs.{部位}{name,max}` 全可编。见 §六 |
| 5 | 校验提示小白看不懂 | **已落地（批14）**：每条错误三段式「哪里（模块/条目/字段中文名）+ 为什么（依据哪条要求、期望什么）+ 怎么办（具体动作）」；规则码/原始消息/位置收进折叠排查信息；**展示层改造、判定不变**；覆盖 12+ 常见规则。见 §六 |
| 6 | 装备槽位无法编辑；装备页改了不同步新部位 | **已落地（批14）**：①`settings.slot_defs` 增删改（对象子字段可编 + 动态键空间）；②保存后引用候选缓存失效 → 物品/装备「部位」下拉即时含新部位（同会话，无需刷新）；③删部位 → 引用者黄提示（沿用既有 refs 机制，不硬拦）。见 §六 |

### 批 3 · 编辑体验（页面化 + 表格化）
| # | 意见 | 落点与做法 |
|---|---|---|
| 8 | 增加更多页面，减少滚动 | 右栏由「单页长滚动」→ **分区页签已具备**，再补：**二级子页签/折叠分组**（大段如 battle 再分组），并把「详情/列表」按需展开；目标是**首屏不看滚动条**（验收：常见模块一屏看完主要字段） |
| 9 | 属性配置用表格（密集可读） | 属性类**标量集合**（如 `stats` 的 key→value）→ 通用「键值表格」渲染（已有键值编辑器的底子）；密集排布、行内编辑 |
| 2 | 默认地图/等级上限/经验曲线合并一栏；曲线默认显示公式；**双击**看/改各等级需求；顶部加**启用/禁用公式** | 新增**曲线控件**（通用）：默认渲染为公式/摘要行；双击 → 展开明细表（等级→经验，可编辑）；顶部开关 `exp_curve.use_formula`（**缺省 true 保持现状**，关掉则用明细表的值）；公式与明细**单一来源**（避免双写打架） |

### 批 4 · 数据模型/引擎（需先定口径）
| # | 意见 | 落点与做法 | 待确认 |
|---|---|---|---|
| 10 | 印记/行动不该有固定概率，应由**使用方**（怪物/装备/技能/套装）按引用处配置 | ①**向后兼容**：定义处的 `probability`/`weight` 保留为**默认值**；使用处的声明可覆盖（`{ref, probability}`）②标记为「默认值（可被引用处覆盖）」+ 编辑器提示 ③引擎取值顺序：**使用处 > 定义处** ④内容侧：给出「如何在使用处覆写」的写法（怪物行动表/装备效果表已有引用字段） | 是否允许"使用处不写 → 用定义处默认"（建议允许）；是否要把定义处概率**藏起来**（建议保留但标注） |
| 11 | 物品新建时可选**模板**（如材料类只需名字+介绍，其他是干扰项） | 通用机制：**条目预设（preset）**——元数据声明「预设名 + 关注字段集 + 默认值」；新建条目时先选预设 → 编辑页**只显示该预设关心的字段**（其余折叠进「其他字段」）；任何模块都能配预设（不只物品） | 预设放**包声明**还是框架？——批16 = 包声明机制；**批17 定案（用户拍板）**：材料/货币袋/药剂/礼包/装备/技能书六种**通用品类**做成**框架默认**（任何包含空白包都有），包可覆盖/追加/关闭 |
| 7 | 英文配置 → 用户填中文，代码层自动转英文/拼音 | **两层**：①**展示层**已完成（字段中文名已下放包内）②**取值层**：新建条目时用户填中文名 → 框架按**拼音/英文**自动生成 `id`（**透明展示生成的 id、可手改**；查重与既有 ID 规则一致）③已存在的英文键不动（避免破坏既有内容） | 生成规则：拼音全拼（`龙鳞甲`→`long_lin_jia`）还是拼音首字母+序号？建议**全拼+下划线**，重名加序号 |

## 二、建议顺序与理由

**批 1 → 批 2 → 批 3 → 批 4**：
- 批 1 全是声明/令牌层改动，**零引擎风险**且立刻见效（结构好找、字号舒服）；
- 批 2 解决"点了不能改/看不懂报错"这两件最影响信任的事；
- 批 3 是观感与效率（页面化、表格化、曲线控件）；
- 批 4 动数据模型与引擎（概率来源、预设、中文 id），**必须先定口径再做**，且需回归既有内容包等价性。

## 三、进度台账（逐条销项）

| # | 意见 | 状态 |
|---|---|---|
| 1 | 设置→基础 + 属性/公式**条目**并入（**非子模块层级**；用户 2026-09-15 澄清） | ✅ 批12（包声明 `field_meta.json.entry_merge` + 读取层 `web/api.py::_entry_merge_map` 通用聚合 + 中栏按来源分小节；编辑/保存路由回原模块；计数口径见《需求与约束》§〇·补六） |
| 2 | 默认地图/等级上限/经验曲线合并 + 曲线控件（双击/公式开关） | 待做（批 3） |
| 3 | 固定选项改下拉（死亡惩罚货币） | ✅ 批12（`field_meta.py` 的 `drop_currency[].currency` 声明展示层引用 `options_ref="settings.currencies"` → 编辑器出下拉；候选 = `settings.currencies` 的 id；字段 type 仍 `str`，F-02 校验语义不变） |
| 4 | 该能编辑的不能编辑（嵌套子字段） | ✅ 批14（`_EDIT_BY_WIDGET["obj"]` = `("objform", True)`；`_descriptor` 按框架元数据**递归**出子字段控件（bool/number/text/enum/ref），登记未配置也出（present=False），未登记键走兜底控件 + 标注；动态键空间可增删子项；既有权校验/白名单不放宽。见 §六） |
| 5 | 校验提示人话化 | ✅ 批14（`atomic_store.RULE_GUIDE` + `_humanize` 出 `why`/`how`/`rule`/`raw`；`editor_ops._decorate` 出 `where`/`field_path`；前端 `vrow` 三段式 + 折叠排查信息；**判定不变**（条数/规则码逐条一致）。见 §六） |
| 6 | 装备槽位可编辑 + 联动同步 | ✅ 批14（`_entry_open_keys` + `open_keys` 写路径 → 部位表增删改；`_build_name_index` 收纳 obj 键空间 + `items/equipment.slot` 声明 `options_ref="settings.slot_defs"`；保存/回退后 `invalidateRefCache` → 候选即时联动；删部位 `ref_holders` 列引用者黄提示不硬拦。见 §六） |
| 7 | 中文录入 → 自动英文/拼音 id | ✅ 批16（默认「前缀+序号」零填充；前缀来源=包声明/模块推导；拼音为可选附加，多音字/未装库回落并提示；生成透明可手改；只影响新建。见 §八） |
| 8 | 增加页面减少滚动 | ✅ 批15（二级折叠块；见 §七） |
| 9 | 属性用表格 | ✅ 批15（见 §七） |
| 10 | 印记/行动概率移到使用处 | ✅ 批16（使用处 > 定义处 > 缺省；定义处保留为默认值；引擎接入 `monster_ai._param`；印记无概率键→无需改动。见 §八） |
| 11 | 物品新建按模板 | ✅ 批16→**批17**（批16 = 包声明 `entry_presets` 机制；批17 = 六种**框架通用默认**（材料/货币袋/药剂/礼包/装备/技能书）+ 合并口径（包覆盖/追加/关闭）+ 空白包也有。见 §八、§九） |
| 12 | 生活系统并入 | ✅ 批12→**批13 定案**（用户要「真正的『生活』归口」→ 用 `entry_merge` **虚拟聚合视图** `life`「生活」聚合 `proficiency/enhance/forge/recipe`；批12 挂 `jobs` 下的临时做法已取消；settings 型生活系统（种植/委托板/品评会/代工）仍在「基础」卡片，见批13 D 与 §四 U5 口径） |
| 13 | 字号三档 | ✅ 批12（`EditorFont` + `html[data-fs="small|medium|large"]` 只覆盖 `--fs-*` 令牌；🎨 面板内单选 + `localStorage`（`qbot.editor.font`）记忆 + 即时生效不丢草稿 + 与三主题正交） |

## 四、批13 · 能力可见性（一号原则落地 · 2026-09-15）

| 块 | 交付 | 实现锚点 |
|---|---|---|
| A | 左栏 = 框架能力集 ∪ 包声明（审计 ①-5） | `qbot_rpg/web/api.py::list_modules` 新增 `views`（虚拟聚合视图）/ `available`（未启用框架能力；`enabled=false` + `count` 如实 + `implemented`）+ `entry_index.available`（检索/ID 唯一性覆盖，不改 `total`）；前端 `renderModules` 灰显 + 一键启用（复用 `/module/{m}/toggle`）；`modules`/`module_count`/条目索引三处口径不变 |
| B | 补框架模块目录条目（审计 ①-6） | `qbot_rpg/content/module_catalog.py`：新增 `farming/contest/assistant/quest_board/codex/templates/gathering`；`ModuleCatalogEntry` 补 `implemented`/`settings_section`；`gathering` 标「未实现」且不可启用；`time_cycle` 依 U2 **不**新增一级模块 |
| C | 补字段登记（审计 ①-1/①-4） | `qbot_rpg/content/field_meta.py`：`time_cycle/message_prefix/pvp/codex/event_log_cap/event_log_capacity/command_mode/require_at/at_text/attr_types/conditional_rules/imprints/shortcut_max/default_job_id/max_dialog_depth/resource_pct` + `templates` ModuleMeta（map）+ `FORMULA_FIELDS` 公式段 + `maps.gather_points/weather_pool/monsters` 时间天气键；**只加展示层与类型登记，校验语义不变** |
| D | 「生活」聚合视图（用户 #12 定案 + U5） | `content/veinborn/field_meta.json`：`entry_merge.life=[proficiency,enhance,forge,recipe]` + `module_labels.life="生活"`；移除批12 `jobs ▸ proficiency/enhance/forge/recipe` 层级；`_entry_merge_map` 允许未声明的**虚拟视图目标**（`list_entries` 同步放行） |

**U1–U7 已定口径（用户「没意见，继续」）**：

- **U1 采集/挖掘**：数据落点在地图 `maps.gather_points`（已补 `name/weather_mods` 登记）+ 目录条目 `gathering`（标「未实现」）；引擎未实现部分进「待排期」清单，不在本批实现引擎。
- **U2 时间天气**：以定稿为准——挂「通用设置 → 【时间天气】卡片」（`settings.time_cycle` 字段登记于 C 块），**不**新增一级模块；依据 = 时间天气定稿 L296。
- **U3 `覆盖审计_F` 缺失-1**：基线过期项，**未**在本批处理（不属能力可见性范围），保留在审计 §五 待确认。
- **U5 `profession.json` 分层**：用 D 块的「生活」聚合视图收敛（`life` 视图）；**不改策划书**。
- **U6 消息前缀 vs 模板**：**不合并**（前缀管首行身份标识、templates 管文案），边界见《需求与约束》§〇·补七。
- **U7 怪物触发枚举双源**：以**校验器现行枚举为准**（实现即事实），编辑器下拉按现行；另一套命名登记为「待收敛项」，**不擅自改校验**。
- **U4/U8/U9/U10**：未在本批裁决范围内，保留在审计 §五 待确认（U4 伤害统计收集器本批仅登记 `formula.stats_collector` 配置字段，未实现引擎）。

## 五、批13.1 · 段入口（补批13 缺口 · 2026-09-15）

批13 只做了一半：字段登记（C 块）做了，但**段入口**没做——`GET /api/pack/{pack}/module/settings/entries`
仍只列包数据已有的键，`time_cycle/message_prefix/pvp/codex/shortcut_max` 等框架已登记、代码已消费的段
在两个包里都看不到 → 违背一号原则（框架实现了就要显示）。

| 节 | 交付 | 实现锚点 |
|---|---|---|
| 1 | 对象型模块条目 = 包数据键 ∪ 框架登记段；未配置段以「未配置」态可见/可填/可存 | `qbot_rpg/web/api.py`：`_UNCONFIGURED` 哨兵 + `_entry_rows`（补齐 object 模块未配置段，条目列表/全局索引/左栏计数/详情/写链路单点口径）+ `_entry_brief`（`unconfigured` 标记）+ `_entry_base`/`_build_fields`（哨兵 → 空对象表单）+ `entry_slot`（哨兵 → 可写 subject）+ `list_entries`/`entry_index`/`list_modules` 计数（`configured_count`/`unconfigured_count`） |
| 2 | 通用性（不写死模块名/段名）；map/list 模块与无登记模块行为不变 | 机制只看 `mmeta.entry_type == "object"` 与 `mmeta.fields`；`templates`/`formula` 等 map 模块的登记不是条目键全集 → 不套用 |
| 3 | 一号原则自检升级：段入口可见性 + 跨包段集合差集 | `scripts/editor_visibility_selfcheck.py`：逐包断言「对象模块条目 ⊇ 框架登记段」+ `_segment_diff` 跨包差集（demo_blank vs veinborn：段集合差 无，仅 2 vs 14 已配置） |
| 4 | 前端「未配置 · 框架支持」标记 + 计数口径 + 页脚批次串 | `qbot_rpg/web/static/index.html`：`renderEntries` 未配置标记 + `loadEntries` 计数 note + `renderDetail` 未配置横幅；页脚 `批13.1 · 段入口` |
| 5 | 回归测试 | `tests/unit/test_editor_batch131_segments.py`（25 用例，含真写盘 + 回退逐字节复原） |

## 六、批14 · 可编辑性与提示（台账批 2 三条 · 2026-09-15）

一号原则（§〇·补）贯穿：**字段显示与控件以框架元数据为准**（框架支持就要显示、能编辑），
包声明只做展示层补充。批13.1 已把「段入口」补齐，本批把「段内子字段」补齐。

### #4 对象子字段可编辑（审计 ①-3 第 19/20/21 条根因）

| 节 | 交付 | 实现锚点 |
|---|---|---|
| 1 | `obj` → `objform` 可编辑控件；按框架元数据**递归**渲染子字段（bool/number/text/enum/ref），嵌套任意层 | `qbot_rpg/web/api.py`：`_EDIT_BY_WIDGET["obj"]=("objform",True)`、`_NESTED_CONTROLS`、`_descriptor`（obj 分支**始终**出 children） |
| 2 | **按元数据渲染、而非只渲染数据已存在的键**：登记未配置 → present=False 可填 | `_descriptor` obj 分支用空对象建子字段；`_build_fields` 登记项 ∪ 数据多出项 |
| 3 | 未登记类型映射 → 按实际值推断为对象（子字段可编）；未登记子字段 → 兜底控件 + 标注「元数据未登记，按实际值推断」 | `_effective_widget`（未登记映射 → `obj`）、`_descriptor.meta_unregistered/meta_note`、`META_UNREGISTERED_NOTE` |
| 4 | 动态键空间（元数据未登记子字段）可新增/删除子项；登记子字段 = 固定 schema 不增删键 | `api.open_keys_of`、`_entry_open_keys`、`entry_detail/entry_slot.open_keys`、`deletable`；写路径 `editor_ops._apply_patch(open_keys=…)`（只放行提交，合法性仍由校验器判定） |
| 5 | 前端递归对象编辑器 + 嵌套路径写回整块对象草稿 + 增删子项（只改草稿） | `index.html`：`objFormEdit/objChildRow/objLeafInput/objPathGet/objPathSet/objPathDel`、`bindControls` 的 `[data-opath]`、`bindObjSections`、`bindOpenKeySections` |
| 6 | 回归测试（嵌套两层可编 / 写盘+回退 / 兜底标注 / 未知键负向断言 / 跨包字段集合一致） | `tests/unit/test_editor_batch14_objfields.py`（14 用例） |

### #5 校验提示人话化（展示层改造 · 判定不变）

| 节 | 交付 | 实现锚点 |
|---|---|---|
| 1 | 规则库：规则码 + 结构化参数 → 「为什么 / 怎么办」人话（类型/枚举/必填/引用/重复ID/范围/未知键/区间倒置/数字非法/概率极端/公式安全…） | `qbot_rpg/content/atomic_store.py`：`RULE_GUIDE`、`_why_how`、`_humanize`（新增 `why`/`how`/`rule`/`raw`，`message`/`code`/`field` 原样保留） |
| 2 | 「哪里」= 模块/条目/字段中文名（路径只在折叠详情）；路径归一兼容 object 模块 `模块.<下标>.<段>.<子字段>` | `qbot_rpg/web/editor_ops.py`：`_decorate`（`where`/`field_path`）、`_field_key`（带 slot 归一；中文名取包声明标签） |
| 3 | 前端三段式 + 折叠排查信息（规则码 / 原始消息 / 位置） | `index.html`：`vrow` 重写 + `.vseg/.vk/.vraw` 样式 |
| 4 | 测试：三段式结构、≥5 条修前/修后对照、**判定不变**（条数+规则码逐条一致）、点名类别覆盖、用词口径、前端契约 | `tests/unit/test_editor_batch14_humanize.py`（12 用例） |

### #6 装备槽位可编辑 + 联动

| 节 | 交付 | 实现锚点 |
|---|---|---|
| 1 | 部位表（`settings.slot_defs`）新增/删除/改部位 | 依赖 #4 的 `objform` + `open_keys` 写路径；条目级增删控件 |
| 2 | 引用候选**即时**联动：部位表变更 → 物品/装备「部位」下拉含新部位 | `_build_name_index` 收纳 `obj` 字段键空间；`field_meta.py` 的 `items/equipment.slot` 声明 `options_ref="settings.slot_defs"`（type 仍 `str`）；`index.html.invalidateRefCache`（保存/回退成功后清候选缓存） |
| 3 | 删部位 → 引用者黄提示（不硬拦，沿用既有 refs 机制） | `api.ref_holders` + `_collect_target_hits`；`editor_ops._orphan_key_warnings`（附 `how_to_fix`） |
| 4 | 一致性：条目详情字段数 ↔ 引用候选 ↔ 命名空间索引同源 | `tests/unit/test_editor_batch14_slots.py`（13 用例，含写盘+回退、候选联动、引用者提示、三处口径） |

**批14 门禁**：全量 `pytest tests/ -q -o addopts=""` 0 failed；`ruff check`（改动文件）干净；
换包验收 `scripts/editor_verify_packs.py` 全 PASS；迁移对拍门禁重定基线到本批末提交
（对象控件由 `readonly` → `objform`、`slot` 由文本 → 展示层下拉，属**展示层**合法演进，
非字段级元数据迁移差异——与此前批 12/13.1 重定基线同理）。

## 七、批15 · 页面化与曲线（2026-09-15）

状态：**✅ 全部销项**（#2 / #8 / #9 + 回归修复 + 门禁语义化 + 测试/文档/页脚）。
本批**未重定迁移基线**（仍 `f7d9c25`）——门禁语义化后，派生展示键（`control` / `widget` /
`block_layout` / `number_step`）的呈现演进不再冒充迁移差异，无需再为「表格化 / 曲线化」重定基线。

| # | 用户意见 | 状态 | 实现锚点 |
|---|---|---|---|
| #8 | 分组页签下二级折叠块 + 隐式「更多字段」（长模块首屏不铺开；字段不因折叠消失） | ✅ | `qbot_rpg/web/api.py`：`_resolve_subgroup` / `_block_plan`（`AUTO_MORE_AFTER` 切主块 + 隐式 `AUTO_MORE_BLOCK`；`subgroup_order` / 包 `subgroup_labels` 覆盖）+ `entry_detail.blocks`；前端 `index.html`：`groupPaneHtml` 的 `.subblk` 折叠块 + `EditorBlocks` 纯逻辑 + `renderDetail` 接线；测试 `tests/unit/test_editor_batch15_blocks.py`（含真 Chromium 滚动量测） |
| #9 | 密集「键 → 标量/小结构」映射表格化（kvtable）：紧凑行高、行内编辑、增删行；map 模块「全表」入口 | ✅ | `qbot_rpg/web/api.py`：`_is_dense_map` / `_is_small_struct` / `_kv_table_spec` / `_kv_columns`、`_EDIT_BY_WIDGET["map"]=("kvtable",True)`、`TABLE_ENTRY_ID` 合成全表条目、`entry_slot.whole`；写路径 `editor_ops._plan`（整值键写回）；前端 `index.html`：`kvTableEdit` / `EditorKv` / `bindKvSections`；测试 `tests/unit/test_editor_batch15_kvtable.py`（含真写盘 + 回退、DOM 密集度量测） |
| #2 | 默认地图 / 等级上限 / 经验曲线**合并为一栏**；曲线**默认显示经验公式**，**双击**展开/编辑各等级经验；页面**顶部**「启用/禁用公式」开关 | ✅ | 合并页：`field_meta_pack.parse_field_meta` 的 `segment_pages` 形态校验 + `api._segment_pages` / `_find_page`（`@page:<id>` 合成条目、`list_entries.pages`、`entry_detail` / `entry_slot` 页面分支；**纯展示层**，数据/段 id/校验路径不动）；曲线：`api._is_curve_value`（形状驱动、不写死 `exp_curve`）/ `_curve_spec`（`use_formula` 缺省 true、`detail_editable`、`summary`、单一来源 note）/ `_effective_widget` 纠偏、`CURVE_FLAG_KEY` / `CURVE_FORMULA_KEY`；前端 `index.html`：`curveEdit` / `curveBodyHtml` / `EditorCurve` / `bindCurveSections`（双击展开、开关、行内编辑、增删行）；测试 `tests/unit/test_editor_batch15_curve_page.py` |
| — | **回归修复**：表格化 / 曲线化只改呈现，不删结构（`children` / `open_keys` / `rows`） | ✅ | `api._descriptor`：对象性判定与呈现控件解耦（`obj_like`）；children / open_keys 独立输出，表格 / 曲线规格并列附加；map 字段旧有 `rows` / `row_count` 照常输出。对拍 0 删除 / 0 值变化（提交 `7121ca0`） |
| — | **迁移门禁语义化**：删除 / 值变化红；新增显式计数；`label` / `help` / `group` / `module_labels` / `module_tree` 严格断言 | ✅ | `scripts/compare_field_meta_migration.py`：`_STRICT_KEYS` / `_DERIVED_DISPLAY_KEYS` / `_diff(strict=…)` / `compare_snapshots`（口径 ①~④，docstring 定稿）；自证测试见 `tests/unit/test_editor_pack_field_meta_migration.py`（提交 `1999f88`） |
| — | 收尾：测试 / 文档 / 页脚批次串 | ✅ | 页脚 `批15 · 页面化与曲线`（`index.html`）+ 全部断言同步；本节台账 |

**批15 门禁**：全量 `pytest tests/ -q -o addopts=""` 0 failed；`ruff check`（改动文件）干净；
换包验收 `scripts/editor_verify_packs.py` 全 PASS；迁移对拍 `删除/修改 = 0`（新增项显式计数）。




## 八、批16 · 中文ID / 概率覆盖 / 预设（2026-09-15）

状态：**✅ 全部销项**（#7 / #10 / #11）。本批**未重定迁移基线**（仍 `f7d9c25`）——
新增的是**扩展声明键**与**说明卡派生键**（`default_note` / `overridable_default`），
属「新增」而非「删除/值变化」，对拍门禁按既有语义（新增显式计数、删除/值变化=0）通过。

### #7 自动生成 ID（默认「前缀 + 序号」，拼音可选附加）

| 节 | 交付 | 实现锚点 |
|---|---|---|
| 1 | 默认 `<前缀>_<序号>`：零填充 3 位（可配 1..12）、取同前缀最大序号 +1、冲突继续递增；序号识别含非零填充写法 | `qbot_rpg/web/api.py`：`ID_WIDTH_DEFAULT` / `_sequential_id` / `suggest_entry_id` |
| 2 | 前缀来源（包声明优先）：预设 `id_prefix` > 包 `field_meta.json.id_prefix[模块]` > 框架 `id_prefix` > `kind` > 模块 id 归一 | `_module_prefix` / `id_rule_spec` / `_pack_id_policy` / `_preset_of` |
| 3 | 非法字符归一（小写 + 下划线）；slug 规则重复仍追加序号 | `slugify` / `_unique_slug` |
| 4 | 拼音**可选附加**：`pypinyin` 可导入才出「拼音 id」按钮；多音字/取不出/失败/未装库 → 回落并明确提示（不报错） | `pinyin_available` / `suggest_pinyin_id`；前端 `newIdentHtml` / `suggestIdFromName(mode)` / `new-id-note` / `EditorEntry.ruleText` |
| 5 | 只影响新建（负向）：只读接口不写盘、既有 id 不动 | `tests/unit/test_editor_batch16_autoid.py`（负向逐字节断言） |
| 6 | 包声明键 `id_prefix` / `id_width` 严格解析 | `qbot_rpg/content/field_meta_pack.py`：`_validate_id_prefix_map` / `_validate_id_width_map` |

### #10 概率/权重「使用处 > 定义处」

| 节 | 交付 | 实现锚点 |
|---|---|---|
| 1 | 取值顺序 **使用处 > 定义处 > 缺省**；显式 0 视为有效覆盖；无覆盖与修前逐字段一致 | `qbot_rpg/core/monster_ai.py`：`MonsterAI._param`（`_random_pool` / `_weight_for` 共用） |
| 2 | 编辑器标注：定义处「默认值（可被引用处覆盖）」；使用处「留空 = 用行动定义处的默认值」 | `qbot_rpg/content/models.py`：`FieldMeta.overridable_default`；`field_meta.py` action 字段 + `ACTION_ENTRY_CHILDREN`；`api.help_card.default_note` / descriptor；前端 `helpLines`「取值来源」行 |
| 3 | 印记侧核查：`marks.json` 无概率类键 → 无需改动（测试设事实断言） | `tests/unit/test_batch16_prob_override.py` |
| 4 | 无覆盖对拍 0 差异 + 有覆盖数值断言 + 定义处默认生效 | 同上 |

### #11 条目预设（新建按模板）

| 节 | 交付 | 实现锚点 |
|---|---|---|
| 1 | 包声明 `entry_presets` 严格解析（七键白名单；id 唯一；fields 非空；width 1..12） | `qbot_rpg/content/field_meta_pack.py`：`_validate_entry_presets` |
| 2 | 新建：选预设 → 只显示关心字段 + defaults 生效；不选预设 = 现状 | `qbot_rpg/web/api.py`：`new_entry_slot(preset=…)` / `new_entry_detail(preset=…)`（字段分区 + `preset_missing_fields`）；`editor_ops.create_entry(preset=…)`；宿主路由 `?preset=` / body `preset` |
| 3 | 一号原则：其余字段进「其他字段」折叠区**仍可编辑**（不永久隐藏） | `index.html`：`newPresetHtml` / `otherFieldsHtml` / `renderNewEntry`（并入 `MAIN_CTX.fieldIndex`）/ `bindSubBlocks` / `bindNewIdent` 预设切换 |
| 4 | 端到端：带预设真写盘（defaults 落盘）→ 回退逐字节复原；真 Chromium DOM 对照 | `tests/unit/test_editor_batch16_presets.py` |

**批16 门禁**：全量 `pytest tests/ -q -o addopts=""` 0 failed；`ruff check`（改动文件）干净；
换包验收 `scripts/editor_verify_packs.py` 全 PASS；迁移对拍 `删除/值变化 = 0`（新增项显式计数）；
页脚批次串 `批16 · 中文ID/概率覆盖/预设`（`index.html`）+ 全部断言同步
（**批17 已把页脚更新为「批17 · 通用预设」**）。

## 九、批17 · 通用预设（框架默认六种，2026-09-15）

状态：**✅ 全部销项**。用户拍板：**材料、货币袋、药剂、礼包、装备、技能书**六种作为
**框架的通用预设**——不再是某个包的专属声明，任何内容包（含完全空白包）都自带；
包可覆盖同 id、追加自己的、按需关闭某个默认。

| # | 交付 | 实现锚点 |
|---|---|---|
| 1 | 框架默认预设表 `{"<模块>": (预设, …)}`（本次 `items` 六种：`id`/`label`/`help`/`fields`/`defaults`/`id_prefix`/`id_width`） | `qbot_rpg/content/entry_presets.py`：`FRAMEWORK_ENTRY_PRESETS` / `framework_presets` |
| 2 | 合并规则 `生效预设 = 框架默认 ∪ 包声明`：同 id → 包**整体覆盖**（非字段级）；包新 id → 追加；**空白包也有六种** | 同上：`merge_entry_presets`（纯函数） |
| 3 | 关闭方案：包 `field_meta.json` 顶层 `entry_presets_disable: {"<模块>": ["<预设 id>", …]}`（严格解析：模块名非空、数组元素非空且不重复） | `qbot_rpg/content/field_meta_pack.py`：`TOP_LEVEL_KEYS` / `PackFieldMeta.entry_presets_disable` / `_validate_entry_presets_disable` |
| 4 | 编辑器接入：`new_entry_*` / `suggest_id(preset=…)` 走合并表；主区顺序 = 预设 `fields` 顺序；预设名 + help 展示；其余字段折叠仍可编辑（一号原则） | `qbot_rpg/web/api.py`：`_effective_presets` / `_preset_of` / `_pack_id_policy` / `new_entry_detail`（`presets` 列生效预设） |
| 5 | 红线：预设引用键必须在框架 `items` 元数据里真实登记（不臆造） | `entry_presets.framework_preset_key_errors(table)` + 测试断言 |
| 6 | 测试：键校验 / 空白包六种 / 包覆盖·追加·关闭 / `id_prefix` 联动 / 不选预设回归 / E2E 写盘回退 / HTTP | `tests/unit/test_editor_batch17_framework_presets.py` |
| 7 | 文档：面向作者（六种清单、覆盖/追加/关闭、`id_prefix` 影响自动 ID） | `docs/编辑器使用说明.md` §十·1/2；契约 `docs/编辑器重写_需求与约束.md` §〇·补八 C |

**type 取值核查（现有包 `content/veinborn/items.json`，165 条实测）**：实际取值 =
`weapon`(37) / `material`(33) / `armor_head`(17) / `armor_hand`(17) / `armor_foot`(17) /
`armor_body`(16) / `armor_leg`(16) / `accessory`(7) / `consumable`(4) / `armor`(1)。
六种预设的 `defaults.type` 取通用英文品类值：`material` / `consumable` 与现有习惯一致，
`currency` / `skill_book` 也在引擎类型词表（`basic_commands._CATEGORY_BY_TYPE`）内；
`equipment` / `gift_box` 为通用品类词，引擎背包筛选暂未收录（会归「其他」），不影响校验与数据。

**批17 门禁**：全量 `pytest tests/ -q -o addopts=""` 0 failed；`ruff check`（改动文件）干净；
换包验收 `scripts/editor_verify_packs.py` 全 PASS；页脚批次串 `批17 · 通用预设`
（`index.html`）+ 全部断言同步。

## 十、批18 · 预设细化与效果扩展（2026-09-16）

状态：**✅ 全部销项**。用户 2026-09-16 拍板：① 框架六预设细化（段 A）；② 新增
`gain_currency`（给货币）/ `learn_skill`（学技能）两类**引擎真消费**的效果能力（段 B）。
段 A、段 B 各成一个 commit。

### 段 A：六预设细化

| # | 交付 | 实现锚点 |
|---|---|---|
| 1 | **装备**预设字段扩到全属性：加全部属性白值 `hp,mp,str,con,spr,lck,spd,mag`；顺序＝标识/部位 → 攻防 → 属性白值 → 百分比/会心 | `qbot_rpg/content/entry_presets.py`：`equipment.fields` |
| 2 | **药剂**预设 `help` 写明「持续回合在引用的效果里设置（效果的『持续』/`turns` 字段）」；字段保持 `name,type,desc,usable,effects,price`；**不加**物品级 `duration`（持续属效果语义，避免重复来源） | 同上：`potion.help` |
| 3 | **货币袋 / 技能书**预设 `help` 补「需要对应效果类型（给货币 / 学技能），见 `gain_currency` / `learn_skill`」 | 同上：`currency_pouch.help` / `skill_book.help` |
| 4 | 六个预设 `fields`/`defaults` 键仍全部通过「框架 items 元数据存在性」断言 | `framework_preset_key_errors` + `tests/unit/test_editor_batch18_preset_refine.py` |

### 段 B：两类新效果能力（引擎必须消费）

| # | 交付 | 实现锚点 |
|---|---|---|
| 5 | `gain_currency`：字段 `currency`（引用 `settings.currencies`）、`amount_min`/`amount_max`（int≥0，max≥min）；**使用物品时给玩家对应货币若干**（`[min,max]` 取随机，用引擎既有 RNG `ctx["rng"]`，缺省 `random` 模块；不新引入随机源）；写 `player["currencies"]` 走既有存档链路 | `qbot_rpg/commands/use_commands.py`：`_currency_grant_of` / `_pick_amount` / `_add_currency` / `_use_consumable` |
| 6 | `learn_skill`：字段 `skill`（引用 `skills.json`）、`level`（int≥1，缺省 1）；使用时按既有技能槽口径把技能以指定等级授予玩家（写 `persistent_state.skill_slots`）；**已学会 → 幂等**不重复授予、不改既有等级 | 同上：`_skill_grant_of` / `_grant_skill` |
| 7 | 编辑器与校验：effects 段登记两新类型（展示层下拉候选 `enum_options`，含新词）+ 新字段元数据（中文名/说明/范围/引用目标）；校验器 `_check_effects_18` 做类型相关必填/范围/引用存在性红拦；未配置态字段可见可填（一号原则） | `qbot_rpg/content/field_meta.py`：`EFFECT_TYPE_CHOICES` / `F_EFFECT_TYPE` / effects 新字段；`qbot_rpg/content/models.py`：`FieldMeta.enum_options`；`qbot_rpg/web/api.py`：descriptor 下发 `enum_options`；`qbot_rpg/content/validator.py`：`_check_effects_18` |
| 8 | **明确不做**：不给既有伤害/治疗等数值效果加「随机区间」（避免全盘动数值手感；将来要「随机伤害」另开专项） | 本批无改动 |

**随机取值口径（写进 docstring）**：`amount_min == amount_max` → 必给固定值；缺 `amount_max`
→ 视为等于 `amount_min`；缺 `amount_min` → 视为等于 `amount_max`（对称缺省）；两者都缺 →
不生效，**校验层红拦**（R-5 `gain_currency_amount_missing`，死配置口径，对齐 §2.1 R-5）。
`amount_min > amount_max` → 红拦 R-5 `dead_range`；负数 → 泛型 R-2 红拦。
`learn_skill.level < 1`：0 → R-1 `skill_level_invalid`，负数 → R-2，非整数 → R-1；缺省 = 1。
`currency` / `skill` 引用不存在 → R-4 红拦（`currency` 走 `settings.currencies[].id` 键空间，
对齐 1g4 F-02 货币引用硬拦口径）。

**类型枚举的落地方式（门禁约束）**：effects.type 是**开放词汇**（L0 动作 + 包内泛化词 +
`x_` 自定义通道），既有校验口径为 `type=str`（不设校验 enum）。批18 要把 `gain_currency` /
`learn_skill` 加进编辑器的类型候选，但**不能改既有字段描述符的 `type`/`enum`/`hint`/`help`**
（迁移对拍门禁 `tests/unit/test_editor_pack_field_meta_migration.py` 对既有字段的
删除/改值 = 硬差异）。故新增**展示层**机制 `FieldMeta.enum_options`（只影响控件候选，不参与
校验）→ API descriptor 新增 `enum_options` 键（新增项 = soft，门禁允许）+ `editor="select"`
（`control` 属派生展示键，改值记 soft）→ 前端 select 读取候选。既有 `x_` 自定义与包内新词
仍按 `type=str` 放行，门禁 0 硬差异。

**批18 门禁**：全量 `pytest tests/ -q -o addopts=""` 0 failed；`ruff check`（改动文件）干净；
换包验收 `scripts/editor_verify_packs.py` 全 PASS；页脚批次串 `批18 · 预设细化与效果扩展`
（`index.html`）+ 全部断言同步；迁移对拍新增项 soft、删除/改值 = 0。

## 十一、批19 · 显示完整性与归口（用户 2026-09-16 第二轮 9 条里的 6 条）

状态：**✅ 六节全部落地**（#4 / #6 / #9 / #7 / #5 / #8）。用户 2026-09-16 第二轮共 9 条，
本批取其中 6 条（另 3 条另批）。一号原则（§〇·补）贯穿：**框架登记/实现的东西必须显示、可编辑**。

| 段 | 用户意见 | 交付 | 实现锚点 |
|---|---|---|---|
| A | #4 消息模板只显示 2 条 | 条目列表 = 框架全量模板表键（`core/templates/template_table.json`）∪ 包 `templates.json` 覆盖；未覆盖标「默认（框架）」可直接编辑→写入包覆盖 | `content/models.py`（`ModuleMeta.key_source`）、`content/framework_keys.py`（来源注册表）、`content/field_meta.py`（`templates.key_source="templates"`）、`web/api.py`（`_entry_rows`/`_entry_base`/`entry_detail`/`entry_slot`/`list_entries`）、`web/static/index.html` |
| B | #6 未启用模块裸英文键 | 模块名回落链 = 包声明 → `module_catalog` label → 模块键（统一 `_module_display_label`） | `web/api.py`（`_module_display_label` + 全部调用点） |
| C | #9 公式区域无中文名 | 按 `core/formula_loader.py` 实际消费面登记：`damage_base`/`heal_rate`（兼容保留，help 注明不消费）/`battle_position.*`（与 `BattlePositionParams` 字段一一对应）/`monster_def_rate`/`crit.negative_crit`/`crit.elem_crit_step`；包可 `field_labels.formula` 覆盖 | `content/field_meta.py`（`FORMULA_FIELDS` / `_formula_sections`）、`web/api.py`（`_entry_base` map 逐键登记优先） |
| D | #7 指令别名不完整 | 别名视图 = 框架内置（`CommandSpec.aliases`）∪ 包 `settings.command_aliases`；同键包覆盖；⚙ 面板展示两类来源 | `assembly/router_setup.py`（`framework_builtin_aliases`）、`content/field_meta.py`（`ALIAS_CONFIG_*`）、`web/api.py`（`list_aliases` / `module_catalog.aliases`）、`scripts/editor_host.py`（`GET /aliases`）、`index.html` |
| E | #5 特性/装备槽 用途与重叠 | 模块页显示 `purpose` + 「当前包未使用」；`slots` ↔ `settings.slot_defs` 功能重叠黄提示（声明驱动、不硬拦） | `content/module_catalog.py`（`overlap_with`/`overlap_note`）、`web/api.py`（`_module_in_use`/`_module_overlap_hints`/`list_entries.purpose/unused`）、`index.html` |
| F | #8 条目级层级 | 新增 `entry_tree` 声明（父节点 = 声明模块或虚拟视图；默认移走 / `keep_top_level` 显示挂载）；左栏条目级叶子、中栏「移入自/挂载自」、计数与检索三处同口径；编辑写回来源模块 | `content/field_meta_pack.py`（`entry_tree` 严格解析）、`web/api.py`（`_entry_tree_map`/`_entry_tree_plan` + `list_modules`/`list_entries`/`entry_index`）、`content/veinborn/field_meta.json`、`index.html`、`scripts/editor_visibility_selfcheck.py`、`scripts/editor_verify_packs.py` |

**F 的声明与层级输出（veinborn 实测）**：

- `life`（虚拟视图，显示挂载）← `settings.alchemy` / `settings.quest_board` / `settings.forge`；
- `maps`（声明模块，移走）← `settings.env_event` / `settings.time_cycle`（`settings` 条目列表
  不再列出，`maps.own_count` 含这两条，`entry_index` 同步；全包 total 不变）。

**#7 框架侧现状核查结论（如实）**：框架**没有**独立的静态别名表。别名两处来源 = ①
`CommandSpec.aliases`（注册指令时自带；本视图标「框架内置」）与 ② 包 `settings.command_aliases`
（装配层既有 `AliasTable.from_config` 装载；标「包覆盖」）。本批从既有 `REGISTER_GROUPS`
装配读取 ①，未另造表；运行时两处都生效，同键以包声明为准。

**#9 核对清单**：已登记 = `damage_base`/`heal_rate`（包内实际存在、当前 Python 侧
`formula_loader` 不消费，标为兼容保留键）+ `battle_position.break_base_damage/break_sqrt_coef/
broken_part_mult` + `monster_def_rate` + `crit.negative_crit`/`crit.elem_crit_step`（后三者按
实际消费面补齐）。**未登记待确认**：无（`formula_engine` 本身只做表达式求值、不按名消费键，
故不存在其它「已消费但未登记」的标准公式键）。

**批19 门禁**：全量 `pytest tests/ -q -o addopts=""` 0 failed；`ruff check` 无新增；
换包验收 `scripts/editor_verify_packs.py` 全 PASS；一号原则自检 PASS；字段元数据对拍以本批末
提交重定基线（A 段模板全量、F 段条目改归父节点均为**展示层合法演进**，与字段级迁移无关）。
