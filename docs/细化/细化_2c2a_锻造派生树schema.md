# 细化 · 2c2a 锻造派生树 schema（forge.json · 每部位一棵树 · 多线分支 · ■最终强化 · 两档素材 · 100% 确定性）

> 依据（严格引用）：`/root/docs_archive/RPG框架项目/锻造系统设计定稿.md`（下称【锻造】，v1.0.1 · 410 行，全篇已读）
> 对应任务：规划_路2c2_锻造.md T0x（forge.json 派生树数据 schema / 校验器 / 验收用例）
> 定位：**本细化只定义 forge.json 派生树数据契约**——每部位一棵树、多线分支、终点■最终强化、两档素材+3:1 合成、100% 确定性、校验器与验收用例；不含 /锻造 /图纸 指令流程（见 2c2b 细化）、套装 P1（§12.2）、客制 P2（§12.3）
> 编号规则：字段表以 D-/T-/N-/M-/S- 编号；校验规则以 V（硬）/W（黄）编号；验收用例 TC-01 起连续编号
> 文档总览：字段 5+5+15+4+5=**34** 个，验收用例 **29** 例（TC-01 ~ TC-27 含 b 变体）

## 0. 一句话定位（可追溯）

| 维度 | 定稿要求 | 引用 |
|---|---|---|
| 锻造 = 铁匠铺 + 装备派生树 | 打怪采素材 → 沿树一级一级锻造升级 → 每线终点=「最终强化」神兵；锻造同时是**带孔装备的唯一常规来源**（孔位=装饰珠插槽） | 【锻造】L17-18 |
| 每部位一棵树 × 多线分支 × 最终强化 | 树 = 每部位一棵树（武器树/防具树×5部位），多线分支，每条线终点=■最终强化 | L117 / L52 |
| 节点 = 装备（item 引用 + 节点改造） | 节点 = 装备（item 引用 items.json 条目 + 节点改造 stats/slots/rarity；品质=items.json 四档枚举） | L115 |
| 两档素材 + 3:1 合成 + 素材即进度 | 普通/稀有两档；3 个普通→1 个稀有（合成引擎 kind=combine）；素材数量+来源提示+图纸全链 | L53 / L144-147 |
| 100% 确定性 | 锻造 100% 成功（素材够就成，对应 MH 素材制）——失败/随机全部留给邻居系统；随机性不同源 | L29 / L32 / L56 |
| 零会话原子 | /锻造 <节点> 一条指令出结果（不用框架 3.18），免疫热重载/双扣 | L51 / L239 |

---

## 一、forge.json 字段级 schema

> 依据：【锻造】§十二 L244-358（12.1 节点 L248-270、12.1.1 双源仲裁 L272-289、12.1.2 级联删除 L291-307、12.4 settings L351-358）、§十三 校验器 L369。
> 结构：`forge.json` 顶层含 `trees`（派生树数组，每部位一棵）、`sets`（P1 套装，引用登记）、`augments`（P2 客制，引用登记）、`settings`（全局开关）。`sets`/`augments` 非本细化主责，仅登记落点与引用，不展开字段。

### 1.0 文档级字段（forge.json 顶层）

| # | 字段 | 类型 | 必填 | 语义 | 引用 |
|---|---|---|---|---|---|
| D-01 | `schema_version` | string | 是 | 数据版本号，如 `"1.0"`；加载器校验 | —（对齐框架配置规范） |
| D-02 | `trees` | array\<ForgeTree\> | 是 | 派生树数组；**每部位一棵**：武器树×1 + 防具树×5（头/身/手/腿/脚）共 6 棵；全空则校验器 V1 硬错（防空池） | 【锻造】L117 / L165 |
| D-03 | `sets` | array\<Set\> | 否 | P1 防具套装段（forge_sets）；默认开、内容包可关；本细化不做字段展开（见 §12.2 L309-322），`settings` 关闭时数据可存在但 W3 黄提示不生效 | L162 / L309-322 |
| D-04 | `augments` | array\<Augment\> | 否 | P2 客制强化段（forge_augments，含 `limit_by_rarity` 四档次数）；默认开、内容包可关；本细化不做字段展开（见 §12.3 L324-340 与四档映射 L342-349），仅登记引用 | L174-183 / L324-349 |
| D-05 | `settings` | object | 否 | 全局可配段（§1.4 表 S-xx） | L351-358 |

### 1.1 树级字段（ForgeTree）

| # | 字段 | 类型 | 必填 | 默认 | 语义 | 引用 |
|---|---|---|---|---|---|---|
| T-01 | `id` | string | 是 | — | 树唯一键，英文小写蛇形（如 `tree_weapon` / `tree_armor_head`），全文件唯一（V1） | 【锻造】L117 |
| T-02 | `name` | string | 是 | — | 部位中文名（如 `武器树` / `防具树·头`），显示用；/锻造树 分页标题 | L234 / L364 |
| T-03 | `type` | enum | 是 | — | 部位标识：`weapon` / `armor_head` / `armor_body` / `armor_hand` / `armor_leg` / `armor_foot`；**全文件 type 唯一 → 每部位一棵树**（V1）；须与树内节点 N-04 一致（V2） | L117 / L165 |
| T-04 | `roots` | array\<string\> | 是 | — | 根节点 id 列表（无 parent），每条主线起点（如 `node_iron_sword` / `node_bone_gs`）；≥1；均须为本树已定义节点（V3 引用） | L120-122 / L256（parent 语义逆推） |
| T-05 | `nodes` | array\<Node\> | 是 | — | 该部位全部节点，每个节点对象见 §1.2（N-xx）；**整个 trees 内节点 id 全局唯一**（V2） | L115-117 / L248-270 |

### 1.2 节点级字段（Node，forge_tree 行）

> 依据：【锻造】§12.1 L248-270（字段别名/注释原文）、§12.1.1 双源仲裁 L272-289。字段名英文小写；`item` 与 `output_item` 为别名二选一（N-03），不双写。

| # | 字段 | 类型 | 必填 | 默认 | 语义 | 引用 |
|---|---|---|---|---|---|---|
| N-01 | `id` | string | 是 | — | 节点唯一键（如 `node_flame_sword_2`）；**全文件唯一**（V2） | 【锻造】L251 |
| N-02 | `name` | string | 是 | — | 装备中文名（如 `炎剑Ⅱ`），/锻造 参数匹配名 | L252 / L232 |
| N-03 | `item` | string | 是 | — | 产物装备引用 `items.json` ID（别名 `output_item` 二选一，不双写）；装备实例属性 = items 基础属性 + 本节点改造（仲裁见 §1.3）；校验器检查存在性与类型匹配（V7/V8） | L253 / L276 |
| N-04 | `type` | enum | 是 | — | `weapon`/`armor_head`/`armor_body`/`armor_hand`/`armor_leg`/`armor_foot`；**须与 items 条目类型匹配**（V8），并与所属树 T-03 一致（V2） | L254 / L276 |
| N-05 | `level` | int | 是 | — | 节点等级（铸造职业门槛：可锻造节点等级上限=职业等级） | L255 / L213 |
| N-06 | `parent` | string \| null | 是* | (*根节点为 `null`) | 父节点 id（派生来源，须先锻造）；指向本文件已定义节点且须同树（V3/V4 无环） | L256 / L369 |
| N-07 | `branch` | array\<string\> | 否 | `[]` | 分支节点 id 列表——**本线可转出的其他线**（多线分支接线）；每个 id 须存在（V5）；空=本线无转出 | L257 / L369 |
| N-08 | `stats` | object | 否 | `{}` | 属性改造（如 `{"atk":24,"element":"fire","element_value":8}`）；元素键走 formula.json 元素注册表（地水火风雷晶月无之一）；覆盖/追加仲裁见 §1.3 | L258 / L200 |
| N-09 | `slots` | array\<{level:int}\> | 否 | `[]` | 孔位（装饰珠插槽，框架 3.5 `slot_level`）；`level ∈ {1,2,3}`（V14）；**孔位来源=锻造**（带孔装备唯一常规来源） | L259 / L18 / L31 / L190 |
| N-10 | `materials` | array\<MaterialReq\> | 是 | — | 素材需求行（M-xx 见 §1.2.1）；≥1 行（根节点至少 1 行）（V10） | L260-263 / L110 |
| N-11 | `cost` | object | 否 | `{}` | 锻造金币开销（如 `{"coins":500}`）；默认按 settings `forge_fee` 节点等级×10 | L264 / L353 |
| N-12 | `rarity` | enum | 是 | 继承 items.quality | 品质=items.json 四档枚举 `normal`/`fine`/`epic`/`legendary`（普通/精良/史诗/传说）；历史整数档兼容映射 1/2/3/4；与 items.quality 冲突以节点为准（仲裁 §1.3） | L265 / L246 / L283 |
| N-13 | `monster_source` | string \| null | 否 | `null` | 怪物来源 id（loot.json 怪物键，可空）；空=非直接怪物掉落节点 | L266 / L156 |
| N-14 | `final` | bool | 是 | `false` | **■最终强化**标记：每条派生线终点=true（V6：叶子必须 true、true 不得有子） | L267 / L117 / L134-136 |
| N-15 | `augmentable` | bool | 否 | `false` | 可否客制（P2）；仅 `final=true` 且为武器时可 true（V16 黄建议） | L268 / L136 / L177 |

> 备注：N-09 `slots` 即任务口径「孔位来源」的落点——锻造产装备带孔位（slot_level 1/2/3 复用框架 3.5），是带孔装备唯一常规来源；开孔道具另可来自掉落/炼金（L190-193），客制孔位项统一开孔渠道（L181）。N-06 `parent` + N-10 `materials` 即「前置节点 + 素材需求」两件套：锻造一条指令 = 前置已锻 + 素材够 → 100% 出结果（§四）。

#### 1.2.1 素材需求行（MaterialReq）

> 依据：【锻造】L260-263、§五 两档素材 L143-148、来源提示 L147、素材表登记 items.json 材料类 L365。
> 档位判定：素材「普通/稀有」两档登记于 items.json 材料类元数据（推荐字段 `material_tier`，本细化登记建议）；节点素材行 `tier` 为**可选覆写**（行覆写 > items 元数据），仲裁风格对齐 §12.1.1。

| # | 字段 | 类型 | 必填 | 默认 | 语义 | 引用 |
|---|---|---|---|---|---|---|
| M-01 | `item` | string | 是 | — | 素材 id，引用 `items.json` 材料类条目；类型须为材料类（V10） | 【锻造】L261 / L365 |
| M-02 | `count` | int | 是 | — | 数量；≥1 正整数（V11） | L261-262 |
| M-03 | `tier` | enum | 否 | 派生自 items 元数据 | 素材档位覆写：`normal`（普通：矿石/草药/皮骨）/ `rare`（稀有：怪物素材/宝玉）；缺省=items 材料 `material_tier`；行覆写优先（双源仲裁） | L144-145 / L53 |
| M-04 | `source_override` | string | 否 | 派生自 items 来源 | 来源提示覆写（显示文本，如 `火龙掉落/商店`）；缺省=items 材料来源标签；来源提示跟随/图纸 缺件报错（TC-14） | L147 / L240 |

### 1.3 节点 × items.json 双源仲裁（属性合并）

> 依据：【锻造】§12.1.1 L272-289。装备实例属性 = `items.json 基础属性 + 节点改造`，**两处都不是自足的**（L277）。

| # | 规则 | 行为 | 引用 |
|---|---|---|---|
| AR-1 | 覆盖 | 节点声明的键（`stats.*`/`slots`/`rarity`/`monster_source`/`final`/`augmentable`…）以节点值为准，**覆盖 items 同键基础值**（节点=该装备在树上的形态定稿） | 【锻造】L281 |
| AR-2 | 追加 | 节点未声明的键，**继承 items 基础值**（节点是"增量改造"，不是全量重写） | L282 |
| AR-3 | 品质 | 节点 `rarity` 必填（四档枚举）；与 `items.quality` 冲突以节点为准；未配置 = 继承 items.quality | L283 |
| AR-4 | 配置模式 | 瘦 items+富节点（默认）或 富 items+薄节点：只建议不限制 | L286-288 |
| AR-5 | 实例快照 | /锻造 完成时按"items 基础 + 节点改造"合并实例化，**属性快照入玩家存档**（防热重载/后续删改回溯，对齐 3.18 快照原则） | L278 / L301 |

### 1.4 settings 段（全局可配）

> 依据：【锻造】§12.4 L351-358。

| # | 字段 | 类型 | 默认 | 语义 | 引用 |
|---|---|---|---|---|---|
| S-01 | `forge_fee` | string \| int | `节点等级×10` | 锻造金币开销（可配）；节点 N-11 显式 `cost` 覆盖它 | 【锻造】L353 |
| S-02 | `synth_ratio_3to1` | bool | `true` | **3:1 合成开关**（P1）＝合成引擎 `kind=combine` 实例开关（3 普通→1 稀有，数值不变）；false=无升档渠道，校验器 W4 提示素材死锁风险 | L354 / L146 / L387 |
| S-03 | `straight_forge` | bool | `true` | 直锻模式：小白一步出结果；false=深度模式（预览节点卡 2 步） | L355 / L57 |
| S-04 | `decompose_rate` | object | `{正式:0.4,...}` | 分解回收率（复用炼金分解规则，/分解 用） | L356 / L235 |
| S-05 | `exp_per_forge` | int | `节点等级×2` | 铸造熟练经验：每件装备 = 节点等级×2（可配） | L357 / L214 |

---

## 二、派生树结构（每部位一棵 · 多线分支 · ■最终强化）

> 依据：【锻造】§四 L111-138（4.1 结构 L113-123、4.2 规模 L125-132、4.3 最终强化 L134-137）、§2.2 五层决策 L60-68、§九 属性分线 L199-203。

### 2.1 拓扑（对齐怪猎 MHW 武器派生树 QQ 化）

```
节点 = 装备（item 引用 items.json 条目 + 节点改造 stats/slots/rarity）  ——  §12.1 / L115
边   = 派生关系（父节点 → 子节点，素材需求）                            ——  L116
树   = 每部位一棵树（武器树/防具树×5部位），多线分支，每条线终点=■最终强化 ——  L117
```

定稿示例拓扑（L120-122）：

```
铁剑〔1级〕→ 铬钢剃刀 → 灭尽龙的裁决 → ■灭鬼凶器（龙）
龙骨大剑 → 召雷剑 → 召雷剑【麒麟】 → ■召雷剑【麒麟王】（雷）
火炎之刃 → 赤红之翼 → 蓝翼大剑 → ■火龙煌剑（火 会+）
```

- **多线分支 = 每级 2-3 分支**：N-06 `parent` 表达主干推进，N-07 `branch` 表达本线可转出的其他线（如：炎剑Ⅱ 分支冰剑 ←需冰晶矿 / 雷剑 ←需雷兽牙，图纸示例 L104-105）。
- **分线即属性分线（九章联动）**：每线天然按 8 属性（地水火风雷晶月无）分线（火线/冰线/雷线…），玩家针对怪物弱点换线（L199-203）；属性键走 formula.json 元素注册表（N-08）。

### 2.2 终点 = ■最终强化（N-14 `final`）

- **每条派生线终点 = 最终强化神兵（MH 惯例）**（L135）；拓扑约束：线终点（叶子）必须 `final=true`，`final=true` 不得有子节点（V6）。
- 最终强化武器可解锁：**客制强化（P2）/ 专属孔位 / 图鉴里程碑**（L136）；`augmentable=true` 仅终于强化武器可客制（L177，客制次数按品质四档 epic×3/legendary×2/终盘×1，L334-339 / L342-349）。

### 2.3 规模建议（内容包作者配置负担控制，软约束）

| 项 | 建议值 | 引用 |
|---|---|---|
| 每部位主干 | 8-12 级 + 每级 2-3 分支 + 每线最终强化 1 个 | 【锻造】L127 |
| 武器树主线数 | 5-8 条（对应主要怪物系/属性系，不用 14 类全做） | L128 |
| 防具树 | 每部位 3-5 条线（通用线/属性线/套装线） | L129 |
| 总节点量 | 武器 300-500 / 防具 500-800（15000 物品总量内） | L130 |
| 编辑器 | 装备树可视化（只读预览+下拉选择目标；节点卡片 + item 下拉引用 items.json） | L131 / L364 |

> 规模为**建议值非硬校验**：超量给 W5 黄提示（配置负担预警），不阻断加载（TC-04b）。

### 2.4 深度来源（五层决策的文字化翻译，供 schema 消费方理解）

```
① 树规划：多线分支选哪条？（火线 vs 冰线 vs 无属性高攻线）            —— schemes.roots / N-07 branch
② 素材分配：稀有素材给武器还是防具？（3:1 合成要不要用）              —— M-03 tier / S-02
③ 套装取舍：五部位凑套装技能 vs 混搭散件（P1，见 sets）              —— D-03
④ 珠位搭配：孔位等级怎么配珠（与炼金闭环）                          —— N-09 slots
⑤ 客制投入：终盘 4 项选哪几项（P2，见 augments）                    —— D-04 / N-15
```

---

## 三、素材两档（普通/稀有 + 3:1 合成）与「素材即进度」

> 依据：【锻造】§五 L140-158、L53、L58、§3.4 L99-107、指令表 /图纸 L233、错误模板 L240、风险-素材死锁 L387。

### 3.1 两档素材

| 档位 | 内容 | 来源（5.2） | 引用 |
|---|---|---|---|
| **普通** | 矿石/草药/皮骨（基础材料） | 挖掘→矿石系；采集→草药/植物系；商店→基础可买（金币回收口） | 【锻造】L144 / L152-157 |
| **稀有** | 怪物素材/宝玉（核心素材，驱动打怪） | 怪物掉落→loot.json 核心素材（稀有素材保底机制复用）；垂钓→鱼/特殊素材；大怪/精英/BOSS 掉落 | L145 / L148 / L156 |

- 每怪对应 2-3 种素材（皮/爪/宝玉），素材需求驱动打怪（L148）；`monster_source`（N-13）登记主来源怪（loot.json 键）。
- 档位登记于 items.json 材料类元数据（`material_tier`），节点素材行 `tier` 可选覆写（M-03，仲裁见 §1.2.1）。

### 3.2 3:1 合成（普通 → 稀有 升档，P1 扩展）

- **3 个普通素材 → 1 个稀有素材**（炼金/锻造工房均可）＝**合成引擎 `kind=combine` 配置实例**（N 素材→1 高级素材，框架 4.1 recipe.json 统一 schema 登记；**原数值不变**）（L146）。
- 开关：settings `synth_ratio_3to1`（S-02）＝该 combine 实例开关；SP 面板 3:1 合成解锁项（L216）。
- 防死锁三通道：**3:1 合成 + 商店基础材料 + 分解回收**（/分解，L235 / L387）——素材死锁风险对策。

### 3.3 素材即进度（/图纸 全链 + 缺件提示）

- 语义：素材数量 + 来源提示 + 图纸全链（/图纸 看还差啥）（L58）。
- `/图纸 炎剑` 输出（L101-107）：派生链全览 + 分支提示 + **当前持有：火龙鳞 1/3 | 矿石 5/5 | 铁剑Ⅰ ✓**。
- 素材不足报错模板（L88 / L240）：`缺 火龙鳞×2（来源：火龙掉落/商店）→ /图纸 查看全链`——消费方从材料需求行（N-10 → M-xx）+ 玩家背包持有量生成，**不产生任何随机事件**（§四）。

---

## 四、锻造 100% 确定性（失败/随机隔离到炼金/强化）

> 依据：【锻造】§1.2 三系统分工 L20-32、§2.1 决策表 L47-58、§十六 三系统对比 L395-407、§3.2 L81-87、§11 指令表 L228-240。

### 4.1 边界铁律（§1.2，逐条接线）

1. **锻造 100% 成功（素材够就成，对应 MH 素材制）——失败/随机全部留给邻居系统**（L29）→ schema 侧合同：锻造引擎读树数据后**无失败分支、无随机源**；成品状态由「前置已锻（N-06）+ 素材够（N-10）+ 职业等级够（N-05）」三个确定性谓词唯一决定（TC-16）。
2. 强化加数字（+5），锻造管形态（剑→炎剑）（L30）→ 本细化数据只含形态层（stats/slots/rarity/树），不做数值堆叠。
3. 锻造 = 带孔装备唯一常规来源（L31）→ N-09 `slots` 为装备孔位唯一常规入口。
4. **随机性不同源：锻造无随机 / 炼金品质特性 / 强化成功率**（L32）→ 差分测试：同输入（前置+素材+等级）锻造多次产出与日志恒定（TC-17）。

### 4.2 零会话原子

- **零会话原子锻造**（/锻造 <节点> 一条指令出结果，C 版碎片化最优，免疫热重载/双扣）（L51）；锻造指令明确**不建立会话状态**（L239）。
- 直锻模式默认开：小白一步出结果；深度玩家可关（预览节点卡 2 步 /锻造 <节点> 预览 → /确认）（L57 / L355 / §3.3 L89-97）。
- 成功即实例化并**属性快照入存档**（AR-5 / L278）——原子写，无中间态。

### 4.3 失败模板（确定性错误，非随机）

| 场景 | 输出模板 | 引用 |
|---|---|---|
| 素材不足 | `❌ 素材不足：需要 铁剑Ⅰ + 火龙鳞×3 + 矿石×5；缺：火龙鳞×2（来源：火龙掉落/商店）→ /图纸 铁剑 查看全链` | L84-87 / L240 |
| 等级不足 | `需要 精通 级，当前 正式（还差 40 熟练）` | L240 / L213 |
| 前置未锻 | 沿 parent（N-06）报缺前置链条，指引 /图纸 | L256（须先锻造） |
| 已删失效节点 | `/锻造 拒绝`；/图纸 显示「已失效：物品已删除」（红名节点，§五 V15/W6/TC-25） | L296 |

> 三系统对比（L397-404）：锻造 随机性=无（100%）、流程=原子（1 步）、深度来源=树规划/素材分配、职业=铸造；随机与失败一律由炼金（品质/特性）与强化（概率成功）承接。

---

## 五、校验器规则

> 依据：【锻造】§13 L369（父节点存在/素材引用/树无环/分支可达/等级引用/node.item 引用存在 + items 类型匹配 + 改造键空间/级联删除复查）、§12.1.1 L284（四项）、§12.1.2 L298（级联复查）、§九 L203（无弱点警告）、§十五 L388（元素武器白板）。硬校验配置加载期执行并记录日志/启动报告，定位到具体字段。

| # | 级别 | 规则 | 失败行为 | 引用 |
|---|---|---|---|---|
| V1 | **硬** | 树级唯一：每部位一棵——全文件 `trees[].type` 唯一；`trees` 非空；树 `id` 唯一 | 加载失败，报树字段 | 【锻造】L117 / L165 |
| V2 | **硬** | 节点全局唯一 + 树内一致：节点 `id` 全文件唯一；`node.type` 与所属树 `T-03` 一致 | 加载失败 | L251（id 语义）/ L117 |
| V3 | **硬** | 父节点引用：`parent` 非空时须为本文件已定义节点且同树；`roots` 的每个 id 须存在 | 加载失败，报缺失 id | L369 / L256 |
| V4 | **硬** | 树无环：沿 `parent` 正向遍历，任一节点不得回到自身（环路检测）；每节点可达某一根 | 加载失败 | L369 |
| V5 | **硬** | 分支可达：`branch[]` 每个 id 须为已定义节点；重复分支 id 去重告警 | 加载失败 | L369 / L257 |
| V6 | **硬** | 终点=最终强化：线终点（叶子，无子引用它）必须 `final=true`；`final=true` 不得有子节点 | 加载失败，报叶子/子树 | L117 / L135 |
| V7 | **硬** | `node.item` 引用存在：引用 items.json 装备条目存在 | 加载失败 | L284 / L369 |
| V8 | **硬** | 类型匹配：items 条目类型与 `node.type` 匹配（weapon ↔ 武器类物品，armor_head ↔ 头部防具…） | 加载失败 | L284 / L276 |
| V9 | **硬** | 改造键空间：节点改造键（stats.*/slots/rarity/final…）必须在 items 元数据键空间内（防新键漂移） | 加载失败 | L284 |
| V10 | **硬** | 素材引用：`materials[].item`（M-01）引用存在且为 items.json **材料类**条目 | 加载失败 | L369 / L365 |
| V11 | **硬** | 素材数量/档位：`count`（M-02）≥1 正整数；`tier`（M-03）枚举 `normal`/`rare` 合法；`materials` 每节点 ≥1 行 | 加载失败 | L260-263（语义）/ L144 |
| V12 | **硬** | 等级合法：`level`（N-05）≥1 整数（职业门槛）；根节点建议 =1（W6 提示） | 加载失败 | L255 / L213 |
| V13 | **硬** | 品质合法：`rarity`（N-12）∈ 四档枚举 `normal`/`fine`/`epic`/`legendary`；历史整数 1-4 兼容映射 | 加载失败 | L265 / L342-349 |
| V14 | **硬** | 孔位合法：`slots[].level`（N-09）∈ {1,2,3}（框架 3.5 slot_level） | 加载失败 | L259 / L190 |
| V15 | **硬** | 级联删除复查：级联操作后无残留悬空引用（红名节点父链完整、branch 已清）；/锻造 拒绝红名节点 | 加载失败（残留引用） | L298 / L296 |
| V16 | **黄** | `augmentable=true`（N-15）且 `final=false` 或非武器 | 提示"仅最终强化武器可客制"，不阻断 | L268 / L177 |
| W1 | **黄** | 改造键与 items 同键冲突 | "覆盖生效"提示（不拦截）——节点值覆盖 items 生效 | L284 |
| W2 | **黄** | 元素武器：节点带元素但怪物表无弱该属性的怪 | 警告"该怪无弱点，元素武器无发挥空间"（允许） | L203 / L388 |
| W3 | **黄** | `settings` 关闭 P1/P2 但存在 `sets`/`augments` 数据 | 提示"该段不生效"，不阻断 | L162 / L174 |
| W4 | **黄** | `synth_ratio_3to1=false` 且素材死锁风险（贵稀稀有素材缺替代来源） | 提示素材死锁风险（对策：合成+商店+分解） | L387 |
| W5 | **黄** | 节点总量超规模建议（武器>500 / 防具>800） | 配置负担预警，不阻断 | L130 |
| W6 | **黄** | 根节点 `level≠1` | 提示建议根=1，不阻断 | L213 / L255 |

> V-W 关系说明：V1-V15 为**硬校验**（含 §13 定稿列项：父节点存在/素材引用/树无环/分支可达/等级引用/node.item 引用存在 + items 类型匹配 + 改造键空间 + 级联删除复查，全数落位）；V16/W1-W6 为黄级提示（不阻断）。§12.1.1 L284 四规则 = V7/V8/V9/W1；§12.1.2 L298 复查 = V15。

---

## 六、验收测试用例

> 编号 TC-01 起连续；断言全部自动化可判定。覆盖矩阵：A 派生树结构 5 例、B 节点字段与双源仲裁 5 例、C 素材两档与 3:1/素材即进度 5 例、D 100% 确定性 4 例、E 校验器 7 例、F 级联删除 3 例，共 **29** 例。

### 覆盖矩阵

| 需求类别 | 定稿章节 | 用例 | 条数 |
|---|---|---|---|
| A 派生树结构 | §4 L111-138 / §12.1 L248-270 | TC-01 ~ TC-04b | 5 |
| B 节点字段与双源仲裁 | §12.1 L248-270 / §12.1.1 L272-289 | TC-05 ~ TC-09 | 5 |
| C 素材两档与 3:1/素材即进度 | §5 L140-158 / §3.4 L99-107 | TC-10 ~ TC-14 | 5 |
| D 100% 确定性 | §1.2 L20-32 / §2.1 L47-58 / §3.2 L81-87 | TC-15 ~ TC-18 | 4 |
| E 校验器 | §13 L369 / §12.1.1 L284 / §9 L203 | TC-19 ~ TC-24b | 7 |
| F 级联删除 | §12.1.2 L291-307 | TC-25 ~ TC-27 | 3 |
| **合计** | — | TC-01 ~ TC-27（含 04b/24b 变体） | **29** |

### A 派生树结构

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-01 | 加载合法树示例：武器树 8 级主干 + 每级 2 分支 + 每线 ■最终强化（L120-122 拓扑） | 全字段按 §1.0-§1.2 落库：trees/nodes/roots/节点 15 字段全部可解析；`/锻造树` 分页正常 | 【锻造】L113-123 / L234 |
| TC-02 | 部署武器+防具五部位共 6 棵 `trees`（type 各一） | 加载成功；重复 `type`（两台 `tree_weapon`）→ V1 硬错 | L117 / L165 |
| TC-03 | 炎剑线节点配 `branch:["node_ice_sword"]`，图纸触发分支提示 | `/图纸` 输出 `├─ 分支：冰剑 ← 冰晶矿（需 采集 冰晶）` 一类分支行 | L104-105 / L257 |
| TC-04 | 线终点（叶子）`final=false`；或 `final=true` 节点有子节点 | V6 硬错：叶子必须 final=true、final 不得有子；最终强化后 `augmentable=true` 可解锁客制 | L117 / L135-136 |
| TC-04b | 节点总量 600（超防具建议 800 下）/ 1500 | ≤ 建议值不提示；超 800 → W5 黄提示不阻断（规模为建议非硬约束） | L130 |

### B 节点字段与双源仲裁

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-05 | `node_iron_sword_2.item="iron_sword"`（items.json 存在） | V7/V8 通过；/锻造 完成时实例 = items 基础 + 节点改造合并（快照入存档） | L253 / L277-278 |
| TC-06 | 节点声明 `stats.atk=24`、`slots=[{level:1}]`、`rarity="legendary"` 与 items 同键 | **覆盖**：成品 atk/孔位/品质取节点值（AR-1） | L281 |
| TC-07 | 节点未声明 `stats.def`，items 有 `def=5` | **追加**：成品继承 `def=5`；节点未声明键不丢（AR-2） | L282 |
| TC-08 | 同一节点同时写 `item` 与 `output_item`（别名双写） | 加载失败（别名二选一，不双写，V 扩）；只写其一通过 | L253 / L276 |
| TC-09 | 节点 `rarity` 与 `items.quality` 冲突 / 节点未配置 rarity | 冲突以节点为准；未配置继承 items.quality（AR-3） | L283 |

### C 素材两档与 3:1 / 素材即进度

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-10 | items 材料元数据 `material_tier`：小怪皮/爪 normal、BOSS 宝玉 rare | 素材行缺省 `tier` 派生正确：普通素材/稀有素材两档落位 | L144-145 |
| TC-11 | 素材行显式 `tier:"rare"` 而 items 元数据 normal | 行覆写优先 → rare（双源仲裁 M-03） | L146（升档需求）/ L283（仲裁风格） |
| TC-12 | `synth_ratio_3to1=true`，配 `kind=combine` 实例（3 普通→1 稀有） | 合成成功且产物数值与原稀有素材一致（数值不变）；false=实例禁用 | L146 / L354 |
| TC-13 | `/图纸 炎剑`：持有火龙鳞 1/3、矿石 5/5、铁剑Ⅰ 已锻 | 输出全链 + `当前持有：火龙鳞 1/3 | 矿石 5/5 | 铁剑Ⅰ ✓` | L101-107 / L58 |
| TC-14 | 缺件报错：火龙鳞 1/3 | 输出 `缺：火龙鳞×2（来源：火龙掉落/商店）`，来源取自 M-04/items；无随机奖励干扰 | L84-88 / L240 |

### D 100% 确定性

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-15 | `/锻造 铁剑`（素材+前置+等级齐备） | 一条指令即时出结果，无会话状态（确认/取消/持久会话均不建立） | L51 / L239 |
| TC-16 | 素材够 + 前置已锻 + 等级够 | **100% 成功**：无失败分支、无概率判定；产出状态由三个确定性谓词唯一决定 | L29 / L56 |
| TC-17 | 同输入锻造 1000 次（种子 2026） | 成品属性/消耗/日志逐次完全一致（差分=0）；锻造引擎源码无随机调用（与炼金品质/强化成功率不同源） | L32 / L401 |
| TC-18 | 素材不足 / 等级不足 / 前置未锻 | 仅确定性错误模板输出（缺项+来源+/图纸 hook），不产生随机事件、不扣素材 | L84-88 / L240 / L256 |

### E 校验器

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-19 | `parent` 指向不存在 id；或构造父链成环（A→B→A） | V3/V4 硬错，报缺失 id / 环路路径 | L369 |
| TC-20 | `branch` 指向悬空 id；或节点 `id` 全文件重复 | V5/V2 硬错 | L369 / L251 |
| TC-21 | `node.item` 引用缺失；或 items 条目类型 ≠ `node.type` | V7/V8 硬错 | L284 / L276 |
| TC-22 | `materials[].item` 缺失；或引用非材料类物品 | V10 硬错 | L369 / L365 |
| TC-23 | 叶子 `final=false` / `final=true` 有子（TC-04 双场景） | V6 硬错 | L117 / L135 |
| TC-24 | 节点 `stats.atk` 与 items 同键 | V7-V15 通过 + **W1 黄提示**"覆盖生效"不阻断 | L284 |
| TC-24b | 元素武器节点，怪物表无弱该属性怪 | W2 黄提示"该怪无弱点，元素武器无发挥空间"（允许加载） | L203 / L388 |

### F 级联删除

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-25 | 删除被 forge 节点引用的 items 装备 | 二选一：①红名保留（/锻造 拒绝、/图纸 显示"已失效：物品已删除"）②整棵子树移除+父节点 branch 同步清理；均无悬空 | L294-298 |
| TC-26 | 删除 forge 节点（玩家已锻造该装备） | 已锻造实例**保留旧属性**（快照不受影响）；未锻造子链 ①上提重连（parent→原父的父）②整支移除并清理引用 | L300-303 |
| TC-27 | 级联删除完成后重复跑校验器 | V15 复查通过：无残留悬空引用、红名节点父链完整 | L298 |

---

## 七、登记与对接（落点确认）

| 落点 | 内容 | 引用 |
|---|---|---|
| forge.json | 本细化 §一 schema（trees/tree/node/materials/settings）；`trees[].type` 对齐框架部位（武器/头/身/手/腿/脚） | 【锻造】L244-358 / L165 |
| items.json | `node.item` 引用装备条目（N-03）；素材材料类条目（M-01）+ 材料类 `material_tier` 档位元数据登记建议（§3.1） | L253 / L365 / L144 |
| recipe.json | 3:1 合成＝合成引擎 `kind=combine` 实例登记（N 素材→1 高级素材，数值不变） | L146 |
| formula.json | 元素注册表（8 属性，N-08 `stats.element` 走注册表） | L200 |
| 框架 3.5 | 孔位 slot_level 1/2/3（N-09 slots 复用） | L190 / L259 |
| 框架 6.9 | 级联删除全局语义（forge 树侧规则见 §12.1.2，删除物品→同步 forge 红名/移除） | L291-307 |
| 玩家存档 | /锻造 完成实例化属性快照（AR-5） | L278 / L301 |
| 校验器 | §五 V1-V15 硬校验 + V16/W1-W6 黄提示（顶格定稿 §13 校验器清单） | L369 / L284 / L298 |
| 编辑器 | 装备树可视化（只读预览+下拉选父/分支）+ item 下拉引用 items.json + CSV 装备树表（父/子/素材） | L364-369 |
| 后续细化 | /锻造 /图纸 /锻造树 /分解 指令流程 → 2c2b；铸造职业等级/SP/铸造王熟练度 → 2c2c；套装 P1/客制 P2 字段展开 → 2c2d | L228-240 / L208-224 |

---

*设计依据：锻造系统设计定稿 v1.0.1（410 行，全篇已读；§四 派生树 L111-138、§五 素材 L140-158、§十二 数据结构 L244-358 为直接依据）+ 框架 v2.8（3.5 孔位 / 6.9 级联 / 公式元素注册表）——本文对应 2c2a 数据层；指令流程见 2c2b、职业见 2c2c、套装与客制见 2c2d*
