# 幻觉审查_1e：《细化_1e_怪物八段schema》

> 审查对象：`/root/QBot-TurnTellerRPG/docs/细化/细化_1e_怪物八段schema.md`（v1.0，259 行）
> 对照定稿：`/root/docs_archive/RPG框架项目/怪物模块设计定稿.md`（v1.0.3，396 行，下称**定稿①**）、`怪物行动AI设计定稿.md`（v1.0.1，322 行，下称**AI 定稿**）
> 附加权威：`细化_0_仲裁决议汇总.md` R-01（触发枚举双源冲突裁决，要求 1e 的 A05/R2/R12/TC-08 改引权威枚举）
> 审查方法：逐条抽取声明 → 回定稿/裁决核对（①行号引对 ②内容一致 ③补白显式标注 ④数值有依据）
> 结论：**2 个 P0 / 3 个 P1 / 4 个 P2 —— 评分 C**（R-01 触发枚举修订只做了一半，残留反向归一映射；chains 节点结构编造定稿不存在的字段并可拦截定稿合法配置，均属必改级）

---

## P0（必改）

### P0-1 触发枚举归一方向残留旧表，与细化_0 R-01 及 AI 定稿 §二 相反（R-01 修订不彻底，文档自相矛盾）

**位置（1e 内）**：S4（L15）、1.4 映射表 ②③④ 行（L99-101）、R12（L211）、TC-08③（L233）、TC-14（L239）。

**R-01 裁决原义**（细化_0 L33-36）：
- 权威枚举 = AI 定稿 §二 13 类，其中 `pv_broken / get_up / battle_start` **就是权威名**（AI 定稿 L64-66）；
- 怪物模块定稿 L296 的 `broken / revive / enter_phase / phase_below / …` 为**旧别名**；
- 归一方向 = **旧名 → 权威名**（"broken↔pv_broken、revive↔get_up、enter_phase↔battle_start"）；旧名接受 + 黄提示迁移，不拦截。

**1e 现状**：
- A05（L89）/ S3（L14）/ R2（L201）已按 R-01 改对（权威 13 类 + 旧名别名表）；**但**
- 1.4 映射表（L99-101）仍写：`pv_broken（别名）→ canonical broken`、`get_up（别名）→ revive`、`battle_start（别名）→ enter_phase`——**方向与 R-01 相反**，把权威名标成别名、把旧名当 canonical；
- R12（L211）仍写"pv_broken/get_up/battle_start 接受……提示归一为 broken/revive/enter_phase"——提示把作者往**旧名**上推；
- TC-08③（L233）把 `pv_broken` 标为"旧枚举别名"，与同文 S3/A05/R-01 称为权威相互矛盾；TC-14（L239）用 `enter_phase` 作开场技验收名（权威应为 `battle_start`）；
- S4（L15）"canonical = 枚举名（L296 旧表）"的框架未改写，是 R-01 未清的根。

**证据**：AI 定稿 L64-66（②pv_broken ③get_up ④battle_start 均在权威 13 类中）；细化_0 L30-36（R-01 归一方向）；1e L99-101、L211、L233、L239。R-01 的"需改既有文档：细化_1e（A05 L89 / R2 L201 / R12 / TC-08 L233）✅必改"未执行到位。

### P0-2 chains 节点 schema 与 AI 定稿 §八 冲突，且未标注补白/裁决（冒充定稿结构）

**位置（1e 内）**：1.4 chains 段（L104）、R15（L214）。

**1e 写法**：节点 `{action, chance 0-100, role: opener/anchor/mid/finisher, armor?, condition?}`，自称"结构以 AI 定稿 §七为准（本表只登记不展开）"。

**AI 定稿 §八 实际 schema**（L253-260）：链节点 `{action, chance, role, armor}`，`role ∈ {chain, finisher}`（样本 `role:"chain"` / `role:"finisher"`），`chance` 为十进制触发率（样本 0.8 / 1.0，"建议 ≥80%"），**无 condition 字段**。

**冲突点**：① role 枚举 `opener/anchor/mid/finisher` 在定稿中不存在（AI 定稿 L260 仅 chain/finisher；§6.2 的"起手/锚点/收尾"是文案比喻非枚举值）；② chance 量纲 0-100 与 AI 样本 0.8/1.0 不一致；③ 凭空增加 `condition?` 字段；④ R15 按错误 role 枚举做**拦截**，会拒绝 AI 定稿合法配置（role:"chain"）；⑤ 上述均未按 1e 自身标注约定（L6）标为【细化定型】或标注裁决依据，却自称"只登记不展开"。

**证据**：AI 定稿 L253-260（对）；1e L104、L214（错）。

---

## P1（要改）

### P1-1 AI 定稿行号批量编造：L502 / L512-515 / L524-525 / L529 全部不存在（AI 定稿全文仅 322 行）；"AI 定稿 §七"应为 §八

**出现处**：S2（L13）、F14（L41）、1.4 chains（L104）、R15（L214）、附录 A（L259）、②表"高"行（L145）——共 6 处引用。
**实际对应位置**：
- 连招载体唯一化 / 节点 schema / 校验器迁移提示 = AI 定稿 **L248-266**（§八）；
- 决策管线"连锁队列只由顶层 chains 表驱动" = AI L138（§四 步3）；
- chains 校验器"接受旧形状并提示迁移" = AI L265；
- 高难"40-60 回合" = AI L205（§6.5），非 L529；
- chains/顶层扩展（phases/zone_change/teach_map/pressure）所在 = AI **§八**（L230-246），非"§七"（§七实为博弈体验 L215-226）。F14（L41）、1.4（L104）、1.7（L133）三处"§七"均错。

### P1-2 概率语义规则与 AI 定稿核心规则冲突（必填/拦截 vs 默认值兜底/只建议不限制）

**位置**：A02/A03（L81-82）、R10（L209）、TC-07（L232）。

**冲突证据**（AI 定稿）：
- L25 核心规则 4："probability **默认值 = 0**（漏配=锚点行动，只被链/条件/状态机触发）；参与随机池写 1"；L44 样本 `"probability": 0`——1e A02 标 probability **必填**，堵死"漏配默认锚点"语义；
- L29："所有字段默认值兜底，**校验器只提示不拦截**"；L7 硬约束"只建议不限制"——1e R10 把 `probability ∉ {0,1}`、`weight ≥0`、空随机池定为**拦截**；
- L51："写其他正值等价 1"——1e R10/TC-07 对 `p=2` 之类判**拦截**，与"等价 1"容忍语义冲突；
- L283 校验器："**概率总和为 0 → 黄提示**"——1e R10 同场景升为拦截，与 AI 定稿校验策略（L280-284 硬拦清单仅：引用不存在/类型错误/负数/结构错误）不符。

### P1-3 A03 归一化示例算术错误（1e 自造数值，无依据且自相矛盾）

**位置**：A03（L82）："例：普攻 50/重击 30/蓄力 20 → **5/8、3/8、2/8**"。
**正解**：权重和=100，归一化应为 **1/2（5/10）、3/10、2/10**（=0.5/0.3/0.2）；文档的 5/8+3/8+2/8=10/8≠1，分母 80 无出处（TC-07 的 50/80、30/80 因入池权重和恰为 80 而正确，反衬 A03 示例错误）。定稿 L145 仅给权重值不涉归一化结果，此分数为 1e 自算、错误。

---

## P2（轻微，记录）

- **P2-1** L3 辅助引用文件名写作《BOSS战编排设计定稿》，实际文件名为《怪物行动AI设计定稿》（有"= AI 定稿"自注缓和，建议更正）。
- **P2-2** "PV 档默认 10/75/300 取区间中值"论据不成立：BOSS 档区间 200-500 中值应为 350（L101），300 实为 AI 定稿样本值（AI L236/L291 "pv": 300，示例 BOSS PV 300）；数值可保留但"区间中值"表述需更正（L6、L147、TC-13② 三处）。
- **P2-3** 个别细化定型未按 L6 约定显式标注：F03 tier 默认 `normal`、D03 `after_action:<action_id>` 语法（定稿仅"特定行动后"语义）、chains"链不成环"检查及"环形链=有意的循环连招"解释（AI 定稿无此语义，属自创理由）。
- **P2-4** ②表"低"行来源引 L237-238 混入精英行（数值取自 L237 无误）；F01"全文件唯一"、部分派生规则未逐条标"派生/推出来源"（L18 约定未兑现）。

---

## 核对通过的主要部分（抽查记录）

以下区块行号引用与内容经逐一回定稿核对**一致**，无幻觉：F01-F18 主体（L19-31/L274-285/L336-340 样本与约束）、S01-S09 九键（L40-45）、W01-W04 双维弱点/天然抗性（L55-62/L106-118）、门禁管线顺序（L250）、A01/A04-A08 行动条目与触发参数（L178-183/L279/L295-296）、D01-D04 掉落三类与 reward 同键（L193-212/L299/L307）、L01-L02 图鉴（L221-230/L300）、木桩特例 ⑤ 全表（L330-392 各条）、校验器 R1-R7（L295-301 校验 1-7 逐条相符）、编辑器联动（L303-307）、TC-01~TC-13 中除 P0-1/P1-2 涉及项外预期与校验规则自洽、TC-14 运行时语义（L97-100）正确。S1（L138/L145/L279）、S2、S3、S5 接缝裁决与定稿文本相符（S2/S3 的行号问题见 P1-1/P0-1）。

---

## 评分与结论

**评分：C**（存在 2 个 P0：触发枚举归一方向与 R-01 裁决相反且文档自相矛盾；chains 节点 schema 编造定稿不存在的 role/chance/condition 结构并据此拦截定稿合法配置）。
**一句话**：主体字段级 schema 与定稿吻合度高、木桩/掉落/图鉴部分扎实，但 R-01 触发枚举修订只改了半截（S4/映射表/R12/TC-08③/TC-14 残留反向别名映射），且 AI 行号（L502 等 6 处）整体不存在、chains 结构冲突，必须修订后再发布。