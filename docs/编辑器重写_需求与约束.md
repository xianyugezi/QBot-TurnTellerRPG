# 内容编辑器 · 重写需求与约束（2026-09-13 起）

> 背景：2026-09-13 按用户拍板**删除**了原 Web 内容编辑器（见 `02_遗留登记.md` #86）。
> 本文件是**重写版编辑器的唯一需求来源**：用户逐条拍板，逐条往下加，不得自行发挥。
> 交付纪律沿用既有约定：先出样式稿/方案给用户审 → 确认后再写实现；面向非技术用户；纯配置优先。

## 〇、定位（**最高优先级原则** · 用户 2026-09-13 纠正）

> 用户原话：「不要参考数据包的内容做，web编辑器是后面给策划用户用的，不是专门为这个数据包做的，
> 不要把数据包的专属东西作为通用框架的东西。」

0. **编辑器 = 通用工具**，服务对象是**未来的内容作者/策划**，**不是为 veinborn（或任何单个包）定制**。
   - **禁止**把某个包的专属业务结构写死进编辑器框架：例如「装备就是 10 项属性」「必须有套装/词条/孔位」
     「技能必须有 buff 表」这类**具体业务字段不得硬编码**。
   - **正确形态 = 元数据驱动**：界面读**内容包的模块声明**（`manifest.modules`）与**字段元数据**
     （`qbot_rpg/content/*_models.py` 的 `FieldMeta`：type / label / default / options / 分组）
     动态渲染表单与分区；换包 → 界面随之变化，**编辑器代码不用改**。
   - 真实数据（veinborn 的技能/装备条目）在样式稿与测试中**只作为示例内容**，不得成为框架假设。
   - 判断标准：**把 veinborn 换成另一个包，编辑器是否零改动可用**；做不到的设计一律推翻重来。

## 〇·补、验收口径（批 7 落地 · 2026-09-13）

> 第〇节的判断标准不变；本补记把「换包零改动」从原则落成**可重复执行**的验收（批7 新增登记）。

- **补-1（验收脚本）**：`scripts/editor_verify_packs.py` 遍历内容目录下**实际存在的全部内容包**
  （**动态发现、不写死任何包名/模块名**），对每个包调用编辑器只读 API 层六入口
  （包列表 / 模块树 / 条目列表 / 条目详情 / 条目索引 / 引用候选），断言：
  ① 不抛异常；② 结构完整（模块树 `count = own_count + Σ子`、条目数「模块树 / 条目列表 / 条目索引」
  三处口径一致、`field_count = Σ分组 count`、字段分组归属完整）；③ 字段全部可渲染（每个字段都有
  `type` 与可渲染 `widget`/`control`）；④ 未知 / 缺元数据键走兜底不崩。输出逐包报告表，exit 0/1。
  纳入 `tests/unit/test_editor_pack_switch.py`（含 tempfile 合成包反向探针、反向对照、只读护栏、通用性护栏）。
- **补-2（只读纪律）**：验收脚本与测试**不得写入真实内容包**；需要写盘的兜底探针一律用临时目录；
  测试须断言跑完验收后 `content/` 逐字节未变。
- **补-3（一键启动）**：`scripts/editor_start.sh` 默认 `127.0.0.1:8090`，`--pack` / `--content-root` /
  `--role` 可传；只监听本机，启动即打印安全暴露提示（SSH 隧道 / 内网）；启动失败给人话报错。
- **补-4（口径提醒）**：内容包数量以 `content/` 现场实际为准（本批实测 7 个真实包 + 2 个合成探针），
  任务书写「8 个」与现场不符时以现场为准，脚本按目录动态发现。

## 〇·补二、包展示元数据契约（批A 落地 · 2026-09-13）

> 依据 `docs/编辑器重写_数据包展示元数据下放方案.md`。本契约固定「包怎么声明展示元数据」，
> 换新包时编辑器/框架零改动即可显示中文。

- **文件名与位置**：`content/<包>/field_meta.json`，**纯 JSON**；不声明 → 完全按现状兜底（裸键），不报错。
- **顶层键只有六个**（其余一律报错，防各家写法漂移）：
  `schema_version`（必填，整数，当前只支持 `1`）、`module_labels`、`module_tree`、
  `field_labels`、`field_help`、`group_labels`。
- **形态**：`module_labels` = 对象<模块名, 非空字符串>；`group_labels` = 对象<模块名,
  对象<组键, 非空字符串>>；`field_labels`/`field_help` = 对象<模块名, 展示表>，展示表的一项
  可为**非空字符串**（叶子中文名/说明）或**嵌套对象**（`_label`/`_help` 为本节点文案，
  其余键递归同形态）——批B 起支持嵌套 children（旧包只写字符串仍然合法）。
  `module_tree` 与包 manifest 的 `module_tree` 同形态（数组或「父: 子项」对象）。
  任一形态非法 → 解析层报错，指出**包名 + 出错键**。
- **优先级**：包声明 > 框架兜底。`field_labels`/`field_help`/`group_labels` 覆盖框架同键值，
  未声明键保持框架现状；`module_labels`/`module_tree` 以 `field_meta.json` 优先于 `manifest`
  的同名字段（`module_tree` 为整体替换）。详见方案 §二 优先级细则。
- **批B 起框架只保留结构**：`qbot_rpg/content/field_meta.py` 不再持有 `*_FIELD_LABELS` /
  `*_FIELD_HELP` / `*_GROUP_LABELS` / `*_CHILD_LABELS` / `*_CHILD_HELP` 等纯展示表；
  子字段结构来自自动生成的 `qbot_rpg/content/field_meta_child_structure.py`，中文名/说明由
  `content/<包>/field_meta.json` 承载（迁移脚本 `scripts/migrate_pack_field_meta.py` 幂等导出）。
- **纪律**：真实内容包内**不得**由测试/验收脚本写入该文件（需要时用 tmp 目录拷贝包）；
  框架代码不得写死任何包名/业务键。
- **契约出处**：本节的字段形态 / 优先级细则以《数据包展示元数据下放方案》**§三（目标结构）**
  与 §二（边界与优先级）为准；批D 起「新包自带中文名、编辑器与框架零改动」由探针包
  `content/zz_probe_packmeta/` 验证（方案 §四 批D）。

## 〇·补三、可启用模块目录契约（批8 落地 · 2026-09-13）

> 用户 2026-09-13 点名：顶栏 ⚙ 模块开关——空白包编辑器全空，小白不知道从哪开始。

- **模块目录（可启用清单）来自框架通用知识**，不是从当前包已有声明反推（空白包什么都没有）：
  `qbot_rpg/content/module_catalog.py::FRAMEWORK_MODULE_CATALOG` 登记各**模块键 + 通用中文默认名 +
  一句话用途 + 骨架形态 entry_type + 前置模块 requires**。
- **与「展示元数据下放」不冲突**：**模块种类是框架概念**（框架登记可启用哪些模块、骨架什么形态）；
  模块的**中文名/层级**属包展示声明 —— 包内 `field_meta.json.module_labels` / `manifest.module_labels`
  / `module_tree` 节点 label 声明了该模块中文名 → **优先用包的**，未声明才用框架通用默认名
  （优先级实现 = `qbot_rpg/web/api.py::module_catalog`，与只读层同一口径）。
- **启用的落盘语义**：勾选 → 写入该包 `manifest.json` 的 `modules` + 按 entry_type 创建最小骨架
  （**list → `[]`；map/object → `{}`**；文件已存在则不覆盖）；**校验 → 原子写 → 自动备份 →
  可回退**（复用批2 链路，不得绕过）。取消勾选 → **只从 `manifest.modules` 移除声明，保留数据文件**
  （人话黄提示「数据文件已保留，重新勾选即可恢复」）。
- **校验容忍的边界（不绕过校验器，只圈定作用域）**：空骨架模块自身的深结构红拦（如技能库空）语义是
  「先启用、后填写」→ 黄提示放行；停用被引用模块产生的 R-4 悬空引用 → 黄提示放行（「暂时摘下」）。
  其余任何红拦照常阻断落盘；被容忍项如实以黄提示返回，不静默。
- **依赖提示不硬拦**：目录声明前置关系（如派生链依赖技能、装备属于物品类）；缺前置仍可启用，
  只给黄提示「建议同时启用 X」。
- **空包首屏**：当前包未启用任何模块 → 编辑器给空态引导（「点右上角 ⚙ 选择要做的内容」+
  直接打开面板按钮），不留空白。
- **权限**：GM 只读身份下 ⚙ 面板勾选不可用（禁用视觉 + 说明）；写入层前置 403（与批2/6 一致）。
- **纪律**：框架目录不得写死任何包的专属业务字段；编辑器代码只认模块键与目录声明，换包零改动。

## 一、字段与数据模型（用户 2026-09-13 拍板）

1. **不设「标签」门类**。技能等条目**没有独立的 tags 字段**。
2. **「简述」= 自由文本**（对应内容包 `brief`）：
   - 想写标签就写标签（如 `【伤害】【消耗】【连段】【派生】`）
   - 想写别的文本也行
   - **也可以直接留空**（留空 = 该条目列表里不出简述行）
   - 文件里「字段缺失」与「显式空串」语义不同：**缺失 → 引擎机制兜底生成标签**；**空串 → 作者明确留空，什么都不显示**
     （引擎侧实现见 `qbot_rpg/commands/basic_commands.py::skill_brief`）
3. **「详情」= 自由文本**（对应 `detail`），长文本，界面里给大的多行输入框。
4. 字段名一律**中文显示**（name/type → 名称/类型），且**中文名 + 原始键名并列**显示
   （中文为主、键名为辅，便于用户既看懂又能对上 JSON 键）；中文 label 只放显示层，不动数据层；
   字段无中文名时只显示原始键，不得凭空造词。

## 二、布局（用户 2026-09-13 拍板）

5. **编辑面板是主角**：占主要面积，**不要被挤到角落**（反对「左窄右窄、中间堆表格」的三栏挤压形态）。
6. **列表只需要名字**：条目列表**不必显示多列字段**（技能页只显示技能名即可），避免把空间让给表格。
7. **点一下就看**：点击列表条目 → 直接在编辑面板里显示该条目信息（不需要再点「编辑」按钮、不要弹小窗）。
8. 顶栏保留：内容包名、保存状态、当前身份。
14. **条目列表不用翻页**：列表用**滚动条**承载长列表（条数多时滚动），**不做分页控件**
   （用户 2026-09-13 补充）；滚动条要**细、低对比、不显眼**（自定义 `::-webkit-scrollbar` 细条，
   悬停才稍微明显），不要系统默认粗条抢视线。

## 二·补、编辑面板分区（用户 2026-09-13 反馈）

9. **编辑面板做分区切换（tab / 分段控件）**：右面板顶部一排页签，**默认只显示第一组**，
   其余字段组按需切换；不要把全部字段堆在一屏。
10. **「套装」等派生/附属信息挪到独立分区**，不出现在主属性区。
11. **装备属性必须列全 10 项**（游戏真实属性表 `content/<包>/stats.json`）：
    生命 hp / 法力 mp / 精力 stamina / 聚焦 focus / 敏捷 agi / 专注 foc / 精神 spr / 幸运 lck /
    攻击 atk / 防御 dfn。每项可填加成数值。
12. **技能要有「附加效果」分区**（用户点名：技能附加 buff 之类没做）：条目 = 效果类型 / 目标（自身·敌方）/
    名称 / 数值或层数 / 概率 / 持续；真实形态参考 `content/<包>/skills.json` 的 `effects`
    （`{"type":"mark_add","target":"self","mark":"sword_aura","count":15}`、`{"effect":"sw_aura_shield"}`、
    状态附加等）与 `consume_marks`（印记消耗）；支持多条、可留空。
13. **建议分区划分**（待用户确认）：
    - 技能页：`基本` / `数值` / `附加效果` / `文本`
    - 装备页：`基本` / `属性` / `词条·孔位` / `套装` / `文本`

## 二·补二、左侧导航结构（用户 2026-09-13 再补充）

14. **模块分类与条目列表拆成「两栏」**（用户 2026-09-13 澄清：**是两栏，不是上下两段**）：
    左起 **第 1 栏 = 模块分类导航**（技能 / 物品 / 怪物 / 地图 / 任务 …，来自包的模块声明）；
    **第 2 栏 = 当前模块的条目列表**（只名字，滚动条，无翻页）；**第 3 栏 = 大编辑面板**（占其余全部宽度）。
    三栏并列、各有独立标题条与边界线；**不许**把"技能/物品"这类分类名与"御剑·突/砺脊刃"这类条目名
    混排在同一栏里（用户原话：太挤了）。前两栏保持窄（合计约 350–400px），编辑面板仍是主角。
15. **模块层级（用户 2026-09-13 两次补充，最终形态）**：
    `技能 ▸ 效果 / 状态 / 印记 / 派生链`、`怪物 ▸ 行动`、`物品 ▸ 装备`
    （用户原话：装备是物品的一种；效果/状态/印记/派生链并入技能；行动并入怪物）。
    **全部由包的 manifest `module_tree` 声明**，编辑器不写死任何归并关系（换包/改声明即可，零代码改动）。
16. **装备归入物品**（用户原话：「装备就是物品的一种，归纳进物品里面吧」）：
    模块导航**不并列出现「物品」与「装备」**，而是**层级**：`物品 ▸ 装备`（装备作为物品下的子类/筛选）。
    **通用实现**：编辑器支持**模块层级（父子）**，归并关系由包的元数据声明，编辑器不写死"装备属于物品"
    —— 别的包若把装备声明为独立模块，界面照其声明显示。

## 三、交互取向（沿用既有约定）

9. **面向非技术用户**：字段中文名；新功能必须有引导（Tip / 帮助），不允许「静默」行为。
15. **发现性优先**：能点的东西要看得出来能点；不给用户需要背的隐藏操作。
16. 校验要给**人话提示**（哪一项、为什么不行、怎么改），而不是只报错码。

## 四、样式（待用户选定）

17. 2026-09-13 已出 4 版样式稿供选（暗色专业风 / 亮色卡片风 / 终端极简风 / 猎师主题风），
    初版按「三栏」做的——**因第 5/6/7 条布局约束，需按新布局（窄列表 + 大编辑面板）重做后再定版**。
18. 定版后：技能页 + 装备页各出一张作为实现基准（装备字段更密集：部位/稀有度/攻击/防御/词条/孔位/套装/绑定）。

## 五、待清视觉细则（实机验收发现，逐条销项）

| # | 位置 | 现象 | 备注 |
|---|---|---|---|
| V1 | 字段网格 | 提示文字撑高整行 → 纵向节奏忽松忽紧（对齐正确但视觉错落） | ✅ 批5.2 销项：`_hint` 文案并入 `help_card`（说明卡新增「提示」行），字段行不再常驻渲染 `.hint`（只读视图说明走 `view_note` 同样进卡）→ 有无提示的行高一致；见 `test_editor_batch52_visual.py` |
| V2 | 顶栏 | 版本/包名下拉开宽度不足被省略（`(veinbo..`）；窗口再窄会先坏 | ✅ 批5.2 销项：`packOptionTitle` 把名称/标识/版本/模块数挂 `title`（`syncPackTitle` 同步到 select + `.pkgsel`），窄屏先舍品牌字样（`@media 760px`）、包名/状态可收缩省略号、身份切换固定宽 |
| V3 | 字段控件 | 空值表现不统一：输入框纯空白 vs「暂无 / 无」文字 | ✅ 批5.2 销项：空值拆两类——输入类（text/number/textarea/表格文本数字格）统一占位符 `INPUT_EMPTY=未填写`，只读/列表类统一 `EMPTY_TEXT`（下拉未选保留「（未选择）」）；全编辑器一套 |
| V4 | 工具栏 | 无备份时「回退」按钮仍是可用外观（缺 disabled 视觉） | ✅ 批5.2 销项：新增 `rollbackDisabled(backup,editable,busy)` + `refreshRollbackButton()` 唯一出口（切模块/空模块/身份切换/请求中统一调用，切模块即清 `state.backup`）；真禁用视觉走 tokens（`.btn.danger:disabled` 灰边灰字） |
| V5 | 元数据横幅 | 一段行塞 4 类信息（标签/计数/可编辑/路径），窄屏必折行 | ✅ 批5.2 销项：主信息分段一行（字段数/分组数/可编辑），来源路径收进右侧 `.src` 并挂 `title` 全文（`flex-wrap:nowrap` + 省略号，窄屏不折三行） |
| V6 | 计数口径 | 左栏「怪物 104」= 含子项总数，中栏「26 条」= 本条数 | ✅ 批5.2 销项：`moduleCountLabel` 对含子项模块显示「104（含子项）」+ 悬停明细（本模块 26 + 子项 78），栏头对层级包标注「计数含子项」；叶子模块计数即本模块条数，口径一致 |
| V12 | 派生步骤表 | 引用列宽不足 → 下拉文本被截（`拔脊斩（`） | ✅ 批5.1 缓解：块首行标量列按控件形态分宽档，引用列 `.lb-scalar.ref` 取 2 倍宽（下拉文本不再被裁） |
| V13 | 派生步骤表 | 条件行编辑器撑高行高 → 同行其它列基线错位、观感像表单 | ✅ 批5.1 销项：条件/键值编辑器移出单元格，各自成块占满宽度 → 基线错位自然消除 |
| V14 | 派生步骤表 | 9 列宽表需横向滚动（已用细滚动条），首屏只见前 5 列 | ✅ 批5.1 销项：元素含嵌套子字段的列表改「块状换行」布局（标量一行 + 嵌套各自成块），不再有 9 列宽表；仅纯标量列表仍用表格，放不下时细滚动条兜底 |
| V9 | 列表区块副标题 | 区块标题下仍显示裸键（如 `特殊行动` → `special_actions`） | ✅ 批5.2 销项：关联分区副标题/空态/命中行统一走 `labeledKey`「中文（键）」；后端为 section 补 `field_label`/`local_field_label`（`_path_field_label` 支持 `steps[].to` 列表通配），查不到才回退原始键 |
| V10 | 列表引用列 | 引用选择器 chip 文本被裁成 `(b ▾)` | ✅ 批5.2 销项：引用列加宽档（`.ltable-edit/.ltable-ro th.ref` 170px）+ 下拉框右侧预留箭头区（`select.cin` padding-right，箭头不遮字）+ chip/下拉 `title` 全文（`refSelectTitle` / `refValChips`）+ chip 省略号 |
| V11 | 列表表头 | 表头三行（中文/键/类型）换行偏高 | ✅ 批5.2 销项：表头/块标签只留「中文 + 类型」，键名缩为悬停 `title`（`headerLabelHtml`，键名在 `title` 与说明卡里都可查）；选此方案因单行紧凑且不丢可发现性 |
| V8 | 枚举字段 | `tier` 实际值 `boss` 在候选内却标「当前值·不在候选内」 | ✅ 批3.5 已销项：`selectOptions` 按候选形态归一（枚举=纯字符串 / 引用={id,name}）——原先一律按对象取 `o.id/o.name`，字符串候选读成 undefined，候选渲染为空且当前值恒判不在候选内；见 `test_editor_field_labels.py` |
| V7 | 列表项 | 部分条目名带前导点（`·训练木桩·轻甲`） | 内容侧命名，非渲染问题 |

## 六、术语统一（用户 2026-09-13 拍板）

| 键 | 统一叫法 | 说明 |
|---|---|---|
| `mp` | **法力** | 原仓库混用「灵能 / 魔力 / 法力」→ 统一为 **法力**（含 stats.json、注册/状态/强化面板、装备属性表、模板表） |
| `con` | **体质** | 原装备表写「体魄」、面板写「体质」→ 统一 **体质** |
| `mag` | **法强** | 原混用「魔法 / 魔力」→ 统一 **法强**（法术强度） |
| `pv` | **防护值** | 怪物字段（依据《怪物模块设计定稿》） |
