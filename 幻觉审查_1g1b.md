# 幻觉审查报告 · 细化_1g1b_战斗迁移表.md

> 审查对象：`/root/QBot-TurnTellerRPG/细化_1g1b_战斗迁移表.md`（95 行，约 80 处行号引用）
> 对照定稿：
> - `/root/docs_archive/RPG框架项目/战斗数值层设计定稿.md`（v1.1.1，375 行）
> - `/root/docs_archive/RPG框架项目/效果系统设计定稿.md`（v1.1，497 行）
> 核对方法：通读细化全文 → 抽取全部行号声明与断言 → 逐条回定稿原文比对（①行号引对 ②内容一致 ③补白显式标注 ④数值有依据）。
> 审查日期：2026-08-18

---

## 总体结论

核心迁移表（8 条主迁移 + 6 条辅助迁移）的**时序骨架与行号引用高度精确**：回合死亡判定顺序 L40–68 的全部子引用（45 dot / 46 即死 / 47–48 胜负标记 / 49 先手 / 50–53 击杀 / 54–57 后手 / 58 tick / 59–63 互杀 / 64–65 结束时机 / 66–67 快照边界）逐行核对无误；§四不变量、§五索引、§六自检清单与定稿一致；数值断言（MP 倍率 L164、掉落经济 L167、death_check 四可配 L236–239）全部有原文依据。

幻觉集中出现在**逃跑（FLY/S6、T6、A6）一行**：两处引用不支撑断言（L59 误引、L66–67 补白冒充引用），另有若干行号偏移与未标注补白（P2 级）。配套文档引用（1c1b L57、技能派生 L57）不在本次两定稿核对范围内，仅作提示。

---

## P0（严重幻觉：0 条）

无。不存在"引用完全不存在的行/章节"或"与定稿核心内容相矛盾的断言"。

---

## P1（明显幻觉：2 条）

### P1-1「连段为战斗内资源」误引 L59（T6 行 46、A6 行 63）

- **细化原文**（T6 动作列）：`成功后清空战斗内资源（连段为战斗内资源 L59，对齐 1c1b T7 清零）`；A6 动作列：`成功→清空连段等战斗内资源退出（L59）`
- **定稿原文** 战斗数值层 L59：`⑦ 同回合互杀（双方死亡标记同轮并存）→ 战斗结束 = 平局（可配 player_loss=玩家败）`
- **判定**：①引错——L59 为互杀判定，与"连段"无关，战斗数值层全文无"连段是战斗内资源"的表述（连段资源语义归属技能派生定稿）；③未标——「逃跑成功后清空连段」属设计补白，未显式标注"补白/待补"，却以 L59 引用形式呈现为定稿依据。
- **影响**：实现者若按 L59 追溯会得到错误来源；"清空连段"行为本身为新增规则。

### P1-2「逃跑仅限回合边界」系补白冒充引用（T6 行 46、不变量 4 行 72、自检清单行 91）

- **细化原文**：T6 条件列 `**仅限回合边界**（回合开始前/回合结束 tick 后，L66–67），不打断回合内结算`；不变量 4：`逃跑同样只在边界发起（L66–67）`；自检：`逃跑入口同样受此约束`
- **定稿原文** 战斗数值层 L66–67：`⑨ 与 3.17 战斗中断恢复的先后：中断快照只落回合边界（回合开始前/回合结束 tick 后）……`——两行只定义**中断快照**落点，全文未定义逃跑发起时机。
- **判定**：①引错——L66–67 不含"逃跑"任何内容；③未标——「逃跑仅限回合边界」是设计决策（合理的推断，但仍是补白），三处均以定稿引用形式呈现，未标"补白/待补"。注：同行已将「成功率/回合数/代价/BOSS 禁逃」标"待补"，边界规则本身却未标注，二者自相矛盾地一实一虚。

---

## P2（轻微：5 条）

### P2-1「随机种子」引用无锚点（S0 行 22、T1 行 41）
T1 初始化项 `随机种子与 per-action 收集器（L311–333）`：L311–333 仅为 §8.1 伤害构成收集器（per-action），不含随机种子。随机种子在战斗数值层唯一相关落点为 L180（rng 区间配置）与 L271（编辑器模拟器"固定种子"），战斗实例随机种子持久化为未标注补白。

### P2-2 快照/还原字段清单系未标注泛化（行 13、T7 行 47）
- 行 13 `状态变量……快照随 3.18 battle snapshot 持久化（战斗数值层 L333）`：L333 原文仅指**伤害构成统计**随快照保存，泛化到整个 battle_state/turn_flags 未标注。
- T7 还原字段 `双方 HP/MP、回合序号、dot/冷却/效果驻留……连段/资源轴（L147）`：L66–67 仅保证"恢复后状态=最近回合边界"，未列还原字段；L147 仅一句"多资源轴管理（连段/印记/MP 三轴同时运转）"。字段清单为合理补白但未标注。

### P2-3 效果系统两处行号偏移 1–2 行（T1 行 41 引用列、T8 行 48、§五索引行 82）
- `dot tick 语义连效果系统 L285`：L285 实为 `damage 直接伤害 ｜ heal 回复 ｜ stat_modifier 属性修正`，**dot 在 L286**（`dot 持续伤害（tick 回合开始/结束）`），偏移 1 行。
- `冷却回合单位 L96–97`：回合单位实际在 L94（`冷却时间：默认为 0，回合单位……`），L96 为"默认 3 回合"、L97 为"冷却剩余回合入快照"，偏移 2 行（索引行 82 同误）。

### P2-4 战斗数值层 L334 单引偏移（T4 行 44、T7 行 47）
`持久化登记统一收尾 L334`：L334 实为 §8.1 代码块收尾 ``` 行，"持久化登记由收尾统一执行"在 L333。T4/T7 单引 L334 落空（L333–334 作区间引用则无碍）。

### P2-5 逃跑语义支撑引用偏题（T6 行 46、S6 行 28）
- T6 `退出战场回地图/战前入口（L298）`：L298 为风险表"中断恢复（碎片化续玩）"，不含"回地图"语义。
- S6/T6 `（**待补**，归流程/怪物定稿，L7 转引）`：L7 原文为"木桩入口 /木桩 <档位> 见怪物模块定稿 §十五"，是**木桩入口**转引，与"逃跑规则转引流程/怪物定稿"意图不符。

---

## 附录：全量核对摘要（约 80 处引用，未列者为核对通过）

| 核对区 | 结果 |
|---|---|
| 头部版本声明（战斗数值层 v1.1.1·08-16、效果系统 v1.1·08-14、L40–68/L133–149 章节定位） | ✅ 全对 |
| 回合死亡判定顺序 L40–68 全部子引用（行 45–67 逐行） | ✅ 全对 |
| S1 MP 数值（普攻0/小技5–10/大招15–25，L164） | ✅ 数值逐字一致 |
| S4/T5 掉落经济（金币=怪物等级×基准、素材保底，L167） | ✅ 逐字一致 |
| formula.json death_check 四可配（L236–239、L264、L235 段位置） | ✅ 全对 |
| 效果系统 §3.4 拦截链（L133–149、L142 扣血→L143 死亡判定） | ✅ 全对 |
| 效果系统 §8.3 快照扩展（L425–434，status/marks/resist/triggers/cooldowns） | ✅ 全对 |
| 互杀三态与基准（L59–63、L236–237）、A3/A5 兜底与即时结束（L54–57、L238、L53/L65/L239） | ✅ 全对 |
| §五引用索引（战斗数值层 15 组 + 效果系统 5 组） | ⚠️ 2 处偏移（见 P2-3），其余全对 |

---

## 评分：**B**

**一句话**：8 条主迁移的时序骨架与 L40–68 行号引用全部精确、数值断言有原文依据，但逃跑行存在 2 处明显引用不支撑断言（L59 误引连段、L66–67 补白冒充快照边界），另有 5 处轻微偏移/未标注补白，整体可信、需修 2 处硬伤。