# 细化 · 1g1a 战斗主循环状态集定义

> 来源定稿：`战斗数值层设计定稿.md` v1.1.1（下文简称「定稿」），严格引用。
> 引用重心：定稿 §一「判定顺序与公式总览」含**回合死亡判定顺序**（行 40–68，即回合时序）+ 关联段落（§五 formula.json、4.x 数值经济、§八 stats_collector）。
> 状态集：**战前准备 / 行动选择 / 结算中 / 死亡判定 / 胜利 / 失败 / 逃跑 / 快照中断**（共 8 态）。
> 行号标注：`行 n` = 定稿第 n 行；`待补` = 本定稿未定义，需依赖/挂起，已在各态注明。

---

## 0. 循环骨架（引用定稿时序行 40–68）

```
战前准备（行 45 之前的初始化）
  ↓ 进入主循环
① 回合开始 dot/持续伤害结算（行 45）
② 死亡判定·回合开始即死（行 46–48）── 玩家死→失败标记│怪物死→胜利标记
③ 行动阶段·先手行动结算（含连段/多段，行 49）＋ ④ 击杀判定（每次扣血后即刻检查，行 50–53）
⑤ 后手行动判定（行 54–57）
⑥ 回合结束 tick（持续回合/冷却/效果衰减，行 58）
⑦ 互杀判定（行 59–63）→ ⑧ 战斗结束统一结算（行 64–65）│ ⑨ 快照只落回合边界（行 66–67）
```
- 回合时序对齐：`回合开始 dot/控制结算 → 玩家行动 → 怪物反击 → 回合结束 tick`（行 41）。
- 判定顺序写死不可配（行 15、42、264）；仅 death_check 的「结果/基准/兜底」可配（行 42、236–239、264）。

---

## 1. 战前准备（PREP / 战后亦可作为再战入口）

**语义**：进入首个回合（① 行 45）之前的初始化节点——生成/载入 1g1a 对战双方（玩家侧 + 敌单），快照双方初始属性与战斗期临时状态，确立本场难度档位与统计收集器，然后进入主循环。定稿未设独立「战前准备」章节，语义由下列段落拼出。

**进入条件**：
- 战斗发起（普通遭遇 `/木桩 <档位>` 等入口，入口定义在怪物模块定稿 §十五，行 7 转引）。
- 恢复中断快照后重入（行 66–67）或读档续玩（行 298）。

**关键数据**：
- 玩家侧：9 项基础属性 base+成长（生命/魔法/力量/智力/体质/精神/专注/敏捷/幸运，行 112–123），MP（行 116、161–168），装备白值/强化（行 165–166）。
- 敌侧：三档难度基线 HP/攻击/防御/敏捷/专注/斩杀回合（行 132–137），难度分层机制与 BOSS 阶段（行 139–151）。
- 战斗初始化配置（弱点/元素表、skill_chains 派生封顶、effects 拦截链引用）→ 沿用 formula.json 唯一配置源（行 8、172–242、249）。
- 本场随机数种子 / 收集器初始化（§八 per-action 收集器，行 311–333）。

**引用行号**：行 7、112–123、132–137、139–151、165–166、172–242、311–333。
**待补**：敌单具体配置表指向怪物模块定稿（行 7 转引外稿）。

---

## 2. 行动选择（ACT_SELECT）

**语义**：当前「行动权」侧选择本回合动作——玩家（先手）选技能/普攻/防御指令/技能派生链；敌单（后手反击）按 AI 选定反击。选完即进入结算中（第 3 态）。

**进入条件**：
- 先手：由 ② 转入——本回合开始即死判定通过（双方存活，行 46–48）后进入 ③（行 49）。
- 后手（怪物反击）：先手结算/击杀判定后进入 ⑤（行 54–57）。
- 目标侧无存活目标：后手无目标时 `no_target_action` 兜底（fallback=普攻打剩余目标 / skip=直接跳过；被先手击杀的怪物本身不执行反击，写死不可配）（行 56–57、238）。

**关键数据**：
- 选中的动作标识：技能 ID/普攻/防御指令（防御指令 ×0.5，行 34）；连段/多段结构（行 49，段数进入收集器 recording，行 319 `seg`）。
- 目标选定（含剩余目标兜底，行 56；多目标场景见 ⑤）。
- 资源校验：技能 MP 消耗按倍率（普攻 0 / 小技 5–10 / 大招 15–25，行 164）；MP 三轴资源管理参考（行 147）。
- 倍率预算约束（普攻 100% / 小技 150–200% / 大招 250–400%；派生累计 ≤1.5×，行 125–130、228–229）。
- 防御/输出/控场循环切换用途（行 148）。

**引用行号**：行 34、41、46–49、54–57、125–130、147–148、164、228–229、238。

---

## 3. 结算中（RESOLVE）

**语义**：把「行动选择」产出的动作喂入定稿固定判定管线并逐段执行：`命中 → 会心 → 格挡 → 双通道（物理/元素独立取整相加）→ 防御率 → 伤害拦截链 → 扣血`（行 16），每次扣血后立即插入死亡判定（第 4 态）；同时按「段」写入伤害构成记录（行 314）。

**进入条件**：动作已选定（第 2 态完成）且目标存活 / 存在剩余目标（行 56）。

**关键数据**：
- 逐环判定结果：命中（行 21–22）、会心档 low/mid/high（行 23–24，记录字段 `crit` 行 322）、格挡（行 25，`blocked` 行 323）、破防 `pierce`（行 30、324）。
- 通道值与最终值：物理通道（行 26–30）、元素通道（行 31–33）、总伤害含防御指令/乱数 [0.9,1.1]（行 34–35）、通道末 floor（行 38）、拦截链减伤→护盾→反弹→吸收→免疫→续行→扣血（行 36，顺序以效果系统 §3.4 为准）。
- 拦截链顺序配置→引用 effects_link（行 231–234）；穿透上限 pierce_cap（行 233）。
- 每段记录 schema（source/seg/ch_phys/ch_elem/crit/blocked/pierce/penetrate/weak_type/weak_elem/final，行 317–329）；收集时机=拦截链→扣血后按段写入（行 314）。
- 多段同套内稳定哲学：后续段照常执行（鞭尸浪费，行 51）。

**引用行号**：行 16、21–38、49–51、231–234、314、317–329。

---

## 4. 死亡判定（DEATH_CHECK）

**语义**：死亡/灭团与胜负标记的唯一判定点。两处触发：① 回合开始 dot 结算后即查（回合开始即死，行 45–46、单元格交界）；④ 行动结算**每次扣血后立即检查**（非等整回合，行 50）。判定结果驱动 胜利/失败/平局 三个出口。

**进入条件**：
- 回合开始：dot/持续伤害结算完成（行 45）→ ② 即查（行 46）。
- 结算中：每次扣血后即刻检查（行 50）。

**关键数据**：
- 玩家方：死 → 战斗失败标记；怪物方：死 → 战斗胜利标记（行 47–48）。
- 连段套内击杀：后续段数照常执行；套完结后目标退出战场、下一回合自动消失不再结算行动（行 51–52）。
- BOSS/最后目标死亡：战斗立刻结束（⑧），不清算后续段数（不鞭尸）（行 53、65；公式配置 `boss_end_immediate` 行 239）。
- 同回合互杀（双方死亡标记同轮并存）→ 结束结果=平局（可配 player_loss=玩家败，行 59、236）；判定基准二选一：order「先判定后手」/ hp_ratio「剩余 HP 比」，默认 order（行 60–63、237）。
- 致死时间点依据：先手反伤/同归于尽致死 vs 回合开始 dot 双杀（无先后）→ 平局（行 62）。

**引用行号**：行 45–53、59–65、236–239。

---

## 5. 胜利（VICTORY）

**语义**：战斗以玩家胜利收束。胜负/奖励/掉落/快照清理统一在**当轮事件末尾结算一次**（行 64）；BOSS/最后目标死亡则立即结束、未执行的后续事件作废（行 65）。

**进入条件**：
- 第 4 态产出「怪物死→战斗胜利标记」（行 48）；
- 或 BOSS/最后目标死亡即时触发（行 53、65、239）。

**关键数据**：
- 结算项：奖励 / 掉落（金币=怪物等级×基准、素材保底，行 167）、dummy_log 战报（行 345–352）。
- 战后汇总：伤害构成统计按来源聚合+占比（行 332、339）；木桩战报默认战后全量（行 339–342）。
- 快照清理与持久化登记统一在收尾执行（行 64、334）。

**引用行号**：行 48、53、64–65、167、332–352。

---

## 6. 失败（DEFEAT）

**语义**：战斗以玩家失败收束（含互杀可配为玩家败的情形）。

**进入条件**：
- 第 4 态产出「玩家死→战斗失败标记」（行 47）；
- 或互杀按 `mutual_kill_result=player_loss` 判为玩家败（行 59、236）。

**关键数据**：
- 失败标记随 ⑧ 在当轮事件末尾统一结算（行 64）。
- 掉落/奖励不发放；快照清理同胜利态走统一收尾（行 64、334）。
- 高难长盘下可进入「碎片化续玩」→ 玩家读档/快照恢复再战（行 298）。

**引用行号**：行 47、59、64、236、298、334。

---

## 7. 逃跑（FLEE）

**语义**：玩家主动放弃本场战斗并退出战场。**策划案（本定稿）未定义「逃跑」专属规则**（无成功率/回合数/代价/是否允许 BOSS 逃），此处仅给出与定稿对齐的挂接点并标为**待补**。

**进入条件（建议挂接）**：
- 仅允许在**回合边界**发起（行 66–67：快照与状态只落回合边界）——逃跑不得打断回合内结算，保证无「逃在死亡判定前/后」歧义（同 ⑨ 语义）。
- 战内存在「放弃行动」等价语义：后手无目标时 `fallback`=空挥/待机（行 56–57）——逃跑可视为该兜底的玩家侧版本。

**关键数据（待补清单）**：
- 逃跑判定（成功率公式/回合数/代价）；对战 _BOSS_ 是否禁逃 —— 定稿行 53/65 规定 BOSS 死即结束但未谈逃跑。
- 逃跑后的状态保留：走中断快照语义（行 66–67、298），逃跑即「手动中断 + 立即结算」——需流程/怪物模块定稿补细则（行 7 转引）。

**引用行号**：行 7、56–57、66–67、298。
**待补**：逃跑专属规则归属流程层，本定稿未承担。

---

## 8. 快照中断（SNAPSHOT）

**语义**：战斗持久化/中断恢复状态——满足高难 15 分钟+ 碎片化续玩（行 298）。**中断快照只落回合边界（回合开始前/回合结束 tick 后），回合内不落快照** → 死亡结算天然先于快照写入，恢复后=最近回合边界状态，无「恢复在死亡判定前/后」歧义（行 66–67，⑨ 对齐框架 3.17）。

**进入条件**：
- 回合开始前、或回合结束 tick（⑥，行 58）之后（行 66）。
- 战斗结束收尾统一做快照清理（行 64；3.18 battle snapshot 行 333）。

**关键数据**：
- 状态内容：双方 HP/MP/状态层数、当前回合序号、本场 dot/冷却/效果驻留（①/⑥ 语义，行 45、58）、连段/资源轴进度（行 147）。
- 伤害构成统计随战斗快照保存（行 333），持久化登记由收尾统一执行（行 334）。
- 恢复点=最近回合边界（行 67），随后回到 ①（行 45）继续下一回合。

**引用行号**：行 45、58、64、66–67、147、298、333–334。

---

## 9. 状态机汇总（转移表）

| # | 状态 | 进入自 | 出口 |
|---|---|---|---|
| 1 | 战前准备 | 战斗发起/快照恢复/读档 | → ① 回合开始 dot（行 45） |
| 2 | 行动选择 | ② 即死通过（行 46–48）/先手结算后（行 49/54） | → 结算中 |
| 3 | 结算中 | 动作已选定 | → 往返 死亡判定（每次扣血，行 50）；→ 后手（行 54–57） |
| 4 | 死亡判定 | dot 后（行 45–46）/每次扣血后（行 50） | → 胜利（行 48）/失败（行 47）/平局（行 59）/继续结算（行 51–52） |
| 5 | 胜利 | 怪物死（行 48）/BOSS 即时结束（行 53、65） | ⑧ 统一结算（行 64）→ 结束 |
| 6 | 失败 | 玩家死（行 47）/互杀玩家败（行 236） | ⑧ 统一结算（行 64）→ 结束/续玩（行 298） |
| 7 | 逃跑（待补） | 回合边界（行 66–67） | → 立即结算退出 |
| 8 | 快照中断 | 回合开始前/回合结束 tick 后（行 66） | → 恢复回 ①（行 45） |

- 结束判定统一在当轮事件末尾（行 64）；BOSS/最后目标死立即结束（行 65、239）。
- 互杀/平局：行 59–63、236–237；后手无目标兜底：行 56–57、238。
