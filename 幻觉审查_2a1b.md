# 幻觉审查_2a1b：《细化_2a1b_通道规则与刷怪》

> 审查对象：`/root/QBot-TurnTellerRPG/细化_2a1b_通道规则与刷怪.md`（203 行）
> 对照定稿：`/root/docs_archive/RPG框架项目/副本系统设计定稿.md`（295 行，下称**副本**）、`怪物模块设计定稿.md`（396 行，下称**怪物**）、`时间天气系统设计定稿.md`（431 行，下称**时间天气**）
> 审查方法：通读细化全文 → 抽取全部声明（R1-32、字段表、TC-01~17、附录）→ 回定稿逐一核对（①行号引对 ②内容一致 ③补白显式标注 ④数值有依据）
> 结论：**0 个 P0 / 1 个 P1 / 9 个 P2 —— 评分 B**。全文 50+ 处行号引用几乎全部引对且内容一致，出没/变速/3.16 边界等规则均与时间天气 §4.3 逐字对齐；唯一必改项是 `spawn` 键名顶替定稿 `monsters`/「怪物行」术语且谎称"原样对齐"。

---

## 核对通过的项目（抽样证据）

- **R1 网状结构**：副本 L40（❌线性）/L41（✅网状，节点+边，方向制）逐字命中。
- **R2 4 方向编辑**：副本 L46-47（4 方向编辑/每地图编辑 4 个方向）+ L156（哪里能走哪里不能走）命中。
- **R3-R6 通道四类型**：副本 L48（双向）/L49（单向）/L50（捷径=绕路连通，L43 熔岩坑道捷径实例）/L51（隐藏=满足条件才显示/可走（可选））全部逐字命中。
- **R11 指令分隔**：怪物 L13「/进入 <方向> 保持空格（上/下/左/右）、/进入 <序号> 数字=序号不带 *」逐字命中；副本 L76 走通道追击命中。
- **R12 换区追击**：怪物 L94-95（逃到 XX 地图 → 玩家走地图通道）、L97-99（残血保持/PV 恢复一半/开场技）、副本 L78 交叉一致。
- **R13 副本重置**：副本 L104（安全区休息≠离开副本，不改变位置）、L81（非战斗状态离开副本区域=副本重置）逐字命中。
- **R16/R17/R18/R28 出没+变速**：时间天气 L209（限定过滤、语义与 active_time 对齐并叠加、3.16 边界同适用、'对方逃跑了'）、L210（有效刷新间隔 = respawn_minutes / weight、只影响刷新/补刷不驱逐已在场怪、0=不刷）——全部逐字命中，无越权改写。
- **R19/R30 不打断战斗**：时间天气 L91（每回合重读懒计算、不中断战斗流、天气/季节不驱逐战斗中怪物）命中。
- **R20 并存正交**：时间天气 L235（active_time 与五时段键并存正交 AND 叠加）+ L19（三套周期互相独立）命中。
- **R31/R32 条件引擎新键**：时间天气 L178-180（season/period/weather 互译表）、L182（全局值 vs 按图取值上下文绑定）、L215-217（副本事件等消费方统一引用新键）命中。
- **字段级追溯**：enemy→L141、count→L142、respawn_minutes→L143、active_time→L144+L28、seasons→L145+L40、periods→L146+L41、weather_weights→L147+L210，全部引对、值与定稿样例一致。
- **TC-01~17** 引用（副本 L48/49/47/50/51/75-78；怪物 L94-99、L295-299；时间天气 L142/143/145/146/147/209/210/91/235/40-41/166）全部存在且语义对应。
- **补白标注合格项**：R21 `max_alive` 默认 10、R22 `spawn_weight` 默认 1、附录①③均显式标【工程补白】/「未采用」；1.2 节"条件打开非走过解锁"的结论核对成立（副本通篇无"走过即解锁"持久态定义）。
- **§1.4 schema `exits`**：up/down/left/right 四键 + mode 枚举 + hidden.condition 结构均对应副本 L46-51，地图 ID 取副本示例（molten_*）自洽。

---

## P0（必改）

无。

---

## P1（要改）

### P1-1 刷怪数组键名 `spawn` 顶替定稿 `monsters`，且谎称"样例原样对齐"、未按文档自身约定标【工程补白】

**位置（2a1b 内）**：§2 标题（L91）、§2.1 注释与 JSON（L103-104）、字段表（L122-127）、R21/R22/R23/R26（L138-140/L148）、TC-08~16（L192-200）——全文 20 处"spawn"。

**2a1b 写法**：`"spawn": [ {...} ]`，L103 注释明写「maps.json 地图 spawn 数组（时间天气§L140–148 样例原样对齐）」——即声明与定稿样例逐字一致。

**定稿实际**：
- 时间天气 L139 数组键为 **`"monsters": [`**（L141-147 为 enemy/count/respawn_minutes/active_time/seasons/periods/weather_weights 七字段）；
- 时间天气 L208 / L312 / **L405（§十一 登记表：maps.json 地图顶层 weather_pool + 怪物行 seasons/periods/weather_weights）** 的登记术语为「**怪物行**」；编辑器 §7.2（L312）亦为「怪物行扩展」；
- 副本 L230「地图配置（怪物/采集/宝箱/机制）完整保留」同用"怪物"概念。

**问题**：字段级七键确实与定稿样例一致（"样例原样对齐"对字段成立），但**容器键名被静默改名** `monsters → spawn`：①"原样对齐"表述对键名不成立，构成对来源的失实表述；②未按 2a1b 自定规则（L7"定稿未显式定义的字段/默认值以【工程补白】标注"）标注此行改名；③实施者按拉出 `spawn` 键将偏离时间天气 L405 已登记的「怪物行」落点与编辑器表单（§7.2 怪物行扩展），产生跨文档字段名漂移。字段语义未错，但键名是落库/编辑器绑定名，属必改级。

**证据**：时间天气 L139/L208/L312/L405（对）；2a1b L91/L103-104/L122-127（错）。

---

## P2（建议改）

### P2-1　R22 引用 副本§L43 标「岔道/矿洞 绕路/宝箱/精英」，实际该内容在 L44
**位置**：2a1b L139。副本 L43 =「│ ├─ 熔岩坑道（捷径，单向门）」，「└─ 岔道 ─ 矿洞 ─┘（绕路/宝箱/精英）」在 **L44**。行号偏移 1（同文档 R5 对 L43 的引用正确，故为孤立错引）。内容真实存在，仅行号指偏。

### P2-2　R9 隐藏通道 condition 引用玩家进度——定稿未定义该能力，属未标注补白
**位置**：2a1b L52。副本 L51 仅定义「满足条件才显示/可走（可选）」，未定义条件可引用 quest 子任务完成度；示例 `var:"subquest_done"` 在三份定稿均无出处（条件引擎既有变量大全不在本次对照范围，无法证实该 var 存在）。R9 以 L163-169（子任务形式）+ L156（熟悉通道是探索目标）作为依据属旁证，核心能力（condition 引用进度）应标【工程补白】而未标。

### P2-3　R2「未配置方向 = 死路 + 提示无通道」为未标注补白
**位置**：2a1b L33。副本 L46-47（4 方向编辑）+ L156（哪里能走哪里不能走）间接支持"未编辑方向不可通行"，但"死路默认/拒绝提示文案"为具体化默认值，未按 L7 约定标【工程补白】。

### P2-4　R23「每 respawn_minutes 分钟补 1 只」颗粒度为未标注推断
**位置**：2a1b L140。时间天气 L143 仅定义字段、L210 仅定义有效间隔公式，无"补 1 只/次"的补刷颗粒度声明（3.6 补刷语义为框架层，本次三份定稿未显式写）。推断合理但未标注。

### P2-5　R26 校验规则「count/respawn_minutes 非负整数、respawn_minutes ≥ 1」为未标注补白
**位置**：2a1b L148。定稿样例仅给 count:5、respawn_minutes:10，无"≥1/非负"约束声明；属校验器补白未标注。

### P2-6　R25 的「weather_weights 键 ∈ 注册天气集（硬拦）」引用落点不准
**位置**：2a1b L147。引用 时间天气 L166（weather_pool 元素校验）与 L230（combat.weather_mult.mults 键校验）均为同类"引用检查"近似项；**精确落点应为校验器 V6（L335）**：`消费方枚举引用 ∈ 注册表：鱼种 seasons/periods、怪物 seasons/periods、weather_weights 键……`。主张内容为真，引文非最精确行。

### P2-7　§1.4 校验器 ①③④（to 引用硬拦 / hidden 必带 condition / 双向一致性黄提示）未逐个标【工程补白】
**位置**：2a1b L87。该校验器段无引用列，①③④ 均为工程补白（仅 ② mode 枚举有 L48-51 依据），未按 L7 约定标注；① 可类比 R24 的"引用检查精神"标注方式。

### P2-8　内部交叉引用错位：R26 与 R28 混指
**位置**：2a1b L123、L127 字段表「被 `weather_weights` 变速（R26）」及 R23 内「带天气权重 → 见 R26」——变速语义在 **R28**（时间天气 L210），R26 实为"非负整数校验"规则。功能指向不致误解，但编号错位应修。

### P2-9　§0 总览「移动指令 | 怪物 §一 | L13」章节归属失准
**位置**：2a1b L18。怪物 L13 位于 §一 标题（L15）之前的文档头引用块（指令分隔规范），内容存在，标为"§一"略偏；建议改标"文档头"或 L12-13。

---

## 评分

**B**（0 P0 / 1 P1 / 9 P2）

一句话：行号引用与出没/变速/边界规则对定稿的忠实度极高（R16-R32、TC 全部可追溯），唯一硬伤是 `spawn` 键名静默顶替定稿 `monsters`/「怪物行」并谎称"原样对齐"，加一处必须批准的键名裁决后即可转 A。