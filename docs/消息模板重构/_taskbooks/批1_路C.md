# 任务书 · 批1·路C：位置/进入/移动/到达（explore 样板）

> 2026-09-12 · 消息模板重构战役 · 执行者：子 agent（本任务书为唯一指令源）

## 0. 一句话

把 explore 的「位置/进入/移动/到达」块 **17 个模板 key 按新规范全新重写**（旧文案不作参考），
写入新全量表，从旧分区删除，修相关测试，产出表片段 + 报告。

## 1. 环境与纪律

- 主仓：`/root/QBot-TurnTellerRPG`（**只读参考，禁止改动**）。
- 你的工作区：**`/tmp/tpl_b1c`**（git worktree，分支 `tpl-b1c`）。一切改动只在这里。
- Python：`/root/QBot-TurnTellerRPG/.venv/bin/python`（3.14，pytest/ruff/mypy 已配）。
- 所有命令在 `/tmp/tpl_b1c` 内执行。测试：`cd /tmp/tpl_b1c && /root/QBot-TurnTellerRPG/.venv/bin/python -m pytest tests/unit -q -rN`
- 隔离自检（应先跑）：`cd /tmp/tpl_b1c && /root/QBot-TurnTellerRPG/.venv/bin/python -c "import qbot_rpg; print(qbot_rpg.__file__)"` → 应输出 `/tmp/tpl_b1c/...`
- git：在 worktree 内 `git add/commit`（分支 tpl-b1c）。**不要 push、不要动 main、不要 merge。**

## 2. 规范（必读）

完整规范：`/root/QBot-TurnTellerRPG/docs/消息模板重构/00_方案与规范_v1.md`（主仓路径，比你的副本新）。
速览（本批强制）：

- **全新重写**：旧文案不符合规范、不作参考；保留 **key 名 / 占位符集合 / 触发语义**；功能等价。
- **宽度**：结构化行 ≤ **14 全角**（=28 半角当量；全角=2、半角=1）。超宽必须拆行/精简。
- **介绍类豁免**：地图介绍行（内嵌描述文本）可自然折行——本批把 `explore_map_desc` 登记进
  表的 `meta.prose_keys`（值写理由）。
- **少 ｜ 多换行**；禁空格对齐；零装饰 emoji（仅 ✅❌ + 「」【】｜→×）。
- **指令免斜杠**：「发 位置」「发 进入 下」「发 怪物 锁定 1」；参数占位 `<…>`；多选一用「或」。
- **错误行**：❌ + 原因（+ 可选「下一步」），≤14 全角/行。
- **大数值**：数值与单位紧贴；行设计按数值 7~9 位仍不折行。

## 3. 范围 = 以下 17 个 key（仅此范围，其他一律不碰）

| # | key | 现文案（⚠️仅供信息要素核对，不是改写底稿） | 占位符 | 渲染/调用点 |
|---|---|---|---|---|
| 1 | explore_enter_ok | `✅ 你来到了「{name}」` | name | _render_enter L240（到达首行） |
| 2 | explore_map_desc | `地图介绍：{desc}` | desc | _render_enter L243（介绍行；本批登记 prose 豁免） |
| 3 | explore_monster_line | `活动怪物：{items}` | items | _monster_line L183（items=各行 join，见下） |
| 4 | explore_monster_row | `{i}.{nm}×{cnt}` | i,nm,cnt | _monster_line L182（怪物条目行） |
| 5 | explore_monster_overflow | ` …` | — | _monster_line L185（>5 只折叠尾） |
| 6 | explore_channel_row | `{dir}：{name}` | dir,name | _channel_lines L205/214（通道行） |
| 7 | explore_tip | `Tip:发送'位置'即可查询当前位置信息` | — | _render_enter L254（尾行） |
| 8 | explore_enter_dungeon | `✅ 你进入了「{name}」（副本）` | name | _render_enter L237 |
| 9 | explore_enter_fail | `❌ {reason}` | reason | _render_enter L233 |
| 10 | explore_enter_fail_reason | `无法进入` | — | _render_enter L232（兜底 reason） |
| 11 | explore_enter_noarg | `❌ /进入：输入方向（上/下/左/右）或副本入口（序号/名称）` | — | explore_commands L451 |
| 12 | explore_enter_not_wired | `❌ 进入功能未接线（引擎未加载）` | — | L456 |
| 13 | explore_enter_engine_error | `❌ 进入失败（引擎返回异常）` | — | L478 |
| 14 | explore_leave_battle_ok | `（已离开锁定怪物所在地图，战斗解除）` | — | L539（移动离开后附句） |
| 15 | explore_position_unknown | `❌ 当前位置未知：{loc}（/进入 探索地图）` | loc | L423 |
| 16 | explore_position_unknown_none | `无` | — | L422 |
| 17 | explore_register_gate | `❌ 请先 /注册 创建角色（/注册 名字 职业）` | — | L53 |

**排除（别碰）**：`explore_rest_*`、`explore_map_*`、`worldtime_*`、`weather_*`、`explore_monster_header`、
`explore_monster_empty`（属其他批次）。

## 4. 关键设计约束（本批特有）

1. **多怪物行拆行**：现 `活动怪物：` + 空格 join 的条目易超宽。改造允许**改渲染拼接**：
   `_monster_line`（L155-186）可改 join 与模板组合，使多怪物（≥2 只）时每条目独占一行
   （首行 `活动怪物：` 或首个条目同行——只要每行 ≤14 全角）；单只怪物可同行。
   溢出标记 `…` 相应处理（长度 ≤3 全角，可保留在末行尾）。
   允许改 explore_commands.py 的**行拼接**（不碰路由/数据/状态机/结果结构）。
2. **通道行**：`上：xxx` 形状保留；确保长地图名（按 6 全角估）仍 ≤14。
3. **位置未知兜底**：语气与其它错误行统一（❌ + 原因 + 建议），免斜杠。
4. **介绍类**：`explore_map_desc` 保持「地图介绍：」前缀 + 原文（不折行、不裁剪）。
5. 其他行的设计自由度在你——按规范重写，风格与主 agent 样例对齐（见 §5）。

## 5. 风格样例（主 agent 已向用户展示过的样板方向，必须对齐）

```text
【到达块 · 位置/进入 输出形态】
✅ 你来到了「断脊丘陵」
地图介绍：丘陵如断脊起伏，荒原狼群在丘脊上逡巡，啃食骸骨碎屑。   ← 介绍类，允许折行
活动怪物：
1.荒原狼×3
2.脊冢幼兽×2
下：骸骨草场
左：裂谷风口
Tip:发 位置 查看当前地图
```

```text
【样式规则例】
样例①  职业：脊剑士（推荐新手） ｜ 位置：龙骨驿站   →   职业：脊剑士（推荐新手）
                                                    位置：龙骨驿站
样例②  你 226/400 | 荒原狼 598/622 → 攻击…          →   你 226/400
                                                    荒原狼 598/622
样例③  未达标：品质降级 ｜ 退 50% 材料               →   未达标：品质降级
                                                    退 50% 材料
```

## 6. 工作清单（顺序执行）

1. 读 `/tmp/tpl_b1c/qbot_rpg/commands/explore_commands.py`（L40-260、L400-480、L520-560）+
   `qbot_rpg/core/templates/explore_tpl.py`，核对每个 key 的占位符与场景。
2. 设计 17 条新文案（自检：逐行宽度 ≤28 半角；命令免斜杠；占位符集合不变）。
3. **写表**：把新文案写入 `/tmp/tpl_b1c/qbot_rpg/core/templates/template_table.json`
   - `templates` 下新增这 17 个 key（**只加你的 key**；用 python 脚本插入，保持 indent=2、ensure_ascii=False）；
   - `meta.prose_keys` 加 `"explore_map_desc": "介绍类：内嵌地图描述文本，允许自然折行"`。
4. **删旧**：从 `explore_tpl.py` 移除这 17 个 key（`DEFAULT_TEMPLATES` 与 `PLACEHOLDER_WHITELIST` 两处都要删）；
   其余 key 原样保留。
5. **改拼接**（按 §4.1 允许范围）：多怪物拆行等。
6. **自校验**：
   - `cd /tmp/tpl_b1c && /root/QBot-TurnTellerRPG/.venv/bin/python scripts/check_template_width.py` → 0 FAIL；
   - `python -m pytest tests/unit/test_template_table.py -q` → 全绿；
   - 试渲染（可选）：`QBotRPG_DB_PATH=/tmp/b1c.db python scripts/qq_group_sim.py --file <你的指令文件>` 看实际输出。
7. **修测试**：`python -m pytest tests/unit -q -rN` 全量跑；修「explore 新文案」导致的旧断言
   （含 `scripts/e2e_m4_smoke.py` 等内嵌断言，如有）。**非 explore 的失败不要动**，报告中列出（应为 0）。
8. **硬编码扫描**：grep `explore_commands.py` / `world/movement.py` 中玩家可见中文串
   （排除本表 key 与注释/docstring），列清单：已迁 / 应迁（给出建议 key）/ 豁免（理由）。
9. **落 fragment**：`/tmp/tpl_b1c/docs/消息模板重构/_fragments/b1c.json`：
   `{"rail":"b1c","templates":{...全部17条...},"prose_keys":{"explore_map_desc":"…"},"notes":{"key":"设计说明一句话"}}`
10. **提交**：`git add -A && git commit`（建议 1-2 个提交：文案+表 / 测试同步）。不要 push。
11. **报告**（回复正文，全文内联）。

## 7. 交付与报告格式（回复必含）

```
【fragment 路径】/tmp/tpl_b1c/docs/消息模板重构/_fragments/b1c.json（已提交: <短hash>）
【新文案全文】key: 文本（17 条逐条列出；换行用 \n 标注）
【变更文件】列表 + 每个一句话
【测试】tests/unit 全量：X passed / Y failed / Z skipped（failed 列表或「无」）
【宽度校验】checker 输出原文（0 FAIL）
【硬编码扫描】清单（已迁/应迁/豁免）
【未完成/存疑】如实（无则「无」）
【commit】短hash + 标题
```

## 8. 中止纪律

如遇工具/配额中断：**优先保证 ① fragment 落盘提交 ② 表/分区/测试改动提交 ③ 报告如实说明已完成部分**。
不要谎报完成度、不要跳过提交。若某测试确无法在本轮修完，如实列出。
