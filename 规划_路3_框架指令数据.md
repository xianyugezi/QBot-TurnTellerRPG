# 规划_路3：框架指令与数据（QBot-TurnTellerRPG 实现层）

> 版本：v1.0 · 2026-08-16
> 定位：新仓库 QBot-TurnTellerRPG 实现层规划·路3——**框架指令基础设施 + 数据层（内容包 JSON schema / loader / 校验器 / 属性与成长 / 消息模板）**
> 追溯原则：**每功能可追溯定稿**——每个原子任务标注【定稿依据】（文档 + 章节），实现时代码注释须带来源（开发规则文档 §1.4），代码与文档不一致 = P0 缺陷（§9.1）
> 前置：本路为独立可执行规划；M1 提供最小工程骨架保证自举，不依赖其他路
> 输入定稿（6 份，路径 /root/docs_archive/RPG框架项目/）：
> 1. RPG回合制框架设计文档.md（v2.19）→ 下文简称【框架】
> 2. 玩家属性方案定稿.md（v1.2）→ 下文简称【属性】
> 3. 指令分隔符统一规范.md（v1.1）→ 下文简称【分隔】
> 4. 消息前缀功能设计定稿.md（v1.0）→ 下文简称【前缀】
> 5. 开发规则文档.md（v1.0）→ 下文简称【规则】
> 6. 单机向体验包设计定稿.md（v1.0）→ 下文简称【单机】

---

## 0. 总体架构（平台无关，NoneBot 仅壳层）

```
┌──────────────────────────────────────────────────┐
│ 壳层（NoneBot 专属，唯一允许 import nonebot）      │
│   plugins/rpg_framework/__init__.py  只装配        │
│   plugins/rpg_framework/commands.py  路由/权限/频率 │
│           │ 调用（翻译异常为人话，不拼业务文案）    │
├──────────────────────────────────────────────────┤
│ 引擎层 engine/（纯规则，零 NoneBot import）        │
│   attributes.py 三层属性管线/会心命中格挡纯函数    │
│   levelup.py 成长 | inventory.py | equipment.py    │
│   battle.py 战斗状态机（后续路挂接，本路留接口）    │
│   worldtime.py 时间系统（后续路挂接）              │
├──────────────────────────────────────────────────┤
│ 状态层 state/（零 NoneBot import）                 │
│   character.py / game.py / storage.py（SQLite）    │
├──────────────────────────────────────────────────┤
│ 内容层 content/（零 NoneBot import）               │
│   loader.py 按 manifest 声明顺序加载/热重载        │
│   validator.py 红拦五类+黄提示（人话）             │
│   registry.py 三注册表（效果/行动/派生链）          │
├──────────────────────────────────────────────────┤
│ utils/ 渲染（消息合并/分页/前缀）、通用工具         │
└──────────────────────────────────────────────────┘
```

- **分层铁律（【规则】§1.1，硬约束）**：引擎层不 import NoneBot（engine/state/content 必须可脱离 NoneBot 单测）；依赖方向单向：commands → engine/state/content → utils；禁止反向依赖、禁止循环 import。
- **目录结构（【框架】§14）**：plugins/rpg_framework/ + tests/ 同仓；新增能力先放对层，放错层视为架构缺陷（评审拦截）。
- **范围边界**：本路实现指令**基础设施**与框架级指令（/注册 /角色 /状态 /背包 /位置 /地图 /进入 /帮助 /设置）、属性与成长、数据层全量；战斗/商店/签到/任务/探索/生活等**业务指令**由后续路在本路基础设施上挂接（本路提供路由骨架/解析/权限/频率/错误模板/会话管理器，battle.py 仅留状态机接口与快照协议）。

---

## 1. A 组：平台无关架构（NoneBot 仅壳层）——5 任务

### 【A1 仓库骨架与分层铁律】
- 【定稿依据】【规则】§1.1（目录结构/分层铁律/依赖单向）；【框架】§2.1（分层）、§14（文件结构）
- 【实现要点】按【框架】§14 建立 plugins/rpg_framework/{__init__.py, commands.py, engine/, content/, state/, editor/, utils/} 与同仓 tests/；__init__.py 只做装配不写业务；编写 import 图静态检查脚本（扫描 engine/content/state 的 import 语句，断言无 nonebot 包）；editor 本期仅留目录占位（编辑器属后续路）。
- 【验收标准】① import 图检查脚本全绿：engine/content/state 零 NoneBot import；② 在无 NoneBot 依赖环境下 pytest 可运行 engine 单测；③ 目录与【框架】§14 一致，无新建顶层模块。

### 【A2 领域模型与类型约束】
- 【定稿依据】【规则】§1.3（类型标注强制/dataclass+TypeAlias/禁裸 dict 满天飞）、§1.2（命名规范）、§1.4（注释写"为什么"带定稿来源）
- 【实现要点】领域类型用 dataclass/frozen dataclass 表达：StatDef、PlayerAttributes（三层）、ContentPack、Registry、SessionSnapshot 等；存档/内容包结构断言全部收敛在 content/validator.py（禁散落 `if "xxx" in data`）；公开函数完整类型标注（Python 3.9 兼容 typing 写法）；模块头部 docstring 一句话职责+引用定稿章节；硬编码数值一律常量或内容包字段。
- 【验收标准】① mypy 通过；② 代码评审清单 F 项（类型标注齐全、注释含设计来源）可勾选；③ 新增字段必须先加字段元数据行（【规则】§2.2）——以 tests 断言元数据表与 JSON Schema 消费方同源。

### 【A3 commands 壳层（路由/权限/频率/错误翻译）】
- 【定稿依据】【框架】§2.1、§7.1（指令表）、§7.5（统一错误反馈模板）、§3.9（GM 权限/静默）；【规则】§1.6（引擎层不拼用户文案，commands 翻译）、§8.1（频率阈值）
- 【实现要点】on_command 注册框架级指令（/注册 /角色 /状态 /背包 /位置 /地图 /进入 /帮助 /设置）；三级权限（机主/GM/普通玩家，数据库配置，不自动授予群管理；无 GM 权限使用 GM 指令 → 直接无视不报错不提示）；频率控制（全局每指令 ≥1s / 单群每分钟 ≤20 条 / 单日总量上限）；错误模板统一"原因 + 正确用法 + 下一步建议"；message_id 幂等键防重复结算；per-player 串行队列（同一玩家指令按到达顺序处理）；业务指令（战斗/商店/签到/任务等）本路只留注册骨架与统一管线入口，业务逻辑由后续路挂接。
- 【验收标准】① 无 GM 权限者发 GM 指令 → 无任何响应（静默断言）；② 所有用户可见错误含三要素（原因/正确用法/下一步）；③ 同玩家并发两条指令按序处理；④ message_id 重复投递不双结算。

### 【A4 存储基座（SQLite/幂等/事务/防抖）】
- 【定稿依据】【框架】§3.7（存档/碎片化续玩/自动保存）、§10（双存储）、§15.2（并发竞态）、§15.4（SQLite 并发）、§15.9（事务）；【规则】§1.5（aiosqlite/防抖 200ms/战斗强制落盘）、§四.3（存档往返）
- 【实现要点】aiosqlite 独立连接 + 单写连接队列 + busy_timeout（禁止多连接并发写）；自动保存防抖合并（200ms 内合并为一次落盘），战斗结算强制落盘；存档 key 规则（【框架】§10.2）：玩家=QQ 号（全局跨群/私聊共享）、世界=全局、群号仅作消息来源记录字段不是存档键；每指令后事务提交、崩溃恢复；存档含 content_pack_id + version（字段级迁移：缺补默认/多忽略）；存档往返 round-trip 硬约束（存什么→读什么→完全一致）。
- 【验收标准】① 并发双购买只扣一次（事务断言，故障注入见 G3）；② 崩溃恢复后存档完好；③ round-trip 测试对每类快照断言完全一致；④ 自动保存防抖合并生效（200ms 内多次变更一次落盘）。

### 【A5 配置装配与模块开关】
- 【定稿依据】【框架】§4.1.1（manifest 模块声明）、附 K.2（三态 full/simple/off）、§6.12（数据包管理/单包启用/数据包保护）；【规则】§2.1
- 【实现要点】启用内容包唯一（机主经编辑器切换，插件读取当前启用包）；manifest.modules 决定加载集合（不声明=系统不加载，向后兼容）；模块 mode 三态解析（alchemy / fishing / time_cycle 等，off 时模块指令提示未启用、相关条件键失效）；数据包保护开关（编辑期间玩家指令返回"作者更新中，请稍后再试…"）；插件入口只做装配。
- 【验收标准】① manifest 声明缺失模块 → 黄色提示不拒绝、该模块不启用；② 未声明的文件存在 → 不加载（防误启用）；③ off 模块指令 → "未启用"人话提示；④ 数据包保护开启时玩家指令被挡。

---

## 2. B 组：玩家属性三层（白值/加成/临时）与职业成长——6 任务

### 【B1 stats.json 数据层与键空间】
- 【定稿依据】【属性】§一（9 项属性表）、§八（stats.json 定义逐字段）、§九（注册制/迁移）；【框架】§4.2.2 stats 三层字段、§4.2.3（虚拟属性键空间）、附 G.1（资源注册段）
- 【实现要点】9 项默认模板逐字段落地：hp/mp（type=resource）与 str/int/con/spr/foc/agi/lck（type=combat，含 base/growth/role/mh_map），数值与【属性】§八 一致（hp 100/growth 10、mp 30/4、战斗属性 10~15、lck growth 0.8）；自定义属性注册制（注册即入键空间，或 x_ 前缀）；键空间 = stats.json 注册属性 ∪ {crit_bonus, crit_mult_up, elem_res} ∪ {ele_atk_<e> | e ∈ formula.json 元素注册表}（元素增删自动增减键）；资源型属性注册段（base/max/reset/display，【框架】附 G.1）；mh_map 怪猎映射桥接旧包（atk→str、mag→int、def→con、spirit→spr、luck→lck）。
- 【验收标准】① 模板 JSON 与【属性】§八 示例逐字段一致（单测比对）；② 未注册键 → 黄提示"属性『xxx』未注册，确认是自定义还是笔误？"；③ 元素键的元素不在注册表 → 黄提示；④ 类型错误/负数 → 红拦。

### 【B2 三层计算管线（写死顺序 + 药剂落层）】
- 【定稿依据】【属性】§五（三层结构与计算模型、防无限叠乘）、§五·药剂；【框架】§4.2.2 stats 管线注释
- 【实现要点】管线写死（结构依赖）：① 基础合计 = 白值 + 加成层 flat（装备固定 + 药剂固定）；② 加成后属性 = 基础合计 × (1 + 加成层 pct)；③ 条件加成基于【基础合计】触发（防循环，如"每点力量+1 智力"）；④ 战斗临时基于【加成后属性】计算；最终 = 加成后 × (1 + temp%) + conditional；药剂落层规则（用户拍板）：战斗外服用 → 加成层（现实时间计时、带到期时间、到期自动移除重算），战斗内服用 → 临时层（回合计时、回合结束 tick 扣减、入战斗快照、战斗结束清除）；同类药剂不叠加（新覆盖旧）；虚拟键（ele_atk_* / elem_res / crit_bonus）运行时进加成层 flat 参与管线。
- 【验收标准】① 确定性单测：给定白值/装备/药剂 → 精确最终属性（含 pct 与 temp 组合用例）；② 战斗外药剂到期自动移除并重算；③ 战斗内药剂战斗结束清除、中断恢复不重置（临时层入快照）；④ 无无限叠乘（pct 基于基础合计——构造 50%+50% 装备断言 ≠ 基础×2.25）。

### 【B3 会心/命中/格挡纯函数模块】
- 【定稿依据】【属性】§二（会心体系 A+B）、§三（命中/格挡/格挡减半）、§三.3（判定顺序写死）；【框架】附 A.2（formula.json 参数段：crit/hit/block，跨文档仲裁：命中 K 以 formula.json hit.k 为准，默认 0.2）
- 【实现要点】engine/attributes.py 纯函数（不依赖战斗状态机）：会心率 = min(cap, √最终幸运 × 0.5% + crit_bonus)（cap 默认 95，0=不限）；会心倍率 P = √幸运 ÷ 2，随机 r∈1~100：r≤P 高级 2.2 / r≤P×3 中级 1.7 / 其余低级 1.3，超会心 Lv1-3 各 +0.05×等级；命中率 = clamp(专注/(专注+K×对方敏捷), 10%, 95%)，对方敏捷 0 → 100%、双方 0 → 100%（0/0 兜底）、怪物敏捷缺省按等级换算兜底；格挡率 = min(40%, 专注/(专注+150))，魔攻击无视格挡可配，格挡=会心后最终伤害减半（与 /防御 乘算），格挡减半特色按 1v1 一轮单次攻击不消费、组队模式预留；判定顺序接口（命中→会心→格挡→双通道→防御率→拦截链→扣血）写死，由战斗路调用；全部参数读 formula.json（damage/hit/crit/block/defense/weakness…）不硬编码。
- 【验收标准】① 幸运 100 → P=5：高级 5% / 中级 10% / 低级 85% 断言；② 会心率封顶 95%（crit_bonus 堆高用例）；③ 专注=0 命中下限 10%；④ 格挡上限 40%；⑤ 伤害下限 max(1,…) 边界由战斗路覆盖，本路断言参数读取路径（测试用独立 formula fixture，禁硬编码生产参数——【规则】§4.4）。

### 【B4 成长引擎 levelup】
- 【定稿依据】【属性】§六（成长设计：自动成长+自由加点可选=白值层、属性点保留）；【框架】§3.2（成长引擎）、§3.13（等级上限/升级回满/经验转货币可选/战后恢复默认关）
- 【实现要点】经验曲线内容包配置（editor 后续路）；升级全属性自动成长（职业成长率 jobs.json 可配）+ 自由加点可选（加点=白值层，内容包决定）；升级 HP/MP 回满；等级上限可配（如 45），满级经验不再增长（或经验转货币可选）；战后恢复默认关闭（花钱治疗=资源循环），可配战后治疗比例/安全区回复；经验曲线边界（0 经验/满级不增长/升级回满）。
- 【验收标准】① 0 经验、满级不增长、升级回满 HP/MP 边界单测（【规则】§4.2 必测表 levelup 行）；② 换职业不重算已加点（偏科=自然成本，【属性】§六）；③ 等级上限/战后恢复开关可配生效。

### 【B5 职业接线 jobs】
- 【定稿依据】【框架】§4.2.2 jobs 段（说明卡 + 职业变换 + 资源轴）、§6.12（进阶职业卡片继承规则：挂谁下继承谁，配置驱动）；【属性】§六（默认四职业成长率示例）
- 【实现要点】jobs.json 字段接线：difficulty/playstyle/recommended_newbie/resource_axes/mechanic_tags/transform（transform_skill/transform_to/duration/revert/cooldown/state_policy/skill_set/equip_restrict/derive_chains）；默认四职业成长率落地（战士 力量2.0/体质1.5/专注1.0；法师 智力2.0/精神1.5/幸运1.0；刺客 敏捷2.0/专注1.5/幸运1.0；盾卫 体质2.0/生命1.5/精神1.0）；进阶职业继承=配置驱动（不在进阶卡片下挂的职业转职后不继承）；职业成长率接入 B2 管线（每级白值 += 白值×growth×职业成长率口径按内容包）；transform 校验（battle 模式配 revert=true → 黄提示矛盾）。
- 【验收标准】① 四职业成长率升级后属性增量断言；② 进阶继承规则（挂在谁下继承谁）用例；③ transform battle+revert 矛盾黄提示；④ jobs 元数据表（编辑器/CSV/Schema/校验四处共用）注册完整。

### 【B6 属性持久化与迁移】
- 【定稿依据】【属性】§九（迁移：mh_map 映射/存档键级迁移/改名提示）；【框架】§3.7（存档字段级迁移）、§3.3（potion_use_counts 入快照）、§3.18/A.5（临时层/状态入战斗快照）；【分隔】§6.6（快捷表存储随存档）
- 【实现要点】玩家存档属性三层键（base / bonus.flat / bonus.pct 带到期时间 / temp）；potion_use_counts 快照（中断恢复不重置、回合推进后限次重置）；mh_map 旧包桥接迁移（atk→str 等，键级迁移）；字段级迁移（缺补默认/多忽略）；改名提示（校验器黄提示"『防御』已改名『体质』"）；临时层/加成层随战斗快照与中断恢复协议（A.5）对齐。
- 【验收标准】① 旧键存档（atk/mag/def/spirit/luck）迁移后属性与 mh_map 映射一致；② 缺字段补默认/多字段忽略（构造旧 schema_version 存档断言）；③ 中断恢复后临时层/药剂冷却计数不重置（round-trip）。

---

## 3. C 组：指令解析——7 任务

### 【C1 分隔符解析器（七符号 + 三条铁律）】
- 【定稿依据】【分隔】§一（分隔符总表）、§二（三条铁律）、§三（命名铁律）、§四（解析管线）
- 【实现要点】统一解析：`/指令名 [位置参数1] [位置参数2|固定子词] [键值列表]`；七符号职责（空格=唯一参数分隔；`*`=数量；`,`=列表；`=`=键值；`+`=等级/数值；`-`=连招/区间；`>`=路径/派生）；参数内解析顺序固定：先按 `,` 拆列表 → 再按 `*` 拆数量 → 再按 `=` 拆键值；名称优先（`*` 左侧先精确匹配名称，失败再按序号）；数量上限默认 99 可配（超限提示不拦截）；`/强化` 禁止 `*`（+N 是等级标记不是数量）；固定子词表（追加/预览/自动/查看/确认/放弃/续）为解析器内置常量，优先于物品匹配；错误模板统一 4 类：缺参/超参/未知分隔符/保留字符违规；命名铁律（物品/配方/装备/特性/珠/套装/技能/行动名禁空格与 `* , = + /`，允许 ·、Ⅱ、-、数字）→ 保留字符检查黄提示。
- 【验收标准】① 规范示例全通过（/使用 经验药水*10、/投料 赤铁矿,火药,硫磺粉、/强化 铁剑+5、/攻击 1-2-3、/锻造 铁剑>铁剑Ⅱ、/雇工 助手名 代采=矿石*5,代调=药剂*2）；② 4 类错误模板各至少 1 断言；③ 数量 100 → "最多一次使用 99 个"提示不拦截；④ `/攻击 2`=序号、`/使用 经验药水*10`=数量 判定正确。

### 【C2 紧凑 + 空格双认】
- 【定稿依据】【分隔】§6.3（紧凑+空格双认）、§七（兼容策略：粘合写法永久兼容/旧格式过渡识别）
- 【实现要点】紧凑与空格等价解析：攻击2=攻击 2、道具3=道具 3、使用经验药水*10=使用 经验药水*10、进入上=进入 上、进入2=进入 2；判定规则：指令名后紧跟数字=序号、含 `*`=数量、汉字=方向/名称；提示展示紧凑格式（"下次可以这样写：攻击2"）；旧"物品 + 纯数字"空格数量格式过渡期自动识别为数量并回显新格式提示（/合成 药水*10）；粘合写法（背包2）永久兼容（正则回退）。
- 【验收标准】① 紧凑/空格矩阵等价断言（各快捷指令清单，【分隔】§6.4）；② 提示文案含紧凑格式示例；③ 旧格式"使用 经验药水 10"按数量处理并回显新格式。

### 【C3 前缀模式三态与防误触】
- 【定稿依据】【分隔】§6.1（三种前缀模式）、§6.2（@机器人 开关）、§6.4（快捷适用清单）、§6.5（防误触）；【规则】§8.4（指令触发模式可配，默认全局快捷需配防误触）；【框架】§7.2（指令触发模式）
- 【实现要点】settings.command_mode 三态：prefix_only（全部必须 / 开头）/ combat_shortcut（战斗内免前缀，战斗外保持 /）/ global_shortcut（全局免前缀，默认）；require_at 独立开关默认 false；command_shortcut_whitelist 白名单（默认按 §6.4 清单：攻击/防御/逃跑/道具/使用/进入/锁定怪物/采集/合成/炼金/锻造/强化/调合/镶嵌/拆珠/状态/背包/怪物/地图；创建与 GM 类低频指令保留前缀：开始/重载/封禁/编辑）；防误触三件套：非指令消息不响应（只匹配白名单指令名开头）、群聊随机文本无匹配即忽略、GM 指令永不快捷（强制 / 前缀）；频率控制照常（防刷屏）；不同群/机主可各自设置（机主 /设置 切换）。
- 【验收标准】① 三态切换行为断言（global_shortcut 下"攻击2"直接生效；prefix_only 下"攻击2"不响应）；② 随机文本（"今天天气不错"）不误触；③ GM 指令在全局模式下仍要求 / 前缀；④ require_at=true 时快捷名需 @机器人。

### 【C4 快捷指令绑定】
- 【定稿依据】【分隔】§6.6（快捷绑定/解绑/列表、存储、冲突检测、铁律、前缀联动）
- 【实现要点】指令：快捷绑定 <名> <对应指令>（可绑带参指令/完整指令串）、快捷解绑、快捷列表；存储=玩家存档 `{快捷名: 完整指令串}`（每玩家独立，随存档持久化，见 B6）；触发解析顺序写死：① 查快捷表（完整消息精确匹配快捷名）→ ② 查指令别名 → ③ 查指令白名单（指令名开头匹配）→ ④ 忽略；冲突检测（绑定名 ∈ 现有指令名/固定子词 → 提示不可绑；GM 指令不可绑——防权限绕过；绑定名含空格/保留字符 → 提示；解绑不存在 → 提示；覆盖绑定直接覆盖+提示，无需二次确认）；上限每玩家 20 条可配（0=不限）；快捷执行铁律：走完整指令管线（物品校验/冷却/MP/权限/频率/会话路由全部照常不绕过），GM 指令执行层权限二次检查；快捷名跟随前缀模式与 @机器人。
- 【验收标准】① 绑定 1=攻击 后发"1"→攻击，发"攻击2"→正常攻击指令（不被快捷影响）；② 绑定"重载"→拒绝（GM 指令）；③ 绑定"攻击"（现有指令名）→ 冲突提示；④ 快捷执行仍触发冷却/MP/权限/频率校验（不绕过断言）；⑤ 第 21 条绑定 → "快捷已满 20 条"提示。

### 【C5 指令别名（世界观定制）】
- 【定稿依据】【分隔】§6.7（command_aliases、keep_original、显示层替换、冲突检测、与快捷绑定区别）
- 【实现要点】settings.command_aliases 配置（内容包作者级、全局生效）：炼金→炼丹、转职→突破、锻造→炼器、强化→淬炼、调合→炼药 等；keep_original：true（默认）= 原指令保留可用，false = 原指令隐藏禁用（发"炼金"→"没有这个指令，试试『炼丹』？"）；显示层替换：/帮助 /角色 提示 技能卡 错误模板全部用别名显示（提示"炼丹成功！"而非"炼金成功！"）；带参指令别名同样生效（炼丹 火灵丹*3 = 炼金 火灵丹*3）；冲突检测（别名 ∈ 现有指令名/其他别名/固定子词 → 黄提示不拦截；含空格/保留字符 → 提示）；上限 50 条可配；别名表与快捷绑定互不干扰（全局 vs 个人），解析顺序：快捷表（个人）→ 指令别名（全局）→ 指令白名单 → 忽略；别名不影响权限校验。
- 【验收标准】① "炼丹 火灵丹*3" 等价 "炼金 火灵丹*3"；② keep_original:false 时发原指令名 → 引导提示；③ 帮助页显示别名（keep_original:false 时只显示别名）；④ 与快捷绑定并存互不干扰。

### 【C6 会话路由（会话管理器）】
- 【定稿依据】【框架】§3.18（通用会话快照：单玩家 1 会话/挂起//角色 提示/30 天回收/version 幂等/ID+名称冗余）、§3.17（战斗中断恢复）、附 D.7（会话路由优先于快捷表）、附 A.5（快照扩展字段）
- 【实现要点】会话管理器（战斗/调合/NPC 对话共用）：单玩家同一时间只允许 1 个会话（战斗/调合互斥，自动挂起）；/角色 提示未完成会话（类型 + 可继续/放弃）；30 天无操作回收（材料/状态返还）；会话 version 号 + 指令幂等（防双扣/重复结算）；快照按旧配置结算（ID+名称冗余存储，防热重载失效；删配置按"无效果/无链/无印"降级不报错）；路由规则（写死）：对话会话激活时，纯数字/继续/退出/选择N 送会话状态机（跳过快捷表纯数字匹配），带指令词照常解析——**会话路由优先于快捷表**；跨群并发互斥（A 群战斗进行中，B 群战斗指令提示"已在战斗中"并指向 A 群；非战斗指令 /状态 /商店 /签到 不受限可并行）；战斗快照字段协议按 A.5 登记（status_state/marks_state/combo_state/ai_state/resource_state/formula_state/npc_state 等由各引擎路填充，本路定义快照容器与序列化契约）。
- 【验收标准】① 单会话互斥：会话进行中发起另一会话 → 挂起提示；② 跨群并发：B 群战斗指令 → "已在战斗中"指向 A 群；③ 30 天无操作 → 回收并按退出结算；④ version 冲突 → 拒绝重复结算（双扣防护）；⑤ 会话激活时"1/继续/退出"走会话状态机（快捷表不拦截）。

### 【C7 内容包自定义指令注册】
- 【定稿依据】【分隔】§五（内容包注册指令时声明参数位）；【框架】§7.1 注释（生活/制造指令注册：指令名/参数/会话态/门槛，框架提供解析/权限/频率控制/错误模板骨架）、§7.7（内容包可禁用指令）
- 【实现要点】注册接口：内容包声明指令参数位 {pos, 名, 类型: target/qty/seq/level/path/keyword, 可选}；解析器按类型自动路由分隔符：qty→`*`、seq→`-`、level→`+`、path→`>`、其余→空格；新指令零特判（按"空格切参数 + 数量位 *"声明，语法自动统一）；固定子词表对所有注册指令生效；内容包可禁用指令（无商店的包 /商店 被拒）；注册进动态指令注册表（与快捷绑定冲突检测共用，见 C4）。
- 【验收标准】① 声明 5 类参数位的示例指令全部按规范解析；② 会话态指令（如调合链 /投料 /继承 /确认）按 C6 路由；③ 禁用指令 → 人话拒绝；④ 新指令无特判代码（评审检查）。

---

## 4. D 组：消息模板与渲染——5 任务

### 【D1 message_prefix 渲染器（回复首行身份前缀）】
- 【定稿依据】【前缀】§二（默认格式与验收示例）、§三（配置段 6 字段）、§四（占位符体系 5 个）、§五（显示规则 5 条）、§六（与称号系统联动）、§七（长度）、§九（校验器红黄）
- 【实现要点】settings.message_prefix 段全字段接线（enabled 默认 true / format 默认 "Lv[等级].[玩家名] -[称号]-" / show_on_system 默认 false / per_channel 默认 all / hide_when_empty 默认 false / empty_title_text 默认 "-" / prefix_max_len 默认 40）；占位符 5 个：[等级]（存档 level）、[玩家名]（角色名，无则 QQ 昵称兜底）、[称号]（读 title_state 当前佩戴，来源=成就奖励/职业王/品评会冠军；无称号→empty_title_text）、[群名]（私聊="私聊"）、[职业]（jobs.json 当前职业）；渲染规则：仅玩家指令触发的回复首行加前缀（系统公告/群广播默认不加）、一轮战斗合并消息只加首行、前缀不影响指令解析（纯渲染层产物）、前缀不计入消息长度限制；hide_when_empty=true 时无称号整段省略含相邻装饰符并清理尾随空格；empty_title_text="" 仅隐藏占位符本体；prefix_max_len 超长截断+黄提示（不阻断正文）；校验器：未知占位符原样输出+黄提示、format 空按默认补全、format>80 字符/占位符>10 个黄提示、per_channel 非法按 all 补全、结构错误红拦。
- 【验收标准】① 验收示例逐字断言：阿伟 35 级佩戴「斩龙者」→ 首行 `Lv35.阿伟 -斩龙者-`，正文第二行起，一轮战斗仍 1 条消息；② 无称号默认 `Lv35.阿伟 - -`；hide_when_empty:true → `Lv35.阿伟`（尾空格清理）；③ 系统公告默认不加前缀，show_on_system:true 时加；④ per_channel=group/private 限定生效；⑤ 私聊 [群名] 输出 "私聊"；⑥ 未知占位符原样输出+黄提示不拦截；⑦ prefix_max_len 截断+提示、不计入正文长度。

### 【D2 统一发送出口与消息合并】
- 【定稿依据】【框架】§8.3（消息合并策略：一轮=1 条/开始=1 条/结束=1 条/探索=1 条，单次操作最多 1-2 条）、§15.7（QQ 消息层：段级提取/CQ 转义/长度预算/统一出口）；【规则】§8.1（风控退避/不爆发补发）、§8.2（超长分两条）、§8.3（防封号核心策略）
- 【实现要点】统一消息发送出口封装（全部发送路径必须走统一出口，禁止裸 send）：失败重试 + 风控指数退避（不无限重发）+ 超长自动截断分两条发送（不吞消息）；CQ 码转义防注入（消息段级提取 at/图片/表情）；渲染层长度预算统一；单次操作最多 1-2 条消息；频率控制联动（超频丢弃或排队）。
- 【验收标准】① 超长消息分两条且内容完整（不吞消息断言）；② 发送失败按退避重试；③ CQ 注入用例（恶意消息内容转义后原样展示）；④ 全仓无裸 send（代码评审检查）。

### 【D3 分页与筛选（5 条/页）】
- 【定稿依据】【框架】§7.4（分页与筛选语法）；【规则】§8.2（列表一律分页：背包/任务/商店默认 5 条/页可配，翻页语法 背包2，筛选链减少翻页）
- 【实现要点】通用分页语法 `<功能> [页数]`（背包2=第二页）；默认 5 条/页可配；筛选链 `<功能>筛选 <物品类型> [类型 <子类目>] [品质 <品质>]`（类型→子类目→品质可叠加）；翻页越界提示；超长自动分两条（D2 出口）。
- 【验收标准】① 背包2 返回第 2 页 5 条；② 背包筛选装备 类型 武器 品质 史诗 叠加生效；③ 越界页码人话提示；④ 页大小可配。

### 【D4 少 emoji 模板库】
- 【定稿依据】【规则】§8 开头（防封号最高优先级：所有设计向"少发消息、发短消息、不像机器人"倾斜）；【框架】§8.1/§8.2（模板图标示例：⚔️🟢❤💙💰）、§4.2.1 marks icon（1 个 emoji）；【前缀】§一.5（前缀只加首行不增加消息条数、不计入长度）；【单机】§六（⛩️ 仪式感揭示卡片唯一特殊格式）
- 【实现要点】全局固定图标集登记表（唯一 emoji 白名单）：战斗 ⚔️（玩家行动）/🟢（怪物反击）、状态面板 ❤HP/💙MP/💰货币、印记 icon（marks.json 每印记 1 个 emoji）、仪式感揭示 ⛩️（一次性卡片，【单机】§六）；规则：模板正文不用装饰性 emoji（用文本符号 `|` `→` `·` 排版）、新 emoji 必须先登记再使用、message_prefix 默认格式不含 emoji、状态行默认显示前 5 个+追加"还有 N 个状态"（【框架】§3.4）与"少 emoji"同源（防刷屏防封号）。
- 【验收标准】① 全仓模板引用图标 ⊆ 登记表（静态检查脚本断言）；② 前缀默认格式无 emoji；③ 战斗/列表正文无装饰性 emoji；④ 登记表外 emoji 用例被检查脚本拦截。

### 【D5 战斗/状态消息模板与 /角色 三层展示】
- 【定稿依据】【框架】§8.1（战斗消息模板）、§8.2（状态面板）、§7.6（战斗指令行）、§3.4（状态行"中毒 2/3"、默认前 5 个）；【属性】§九（/角色 折叠展示三层）；【前缀】§二（前缀+正文排版）
- 【实现要点】战斗一轮消息模板逐字对齐 §8.1（玩家行动结算 + 🟢 反击 + 双 HP 行 + 行动提示行，前缀首行由 D1 注入）；状态面板模板 §8.2（部位名来自内容包）；/角色 三层折叠展示：白值/加成（flat+pct）/临时分层展示，战斗外展示加成层、战斗中追加临时层；状态行剩余回合显示（"中毒 2/3"）、默认前 5 个状态、超出追加"还有 N 个状态"；/查看目标 查看目标属性（掉落不显示）。
- 【验收标准】① §8.1/§8.2 示例逐字对齐（含 D1 前缀行）；② /角色 三层结构展示正确；③ 状态行"中毒 2/3"与第 6 个状态起"还有 N 个状态"。

---

## 5. E 组：数据文件清单（每个 JSON → 定稿 schema）——8 任务

### 5.1 内容包 JSON 总表（29 文件，schema 唯一数据源 = 字段元数据层【框架】§4.2）

| # | JSON 文件 | 定稿依据（schema） | 核心内容 | 加载批次* | 校验要点（红拦/黄提示） |
|---|---|---|---|---|---|
| 1 | manifest.json | 【框架】4.1/4.1.1；【规则】2.1 | name/version/schema_version/author/modules | 0 | 缺模块黄提示；未声明文件不加载 |
| 2 | settings.json | 【框架】4.1 注释、6.12（货币/出售/排行/事件推送/数据包保护）、附 K.1/K.2；【前缀】§三；【分隔】6.1/6.7 | 通用设置/货币/PVP/死亡惩罚/等级上限/command_mode/require_at/whitelist/aliases/message_prefix/alchemy/fishing/time_cycle | 0 | mode 枚举硬；前缀段红黄按【前缀】§九 |
| 3 | slots.json | 【框架】5.1 | 部位定义（name/max/occupies）+ mutual_exclusions | 1 | 互斥环红拦（加载期） |
| 4 | stats.json | 【属性】§八；【框架】4.2.2、4.2.3、附 G.1 | 9 项模板+注册制+资源注册段+虚拟键空间 | 1 | 类型/负数红；未注册键黄 |
| 5 | jobs.json | 【框架】4.2.2；【属性】§六 | 说明卡/成长率/进阶/transform | 1 | transform battle+revert 矛盾黄 |
| 6 | skills.json | 【框架】4.2.2（ActionCore 玩家侧扩展）、附 G.2 | type/mp_cost/cooldown/job_restrict/job_form/level/interrupt/marks_add/clear_marks/energy_gain/energy_cost/hits/element/chain_refs | 2 | 引用存在红；与 action 同库唯一红 |
| 7 | skill_chains.json | 【框架】4.2.1 | 链/派生四模式/霸体/consume/variant_override | 2 | 死配置（eq>max_combo、reset eq=max）红 |
| 8 | statuses.json | 【框架】3.4、4.2.2 | 四模型字段/执行体/叠加上限 | 2 | 引用红；效果家族跨表唯一红 |
| 9 | effects.json | 【框架】4.2.1、3.4/A.1 | 三层分类/触发三态/冷却/actions 原子动作 | 2 | 动作引用红；链环检测；概率三态 |
| 10 | marks.json | 【框架】4.2.1 | max_stack/appliable_to/polarity/element/duration | 2 | 效果家族跨表唯一红 |
| 11 | traits.json | 【框架】4.1 | 特性/觉醒/潜力（炼金共用） | 3 | 引用存在红 |
| 12 | action.json | 【框架】4.2.1、附 C.2 | ActionCore+AI 字段（weight/cooldown/hungry/chain/intent/preview…） | 2 | 与 skills 同库唯一红；引用 effects 红 |
| 13 | items.json | 【框架】4.2（元数据例）、4.2.2（虚拟键/品质四档唯一注册表） | type/quality/slot/job_restrict/bound/cooldown/stats_bonus/effects/potion_type/装饰珠 | 3 | 品质未注册黄；引用红；键空间黄 |
| 14 | formula.json | 【框架】3.1、附 A.2、附 B.3；【属性】§二/§三 | damage/hit/crit/block/defense/weakness/weapon_type_mult/type_affinity/luck/derived/effects_link/elements/expr | 3 | 元素引用注册表=唯一硬拦；交叉一致性黄 |
| 15 | loot.json | 【框架】4.1 | 掉落表（保底次数/宝石列） | 3 | 物品引用红；概率 0-100 |
| 16 | enemies.json | 【框架】4.2.2、附 A.3、附 C | 八段结构/AI 状态机/阶段/连招/三类掉落/lore | 4 | 行动引用红；概率总和 0 黄；弱点 0 提示 |
| 17 | maps.json | 【框架】4.1、附 A.4；【单机】§二（隐藏要素载体） | 怪物/采集（稀有度）/通道/隐藏/安全区/weather_pool/seasons/periods/zone_effect/gate | 4 | 怪物引用红；隐藏条件永假黄 |
| 18 | shop.json | 【框架】附 E | 5 类型/商品/限购/刷新/黑市/open_condition | 4 | 商品引用红；货币注册红；空店黄 |
| 19 | checkin.json | 【框架】附 F | loop/monthly/activity/每日连签月度/补签 | 4 | 奖励引用红；连签超周期黄 |
| 20 | enhance.json | 【框架】4.1 | 强化上限/成功率曲线/失败惩罚 | 4 | 数值范围黄 |
| 21 | recipe.json | 【框架】4.1 登记、4.2 recipe 元数据表 | kind（craft/combine/upgrade）/inputs/cost/output/conditions | 4 | 引用红；死配置（count 越界等）红 |
| 22 | quest.json | 【框架】4.1、附 D.3 | 任务板/奖励/条件 {var,op,value} | 4 | 条件 op 合法硬；死条件黄 |
| 23 | forge.json | 【框架】4.1 | 装备派生树 | 4 | 引用红 |
| 24 | dungeon.json | 【框架】附 A.4 | 探索/BOSS 副本/maps/boss/subquests/zone_change | 4 | maps/boss 引用红 |
| 25 | npc.json | 【框架】附 D | 交互层（选择 N）/条件引擎/地图挂点/发牌员 | 4 | 交互引用红；条件 op 硬 |
| 26 | proficiency.json | 【框架】附 H | 7 级称号/熟练度/SP 面板/能量条 | 4 | 等级/职业引用红 |
| 27 | profession.json | 【框架】附 H、附 J | 委托板/种植/品评会/代工/fishing 段（mode 三态） | 4 | 引用红；空表黄 |
| 28 | fishing.json | 【框架】附 J | 鱼种/冠级阈值/饵/时段/钓点 | 4 | 鱼种区间硬；阈值 0<5<85<95<100 硬；饵引用硬 |
| 29 | achievements.json | 【框架】附 I；【单机】§三 | 条件/触发类型/reward/称号型奖励 | 4 | 条件引用硬；奖励引用硬；条件永假黄 |

*加载批次为推荐依赖序（效果家族先注册：effects→statuses→marks【框架】§4.4/【规则】§2.1；skill_chains/action 次之）；实际顺序以 manifest.modules 声明顺序为准，loader 按声明加载。

### 5.2 玩家存档键映射表（数据文件清单之存档部分）

| 语义分类（【框架】§3.7.1） | 状态键 | 内容 | 生命周期 |
|---|---|---|---|
| ① 会话快照（3.18/A.5） | battle/alchemy 会话快照：status_state、marks_state、combo_state、ai_state、resource_state 战斗段、formula_state、npc_state、potion_use_counts | 进行中对局全量还原 | 单会话互斥可挂起；30 天回收；中断恢复不重置 |
| ② 非会话持久状态 | checkin_state、shop_state（含 daily_limit）、resource_state 常驻段、time_state、dummy_log/dummy_override | 签到/商店/资源轴/时间天气 | 常驻不回收 |
| ③ 长线计数 | longline_counters（kill_count/gain_count/冒险日志 300 条环形+环境快照【单机】§四） | 只增不减 | 跨版本保留 |
| ④ 声望与图鉴 | reputation_state、codex_state（四册：monster/fish/item/craft，总册加权【单机】§三） | 解锁条目+完成度 | 常驻迁移 |
| ⑤ 成就与称号 | achievement_state、title_state（当前佩戴 1 槽） | 成就集合+称号池 | 常驻迁移 |
| 附加 | 玩家快捷表 {快捷名:完整指令串}（【分隔】§6.6） | 快捷绑定 | 随存档持久化 |
| 附加 | 玩家属性三层 base/bonus.flat/pct（带到期）/temp（【属性】§五） | 三层属性 | 随存档迁移（B6） |

### 任务

### 【E1 基础两文件：manifest + settings】
- 【定稿依据】总表 #1/#2 行；【框架】4.1/4.1.1、附 K.1/K.2；【前缀】§三；【分隔】§6.1/§6.7
- 【实现要点】manifest：schema_version 整数、modules 数组（loader 按声明顺序加载、不声明不加载、缺失仅提示）；settings：通用设置段（货币定义/出售比率/等级上限/PVP/死亡惩罚/数据包保护）、command_mode/require_at/command_shortcut_whitelist/command_aliases、message_prefix 段（D1 消费）、time_cycle 段、alchemy/fishing 模块 mode；字段元数据表登记（编辑器/CSV/Schema/校验四处共用）。
- 【验收标准】① 声明缺失模块 → 黄提示"没找到『effects.json』…旧包照常玩"（文案断言）；② 未声明文件存在 → 不加载；③ settings 全部登记字段加载进 registry；④ schema_version 决定迁移路径（旧版存档字段级迁移）。

### 【E2 基础引用目标：stats + slots】
- 【定稿依据】总表 #3/#4 行；【属性】§八；【框架】5.1、4.2.2、4.2.3、附 G.1
- 【实现要点】stats 按 B1；slots：部位定义（max/occupies）+ mutual_exclusions 互斥检查（占用集合→互斥→数量→穿上）；部位名/数量/互斥全部内容包自定义；slot_level 槽位等级字段保留（装饰珠【框架】§3.5 后续路）。
- 【验收标准】① 互斥环 → 加载期红拦人话"你的武器、盾牌、副手互相排斥形成了一个圈…"；② 装备冲突运行期人话拒绝（接口留战斗路）；③ stats 模板与定稿一致（B1 断言复用）。

### 【E3 职业与生活四文件：jobs + proficiency + profession + fishing】
- 【定稿依据】总表 #5/#26/#27/#28 行；【框架】4.2.2、附 H、附 J
- 【实现要点】jobs 按 B5；proficiency：7 级称号（tier_names 可改名）/exp_sources/sp_per_level/sp_panel/energy 可选能量条；profession：quest_board/farming/contest/assistant/economy + fishing 段（mode 三态）；fishing：鱼种（大小重量区间/稀有度/时段/钓点/饵偏好/图鉴文案）+ 冠级阈值（0<5<85<95<100 硬）+ king_event；熟练经验入账判定顺序（制作/采集/战斗结算时→升级判定→SP 发放）接口留业务路。
- 【验收标准】① 四文件按元数据表加载；② fishing 冠级阈值非法 → 红拦；③ 王称号/品评冠军/钓鱼王为 title_state 称号来源之一（与附 I 对齐，前缀 [称号] 可读）；④ profession/fishing 空表黄提示。

### 【E4 技能行动三文件：skills + skill_chains + action】
- 【定稿依据】总表 #6/#7/#12 行；【框架】4.2.1、4.2.2、附 C.2、附 G.2
- 【实现要点】skills：ActionCore 玩家侧扩展全字段（含 energy_gain/energy_cost 时机：结算时增益/施放前检查消耗）；skill_chains：派生四模式（replace/strengthen/append/unlock）+ 边条件原语集 + 霸体/consume/variant_override；action：ActionCore+AI 字段（weight/probability 默认 0=锚点/cooldown/hungry/chain/intent/preview/reveal_condition）；技能-行动同库唯一（共用 ActionCore 命名空间）；派生条件与 JS 公式分离不互通（附 B.8）。
- 【验收标准】① skills↔action 同库撞名 → 红拦；② probability 缺省=0 锚点语义（不入随机池）断言；③ energy_cost 不足 → 指令被拒不耗回合（接口留战斗路）；④ 派生死配置（eq>max_combo、reset 模式 eq=max）红拦。

### 【E5 效果家族四文件：effects + statuses + marks + traits】
- 【定稿依据】总表 #8/#9/#10/#11 行；【框架】3.4、4.2.1、A.1、4.4（效果家族先注册）
- 【实现要点】三表统一登记进效果注册表（ID 跨表唯一，供挂载点统一引用）；effects：三层分类（special/status/atomic）+ 触发三态（-1 必定/0~100 固定/+0~100 幸运修正）+ 冷却 + actions 原子动作（damage/heal/stat_modifier/dot/control/status_apply/dispel/shield/mark_add/mark_remove/clear_marks/interrupt/summon/convert/aoe + 结算修正器 lifesteal/pierce/mitigation + 触发容器 proc/element_modifier）；statuses：四模型字段（stack_frame/decay/hit_rate/resist_gain/immune_to）；marks：max_stack/appliable_to/polarity/element/duration；traits：特性/觉醒/潜力表；特效链环检测（图论环）与概率三态校验。
- 【验收标准】① 跨表撞名红拦（effects vs statuses vs marks）；② 动作引用存在性红拦；③ 特效链环 → 红拦/黄提示按环类型；④ 概率三态非法值红拦；⑤ 注册顺序：效果家族先于 traits/items/skills/enemies 入 registry。

### 【E6 物品经济八文件：items + loot + shop + checkin + enhance + recipe + forge】
- 【定稿依据】总表 #13/#15/#18/#19/#20/#21/#23 行；【框架】4.2（items 元数据例）、4.2.2（虚拟键/品质四档）、附 E、附 F、4.2 recipe 元数据表
- 【实现要点】items：type/quality（四档唯一注册表 normal/fine/epic/legendary，存储用英文键）/slot/job_restrict/bound/cooldown（回合/秒）/stats_bonus（键空间校验）/effects/potion_type/装饰珠 type；loot：三类掉落表+保底；shop：5 类型/商品条目（限购/刷新/黑市浮动/open_condition 条件键）；checkin：三表+四奖励通道+补签+日界 05:00 懒计算；enhance：上限/成功率曲线/失败惩罚（引用 formula.luck.enhance_rate）；recipe：统一合成引擎 schema（kind/inputs/cost/output/conditions——机制=框架代码，各系统=配置实例）；forge：装备派生树。
- 【验收标准】① 品质未注册档位黄提示、类型错误红拦；② 商品/奖励/掉落引用红拦；③ 货币键未注册红拦；④ recipe kind 三值路由（合成指令统一入口）接口留业务路；⑤ 日界 05:00 与 quest_daily/商店同刻对齐（懒计算断言）。

### 【E7 世界内容六文件：enemies + maps + dungeon + quest + npc + achievements】
- 【定稿依据】总表 #16/#17/#22/#24/#25/#29 行；【框架】4.2.2、附 A.3/A.4、附 D、附 I；【单机】§二/§三
- 【实现要点】enemies：八段结构（stats/weakness/pv/resistance/actions/special_actions/drops/lore/ai/phases/chains/zone_change/teach_map/pressure）；maps：怪物（数量 0=无限）/采集（稀有度）/通道/隐藏地图/安全区/复活点/weather_pool/seasons/periods + 隐藏要素载体（【单机】F1：隐藏 BOSS=怪物行限定窗口+图鉴条件；彩蛋=☑隐藏地图+交互点）；dungeon：探索/BOSS 双副本；quest：任务板/奖励/条件统一 {var,op,value}；npc：交互层/地图挂点/发牌员/条件引擎；achievements：条件+reward（含称号型条目）+图鉴阶梯（25/50/75/90/100% 里程碑模板，【单机】§三）。
- 【验收标准】① 行动/怪物/地图/物品引用红拦；② 条件 op 非法硬拦、死条件黄提示；③ 隐藏要素条件永假 → 黄提示"无发现路径=可能永不可发现"（【单机】§2.3）；④ codex 四册结构（monster/fish/item/craft）与总册加权接口就绪。

### 【E8 formula 参数段 + 存档键映射（storage schema）】
- 【定稿依据】总表 #14 行；【框架】3.1、附 A.2、附 B.3；【属性】§二/§三；【框架】3.7.1、附 I.4
- 【实现要点】formula.json 全参数段（damage/hit/crit/block/defense/weakness/weapon_type_mult/type_affinity/luck/derived/effects_link + elements 元素注册表 + expr 安全参数段）；引用规则（enhance 引用 luck.enhance_rate、effects 引用 effects_link、skill_chains 引用 derived——全系统引用参数不复制数值）；存档键映射表（5.2）落地为 state/storage.py 的表/字段定义与迁移链。
- 【验收标准】① 元素注册表增删 → ele_atk_<e> 键自动增减（动态键断言）；② formula 参数全部可配且被 B3 消费（无硬编码）；③ 五类语义键的存取/回收/迁移策略单测（30 天回收/常驻/只增不减）。

---

## 6. F 组：loader/校验器接线（旧教训：校验空转）——6 任务

### 【F1 loader 按 manifest 声明顺序加载】
- 【定稿依据】【框架】§4.4（加载流程 4 步）；【规则】§2.1（模块清单与 manifest 硬约束）
- 【实现要点】loader 读 manifest → 按 modules 声明顺序加载 → 校验模块齐全（缺失仅提示不拒绝）→ 逐模块加载并交 validator → 注册 registry；新模块注册顺序固定：effects → statuses → marks（效果家族先注册）→ skill_chains → action（被 traits/items/skills/enemies 引用，须先入 registry）；旧配置自动迁移（内联 effects 数组 → effects.json ID 引用，语义字段取默认值，旧包零改动可跑）。
- 【验收标准】① 加载顺序断言（registry 出现顺序符合依赖序）；② 缺 effects.json 的旧包可加载且效果系统不启用；③ 内联 effects 旧配置迁移成功。

### 【F2 registry 三注册表与 ID 唯一规则】
- 【定稿依据】【框架】§4.4（三注册表）、§4.5（ID 唯一性规则：效果家族跨表唯一/其余表内唯一/技能-行动同库唯一）
- 【实现要点】效果注册表（effects/statuses/marks 三表统一登记，ID 跨表唯一，供挂载点统一引用）；行动注册表（action.json，enemies.actions 按 ID 引用，一行动多怪复用）；派生链注册表（skill_chains.json 连段标签/派生条件索引）；撞名语义：同表/同库撞名=红拦（寻址歧义），跨表撞名（非效果家族）=黄提示（引用带表上下文，仅影响显示层）。
- 【验收标准】① 效果家族跨表撞名红拦；② 同表撞名红拦（物品示例）；③ skills↔action 同库撞名红拦；④ 跨表非效果家族撞名黄提示不拦截。

### 【F3 validator 红拦五类 + 黄提示 + 人话模板】
- 【定稿依据】【框架】§4.5（人性化报错表）、§4.2.4（只建议不限制原则）；【规则】§2.3（5 类红拦/黄提示/人话模板/措辞修正映射表）
- 【实现要点】红拦仅限 5 类：类型错误（数字填成文字/NaN/null）、负数（<0）、非数字、引用不存在、结构错误（JSON 格式坏/必填缺失/死配置：条件 min>max、reset 模式 eq=max、battle+revert 矛盾）；黄提示（不拦截可加载）：数值范围/概率高低/组合强度/软标注字段（difficulty/playstyle 永不红）/新模块缺失；报错人话模板（『巨剑』引用的部位『双手武器』不存在，请先在部位页面添加）；禁止"必须/强制/上限/封顶/拒绝"类文案（措辞映射表）；x_ 前缀自定义字段/动作放行（仅查引用存在性）。
- 【验收标准】① 5 类红拦各至少 1 个 fixture 断言（tests/fixtures/packs 坏包）；② 黄提示不拦截加载断言；③ 文案措辞审查测试（禁词扫描）；④ 报错带模块名+可定位信息。

### 【F4 校验接线防空转（旧教训专项）】
- 【定稿依据】【规则】§2.4（热重载后必须过完整校验器；校验失败=回退旧 registry，绝不半套配置运行）、§1.3（结构断言收敛 validator，禁止散落 `if in data`）；【框架】§4.4/§15.5
- 【实现要点】接线契约（写死）：loader 每模块加载**必经** validator（红拦 → 该模块不注册 → 启动/重载失败 → 回退上一份校验通过的 registry 快照 + 人话提示）；禁止 loader/engine 内散落 `.get()` 绕过校验路径；校验结果可观测（日志记录红/黄计数与模块名，供编辑器红显）；防空转自测：tests/fixtures 坏包断言"确实被拦截"（而非"通过"）、每个红拦类别有测试、校验器分支覆盖率纳入 G4 门禁；校验输出可定位（模块名+条目名）。
- 【验收标准】① 坏引用包 → 启动失败/对应模块不加载（断言，非静默通过）；② loader 全模块无绕过路径（代码评审清单 + 静态检查）；③ 红/黄计数日志断言；④ validator 分支覆盖率达标（G4）；⑤ 新增模块未接线 → CI 拦截（校验器调用点登记表与 loader 对照）。

### 【F5 热重载原子性】
- 【定稿依据】【框架】§15.5（热重载原子性）、§4.4（mtime 增量/旧局旧配置）；【规则】§2.4
- 【实现要点】先写临时文件再原子 rename；全部文件写完统一触发重载；按文件 mtime 增量校验，只重载变更模块（新模块同样纳入 mtime 监控）；校验失败 → 回退上一份 registry 快照 + 人话提示；进行中对局持旧快照（ID+名称冗余）、新对局用新配置；删除配置按"无效果/无链/无印"降级不报错。
- 【验收标准】① 写入非法 JSON 触发重载 → 服务不崩 + 回退 + 人话提示（故障注入 G3 复用）；② 变更单模块仅重载该模块（mtime 断言）；③ 进行中对局持旧配置（快照冗余断言）。

### 【F6 内容包冒烟】
- 【定稿依据】【规则】§四.1（内容包冒烟测试：对 content/ 全部预置包跑 validator + 模拟一局）；【框架】§4.3（数据包体系五档）
- 【实现要点】tests/fixtures/packs 四类固定样例：合法包 / 坏引用包 / 缺模块包 / 旧 schema 包；demo_blank 最小可玩包起步，按 §4.3 五档数据包（空白/新手 1-15/成长 1-30/进阶 1-45/完整）逐步建立；冒烟=validator 全绿 + 模拟一局（注册→锁定→攻击→结算，battle 接口按 A3 留桩，本路先跑通属性/背包/渲染链路）。
- 【验收标准】① 四类样例包断言各自预期（合法全过/坏引用拦截/缺模块提示/旧 schema 迁移）；② 冒烟闭环走通且输出留档（verify 归档）。

---

## 7. G 组：验证体系（每里程碑 verify）——4 任务

### 【G1 里程碑与 verify 门禁定义】
- 【定稿依据】【规则】§六.3（发布检查单）、§四（测试规范）；【框架】§13（验收标准）
- 【实现要点】定义 M1-M6 里程碑（见 §8）与统一 verify 清单模板：pytest 全绿（含本里程碑新增用例）→ ruff/mypy 通过 → 预置内容包 validator 全绿 → 冒烟闭环 → 里程碑验收单（对照本规划任务勾选）→ 结果归档；**verify 必须可执行且有断言**（禁"人工看了没问题"），每个里程碑 verify 后更新 CHANGELOG（【规则】§6.2）。
- 【验收标准】① 每里程碑 verify 清单逐项可勾选且命令可复跑；② verify 输出留档于仓库 docs/；③ 任一红拦类回归失败 → 里程碑不通过。

### 【G2 测试体系（分层 + 确定性 + 往返）】
- 【定稿依据】【规则】§四.1（测试层级：unit/integration/冒烟）、§四.2（引擎覆盖要求表）、§四.3（存档往返硬约束）、§四.4（伤害公式确定性/性质用例）
- 【实现要点】unit（engine/content/state 纯逻辑，不联网不启 NoneBot，SQLite 用 :memory: 或 tmp_path）；integration（commands → engine 全链路，NoneBot 测试夹具打桩）；确定性用例（固定种子/固定输入→精确数值）+ 性质用例（随机 1000 组→不变量：伤害 ≥1、乱数区间、会心率 ≤95%）；存档往返（存什么→读什么→完全一致，每类快照）；固定随机种子 fixture（seed 参数化）；公式参数用独立 fixture 包，禁硬编码生产参数。
- 【验收标准】① 【规则】§4.2 必测表（battle/levelup/inventory/equipment/worldtime/storage）逐项有测试（本路覆盖 levelup/inventory 接口/equipment 部位/storage 往返，battle 留后续路）；② 往返测试对每类快照断言完全一致；③ 性质用例 1000 组不变量全绿。

### 【G3 故障注入套件】
- 【定稿依据】【规则】§四.5（故障注入表：回复前崩溃/存档写失败/热重载失败/公式异常/双扣/掉线重连）；【框架】§15.16
- 【实现要点】mock 发送出口抛异常 → 战斗状态回滚/可重试、不双结算（message_id 幂等）；mock storage 抛 OSError → "保存失败，请检查磁盘空间"提示 + 本地暂存补写；写入非法 JSON 触发重载 → 回退旧 registry 不崩；公式异常/超时 → 兜底 0/条件不满足不崩溃；并发两条购买 → 事务下只扣一次；掉线重连 → 玩家数据完好。每个用例必须有断言（禁"不崩就行"）。
- 【验收标准】① 六类故障注入用例全绿且均有断言；② 双扣防护用例覆盖限购/货币两条路径。

### 【G4 质量门禁（工具链/Git/评审）】
- 【定稿依据】【规则】§一.7（ruff/mypy/pytest CI 门槛）、§三（Git 规范：Conventional Commits/内容包与代码分提交）、§五（评审清单 P0 必清）、§四.6（覆盖率 engine/content ≥80%）
- 【实现要点】CI 门槛：ruff + mypy + pytest 全绿才合入；覆盖率门槛 engine/ + content/ 行覆盖 ≥80%，新功能必须有测试才合入；提交信息 Conventional Commits（中文 subject、正文关联定稿章节、破坏性变更 BREAKING 标注）；内容包 JSON 变更（feat(pack)）与代码变更分开提交；PR 评审清单（安全/性能/错误处理/零代码/并发/可维护性六类，P0 未清零不得合并）。
- 【验收标准】① CI 全绿方可合并（流水线断言）；② 覆盖率达标报告归档；③ 提交信息规范抽查（subject ≤72 字、关联定稿）；④ 评审清单 A-F 逐项可勾选。

---

## 8. 里程碑规划与 verify 门禁（M1-M6）

| 里程碑 | 范围 | 任务 | verify（每项可执行有断言） |
|---|---|---|---|
| **M1 工程骨架** | 平台无关架构 | A1-A3、A5 | import 图检查脚本全绿（引擎层零 NoneBot）；无 NoneBot 环境 pytest 空跑通过；manifest 加载成功；框架级指令路由注册表断言 |
| **M2 数据层** | 内容包全量 schema | E1-E8、F1-F4 | 四类 fixture 包断言（合法全过/坏引用拦截/缺模块提示/旧 schema 迁移）；防空转自测全绿（F4 验收标准逐条）；29 文件总表逐项加载断言 |
| **M3 属性与成长** | 玩家属性三层/成长 | B1-B6 | 三层管线确定性用例；药剂落层与到期重算；会心/命中/格挡边界断言（幸运 100/封顶 95%/下限 10%/上限 40%）；成长边界（0 经验/满级/回满）；迁移往返 |
| **M4 指令解析** | 解析/快捷/别名/会话路由 | C1-C7 | 分隔符矩阵用例；紧凑/空格等价；三态前缀与防误触；快捷绑定 6 条验收；别名 4 条验收；会话路由 5 条验收；自定义指令注册 |
| **M5 消息模板** | 渲染层 | D1-D5 | 前缀验收示例逐字断言（含 hide_when_empty/私聊/截断）；一轮一条合并；分页筛选；少 emoji 静态检查；战斗/状态模板逐字对齐 |
| **M6 接线闭环** | 热重载/冒烟/质量 | F5-F6、G2-G4 | 热重载失败回退不崩；冒烟闭环（注册→锁定→攻击→结算）；故障注入六类全绿；覆盖率 engine/content ≥80%；ruff/mypy/pytest 全绿；CHANGELOG 归档 |

依赖关系：M1 → M2 → M3 → M4 → M5 → M6（M3 的 B3 与 M4 的 C 组可并行；M5 依赖 M4 路由与 M1 渲染出口）。

---

## 9. 风险与旧教训登记

| # | 风险/教训 | 对策（任务归属） |
|---|---|---|
| 1 | **校验空转（旧教训）**：validator 存在但未接线（loader 用 .get() 绕过）、自测与加载路径分离、坏包也"通过"、校验无断言 | F4 接线契约（每模块必经 validator + 失败回退 + 防空转自测 + 可观测日志 + 覆盖率门禁）；坏包断言进 M2 verify |
| 2 | 属性三层叠乘失控 | 管线写死（B2）：pct 基于基础合计、conditional 基于基础合计防循环；确定性用例固守 |
| 3 | 指令解析冲突（快捷/别名/会话/紧凑歧义） | 解析顺序写死（C4/C5/C6）：快捷表→别名→白名单→忽略；会话路由优先于快捷表；名称优先、序号兜底 |
| 4 | 刷屏/封号 | 一轮一条消息（D2）+ 频率控制（A3）+ 5 条/页（D3）+ 少 emoji（D4）+ 前缀不计长度（D1） |
| 5 | 分隔符实现漂移（各系统自造语法） | 单一解析器（C1）+ 内容包参数位声明（C7）自动路由，禁止特判 |
| 6 | 热重载导致对局异常/半套配置 | 原子写 + 快照回退 + 旧局旧配置（F5），校验失败绝不半套运行（【规则】§2.4） |
| 7 | 存档迁移丢数据 | 字段级迁移（缺补默认/多忽略）+ round-trip 硬约束（A4/B6/G2）+ mh_map 桥接（B1） |
| 8 | 跨文档冲突（如命中 K 默认值） | 以最新定稿与登记段仲裁为准：命中 K 以【框架】附 A.2 formula.json hit.k 为准（B3 已注明） |

---

*设计依据：6 份输入定稿逐章追溯（【框架】§2-§15 与附 A-L /【属性】§一-§十 /【分隔】§一-§七 /【前缀】§一-§十三 /【规则】§一-§九 /【单机】§一-§十一）+ 仓库现状（QBot-TurnTellerRPG/README.md 仅设计层声明）+ 实现层路3 规划（每功能可追溯定稿，代码注释带来源）。*
