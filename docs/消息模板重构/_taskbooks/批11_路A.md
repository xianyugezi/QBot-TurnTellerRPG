# 任务书 · 批11·路A（职业 8 键 + 对话框 3 键）

> 2026-09-12 · 消息模板重构战役 · 执行者：子 agent（本任务书 + `docs/消息模板重构/00_方案与规范_v1.md` 为唯一指令源）
> **先读** `<worktree>/docs/消息模板重构/00_方案与规范_v1.md`（结构化行 ≤14 全角 / 免斜杠 / 少｜多换行 /
> 错误行 ❌+原因(+下一步) / 拆行优先序 / 大数值独立行 / 零装饰 emoji / 介绍类可豁免）再开工。

## 1. 范围（本路 11 键）

job_tpl.py（8 键）：job_list, job_list_tip, job_not_found, job_switch_success,
job_detail_usage, job_detail_header, job_detail_rec, job_detail_line
dialog_tpl.py（3 键）：dialog_menu_head, dialog_dispatch_error, dialog_dispatch_bad_return
（注：job_detail_rec/line 为详情面板字段行片段；dialog_dispatch_* 为引擎回传异常提示）

**每键必做**：读旧文案拿到「信息要素 + 触发场景 + 占位符集合」，然后**按规范全新撰写**（旧文案不作参考）。
占位符沿用；`tpl_of(` 键名与参数名**不得改**；**只动本路这 11 键**（同文件其他键属别的批，不要碰）。
风味/介绍类文本允许自然折行 → 在 fragment `prose_keys` 逐条登记理由。

## 2. 工作清单（每步都落盘）

1. **第一件事**：写 `<worktree>/docs/消息模板重构/_fragments/b11a.json`
   （`{"rail":"b11a","templates":{…}}`）→ commit。
2. 合表：`python3 scripts/tpl_merge_fragments.py docs/消息模板重构/_fragments/b11a.json`
3. 清分区：从对应 `*_tpl.py` 删除已迁 11 键；`content/test_demo/templates.json` 同 key 同步（缺则补）。
4. 代码/测试同步：`grep -rn "<key>" qbot_rpg/ tests/ scripts/` → 常量别名/旧文案断言/docstring 逐处更新；
   **不得**为过测试改回旧文案。
5. 自校验：checker（本路键 0 FAIL）+ `test_template_table` + `test_emoji_discipline` + 定向本模块测试
   + **最后跑一次全量**（`-o addopts=""` 可取到计数）。
6. `.venv/bin/ruff check <改动文件>` 干净；commit。**禁止 push / 禁止动 main**。

## 3. 报告（回复正文**内联**）
fragment 路径 + 11 键新文案 JSON 全量；合表结果；改动文件清单；checker 输出；定向+全量测试计数原文；
未完成/存疑；commit hash。预算吃紧时优先 fragment 落盘 + 代码 commit + 如实列未完成。
