# 幻觉审查报告 · 《细化_1c3_连段测试集.md》

- 审查对象：`/root/QBot-TurnTellerRPG/docs/细化/细化_1c3_连段测试集.md`（124 行，TC-01~TC-52 共 52 条用例）
- 对照定稿：`/root/docs_archive/RPG框架项目/技能派生系统设计定稿.md`（408 行，v1.0 · 2026-08-14）
- 审查方法：通读细化全文 → 抽取全部声明（用例断言 + 行号引用 + 覆盖声明）→ 回定稿逐条核对（①行号引对 ②内容一致 ③补白显式标注 ④数值有依据）
- 审查日期：2026-08-18

---

## 总览

对 52 条用例的全部 90+ 处行号引用逐一回查，**所有行号均指到定稿真实内容，且断言内容与定稿行文一致**（含消息原文逐字核对）。未发现编造规则、未发现与定稿实质冲突的断言。仅发现 1 处 P1（补白漏标）与 5 处 P2（示意性/解释性误差）。

**结论评分：A** —— 全文零编造、零实质冲突，行号引用全部准确，断言与定稿逐条对应（含消息文案逐字一致），仅 1 处默认值补白未标注与数处覆盖率表述/边界语义等轻微瑕疵。

---

## P0（编造 / 实质冲突）：0 项

无。全部 52 条用例的断言均能在定稿找到直接支持行，无自造规则、无跨章节拼凑、无与定稿矛盾的语义。

代表性逐字核对（全部通过）：
- TC-08/TC-34/TC-36 的提示文案逐字命中定稿 L143/L235（"连段中断！（原因：使用了普通技能）"）、L141（"⚡ 盾击打断连斩！连段中断"）、L142（"🛡 大追斩（霸体）！无视打断"）
- TC-09 的复合条件（count min 3 + target_hp_pct max 30 + self_status 冰冻）与定稿 L322-326 的 `and` 数组逐字段一致
- TC-18/TC-16/TC-17 的校验器行为分别命中 L357/L361/L358 原文
- TC-24 的职业参数（剑4/reset、法5/hold、刺2/reset、盾3/hold）命中 L170-173
- TC-46/TC-47/TC-43 分别命中 L189-191（多链存储/按新链判定）、L343（删链降级不报错）、L183+L332-342（combo_state 快照，含"3.18"引用与 JSON 结构字段一致）

---

## P1（行号引错 / 补白漏标 / 数值无依据）：1 项

| 位置 | 声明 | 定稿实际 | 证据 | 定性 |
|---|---|---|---|---|
| TC-48（细化 L111） | "连段转少量经验 / 下场起始 +1（可配项，**默认不启用**）" | 定稿仅写"（可配）"，**未声明默认值**；全文中默认值类表述只有 L213"跨战斗蓄力开关默认关"、L196/L359"≤1.5× 默认可配值"，无任何一行支撑"补偿默认不启用" | 定稿 L238："大招溢出补偿：目标死亡时连段未消耗 → 转少量经验/下场起始+1（可配）"；对比 L213、L196、L359 | **补白漏标**：作者补充了定稿未指定的默认值且未标注为补白（轻微——推测系从 L213 "默认关"惯例类推出，逻辑合理但无依据行） |

行号引错：**0 处**。数值无依据：**0 处**（所有数值——max_combo 4/5/2/3、eq 2/4、cost/mp_cost 字段、hit 计数、5→3 消耗——均有定稿行支撑；测试输入场景本身属用例构造，不视为断言）。

---

## P2（轻微）：5 项

1. **文末覆盖声明夸大**（细化 L124："覆盖定稿 L29-L388 全部连段相关规则行"）。定稿中多条连段相关规则未被任何 TC 引用：6.3 倍率封顶（L196-198）、6.4 每节点≥2 出路（L205）、6.5 怪物侧预留/装备特性词条（L210/L212）、7.4 消息合并（L243-245）、八章学习分级（L251-265）、十一章编辑器建议（L369-375）、校验器条目 L351-353/L355-356/L359-360/L362。声明"全部"言过其实，但"无跨章节自造规则"成立。
2. **覆盖矩阵章节列与实际行号引用不完全对应**（细化 L12-L20）：①行列 2.1/2.2/3.3/6.5/7.1，但 TC-01~08 实际还引用 3.2（L81）、4.3（L143）、7.3（L235）、9.2（L314）；⑥行列 2.2/6.4/12，TC-32 却引用 3.3 的 L94；⑧行列 6.2/9.4/7.3，TC-45 引用的是 6.5 的 L213。属示意性组织误差，不影响逐条核对。
3. **TC-11 边界语义为解释性补白**（细化 L44）：断言"连段 4 且 HP 50% 满足（≤ 含边界）；HP 51% 不满足"。定稿 L89/L295 仅写 "max: 50"，未明示含等号。作者在括号内显式声明了约定（透明处理），但按"补白应标注"口径仍未标识为解释。
4. **TC-17 "禁止加载"为强度推演**（细化 L55）：断言"校验器可达性检查报错（死配置），禁止加载"。定稿 L358 仅列"可达性：所有链节点从任一入口可达"；同节 L357 对 reset eq=max 死配置只写"提示改用"，未给任何校验项"禁止加载"的严重级别。由 L354 "死配置检测"标题推演而来，逻辑成立但属未标注补白。
5. **TC-11 引用的 L295 与场景不同构**（细化 L44）：场景写范围条件 `{count:{min:4}}`，但所引 L295 实为 9.1 示例步骤中的 `{"count": {"eq": 4}, "target_hp_pct": {"max": 50}}`（eq 而非 min）。min:4 的实际依据在 L89，L295 仅能支撑 target_hp_pct 复合结构与 max 字段存在。属"示例行支撑略松"，非错引。

---

## 核对明细（抽样摘录，覆盖全部 9 类）

- **① 连段计数**：TC-01 L42/L51（+1 规则）/L223（状态行格式 "连段 X/4"）/L314（mp_cost 字段）✓；TC-03 替换式=用后还原（L81）+ 派生保留连段（L73）+ 链上节点非终点（L69）+ eq 2（L88）+ 快照评估（L92）✓；TC-05 hits 按施放次数计（L211）✓
- **② 条件化派生**：TC-13 求值失败默认不满足（L328-329）✓；TC-14 回合数 vs 段数区分（L214）✓；TC-04 置灰+标注原因"需连段2"不隐藏（L94+L230）✓
- **③ 循环互派生**：TC-15 链形 A→B'→A''→B'' 与 L99-101 逐节点一致 ✓；TC-16 环=特性不做无环校验（L103+L361）✓
- **④ 到顶 reset/hold**：TC-19/20（L154/L156 + 职业 L170/L171）✓；TC-21 先判 eq=max 派生再归零（L158）✓；TC-22/23 hold 免疫被动态与消耗退出免疫（L163-165）✓
- **⑤ 防御不打断**：TC-25~29 全部命中 L54/L29/L55/L58/L57（含"防御同款""断节奏不断积累"原文）✓
- **⑥ 指令被拒**：TC-30~33 命中 L56/L203/L204/L388（冷却派生技降级为原技能+连段保留 L203 与指令被拒 L204 的调和解读正确）✓
- **⑦ 打断/霸体**：TC-34~40 全部命中 L112-117/L124/L128/L135/L141/L142/L385-386；TC-39/40 的"归口"表述（interrupt 唯一实现走 effects.json、immune_vs:interrupt 免疫矩阵维度）与 L46/L47/L315 原文一致 ✓
- **⑧ 战斗结束清零**：TC-41~48 命中 L59/L182-184/L189-191/L213/L238/L332-343 ✓
- **⑨ MP 不足**：TC-49~52 命中 L56/L203-204/L388/L51；TC-50"MP 不足整体拒绝、区别于冷却降级"与 L203（冷却降级）vs L204（被拒）的分野一致 ✓

---

## 结论

**评分 A**：52 条用例、90+ 处行号引用全部真实准确，断言与定稿逐条对应、消息文案逐字一致，无任何编造或实质冲突；仅 1 处默认值补白未标注（P1）和 5 处覆盖表述/边界语义等轻微瑕疵（P2），不影响文档作为验收测试集的可用性与可信度。