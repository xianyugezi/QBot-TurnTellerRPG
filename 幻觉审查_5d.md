# 幻觉审查报告：细化_5d_测试体系总纲.md

- 审查对象：`/root/QBot-TurnTellerRPG/细化_5d_测试体系总纲.md`（v1.0，2026-08-18 02:09 生成）
- 定稿依据：`/root/docs_archive/RPG框架项目/开发规则文档.md`（v1.0，590 行，已逐行通读）
- 核对方法：通读细化全文 → 抽取全部行号引用/数值声明 → 回定稿逐一核对；对 1228 条 TC 统计口径做全量文件枚举 + 逐文件 TC 行数实算；对细化文档引用（TC 号/前缀/区间）逐一现场核对。
- 核查日期：2026-08-18

---

## 一、行号引用核对结果（抽查全量）

**全部 47 处【规则】行号引用经逐行比对：内容一致、行号正确（绿色通过），无一处引错。** 明细：

| 细化_5d 引用 | 定稿实际内容 | 结论 |
|---|---|---|
| L279（pytest 单测+故障注入，转引框架文档15.16） | 「依据《框架设计文档》15.16：pytest 单测伤害公式/状态机/存档往返；故障注入（回复前崩溃路径）」 | ✅ |
| L47（tests 与插件同仓） | 目录树 `tests/ # pytest 测试（与插件同仓）` | ✅ |
| L52（引擎不 import NoneBot）、L53（依赖单向） | 1.1 分层铁律两条 | ✅ |
| L92（校验收敛） | 「禁止散落各处 if "xxx" in data」 | ✅（"校验收敛"为细化自拟概括，语义一致） |
| L129（JSON 是运行时公共 API） | 二章引言原文 | ✅ |
| L133 / L394（schema_version） | 2.1 manifest 定义 / 6.1 内容包版本 | ✅ |
| L152-157（红拦 5 类） | 2.3 红色拦截 5 类逐条 | ✅ |
| L159-163（只建议不限制/黄提示） | 2.3 黄色提示 4 条 | ✅ |
| L178（校验失败回退 registry） | 「校验失败 = 回退旧 registry + 人话提示，绝不半套配置运行」 | ⚠️ 内容对，但见 P1-2（PackLoadError 异名不存在于此行） |
| L269（测试执行结果附 pytest 摘要） | 3.4 PR 描述模板第二项 | ✅ |
| L283-285（单元/集成/冒烟三层） | 4.1 三条 | ✅ |
| L289-297（七模块必测场景） | 4.2 表格 battle/damage/levelup/inventory/equipment/worldtime/storage 七行（L291-297） | ✅ |
| L293（levelup 经验曲线边界）| 4.2 表 levelup.py 行 | ✅ |
| L294-295（inventory/equipment）| 4.2 表两行 | ✅ |
| L296（worldtime 刷新补刷/跨天/离线封顶）| 4.2 表 worldtime.py 行 | ✅ |
| L297（storage 事务/崩溃/并发写/防抖）| 4.2 表 storage.py 行 | ✅ |
| L299-304（4.3 存档往返）| 4.3 全文 | ✅ |
| L301（五类快照）、L302（删除配置降级）、L303（版本迁移）、L304（random_seed 往返）| 4.3 四条 | ✅ |
| L306-311（4.4 伤害公式）| 4.4 全文 | ✅ |
| L308（确定性用例 ATK=30 例）、L309（性质用例 1000 组）、L310（判定顺序）、L311（公式参数 fixture 禁硬编码）| 4.4 四条 | ✅ |
| L313-326（4.5 故障注入）、L317-325（六故障点表）、L319-324（六行）、L326（禁止"不崩就行"）| 4.5 全文 | ✅ |
| L330（SQLite :memory:/tmp_path）、L331（固定种子）、L332（四类样例包各一）、L333（覆盖率≥80%）| 4.6 四条 | ✅（L333 仅定门槛，工具选型参见 P2-1） |
| L351（zip 白名单+条目/大小上限）| 5.2 A 组 zip 项 | ✅ |
| L365（保存失败兜底+本地暂存）、L366（message_id 幂等键）| 5.2 C 组两项 | ✅ |
| L369-372（零代码约束四条）| 5.2 D 组 | ✅ |
| L370（默认值兜底）、L371（字段元数据表）、L375（上限可配 0=不限）| 5.2 D/E 组 | ✅ |
| L378-380（串行队列/全局锁/会话互斥）| 5.2 E 组三条 | ✅ |
| L385（测试覆盖 P0）| 5.2 F 组 | ✅ |
| L420-433（发布检查单、L422 三件套、L423 ruff/mypy、L426 黄提示记录、L429 安全回归）| 6.3 检查单（L420 标题，L422-433 各项） | ✅ |
| L435（冒烟 /注册 /锁定 /攻击 一轮闭环）| 6.3 发布顺序末句 | ✅ |

**定稿引用小结：47/47 行号引对、内容一致。主要依据引用质量优秀。**

---

## 二、细化文档引用核对结果

| 细化_5d 声明 | 现场核对结果 | 结论 |
|---|---|---|
| 细化_1c3 为 TC-01~TC-52（52 条）| `细化_1c3_连段测试集.md` 实有 52 行 TC-01~TC-52；TC-13 内容=「条件表达式求值异常→安全失败」与 5d 故障注入"兜底0/条件不满足"同断言 | ✅ |
| 细化_1c1c 19 条，分组前缀 TOP/DEF/END/INT/ARM | 1c1c 实为 **TOP(7)+DEF(5)+END(7)=19 条，无 INT/ARM 前缀**；INT/ARM 前缀属于细化_1c2（INT-01~05、ARM-01~04，共 15 条=矩阵 1c2 计数） | ❌ 见 P1-1 |
| 细化_2b3 42 条 | `细化_2b3_商店引擎契约.md` 实有 TC-01~TC-42 | ✅（但 3.1 的 TC-201 举例矛盾，见 P1-3） |
| 细化_3c TC-01~45 | 实有 45 行 TC-01~TC-45 | ✅ |
| 细化_3e2 TC-06~09（回退/人话提示）| TC-06=非法 JSON 回退 registry；TC-07 红拦；TC-08 死配置；TC-09 连续坏包轮询暂停——均符 | ✅ |
| 细化_3e2 TC-10~12（旧局旧配置边界）| TC-10 战斗中删除配置持旧快照；TC-11 中断恢复用旧配置；TC-12 新旧两局并发——均符 | ✅ |
| 细化_5c 数值经济总表 TC-01~18 | 实有 18 条，但编号为 **TC-1~TC-18（无补零）** | ⚠️ 计数对、编号格式不符，见 P2-2 |
| 细化_1a/1b/1c1a/1c1b/1d/1g1a/1g1b/1g2/1g3/2a1a 无具名 TC | 逐一核实均为 0 条 | ✅ |
| 细化_2a1b/2a4a/2a4c 无具名 TC（§0.2 点名）| **假**：2a1b 实有 17 条、2a4a 实有 22 条、2a4c 实有 25 条具名 TC 表 | ❌ 见 P0-2 |
| 实现层规划文档 M01-M43 / T01-T53、M43 时间锚点回归 | `/root/QBot-TurnTellerRPG/实现层规划文档.md` 实含 M1~M43、T1~T53；M43=「周期值只能由锚点公式得出，无定时器驱动」回归 | ✅ |
| PackLoadError 聚合抛错（L178）| 定稿全文 0 次出现该名；该异名实出自 `细化_3e_loader校验接线.md`（7 处）| ❌ 见 P1-2 |

---

## 三、P0 / P1 / P2 清单

### P0-1 统计口径不成立：「62 份文档 / 合计 1228 条 TC / 全部覆盖」与真实语料不符

**证据（全量枚举实测）**：
- 目录实有 **73 份** 细化文档（`细化_0` ~ `细化_6c`），非 62 份；声明范围「细化_1a~细化_5c」一档内实为 67 份（含 5d），64 份为 5d 之外——无论按哪种口径都凑不出 62。
- 全量具名 TC 行（按 §0.2 自定口径「表格行首为 | TC- 的具名用例行」逐文件实算）：**1505 行（含 5d 自测 36 条）／1469 行（不含 5d）**；声明范围 1a~5c 内为 **1381 行**。
- **1228 = 仅矩阵点名的 49 份带 TC 文档计数之和**（各文档计数经逐一核实全部准确、各里程碑合计 87/116/34/81/176/620/114 与加权和 1228 算术自洽）——它是一个真实的**子集小计**，却被标注为「62 份文档合计」「全部覆盖」的总量。
- 即便按最宽松解读（62 份=矩阵点名文档数 49+13），「全部覆盖」亦不实：范围内仍有 5 份带 TC 文档（1g4:17、2a1d:12、2c2d:24、3h:18、5a2:18，共 **89 条**）完全未入矩阵；其文件 mtime 显示写作时点（08-18 02:09）前后同批生成，5e(02:08)、2a1b(00:02)、2a4a(00:06)、2a4c(00:09) 明确先于 5d 落盘。
- 影响面：§0.2 口径、§2.1 矩阵、§6.1 门禁 G1~G6 的「87/116/34/81/176/620/114 条 TC 全绿」、附录 A 全部建立在该错误总量上；按此实现 verify 脚本将**系统性漏测约 153 条（范围内）/241 条（全库）用例**，且门禁数量基准可能失真。

**处置建议**：重算口径。正确基线应为 范围 1a~5c 内 1381 条（或全库 1469 条），矩阵补入 1g4/2a1d/2c2d/3h/5a2 五份文档（或明确声明排除并给出理由）。

### P0-2 「无具名 TC 文档」点名错误：2a1b/2a4a/2a4c 实含 64 条具名 TC

- §0.2 明文把 **2a1b（通道刷怪）、2a4a（时间引擎）、2a4c（时间天气接口）** 列入「无具名 TC 的文档」，且 §2.1 M3 行的「补充引擎覆盖」栏同样把它们当作无 TC 处理（→worldtime.py 必测场景承接）。
- 实测三份文档均为正规具名 TC 表：`TC-01~TC-17`（2a1b）、`TC-01~TC-22`（2a4a）、`TC-01~TC-25`（2a4c），共 64 条，内容为该子系统具体行为断言。
- 后果：这 64 条既不在 1228 计数内、也不会被任何 verify 脚本执行——M3 里程碑的验收范围实际少掉一整个时间/通道语义面。属于可验证为假的声明（非统计口径歧义）。

### P1-1 前缀分组实例张冠李戴：TC-5d-11 断言的前提不成立

- TC-5d-11：「对 1c1c 文档执行 TC 采集 | 分组前缀编号（TOP/DEF/END/INT/ARM）被正确归组，19 条全采出」。
- 实测 1c1c 的 19 条只有 **TOP(7)/DEF(5)/END(7)**；**INT/ARM 前缀只存在于细化_1c2**（TC-INT-01~05、TC-ARM-01~04）。按 TC-5d-11 原样执行采集器必然采不到 INT/ARM 组，用例无法通过。
- §0.2 的前缀举例同样错位：`TC-INT-xx / TC-ARM-xx` 应属 1c2；`TC-2a-xx` 实为 `TC-2a3-xx`（细化_2a3）。

### P1-2 PackLoadError 被伪引到【规则】L178

- §2.2「内容包全量 validator（L178 阻断式抛错）」与 §5.1「聚合抛 `PackLoadError`（L178）」：**定稿全文无 PackLoadError 一词**，L178 仅表述「校验失败=回退旧 registry + 人话提示，绝不半套配置运行」。
- 该异常类名真实出处是 `细化_3e_loader校验接线.md`（7 处）。行号引对了"校验失败阻断"语义，但把异名归因到不存在的行——引用应改为细化_3e 对应条款。

### P1-3 过滤示例自相矛盾：2b3 不可能有 TC-201

- §3.1 表格：「`--only m5:201-260` …（201-260 为 细化_2b3#TC-201 起举例）」。
- 但 §2.1 矩阵与实测均确认 2b3 只有 TC-01~TC-42。示例选取了一个不存在于任何文档的 TC 区间，与同一文档自己的矩阵声明冲突；TC-5d-20 中该区间又被正确写作 `2b3:1-42`，可见是写作疏漏，但作为"官方回归入口"的文档示例必须自洽。

### P2-1 「L333 已定」越界

- 文档头声称「不含覆盖率工具选型细节（【规则】L333 已定）」；定稿 L333 只定了 `engine/+content/ ≥80%` 门槛与"新功能必须有测试"原则，**并无工具选型**。建议改为「L333 已定门槛」。

### P2-2 编号形式描述与实际不符

- §0.2 称「文档内连续编号（主形式）TC-01 ~ TC-NN」；实测 细化_5c、2a4b、1e、3e 等使用无补零的 `TC-1~TC-N`；§4.1 引用的「细化_5c（TC-01~18）」与实际 `TC-1~TC-18` 不符。收集器若按 `TC-\d{2}` 正则实现会漏采这几个文档。

### P2-3 新设默认值未入 §7.4 决策记录

- 文档新增的具体阈值——斩杀线容忍带默认 100%、概率偏差容差默认 1%、性质用例 --fast 抽样 100 组、datapack 全量 10 万次抽样、执行预算 全量<15min/--fast<3min——均为实现层新设定，D1~D4 决策记录未覆盖。文档自身纪律「任何偏离【规则】之处必须写入 §7.4」，建议补 D5~D8 显式登记（审计项③补白标注不完整，非语义错误）。

---

## 四、数值与算术自检（通过项摘要）

- 各里程碑小计与矩阵加和：87/116/34/81/176/620/114，合计 1228，算术无误（但见 P0-1）。
- 附录 A：7 脚本、15 脚本合计（7+1+1+6）、8 道门禁（G0~G7）、36 条自测（6+10+20）全部自洽。
- 7.4 决策记录 D1~D4 有依据引用、有影响用例，补白机制存在且诚实。
- 47 处定稿行号引用零错误（见第一节明细）。

---

## 五、评分与结论

**评分：C**（可用基础扎实——定稿引用与逐文档计数极其可靠、结构纪律良好；但头条统计口径与"全部覆盖"声明是实质错误，将导致按本总纲实现的验收体系系统性漏测 64~241 条用例，且两条验收用例建立在可证伪的前置上，须修订后放行。）

**一句话：行号与单文档计数全对、1228 仅为矩阵子集小计却被宣称 62 份文档全量覆盖，实际应为范围内 1381 条（全库 1469 条），并需修正 1c1c 前缀归组、PackLoadError 出处、2b3#TC-201 三处引用错误。**