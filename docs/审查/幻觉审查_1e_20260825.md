# 幻觉审查_1e（复审 2026-08-25）：《细化_1e_怪物八段schema》实现前最终幻觉审查 + 实现视角缺口检查

## 头部

- **审查对象**：`/root/QBot-TurnTellerRPG/docs/细化/细化_1e_怪物八段schema.md`（259 行，v1.0；注意：文件名小写 `schema`，非任务题述的 `Schema`）
- **对照基准（已 wc -l 核对行数）**：
  - 《怪物模块设计定稿.md》v1.0.3 = **398 行**（首审时为 396 行，**定稿在首审后新增 2 行，详见结尾基准说明**）
  - 《怪物行动AI设计定稿.md》v1.0.1 = **322 行**
  - 兄弟细化：《细化_1f_怪物AI状态机.md》= 266 行；《细化_1g4_战斗世界边界.md》= 334 行
  - 首审报告：《幻觉审查_1e.md》= 91 行（08-18，2 P0 / 3 P1 / 4 P2）
- **审查方法**：静态审查（本环境无 bash 沙箱，仅 read_file/write_file，未运行任何命令做运行时验证；所有结论为「静态推导」）。四查口诀：①行号引对 ②内容一致 ③补白显式标注 ④数值有依据。行号引用逐条回定稿原文核验（含 1e 全文所有 [Lxx]/[AI Lxx]），不采信 1e 自述。
- **范围**：①首审 P0/P1 闭环核对 ②1e 全文行号全量核验 ③实现视角缺口（新增）④交叉一致性（1e×1f×1g4×AI 定稿）⑤内部自洽（字段计数/TC）。

---

## 总体结论

**P0 = 1 / P1 = 2 / P2 = 7 —— 评分 C**

**一句话**：首审大项已闭环大半——P0-2（chains 编造结构）、P1-1（AI 行号 L502 等 6 处编造）、P1-3（A03 归一化算术 5/8 错误）三项**已修复且核对准确**；但 **P0-1 触发枚举归一方向未完全闭环**（映射表 canonical 列 / TC-08③ / TC-14 三处旧别名残留，与 S3/S4/A05/R12/R-01 自相矛盾），**P1-2 概率语义冲突复发**（1e 的"必填+拦截"与 AI 定稿 L25/L29/L51/L283 及兄弟 1f §4.1/4.2 相左），且**新检出定稿 396→398 行基准漂移导致 1e 后半段约 35+ 处行号引用整体失准（多为 +2）**——属行号失真非捏造（内容均在 ±2 行内可找回），可放宽定级但必须全量修订后再实现。

---

## P0（必改）

### P0-1 触发枚举归一方向残留 3 处，首审 P0-1 未完全闭环（S3/S4/A05/R2/R12 已修对，但映射表/TC-08③/TC-14 仍反向）

- **位置（1e 内）**：1.4 映射表 ②③④ 行（L99-101）、TC-08③ 输入标注（L233）、TC-14（L239）。
- **声明**：1e 自述 S3/L12、S4/L15、A05/L89、R2/L201、R12/L211 已按细化_0 R-01 归一（canonical = AI 定稿 §二 13 类权威名 pv_broken/get_up/battle_start；旧名 broken/revive/enter_phase 为兼容别名）。但映射表与两条 TC 仍带旧别名方向。
- **核对**：
  - 映射表（L99-101）列头为「§7.1 语义 | 示例写法（L178-183）| **canonical 枚举** | timing 示例」，②③④ 行 **canonical 枚举列**分别写 `broken（旧别名）`/`revive（旧别名）`/`enter_phase（旧别名）`——把旧别名置于 canonical 位置，与同表行 ①（hp_below→hp_below）的"canonical=权威名"自洽方向相反，也与 S3/S4/A05/R-01 矛盾。定稿 §7.1 示例 L179-181 实际写法即为 `pv_broken`/`get_up`/`battle_start`（=权威名），canonical 列应同为权威名。
  - TC-08③（L233）输入标注仍写 `type:"pv_broken"（旧枚举别名）`，但同格预期又写「③ 通过（权威枚举 pv_broken，R-01）」——标注与预期自相矛盾（pv_broken 是权威名非旧别名）。
  - TC-14（L239）运行期验收仍以 `enter_phase` 作开场技触发名——权威名应为 `battle_start`（AI 定稿 §二 ④ L66），enter_phase 为旧别名（定稿 L298 旧枚举）；按此验收会迫使实现/测试使用错误字段名。
- **证据**：AI 定稿 L63-66（② pv_broken ③ get_up ④ battle_start 均为权威 13 类名）；细化_0 R-01 归一方向（旧名→权威名，首审 L17-20 转述）；1e L99-101、L233、L239 vs 1e L14/L15/L89/L201/L211（方向已对）。首审 P0-1 所列 5 处（S4/映射表/R12/TC-08③/TC-14）中仅 S4、R12 修复，映射表/TC-08③/TC-14 三处残留。
- **建议**：映射表 canonical 列 ②③④ 改为 pv_broken/get_up/battle_start（旧别名仅留作"兼容别名"注记或并入独立别名表）；TC-08③ 输入改为 `type:"broken"`（旧别名）测别名接受，或删"旧枚举别名"标注；TC-14 `enter_phase`→`battle_start`。修后需全表 grep 确认无 broken/revive/enter_phase 出现在"canonical/验收名"位置。

---

## P1（要改）

### P1-1（新检出·批量）定稿 396→398 行基准漂移，1e 后半段行号引用系统性失准（校验器/编辑器/木桩/§十二相关，约 35+ 处，多为 +2）

- **位置（1e 内）**：R1-R7（L200-206）、R8（L207）、R12（L211）、R13（L213 的 L307 引用）、R14（L213）、S3（L14 的 L296 引用）、S5（L16 的 L325-392）、F03（L30）、F04（L31）、F05（L32）、F09（L36）、F10（L37）、F17（L44）、F18（L45）、A01（L80）、A03（L82）、A04（L88）、A05（L89）、D01/D02（L114-115）、D03（L116）、L01（L125）、②表 L139/L152、默认值语义 L151、④ 木桩表（L174-190 全表）、⑤ 编辑器 L216、附录 A 末三行（L253/L256-258）。
- **声明**：1e 各条标注的 [Lxx] 为"正确那一行"。
- **核对（抽样实测，均对照当前 398 行定稿）**：
  - 校验器 7 条实际在定稿 **L297-303**（1e 引 L295-301，+2）；编辑器在 **L305-309**（1e 引 L303-307，+2）；
  - 木桩 15.2 特例实际在 **L350-356**（1e 引 L348-354，+2）；15.1 tier/type 实际 L338-339（1e 引 L336-337）；15.4.1 调整木桩实际 L381-384（1e 引 L379-382）；15.3 校验器实际 L365（1e 引 L363）；
  - §十二 样本：pv_recover 实际 **L278**（1e 引 L277，+1）；actions 样本实际 **L281**（1e 引 L279，+2）；校验 6 图鉴实际 L302（1e 引 L300）；编辑器掉落条件下拉实际 L309（1e 引 L307）；
  - 木桩"不可反击"实际 L350（1e 引 L348）、"无掉落"实际 L351（1e 引 L349）、"pv 强制 0"实际 L352（1e 引 L350）、"不入图鉴"实际 L353（1e 引 L351）。
- **证据**：当前定稿 wc -l = 398；首审报告 L4 记定稿为 396 行；1e 引用在这些区段全部 +1~+2 偏移，而 1e 前半段（L19-31/L40-45/L55-62/L67-102/L106-118/L122-148/L152-185/L189-212/L216-230/L234-242）经逐条回核**全部准确**——即偏移始于 §十三 校验器区段之前，且首审（396 行基准）通过过这些引用，判定根因=定稿后续新增 2 行、1e 未重刷引用。
- **性质**：**行号失真非捏造**（引用目标内容均存在、距标注行 ±2 行内），按任务惯例可放宽，但因覆盖面大（约 35-40 处）、且 1e 是"实现前最终审查"对象，实现者逐条追引会落错行，必须整体修订后方可交付。
- **建议**：以当前 398 行定稿为基准全量重刷后半段引用（或统一批量 +2/+1 校正）；修订后需再跑一次全量行号回核。

### P1-2（首审未修复·复发）概率语义仍与 AI 定稿及兄弟 1f §4.1/4.2 冲突（必填+拦截 vs 默认0+等价1+只提示）

- **位置（1e 内）**：S1（L12）、A02（L81）、A03（L82）、R10（L209）、TC-07（L232）。
- **声明**：1e 定 probability「必填 ∈{0,1}」、weight「必填 ≥0」、"至少一个入池条目 weight>0"（防空随机池），R10 将「probability ∉ {0,1}」「空随机池」定为**拦截**，S1 自述"三处一致，无歧义，只做字段化"。
- **核对（AI 定稿权威，逐条冲突）**：
  - AI 定稿 **L25**「probability 默认值 = 0（漏配=锚点行动，只被链/条件/状态机触发）；参与随机池写 1」——1e 标**必填**，堵死"漏配默认锚点"语义；
  - AI 定稿 **L51**「写其他正值等价 1」——1e R10/TC-07 对 p=2 之类判**拦截**；
  - AI 定稿 **L29**「所有字段默认值兜底，**校验器只提示不拦截**」、**L282-283** 硬拦仅「引用不存在/类型错误/负数/结构错误」、「概率总和为 0 → **黄提示**」——1e R10 把"空随机池/防空"升为**拦截**。
  - 兄弟细化 **1f §4.1（L145）** 已按 AI 定稿取「probability 默认 **0**、写其他正值等价 1」，**1f §4.2（L155）** 取「概率总和为 0 → 黄提示；只建议不限制」——**1e 与 1f 概率语义不一致**（交叉一致性检查项失败）。
- **证据**：AI 定稿 L25/L29/L51/L282-283；1f L145/L155；1e L12/L81-82/L209/L232。
- **建议**：A02 改「选填，默认 0（漏配=锚点）」；R10 拦截级收敛为「类型错误/负数/结构错误」，probability∉{0,1} 按"等价 1"容忍（或黄提示）、"概率总和为 0/空随机池"降为黄提示；TC-07 相应改预期；S1 删"三处一致无歧义"表述并显式声明采纳 AI 定稿 L25/L29/L51 语义，与 1f §4 对齐。

---

## P2（轻微，记录）

- **P2-1**：F14（L41）"节点结构以 AI 定稿 **§七**为准"、1.7（L133）"chains 节点结构（按 AI 定稿 §七）/ BOSS 编排扩展（AI 定稿 §七）"——两处"§七"仍错，实际均在 AI 定稿 **§八**（连招载体唯一化 L248-266；顶层扩展 phases/zone_change/teach_map/pressure L232-246；§七 实为博弈体验 L215-226）。行号引用已对（AI L248-266），仅 §编号文本未改——首审 P1-1 的 §七 残留。
- **P2-2**：chains"链不成环（提示不拦截）[AI L265]"引用不当——AI L265 实为"校验器：接受旧形状并提示迁移"，与成环无关；R15（L214）"环形链=有意的循环连招时放行"为 1e 自创理由，均未按 L6 标注约定标【细化定型】/【派生】/【工程补白】（首审 P2-3 残留）。
- **P2-3**："PV 档默认 10/75/300 取区间中值"论据仍错（L6、L147、TC-13②）——BOSS 档区间 200-500 中值应为 **350**，300 实为 AI 定稿样本值（AI L236 `"pv": 300`）；数值可保留但"取区间中值"表述需更正（首审 P2-2 残留）。
- **P2-4**：L3 辅助引用文件名仍作《BOSS战编排设计定稿》，实际文件为《怪物行动AI设计定稿》（有"= AI 定稿"自注缓和）（首审 P2-1 残留）。
- **P2-5**：细化定型未按 L6 标注：F03 tier 默认 `normal`、D03 `after_action:<action_id>` 语法（定稿 L209 仅"特定行动后"语义）（首审 P2-3 残留）。
- **P2-6**：门禁管线顺序（①免疫→②抗性→③PV 减半→④破防）引 [L250]，实际 ①-④ 顺序声明在定稿 **L251**（L250 为"效果系统：PV 减半=拦截链"），差 1 行。
- **P2-7**：②表"低"行来源引 L237-238 混入精英行（数值实际取 L237 无误）（首审 P2-4 残留）；交叉项：1e/定稿木桩覆盖存储键 `dummy_override`（定稿 L384）与 1g4 的 `dummy_state`（1g4 HR-04/J-02）命名不一致，建议统一登记。

---

## 核对亮点（首审闭环情况）

- **P0-2（chains 编造定稿字段）→ 已修复 ✓**：现 1.4（L104）/R15（L214）节点 = `{action, chance 0-1（示例 0.8/1.0）, role: chain|finisher, armor?}`，与 AI 定稿 §八 完全一致（L253-260 schema、L260 role/armor 语义、L254-255 样本 0.8/1.0）；"§七"已改为"§八"（1.4/R15）；`condition` 显式标【工程补白】"AI 定稿节点无 condition 字段"；R15 role 校验改回 {chain, finisher}，不再拦截 AI 定稿合法配置。
- **P1-1（AI 行号编造 L502/L512-515/L524-525/L529）→ 已修复 ✓**：全部清除；现引 [AI L248-266]（=§八 连招载体唯一化，核对准确）、[AI L265]（=校验器接受旧形状提示迁移，准确）、[AI L205]（=6.5 高难 40-60 回合，准确）均回核无误。
- **P1-3（A03 归一化算术 5/8,3/8,2/8）→ 已修复 ✓**：现为 1/2、3/10、1/5（=50/100、30/100、20/100），计算正确，旧分数无残留。
- **P0-1 → 部分修复**：S3/S4/A05/R2/R12 五处主体已改对方向（权威 13 类 + 旧名兼容别名、x_ 前缀可扩展），但映射表/TC-08③/TC-14 三处残留（见 P0-1），**未闭环**。
- **P1-2 → 未修复（复发，见 P1-2）**。
- **主体核对通过**（内容一致、仅涉行号漂移已单列）：F01-F18 前半段样本与约束（L19-31/L274-287）、S01-S09 九键（L40-45）、W01-W04 双维弱点/抗性（L55-62/L106-118）、PV 规则与换区恢复（L67-102）、掉落三类与 reward 同键（L189-212）、图鉴/codex_state（L216-230/L230）、② 难度模板乘区（L42-45/L101/L237-239）、TC-01~06/09~13 预期与正文自洽、④ 木桩特例内容与定稿 §十五 一致（仅行号漂移）；R8-R10 派生标注较首审已有改善（R8"派生自结构定义"/R9"派生自模板语义"/A03"派生自归一化语义"）。

---

## 实现视角缺口清单（本次新增检查，逐项给出"实现者按此落地会不会踩坑"）

1. **chains 顶层表结构（任务 ③①）**：1e L104 已列节点字段 `{action, chance 0-1, role chain/finisher, armor?}`（比首审"只登记不展开"已改进），但**链条目顶层结构 {id, actions[]} 未登记**（AI 定稿 L250-258 才有 `chains:[{id, actions:[节点...]}]`）、**chains.id 唯一性/引用存在校验未提**、**special_actions.chain_ref→chains.id 引用链路在 1e 完全缺失**（A04-A08 无 chain_ref 字段）。→ **会踩坑（中）**：按 1e 落地会丢掉连招引用能力。
2. **掉落 condition 与 13 类触发枚举关系（任务 ③②）**：D03 condition ∈ {pv_broken, no_damage, after_action:<id>} 与 A05 trigger.type 13 类是**两套独立枚举**（no_damage 无触发对应；pv_broken/after_action 虽同名但一个用于掉落条件、一个用于触发类型），1e 未声明二者独立；`after_action:<id>` 语法未标细化定型/补白。→ **会踩坑（低-中）**：实现者可能误复用 trigger.type 校验或对 no_damage 困惑。
3. **换区 PV 恢复一半「向下取整」（任务 ③③）**：1e F10 表述与定稿 L98（"恢复比例=zone_change.pv_recover 默认 0.5…取整默认向下取整"）、AI §八 L242/L193 一致；与 1g4 **无冲突**（1g4 只管 HP 脱战回血 HR-01~05，1e 管 PV 换区恢复，两线正交）。缺口：1e F10 未登记恢复比例键 `zone_change.pv_recover`（归口 AI §八，不在八段），且未声明 1g4 J-01 的副本 BOSS 换区边界（副本换区不恢复 HP，PV 恢复是否适用未裁）。→ **基本清楚，轻度踩坑**：实现者需联读 AI §八 + 1g4。
4. **木桩 def_base「直读 vs 映射 stats 体质二选一」（任务 ③④）**：定稿 L341 与 1e F17/R14 均保留"二选一，校验器提示一致性"——**无模式标记字段**，引擎无法判定当前木桩走直读分支还是映射分支，"校验器提示一致性"的实现口径（启发式？配置约束？）未定义。→ **会踩坑（真实歧义）**：建议 1e 细化定型（默认直读；映射需显式声明或新增 mode 补白字段），并定义校验器提示的具体触发条件。
5. **编辑器联动段（任务 ③⑤）**：只读引用（L216），不新增数据字段，不影响 enemies.json schema 落地；仅行号漂移（L303-307→L305-309）。→ **不踩坑**。
6. **（附加·高）special_actions 条目 schema 不完整**：1e A04-A08 仅登记 action+trigger 5 参数，缺 AI 定稿 §二 条目的 `id / priority / trigger_cooldown / max_triggers / once / post_state / chain_ref`（AI L78-89 样本）——而 1f ② L3 条件行动评估依赖 priority/once/max_triggers/trigger_cooldown（1f TC-05/TC-08 直接用 once/max_triggers），按 1e 落地的 special_actions 与 1f/AI 不兼容。→ **会踩坑（高）**：本缺口比 ① 更实质，建议 1e 补登记或显式交叉引用 1f §二/AI §二。
7. **（附加·中）actions 条目 schema 不完整**：1e A01-A03 仅 3 字段，缺 AI 定稿 §一/1f §4.1 的 `condition / cooldown / hungry`（1f L138-145）——实现者按 1e 的 actions 条目会丢条件权重修正/冷却/饥饿保底能力。→ **会踩坑（中）**：建议补登记或交叉引用 1f §四。

---

## 结尾核对基准说明

- 行数核对（wc -l）：怪物模块设计定稿 = **398**（首审记录为 396，定稿在 08-18 后新增 2 行，位于 §十三 校验器区段之前，导致 1e 后半段引用整体偏移 +1~+2）；怪物行动AI设计定稿 = **322**；1f = 266；1g4 = 334；首审报告 = 91；1e = 259。
- 首审基准（396 行）下 1e 的校验器/木桩等引用曾经正确（首审"核对通过"已确认），本次偏移属**基准漂移未同步**而非首审漏检，亦非 1e 捏造。
- 交叉一致性结论汇总：概率语义 1e×1f **不一致**（P1-2）；PV/换区 1e×1g4 **无冲突**（正交，唯 dummy_override vs dummy_state 命名、木桩战败死亡惩罚口径存在张力）；chains 1e×1f §五 **同源一致**（均引 AI §八 {action,chance,role,armor}，role chain/finisher），仅 1e 独增"链不成环"检查 1f 未提（P2-2）。
- 1e 内部自洽：字段计数 顶层 18 / 全量 50 加总验证通过（18+9+2+2+3+2+5+3+4+2=50）；TC-01~14 与正文规则一致，唯 TC-07（拦截级，P1-2）、TC-08③（标注矛盾，P0-1）、TC-14（enter_phase，P0-1）三处例外。
- 本审查为静态推导，未运行任何校验/脚本；"命中后行为/拦截语义"等结论基于文档文本比对。
- 发布前提：P0-1 三处残留 + P1-2 概率语义 + P1-1 批量行号漂移（约 35-40 处）修订完成、并以当前 398 行定稿全量回核通过后方可进入实现。
