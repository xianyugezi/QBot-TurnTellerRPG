# veinborn 阶段一 · 进度存档与交接（2026-09-03 凌晨）

> 用途：开新对话读本文件即可无缝续工。
> 基线：HEAD 57860f2，工作区干净。全仓 unit 相关测试绿（装备/商店/装配 97+ 全过）。

## 一、用户最新拍板（本存档会话尾）

> 「你先把这个存档，然后把框架 M12 一块做完再做，不然老是漏东西」→ 用户决定：
> **先把框架（M12 编辑器GM + 战斗奖励结算等缺口）做完，再回 veinborn 阶段一**。
> 开新对话做框架 → M12 完成后回来接 veinborn。

## 二、veinborn 阶段一进度（已提交 10 commit 本会话 + 此前）

### ✅ 已完成（实机可玩基线）
- **实机战斗全通**：注册→锁定→攻击→击杀→session 释放→可再锁定
- **全指令免 / 前缀**（068bcd4，GM 仍强制）
- **引导文案去 /**（a3f0ea6）
- **rng 快照状态修复**（88f6580）——战斗恒 miss 根因（只存 seed→随机序列重放）
- **战斗正文补发**：pipeline sent 经桥接层发群（此前只显标题行）
- **技能渲染**：action_name 注入（你skill→你脊斩）+ 占位条目过滤（a9aa5ab）
- **幼兽数值跑通**（hp90/str8/agi4，Lv1 可击杀）
- **测试接口** scripts/veinborn_test_api.py（HTTP 8123，纯 asyncio 单循环——v1 ThreadingHTTPServer 会 aiosqlite 循环崩溃）

### ✅ 装备系统（本会话新做，框架级）
- **槽位可配置化**（da7d1cc）：settings.slot_defs 内容包声明槽位 → make_context 注入
  ctx["slots"]（包装形态 {"slots": {...}}）+ ctx["equip_engine"]（EquipmentEngineAdapter(slots=...)）；
  无配置 → 默认 6 槽零影响。**渲染层 _slot_order/_slot_name 已消费 ctx["slots"]**。
- **装备加成断链修复**（0c4902f）：def 数值字段(atk/def/hp/mp...)→ stats_bonus 全链路
  （add_item/shop_tx 实例化填 + runner 落档透传 slot/stats_bonus）——原穿装 bonus.flat 恒空。
- **shop 可售装备**（a46d457）：shop 商品引用集合并 items∪equipment（item_lib 同库语义），
  保留 items 缺失放行语义。
- **veinborn 8 槽**（7d2b55b）：武器/头部/躯干/手部/护腿/鞋子/饰品/脉玉（slot_defs）
- **veinborn 装备**（equipment.json 10 件）：3 武器线（砺脊刃 atk4→长脊大剑 atk8→砾冕斩脊 atk14）
  + 7 防具（全 8 部位，def 1-3/hp/foc/agi）
- **veinborn 商店**（shop.json 龙骨铁匠铺）：10 装备 + 脉火药剂上架（sold_out_once 砾冕斩脊限量 1）
- **幼兽货币掉落修正**（57860f2）：8→50 币 + **货币 id 修正**（name「脉晶币」→id "coins"——
  原写 name 触发 unknown_currency 被跳过 = 不掉币！）

### 🚧 拦路缺口（用户拍板：做框架时一并补）
1. **战斗击杀奖励结算断链**（核心）：击杀「获得经验 0 金币 0」——battle_reward_fn 装配恒 None
   （context.py L1175），击杀奖励落账（enemy rewards/drops → 玩家 exp/币/掉落 + 升级）从未实现。
   炼金/钓鱼/签到各有结算唯独 PvE 战斗没有。补默认 battle_reward_fn ≈40-60 行。
   参考：battle_commands.py L338 _battle_rewards（消费 fn(engine,report,ctx)->{exp,gold,drops}）、
   enrich_round_report、core/levelup.py（升级引擎）、core/reward.py（货币键空间 settings.currencies[].id）。
2. 状态面板无装备栏显示（8 槽穿装后状态不显装备——待确认渲染是否消费 ctx["equipment"]+slots）
3. 商店装备显示 id 英文名（ridge_blade_starter 该显砺脊刃——商品行渲染 name 解析待查）

### ⏳ 待做（veinborn 阶段一剩余）
- **技能蓄力化**（MHR 大剑流，设计稿 /root/deliverables/veinborn_脊剑士_MHR大剑_v5.md）：
  新增 self charge 印记（marks appliable_to=['self'] 已支持）→ 蓄力斩+1 层 / 真蓄斩 chain
  self_marks charge≥3 → 300% 霸体 / 猛进斩消耗蓄力取消。skill_chains self_marks 条件已实证支持。
- 脉矢手 = 弓（阶段一占位，v0.2 补全）
- 完整 e2e（穿装 atk 提升验证——装备加成链已修但未端到端验完）

## 三、框架 M12 待办（用户拍板先做 M12 + 战斗奖励等缺口）

- **M12 编辑器GM**（docs/m12_启动包.md，15a7db4 启动包 + 82a7728 摸底 + e909068 UX 方案）：
  5a 六页壳 + 5a2 扩展页 13 页 + 5b GM 14 条。暂停原因曾是「技能/职业页依赖 M13」——M13 已收官，
  现可续。待实现：web/api.py 15 端点 + editor.json + 认证 + 原子写盘 + GM 9 条 + admin_users/audit_log + 5a2 扩展页。
- **战斗奖励结算**（上 §三.1）
- **其他框架缺口**（本次 veinborn 暴露）：状态面板装备栏显示、商店商品中文名渲染

## 四、关键文件速查

| 文件 | 内容 |
|---|---|
| /root/QBot-TurnTellerRPG | 仓库（.venv/bin/python 3.14，勿用系统 python3） |
| content/veinborn/ | veinborn 包（settings 8 槽 / equipment 10 件 / shop 龙骨铁匠铺） |
| qbot_rpg/assembly/context.py | make_context（L1529 后 slot_defs 注入段；L1175 battle_reward_fn=None 待补） |
| qbot_rpg/core/equipment.py | EquipmentEngine（slots 注入三形态：None/包装/平铺） |
| qbot_rpg/commands/battle_commands.py | L338 _battle_rewards（奖励读取） |
| scripts/veinborn_test_api.py | 测试接口（HTTP 8123，常驻可 curl 测） |
| /root/deliverables/veinborn_脊剑士_MHR大剑_v5.md | 技能蓄力化设计稿（用户拍板方向「挺好」） |
| /root/docs_archive/派生树/ | 怪猎 19 武器派生树参考（脊剑士=大剑 MHR 章） |
| docs/m12_启动包.md | M12 启动包（15a7db4） |

## 五、实机状态

- nonebot2.service 常驻（QQ bot 3124097014，NapCat 已重新扫码登录）
- 测试接口 veinborn_test_api.py 后台运行中（proc 会话，8123 端口）——新对话可直接 curl
- 记忆索引：~/.hermes/memory_index/projects/veinborn-项目状态.md

## 六、新对话续工路径

1. 读本文件 + git log 确认基线（57860f2）
2. 用户拍板顺序：**先框架 M12 + 战斗奖励结算等缺口 → 后回 veinborn 阶段一（技能蓄力化 + e2e）**
3. M12 读 docs/m12_启动包.md（22 批规划在 docs/m13_启动包.md 可参考节奏）
