# 幻觉审查报告 · 细化_2c3c_强化失败分级.md

- **审查对象**：`/root/QBot-TurnTellerRPG/docs/细化/细化_2c3c_强化失败分级.md`（wc -l = 395，与文件实际一致）
- **依据定稿**：`/root/docs_archive/RPG框架项目/强化系统设计定稿.md`（wc -l = 235，与 L4 自称「共 235 行」一致；全文逐行核对）
- **核对方法**：通读细化全文 395 行抽取全部声明 → 回定稿逐条比对（①行号引对 ②内容一致 ③补白显式标注 ④数值有依据）→ 字节级 grep -F 复核高风险引文 → 兄弟文档引用点（2c3a / 2c2a / 2c2b）逐一 grep 实证 → 内部一致性（TC 期望 vs 正文规则 vs 定稿示例语义三向互验）→ 参考《幻觉审查_2c2d.md》对齐松紧惯例。
- **核对范围**：§0 键值索引 7 行、§1.1 裁定与三表、§1.2-1.4、§2.1-2.4、§3.1-3.3、§4.1-4.5、§5、§6 字段表 F-1~F-10、§7 判定链、§8 用例 TC-01~24（逐条验算）、§9 行号回查表、附录 A 4 项、附录 B 补白 P-1~P-8——逐项核对。

---

## 总体结论

**P0 = 0 条 / P1 = 1 条 / P2 = 8 条**

**评分：B**（P1 为 TC-02 用例期望与正文 §1.2 / 定稿 L42 掉级口径冲突，修正后可达 A）

**一句话**：本档是全轮次引用密度最高的细化之一——40+ 处定稿行号引用经逐条回查**0 处落空、0 处张冠李戴**，高风险点（fail_tier_split 两段制、L42 掉级文案、L87 幸运公式、L127/L160 转移默认关、L68 保护石触发语义）全部字节级吻合，补白纪律优秀（§1.1 三档裁定、附录 B P-1~P-8 全部显式分离）；唯一 P1 是 TC-02 期望把「高段掉级」按「目标−1」口径写成「维持原级」，与本文 §1.2「当前 +N → +(N−1)」及定稿 L42 铁剑+5→+4 示例（当前−1 口径）直接冲突；另有 8 处 P2 均为示例扩展未注、逐字引用漏标点、数学批注笔误等轻微瑕疵。

---

## P0（严重幻觉：虚构定稿内容 / 方向性错误引用）— 0 条

无。定稿引用逐条核对全部真实：

- **§0**：L6 用户拍板七条原文逐字吻合（字节级 grep 确认）；L15-16 强化定位两行合并引用内容一致。
- **§1.1 高风险点核对**：
  - L53-55 低/高段划分：`低等级段（+0 → 低段上限，默认 +3）：失败 = 装备不变（无惩罚）` / `高等级段（低段上限以上，默认 +4+）：失败 = 强化等级 -1（掉 1 级）` / `分级阈值可配（fail_tier_split）`——三行逐字属实 ✓；据此「默认两段制、中[4-6]高[7+]同属高段」裁定成立，且裁定过程显式展示行号依据（L53-54 vs L179-181），非把推导冒充原文 ✓。
  - L42 掉级文案示例：`❌ 失败！强化等级 -1（铁剑+5 → +4）` 字节级与原文字符完全一致（含全角标点）✓（字节比对 0 处差异）。
  - L38/L45：`（失败：+1~+3 阶段失败不变 → "强化失败，装备保持不变"）` 与 `强化成功提示数值变化；失败提示惩罚类型` 均字节级吻合 ✓。
  - L60-63 碎装备四行、L156-158 三字段、L179-181 stone_tiers 三行数据、L200 校验器 3——内容全部属实 ✓。
- **§2 保护石**：L68-70（触发/来源/作用）、L188（protect_stone 物品 ID 可空）、L215（/强化保护 指令）三处引用原文逐字吻合；L70 引用仅截取「作用：高等级段堆装备的保险」未含后文括号（见 P2-4）；「低段失败不消耗」为 L53+L68 显式标注【裁定】的推导，补白纪律合格 ✓。
- **§3 幸运**：L87 公式 `实际成功率 = 基础成功率 +（√我方幸运 − √对方幸运）%（对方=0 时即 √我方幸运%）`、L88、L89（luck_affects 默认开可关）、L90 显示示例、L36/L41 两条箭尾——全部字节级吻合 ✓。
- **§4 转移**：L127（transfer_allowed: false 默认关）、L160 开关位、L129-130（我们不开+经济出口）、L229（换装浪费故意设计）——均属实；L172 对 L160 的逐字引用漏一逗号（见 P2-1）✓；转移开启语义三张表全部显示标注【工程补白】/精神延伸/「未定义」字样，无一条冒充定稿 ✓。
- **§5-9**：L115 达顶提示、L113-114 上限四档、L131/L202 校验器、L214-216 指令清单（确认未列转移指令，§4.5 如此声明且属实）、L198-204 校验器 7 条——全部核对通过。
- **外部引用抽查全部真实存在**：2c3a §1.5（L80-88：失败不返还材料 L88、材料不足拒绝 L85-86）✓ / §2.1（L94 基础率表）✓ / §2.4（L134-142：clamp 0/100、rand(0,100)<rate、luck 缺失按 0）✓ / §3.2（达顶零消耗 L163）✓ / §3.3（level_gated L169）✓ / §3.4（values L179）✓ / §4（失败分级 L193，与 L5「同源」声明相符）✓ / §5（数值表 L231）✓ / S3（L336：luck 缺失按 0+黄提示、PvE 对手幸运恒 0）✓ / S4（L337：protect_stone 存在性校验缺失→软降级「未开放」）✓ / D-13（L253：protect_stone 字段定义）✓ / TC-24（L323：转移关闭无入口）✓；2c2a（品质=items.json 四档枚举，L15/L64/L91）✓；2c2b（指令契约文档，L1）✓。
- **数值全部有据**：fail_tier_split=3（L155）✓、shatter 三字段（L156-158）✓、luck_affects（L159）✓、transfer_allowed（L160）✓、protect_stone=item_protect_stone（L188）✓、上限四档 5/8/10/12（L113-114）✓；TC-12 验算：√36−0=+6.0pp ✓、√4−√16=−2 pp（期望值正确，但批注笔误见 P2-3）✓；TC-13 钳制来自 2c3a §2.4 同款 ✓。

---

## P1（明显问题：引用错误 / 内部不一致）— 1 条

### P1-1　TC-02 期望与本文 §1.2 / 定稿 L42 掉级语义直接冲突（口径错置）
- **位置**：细化 L310（TC-02 用例行）
- **声明**：`| TC-02 | 高段失败掉级 | +3→+4 失败 → 维持 +3（目标 4>split）；+4→+5 失败 → +5→+4（L42） |`
- **核对**：本文 §1.2（L57）自述规则为「高段失败：当前 +N → +(N−1)（一次 1 级，L42/L54「掉 1 级」）」，定稿 L42 示例 `❌ 失败！强化等级 -1（铁剑+5 → +4）` 展示的正是「当前等级−1」口径（铁剑+5 强化中失败 → 掉到 +4，低于起点）。按此口径：**+3→+4 失败 → 当前 +3 −1 = +2**（TC-02 却写「维持 +3」）；**+4→+5 失败 → 当前 +4 −1 = +3**（TC-02 却写「+5→+4」，即结果 +4、未低于起点）。TC-02 两段统一按「目标−1」口径写作（目标 4→3 = 维持+3 ✓自洽、目标 5→4 = +4 ✓自洽），但该口径与定稿示例（目标 6 失败 → +4，非 +5）和本文 §1.2 规则矛盾——对同一场景「+5→+6 失败」，按 TC-02 得 +5，按 §1.2/L42 得 +4。用例期望若落地为断言，将按错误口径断言或与规则实现互斥。
- **证据**：定稿 L42 原文 `❌ 失败！强化等级 -1（铁剑+5 → +4）`（字节级 grep 确认）；细化 L57 原文 `当前 +N → +(N−1)`；细化 L310 TC-02 原文如上。
- **影响**：本 TC 是「高段失败掉级」的核心冒烟用例，口径错置会直接引导实现/测试按「高段失败 = 等级不变（仅回执展示掉级）」实现，实质削弱掉级惩罚，与用户拍板「高掉级」不符。
- **建议**：TC-02 期望改为与 §1.2/L42 一致：`+3→+4 失败 → +3→+2（当前−1）；+4→+5 失败 → +4→+3；参照 L42：+5→+6 失败 → +5→+4`（或明确并入 §1.1 裁定：若意图改为「目标−1 口径」需同步改写 §1.2 并否定 L42 示例语义——不推荐）。

---

## P2（轻微问题：行号偏移 / 引用过弱 / 取舍未标注 / 补白不齐 / 笔误）— 8 条

### P2-1　L160 逐字引用漏逗号（引用瑕疵）
- **位置**：细化 L172
- **声明**：`【强化定稿】L160：`"transfer_allowed": false     // 强化转移（我们关）``
- **核对**：定稿 L160 原文为 `"transfer_allowed": false,     // 强化转移（我们关）`——`false` 后有一逗号（settings 对象中该键非末键），细化逐字引用时漏掉。其余逐字吻合。
- **建议**：补回逗号或加省略说明。

### P2-2　低段失败回执示例在 L38 原文上扩展未标注（对照 L83 行已标「文案细化」，处理不一致）
- **位置**：细化 L81（§1.4 表 低段失败行）
- **声明**：`❌ 强化失败，装备保持不变（铁剑+3）`，引用列仅 `L38`
- **核对**：定稿 L38 原文为 `（失败：+1~+3 阶段失败不变 → "强化失败，装备保持不变"）`——核心文案「强化失败，装备保持不变」属实，但示例中的 `❌` 前缀与 `（铁剑+3）` 后缀为细化创作，L38/L53 均无；同表 L83 保护石行对同类扩展显式标注了「（文案细化）」，此行未标注，体例不一致。
- **建议**：引用列改注 `L38（文案细化）`，与 L83 同体例。

### P2-3　TC-12 数学批注笔误（期望值正确，注记自相矛盾）
- **位置**：细化 L330（TC-12 行）
- **声明**：`我幸 4/敌幸 16（√2−√4=−2）：−2.0pp（L87）`
- **核对**：期望值 −2.0pp 正确（√4−√16 = 2−4 = −2）；但批注写 `√2−√4=−2` 数学上不成立（√2−√4≈−0.586）。应为 `√4−√16` 或直接 `2−4`。L87 公式引用属实。
- **建议**：批注改为 `（√4−√16 = 2−4 = −2）`。

### P2-4　L70 引用截断未用省略号
- **位置**：细化 L93（§2.1 引言块）
- **声明**：`【强化定稿】L68-70：…作用：高等级段堆装备的保险`（省略号缺失）
- **核对**：定稿 L70 原文为 `- 作用：高等级段堆装备的保险（堆装备乐趣的支撑）`——细化只取前半句且无省略号标注，隐含「引用全文」的错觉。内容本身不冲突。
- **建议**：补 `（…）` 或 `（堆装备乐趣的支撑）`。

### P2-5　L32 stone_tiers 引用区间未含起始行 L178
- **位置**：细化 L32
- **声明**：`【强化定稿】L179-181：`"stone_tiers": [ {"tier":"low",…}`（开括号与键名在 L178）
- **核对**：定稿 L178 = `"stone_tiers": [  // 强化石档位（引用 items.json）`，三个数据行在 L179/180/181；细化引用区间 L179-181 未含 L178，而引用文本含开括号 `"stone_tiers": [`。数据内容本身逐字属实（仅空白规范化为紧凑格式）。
- **建议**：引用区间改 L178-181。

### P2-6　§1.3 判定序「先掷碎 → 未碎再掉级」为实现约定，未标补白
- **位置**：细化 L73
- **声明**：`判定序（高段失败后）：先掷碎（shatter_mode=true 且 shatter_chance>0）→ 未碎再执行掉级；碎 = 装备消失（归宿由内容包定义，默认不碎）`
- **核对**：定稿 L60-63 只定义碎装备开关/默认 0%/软警告，未定义「碎 vs 掉级」的掷判定顺序（碎先或掉级先）。判定序为纯实现约定，且「碎 = 装备消失（默认不碎）」的表述本身与默认 0% 语义自洽，但 L73 整条未标【工程补白/裁定】字样——同类实现约定在 §2.1 L98、§4.5 均显式标注，此处漏标。
- **建议**：行首标注【工程补白】。

### P2-7　TC-23 期望含「本次消耗」，超出 L216 原文（兄弟文档同惯例，建议显式声明）
- **位置**：细化 L351（TC-23 行）
- **声明**：`展示 当前 +N / 成功率（含幸运拆分）/ 上限 / 本次消耗（L216）`
- **核对**：定稿 L216 原文为 `/强化信息 <装备>  # 查看当前 +N / 成功率 / 上限`——无「本次消耗」；「本次消耗」为细化扩展（2c3a TC-25 亦同款扩展，属兄弟文档共享惯例），但引用列仅 L216，未声明扩展来源。
- **建议**：引用列注 `L216（扩展：本次消耗，对齐 2c3a TC-25）`。

### P2-8　§3.1 代码块 clamp(0,100) 钳制为实现默认，未列入附录 B 补白清单
- **位置**：细化 L140（代码块 `final_rate = clamp(rate, 0, 100)` 及注释「边界钳制」）
- **声明**：`clamp(rate, 0, 100)`（推导公式）
- **核对**：定稿 L87 公式未含钳制；钳制语义由 2c3a §2.4（L138-140 clamp 到 0/100）跨文档继承，TC-13 亦依赖之——数值本身有据，且注释已注明「2c3a §2.4 同款」，但作为实现默认未在附录 B 补白清单中登记（附录 B 无对应项），与本档「推导值/实现默认显式标注」纪律略有出入（§3.3/§3.2 已有讨论，属可接受边缘，登记后纪律更完整）。
- **建议**：附录 B 增登记「clamp(0,100) 钳制（2c3a §2.4 继承）」一项，或该行注释明确「2c3a §2.4 既定约定、非本文新设」。

---

## 核对亮点（加分项）

1. **引用密度极高且零落空**：全文 40+ 处定稿行号引用（含最易造假的细粒度字节级文案——L42 掉级示例、L87 公式、L38/L45 回执、L215 指令、L188 物品 ID——全部 grep -F 字节比对通过），外部引用 2c3a S3/S4/D-13/TC-24/§1.5/§2.4/§5、2c2a、2c2b 全部实证存在且口径相符，无一处落空。
2. **补白纪律优秀**：附录 B 补白 P-1~P-8 与定稿引用严格分离（8 项均有「依据缺口」说明）；§1.1「三档 vs 两段」裁定以行号推导方式显式展开（L53-54 vs L179-181），把任务口径与定稿原文的张力摆上台面处理，而非悄悄改数值；F-8~F-10 补白字段以 `*` 号标记并在表下声明，审查友好；附录 A「不实现仅记录」处置诚实（碎装备/转移/level_gated 全部对照 L62/L129/L141 落实关闭）。
3. **高风险的「保护石」语义处置规范**：触发条件（仅高段）、成功不消耗、低段不消耗（【裁定】）、不足拒绝（【工程补白】+L44 原子精神）、空配置软降级——逐条都有出处或显式补白/裁定标记，无一条冒充定稿原文；消耗时机明确为「失败且将产生掉级时」（§0 表）。
4. **三向自洽度高**：§9 行号回查表 9 个章节引用集合与正文实际使用行号完全吻合；TC 用例 24 条编号连续无重复；附录 B 8 项与正文补白点一一对应；仅 TC-02 一处三向（期望 vs §1.2 vs L42）失谐（P1-1）。

---

## 结尾核对基准说明

- 定稿 235 行全文逐行通读（read_file 全量 + 高风险行 sed 原始字节复核）。
- 细化 395 行全文逐条通读，抽取声明约 90 处，逐条回查。
- 兄弟文档引用点 13 处抽查（2c3a 11 处 + 2c2a 2 处 + 2c2b 1 处）全部实证存在；`覆盖审计_F_生活生产.md` 已确认在库（310 行），2c3c 全文未引用该文件，无对应核对义务。
- 字节级复核 12 组高风险引文：10 组完全吻合、1 组漏逗号（P2-1）、1 组截断无省略号（P2-4）。
- 用例数值人工验算：TC-12 两组 √差、TC-13 两组钳制、TC-04 下限钳制、TC-02 口径冲突——全部验算。

*核对依据：定稿/细化全文 + grep -F 字节比对 + 兄弟文档定向检索。*

---

## 补遗（2026-08-18 12:20 · 轮 13 合并记录）

1. **P1-1（TC-02）升级为 P0 级跨文档问题**：TC-02 只是暴露点——本文档失败分段判定全线基于「目标等级」（范围行 L6、裁定 L34、段位区间表 L36-42、掉级行 L49、保护石触发 L95、幸运联动 L162、F-1 字段 L264、判定链 L289），与定稿 L53-54（按当前等级分段）+ L42（当前−1）冲突；边界场景「当前 +3（目标 +4）失败」会被判掉级而按定稿应不变。与 2c3a P0-1（已修）/2c3b 补遗同根同源。
2. **处置（已当场修 16 处）**：L6/L34/L36/L40-42/L49/L95/L162/L264/L289 改为「当前等级」口径 + TC-01/02/03/08/10 期望改按当前等级并补充边界断言（当前+3 低段不变、当前+4 掉到+3、当前+5 掉到+4 参照 L42）。原 P2-3（TC-12 √2−√4 批注笔误）、P2-7（TC-23 本次消耗）等维持待轮 30。
3. **并行审查（deleg_975a3112）对本档判 P0=0/P1=1/P2=4 评分 B**：其 P1 与本报告 P1-1 同源（TC-02 掉级期望值）；P2 差异查重：TC-12 算式笔误 = 本报告 P2-3、TC-23 本次消耗 = 本报告 P2-7、§1.2 依据偏弱 = 本报告 P2 系同类、附录 B clamp 登记 = 本报告 P2-8——无新待办，轮 30 按本报告 1 P1 + 8 P2 清单执行。