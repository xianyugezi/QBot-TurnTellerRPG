# 审查报告：M5 设计层·批次1（细化_3d_消息模板规范 vs 定稿/框架/代码）· j-space 静态审查

> 审查日期：2026-08-27（会话内）· 审查方式：**纯静态审查**（本环境无 bash 沙箱，未运行任何命令/脚本/验证，全程仅用 read/grep/glob；所有运行行为结论一律标注【静态推导】）
> 审查对象（主）：`docs/细化/细化_3d_消息模板规范.md`（348 行，当前版本）
> 权威定稿（仓库 `docs/审查参考/` 副本，行号以此为基准）：
> - `消息前缀功能设计定稿.md`（181 行，主依据【前缀】，全篇行号引用逐条核对）
> - `RPG回合制框架设计文档.md`（2794 行，§3.4 / §7.4 / §8.3 / §15.7 按行号定位）
> - `开发规则文档.md`（§8 防封号，L493-528）
> 已实现渲染模块（静态读代码）：`qbot_rpg/core/message_format/prefix_render.py`（139 行）· `list_render.py`（201 行）· `qbot_rpg/commands/sender.py`（192 行）· `qbot_rpg/commands/errors.py`（37 行，TPL-12/13/14 唯一文案源）
> 辅助证据：`审查_M4设计_批次1_jspace.md`（P0-2 页码夹取裁决）、`docs/审查/幻觉审查_3d.md`（既有 P1-1/P1-2 错引）、`docs/细化/细化_4f_基础指令组契约.md`（RUL-16/18）

---

## 〇、结论摘要

| 级别 | 数量 | 一句话 |
|---|---|---|
| **P0** | **1** | §2.2/TC-09/10「超总页数→报错」与本文档尾部 M4 裁决「夹取最后一页」直接矛盾，且与已实现 list_render 相反，裁决要求「同步 TC-09/10」未执行 |
| **P1** | **3** | 框架 §8.3 消息合并策略仅覆盖「一轮=1条」（开始/结束/探索=1条、单次≤1-2条 未落地）；【前缀】L25「汇总单条消息语义」错引 5 处；【前缀】L46「费用语义」编造 4 处 + 签到 D-03 引用链两端不实 |
| **P2** | **7** | §15.7 统一出口四项未进细化文档（代码已实装）；页码粘合语法/筛选链/可配性未交叉引用；TPL-02/04 输出相同信息项；sender.py L523 注释错引；errors.py docstring 三要素自述不符；状态行规则未覆盖；16 行折叠 vs 定稿「分两条发送」关系未声明 |
| 继承（不重复计分） | — | M4 P1-1（emoji 三方分裂）、P1-2（错误模板两套体系）仍跨文件存在，本批仅在涉及处点明 |

**一句话总评**：本批次【前缀】定稿已入仓库（181 行），对细化_3d 全部约 90 处【前缀】行号引用做**全量**核对——除既有已知的 L25/L46 两处错引外，其余全部真实、行号精确、内容一致（M4 P1-3「不可核验」缺口就此收口）；补白标注纪律完整（D-01~05 + M4 裁决引用全部真实，无冒充）。核心缺陷集中在「页码越界行为」文档内部自相矛盾（P0）与 §8.3 消息合并策略覆盖不全（P1）。

---

## 一、维度① 缺漏——「定稿要求 X 但细化无 Y」清单

### 1.1 P1-1 框架 §8.3 消息合并策略：只覆盖四分之一

- **定稿要求 X**：框架 §8.3（L1398-1405）「战斗一轮 = 1 条 / 战斗开始 = 1 条（敌人入场+属性）/ 战斗结束 = 1 条（结算）/ 探索结果 = 1 条 → 单次操作最多 1-2 条消息，防刷屏防封号」；开发规则 §8.3（L514-515）同款「战斗开始 1 条、战斗结束 1 条、探索结果 1 条；单次操作最多 1-2 条消息」。
- **细化有 Y（仅 1/4）**：细化 §3.1（L163）+ TC-01（L274）+ TC-23（L321）只承接「**一轮战斗仍 1 条消息**（玩家行动+怪物反击合并）」。grep 细化全文「战斗开始/战斗结束/探索结果/探索 =」→ 零匹配；「单次操作最多 1-2 条」总量约束无任何细化承接。
- **判定**：细化自我定位「本规范只定**消息骨架与横切规则**」（L8）且已主动承接「一轮=1条」，但把 §8.3 其余三条合并策略和「单次≤1-2条」总约束留在空白。防封号是开发规则 L495 明示的「最高优先级运营约束」，横切消息数量规则应在本规范汇总承接或显式转交战斗细化（细化_1g 系列）并注明——当前两者皆无。
- **修复建议**：§3.1 增补「消息合并策略承接表」：战斗开始/结束/探索结果各 =1 条、单次操作最多 1-2 条，注明四条各自承接方（本规范或战斗细化），引用框架 §8.3 L1398-1405 与开发规则 §8.3 L514-515。

### 1.2 P2-1 框架 §15.7 统一出口四项：细化文档零覆盖（代码已实装）

- **定稿要求 X**：框架 §15.7（L1622-1623）「统一消息解析器（段级提取 at/图片/表情 + **CQ 码转义防注入**）；渲染层**长度预算**；发送出口统一封装含**失败重试与风控退避**」；开发规则 L507（长度预算分条）/L510（CQ 码转义、段级提取）/L528（统一出口、禁止裸 send）。
- **细化无 Y**：grep 细化_3d 全文「CQ|转义|分两条|段级|4000|重试|退避」→ **零匹配**。细化作为「全部对外消息的模板注册表与渲染规范」（L7），未把 CQ 转义、长度预算、重试退避、统一出口、段级提取任何一项写进契约（已由 sender.py 实装，缺口在细化文档本身，见 §四 4.3）。
- **修复建议**：细化 §3.3 增补「发送出口」小节，承接 §15.7 四项（引用框架 L1622-1623/规则 L507/L510/L528）。

### 1.3 P2-2 §3.2 16 行折叠 vs 定稿「分两条发送」：策略关系未声明

- **定稿要求 X**：框架 §7.4（L1346）/§15.7（L1623）/开发规则 L507 的定稿超长策略 =「超长自动截断**分两条发送**（不吞消息、不超长）」。
- **细化有 Y**：§3.2（L171）自创「一条消息总渲染行数上限默认 16 行，超限则按『正文尾部 → 中间过程行』优先折叠」并标注「D-03 延伸，工程补白」。
- **判定**：折叠（把尾部变成省略行 TPL-09，视觉吞内容）与分两条（保留全部内容）是两套不同的「超长处理」策略；细化未在 ADR 中说明二者关系、未标注其对定稿「分两条发送」的偏离（§0.3 变更纪律只要求对【前缀】偏离入册，未覆盖对框架的偏离）。
- **修复建议**：给 §3.2 补一条 ADR 注记：「16 行折叠 = 内容层防刷屏；QQ 4000 字符长度预算分两条 = 传输层兜底（sender 出口），二者分层并存」——或直接改引定稿「分两条」。

### 1.4 P2-3 开发规则 L509 战斗状态行规则未覆盖

- **定稿要求 X**：开发规则 L509「战斗状态行：默认显示前 5 个状态，超出追加『还有 N 个状态』（防一条消息塞满）」。（任务所述「框架 §3.4 状态行」在框架中实为 §3.4「状态异常」（L140，状态效果定义），与消息状态行无关；状态行规则真正位于开发规则 L509。）
- **细化有 Y**：仅 §4.1 降级示例（L208）展示了状态行文本 `你 21/30 | 史莱姆 7/25 → /攻击…`，但无「状态数 ≤5 + 还有 N 个状态」规则。该规则与细化「5 条/页」「16 行上限」同属防刷屏横切，未承接。
- **修复建议**：§三 增补一行「状态行：状态数 ≤5，超出追加『还有 N 个状态』」，引用开发规则 L509。

### 1.5 P2-4 §7.4 分页筛选部分覆盖（粘合语法 / 筛选链 / 可配性）

- **定稿要求 X**：框架 §7.4（L1334-1347）「分页默认 5 条/页（**可配置**）；翻页 `背包2`；筛选链：物品类型→类型(子类目)→品质，可叠加；超长消息自动截断分两条发送」；开发规则 L508 同款。
- **细化有 Y**：5 条/页上限 ✓（§2.1）；页码输入 ✓（§2.2）；页脚 TPL-08 ✓。
- **细化无 Y**：① 翻页只写「`/指令 页码`（如 `/商店 2`）」（L140），未提及定稿与 4f RUL-16（L198）的**粘合等价形式 `背包2`**；② 筛选链（类型/子类/品质）未提及（4f RUL-16 已定义，属指令解析层，可接受但应交叉引用）；③ 「5 条/页可配置」未声明（细化固定为「最多 5 条/页上限」，与签到定稿 L28 收敛但吞掉了框架的「可配置」自由度）。
- **修复建议**：§2.2 增注「翻页语法：`/指令 页码`（空格）与 `背包2`（粘合）等价且永久兼容（4f RUL-16，框架 L1272/L1334-1340）」；§2.1 注明「5 条为默认上限，作者可配（框架 §7.4/规则 L508）」。

### 1.6 全覆盖确认（无问题）

- **【前缀】§五 显示规则 5 条全落地**：①仅玩家相关回复加前缀/系统公告默认豁免（§1.5 L123、TC-24）；②群聊+私聊默认生效/per_channel 三值（§1.5 L123、TC-25）；③前缀只加合并消息首行（§3.1 L163、TC-23）；④前缀不影响指令解析（§1.5 L124、TC-26）；⑤[群名] 私聊输出「私聊」（§1.3 L98、TC-05）。全部落地 ✓
- **【前缀】§六 空称号三态齐**：默认 "-"（TPL-02/§1.4 L111）；hide_when_empty 整段省略+相邻装饰符+尾随空格清理（TPL-03/§1.4 L112/L115-116）；empty_title_text:"" 仅隐本体保留装饰符（TPL-04/§1.4 L113）。三态齐全 ✓
- **【前缀】§九 校验器红黄清单逐条对齐**：细化附 一览 L334「硬拦（类型/负数/结构）+ 黄提示（未知占位符/format 空/超长/枚举/prefix_max_len 区间）」覆盖定稿 §九全部 10 项——红：enabled 非布尔/format 非字符串（类型）、prefix_max_len 负数（负数）、结构错误（结构）；黄：未知占位符、format 为空、format 超长(>80)、占位符过多(>10)、per_channel 枚举非法、prefix_max_len >200（区间）。零遗漏 ✓

---

## 二、维度② 错误

### 2.1 P0-1 页码越界行为：§2.2/TC-09/10 与本文档尾部 M4 裁决自相矛盾，且与实现相反

- **细化正文（旧口径）**：
  - §2.2（L141-143）「非法值（0/负数/非数字/**超总页数**）→ 输出 TPL-12（指令不正确）+ 页脚指引，**不静默翻到兜底页**（防误解）；**尾页溢出输入 999 同理报错**」；
  - TC-09（L287）「`/商店 4`（超总页数 3）→ 输出 TPL-12…**不静默翻到兜底页**」；TC-10（L288）「同上 TPL-12 统一报错」。
- **本文档尾部裁决（新口径，L344-348，2026-08-27 用户拍板选 4f 口径）**：「页码越界统一『夹取到最后一页』…**列表页码超过总页数 → 夹取到最后一页并提示『已到最后一页』；0/负数/非数字仍按 TPL-12 报错。同步 TC-09/10**」。
- **实现侧【静态推导】**：`qbot_rpg/core/message_format/list_render.py` `resolve_page`（L106-107）对超总页数返回 `clamped=True, page=total_pages` 并附 `LAST_PAGE_HINT`（L42「（已到最后一页）」），**完全按裁决实现**；0/负数/非数字 → `invalid=True`（L104-105）。即：实现 = 裁决口径，≠ 细化正文口径。
- **判定**：同一文档内 §2.2/TC-09/10（报错）与尾部裁决（夹取）对同一输入类给出相反行为；裁决白纸黑字要求「同步 TC-09/10」，但正文未改。若实现方照 TC-09 写测试，将直接与已实装 list_render 冲突。根因正是 M4 报告 P0-2 所指矛盾，仲裁后只追加了裁决段、未回改正文——裁决执行不完整。
- **修复建议**：按裁决更新——§2.2（L142-143）改「超总页数 → 夹取最后一页 +『已到最后一页』提示；0/负数/非数字 → TPL-12」；TC-09 改「`/商店 4`（仅 3 页）→ 返回第 3 页 +（已到最后一页）」；TC-10 保留 TPL-12（0/负数/非数字）。并在 §2.2 注明「夹取口径 = M4 裁决（审查_M4设计_批次1_jspace.md P0-2）+ 4f RUL-16」。

### 2.2 settings message_prefix 7 字段核对（无问题）

定稿 §三 7 字段（JSON L41-49 + 元数据表 L54-62）：enabled true / format "Lv[等级].[玩家名] -[称号]-" / show_on_system false / per_channel all / hide_when_empty false / empty_title_text "-" / prefix_max_len 40。细化引用逐字段一致：
- §1.5（L123）show_on_system 默认 false / per_channel all/group/private / enabled 默认开 ✓
- TPL-02（L75）empty_title_text 默认 "-" ✓；§1.4（L111-113）hide_when_empty 默认 false + empty_title_text "-" ✓
- §3.3（L177）prefix_max_len 默认 40，0=不限 ✓
- 附 一览（L332）7 字段名与定稿逐字一致 ✓
**字段名/默认值/枚举零差异** ✓

### 2.3 TPL-01~14 与【前缀】§二 验收示例逐字核对（无问题 + 一个信息项）

- TPL-01 `Lv[等级].[玩家名] -[称号]-`（L74）= 定稿 L22/L43/L57；渲染例 `Lv35.阿伟 -斩龙者-`（TC-01 L274、§4.1 L205）= 定稿 L28 逐字 ✓
- TPL-02 `Lv[等级].[玩家名] - -`（L75）= 定稿 L35/L90/L92 口径 ✓
- TPL-03 `Lv[等级].[玩家名]`（L76）= 定稿 L35/L91 ✓
- TPL-04 `Lv[等级].[玩家名] - -`（L77）= 定稿 L92（装饰符保留）✓
- TPL-05 `[职业] Lv[等级].[玩家名]`（L78）= 定稿 L130 示例2 逐字 ✓
- TPL-06 `【[群名]】[玩家名]`（L79）= 定稿 L133 示例3 逐字 ✓
- TPL-07~14 为补白模板（定稿无对应），其自身一致性：TPL-12/13/14（L85-87）与 `qbot_rpg/commands/errors.py` TPL_ERR_BAD_COMMAND/CONDITION/LACK_RESOURCE（L23-25）逐字一致 ✓；TPL-08（L81）与 list_render.render_footer（L142）逐字一致 ✓
- **P2-5（信息项）**：TPL-02 与 TPL-04 渲染输出相同（均 `Lv35.阿伟 - -`），空称号三态中两态不可区分——这是定稿自身口径（L35/L90/L92 三种表述同串），细化与 prefix_render 均忠实继承（【静态推导】见 §四 4.1）。建议文档加一句澄清「empty_title_text='-' 与 '' 在当前默认装饰下渲染同串，属定稿口径」，避免验收测试写成可区分断言。

### 2.4 其余行号引用抽查（无问题）

§1.1 追溯总览各节区间（L56-66：L4/L13-15、L22/L28-35、L41-62、L64-76、L78-84、L86-93、L95-101、L110-121、L123-141、L143-155、L168-179）全部落在定稿对应章节 ✓；TP-01~06 依据行（L22/43/57/35/90/61/46/91/47/92/130/74/133/84）逐一命中 ✓；占位符 L68-72、L74-76 ✓；三态 L35/L90-92/L165 ✓；长度 L48/L62/L97-101/L176 ✓；校验器 L110-121 ✓；配置 L123-141 ✓；登记 L143-155 ✓；验收 L168-179 ✓；附 一览 L149/L151-154/L152/L153/L154/L165 全部命中 ✓；头注 L6「经【前缀】L181 设计依据转述引用」= 定稿 L181 属实 ✓。

---

## 三、维度③ 幻觉——「声称 vs 实际 grep 结果」对照

### 3.1 P1-2 【前缀】L25「汇总单条消息语义」——行号存在但语义被改写（5 处）

| 声称 | 实际 grep 结果 |
|---|---|
| D-02 依据（L43）「【前缀】L25 只定**汇总单条消息语义**，未定列表内部结构」 | 定稿 181 行 grep「汇总」→ **零匹配**；L25 = 「**验收口径示例**（玩家：阿伟，35 级，佩戴称号「斩龙者」= 成就奖励）：」，仅为示例引入行，与列表/汇总/消息语义无关 |
| TPL-07 依据（L80）、§二（L130）、矩阵②（L263）、TC-07（L285）「对齐【前缀】L25 汇总单条消息语义」 | 同上，5 处同一错引（L43/L80/L130/L263/L285） |

- 定稿中与「合并/单条消息」最接近的是 L34（「一轮战斗仍 1 条消息」）与 L17（「不增加消息条数」），与 L25 无关。既有 `幻觉审查_3d` P1-1 与 M4 继承清单已确认，**当前版本仍未修复**。
- **修复建议**：改引 L34（合并消息策略）或删除该依据，D-02 补白理由改为「定稿未定义列表内部结构」。

### 3.2 P1-3 【前缀】L46「费用语义」——行号存在但内容被编造（4 处）+ 签到 D-03 引用链不实

| 声称 | 实际 grep 结果 |
|---|---|
| TPL-14 依据（L87）、§5.3（L245）、矩阵⑤（L266）、TC-22（L315）「对齐【前缀】L46 **费用语义**」「不扣款、不消耗、不写存档（对齐【前缀】L46 费用语义 + 签到契约 D-03 补签同款原则）」 | 定稿 181 行 grep「费用|扣款|不扣」→ **零匹配**；L46 = `"hide_when_empty": false, // 无称号时整段省略…`，与费用/扣款/副作用毫无关联。定稿全篇无任何费用语义 |
| L245「签到契约 D-03 补签同款原则」 | 签到引擎契约（细化_2b5）D-03（L41）= 补签归属连签连续性 + 同日幂等不重复扣费，语义是「补签/幂等」，**不是**「失败无副作用」；真正承载「余额不足→不扣款」的是 2b5 TC-21（L395）。引用链两端均不实（M4 P1-4 已确认，当前版本未修复） |

- 注：L46 作为 **hide_when_empty 字段引用**（TPL-03 依据 L77、TC-03 依据 L276）是**正确**的——错引仅发生在「费用语义」语境（L87/L245/L266/L315）。
- **修复建议**：删除「对齐【前缀】L46 费用语义」，把「失败无副作用」显式归入 D-04 补白；签到侧改引 2b5 TC-21（L395）或删「同款原则」表述。

### 3.3 补白标注纪律确认（无问题，无新冒充）

- D-01~D-05 五条 ADR（L42-46）全部显式标注（补白）/【工程补白】，每条理由均指向定稿**真实空白**：emoji 纪律未定义（L42）、列表内部结构未定义（L43）、超长处理未定义（L44）、错误文案未定义（L45）、模板编号未定义（L46）。理由中的【前缀】L16/L17/L99/L114/L25 除 L25 外均真实命中。**未发现把工程补白写成「定稿已定义/裁决已授权」的冒充** ✓
- 尾部 M4 裁决（L344-348）引用「审查_M4设计_批次1_jspace.md P0-2」→ 文件存在且 P0-2 属实（M4 报告 L55-58）✓，裁决为真实仲裁记录，非编造 ✓

### 3.4 幻觉维度整体收口（重要确认）

本批次【前缀】定稿已入仓库（`docs/审查参考/消息前缀功能设计定稿.md`，181 行），对细化_3d **全部约 90 处【前缀】行号引用**逐条核对：除 L25/L46 两处既有已知错引外，其余全部真实、行号精确、内容一致。M4 报告 P1-3「【前缀】约 60 处引用在仓库内无法独立复核」的审计缺口，本批次针对细化_3d 收口。

---

## 四、维度④ 与已实现渲染模块衔接（静态读代码）

### 4.1 prefix_render.py（139 行）与 §1.2/§1.4/§3.3 对齐（无问题）【静态推导】

- TPL-01 默认格式（L30 `DEFAULT_PREFIX_FORMAT`）= 细化 §1.2 ✓
- 空称号三态（L74-101）：hide_when_empty → 整段省略+`_collapse_space().rstrip()` 尾随空格清理（L86）= §1.4 TPL-03 ✓；empty_title_text ∈ ("-","") → 装饰符保留 `Lv35.阿伟 - -`（L88-98）= §1.4 TPL-02/04 ✓；自定义空称号文本占位符替换（L100-101）为合理泛化 ✓
- 缺称号按无称号（L39-44）= §1.4「缺佩戴状态不报错」✓
- 未知占位符原样透传（L104/L107）= §1.3/TC-06 ✓
- prefix_max_len 默认 40 / 0=不限 / 截断 + `truncated` 可观察信号（L111-114）= §3.3/TC-13，黄提示归属壳层 sender 的职责划分与细化 §3.3「不阻断正文」一致 ✓
- 无对齐缺口。唯一注意：TPL-02 与 TPL-04 输出同串（同 2.3/P2-5 信息项）。

### 4.2 list_render.py（201 行）与 §二 分页对齐（1 处 P0 镜像 + 其余对齐）【静态推导】

- 5 条/页（L39 DEFAULT_PAGE_SIZE=5）= §2.1 ✓
- 条目行 TPL-07（L127-131）= §1.2 逐字 ✓
- 页脚 TPL-08（L134-142，含单页无页脚）= §2.3/TC-11/12 逐字 ✓
- 0/负数/非数字 → invalid → 壳层组装 TPL-12（L104-105）= §2.2 ✓
- **超总页数 → 夹取最后一页 + 「（已到最后一页）」**（L106-107、L42）= **尾部 M4 裁决**口径，但 **≠ 细化正文 §2.2 L142/TC-09 L287**（正文仍写报错）→ 与 2.1 P0-1 同一根因，实现侧已按裁决，**文档正文需回改**。

### 4.3 sender.py（192 行）与框架 §8.3/§15.7（统一出口）对齐（实装无缺，2 处注释小瑕疵）【静态推导】

- CQ 转义（L59-65 `cq_escape`）= 框架 L1622 / 规则 L510 ✓
- 长度预算分两条（L68-83 `segment_by_length`，4000 字）= 框架 L1346/L1623 / 规则 L507 ✓
- 失败重试 + 指数退避（L177-192，MAX_RETRIES=3，BACKOFF_BASE=2.0）= 规则 L503 ✓
- 统一出口禁止裸 send（L119-175 Sender）= 规则 L528 ✓
- 页码非法 → TPL-12 + 页脚（L108-116 `page_error_tpl12`）= 3d §2.2 / 裁决② ✓
- **P2-6**：sender.py 头注（L7）「禁止裸 send…（【规则】L523）」——规则 L523 =「GM 指令对无权限者静默」，与「禁止裸 send」无关，正确出处为 **L528**。错引 1 处。
- **P2-7**：errors.py 头注（L5）引【规则】L117/【框架】L1349-1357 称模板为「原因 + 正确用法 + 下一步」三要素，但实际 TPL-12/13/14 常量为**一句式**（errors.py L23-25，与 3d 逐字一致）——docstring 与实现不符，属 M4 P1-2「错误模板两套体系」在代码注释侧的残留（本批不展开，仅点明）。

---

## 五、无问题维度确认

- ✅ **7 字段逐字段一致**（§2.2）：字段名/默认值/枚举零差异。
- ✅ **TPL-01~06 与【前缀】§二验收示例逐字一致**（§2.3）；TPL-07~14 补白模板自身与 errors.py/list_render 逐字一致。
- ✅ **§五 5 条 / §六 三态 / §九 红黄 10 项全覆盖**（§1.6）。
- ✅ **补白标注纪律完整**：D-01~05 全部显式（补白）+ 理由指向真实定稿空白；M4 裁决引用真实（§3.3）。
- ✅ **其余约 90 处【前缀】行号引用全量核对通过**（除 L25/L46，§3.4）。
- ✅ **prefix_render.py 与细化完全对齐**；**sender.py 统一出口四项全部实装且主要引用真实**（§4）。
- ✅ **「签到契约 5 条/页」外部对齐属实**：签到系统设计定稿 L28「列表模板 5 条/页上限（全局规范）」命中（D-02 引用链中唯一真实的一环）。
- ⚠️ 未检查项（诚实声明）：细化_3d 头注提及但本批未读的「细化_3a §5.2 S6」「细化_4f RUL-16/18」等代码侧引用，只核对了 4f RUL-16/18 的存在与语义；战斗/商店等业务系统的渲染接线未核（属各系统契约范围）。

---

## 六、修复优先级建议

1. **先修 P0-1**：按 M4 裁决回改 §2.2（L142-143）+ TC-09/10（L287-288），与 list_render.resolve_page 对齐——这是文档内部自相矛盾，实现与测试直接受影响。
2. **再修 P1-2/P1-3**：删除 L25/L46 两处错引（5+4 处），「失败无副作用」改挂 D-04 补白/2b5 TC-21——引用卫生，既有审查已挂账两轮。
3. **补 P1-1**：§8.3 消息合并策略承接表（开始/结束/探索=1条 + 单次≤1-2条）。
4. **P2 七项**按报告逐条校准：§15.7 统一出口进契约、页码粘合语法/可配性/筛选链交叉引用、16 行折叠与「分两条」关系声明、状态行规则、sender.py L523→L528、errors.py docstring、TPL-02/04 澄清注。

*本文档由 j-space 全档（loop：capacity + broadcast + self-monitoring）产出；hub = {【前缀】181 行行号映射、框架 §8.3/§15.7/§7.4、开发规则 §8、三代码文件}；全部行号均来自本会话实际 read/grep，未运行任何命令（无 bash 沙箱）。*
