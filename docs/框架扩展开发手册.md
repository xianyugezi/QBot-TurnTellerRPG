# 框架扩展开发手册（入口）

> **给谁看**：① **外部扩展开发者**（写内容包 / 包自持代码 / 用 E 系扩展点）② **框架维护者**（改代码前先看它）
> **为什么有它**：让扩展开发有据可依；**并在框架更新时，避免把「故意的设计」当成 bug 修掉**。
> **生成于**：2026-09-21（批 70~73 体检 + 三路编撰之后，以**清理后**的状态为准）

---

## 一、怎么读（按角色）

| 角色 | 阅读顺序 |
|---|---|
| **不写代码的人（策划/作者/服主）** | **`docs/Vibecoding说明书.md`** ← 先读这份（配置能做什么、怎么找 AI 做新机制、怎么验收） |
| **第一次做内容包** | 本文 §三「最小上手」→ 《手册·2 事件与扩展点》§7 包声明段全表 → 《手册·1》§4 安全改法 |
| **写包自持代码（ext/）** | 《手册·2》§5 E1/E2/E3 契约 + §6 **`ext_api` 稳定面**（**包代码唯一允许 import 的框架面**）→ 《手册·1》§2 数据结构 |
| **改框架代码** | 《手册·3》§4 **改代码前 checklist** → 《手册·3》§3 **「勿当 bug 修」清单** → 查相关设计文档 → 跑门禁与护栏 |
| **排查"某个字段为什么没生效"** | 《手册·1》§1 常量总表（看**消费点**列）→ 《手册·3》§3（是否属"故意未实现"）→ 否则按 bug 处理 |
> **不写代码的读者**：先看《Vibecoding说明书》**§二 框架能力地图**（常量/变量/事件/状态机 + 谁拥有·改哪·影响谁）与 **§三「别重复造」对照表**（想做 X → 该用 Y → 别造 Z），再决定要不要 vibecoding。

---

## 二、三章索引

| 章 | 文件 | 页数级 | 内容 |
|---|---|---|---|
| **1** | `手册_1_常量与数据结构.md` | 1104 行 | 常量总表（键族 / **17 条特效轴**含范围·默认·双向·别名·**消费点**）/ 数据结构（`Player`·`ItemInstance`·`EquipmentSlot`·combatant·registry·**`ctx` 注册态 162~166 / 未注册态 134~138 键**（批82 · Q1 区间口径））/ `ItemInstance` 生命周期与构造点 / **安全改法清单** / **【稳定契约】汇总** / **无单一源缺口** / **待查（不猜）** |
| **2** | `手册_2_事件与扩展点.md` | 1284 行 | **两套事件体系辨析** / `EVENT_POINTS` **逐时点（17 点，标注是否真有派发点）** / **易混对专章（正反例）** / **owner 归属规则** / **E1/E2/E3 契约** / **`ext_api` 稳定面** / **包声明段全表（93 条）** / hooks（哪些是扩展点、哪些不是） / **怎么接** |
| **3** | `手册_3_权责与勿当bug修.md` | 807 行 | **三层权责地图**（框架 / 内容包 / 包自持代码 / 编辑器）/ **28 条门禁逐条表 + 门禁边界** / **★「勿当 bug 修」正式清单**（A 铁律·B 合理并存·C 注释直述·D CHANGELOG 裁决 + 待查）/ **改代码前 checklist** |
| 附 | `../审查/审计与方案/API手册_编撰前盘点.md` | 1626 行 | 素材盘点与缺口清单（编撰底稿，含 `file:line` 索引） |

---

## 三、最小上手（外部开发者最快的路）

1. **新建内容包**：`content/<包>/manifest.json` 声明模块 → 编辑器「**推荐组合**」一键勾（基础 RPG / 生活冒险 / 故事探索）→ 写各模块 JSON；
2. **要加玩法**：先在《手册·2》§7 找**有没有现成声明段**（93 条里有就写声明，不写代码）；
3. **要加代码**：走 **E1 指令 / E2 渲染 / E3 测试脚本**（**双闸默认关**，重名拒）；**只 import `qbot_rpg.ext_api`**（其余 `qbot_rpg.*` 为内部实现，**不承诺兼容**）；
4. **校验**：起编辑器 → 点「校验」→ 红拦必改、黄提示按需；
5. **交付**：导出 `.ttrpack`。

---

## 四、★ 给维护者：「勿当 bug 修」速览（完整版见《手册·3》§3）

| 现象（看着像 bug） | 真实设计 | 判据 |
|---|---|---|
| **锻造没有随机/成功率/失败分支** | 失败与随机**刻意隔离**到炼金与强化；锻造是确定性派生树、零会话原子 | `docs/m9_启动包.md:75/95`、`docs/细化/细化_2c2b_锻造流程契约.md:14/250` 等 8 处 |
| **同一效果旧键/新键都能写，且旧键符号常相反** | **键族别名归并**（`EFFECT_LEGACY_ALIASES` 等），为兼容既有内容 | `data/gear_stats.py`（批50/53 归并不双计） |
| **某表/常量还在，但已不生效** | **legacy 兜底 + 声明优先双轨**（包声明驱动新路径，旧常量兜底不砸包） | 批71 模式（`stance` / `role` / `sp_effects` / `schema_ext`） |
| **`mitigation` 阶段没并进承伤乘区** | 与**护盾/保底伤害交织**，混入易伤会算错（有意接受收敛度更低） | 批52 决策记录 + 批50 `consumer_note` |
| **`EVENT_POINTS` 里有 17 点，但只有 11 点会派发** | 其余属**二期预留**（声明 ≠ 可用；**用前先查手册**） | 《手册·2》§2 |
| **`ctx` 有约 170 个键却没有单一源** | 历史演进所致，**已知缺口**（不是设计，属待收敛） | 《手册·1》§6 |

> **判定原则（《手册·3》§3.7.2）**：能给出**文档铁律 / 代码注释 / CHANGELOG 裁决**判据的 → **不要动**；只能给出"看起来不合理"的 → **按 bug 处理并登记**。**置信度"待查"的，先查清再动手。**

---

## 五、维护规则（**重要**）

1. **本手册与其三章随框架版本更新**：凡改动 **[稳定契约]**（《手册·1》§5 / 《手册·2》§6 等）→ **必须同步更新手册并评审**；
2. **新增常量/字段/事件/声明段** → 同步进对应章节的表格（**表格是本手册的价值所在**）；
3. **发现"故意设计"被误改** → 立即回滚并在《手册·3》§3 补一条判据；
4. **手册与实现冲突时**：以**代码 + 门禁脚本**为准，并在文档登记偏差（不得只改文档不改代码）。

---

## 六、已知待办（来自本轮体检与手册编撰）

> **批74（2026-09-23）更新**：下表 BUG-1/2/3 三条已处置（BUG-1 修、BUG-2/3 裁定保留）；
> 另登记批74 查证发现（行号漂移 + veinborn 冒烟 2 缺口）。
> **批81（2026-09-23）更新**：批74 登记的 veinborn 冒烟 2 缺口 + BUG-5（G-5/G-6）已修复；
> `verify_veinborn_smoke.py` 已 SKIP → **PASS 10/10**；另新登记 3 条内容侧发现（见下）。
> **批82（2026-09-23）更新**：依据 `/root/deliverables/手册剩余项_核清与口径.md`，本条清单
> 的**手册剩余项全部收口**——用户 4 条裁决（D1 `pvp.mode` 保留·D2 命中键 v1 不分治·
> D3 打造成本各系统自算·D4 `prof.energy.enabled` 兜底落地）+ 可立即执行项（B2 `slot_defs`
> 措辞、C1 B5 门槛统一）+ 《手册·1》§7 Q1~Q8 逐条查清入册；新登记 NEW-1~9（NEW-1 由 D4
> 落地，其余为文档错 / 待查 / 待收敛，见 `docs/矛盾与待裁决登记.md`）。
> **批83（2026-09-23）更新 · 真缺口收敛**：Q4 的 **N2/N3/N5 已修**——`ItemInstance` 写路径
> 两条内联归一收敛为公共函数 `data/item.py::item_instance_from_mapping`（补回
> `effect_refs`/`stack_max`/`cooldown_until`）；Q2 的 **3 个零消费 ctx 键已逐键标注**（见
> 《手册·1》§2.7.3，不删键）；NEW-2/3/4/5/6/7/8/9 逐条处置（见 `docs/矛盾与待裁决登记.md`）。

| 项 | 来源 | 状态 |
|---|---|---|
| `pvp.py` free 模式 `enemy_act` 真 bug | 《手册·3》§3.7.1 BUG-1 | ✅ **已修（批74）**：free 模式移除已删的 `enemy_act` 调用（`core/pvp.py:367-378`）；防守方「一直防御」由引擎 `_ai_action_dict()` 对 `battle_type=="pvp"` 恒返回 guard 保证（`core/battle.py:5238`）；新增回归测试 2 条 |
| verify 脚本过期调用（`enemy_act`/`end_turn` 壳） | BUG-1 伴随项 | ✅ **已修（批74）**：`scripts/verify/verify_m1.py` 2 函数改 CTB 等价（`player_act`）；`scripts/verify_veinborn_smoke.py` 改 CTB 等价并登记 2 条独立缺口（见下） |
| `damage_base` / `heal_rate` 死兼容键 | BUG-2 | ✅ **裁定：保留（批74）**——JS 侧零硬编码 + 8 包实带 + 测试正锁 + 兼容承诺（`docs/编辑器重写_需求与约束.md:332`）；理由/删除条件见 `qbot_rpg/content/field_meta.py:1426-1438` |
| `data/status.py::StatusInstance` 死表示 | BUG-3 | ✅ **裁定：保留（批74）**——TC-04 门禁必需类型（`scripts/check_architecture.py:49-51`）+ 契约 spec；理由/删除条件见 `qbot_rpg/data/status.py:16-24` |
| 审计引文行号漂移（BUG-2：审计原 `:1381-1391`） | 批74 顺带登记 | ✅ **已登记**：批74 HEAD 现状 = `qbot_rpg/content/field_meta.py:1440`（`damage_base`）/ `:1444`（`heal_rate`）；同步于 API手册_3 §3.7.1 |
| `verify_veinborn_smoke.py` 2 条独立缺口 | 批74 查证发现（**登记不动手**） | ✅ **已修（批81）**：① CTB 战斗不再派发 `turn_end` → `surge_tick` 失效（困斗蓄能恒 0）——`core/battle.py::_after_actor_action`（ACTOR_TURN_END / AFTER_ACTION 尾部）补派发 `turn_end`（依据 功能三 §2.4 + 01_asset_inventory §0.2 映射）；② `vb_core_breaker.consume_marks` 误删为 `{}`（原 `{break_vein_core:120}`，91df0bd 大剑化重做误改）——已恢复。脚本 SKIP → **PASS 10/10** |
| `settings.pvp.mode` 现无分支消费 | 批74 修 BUG-1 发现（**登记不动手**） | ✅ **已裁决（批82 · D1，用户 2026-09-23 裁决 C）**：**保留**键 + 写明"暂未生效（二期）"——`content/field_meta.py` mode help「CTB 下非回合制『连续输出』尚未实现，当前两模式行为一致」+ `core/pvp.py` 注释 + `细化_4e`/`细化_3h` 文档注（后者顺带更正枚举 `turn/realtime`→`turn_based/free`，NEW-3）；**零逻辑改动**，勿当 bug 修 |
| `settings.slot_defs` ↔ `slots.json` "注释矛盾" | T4 / 批78 X3（**批82 B2 收口**） | ✅ **已登记（批82 · B2）**：二者是**不同数据空间**（运行时部位表 vs 珠插槽），非重复落点；`module_catalog.py` `overlap_note` 由「功能重叠/归口一处」纠正为「不同数据空间 / 各自单一源」，黄提示保留（`web/api.py` 兜底文案同步）；与批78 X3 一致 |
| B5 门槛两口径（`tier_index_for_level` vs `level` 直比） | `审查_M8实现_批次E2`（**批82 C1 收口**） | ✅ **已收敛（批82 · C1）**：`commands/alchemy_commands.py` 10 处直比统一走 `_tier_index_of`（`tier_index_for_level`，与 `/登记` 同口径）；全包 `tier_names=7` + PRF-02 等长 + 等级封顶 ⇒ **恒等价**（250 决策对拍 mismatch=0，零行为变化） |
| 打造成本两口径（`craft_rules.cost_*` vs `forge.forge_fee`） | 审计3 §6（**批82 D3 收口**） | ✅ **已登记（批82 · D3，用户裁决 A）**：三者**范畴不同、各系统自算**——深度打造 `cost_*` = 投入预算（**不扣货币**）；`forge.forge_fee` / `recipe.cost` = 真扣货币；两处 help 互注，**不统一** |
| `debuff_chance_pct` / `buff_chance_pct` 是否按 scope 分治 | D-new-5（**批82 D2 收口**） | ✅ **已裁决（批82 · D2，用户裁决 B）**：**v1 不分治**——两旧键 = `status_chance_pct` 的**通用命中同义别名**（对 buff/debuff 一视同仁，批53 有意）；`data/gear_stats.py` help 改「通用状态命中提升」+ `consumer_note` 登记 |
| `proficiency.energy` 双源（契约兜底未实现） | C2 / NEW-1（**批82 D4 落地**） | ✅ **已实现（批82 · D4，用户裁决 A）**：`core/energy_bar.py` 增 `proficiency` 注入——`settings.alchemy.energy_enabled` **缺键**时回落 `proficiency.energy.enabled`；装配/指令壳接线；`max_by_tier`/`regen_sec` 标 **deprecated**；现存包逐字段对拍一致（零行为变化） |
| 《手册·1》§7 / 《手册·3》§3.6 的**待查项** | 各章 | ✅ **已核清入册（批82）**：Q1 ctx 键数改**区间口径**（注册 162~166 / 未注册 134~138）；Q2 三键零消费（`monster_pool`/`shop_engine`/`worn_refs`）登记结构性缺口；Q3 temper 优先链补引 `core/temper.py:136-167`；Q4 N2/N3 **未被裁决接受 → 真缺口**（排收敛批）；Q5 结案（读取侧双形态兼容）；Q6 `modules_raw` 形态表（36 = 24 list / 3 map / 9 object）；Q7 `_KIND_FOR_MODULE`=**28**（非 29）+ `catalog−kind` 差集（`settings`/`templates` 豁免，另 **6 项 NEW-6 已由批83 查清：能力标记豁免**）；Q8 `ABSOLUTE_QUALITY_MAX=100` **无 settings 旁路**（标注） |
| 文档与实现不一致（覆盖率目录 / 旧分层名） | BUG-5（API手册_3 §2.3 G-5/G-6） | ✅ **已修（批81，文档侧）**：G-5 `docs/细化/细化_M6_质量门禁.md` 覆盖率口径 → `core + content` 两目录（对齐 `scripts/run_all_tests.py:117`）；G-6 `docs/审查参考/RPG回合制框架设计文档.md` §2.1/§2.2 + `docs/审查参考/开发规则文档.md` §1.1 旧目录 → 实际 `qbot_rpg/{commands,core,world,storage,content,data,web}`（对齐 `scripts/check_architecture.py:31-47`）；代码未动 |
| `vb_tail_breaker.consume_marks` 同批误删 | 批81 查 A2 顺带发现（**登记不动手**） | ⏳ **未修**：91df0bd 同一提交把 `vb_tail_breaker.consume_marks` 由 `{break_tail_hammer:90}` 改为 `{}`（`derive_only` 同批 false→true），并删 `rb_leap`。与 A2 同因，但超出本批授权范围（只裁决 `vb_core_breaker`）→ 报告待裁决 |
| veinborn「机动泄压」内容侧缺失 | 批81 修 A1 顺带发现（**登记不动手**） | ⏳ **未修**：`content/veinborn/marks.json` 的 `surge_mark.desc` 承诺「玩家机动牌可移除1层」，但 `rb_leap` 已随 91df0bd 删除，现包内无任何技能移除 `surge_mark`；`surge_vent5` 效果定义悬空（无引用）；enemy action 的 `apply_mark` 字段引擎侧亦无消费点 |
| 依据文档曾悬空 | BUG-6 | ✅ 已修（回填 `docs/审查/审计与方案/`，本手册同批入库） |
| **N2/N3/N5**：`ItemInstance` 写路径归一静默丢字段（`/装备` 丢 `effect_refs` 且写回；`stack_max` 两条链路均丢；两内联归一与读档 codec 不一致） | 《手册·1》§6.2 N2/N3/N5 + Q4（**批83 修复**） | ✅ **已修（批83 · BUG-7）**：收敛为公共函数 `data/item.py::item_instance_from_mapping`（20/20 唯一源），`runner`/`basic_commands` 两条链路改为调用；修前 `effect_refs=()`/`stack_max=99` → 修后保留；正常流程逐字段对拍一致 |
| Q2 三键零消费（`monster_pool`/`shop_engine`/`worn_refs`） | 《手册·1》§2.7.3（**批83 标注**） | ✅ **已逐键标注（批83）**：前两者「当前无框架消费方」、`shop_engine`「恒 None · 准死键 · 待收敛」；包自持代码零读取；**不删键** |
| NEW-2/4/6/7/8（文档错 / 待查） | `docs/矛盾与待裁决登记.md`（**批83 处置**） | ✅ **已处置（批83）**：NEW-2 `jewel.py:41` 注释来源改正；NEW-4 悬空路径改引仓库根；NEW-6 勾选门禁口径（`CAPABILITY_MARKER_MODULES` 豁免）；NEW-7 `templates` 补注属有意；NEW-8 草案 `floor_ratio` 改 0.0 |


---

## 七、附：技能等级变量（批80 新增 · 2026-09-23）

**用途**：技能等级**不内置任何数值映射**；框架只把它作为**变量**交给作者，**由作者在技能公式里自行使用**。

| 项 | 说明 |
|---|---|
| **写法** | 技能公式中的占位符 **`[技能等级:<技能ID>]`** → 求值时取 `attacker.skill_level[<技能ID>]` |
| **值从哪来** | **三源并集**（**装备赋予** / **套装档位** / **`skill_slots` 行 level**），**同技能多源取最大**；纯读、不写存档（`core/skill_slots_battle.py`） |
| **何时可得** | **施放该技能时**（由装配层进战注入 `self._config["skill_levels"]`，`core/battle.py`） |
| **缺省行为** | 该技能**无等级来源** → 等级 **1**；公式**不引用该变量** → 结果**逐字节与改动前一致** |
| **声明侧（F18）** | `skills[].level = {max, growth[]}`：**`max ≥ 2` 才进等级线**；`growth` 长度须 = `max`、`growth[0]` 须 = 1.0（**非法红拦**）；既有标量 `level: 1` **兼容**（不报错、不进线） |
| **示例** | `[技能等级:fireball] * 8 + 100`（等级越高倍率越大——**用什么曲线由作者定**） |

> **【稳定契约】**：变量名 `[技能等级:<技能ID>]` 与三源合并规则（多源取最大）为稳定面，改动需评审。
> **勿当 bug 修**：框架**故意不内置**"等级→伤害/冷却"的映射——那是**作者的公式自由**，不是缺失功能。
