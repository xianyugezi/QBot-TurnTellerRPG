# 任务书 · 专项·引擎层直出文案 第二批（炼金引擎 + 残余兜底）

> 2026-09-12 · 消息模板重构战役 · 执行者：子 agent（本任务书 + `docs/消息模板重构/00_方案与规范_v1.md` 为唯一指令源）
> 背景：迁移已归零（分区未迁键 = 0，全量表 713 键），但**引擎/壳层仍有玩家可见中文直出**。
> 本批清台账 02_遗留登记 #16/#52 登记的余项。

## 1. 范围（逐处处理，全部要改成表驱动）

**A. 炼金引擎 message（台账 #16）**
1. `qbot_rpg/core/alchemy_battle.py` L318 / L489：「本场战斗已使用过即时调合（限 1 次/场）」等直出串
2. `qbot_rpg/core/alchemy_harvest.py`（约 8 处 message）：「等级不足：炼金职业需达到 正式」、
   「尚未种植/无待收取」类提示
3. `qbot_rpg/core/alchemy_helper.py`：「任务格式非法」、「请指定 代采 或 代调 任务…」、
   「缺少能源道具…」类提示
4. `qbot_rpg/core/alchemy_materials.py` 兜底（材料不足/配方不存在，台账 #16）

**B. 残余兜底（台账 #52）**
5. `qbot_rpg/core/investigate.py` map_reveal / hunt 的 f-string 兜底句
6. `qbot_rpg/commands/achievement_commands.py` 的 `"状态："+…` 拼接行
7. `qbot_rpg/core/hidden_trigger.py` `DEFAULT_CONFIRM_TEXT`

> 说明：若某处 message 由**引擎纯函数**产出且调用链拿不到 ctx，允许「模板表默认值 + 调用方注入覆盖」
> 两级形态（对齐第一批 `environment_lore` 的做法），但**必须**在报告里逐处说明该兜底路径。

## 2. 达标口径

- 新键进全量表（命名成体系，如 `alchemy_engine_*` / `invest_hunt_*` / `ach_status_line` / `hidden_confirm`），
  `content/test_demo/templates.json` 同步；免斜杠；错误行 ❌+原因(+下一步)；结构化行 ≤14 全角。
- **引擎侧测试常按旧 message 逐字断言**（如 `tests/unit/test_alchemy_battle.py:274`、`test_resource_commands.py`）——
  同步更新断言即可，**不得**为了过测试把文案改回硬编码。
- `scripts/check_template_width.py` 0 FAIL；`test_template_table` / `test_emoji_discipline` 绿；全量 `pytest tests/ -q` 绿。

## 3. 工作清单
1. 先落 fragment：`docs/消息模板重构/_fragments/spec_env2.json` → commit。
2. 合表：`python3 scripts/tpl_merge_fragments.py docs/消息模板重构/_fragments/spec_env2.json`
3. 改代码：上述 7 处 → 表驱动 + 调用链注入；内容包同步。
4. 自校验：checker + 定向（alchemy 各模块 / investigate / achievement / hidden_trigger）+ 全量（报计数）。
5. `ruff check <改动文件>` 干净；commit。**禁止 push / 禁止动 main**。

## 4. 报告（回复正文内联）
- fragment 路径 + 新键文案 JSON 全量；
- 「旧硬编码（文件:行）→ 新模板键 → 调用链」对照表；
- 改动文件清单；checker 输出；定向+全量计数原文；**仍保留兜底常量的清单**（如有）；commit hash。
