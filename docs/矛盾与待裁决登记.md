# 矛盾与待裁决登记（编辑器框架）

> **来源**：用户 2026-09-16 拍板落地 CakeGame《表写入规范》可借鉴项 **E**——「文档矛盾登记（⚠️ 只登记不裁决）」正式化；
> 归入本仓既有散落项（`docs/能力缺漏审计_四方对照.md` §五 U1–U10、台账「待确认」区、各文档「待确认 / 待裁决 / 存疑 / 矛盾」标记）。
> **互链**：`docs/编辑器修改意见0915_台账与方案.md` §十三（批次入口）· `docs/能力缺漏审计_四方对照.md` §五（未确认区）。
>
> **纪律（写死，本表唯一规则）**：
> 1. **只登记，不裁决**——本表不替用户拍板；未裁决项一律保留双方证据。
> 2. 每条含三段：**编号 / 一句话争点 / 双方证据（文件:行 或 文档行号）** + **状态**。
> 3. 状态取值只三档：**待实测**（含「待排期 / 待专批 / 待收敛」等实测性质）｜**待用户裁决**｜**已裁决-日期 + 结论与依据**。
> 4. 已裁决项**必须**写清裁决日期与依据（拍板记录 / commit / 契约文档行号）；文档间的矛盾**不自行压平**。
> 5. 新矛盾**追加**，旧条不删——只把状态从「待…」改为「已裁决-…」。
>
> **维护注记（批76 · 元级教训）**：
> - **本文下次对账基线 = 批76 HEAD（a28cc6d）**（本轮重核基线 = 批75 HEAD `a40d755`；
>   重核报告 `对账清单_26条重核.md` 的基线为批74 `ed4fad4`，批75 仅改文档/页脚/verify，代码证据行号未变）。
> - **登记表会漂移：销项 / 改状态 / 补计条目后，必须在同一提交内同步 §二 汇总与「独立条目」注记。**
>   本轮元级发现 = 批35 追加 X17/X18 后汇总未同步，§一 明细实为 **28 条**而 §二 仍自称「26 条」；
>   下次对账前先跑「§一 明细 ↔ §二 汇总」一致性校验。

---

## 〇、采集口径与来源清单

**grep 口径**（本仓自有文档，排除冻结策划书与历史审计报告）：

```
grep -rn "待确认|待裁决|存疑|矛盾|待定|待收敛|未定案|未登记待确认" docs/ --include=*.md
  # 排除 docs/审查参考/（冻结策划书原件）与 docs/审查/幻觉审查*（历史审计报告）
```

原始命中 349 行；按「是否构成编辑器/框架的**独立**未决项」去重收敛为 **28 条**（U1–U10 + X1–X18；X17/X18 于批35 追加，批76 重核补计）。其余命中多为 M0–M13 历史里程碑报告的「未实现」状态标签，不构成独立争点。

| 来源文件 | 条目 |
|---|---|
| `docs/能力缺漏审计_四方对照.md` §五（:312-325） | U1–U10 |
| `docs/编辑器修改意见0915_台账与方案.md`（§四 :100-108、§十一 :296-329） | U1–U7 裁决依据；X1 / X3 / X4 / X5 |
| `docs/编辑器重写_需求与约束.md` | U5 / U6 边界；X4 / X5 契约 |
| `docs/消息模板重构/02_遗留登记.md` | X6 / X7 / X8 / X9 |
| `docs/m12_UX架构方案.md`（原 :467；作废块 :3-10） | X10 |
| `docs/指令清单_全量.md:19` | X11 |
| `docs/审查/幻觉审查_1a.md:18` | X14（唯一保留的历史审计独立未决项） |
| `docs/审查参考/战斗数值层设计定稿.md:1070-1077`、`docs/进阶职业继承_设计口径.md:176-179` | X17 / X18 |
| `qbot_rpg/**`（代码实测） | U1 / U2 / U7 / X2 / X3 / X11 的实现事实 |

---

## 一、登记表

### A · 已解决 / 已裁决（✅ 26 条）

| 编号 | 一句话争点 | 双方证据（当前 HEAD） | 状态 |
|---|---|---|---|
| U1 | **采集 / 挖掘**在编辑器里是独立模块，还是 `maps` 子段？ | 裁决落点：`qbot_rpg/content/field_meta.py:1044`、`qbot_rpg/content/map_models.py:284-336`（V-1Z~V-5Z 校验）、注册 `qbot_rpg/assembly/router_setup.py:56/129`；目录条目 `qbot_rpg/content/module_catalog.py:132-133`（`gathering` **已无 `implemented=False`**，`:128-131` 注释逐字「批36 · X2 已实装」） | **已裁决-2026-09-15**：数据落点 = `maps.gather_points` + 目录条目 `gathering`；「引擎待排期 / 目录条目标未实现」子句已由 X2（批36）关闭，条目翻转可启用 |
| U2 | **时间天气**编辑器归属：挂「通用设置」卡片，还是独立一级模块？ | `qbot_rpg/content/field_meta.py:3101-3102`（`settings.time_cycle` → `_soft_display("时间天气","obj",…)`）；`qbot_rpg/content/module_catalog.py` 全文 grep `time_cycle` = **0 命中**（无一级模块）；引擎 `qbot_rpg/core/worldtime.py` 存在 | **已裁决-2026-09-15**：挂「通用设置 → 【时间天气】卡片」，**不**新增一级模块 |
| U3 | `覆盖审计_F` 缺失-1（锻造防具套装 P1）判据是否仍成立？ | `docs/细化/细化_2c2d_锻造套装与客制.md` **已存在**（55560 B，`ls -la` 实测）；`docs/能力缺漏审计_四方对照.md:318`/`:414` 基线过期自述 | **已裁决-已失效（基线过期）2026-09-16（批30）**：「2c2d 不存在」判据失效；**不代表**套装/客制已实现 |
| U5 | `profession.json` 分层冲突如何收敛？ | `content/veinborn/field_meta.json:3-10`（`entry_merge.life = [proficiency, enhance, forge, recipe]`）；聚合实现 `qbot_rpg/web/api.py:407-471` `_entry_merge_map()`；展示名 `content/veinborn/field_meta.json:1905`「生活」 | **已裁决-2026-09-15**：用 `life` 虚拟聚合视图收敛，**不改策划书** |
| U6 | `message_prefix` 与 `templates` 职责边界，是否合并？ | `qbot_rpg/content/field_meta.py:3140-3141`（`settings.message_prefix` 登记）；消费 `qbot_rpg/commands/prefix_wiring.py:1-58`（`read_message_prefix_settings`/`apply_message_prefix`）；`qbot_rpg/content/module_catalog.py:147-148`（`templates` 独立模块） | **已裁决-2026-09-15**：**不合并**（前缀管首行外壳、templates 管正文文案） |
| U7 | **怪物触发类型枚举双源冲突**，以哪份为准？ | `qbot_rpg/content/validator.py:247-253`（`TRIGGER_TYPES` 权威 15 类，含印记扩展 2 类）、`:254-258`（`TRIGGER_ALIASES` 归一）、`:260-264`（`TRIGGER_LEGACY` 6 类接受不提示） | **已裁决-2026-09-15**：以**校验器现行枚举为准**；另一套命名作 legacy 继续接受 |
| U8 | 商店字段新旧混用：编辑器下拉应出哪套？ | `qbot_rpg/content/field_meta.py:819`（`SHOP_ITEM_ENTRY_CHILDREN`）；兼容读 `qbot_rpg/core/shop.py:537-548`（`per_player`→limit、`per_player_period`→period，不静默丢弃）；枚举同源 `qbot_rpg/content/shop_models.py:70/81/88/92`；4 包 `shop.json` 实测无旧键；回归 `tests/unit/test_batch30_shop_field_convergence.py` 12 passed | **已裁决-已收敛（定稿键）2026-09-16（批30）**：写入用定稿 `scope/limit/period`，旧键只兼容读 |
| U9 | 总纲 §6.12 编辑器进阶能力（L993-1198，约 200 行）是否需补专项逐条核对？ | 交付物**已存在**：`/root/deliverables/U9_编辑器进阶能力_逐条核对.md`（769 行，57620 B，2026-09-17；自述「范围 §6.12（v2.3）L993–1198，共 **25 个主条目**、拆出约 **79 个子检查点**」）；X16/X17 的登记证据均引该文件（`:284-305`/`:685`） | **已裁决-已交付 2026-09-17（批31）**：所问「是否需补专项逐条核对」的执行结果已交付，本条销项 |
| U10 | `enemies.active_time` 双份 schema：刷怪行字段还是怪物字段？ | 登记位 = 刷怪行：`qbot_rpg/content/field_meta.py:1007-1022`（`DUNGEON_MAP_ELEM_CHILDREN["monsters"].element.children`，`:1020` `active_time`）；**已接到 maps 模块**：`qbot_rpg/content/field_meta.py:2855-2860` 注释逐字「批13 C（审计 ①-2-12/B6）：把 DUNGEON_MAP_ELEM_CHILDREN 的刷怪行结构接到 maps 模块」；引擎消费 `qbot_rpg/world/spawn.py:151-162/261`、`qbot_rpg/world/spawn_weather.py:216`；`enemies` 模块无该字段 | **已裁决-已收敛（刷怪行字段）2026-09-21（批76 重核）**：旧「maps 模块不生效」论据（B6）已失效；若要在**怪物条目级**也配 `active_time`，属**新增需求**而非本项遗留 |
| X1 | `#9 公式未登记键`：`formula` 是否仍有「已消费但未登记」的标准键？ | `qbot_rpg/core/formula_loader.py:71-99`（只按已知段读取）、`:164`（装配 `monster_def_rate`）；台账 `docs/编辑器修改意见0915_台账与方案.md:324` 逐字「**未登记待确认：无**」 | **已裁决-2026-09-16（批19）**：清单闭合，无新未登记键 |
| X2 | **采集 / 挖掘引擎未实现**（独立 `/采集` 与「挖掘」通道均无引擎） | 引擎 `qbot_rpg/core/gathering.py`（16246 B，`grep '^def'` 实测 11 函数，覆盖 GP-01~GP-11 全链）；指令壳 `qbot_rpg/commands/gather_commands.py`（14102 B）；接线 `qbot_rpg/assembly/router_setup.py:56/129`（`:255-267` stub 字典实测**已无「采集」**，`:260` 注释「批36 · X2：/采集 已接真实采集引擎 → 从 stub 清单移除」）；旧自承已撤销 `qbot_rpg/commands/explore_commands.py:1-6`；字段与校验 `qbot_rpg/content/map_models.py:284-336`；编辑器 `qbot_rpg/content/field_meta.py:1044`/`:2871`；目录 `qbot_rpg/content/module_catalog.py:132-133`；回归 `tests/unit/test_gathering.py` 21 + `test_gather_commands.py` 16 + `test_gather_points_validation.py` 21 = 58 passed | **已实现-批36（2026-09-19）**：「挖掘」按定稿落为来源通道/产物族（矿石系），不新造 `/挖掘` 指令与 schema 字段；**GA-1 待用户裁决**见 `docs/采集挖掘_实现口径.md:190` |
| X4 | 生活系统归口：挂 `jobs` 父模块下，还是独立「生活」？ | 同 U5：`content/veinborn/field_meta.json:3-10`（`entry_merge.life`）；`qbot_rpg/web/api.py:471` 注释「虚拟聚合视图（如「生活」）」 | **已裁决-2026-09-15**：`life` 虚拟聚合视图，不改策划书 |
| X5 | 台账批4 表头「待确认」三条（#7 中文 ID 规则 / #10 概率覆盖 / #11 预设归属） | 契约 `docs/编辑器重写_需求与约束.md:207`「〇·补八、自动 ID / 使用处覆盖 / 条目预设契约（批16 落地）」；逐条 `:218`（id_prefix 来源）/`:230-234`（使用处 > 定义处）/`:244-251`（entry_presets + framework_preset_key_errors） | **已裁决-2026-09-15**：#7「前缀+序号」/ #10「使用处 > 定义处 > 缺省」/ #11 六预设 ∪ 包声明 |
| X7 | 「表外旧键 214 条」待收敛 | 脚本实测全部 `content/*/templates.json`：`test_demo` 718 键 **表外 0**、`veinborn` 4 键 **表外 0**，**合计 0**；表键总数 902（`TABLE_TEMPLATES`）；门禁 `tests/unit/test_template_table_final.py:83-106`（`resolve_templates(pack, strict=True)` + 表外键 assert）6 passed；收敛路径 `docs/消息模板重构/02_遗留登记.md:57`「删除 21 个表外旧键 → 619 键」（此后扩表至 902） | **已裁决-已解决（批76 重核）**：214 → 0；门禁已固化（建议同步 `02_遗留登记.md` #20 状态为已闭环） |
| X8 | 炼金两处换行 / 去头口径存疑 2 项 | `template_table.json` 四键逐字（`alchemy_feed_chain`/`alchemy_feed_chain_effect`/`alchemy_inherit_slot_used`/`alchemy_inherit_gold_slot`）；宽度门禁 `scripts/check_template_width.py` **FAIL 0 / WARN 14**（表内 902 条）；规范 `docs/消息模板重构/00_方案与规范_v1.md:39-55` | **已裁决-实测无误 2026-09-16（批30）**：两处**不改** |
| X9 | GM「编辑」指令（链接源已清空）删除 / 改造？ | 全仓**无生产引用**：`qbot_rpg/data/gm_constants.py:43` 仅剩注释 `# GM_CMD_EDIT = "编辑" # G13 —— 批30 删除`；`qbot_rpg/commands/gm_commands.py:14/804/1038` 均为删除说明注释；grep `GM_CMD_EDIT|cmd_gm_edit|editor_link|editor_url` 命中**全为注释** | **已裁决-已删除 2026-09-16（批30）**：5b 契约 G13 已删 |
| X10 | `docs/m12_UX架构方案.md` 附录 B 九项【待定】/ 整档是否作废？ | `docs/m12_UX架构方案.md:3-10` 顶部醒目作废块逐字在（「本文档已被《编辑器重写_需求与约束》取代（2026-09-16）；附录 B 的 9 项【待定】随之作废」） | **已裁决-整档作废 2026-09-16（批30）** |
| X11 | 全量指令清单「设计未实现」约 36 条 | `docs/指令清单_全量.md:19` 汇总行已修正（移除「时间天气/木桩/GM 运维」等过期表述，条数待重算）；实测 `build_router` 注册 **119 条**、`DEFAULT_WHITELIST - registered = []`（白名单**零未接线**）；GM 运维真缺口 5 条（G5 /调试 · G6 /测试 · G7 /广播 · G9 /玩家查询 · G11 /解封）已全部接线（`qbot_rpg/data/gm_constants.py` + `qbot_rpg/commands/gm_commands.py` + `qbot_rpg/commands/parsers.py` + 模板表）；口径 `docs/GM运维批75_实现口径.md` | **已实现-批75（2026-09-23）**：GM 真缺口清零；`/广播 定时=HH:MM` 如实登记为缺口（框架零 apscheduler）；清单其余过期行（PVP 决斗 4 等）按系统开发/重生成待办 |
| X12 | 战后恢复 `post_battle_recovery` 未实现 | 引擎 `qbot_rpg/core/post_battle_recovery.py`（7804 B，`SECTION_KEY` `:43`）；战斗结束消费 `qbot_rpg/commands/battle_commands.py:1243`、`_apply_post_battle_recovery` `:1270-1282`；字段 `qbot_rpg/content/field_meta.py:3168-3172`；校验 `qbot_rpg/content/validator.py:869-871` + `:1950-1975`；渲染 `qbot_rpg/core/message_format/battle_render.py:247` + `qbot_rpg/core/templates/template_table.json:503`；回归 `tests/unit/test_post_battle_recovery.py` 24 passed | **已实现-批37（2026-09-19）**；**D1~D8 数值口径待用户裁决**（`docs/战后恢复_实现口径.md` §四） |
| X14 | 怪物防御率因子：定稿无定义？ | 定义 `docs/审查参考/战斗数值层设计定稿.md:42`（`monster_def_rate` · R-09，2026-08-18）；因子 `:29`/`:34`；消费 `qbot_rpg/core/formula_loader.py:164`、`qbot_rpg/core/battle.py:4815-4832`、`qbot_rpg/core/damage.py:319/530/552` | **已裁决-不清理（前提不成立）2026-09-16（批30）**：本仓 383 行版已有完整定义 |
| X15 | §6.12-01 的「**版本快照**」是否立项？ | 排除依据 `docs/编辑器重写_实现方案.md:733`「不做富文本编辑、不做多人协同、**不做版本 diff 视图**」；撤销/重做 `qbot_rpg/web/static/index.html:1044-1048`（工具栏）/`:1295-1301`（历史栈）/`:3210-3217`；单级备份 `qbot_rpg/web/editor_ops.py:1564` `module_backup` / `:1573` `rollback_module` | **已裁决-不做 2026-09-17（批33）**：多版本快照**不做**；草稿级撤销/重做已落地 |
| X16 | §6.12-05 CSV 导入顺序与验证 / §6.12-20 公式注入防护 | `qbot_rpg/content/csv_codec.py`（18995 B，注入中和）、`qbot_rpg/web/csv_ops.py`（25350 B，行序/逐行引用/冲突/落盘）；路由 `scripts/editor_host.py:268`（export_csv）/`:282`（import_csv）；回归 `tests/unit/test_batch34_csv_ops.py` 19 passed | **已裁决-已落地 2026-09-17（批34）**：单对象（object）模块不支持 CSV 为如实边界 |
| X17 | §6.12-12 进阶职业卡片（职业树继承）是否落地？ | `qbot_rpg/core/job_slots.py:107` `resolve_inherit_chain` / `:148` `inherited_skill_ids` / `:326` `job_inherit_summary`（装配注入 `:315-317`）；编辑器 `qbot_rpg/web/api.py:1414` `job_tree` + `scripts/editor_host.py:120-122` `/api/pack/{pack}/job-tree`；回归 `tests/unit/test_batch35_job_inherit.py` 20 + `test_batch35_job_tree_editor.py` 7 passed | **已裁决-已落地 2026-09-17（批35）**：配置驱动、传递继承；语义粒度见 X18 |
| U4 | 伤害构成统计 / `dummy_log` 是否已实现？ | ①收集器（既有，批78 补 schema）：`qbot_rpg/core/battle.py:1085-1110` `per_action`——enabled 时记录带 `name`（展示名）/`penetrate`，disabled 时字段集=批77 基线 10 字段。②聚合/配置：`qbot_rpg/core/damage_stats.py`（`aggregate_per_action` 按来源求和+占比降序+通道占比+最大连段+会心/格挡；`load_stats_collector_cfg` 归一 formula.json `stats_collector`；dummy_log 环形缓冲）。③展示：木桩战后 `qbot_rpg/commands/battle_commands.py` 注入 `ctx["battle_summary"]` → 既有 `render_battle_end`/BREP-25（模板 `battle_summary_*`）；`dummy_realtime` 行动末累计摘要（`EnrichedTurnReport.stats_line`）。④存档/查询：`dummy_log` 写玩家 `persistent_state`，`/木桩 记录` 分页展示（`commands/dummy_commands.py`）。⑤回归 `tests/unit/test_batch78_u4_stats.py` 18 passed | **已实现-批78（2026-09-24）**：聚合/展示/dummy_log 全部落地；`enabled=false` 逐字段零变化（测试内对拍断言）；残留口径差异见 §C U11/U12 |
| X3 | `slots` 模块 ↔ `settings.slot_defs` 功能重叠归口 | 已纠正：`qbot_rpg/content/module_catalog.py:87-101` label/purpose=「珠插槽」、`overlap_note` 说明二者语义（settings.slot_defs=EQP-04 运行时部位表；slots.json=`{equip_id, slots:[{slot_level}]}`=珠插槽，`qbot_rpg/core/jewel.py` 消费）与「改哪处 / 归口 / 不阻断」；`qbot_rpg/web/api.py:790-813` `_module_overlap_hints()` 黄提示机制不变（目录声明驱动，框架无包业务名）。证据：`content/test_demo/slots.json` 4 条珠插槽且 settings.json 无 slot_defs（target_present=False）；veinborn 反之（True）。回归 `tests/unit/test_batch78_x3_slots_hint.py` 5 passed | **已裁决-已纠正（批78）**：非重复落点（不同数据空间），保留黄提示说明关系；X3 关闭 |
| X18 | 职业树继承的粒度：**单级**还是沿树**传递**？是否需要 `mode`（替换/追加）与技能等级继承？ | 引擎 `qbot_rpg/core/job_slots.py:121-188` `resolve_inherit_chain`/`inherited_skill_ids` + `:210-241` `_apply_inherit_replace`（批79）；校验 `qbot_rpg/content/job_validator_v58.py:505-552` V9（mode 未知 Y-23 / replace 悬空 R-4）；字段 `qbot_rpg/content/field_meta.py:2578-2600` + `qbot_rpg/content/job_models.py:387-394`；编辑器 `qbot_rpg/web/api.py:1414` `job_tree`；口径回填 `docs/进阶职业继承_设计口径.md` §5（Q1~Q4）+ `docs/职业继承mode批79_实现口径.md`；回归 `tests/unit/test_batch79_inherit_mode.py` 17 passed | **已裁决-2026-09-23**（①传递闭包 ②空=全部 ③`mode`=替换/追加 已实现 ④技能等级不做·另立设计）：X18 关闭 |

### B · 待用户裁决（❌ 仍未解决 · 0 条）

| 编号 | 一句话争点 | 双方证据 | 状态 |
|---|---|---|---|
| — | （无） | — | — |

> 原 B 区 **X18** 于**批79 销项**（用户 2026-09-23 裁决，已移入 §A）；
> 原 B 区 U9 / U10 两条于**批76 销项**，已移入 §A（U9 = 专项核对已交付；U10 = 已由实现收敛为刷怪行字段）。

### C · 部分解决 / 待实测（🟡 3 条）

| 编号 | 一句话争点 | 双方证据 | 状态 |
|---|---|---|---|
| U11 | `stats_collector.enabled=false` 是否应停掉既有 per-action 收集？ | 定稿 §8.4 L368 括注「关=不收集不展示」；本批为守「零行为变化」，enabled 只门控新增聚合/展示/dummy_log，既有 per-action 收集保持（disabled 时记录字段集与批77 基线逐字段一致，见 `test_batch78_u4_stats.py`） | **待用户裁决**：①保持现状（enabled 只管新增能力）②按定稿关闭收集（disabled 时 `per_action` 置空 = 行为变更） |
| U12 | `dummy_realtime` 的「本回合」口径 + 普通战斗战后明细开关键名 | 定稿 §8.2 L346 写「本回合会心数」；per-action `seg` 现记录全局流水号（非遗留连段段数，见 U4 旧证据）→ 无法可靠拆分本回合；§8.4 无「普通战斗战后明细」键，而 §8.2 L350 称「作者可开战后明细」 | **待用户裁决**：①realtime 保持累计值（现状）②先修 `seg` 语义再拆本回合 ③补普通战斗明细开关（新键名待定） |
| X6 | 引擎 / 跨模块渲染文案是否迁入新式模板表？ | 已迁：`qbot_rpg/core/templates/template_table.json:754` `explore_rest_reason_not_safe`、`:759` `battle_lock_weak_block`（批71 F1），`qbot_rpg/commands/sender.py:86-95`（`TPL_ERR_BAD_COMMAND`），`qbot_rpg/commands/forge_commands.py:282`（尾行 Tip）；**仍未迁**（AST 粗筛）：`qbot_rpg/core/checkin.py` 24 处（`:856/889/893/935/942-951/961/1027` 等）、`qbot_rpg/commands/gm_commands.py:236` `_EMPTY_LOG`、`qbot_rpg/world/movement.py:62-70` `_REASON_*` 8 条、`qbot_rpg/core/battle.py:3023/3042/3089/3129`、`qbot_rpg/core/equipment.py:355/359/763/772/877/880`、`qbot_rpg/core/shop.py` 与 `qbot_rpg/core/alchemy_deep.py` 多个直出串 | **待实测-部分解决（待专批·未闭环）**：批71 F1 只关两条键，专批整体未做；剩余面横跨 7+ 文件，工作量中~大 |

### D · 已裁决 · 不做（非缺口，是决定 · 1 条）

| 编号 | 一句话争点 | 双方证据 | 状态 |
|---|---|---|---|
| X13 | NPC `repair` / 装备耐久未实现（策划书自承降级） | `enemies`/`items` 无 `durability` 字段（`grep durability qbot_rpg/content/field_meta.py` = **0 命中**）；`qbot_rpg/core/npc.py:111-112` `DEGRADED_ACTIONS=("repair",)`、`:996-998` `_action_repair` **恒** `_res("repair", False, kind="degraded", reason="repair_unavailable", …)`；`qbot_rpg/content/npc_models.py:32/71-73/797` 同口径；`docs/能力缺漏审计_四方对照.md:102`（C10） | **已裁决-不做（2026-09-23 · 用户明确「装备耐久就不做了」）**：NPC `repair` 所依赖系统的「降级」即最终口径；**这不是缺口，是决定**，勿当 bug 重提（详见文末「追加：X13 装备耐久 · 用户裁决」） |

---

## 二、汇总

| 判定 | 条数 | 编号 |
|---|---|---|
| ✅ 已解决 / 已裁决 | **26** | U1 / U2 / U3 / U4 / U5 / U6 / U7 / U8 / U9 / U10 / X1 / X2 / X3 / X4 / X5 / X7 / X8 / X9 / X10 / X11 / X12 / X14 / X15 / X16 / X17 / X18 |
| 🟡 部分解决 | **3** | X6 / U11 / U12 |
| ❌ 仍未解决（待用户裁决） | **0** | — |
| ⚪ 已失效（前提不再成立） | **0** | — |
| ⛔ 已裁决 · 不做（非缺口，是决定） | **1** | X13 |
| **独立条目合计** | **30** | U1–U12 + X1–X18 |

> **计数口径（批76 重核）**：✅23 = 重核报告 §0.2 的 ✅22 **+ X11**（批75 销项）；🟡3 = 报告 🟡4 **− X11**；X13 单列「已裁决·不做」。
> X11 状态已由批75 改为 `已实现-批75`，故不再留在 🟡。23 + 3 + 1 + 0 + 1 = **28**。
>
> 注：批30（2026-09-16 对账清账）裁决 7 条：U3（失效·基线过期）· U4（已有·部分）· U8（已收敛·定稿键）·
> X8（实测无误）· X9（已删除）· X10（整档作废）· X14（不清理·前提不成立）；批33 X15；批34 X16；
> 批35 X17（新增）+ X18（新增，**批76 补计进汇总**）；批36 X2 结转「已实现」；批37 X12 结转「已实现」；
> 批75 X11 销项 + X13 归「已裁决-不做」。
> **批76 重核**（基线批75 HEAD `a40d755`）：销项 **U9**（交付物已存在）/ **U10**（实现已收敛为刷怪行字段）/ **X7**（表外实测 0）；
> 补计 **X17/X18** 进汇总（26 → 28）；纠正 **U1**（删「目录条目标未实现」过期子句）/ **X3**（描述与实现冲突如实登记）；
> **X11** 由批75 销项、GM 真实缺口清零。
> **批79 销项**（2026-09-23 用户裁决）：**X18** 由「待用户裁决」→「**已裁决-2026-09-23**」
> （①传递闭包 ②空=全部 ③`mode`=替换/追加 已实现 ④技能等级不做·另立设计）→
> ✅ 25 → **26**、❌ 1 → **0**，独立条目合计 **30** 不变；**B 区清零**。
>
> **批82 新登记（NEW-1~9，独立命名空间，不改 U/X 计数）**：手册剩余项核清新发现 ——
> NEW-1 `prof.energy` 契约→实现未接线（**真缺口 → 已由批82 · D4 落地**）；NEW-2 `jewel.py:41`
> 注释来源记错（待修）；NEW-3 PVP 枚举两套名（已随 D1 更正 3h）；NEW-4 引用路径悬空
> （**重申已在册**，非新发现）；NEW-5 `_KIND_FOR_MODULE` 28（已随 Q7 更正）；NEW-6 6 个能力标记
> 目录条目无 loader kind（**待查**）；NEW-7 `templates` map 不进注册表（口径差·低）；NEW-8
> deep_craft 草案 `floor_ratio` 旧值（文档偏差·低）；NEW-9 ctx 3 键无消费方（**待收敛**）。
> 详见文末「追加：批82」。

---

## 三、维护约定

- 本表由**批次收尾**时更新：新增矛盾追加新 `X<n>`；已裁决条目改状态并写日期 + 依据（引用拍板记录行号 / commit）。
- **不得**因「本表存在」就跳过实测：`待实测` 项要在对应批次给出原始证据后再改状态。
- 与 `docs/能力缺漏审计_四方对照.md` §五 的关系：§五 是**审计原始未确认区**（U1–U10），本表是**正式登记 + 状态跟踪**；两处编号一致，§五 不删。
- **维护注记（批76 元级教训）**：
  - **本文下次对账基线 = 批76 HEAD（a28cc6d）**。
  - **登记表会漂移：销项 / 改状态 / 补计条目后，必须在同一提交内同步 §二 汇总与「独立条目」注记。**
    本轮教训 = 批35 追加 X17/X18 后汇总未同步，§一 明细实为 28 条而 §二 仍计 26 条；
    下次对账前先跑「§一 明细 ↔ §二 汇总」一致性校验。
  - 与 `docs/消息模板重构/02_遗留登记.md` 的 #20/#45 应与本表 X7/X6 同步状态（本轮 X7 已 0 条，建议该文档标注已闭环）。


---

## 追加：CakeGame 借鉴条目落地进度与用户裁决（2026-09-16）

> 来源：`/root/deliverables/CakeGame字段差距分析.md`、`CakeGame字段差距_补漏.md`。用户拍板「一二档全做」，补漏 α/D 组按下列口径处理。

### 已落地
| 批 | 范围 | 状态 |
|---|---|---|
| 批22 | 装备职业限制 `job_restrict`（复用 skills 同名键）· 使用等级 `use_level` · 战斗词条族（吸血/免伤/物穿值·比/法穿值·比，只加 `data/gear_stats.py` 注册表）· 无视护盾/免疫反弹（并入 effects）· 怪物侧复用同族键 | ✅ 并入 main（`3937213`），全量 7762 passed |

### 已裁决 · 明确不做（用户 2026-09-16）
| 条目 | 裁决 |
|---|---|
| **咏唱**（α8，装备附加 Re 内的咏唱机制） | **不要**（用户明确） |
| **同伴 / 好感系统**（补漏 H4 / D-3） | **不要**（用户明确） |
| G2 提交时消耗 vs 仅校验持有 | **不做**——补漏已判"已有等价"：我们 `quest.consume`（deliver/collect 两态）同义 |

### D 组已裁决（2026-09-16 · 主 agent 依用户授权判断）
> 本区原列「待裁决（保留，等用户）」；用户 2026-09-16 授权「你判断就行」后，D-1/D-3/D-5/D-6/D-7
> 五条全部收口，逐条状态与**可核对依据**见下（另见 §追加 D 组裁决表）。
| # | 事项 | 状态与依据 |
|---|---|---|
| D-1 | **转职属性继承开关** 是否推翻 `core/levelup.py:262`（2026-09-09 拍板：职业成长跟随职业，不保留旧职业成长） | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）**：依据 `core/levelup.py:262` 的 2026-09-09 拍板（保留旧职业成长会数值叠加膨胀）；开开关=把已定数值口径重新变成可选项，风险大于收益 |
| D-3 | 排行榜 / 玩家寄售商店（同伴好感用户已否） | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）**：系统级、成本最高；QQ 群场景优先级低于内容与编辑器 |
| D-5 | `Lost_target`（丢失目标机制）我们是否已有等价 | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）**：批28 核查=战斗链**无**仇恨/隐式寻敌等价物，属 CTB 显式目标模型差异（证据见 §追加 D 组裁决 D-5 行） |
| D-6 | `transfer_allowed`（强化转移）：**键在、校验在、`core/` 无消费点**（半个空壳） | **已裁决-已执行-删除（2026-09-16 · 主 agent 依用户授权判断）**：批28 删除框架子结构 + V5 校验 + 缺省键；批29（用户拍板「12 做」）清理内容包两处残留，全仓该键只剩设计文档历史引述与删除说明（详见 §追加 D 组裁决 D-6 行） |
| D-7 | `Basic_User` 34 项属性是否要在我们侧做"可配初值表" | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）**：新手锚点已由 K1（注册礼包 + 初始等级，台账 §十七 K1）覆盖；再压一层"属性初值表"违背"少而深"，运营侧无此需求 |
| — | **α 组其余条目**（α1 装备赋予技能【P0】· α2 物品最大获取数量【P0】· α3 装备增幅技能 · α4 技能条件进阶 · α5 替换普攻 · α6 替换职业） | **用户未否**，排入后续批次（拟批26） |

### 批次队列（串行，因均改 `field_meta.py`）
- 批23 技能/职业（`hp_cost` · 转职前置 `advance` 子对象 · `is_basic`）— 进行中
- 批24 地图/副本/任务（隐藏禁传送 · 进图消耗/门票 · 主动遭遇概率+参战数 · 副本层间推进 · 任务重置周期；**G2 已剔除**）
- 批25 NPC/成就/全局设置（功能次数上限 · 收费多通道 · 成就计入总数/自动领取 · 注册礼包 · 按状态禁用指令 · **指令限流 K3** · 消息分段上限）
- 批26 α 组（除咏唱/好感）

---

## 追加：D 组裁决（主 agent 判断，用户授权）· α 组全落地（2026-09-16）

### D 组裁决（用户授权"你判断就行"）
| # | 事项 | 裁决 | 理由 |
|---|---|---|---|
| D-1 | 转职属性继承开关 | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）**（保持 `core/levelup.py:262` 2026-09-09 拍板：职业成长跟随职业，不保留旧职业成长） | ①那是刻意拍板（保留旧成长会导致数值叠加膨胀，与他们定稿的"套装/成长数值膨胀对策"同向）；②开开关=把已定的数值口径重新变成可选项，风险大于收益；③将来真有玩法需要，再单开专项 |
| D-3 | 排行榜 / 玩家寄售商店 | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）** | 系统级、成本最高；QQ 群场景优先级低于内容与编辑器（好感一项目用户已否） |
| D-5 | `Lost_target`（丢失目标）是否有等价 | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）** | 我们的战斗是 CTB 行动条 + 显式目标，不存在"实体丢失目标"的隐式寻敌模型 → 不是缺口，是**模型差异**。**批28 核查证据**：①战斗为 1v1 显式对侧，`qbot_rpg/core/battle.py:853` `_opposite()`；②无仇恨/威胁表：`grep -rniE "aggro\|hate\|threat\|仇恨" qbot_rpg/` 全仓唯一命中 `qbot_rpg/core/battle.py:3412`「完整仇恨值（来源/衰减/远程系数）依赖多目标选择，属组队里程碑前置」（即未实现）；③唯一"丢失"构造是世界层 F-08 `lost_pending`（`qbot_rpg/world/battle_boundary.py:47-201` `TargetRef/build_lost_pending/decide_lost`；快照字段 `qbot_rpg/data/battle.py:75`、`qbot_rpg/core/battle.py:2211-2213` 仅置 None），语义=目标实例在世界/会话边界消失（被他人击败/刷怪删除/时段结束），**且全仓无生产调用点**（`decide_lost`/`build_lost_pending`/`apply_lost_pending`/`clear_lost_pending` 仅 `battle_boundary.py` 自身 + `tests/unit/test_battle_boundary.py`）；④`qbot_rpg/world/chase_resume.py` 为换区追击续战，与"丢失目标"不等价。故**不做** |
| D-6 | `transfer_allowed`（键在、校验在、`core/` 无消费点） | **已裁决-已执行-删除（2026-09-16 · 主 agent 依用户授权判断）** | 遵循奥卡姆：多余实体直接删除，不留"看着像功能其实没接线"的字段；将来要做"强化转移"再按需求设计。**执行证据（批28）**：①框架子结构 `qbot_rpg/content/field_meta_child_structure.py`（`enhance.settings`）；②V5 布尔校验循环 `qbot_rpg/content/enhance_models.py:308-311`；③缺省锚点 `DEFAULT_ENHANCE["settings"]`——三处已删；回归 `tests/unit/test_batch28_d6_transfer_removed.py`。**内容包残留已清理（批29，2026-09-17；用户 2026-09-16 拍板「12 做」）**：`content/veinborn/enhance.json` 数据键 + `content/veinborn/field_meta.json` 展示名两处已删；清理后 `check_pack` 仍 0 error / 332 warning（与删前一致，无红拦）；回归 `tests/unit/test_batch29_transfer_residue_cleaned.py`；迁移对拍基线按门禁语义重定（删除内容包展示名会让基线侧出现「仅迁移前有（删除）」硬差异 → 重定到本批清理提交） |
| D-7 | 玩家属性初值表可配 | **已裁决-不做（2026-09-16 · 主 agent 依用户授权判断）** | 新手锚点已由 K1（注册礼包+初始等级，台账 §十七 K1）覆盖；再压一层"属性初值表"违背"少而深"，且运营侧并无此需求 |

### α 组：全部落地（用户"阿尔法组全要"；**咏唱已明确不要**）
| 条目 | 内容 | 归属 |
|---|---|---|
| α1 | **装备赋予技能**（穿戴获得 / 卸下收回） | equipment |
| α2 | **物品最大获取数量**（0 不限 / 1 唯一 / -1 禁止获取） | items/equipment |
| α3 | 装备增幅技能（伤害/冷却/熟练 三类增减 + 上下限） | equipment |
| α4 | 技能按条件进阶/退阶（24+ 条件分支） | skills |
| α5 | 替换普攻（替换技能 + 触发概率） | equipment |
| α6 | 替换职业（替换职业列表 / 等级重置 / 替换名称） | equipment |
| α7 | 技能附加的「对方」减益与持续制式（时间制/回合制、可叠加） | skills/effects |
| ~~α8~~ | ~~咏唱~~ | **用户明确不要** |
| β1 | 每技能战斗播报模板 | templates/skills（随批26 评估） |
| β4 | 技能复活标记（复活技能，非副本复活点） | skills |

### 批27 落地结论（α组技能侧 · 2026-09-17）
| 条目 | 落地 | 说明 |
|---|---|---|
| α4 | **既有机制已覆盖（形态替换）→ 缺口已补齐（条件集）** | 「同技按条件换形态」由 `skill_chains` 步骤 + `mode:"replace"`（`core/combo._resolve_derivation` 路径 c）等价表达：释放基础技前求 `condition`，命中即自动替换为进阶技。用法见 `tests/unit/test_batch27_alpha4_conditional_advance.py`。**批29（2026-09-17）补齐条件集缺口**：新增 `level`（`{eq\|min\|max}`，阈值 int ≥ 1）/ `job`（`{in\|not_in: [job_id…]}`，引用 jobs）/ `quest`（`{quest_id: {state: in_progress\|completed\|not_started}}`，引用 quest）三类，**沿用同一条件对象形态与同一求值入口**（`core/combo.evaluate_condition` + `ConditionCtx`），编辑器主体声明在 `content/field_meta.py::CHAIN_CONDITION_SUBJECTS`，红拦校验在 `content/skill_validator.py::validate_skill_chains`，端到端见 `tests/unit/test_batch29_level_job_quest_conditions.py`。仍存缺口：「退阶」用 `from` 侧条件取反表达；无「取消冷却/取消覆盖」 |
| α7 | **既有字段已可表达 → 不新增字段** | 对方属性减益走 `stat_modifier`（即时）或 status + `status_apply`（持续、到期自动回滚）；`duration.turns` = 回合制；`stack_frame:"dual"` 可叠加。用法见 `tests/unit/test_batch27_alpha7_enemy_debuff.py`。缺口：时间制（秒）在本 CTB 回合框架无对应物（模型差异）；战斗内属性为快照键，无基础/特殊/技能/熟练/物品分层 |
| β1 | **落地 `skills[].message_key`**（只存模板键，不存文案） | 渲染层按最终 skill_id 取键；编辑器下拉 = 全量模板表 key；未配置逐字段一致。边界：内容包不得新增表外模板键（既有门禁不放松），技能独有文案需框架表登记 |
| β4 | **落地 `skills[].revive`**（bool，缺省 false） | `core/battle.revive_side` 沿用既有 `revive` 事件链路（清死亡标记 + 恢复 HP + 去虚弱 + 回收胜负标记）。缺口：CTB `mark_dead` 无反向接口，只做战斗态复活、不回插调度队列；1v1 无双侧友军，落为「复活死亡态目标」 |

---

## 追加：批30 · 对账清账（7 项）· 2026-09-16（主 agent 依用户授权）

> 来源：用户 2026-09-16「按你建议」。范围 = 登记表 §B/§C 的 7 条未决项（U3 / U4 / U8 / X8 / X9 / X10 / X14）。
> 逐项动作与可核对证据见 §一 登记表对应行；本节只作索引与口径备注。

| 项 | 类型 | 结论 | 关键证据 |
|---|---|---|---|
| U3 · 文档 | 文档 | **已裁决-已失效（基线过期）** | `docs/细化/细化_2c2d_锻造套装与客制.md` 已存在 |
| U4 · 实测 | 实测 | **已裁决-已有（部分）** | per-action 收集器 `core/battle.py:1024`；聚合/展示/`dummy_log` 未实现 |
| U8 · 代码+文档 | 代码+文档 | **已裁决-已收敛（定稿键）** | `field_meta.py` 12 字段；`core/shop.py:536-549` 兼容读；回归 `test_batch30_shop_field_convergence.py` |
| X8 · 文档+实测 | 文档+实测 | **已裁决-实测无误** | 两键实测渲染 + 宽度 23/19 ≤ 28；规范 §三 |
| X9 · 代码+文档 | 代码+文档 | **已裁决-已删除** | 5b 契约 G13 删除（2026-09-16）；回归 `test_batch30_gm_edit_removed.py` |
| X10 · 文档 | 文档 | **已裁决-整档作废** | 5a/5a2/摸底已删；重写文档为唯一来源 |
| X14 · 文档 | 文档 | **已裁决-不清理（前提不成立）** | 定稿 L42 已有 `monster_def_rate` 定义（R-09 2026-08-18）+ 实现消费 |

**口径备注**：
- **U8 兼容映射**：读侧 = `limit` 缺省回落 `per_player`、`period` 缺省回落 `per_player_period`（`core/shop.py:536-549`），`scope` 生效侧由 `shop_models.ShopItemDef.effective_scope` 推断（显式 `scope` > 旧 `per_player` → personal > 缺省 global）；写侧编辑器只出定稿键。迁移影响面：现有 4 个内容包 `shop.json` **均无旧键**（实测遍历 items+pool），无需数据迁移。
- **X14 未执行「删除」的原因**：任务书前提（定稿全文无定义）在**本仓 383 行版不成立**——归档 375 行版才有此问题；删除已定义且已被引擎消费的因子属擅自改设计，故保留并在登记表写明。



---

## 追加：批72 · 重复机制收敛（审计3 §3）· 未完成项登记 · 2026-09-23

> 来源：`审计3_勿增实体_重复机制.md` §3 真重复清单 8 条 + `框架体检报告.md` §三 P1。
> 本批**已收敛** F1（`forge.json["settings"]` 废弃，唯一源 = `settings.json.forge` + 黄提示 Y-22）、
> F3（`core/upgrade._rune_tier_of` 委托唯一源 `data/runes.rune_tier_of`）；F2 经核实**已由批70 删净**。
> 下表为**按预算不动手**的其余真重复（风险大 / 需裁决 / 收益边际），登记待后续批，**不是被遗忘**。

| 项 | 类型 | 状态 | 关键证据与不做的原因 |
|---|---|---|---|
| **F4** 材料回收公式两写 | 代码 | 待收敛（同改时顺手） | `core/gem_wallet._recover_materials` vs `core/decompose.material_recovery`（逐材料 `floor(count×rate)`）。审计3 §6-R5 明示两路径（item_id / uid 实例）有意并存，**「不要为清理单独重构」**；抽出共同核需先覆盖 ctx/hooks 归一，属中风险 |
| **F5** 珠档位第二解析 | 代码 | 待收敛（需模式位） | `core/jewel.jewel_tier_of`（宽松：回退 `common` + Mapping/int-quality 分支）vs `core/upgrade._jewel_tier_of`（严格：非法 → `None` 数据直通）。**同一输入可给不同答案**，收敛必须给唯一源加「严格模式」参数（等于把第二解析搬进 jewel.py），且 `_as_int`/`_to_int` 对整值 float 口径不同 → 需逐位对拍，风险中 |
| **F6** `StatusInstance` 死表示 | 代码 | 待批73 死实体清理 | `data/status.py::StatusInstance` 生产零消费（仅 `data/__init__` re-export + `test_data` 构造），运行时真身 = `core/effects` dict 形态。**与 `scripts/check_architecture.py` TC-04 `REQUIRED_TYPES` 必需且 `dataclass(frozen)` 耦合**——删除需同步架构门禁口径，属批73 范畴 |
| **F7** `damage_dealt_pct` 死轴 + 假 consumer | 代码 | 待用户裁决（D1） | `data/gear_stats.py` 登记 `consumer="damage.total_damage"` 但 `core/` 零读取；别名 `weakness_dmg_pct` 桥入却端到端无人消费。**接线（新增弱点/终伤乘区，需数值评审）vs 撤登记+撤别名+撤 preset**，二者必选其一，属设计裁决 |
| **F8** RNG 适配器小重复 | 代码 | 待收敛（收益边际） | `core/enhance_affix._as_rng` / `core/deep_craft._rand_unit` / `commands/enhance_commands._roll_unit` 三处「注入随机源 → [0,1)」。审计3 §3-F8 自认「不构成机制级重复（都是薄函数）」；收敛需新增公共函数（**增实体**）并动三处 import，收益 < 风险 |


---

## 追加：X13 装备耐久 · 用户裁决（2026-09-23）

> **用户原话**：「等等，**装备耐久就不做了**。」
> **处置**：X13 由「待实测/待排期」→ **已裁决 · 不做**。
> **影响**：NPC `repair`（`docs/能力缺漏审计_四方对照.md:102` C10 / NPC 定稿:139）**一并搁置**——
> 它依赖的正是装备耐久系统；**策划书自承的"降级"即最终口径**。
> **给后人的提示**：**这不是缺口，是决定**；不要再把它当 bug/缺漏重提（《框架扩展开发手册》§四「勿当 bug 修」的精神同样适用）。

## 追加：批78 · U4 伤害构成统计 + X3 slots 归口提示（2026-09-24）

> 来源：本批任务书（U4/X3）；动作 = U4 聚合/展示/dummy_log 全部实装，X3 目录条目定位纠正。
> 逐项证据见 §一 登记表 U4 / X3 行；本批新登记口径差异 = §C U11（enabled 关是否停收集）/ U12（realtime「本回合」口径 + 普通战斗明细开关）。

| 项 | 类型 | 结论 | 关键落点 |
|---|---|---|---|
| U4 | 代码+展示+存档 | **已实现-批78** | `core/damage_stats.py`；`core/battle.py:1085-1110`；`commands/battle_commands.py`（summary/dummy_log）；`commands/dummy_commands.py`（/木桩 记录）；回归 `test_batch78_u4_stats.py` 18 passed |
| X3 | 文档+目录 | **已裁决-已纠正（批78）** | `content/module_catalog.py:87-101`（label=珠插槽 + `overlap_note`）；黄提示机制 `web/api.py:790-813` 不变；回归 `test_batch78_x3_slots_hint.py` 5 passed |

## 追加：批79 · X18 销项 · 职业继承 `mode`（替换/追加）（2026-09-23）

> 来源：用户 2026-09-23 对 `docs/进阶职业继承_设计口径.md` §5 四问 Q1~Q4 的拍板；动作 = Q3 `mode` 新增实现 + Q1/Q2/Q4 保持并回填文档。
> 逐项证据见 §一 登记表 X18 行；实现口径 `docs/职业继承mode批79_实现口径.md`。

| 维度 | 用户裁决 | 本批动作 | 关键落点 |
|---|---|---|---|
| ① 继承粒度 | 保持**传递闭包**（A→B→C 时 C 继承 B 与 A） | **不改** | `core/job_slots.py` `resolve_inherit_chain` |
| ② `skills` 白名单 | 保持（**空=全部**；非空=只列出的） | **不改** | `core/job_slots.py` `_inherit_whitelist` |
| ③ `mode` | **新增**：`append`（默认=现状）/ `replace`（按 `replace` 映射替换继承技能） | **本批实现** | 引擎 `_apply_inherit_replace`；校验 V9（mode 未知 Y-23 / replace 悬空 R-4）；字段 `field_meta.py` + `job_models.py`；回归 `test_batch79_inherit_mode.py` 17 passed |
| ④ 技能等级继承 | **不做**（技能无等级维度；另立设计） | **不做**（文档注明） | `docs/进阶职业继承_设计口径.md` §5 Q4 |

> **零行为变化**：不带 `inherit` 及 `mode=append`/未配置 → 装配结果与批35 **逐字段一致**（测试内对拍断言）。
> **X18 关闭**：§B 待用户裁决清零。

## 追加：批82 · 手册剩余项收口 · 新登记 NEW-1~9（2026-09-23）

> 来源：`/root/deliverables/手册剩余项_核清与口径.md`（《框架扩展开发手册》§六剩余项逐条核清）。
> 本批落地 D1~D4（用户 4 条裁决）+ B2/C1（可直接执行项）；下表为本轮**新发现**（只登记，除
> NEW-1 已按用户裁决 D4 落地外不动手）。分类沿用《手册·3》§3.7.2：能给「文档铁律/代码注释/
> CHANGELOG 裁决」判据的才算"勿修"；此处列新查出、无判据的缺口或文档错误。
> **另**：**N2/N3**（`/装备` 丢 `effect_refs`、两链路丢 `stack_max`）为**手册已登记**的疑似缺口，
> 本轮 Q4 核实「**未被任何裁决认定为可接受**」→ **按真缺口处理**，排收敛批（不重复列入下表）。

| # | 事项 | 分类 | 证据 `file:line` | 状态 / 处置 |
|---|---|---|---|---|
| **NEW-1** | **`proficiency.json[].energy` 登记 + PRF-07 校验，但运行时零消费**（契约明文承诺的"prof 兜底"从未实现） | **真缺口（契约→实现未接线）** | 登记 `content/field_meta.py:3060-3064`；校验 `content/alchemy_models.py:1215-1228`；契约 `docs/m8_contract_数据与校验.md:131`/`:201`；实现原只读 settings `core/energy_bar.py` | ✅ **已由批82 · D4 落地**（用户 2026-09-23 裁决 A）：`energy_bar` 增 prof 注入、settings 缺键回落 `prof.energy.enabled`；`max_by_tier`/`regen_sec` 标 deprecated |
| **NEW-2** | `core/jewel.py:41` 的 ctx 键来源记错 | 文档错（注释） | `qbot_rpg/core/jewel.py:41` 写「ctx["slots"] 被『部位定义 slots.json』占用」；实际 `ctx["slots"]` 来自 `settings.slot_defs`（`assembly/context.py`），`ctx["slot_defs"]` 才来自 slots.json；同文件 `:365` 正确 | ⏳ **未修**（登记）：建议改 `:41` 为「被装配层**装备部位定义**（settings.slot_defs）占用」 |
| **NEW-3** | **PVP 模式枚举两套名** | 文档错 | 实现/4e = `turn_based`/`free`（`core/pvp.py:38`、`content/field_meta.py`、`细化_4e_PVP决斗契约.md:148`）；`docs/细化/细化_3h_settings通用设置.md:206` = `turn`/`realtime` | ✅ **已更正（批82 · D1）**：3h 示例统一为 `turn_based`/`free` |
| **NEW-4** | 审计/手册引用路径悬空 | 文档错（**已在册，非新发现**） | `docs/审查报告/审查_M8实现_批次E2_jspace.md` 不存在，真实文件在仓库根；**已在《手册·3》§6.2 偏差总表登记** | 无需再登记；仅需按已登记建议改引路径 |
| **NEW-5** | `_KIND_FOR_MODULE` 项数文档偏差 | 文档错 | 实测 **28** 项（`content/loader.py:146-198`）；若写 29 则错 | ✅ **已更正（批82 · Q7）**：手册/盘点更正为 28 |
| **NEW-6** | **6 个"能力标记"目录条目无 loader kind、无 field_meta 校验器** | **待查**（可能是设计，可能是缺口） | `assistant/codex/contest/farming/gathering/quest_board`：catalog 有条目 + `implemented=True` + `settings_section` 非空；`_KIND_FOR_MODULE` 无、field_meta 无 → 实测 `check_manifest_modules_registered` 报出这 6 个 | ⏳ **待查**：读 `web/api.py` 勾选/骨架创建路径，确认是否会写入 `manifest.modules`。若会 → 真缺口；若否 → 补注「仅 settings 能力标记」（**未查清，不猜**） |
| **NEW-7** | `templates` 是 `map` 形态但不进注册表 | 口径差（低） | `loader.py:113` 仅 `("stats","formula")` 走 map 键=ID 注册；`templates` 同为 map 但不注册 → 由 `context._templates_table` 走 `modules_raw` | ⏳ 待确认是否有意（登记；《手册·1》§2.6.3 补注） |
| **NEW-8** | `deep_craft` 草案 `floor_ratio` 旧值 | 文档偏差（低） | 草案 `docs/审查/审计与方案/打造_材料与图纸内容草案.md:198` 写 `floor_ratio:0.25`；实现默认/`zz_craft_demo` = `0.0`（`content/deep_craft_settings.py:123`） | ⏳ 以代码为准（非实现 bug）；草案加"旧值"注 |
| **NEW-9** | **`ctx` 3 键框架内无消费方** | 结构性缺口（登记） | `monster_pool`（`assembly/context.py:1644`）、`shop_engine`（`:1322` 恒 `None`）、`worn_refs`（`:1423`/`:1608`）；全仓无生产读点（Q2） | ⏳ 标"无消费方/准死键"，**待收敛批**处置（不单删——需先确认无包 ext 读取） |

> **计数说明**：NEW-1~9 为**新命名空间**（不与 §一 U/X 编号混计）；其中 NEW-1 已落地、NEW-3/NEW-5
> 已随本批文档更正、NEW-4 属重申已在册项；剩余 NEW-2/6/7/8/9 为待修/待查/待收敛。§二 U/X 计数不变。
