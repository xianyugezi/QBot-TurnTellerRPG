# M11 4d 图鉴聚合 · 摸底侦察报告（docs/m11_图鉴摸底.md 内容）

> 侦察范围：细化_4d 契约（443 行全读）+ codex.py（343）/ codex_milestones.py（375）/ codex_commands.py（178）/ fishing_codex.py（334）/ codex_tpl.py（44）/ environment_lore.py（520）/ condition_engine.py（var:codex 段）/ loader.py（_KIND_FOR_MODULE）+ 现行 6 个测试文件。全部行号经真实 read 验证。

## 一、细化_4d 契约摘要（v1.0 · 443 行）

### 1.1 四册定义（§1，L77-152）
| 册 | param 键 | 条目来源 | 点亮结算点 | 依据 |
|---|---|---|---|---|
| 怪物 | `monster` | enemies.json 非木桩（tier=training/type=dummy 排除，COD-05 L193） | 首杀 | 怪物 L301/L351；D-02 L66 |
| 鱼类 | `fish` | fishing.json species[] + king 鱼王条目 | 首获/鱼王首胜 | 鱼细化 L62；钓鱼 L21/L22 |
| 物品 | `item` | items.json **无制造路径**物品（D-04 L68） | 首次获得（入包结算） | D-04；L83-84 |
| 制造 | `craft` | forge 树节点产物（装备）+ 炼金 recipe 产物（珠/料理）（D-04） | 首次制作成功 | D-04；L111-113 |

- 隐藏要素归册（D-02 L66）：隐藏 BOSS→monster、鱼王→fish、彩蛋物品→item、隐藏配方→craft；未发现时占位「？？？」计入分母（L124）。
- 台账 = 注册表静态盘点，分子=与台账交集（COD-01 L189），只增不减（COD-02 L190），比率可回落（COD-03 L191）。

### 1.2 加权完成度（§2，L156-206）
- `V_i = U_i/T_i`；总册 `T = Σ(w_i·V_i)/Σw_i`，默认等权 1:1:1:1（L173-178）；权重配置 `codex.weights` 四键必填≥0 且不全 0（L162-167）。
- 求值链：var:codex 缺省=总册 T，param=monster/fish/item/craft=分册 V_i（L183）；实时聚合不落库（D-03 L67）。
- COD-08（L196）：展示四舍五入取整百分比，**里程碑判定用未取整精确值**。
- 求值失败默认不满足（COD-07 L195）。

### 1.3 里程碑阶梯（§3，L210-256）
- 五档 25/50/75/90/100（L214）；跨档跳升逐档授予（D-06 L70）；90% 档 desc 软锚「全收集还有更深处」（L221-222）；分册称号怪物博士/鱼类大师/物品收藏家/制造行家 + 总册「收藏家」（L229-233）；100% 三件套=收藏家称号+世界之书+隐藏神龛（L240-243）。

### 1.4 展示契约（§4，L260-319）
- `/图鉴` 总览（四册进度+总完成度+下一档提示）；`/图鉴 怪物｜鱼｜物品｜制造品` 分册 5 条/页；`/图鉴 <条目名或序号>` 详情；`/图鉴 鱼 <条目名>` 冠级别名（L266-269）。
- 未解锁条目「？？？」占位不渲染名称（D-07 L71）；隐藏要素额外不渲染条件线索；不提供 /秘密（L319）。
- 鱼条目冠级标注：best_crown 链条取最大+逆金冠×N 独立标注，不写判定公式（L294-299）。

### 1.5 lore 情报解锁（§5，L323-357）
- 结构 `lore: [{unlock 1-100 递增, desc}]`（L327-328）；**判据=玩家总册完成度 T（未取整）≥ unlock**（D-05 L69，L337-344）；隐藏要素发现后 lore 全集一次性解锁（L356）；情报行不参与完成度计算（L345）。

### 1.6 25 TC 覆盖矩阵（§6，L361-422）
①四册定义 TC-01~05（L378-382）②加权规则 TC-06~10（L388-392）③里程碑 TC-11~15（L398-402）④展示 TC-16~20（L408-412）⑤情报解锁 TC-21~25（L418-422）。

## 二、现状 vs 契约差距表

| # | 契约要求 | 现状（已验证） | 差距 | 影响 TC |
|---|---|---|---|---|
| G-1 | 四册 monster/fish/item/craft | codex.py CATEGORIES 五分类 `monster/weapon/item/alchemy/fish`（L42-56）+ CATEGORY_ORDER 五册（L60） | **无 craft 册**；weapon 册存在（映射 equipment kind + items type=weapon 补白，L123-152）；alchemy 是炼金特判分册（registry kinds=("recipe","item") L49）非 craft 册 | TC-01~05 |
| G-2 | 锻造产物→craft 册 | forge_commands.py L1075-1076：首锻 `mark_seen(ctx,"weapon",…)+mark_seen(ctx,"item",…)` 双登记 | 按 D-04 铁剑=forge 树节点产物应归 craft 册；现状双写 weapon+item 造成**①武器册与物品册重复计数 ②craft 册零分子** | TC-03/TC-04 |
| G-3 | 炼金 recipe 产物（珠/料理）→craft 册 | 炼金无 mark_seen 接线；alchemy_commands 走 AlchemyMeta.codex_summary 独立渲染（render_alchemy_codex L2669，/图鉴 炼金 特判 codex_commands L139-141），registry kind="recipe" 在 alchemy 分册 | 炼金产物未入 craft 册；alchemy 分册作为独立展示册与 4d 四册冲突 | TC-03 |
| G-4 | 鱼获 21 条（species20+king）| fishing_codex.py fish_codex_update 七键入册+首获点亮（L181-285）+ king_victory_count 综述（L106/L131-143）+ render_fish_codex 特判渲染（fishing_codex_commands L119-155） | **已实现**（M10）；但 fish 册无 registry 分母→通用 codex_progress(ctx,"fish") total=0 fail-safe（codex.py L149-152 补白），总览 fish 行恒 0% | TC-02/TC-09 |
| G-5 | 加权完成度 w 可配默认等权 | codex_progress 全局=**5 册等权均值**（codex.py L184-194 按 CATEGORY_ORDER 长度均分） | 无 weights 配置读取；分母被 alchemy/fish 两特判册稀释（均 0%）；无 param 分册条件键 | TC-06~09 |
| G-6 | 条件键 param 分册 | condition_engine.py L531-532：`var=="codex"` 读 `ctx["codex"]` 标量，**忽略 param**；VAR_ALIASES [图鉴完成度] L159 无 param | param=monster/fish/item/craft 分册求值未实现；分册称号成就条件无法求值 | TC-09 |
| G-7 | 木桩不入图鉴（COD-05）| codex.py 补白 1 注明 tier=training/type=dummy 不计入分母「由调用方不 mark 或本引擎登记补白」；validator.is_dummy_enemy 存在（validator.py L267） | 木桩排除逻辑**未落 codex 引擎代码**（无显式过滤），靠业务方不 mark——契约要求引擎侧硬保证 | TC-01/TC-05 |
| G-8 | monster 册首杀点亮 | battle_commands.py L722-736 仅 log_first_kill（冒险日志），**无 mark_seen(ctx,"monster",…)**；全仓 grep mark_seen 调用方仅 forge_commands/fishing_codex/forge_king | monster 册点亮缺口（首杀不入 codex_state）→ 完成度恒低 | TC-01/TC-05/TC-21 |
| G-9 | item 册首次获得点亮 | 全仓 grep：reward/inventory/checkin/shop/quest 均无 mark_seen 接线；仅 forge 双登记时顺带点亮 item | 材料/消耗品/任务物品/唯一物品首获点亮缺失（含彩蛋物品） | TC-03/TC-04 |
| G-10 | lore 情报按总册完成度阈值解锁（D-05）| environment_lore.py `_base_visible_lines` L287-304 已实现「完成度≥unlock 才显示基础段」；lore_view L439-468 按 lore_unlocked 返回；unlock_lore_wired L474-520 写 lore_unlocked | **读路径已具备 D-05 语义**；但 codex.py `unlock_lore(ctx,category,ref_id)`（L261-273）是**单条目布尔解锁**（无行级 unlock 索引/行数状态），未按条目 lore 行数逐行存（契约要求 unlocked_lore 行状态 L329/L93）；且解锁推进挂在 hook 未接线 | TC-21~24 |
| G-11 | 里程碑阶梯（25/50/75/90/100+软锚+三件套）| codex_milestones.py 全部已实现（MILESTONE_DEFS L78-89、90 软锚 L86、100 三件套 L311-334、幂等 L141-155） | **核心已实现**；但全仓 grep `check_milestones(` 调用方为空（无结算点接线）；且称号名「收藏新手/收藏家/资深收藏家/收藏大师」与契约分册称号（怪物博士等）不同维度——契约分册称号成就依赖 param 条件键（G-6 未实现）| TC-11~15 |
| G-12 | 总览展示：四册进度条+下一档+分册称号 | codex_commands `_overview` L67-83：仅五行进度（CATEGORY_ORDER 顺序）+用 round() 取整（L49/L80）——已符合 COD-08 展示取整 | 无进度条 ▓/░、无「下一档里程碑」提示、无分册称号行、鱼册恒 0%、无「怪物博士/鱼类大师」等目标行 | TC-16/TC-17 |
| G-13 | 未解锁「？？？」占位 | codex_view L291-343 已实现（name=??? L321）；codex_commands L106-111 渲染 ✅/??? | **已实现**（含 5 条/页 L327/分页夹取 L330）| TC-17/TC-19 |
| G-14 | 鱼冠级标注不写公式 | render_fish_entry_line（fishing_codex.py L291-320）best_crown 链+逆金冠×N；render_fish_codex 不含判定数字 | **已实现** | TC-18 |
| G-15 | item/craft 数据驱动归属判定（D-04）| 无归属判定函数；loader.py _KIND_FOR_MODULE 已登记 items→item/equipment→equipment/recipe→recipe/forge→forge（L150-183）——**判定所需数据源齐备**（items kind 表 + forge.nodes()/recipe 表）| 需新增「能否解析到制造源」判定器（forge 节点 item 引用 ∪ recipe 产物 → craft，否则 item）| TC-03/TC-04 |
| G-16 | 权重配置校验（四键≥0 不全 0）| 无 | 缺校验器 | TC-07 |
| G-17 | 里程碑判定用未取整精确值 | codex_milestones._read_pct L203-230 读 ctx["codex"] 标量后 `int(float(raw))` 夹取（L227-230）——**45.6% 会被 int() 截断为 45** | **违反 COD-08 精确值比较**（截断而非取整，49.6%→49 判定准确但浮点边界语义需复核）| TC-06/TC-16 |

## 三、关键接口签名（真实 read 核对）

### qbot_rpg/core/codex.py
```python
CATEGORIES: Mapping[str, Tuple[str,...]] = {monster:(enemy,), weapon:(equipment,),
    item:(item,), alchemy:(recipe,item), fish:(fish,)}          # L42-56
CATEGORY_ORDER = ("monster","weapon","item","alchemy","fish")   # L60

def codex_progress(ctx: MutableMapping[str, Any],
                   category: Optional[str] = None) -> dict
    # → {total, seen, killed, pct(float 未取整 0-100)}；category=None=全局 5 册等权均值（L184-194）
    # pct=seen/total×100；total=0 → pct=0

def mark_seen(ctx: MutableMapping[str, Any], category: str, ref_id: str,
              name: str, *, killed: bool = False) -> dict
    # → {ok, first_seen, category, ref_id}；首见写 state{name,seen,killed,lore_unlocked}
    #   + log_codex_new + bump_event + 刷新 ctx["codex"] 投影（L197-243）
    # ⚠ 整条目覆盖陷阱：cat[rid]={4键} 会冲掉 caught_count/best_crown 等扩展键（L221-229）

def mark_killed(ctx: MutableMapping[str, Any], category: str, ref_id: str) -> dict
    # → {ok, category, ref_id}；未 seen 拒绝（L246-258）

def unlock_lore(ctx: MutableMapping[str, Any], category: str, ref_id: str) -> dict
    # → {ok, category, ref_id}；条目级布尔 lore_unlocked=true（非行级，L261-273）

def codex_view(ctx: Mapping[str, Any], category: str, page: int = 1) -> dict
    # → {entries[{ref_id,name,seen,killed,lore_unlocked}], order, page, pages,
    #     total, page_size=5, category, label, ok}
    # 未见条目 name="???"（L291-343）
```

### qbot_rpg/core/codex_milestones.py（M7 已实现，M11 复核）
```python
MILESTONE_DEFS: tuple = (25/50/75/90/100 五档+称号+message)      # L78-89
MILESTONE_PCTS = (25,50,75,90,100)                               # L91
COLLECTOR_TITLE = "收藏家"                                        # L94
CODE_X_MILESTONES_KEY = "codex_milestones"  # persistent_state 幂等集合  # L97
HIDDEN_SHRINE_KEY = "hidden_shrine"                               # L100

def check_milestones(ctx: MutableMapping[str, Any], *,
                     today: Optional[str] = None) -> dict
    # → {granted:[{milestone,pct,tier,message,title_written,today}], message}
    # 100 档附带 world_book/hidden_shrine（L337-375）
    # ⚠ 全仓无调用方（结算点未接线）；_read_pct int() 截断（L227-230）
```

### qbot_rpg/commands/codex_commands.py
```python
_CAT_ALIASES = {怪物/武器/物品/炼金/鱼 → monster/weapon/item/alchemy/fish}  # L34-42
def _overview(ctx) -> str   # 五行进度+总完成度，round() 取整（L67-83）
def cmd_codex(parsed, ctx) -> str  # 无参=总览；alchemy→render_alchemy_codex(L139-141)
                                   # fish→render_fish_codex(L143-145) 特判
def register_codex_commands(router, *, make_context=None) -> router  # L155-178
```

### qbot_rpg/core/fishing_codex.py（M10 已实现）
```python
CODEX_FISH_KEYS = (caught_count, best_crown, best_size, best_weight,
                   min_size, min_weight, reverse_crown_count)      # L87-95
CROWN_PRIORITY = (big_gold, gold, big_silver, silver, normal)      # L99
KING_VICTORY_COUNT_KEY = "king_victory_count"  # __meta__ 综述（L102-106）
def fish_codex_update(ctx, species_id, catch, *, name=None) -> dict
    # → {ok, first_seen, species_id, caught_count, best_crown, reverse_crown_count}
    # 七键更新+首获点亮+防整条目覆盖（dict(existing) update，C-6 L255-267）
def render_fish_entry_line(name, best_size, best_weight, best_crown,
                           reverse_crown_count, *, lv=0) -> str    # L291-320
```

### 条件引擎现状（G-6）
```python
# condition_engine.py L531-532：var=="codex" 读 ctx["codex"] 标量，param 忽略
if var == "codex":
    return _num(_ctx_get(ctx, "codex"))
# L132 VAR_CATEGORIES 状态类含 codex；L159 别名 [图鉴完成度]=(codex, None)
```

### 邻接系统（M11 复用）
- loader.py `_KIND_FOR_MODULE`（L150-183）：items→item / equipment→equipment / recipe→recipe / forge→forge / fishing→fish / enemies→enemy —— craft 归属判定数据源齐备。
- forge_tree.py `nodes()`/`load_trees()`（L203-211）可枚举 forge 产物 item 引用 → craft 分母。
- environment_lore.py：`lore_hint`（L408）、`lore_view`（L439）、`unlock_lore_wired`（L474）——D-05 读路径已具雏形。

## 四、25 TC 逐条承载建议

| TC | 场景 | 承载位置（新建或扩展） | 备注 |
|---|---|---|---|
| TC-01 | monster 册盘点 29（木桩 3 不计）| **2A** tests/unit/test_codex.py 扩展 `_FakeRegistry` 加 dummy→ 断言 total 排除 | 需引擎侧木桩过滤（G-7） |
| TC-02 | fish 册 21 条盘点+首获 | 已实现（test_fishing_settle.py）→ 2C 复核 fish 册分母接入通用 progress | 依赖 G-5 fish 分母修复 |
| TC-03 | item/craft 归属判定（匕首/火伤药→craft；矿铁/世界之书→item）| **2A** 新 tests/unit/test_codex_item_craft.py：归属判定器单测 | 核心新功能 |
| TC-04 | 隐藏要素四类归册 | **2A** 同上扩（蚀月之狼/金冠鲤/旧怀表/龙涎饵→四册）| 占位「？？？」已有（codex_view） |
| TC-05 | 木桩击杀不变 | **2A** test_codex.py 扩：mark_seen 木桩被拒/分母不含 | 需引擎侧拦截 |
| TC-06 | 默认等权 12/29,3/20,0/15,0/10→T=14.095%→14% | **2B** test_codex.py `test_progress_single_and_global` **改写**：4 册+等权 | ⚠ 变更现有 5 册均值断言（见 §五） |
| TC-07 | 自定义权重 {2,1,1,0}→24.44% | **2B** 新 test_codex_weights.py | 权重配置读取+校验（G-16） |
| TC-08 | 热重载分母 29→32→回落 37.5% | **2B** 新：registry 热重载后 progress 重算 | COD-01/03 |
| TC-09 | param 分册条件键（fish 21/21→鱼类大师）| **2B** condition_engine `var=="codex"` 分支加 param 分发 + test_condition_engine.py | G-6 核心 |
| TC-10 | 求值失败降级（悬空 ID 交集）| **2B** test_codex.py 扩（unlocked 含未注册 ID→不计数）| 交集口径 |
| TC-11 | 22%→48% 只授 25 档 | **已有** test_codex_milestones.py L152-173（跨档语义）→ 复核精确值比较 | G-17 复核 _read_pct |
| TC-12 | 54%→96% 逐档+90 软锚 | **已有** L179-189 + 扩 96 场景 | — |
| TC-13 | 100% 三件套 | **已有** L207-263 | — |
| TC-14 | 分册 100% 互不阻塞 | **2B** 新：param 条件键+分册称号成就（依赖 G-6）| 分册称号 4 个新成就 |
| TC-15 | 作者删 75% 档热重载 | DELAYED→成就系统（4c 批1 承接，achievements.json 阶梯模板）；M11 复核 | — |
| TC-16 | /图鉴 总览 14%+进度条+下一档 | **2C** test_codex_commands.py 扩：总览新渲染断言 | G-12 展示重写 |
| TC-17 | 分册 5 条/页+??? 占位 | **已有** test_codex.py L198-224 + test_codex_commands.py L88-97 → 扩 craft 分册别名 | — |
| TC-18 | 鱼条目冠级标注 | **已有** test_fishing（render_fish_entry_line 断言）| 复核无 5%/85%/95% |
| TC-19 | 未解锁隐藏条目零泄露+无 /秘密 | **已有**??? 占位（codex.py L321）+ 新断言：/图鉴 无隐藏名称 | — |
| TC-20 | 点亮回显：普通并入结算+隐藏⛩️一次性 | **2C** 新：mark_seen 回显渲染 + 隐藏条目 ⛩️ 卡（依赖事件标记）| D-07 关联 |
| TC-21 | lore unlock 10/50/100 按完成度解锁 | **2C** environment_lore 已有 `_base_visible_lines` L287-304 阈值过滤 → 补行级解锁状态 | G-10 核心 |
| TC-22 | 50%→52% 推进行 2 解锁行 3 占位 | **2C** 同上扩展 | — |
| TC-23 | 情报内容三类型（弱点/机制/掉落）| 数据层（内容包 lore desc）→ **2C** 展示断言 | — |
| TC-24 | 隐藏 BOSS 击败→lore 全集解锁 | **2C** unlock_lore_wired 已有（L474-520）→ 接线隐藏触发点 | — |
| TC-25 | 校验器 lore 非递增/越界/木桩黄提示 | **2C**（或收口）validator 扩 R6/R7（怪物细化 L205-206）| 校验器侧 |

## 五、硬计数断言风险清单（动 CATEGORY_ORDER/全局均值前必同步）

M10 批4 教训（记录.md L66）：加 codex 分册破坏全局均值断言。本次 5 册→4 册收敛**再次触发同类风险**，已 grep 确认的破坏点：

| 文件:行 | 断言 | 收敛后影响 |
|---|---|---|
| tests/unit/test_codex.py L114 | `abs(gp["pct"] - (100/3+0+50+0+0)/5) < 0.01` | **5 册均值** → 四册(或含 fish 有效分母)需改 |
| tests/unit/test_codex.py L192 | `abs(float(ctx["codex"]) - (100/3+0+0+0+0)/5) < 0.01` | **5 册投影** → 改 |
| tests/unit/test_codex.py L230-234 | `test_categories_contract`：CATEGORIES["monster"]==("enemy",) / ["weapon"]==("equipment",) / ["item"]==("item",) | weapon 键删除/改 craft 时**必须改** |
| tests/unit/test_codex.py L151-160 | `test_weapon_progress_total_from_items_type_weapon`：weapon total==2（equipment ∪ items type=weapon）| weapon 册并入 craft 后该测试语义消失→改写为 craft 归属判定测试 |
| tests/unit/test_codex.py L163-175 | `test_weapon_codex_view_lists_type_weapon_items` | 同上 |
| tests/unit/test_forge_commands.py L606-620 | 首锻断言 `codex_state["weapon"]["iron_sword"].seen` + `codex_state["item"]["iron_sword"].seen` | **craft 册收敛后铁剑应进 craft**——此断言必随 forge_commands L1075-1076 双登记改造同步 |
| tests/unit/test_codex_commands.py L77-85 | `test_codex_overview`：断言「怪物图鉴」「总完成度」存在 | 总览重写（G-12）后若模板 key 变化需同步；断言较宽松可能幸存 |
| tests/unit/test_codex_commands.py L88-97 | `test_codex_category_page_seen_and_hidden`：「???」占位+「木狼」不泄露 | 分册别名加「制造品」需扩 |
| tests/unit/test_codex_milestones.py L313-319 | `MILESTONE_PCTS==(25,50,75,90,100)` 等公开常量 | 里程碑本体不动则安全 |

**必查动作**：批2 任何一路动 CATEGORY_ORDER 前，`grep -rn "CATEGORY_ORDER\|/ 5\|/5)" tests/ | grep -v test_codex_milestones`；另全仓 grep `"weapon"` 在 tests/ 的引用（test_spawn/test_deep_commands 各 1 处涉 codex 关键字需复核上下文）。

## 六、派工建议（批2 三路切分，对齐 m11_batch_plan.md L25-29）

三路依赖关系为 2A（归属）→ 2B（分母/权重）→ 2C（展示/lore），建议按「数据→计算→展示」串行接力但**三路文件隔离、并行安全**：

- **路 2A（craft 册收敛+归属，~25% 工作量）**：codex.py 新增归属判定器（items kind 表 + forge nodes()/recipe 产物 → craft，否则 item）；CATEGORIES 改四册（monster/fish/item/craft，**删 weapon/alchemy 键**或保留兼容映射——建议删干净走 4d D-01，hard-cut 同步 §五 测试）；木桩过滤落引擎（validator is_dummy_enemy 复用）；改 forge_commands L1075-1076 双登记→mark_seen(ctx,"craft",…)；对应测试改写（test_codex.py 契约段 test_forge_commands L606 段）。**此路最先，硬计数破坏面最大，建议先做先同步测试**。
- **路 2B（加权+条件键，~30%）**：codex_progress 全局改 4 册加权（含 weights 配置读取 settings.codex.weights）；condition_engine var:"codex" 加 param 分发（monster/fish/item/craft → V_i，总册缺省）；COD-08 精确值复核（_read_pct int 截断修复为 float 比较）；新 test_codex_weights.py + test_condition_engine param 用例；**同步改写 test_codex.py L114/L192 两个 5 册均值断言**（此二断言被 2A 与 2B 双重影响，建议 2B 收口时统一改）。
- **路 2C（lore+展示，~30%）**：lore 行级解锁状态（codex_state 怪物条目解锁行索引，按总册完成度 T 未取整 ≥ unlock 逐行解锁，接结算点 hook）；/图鉴 总览重写（四册进度条+下一档提示+分册称号行，模板走 codex_tpl 新 key）；怪物条目详情页（lore 行渲染：已解锁 desc/未解锁「？？？（图鉴进度 N% 解锁）」）；monster 册首杀 mark_seen 接线（battle_commands L722-736 补）+ item 首获接线；隐藏要素⛩️揭示回显。**此路依赖 2A 分母与 2B 加权正确**，可并行开发但联调在 2A/2B 落地后。

**风险提示给主 agent**：①check_milestones 目前全仓零调用方——无论批2 哪路，须确认里程碑检查挂到图鉴点亮结算点（契约 L149-150 ① 点亮→② 日志→③ 完成度重判→里程碑→情报），否则阶梯成就死代码；②4c 成就系统（批1）与 4d 分册称号（2B param 条件键）交叉依赖——成就里程碑条件 `{var:codex, ge, 100, param:"monster"}` 需 2B 先落地条件键，建议批1 与 2B 对齐条件引擎契约。

---

## 最终总结

**完成内容**：精读细化_4d 契约全 443 行 + 7 个实现文件 + 5 个测试文件 + 条件引擎/loader 相关段，完成全部六项交付的侦察汇编（契约摘要/现状差距表 17 项/接口签名/25 TC 承载/9 个硬计数断言风险/批2 派工建议）。所有行号引用均在真实 read 验证的范围内，未编造。

**关键发现**：
- 最大差距集中在三处：**无 craft 册**（锻造产物现双写 weapon+item，forge_commands L1075-1076）；**全局完成度是 5 册等权均值**（alchemy/fish 两个特判册稀释，contract 要求 4 册加权）；**条件引擎 var:codex 忽略 param**（分册条件键不可用，condition_engine L531-532）。
- monster 册首杀点亮、item 册首获点亮两处结算点接线缺失；check_milestones 全仓零调用方。
- 已实现可复用：??? 占位、5 条/页分页、鱼册七键+冠级渲染、里程碑五档+90 软锚+100 三件套、environment_lore 的完成度阈值读路径。

**交付物**：本文档正文即 docs/m11_图鉴摸底.md 全部内容（父 agent 落盘即可，不 commit）。

**未竟事项**：因迭代上限，未能执行文件写入操作——docs/m11_图鉴摸底.md 未落盘。父 agent 可基于上方正文直接创建文件，或让本子 agent 在下一轮继续写入并校验。
