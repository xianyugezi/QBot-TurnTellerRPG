# M13 批0 路0C · 6c 资源轴与职业机制摸底报告

> 依据：docs/细化/细化_6c_资源轴与职业机制.md（497 行，v1.0）
> 版本：v0.1 · M13 批0 路0C 摸底 · 不改码
> 现状核对：stats / effects / marks / condition_engine / combo / battle 快照与回合结算 / validator / field_meta / worldtime（全部行号经 grep/read 验证在文件范围内）

## 〇、结论摘要
- 已就绪：条件引擎 [季节:X] 互译键、季节引擎全链路（worldtime）、施放前被拒管道（combo.should_reject）、proc 容器与触发上限、L0 词汇执行器全家、印记层数载体、校验器框架、快照自定义键先例。
- 核心缺口：M1 注册段 10 字段缺 5、M2 energy_gain/cost 零实现、M3 回合结清无资源槽、M4 resource_state 快照零、M5 季节技能组无载体（skills 库不存在）、M6 换季边界无接线、M7 事件枚举无登记、M8 combo_table 零实现；V1~V11 全缺；TC-01~20 基本不可执行。

## 一、已就绪资产（可复用/直接承接）
1. **条件引擎 [季节:X] 互译键**：condition_engine.py L164 VAR_ALIASES 含 "[季节:{T}]"→("season","{T}")、L136 REGISTERED_VARS 时间类含 season；L459-471 _season_now 读 ctx.season_now/worldtime.season_now——M6② 消费点2（var=season eq param）与 TC-13② 已可求值。
2. **季节引擎全链路**：worldtime.py L91 SEASONS 四季恒定枚举、L227 season_now、L244 check_changes（IF09 缓存索引对比=换季检测现成）、L267 maybe_broadcast（_SEASON_CN 中文名/文案渲染先例，战斗外换季文案可复用）→ F-R2①懒重读/②检测差异/⑤文案有基座。
3. **施放前被拒管道**：combo.py L856 should_reject（MP/冷却/条件，enforce_mp 默认 False）→ battle.py L1215-1229 apply_action rejected 短路「不耗回合、回滚 _turn_acted、连段不变、可反复尝试」——M3 F-R1 施放前门禁的管道现成，energy_cost 可挂同点。
4. **proc 容器与触发上限**：effects.py L1618 execute_proc_action 接受任意 trigger 字符串（L1655 无白名单）、L83-84 config max_triggers_per_turn=10/max_triggers_per_battle=99 且 L1642-1647 消费——M7 E3 上限字段就绪、on_season_change 事件的 proc 消费侧无枚举拦截。
5. **L0 词汇**：effects.py L1392 execute_action 含 damage/heal/dot/control/dispel/shield/stat_modifier/mark_add/mark_remove/clear_marks/aoe/proc、L1429 mitigation、L1454 heal（%max 公式经 _resolve_value）、L1503 control、L1250-1257 tick_turn_end regen——combo_table 行 effects 引用的行为类别（燃烧/护盾/减伤/回复/再生/混乱）全部有执行器。
6. **印记层数载体**：marks.py MarksManager（mark_add/remove/clear/evaluate/tick_turn，L298-325 effects 转接 + L464）——层数型资源走印记的既有支点，V7 隔离的印记侧就绪。
7. **校验器框架**：validator.py _Checker/_err/_warn 红黄二分、R-1~R-5 泛型、stats map 形态 value_meta 校验（L496-502）；field_meta.py STAT_CHILDREN（L177-186）——新增 V1~V11 可沿用该管道。
8. **快照自定义键先例**：battle.py L1786 record_alchemy_used（_snap.battle_alchemy_used）证明 battle_state 加顶层自定义快照键模式可行。

## 二、核心缺口清单（按 8 机制）
- **M1 注册段**：stats.json（test_demo 56 行）仅 hp/mp type=resource+base/max，无 rage/element_energy；M1 10 字段仅 5 个现成（name/type/base/max+growth），缺 icon/reset/display/max_per_pool/pools/pool_icons；STAT_CHILDREN 亦缺同 5 键——注册段 schema 与内容包均缺口。
- **M2 增减字段**：全库 grep 零命中 energy_gain/energy_cost（engine/core/content 均无）——字段级 schema、K1-K6 键空间、proc 内增减、触发类 D-03 语义全部零实现（battle 结算末尾无 gain 挂点、施放前无 cost 检查）。
- **M3 回合结清**：combatant 无资源槽（rage/heat 值无处存放）；被拒不耗回合管道现成但无 cost 判定；战斗结束 _settle L742 清 combo/marks 但无 reset 策略（battle/keep/battle_start）处理；被控 skip_turn L1107 不动资源=天然保留（半就绪）。
- **M4 resource_state**：battle.py to_snapshot L1748-1780 快照键无 resource_state 段；from_snapshot L1804 无恢复逻辑；data/battle.py 为 spec dataclass（L13-20 登记实际是 1g3 dict）——快照段、RS-1~6（时点/恢复/keep 双落/旧局/降级/池级原子）全部缺口。
- **M5 季节技能组**：skills.json 技能库整体不存在（无 skills 模块登记，field_meta L583 仅占位注释「技能库 M6 注入」；ctx["skills"] 装配 context.py L1279 恒空）——season 字段、技能位装配、进战懒加载（EFF-2）、展示过滤/置灰（EFF-3）、行动校验（EFF-5）均无载体；/技能 列表（basic_commands.py L1415 skill_rows）仅 job_restrict/type 过滤，无季节维度。
- **M6 换季边界**：battle.end_turn L1653（tick_turn_end L1666 → 死亡判定 → start_turn L1693）是「回合结束 tick 后、下一回合开始前」的天然挂点，但无季节检测/标记/切换逻辑；战斗内无 season 读取调用。季节互译键已就绪。
- **M7 on_season_change**：proc 容器可跑任意 trigger（L1655）故消费侧通，但无换季结算钩子触发点、无「恰一次」幂等标记、无 1b 事件枚举登记表（V11 依赖）、无四时调和被动实例，战斗内触发完全缺失。
- **M8 combo_table**：combo.py 为连段引擎（1c1a/b/c），**无组合表达任何实现**——combo_table 字段解析、F-C1 三重门禁（常规→总量门→多重集匹配）、F-C2 按行扣减+行为执行、CM-1~4、RE 可达性统计全缺；MP 扣费 battle 层亦无（enforce_mp 默认 False 且未消费）。

## 三、24 字段核对（10 注册 + 4 技能扩展 + 7 组合行 + 3 快照）
已实现 0；部分（结构可承载）3：快照 player/enemy 段形态（dict 快照按 side 嵌套惯例现成，只差键）、effects 引用字段（effects.json+overrides 概念在 1b 现成）、kind/power/element/hits 与 ActionCore 同构（action.json L3-90 有 kind/power/element 先例）。其余 21 字段全部缺口（注册 10 缺 5 字段 x2 型、技能扩展 4、组合行 combo/name 2、快照 3 全缺）。

## 四、11 条校验（V1~V11）核对：全部缺口
V1 资源引用/ V2 超上限/ V3 gain 负/ V4 reset 枚举+type 归一/ V5 变量引用（[我方资源:ID] 无别名无求值）/ V6 注册段结构/ V7 any 互斥+层数隔离/ V8 组合表+可达性/ V9 季节组（每季≤5/通用≥1）/ V10 互译表登记（注：season 键本身已登记 L164，V10 的「互译表缺失」场景仅剩 content 包自定义季节）/ V11 事件登记——因 skills 模块与注册段扩展均不存在，无一可评估实现。

## 五、20 TC 核对
TC-01~08 资源轴、TC-14~16 组合表、TC-17~20 校验：全不可执行（无内容包三件套 berserker/element_mage/season_mage、无 skills 库、无 rage/element_energy 注册、战斗无资源槽）。TC-09~13 季节组：部分可测（条件引擎 [季节:X] 求值、worldtime 换季检测、proc 容器机制可单测），但季节技能组载体（season 字段/技能位）不存在，端到端不可行。

## 六、4 流程 F-R1 回合结清 / F-R2 换季边界 / F-C1 组合触发 / F-C2 组合结算
全部未接线（各自挂点已定位：F-R1 施放前=combo.should_reject/battle.L1215、结算末尾=resolve_damage_action 命中后；F-R2=end_turn L1666-1693 间；F-C1/F-C2=apply_action 扩展）。

## 七、关键行号锚点（已 grep 验证在文件范围内）
condition_engine L164/L459；worldtime L91/L227/L244/L267/L106；effects L83/L1618/L1642/L1392/L1454/L1503/L1250；combo L856/L1092/L1215；battle L1191/L1217/L1280/L1653/L1666/L1693/L1748/L1804/L742/L1786；field_meta L177/L733/L757；validator L496/L1302；loader L150；battle_commands L819；stats.json 全文 56 行。

## 八、批8~批12 实施切法建议
**【批8 · 资源轴运行核心】**（依赖：批2 6a skills 库先行或并行共享 schema）
- 路8A 注册段 schema+内容包：stats.json 扩展 STAT_CHILDREN 加 icon/reset/display/max_per_pool/pools/pool_icons（field_meta L177 注入）；新增 element 8 元素注册表消费（validator._element_registry L671 现成）；test_demo stats.json 落 rage+element_energy 注册示例。依赖：field_meta 无冲突。
- 路8B energy_gain/cost 运行时：battle.py L1215 apply_action 前插 energy_cost 门禁（不足→rejected 复用 L1217-1229 管道不耗回合）、hit 后结算末尾（_resolve_damage_action 命中分支后）插 energy_gain 追加+封顶（≤max/max_per_pool）；proc 内增减经 effects L1618 容器触发点扩展；D-03 触发类不足不生效不计上限。依赖：8A 注册段、combo.should_reject 扩展。
- 路8C resource_state 快照：battle_state 顶层新增 resource_state 段（per-side dict，数值型单键/子池型池级展开 D-04），to_snapshot L1748 写入、from_snapshot L1804 恢复（RS-2）、_settle L742 reset 策略清零（battle）/保留（keep 落 3.7 存档）；参照 record_alchemy_used 先例。依赖：8A/8B 的资源槽存取。

**【批9 · 季节技能组】**（依赖：批1-2 6a skills 库 schema 落地、skills.json 内容载体）
- 路9A 季节字段+展示：skills 技能位装配加大 season 过滤（basic_commands.skill_rows L1415 扩展：season≠当前→置灰+「此术式与当前时节不合」）；进入战斗懒加载季节组（EFF-2 挂战斗开始）。依赖：skills 模块、worldtime.season_now。
- 路9B 换季结算边界：battle.end_turn L1666-1693 之间（tick_turn_end 后、start_turn 前）插换季检测（check_changes IF09 复用）→标记待结算（D-05 旧组校验当回合）→切换季节组→恰一次 on_season_change 触发+一行换季文案；普攻/防御兜底零空窗（现有 guard/normal 通道天然全年可用）。依赖：9A、effect_triggers 计数（每回合10/每场99 现成）。
- 路9C 条件与事件接线：season_element 随季覆盖（element 求值点）、proc trigger=on_season_change 挂载四时调和示例、[季节:X] 条件消费点三处测试（TC-13）。依赖：9B 钩子、effects.proc 容器（现成）。

**【批10 · combo_table 组合表达】**（依赖：批8 子池型能量存取、批1 6a skills 字段）
- 路10A 字段+校验：组合行 schema（combo/name/kind/power/element/hits/effects C1-C7）+ V8（元素∈pools/枚举/effects 引用/power 0-400/行数≤C(n+1,2)/可达性 RE 统计）。
- 路10B 触发判定 F-C1：施放前三重门禁（常规→总量门 any:n→多重集匹配），锁定组合行、先匹配后消耗（CM-2）；无匹配→拒不漏回合（复用 rejected 管道）。依赖：8B energy_cost 门禁同点。
- 路10C 组合结算 F-C2：按锁定行扣池（池级原子 RS-6）、行 kind/power/element/hits/effects 走常规执行器（effects L1392 现成）、cooldown 起算（combo.should_reject 冷却通道现成）。

**【批11 · 校验器 V1~V11】**（依赖：8A schema、批10A、skills 库登记进 loader._KIND_FOR_MODULE+field_meta）
- 路11A V1~V5+V6：资源引用红/超上限黄/gain 负黄/reset 枚举红+type 归一/变量引用黄/注册段结构红（原语沿 validator 泛型管道扩专项）。
- 路11B V7~V11：any 互斥+层数隔离红/组合表+可达性黄/季节组枚举+组规模红/互译表登记红（season 键已现成仅需包级校验）/事件枚举登记红。
- 路11C 校验测试：TC-17~20 坏包注入 fixture（对齐 A12 五处修复/TC-18 七项/TC-19 六项）。

**【批12 · 装配闭环+TC 集】**（依赖：批8-11 全量）
- 路12A 三内容包（berserker/element_mage/season_mage）+ /攻击 技能消费链路（battle_commands._resolve_skill L819 扩展 energy/season/combo 行动解析）+ ctx["skills"] 装配（context.py L1279 现成表位）。
- 路12B 战斗内资源展示（状态栏 💢72/100 与 🔥2💧1🌪0 渲染，battle_tpl 模板）+ 能量变化即时反馈（CM-4：火元素能量积蓄 火1/3）。
- 路12C TC-01~16 自动测试集 + 全链路 TC-20（六组合×双实例×换季联动）+ verify 门禁脚本（仿 scripts/verify 既有模式）。

**跨批依赖铁律**：批8A→8B→8C→10B/10C 串行；批1-2（6a skills schema）为 9A/10A 前置；9B 依赖 9A+effect 计数；11 系依赖全部 schema；12 系最后收口。每批 3 路并行时共享同一 field_meta/loader 登记表，防键空间分裂。

## 九、contract_deviations（10 条）
1. 【M1 字段缺 5】注册段 10 字段仅 name/type/base/max/growth 5 个现成（stats.json L2-55、field_meta STAT_CHILDREN L177-186），icon/reset/display/max_per_pool/pools/pool_icons 全缺——D-01 两型判别/ D-01b type=resource 归一（type 现仅 str 无枚举，resource|combat 注释占位 L179）均未兑现。
2. 【M2/M8 整机制缺失】energy_gain/energy_cost/combo_table 三字段全库零命中（engine/core/content grep 0），M2、M8、F-C1、F-C2 整体偏离。
3. 【M4 快照缺段】1g3 dict 快照（battle.py L1748-1780）无 resource_state 键，与 6c RS-1~6 及「与 transform_state/status_state 同批落快照」不符；data/battle.py dataclass 亦无 resource_state 字段（P1-3 已登记实际以 dict 为准，L13-20）。
4. 【M5 载体缺失】skills.json 技能库模块不存在（field_meta 无 skills ModuleMeta、loader._KIND_FOR_MODULE L150 无 skill kind、ctx.skills 恒空 context.py L1279）——season 字段/技能位/EFF-2~5 无承载，契约假设的技能行落点（6c §2.1「skills.json 技能行新增可选字段 season」）不成立。
5. 【M7 事件枚举】effects.py L1655 trigger 无白名单/无 1b 事件枚举登记表，V11「未登记事件红拦」无对象可校验；D-03 触发类 energy_cost 不足不计上限语义无实现。
6. 【V 系列全部未实现】V1~V11 无一落地（缺 skills 模块与注册段扩展前置），但 V10 的互译表层半就绪：condition_engine L164 已登记 [季节:{T}]→season，契约要求的 var=season 键空间已存在，与 6c「互译表缺失→红拦」的最坏场景不重叠。
7. 【A7/A9 变量缺口】var=resource 与 [我方资源:ID]/[对方资源:ID]（含池级 axis.pool）在 condition_engine 无注册无求值分支（L508-607 无 resource 分支），A7/A9/V5/TC-08 self_energy 落空。
8. 【MP 扣费未接线】combo.py L153-154 enforce_mp 默认 False 且 battle 层无 mp 扣费代码，6c 双耗「MP16+能量」的 MP 侧实际未消费（F-24 递延 M5 的已知偏离，6c TC-15⑧ 依赖此）。
9. 【控制 chance 未消费】effects.py L1503-1511 control 分支无 chance 参数处理，combo_table「蒸汽冲击 control chance:80」的命中率维度需在批10 补。
10. 【被控/战斗结束资源保留】M3 S4/S5（被控保留、战斗结束清零）无资源槽可作用（_settle L759-769 只清 combo/marks）；被控路径 L1107 本身不改资源故天然合规，但作为契约行为无显式实现。

其余 6c 契约（M6 互译、M7 上限字段、F-R1 被拒管道、L0 执行器）与现状一致，无额外偏离。
