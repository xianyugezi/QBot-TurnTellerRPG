# 细化_5d_测试体系总纲（测试金字塔 · 验收脚本 · 一键回归 · 数据包校验 · 故障注入 · 验收门禁）

> 版本：v1.0 · 类型：实现层细化契约（测试体系总纲：分层策略 / 里程碑验收脚本 / 一键回归 / 数据包数值校验 / 故障注入 / 门禁与用例组织）
> 追溯依据（**严格引用策划案，引用到行号 Lxx**）：
> - 《开发规则文档.md》v1.0（2026-08-15）→ 简称【规则】（**主依据**，全篇行号引用，本文档全部测试条款以 Lxx 为准）
> - 《框架设计文档》15.16 → 经【规则】L279 转引：**pytest 单测伤害公式/状态机/存档往返；故障注入（回复前崩溃路径）**
> - 各子系统细化契约（细化_1a~细化_5c，62 份）→ 用例组织来源（具名 TC 用例合计 **1228 条**，统计口径见 §0.2）
> - 实现层规划《实现层规划文档.md》任务编号（M01–M43 / T01–T53）→ 里程碑批次对齐（§2）
> 范围：QQ 回合制 RPG 框架**测试体系的实现层总纲**——分层策略、每里程碑验收脚本（verify_m0~m6）、
> 一键回归入口（run_all_tests）、数据包数值校验（verify_datapack：斩杀线/经济模拟）、故障注入套路、
> 里程碑验收门禁、以及本总纲自身的验收用例（TC-5d-01~36，共 36 条）。
> 不含具体系统语义用例（各细化契约已具名 TC-01~NN 承载）、不含覆盖率工具选型细节（【规则】L333 已定）。
> 变更纪律：任何偏离【规则】之处必须写入 §7.4 决策记录并注明理由+影响用例，禁止静默改语义。

---

## 〇、总则与统计口径

### 0.1 测试体系的目标（一句话）

> 每个里程碑交付时，用**一行命令**证明：本里程碑所属细化契约的具名 TC 全绿、引擎覆盖要求（【规则】4.2）达标、
> 存档往返与公式确定性（4.3/4.4）成立、故障注入（4.5）无静默吞错、覆盖率 ≥80%（4.6）——**全绿才算验收通过**（【规则】L422）。

### 0.2 用例统计口径（溯源 1228 条 TC）

- 统计范围：`/root/QBot-TurnTellerRPG/细化_*.md` 62 份文档中，**表格行首为 `| TC-` 的具名用例行**。
- 统计结果：**1228 条**具名 TC 用例；编号形式两类：
  1. **文档内连续编号**（主形式）：`TC-01` ~ `TC-NN`，即文档总览（计数）表声明的区间，如 细化_1c3 为 TC-01~TC-52（52 条）；
  2. **语义分组前缀编号**：`TC-TOP-01` / `TC-DEF-01` / `TC-END-01` / `TC-INT-xx` / `TC-ARM-xx` / `TC-2a-xx` 等（细化_1c1c 等状态集类文档），组前缀=状态族。
- 无具名 TC 的文档（细化_1a 伤害公式数值、1b 效果系统、1c1a/1c1b 迁移表、1d 印记、1g1a/1g1b/1g2/1g3、2a1a/2a1b/2a4a/2a4c 等）：
  其行为验证不缺席，由【规则】4.2 引擎覆盖要求（battle/damage/levelup/inventory/equipment/worldtime/storage 七模块必测场景，L289-297）+
  §2 各里程碑脚本的"补充引擎覆盖段"承接；状态迁移表类文档按 4.3 **逐格迁移断言**验证（每个迁移格=一条生成用例）。
- 引用规范：脚本与 CI 日志中用例定位一律写 `细化_XX.md#TC-NN`；本文档自身用例用 **TC-5d-NN** 前缀（全局唯一，避免与各文档内部 TC-01 歧义）。

### 0.3 测试可落地的架构前提（硬前置，违反即返工）

> 【规则】L52：**引擎层不 import NoneBot**（`engine/`、`state/`、`content/` 必须可脱离 NoneBot 单测）——这是测试规范能落地的前提。
> 【规则】L47：`tests/` 与插件同仓（`plugins/rpg_framework/tests/`），pytest 直接发现。
> 门禁判断（§6 G0 前置门）：首个里程碑 M0 若出现"测试依赖 NoneBot 才能跑"的用例，**直接判架构不合格**，按【规则】1.1 分层铁律整改后再测。

---

## 1. 测试金字塔（单元 / 集成 / 契约 / 端到端）

### 1.1 分层总览

| 层 | 目录 | 依据 | 依赖条件 | 速度目标 |
|---|---|---|---|---|
| L1 单元 | `tests/unit/` | 【规则】L283 | engine/content/state 纯逻辑，不联网、不启 NoneBot | 单用例 <100ms |
| L2 集成 | `tests/integration/` | 【规则】L284 | commands → engine 全链路，NoneBot 测试夹具打桩 | 单用例 <1s |
| L3 契约 | `tests/contract/` | 【规则】L306-311 + 各细化契约 TC | schema/字段元数据/消息模板/指令表 逐项比对（数据结构级） | 单用例 <50ms |
| L4 端到端 | `tests/e2e/` + 里程碑 verify 脚本 | 【规则】L285、L435 | 内容包冒烟（validator+模拟一局战斗）+ 里程碑全链路剧本 | 每里程碑 <3min |

> 补充说明：L3 契约层在【规则】4.1 的三层（单元/集成/冒烟）之上**显式化**——本项目零代码约束（【规则】L369-372：玩法/数值全走内容包配置、字段四处同步、JSON 是运行时公共 API L129）
> 决定了"配置结构即接口"，契约测试把 62 份细化契约的 schema 断言收敛为一层，避免散落各处的 `if "xxx" in data`（【规则】L92 校验收敛铁律同样适用于测试断言）。

### 1.2 各层职责与用例来源

- **L1 单元**：承接【规则】4.2 七模块必测场景（L289-297）+ 4.3 存档往返（L299-304）+ 4.4 伤害公式（L306-311）。
  - 确定性用例：固定种子/固定输入 → 精确数值断言（L308；如 ATK=30、倍率 100%、无弱点、命中必中 → 期望伤害区间）。
  - 性质用例：随机 1000 组属性 → 不变量断言（L309：伤害 ≥1、非会心 ∈ [0.95,1.05]×基础、会心率 ≤95%）。
  - 判定顺序用例：构造命中/会心/格挡同时满足的输入 → 断言顺序（格挡作用于会心后、双通道相加后，L310）。
  - **禁止硬编码生产参数**：公式参数一律读 fixture 包 formula.json 参数段（L311）。
- **L2 集成**：承接指令解析（细化_3c TC-01~45）→ 引擎全链路；会话互斥、串行队列、单玩家 1 会话（【规则】L378-380）的并发用例在此层打桩验证。
- **L3 契约**：循环遍历全部预置内容包，对 `schema_version`（L133/L394）、字段元数据表（L371）、消息模板变量占位（细化_3d）、指令表（细化_3c）做结构断言；**旧 schema 包必须在契约层报"可读+可迁移"**（4.3 L303）。
- **L4 端到端**：内容包冒烟（L285：validator + 模拟一局战斗 锁定→攻击→结算）+ 每里程碑 verify 脚本的剧本链路（§2）。

### 1.3 分层纪律（否决项）

| 违规形态 | 判定 | 处置 |
|---|---|---|
| L1 用例 import NoneBot | 破坏 L52 前提 | P0 拦截，移回对应层或打桩 |
| L2/L4 用例断言模糊（"不崩就行"） | 违反 L326 | P0 拦截，补明确断言 |
| 用例改生产配置/生产存档 | 违反 L311（fixture 独立） | P0 拦截，迁到 fixtures |
| 覆盖率不达标合入 | 违反 L333（engine/+content/ ≥80%） | P0 拦截，缺测补测 |
| 新功能无测试合入 | 违反 L385（评审清单 F 项 P0） | PR 不合并 |

---

## 2. 验收脚本规划（每里程碑 verify 脚本）

> 依据：【规则】L422 验收=「pytest 全绿 + 覆盖率达标 + 内容包冒烟」；实现层规划文档的批次依赖序（批次 0 数据骨架 → … → 集成回归）。
> 每个里程碑一个 `verify_m<N>.py`，位于 `scripts/verify/`，**幂等可重跑，退出码 0=通过**。

### 2.1 里程碑 → 细化文档 TC 覆盖矩阵（合计 1228 条）

| 里程碑 | verify 脚本 | 覆盖细化文档（具名 TC 计数） | 具名 TC 小计 | 补充引擎覆盖（无具名 TC 文档 → 4.2/4.3/4.4 承接） |
|---|---|---|---|---|
| **M0 框架** | `verify_m0.py` | 3a 架构分层(22)、3b 玩家属性三层(18)、3d 消息模板(26)、3f 单机向体验(21) | **87** | settings.json 默认值兜底（L370）；依赖方向单向（L53）静态检查 |
| **M1 战斗** | `verify_m1.py` | 1c1c 到顶与清零(19)、1c2 combo 配置(15)、1c3 连段测试集(52)、1g1c 战斗状态数据(30) | **116** | 1a 伤害公式(4.4 三型用例)、1b 效果、1c1a/1c1b 迁移表(4.3)、1d 印记、1g1a/1g1b(迁移格断言)、1g2 回合时序、1g3 快照续战(4.3 round-trip + random_seed L304)；battle.py/damage.py 必测场景 L291-292 |
| **M2 怪物** | `verify_m2.py` | 1e 怪物八段 schema(14)、1f 怪物AI 状态机(20) | **34** | 怪物数据走 content 校验（1e schema 隶属 3e 校验清单）；AI 状态机逐格迁移断言 |
| **M3 地图** | `verify_m3.py` | 2a1c 地图副本衔接(25)、2a2 换区追击(24)、2a3 副本两型(16)、2a4b 天气引擎(16) | **81** | 2a1a 地图 schema、2a1b 通道刷怪、2a4a 时间引擎、2a4c 时间天气接口 → worldtime.py 必测场景 L296（刷新补刷/离线封顶/跨天判定）+ 时间锚点式无定时器回归（实现层规划 M43） |
| **M4 指令** | `verify_m4.py` | 3c 指令解析(45)、4a 存储层(18)、4b 物品背包(22)、4c 成就(22)、4d 图鉴(25)、4e PVP(20)、4f 基础指令组(24) | **176** | storage.py L297（事务/崩溃恢复/并发写队列/防抖合并）+ inventory/equipment L294-295 + 4.3 存档往返（快照类：战斗/调合会话/状态印记/连段/商店限购 L301）+ 双扣防护 L323 |
| **M5 生活** | `verify_m5.py` | 2b1 NPC/发牌员(20)、2b2 对话状态机(21)、2b3 商店(42)、2b4 任务引擎(31)、2b5 签到(33)、2c1a 鱼种(25)、2c1b 钓鱼状态机(25)、2c1c 鱼王图鉴(18)、2c2a 锻造派生树(29)、2c2b 锻造流程(28)、2c2c 素材经济(22)、2c3a 强化曲线(25)、2c3b 强化流程(23)、2c3c 失败分级(24)、2c4a 炼金漏斗(26)、2c4b 宝石货币(25)、2c4c 珠与合成(30)、2c4d 炼金指令表(34)、2c4e 品质特性(26)、2c4f 投料触媒(38)、2c5a 职业等级 SP(26)、2c5b 委托声望(25)、2c5c 种植品评(24) | **620** | levelup.py L293（经验曲线边界）；经济类用例优先走 simulate 模式（§4），不在脚本内硬编码数值断言 |
| **M6 数据** | `verify_m6.py` | 3e loader 校验(30)、3e2 热重载(14)、5a 编辑器(18)、5b GM 指令(34)、5c 数值经济总表(18) | **114** | 4.6 四类样例包加载矩阵（合法/坏引用/缺模块/旧 schema，L332）；热重载回退（L321）；安全回归（JS 沙箱逃逸、zip slip，L429） |
| **合计** | 7 脚本 | 62 份细化文档全部覆盖 | **1228** | — |

### 2.2 脚本骨架（七个 verify 脚本同构，仅清单不同）

```
scripts/verify/verify_mX.py
├── 1. 收集范围      # 从本脚本内置的 细化文档清单 读取（文档名+TC 区间）
├── 2. 契约层前置    # 范围涉及的内容包全量 validator（L178 阻断式抛错）
├── 3. 逐用例执行    # 加载 tests/ 对应采集器，按 细化_XX.md#TC-NN 逐条跑并回传结果
├── 4. 补充引擎段    # §2.1 右侧列出的 4.2/4.3/4.4 引擎必测场景
├── 5. 覆盖率核算    # engine/ + content/ 行覆盖 ≥80%（L333），不达标直接失败
└── 6. 输出报告      # 用例数/通过数/失败清单（含失败用例的 细化文档+TC 号+断言原文）
```

- 运行方式：`python scripts/verify/verify_m4.py` 或 `run_all_tests.py --only m4`（§3）。
- 失败输出必须可回溯：失败行给出 `细化_<X>.md#TC-NN` + 期望断言原文 + 实际值，供修复者不翻代码即定位。
- 每个脚本末尾打印【规则】L269 要求的"测试执行结果（附 pytest 输出摘要）"段落，直接粘贴进开发记录字段。

---

## 3. 一键回归（run_all_tests）

> 入口：`scripts/run_all_tests.py`（唯一官方回归入口，CI 与本地共用），依据【规则】L422 的"pytest 全绿 + 覆盖率达标 + 内容包冒烟"三件套。

### 3.1 命令形态

| 命令 | 行为 | 适用 |
|---|---|---|
| `python scripts/run_all_tests.py` | **全量回归**：L1~L4 全部（unit + integration + contract + e2e）+ 7 个 verify_mX + verify_datapack + 故障注入+ 覆盖率核算 | 发布检查单（6.3）、每日 CI、里程碑门禁终验 |
| `python scripts/run_all_tests.py --fast` | **冒烟模式**：跳过 L1 全量性质用例（1000 组随机收抽样 100 组）、跳过 verify_datapack 全参数扫描（收抽样）、只跑内容包冒烟 + 各 verify 脚本的契约层前置 | 本地开发迭代、代码评审前自检 |
| `python scripts/run_all_tests.py --only m4` | **过滤模式**：只跑 `verify_m4.py`（即只验证 M4 指令里程碑范围）；`--only unit` / `--only fault` / `--only datapack` 同理 | 单里程碑返工、故障定位 |
| `python scripts/run_all_tests.py --only m5:201-260` | **用例级过滤**：只跑指定细化文档 TC 区间（201-260 为 细化_2b3#TC-201 起举例） | 单用例修订验证 |
| `python scripts/run_all_tests.py --exclude security` | 排除段（默认安全回归不排除）；`--exclude slow` 等价 `--fast` | 特殊情况 |

### 3.2 执行顺序与短路策略

```
1. 静态与覆盖前置：ruff/mypy 快速门（L423）→ 不通过直接中止（配 --skip-lint 可跳）
2. L3 契约层（最快，先暴露配置级问题）
3. L1 单元（确定性 → 性质 → 往返）
4. L2 集成
5. 故障注入（并发/故障类，独立进程跑防串扰）
6. verify_datapack（数值模拟，耗时大头，--fast 抽样）
7. verify_m0 → m1 → m2 → m3 → m4 → m5 → m6（顺序执行，严格按里程碑依赖序）
8. 覆盖率核算（engine/+content/ ≥80%，L333）→ 输出覆盖率报表
9. 汇总：通过/失败/跳过 + 各里程碑小节 + 失败文件与 TC 定位列表
```

- **短路原则**：任一步骤失败 → 后续步骤仍执行到可诊断粒度（收集全部失败，不掩盖），但**退出码非 0，门禁不放行**。
- **幂等原则**：回归使用 `tests/fixtures/` 独立数据（L311/L332），绝不写生产存档；SQLite 一律 `:memory:` 或 tmp_path（L330）。
- 执行时间预算：全量 <15min（CI 上限 20min）；`--fast` <3min；单里程碑 <3min。

### 3.3 用例过滤协议（--only 的分层语法）

| 语法 | 匹配范围 | 示例 |
|---|---|---|
| `m0`~`m6` | 对应 verify 脚本整段 | `--only m1` |
| `unit`/`integration`/`contract`/`e2e` | 金字塔层级整层 | `--only contract` |
| `fault` | 故障注入全组 | `--only fault` |
| `datapack` | 数据包数值校验 | `--only datapack` |
| `m5:<区段>` | 细化文档名+TC 区间（`细化_2b3#TC-xxx` 简写为文档号） | `--only m5:2b3`、`--only m5:2b3:1-42` |
| 组合 | 逗号分隔，语义取并集 | `--only m4,fault` |

---

## 4. 数据包校验（verify_datapack：斩杀线 / 经济模拟）

> 依据：【规则】L309 性质用例精神（随机组断言不变量）+ 细化_5c 数值经济总表（TC-01~18）+
> 本体系对经济闭环的**模拟验证**要求（纯期望断言不足以发现"单点正确、全局通胀"）。脚本：`scripts/verify/verify_datapack.py`。

### 4.1 校验范围

| 校验面 | 输入 | 断言不变量 | 用例来源 |
|---|---|---|---|
| **斩杀线** | 预置战斗配置（技能/怪物 HP/防御公式参数） | 核心输出技能单发/整套连段的期望斩杀线 ≤ 怪物 HP 的可配容忍带（默认 100%，可配 0=不限，遵循上限可配原则 L375）；斩杀线计算与 4.4 确定性公式同源（fixture 参数，L311） | 细化_1a、1b、1e、1g1c + 5c TC 组 |
| **战斗节奏** | 同斩杀线输入 | 平均回合数、连段达顶节奏在配置区间内；任何职业组合不出现"击杀期望 <0 回合"等除零/负值 | 【规则】L309 不变量精神 |
| **经济模拟** | 各产出/消耗条目（2c 系 + 5c 三币全景） | 模拟 N 万玩家回合：金币/宝石/经验三条曲线单调不爆炸；单日产出上限收敛；无明显"印钞机"组合（如：某种循环产出>消耗且无约束） | 细化_5c 货币体系、2c4b 宝石货币、2c2c/2c3a/2c4f 素材经济 |
| **掉落/强化/炼金概率** | 概率表（锻造成功率/强化分级/炼金成功率） | 按种子模拟 10 万次：实际频率与配置概率偏差 ≤ 可配容差（默认 1%）；强化失败分级分布符合 2c3c 契约 | 细化_2c3a/2c3c/2c4a~f |

### 4.2 运行模式与门禁关系

- 全量模式：完整参数扫描 + 10 万次抽样（数值噪声收敛）；`--fast`/`--only datapack --sample 10000` 支持抽样降时。
- 固定种子：全部模拟用固定 seed（L331 固定随机种子 fixture 精神），**同版本两次运行结果必须逐位一致**（确定性回归，重跑不抖动）。
- 违规处置：所有发现均**提示不拦截**（只建议不限制，L159-163），以黄色提示清单输出并以警告码退出（exit 2=仅提示）；**仅当断言不变量本身被破坏**（除零/负数/超容差）才以 exit 1 拦截。
- 门禁关联：M5 生活、M6 数据两里程碑的 verify 脚本**必须**内联跑 verify_datapack 相应子面（§6 门禁 G5/G6 硬性要求）。

---

## 5. 故障注入（坏包 / 断网 / 热重载回退）

> 依据：【规则】4.5（L313-326）六类故障点表 + L326「每个故障注入用例必须有断言，禁止"不崩就行"」。
> 实现：`tests/fault/` 六脚本，与主回归分离执行（防注入污染其他用例），由 `run_all_tests.py --only fault` 统一驱动。

| 脚本 | 对应故障点（L317-325） | 注入方式 | 断言（至少一条，违者 P0） |
|---|---|---|---|
| `fault_inject_crash.py` | 回复前崩溃（L319） | mock 发送出口抛异常 | 战斗状态回滚/可重试；**不双结算**（message_id 幂等键生效，L366） |
| `fault_inject_save.py` | 存档写入失败（L320） | mock storage 抛 OSError | 弹「保存失败，请检查磁盘空间」；绝不静默丢数据；失败本地暂存可补写（L365） |
| `fault_inject_reload.py` | 热重载失败（L321） | 写入非法 JSON 触发重载 | 回退上一份校验通过的 registry；服务不崩；人话提示（细化_3e2#TC-06~09） |
| `fault_inject_formula.py` | 公式求值异常/超时（L322） | 公式含死循环/非法变量 | 兜底 0 / 条件不满足，不崩溃（细化_1c3#TC-13 同断言） |
| `fault_inject_doublepay.py` | 双扣防护（L323） | 并发两条购买指令 | SQLite 事务下只扣一次（限购/货币）；存储层并发写队列生效（L297） |
| `fault_inject_netdrop.py` | 掉线重连（L324） | 模拟网络断开 | 静默恢复或广播「我回来了」；玩家数据完好 |

### 5.1 坏包矩阵（校验器故障面，挂 M6）

- 样例包四件套放 `tests/fixtures/packs/`（L332）：合法包 / 坏引用包 / 缺模块包 / 旧 schema 包——**四个各一，禁止更多**（保证样例即基线）。
- 每个坏包必须断言：红拦 5 类之一被捕获（L152-157）、聚合抛 `PackLoadError`（L178）、registry 未被污染（原子快照替换，热重载契约 3e2#TC-06~09）。
- 旧 schema 包不拦截加载而是走迁移（4.3 L303），断言：字段级缺补默认/多忽略，且「无效果/无链/无印」降级不抛异常（L302）。
- zip 导入安全：路径白名单（拒 `../` 与绝对路径）+ 条目数/大小上限（L351）+ 安全回归复跑（L429）。

### 5.2 注入隔离纪律

- 夹具内注入，**禁止黑入生产模块**（patch 仅作用于测试实例）；每个故障用例独立 fixture 起独立 `:memory:` 库（L330），互不串扰。
- 每个用例的注入点、断言、恢复路径三要素必须有注释；恢复路径必须在用例末尾执行（finally），用例失败不允许污染后续用例。

---

## 6. 验收门禁（验证全绿才可进入下一步）

> 依据：【规则】L422「全部测试通过（pytest 全绿 + 覆盖率达标 + 内容包冒烟）」作为发布检查单第一项；
> L385「测试是否覆盖本变更？（P0）」；实现层规划文档批次依赖序（前一启批次未验收通过不得进入后一依赖批次）。

### 6.1 门禁定义（G0~G6 + 发布门）

| 门禁 | 内容 | 进入下一步的条件 |
|---|---|---|
| **G0 架构前置** | 依赖方向单向、引擎零 NoneBot（L52-53）静态检查 + L2/L4 冒烟打通（注册→锁定→攻击 →结算闭环，L435 冒烟形态） | M0 开工前必须全绿 |
| **G1 框架（M0）** | `verify_m0.py` 全绿（87 条 TC）+ 覆盖率核算 ≥80% | 进入 M1 战斗 |
| **G2 战斗（M1）** | `verify_m1.py` 全绿（116 条 TC）+ 4.4 公式三型用例全过 + 存档往返（4.3） | 进入 M2 怪物 |
| **G3 怪物（M2）** | `verify_m2.py` 全绿（34 条 TC）+ 怪物数据 validator 无红拦 | 进入 M3 地图 |
| **G4 地图（M3）** | `verify_m3.py` 全绿（81 条 TC）+ worldtime 跨天/离线封顶用例 | 进入 M4 指令 |
| **G5 指令（M4）** | `verify_m4.py` 全绿（176 条 TC）+ 双扣并发用例 + 存档往返全快照类 | 进入 M5 生活 |
| **G6 生活+数据（M5/M6）** | `verify_m5.py`（620 条）+ `verify_m6.py`（114 条）全绿 + `verify_datapack` 全子面 + 故障注入六脚本全过 + 安全回归（L429） | 进入发布冻结 |
| **G7 发布门** | 6.3 发布检查单逐项（L420-433）+ 全量回归三件套 + 服务器冒烟（/注册 /锁定 /攻击 一轮闭环，L435） | 打 tag 发布 |

### 6.2 门禁判罚纪律

- **全绿定义**：退出码 0 且覆盖率达标且无黄色提示遗漏登记（黄提示记录在案可接受，L426）；任一红拦/失败 → 门禁不通过。
- **返工回路**：门禁失败 → 修复 → 重跑**该里程碑 verify 脚本 + 被影响上游的冒烟**（依赖序上游也需复验，防止破坏性修复）→ 再进门禁。
- **门禁记录**：每次通过/失败在《开发记录》登记「测试执行结果（附 pytest 输出摘要）」（L269），失败必须附失败 TC 清单。
- **回归防线**：每个里程碑门禁通过后，其 verify 脚本锁定为**回归基线**，后续里程碑改动触发全量回归时必须保留全绿（只增不改断言，断言修改必须走 §7.4 决策记录）。

---

## 7. 验收测试用例（TC-5d-01 ~ TC-5d-36，共 36 条）

> 编号规则：TC-5d-NN（NN 从 01 起连续）；引用格式 `细化_5d_测试体系总纲.md#TC-5d-NN`。
> 断言风格：每条均可自动化断言（文件存在性 / 退出码 / 输出文本 / 用例计数 / 目录结构）。

### 7.1 金字塔层（L1~L4）验收

| 编号 | 场景 / 输入 | 期望断言 | 依据 |
|---|---|---|---|
| TC-5d-01 | 检查 `tests/unit|integration|contract|e2e` 四个目录存在 | 四目录齐全；unit 下用例文件无 `from nonebot` 顶层 import | 【规则】L283-285、L52 |
| TC-5d-02 | 检查 L1 单元用例归属 | 七模块必测场景（battle/damage/levelup/inventory/equipment/worldtime/storage）各有 ≥1 条具名用例 | 【规则】L289-297 |
| TC-5d-03 | 检查存档往返用例 | 每类快照（战斗/调合会话/状态印记/连段/商店限购）有 序列化→反序列化→比对 用例；含 random_seed 往返用时序断言 | 【规则】L301-304 |
| TC-5d-04 | 检查伤害公式三型用例 | 确定性用例存在（固定种子固定输入→精确数值）+ 性质用例存在（≥1000 组随机参数）+ 判定顺序用例存在（命中/会心/格挡同时满足） | 【规则】L308-310 |
| TC-5d-05 | 检查公式参数来源 | 全仓 grep：测试内无公式参数硬编码常量（除 fixture 读取代码）；参数一律来自 `tests/fixtures/packs/*/formula.json` | 【规则】L311 |
| TC-5d-06 | 检查内容包冒烟存在 | e2e 含「validator 全量跑 + 模拟一局战斗（锁定→攻击→结算）」剧本用例 | 【规则】L285、L435 |

### 7.2 脚本组织验收

| 编号 | 场景 / 输入 | 期望断言 | 依据 |
|---|---|---|---|
| TC-5d-07 | 检查 7 个里程碑脚本存在 | `scripts/verify/verify_m0.py`~`verify_m6.py` 齐全且可导入 | §2.1 |
| TC-5d-08 | 检查每个 verify 脚本的 TC 覆盖声明 | 脚本内置清单与 §2.1 矩阵一致：脚本内声明的 细化文档+TC 区间条数 与目标文档实际 TC 行数相等 | §0.2 统计口径 |
| TC-5d-09 | 跑 `verify_m4.py`（在搭建完 M4 的 fixture 后） | 退出码 0；输出含「176 条用例」通过统计；失败时逐条给 `细化_XX.md#TC-NN`+断言原文 | 【规则】L269 |
| TC-5d-10 | 对 1c3 文档执行 TC 区间采集 | 采集器从 `细化_1c3_连段测试集.md#TC-01~TC-52` 采出 52 条，条条可映射到断言函数 | §0.2 |
| TC-5d-11 | 对 1c1c 文档执行 TC 采集 | 分组前缀编号（TOP/DEF/END/INT/ARM）被正确归组，19 条全采出 | §0.2 |
| TC-5d-12 | 对无具名 TC 文档（如 1a、2a4c）执行采集 | 采集器返回「0 具名，走补充引擎段」标记，verify 脚本正确进入 4.2/4.3 补充用例 | §2.1 |
| TC-5d-13 | 检查 fixtures 四件套 | `tests/fixtures/packs/` 有且仅有 合法/坏引用/缺模块/旧 schema 四包；每包带 README 说明其破坏点 | 【规则】L332 |
| TC-5d-14 | 检查 SQLite 与随机种子 fixture | 全仓测试代码无真实存档路径写入；SQLite 用 `:memory:` 或 tmp_path；随机种子 fixture 被伤害/掉落/模拟用例使用 | 【规则】L330-331 |
| TC-5d-15 | 检查覆盖率装置 | run_all_tests 末尾输出 engine/ + content/ 行覆盖率并与 80% 阈值比对 | 【规则】L333 |
| TC-5d-16 | 检查失败输出可回溯性 | 人为注入一条失败断言 → 报告给出 文档名#TC-NN + 期望断言原文 + 实际值 三要素 | §2.2 |

### 7.3 一键回归 / 数据包 / 故障注入 / 门禁验收

| 编号 | 场景 / 输入 | 期望断言 | 依据 |
|---|---|---|---|
| TC-5d-17 | 跑 `run_all_tests.py` 全量 | 退出码 0；输出含 金字塔各层 + 7 个 verify + datapack + 故障注入 小节统计；覆盖率 ≥80% | §3.2、L422 |
| TC-5d-18 | 跑 `run_all_tests.py --fast` | 冒烟模式 3min 内完成；性质用例抽样 100 组；verify_datapack 抽样运行；输出标注「冒烟模式」 | §3.1 |
| TC-5d-19 | 跑 `run_all_tests.py --only m4` | 只执行 verify_m4 段，输出不含其他里程碑小节 | §3.1、§3.3 |
| TC-5d-20 | 跑 `--only m5:2b3` 与 `--only m5:2b3:1-42` | 用例级过滤生效：前者跑 2b3 全部 42 条，后者只跑 1~42 区间 | §3.3 |
| TC-5d-21 | 人为置一个用例失败再跑全量 | 失败用例被列出且**后续步骤仍执行**（不短路吞错）；退出码非 0 | §3.2 短路原则 |
| TC-5d-22 | 连续两次跑全量（同代码状态） | 两次输出中确定性用例统计逐位一致（固定种子，无抖动） | §4.2、L331 |
| TC-5d-23 | 跑 `verify_datapack.py` 全量 | 斩杀线/战斗节奏/经济模拟/概率四子面完成；经济模拟抽样 ≥1 万次（全量 10 万次）；无除零/负值不变量破坏 | §4.1 |
| TC-5d-24 | 改坏一处预置经济配置后跑 datapack | 只建议不限制：以黄色提示清单输出（exit 2），不拦截加载；不变量本身破坏才 exit 1 | §4.2、L159-163 |
| TC-5d-25 | 跑 `--only fault` 六脚本 | 六脚本全过；每用例断言数 ≥1（脚本级静态检查断言存在） | 【规则】L326 |
| TC-5d-26 | 坏包回退注入（写非法 JSON 触发重载） | 回退上一份 registry；服务不崩；人话提示；断言非空 | L321、细化_3e2#TC-06~09 |
| TC-5d-27 | 双扣并发注入（并发两条购买指令） | SQLite 事务下只扣一次 | L323、L297 |
| TC-5d-28 | 断网注入 | 静默恢复或广播「我回来了」；玩家数据完好 | L324 |
| TC-5d-29 | 检查门禁脚本 | `scripts/gate/` 含 G0~G7 判定脚本或 run_all_tests 输出适配；gate 输出 通过/失败 清单 | §6.1 |
| TC-5d-30 | 模拟 G2 门禁失败场景（植入 1 条战斗用例失败） | 门禁判定不通过；返回明确的失败里程碑与失败 TC 清单；修复后重跑通过 | §6.2 |
| TC-5d-31 | 检查发布门素材 | 发布检查单逐项可执行（CHANGELOG 校验/迁移链校验/热重载验证/安全回归复跑均有对应脚本或脚本段） | 【规则】L420-433 |
| TC-5d-32 | 检查归档记录 | 门禁通过/失败在开发记录登记「测试执行结果（附 pytest 输出摘要）」 | 【规则】L269 |
| TC-5d-33 | 执行 G0 架构前置静态检查 | 依赖方向单向扫描（commands → engine/state/content → utils）通过；引擎目录无 nonebot import | 【规则】L52-53 |
| TC-5d-34 | 检查回复前崩溃注入用例断言 | `fault_inject_crash.py` 中每个用例含 状态回滚 或 message_id 幂等去重 断言（静态扫描断言关键字） | 【规则】L319、L326 |
| TC-5d-35 | 检查旧 schema 迁移链用例 | 迁移用例存在：构造旧 schema_version 存档 → 迁移 → 断言字段级缺补默认/多忽略；删除配置后读档走「无效果/无链/无印」降级不抛异常 | 【规则】L302-303 |
| TC-5d-36 | 检查热重载旧局旧配置边界用例 | 热重载用例含「战斗中不中断、旧局沿用旧配置、新局用新配置」断言（细化_3e2#TC-10~12） | 细化_3e2、L321 |

### 7.4 决策记录

| 序号 | 决策 | 依据/理由 | 影响用例 |
|---|---|---|---|
| D1 | 在【规则】4.1 三层上显式增加 L3 契约层 | 零代码约束（L369-372）+ JSON 为公共 API（L129）要求配置结构级断言收敛，避免测试断言散落 | TC-5d-01、05、08 |
| D2 | 本文档用例统一 TC-5d-NN 前缀 | 62 份细化文档内部均以 TC-01 起编号，无前缀会全局歧义；TC-5d 全局唯一 | TC-5d-01~32 |
| D3 | verify 脚本失败不短路（收集全部失败） | 门禁需要完整失败清单用于返工排序（§6.2），与 4.5「禁止无断言」同精神 | TC-5d-21 |
| D4 | datapack 数值越界只提示不拦截（exit 2） | 对齐校验二分法（L159-163 只建议不限制）；仅不变量破坏才拦截 | TC-5d-24 |

---

## 附录 A：用例与脚本一栏（最终口径）

| 项 | 数量 | 说明 |
|---|---|---|
| 细化文档具名 TC 用例 | **1228 条** | 62 份细化契约（§0.2 统计口径，含文档内连续编号 + 语义分组前缀两种形式） |
| 里程碑 verify 脚本 | **7 个** | verify_m0 ~ verify_m6（§2.1 矩阵 87/116/34/81/176/620/114） |
| 一元回归入口 | **1 个** | run_all_tests.py（全量 / --fast 冒烟 / --only 过滤，§3） |
| 数据包校验脚本 | **1 个** | verify_datapack.py（斩杀线 / 战斗节奏 / 经济模拟 / 概率，§4） |
| 故障注入脚本 | **6 个** | crash / save / reload / formula / doublepay / netdrop（§5，对齐 L317-325 六故障点） |
| 门禁 | **8 道** | G0 架构前置 + G1~G6 里程碑 + G7 发布门（§6） |
| 本总纲验收用例 | **36 条** | TC-5d-01~36（§7，金字塔 6 / 脚本组织 10 / 回归与门禁 20） |
| 脚本合计 | **15 个** | 7 verify + 1 run_all + 1 verify_datapack + 6 故障注入（门禁判定并入 run_all 输出适配层） |