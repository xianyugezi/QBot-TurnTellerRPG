# 细化规划·1c1a：连段状态集（combo_state 状态机 + 字段级数据结构）

> 依据：《技能派生系统设计定稿》v1.0（2026-08-14）。**引用格式：L=定稿行号**（如 L42）；
> 策划案未明处标注【细化补充】，供策划拍板（不改变策划案语义，仅收敛实现歧义）。
> 状态数：**6**（空闲 / 连段中 / 段数达标 / 派生中 / 到顶-reset / 到顶-hold）。
> 本文件与《细化_1c2_combo配置与打断霸体.md》配套：1c2 管**配置 Schema + 时序**，本文件管**运行期状态机 + combo_state 字段**。

---

## ① 状态集（6 状态）

### 1.0 总览

| # | 状态 | 枚举值【细化补充】 | 一句话语义 | 核心判定 | 引用 |
|---|------|------|------|------|------|
| 1 | 空闲 | `idle` | 无活跃连段链（count=0） | chain_id=null 且 count=0 | L45 / L59 / L112 |
| 2 | 连段中 | `in_combo` | 链已注册，段数积累中（1..max-1），无满足条件的派生 | 1 ≤ count < max 且条件评估全不满足 | L42 / L51-52 / L92 |
| 3 | 段数达标 | `derivable` | 当前快照 ≥1 条派生条件成立，派生可用（不强制派生） | 条件评估 ≥1 条为真 | L92-94 |
| 4 | 派生中 | `deriving` | 本回合正在执行派生形态（链上节点非终点） | 玩家自选派生并施放 | L28 / L69 / L93 |
| 5 | 到顶-reset | `at_max_reset` | count==max 且行为 reset：下一次连段技归零重打（节奏循环） | count==max 且 behavior=reset | L152-155 / L158 |
| 6 | 到顶-hold | `at_max_hold` | count==max 且行为 hold：保持最大不再增长，被动态免疫打断 | count==max 且 behavior=hold | L156-157 / L163-165 |

### 1.1 空闲（idle）

- **语义**：无活跃连段链，count=0。连段是战斗内资源（L59），从零开始；此状态下使用普通技能无"自断"语义（本来就无链可断）。所有状态在清零路径上汇入本状态。
- **进入条件**：
  1. 战斗开始 / 战斗结束（胜利/失败/死亡/逃跑成功/怪物丢失/中断回收）→ 清零（L59、L182）；
  2. 被打断成功：目标连段清零（L112、L385）；
  3. 使用无标签普通技能 → 连段归零（L45、L53）；
  4. 逃跑成功 → 连段清空（L57）；
  5. 复活不携带连段（L184）；
  6. 链被删除：进行中战斗按"无链=普通技能"降级（L343）。
- **关键数据字段**：`chain_id=null`、`chain_name=null`、`count=0`、`hold=false`、`step_index=-1`（无链进度）、`pending_derivations=[]`（无链不评估条件，L92）。
- **保持（非进入条件）**：指令被拒不改变（L56）；防御/道具不打断（L54-55）；被控制保留（L58）——这些不产生状态转移。

### 1.2 连段中（in_combo）

- **语义**：活跃链已注册，段数在 1..max-1 积累中，且当前快照下无任何派生条件满足（否则为"段数达标"）。可继续连段技（+1）、喘息技（+0/+N）、推进段数；也可能被普通技能自断、被对手打断技清零。
- **进入条件**：
  1. 连段技成功结算 +1 后，count ∈ [1, max-1]（L42、L51）；
  2. 保留连段/推进连段成功结算（+0/+N 不清零）后，count 未到顶且不满足条件（L43-44、L52）；
  3. 派生中结算后按保留段数回到本状态——派生是链上节点非终点（L28、L69、L73）；
  4. reset 到顶后"下一次连段技归零重打"重新积累（L154）；
  5. 冷却中派生技自动降级为原技能 + 连段保留后，保持/回到本状态（防链卡死，L203）。
- **关键数据字段**：`chain_id`=链ID、`chain_name`（冗余防热重载，L337）、`count ∈ [1, max-1]`、`hold=false`、`step_index`=最近执行步骤（互派生形态机进度，L97-103、L340）。
- **保持（不转移）**：防御（L54）、道具（L55）、被控制 skip_turn（L58）、指令被拒（L56）、逃跑失败（L57）——"断节奏不断积累"。

### 1.3 段数达标（derivable）

- **语义**：当前战斗快照下 ≥1 条派生条件成立，对应派生技能可用。**不强制派生**：玩家可自选派生技，也可继续用连段技推进（多派生同时满足时列表全部可用，L93）。条件不满足的派生置灰 + 标注原因（"需连段2"），不隐藏（L94）。反馈：状态行"连段 2/4 | 可派生：追斩·变 ✨"（L223）；技能列表派生提示"追斩 → 派生：大追斩（需连段 4）置灰（当前 2/4）"（L229-230）。
- **进入条件**：
  1. 指令处理时基于当前战斗状态**快照评估一次**，至少一条条件成立（L92）——精确 eq / 范围 min-max / 低段 max / 复合条件（L88-91）；
  2. 到顶当回合：先判定 eq=max 派生再执行归零（L158）。
- **关键数据字段**：`pending_derivations`=全部满足的派生步骤列表（L93，步骤引用链配置 from/to/mode/armor/consume，L281-301）；各步骤 `condition` 求值结果（L92；求值失败默认"条件不满足"，L329）；`count` 保持当前值。
- **退出**：选中派生 → 派生中；继续连段技 → count+1 后进入下一指令评估（施放后不重评，L92）。

### 1.4 派生中（deriving）

- **语义**：本回合正在执行派生形态。派生后**默认保留连段链**（怪猎式招式衔接，L28、L69、L73）；可配置【消耗连段】consume N 段释放大招（用后连段减 N、需重新攒，L74）；派生技标签独立配置、缺省继承原技能标签（L75）；`armor=true` 时**使用期间免疫打断**（L47、L115、L386）；单轮派生合并一条消息链式排版（L243）。
- **进入条件**：指令处理阶段玩家从可用派生列表（pending_derivations）**自选一项**（L93），且 MP/冷却/条件检查通过（指令被拒不耗回合不改连段，L56）。特例：冷却中派生**不入本状态**，降级为原技能 + 连段保留（L203）。
- **关键数据字段**：`derived_step`={from, to, mode, armor, consume, variant_override}（L281-299）；`armor_active` 瞬态标记（L47、L311）；结算规则：保留 `count' = count`（L73）或消耗 `count' = count - consume`（L74，大招消耗量必填 L399）。
- **退出**：结算完成 → 按保留/消耗后的 count 落回"连段中"（保留，L73）或"连段中/空闲"（consume 后需重新攒，L74、L157）。

### 1.5 到顶-reset（at_max_reset）

- **语义**：count==max 且链行为 reset（循环型）。**节奏循环**：到顶后下一次连段技归零重打（L154-155）；到顶当回合**先判定 eq=max 派生再执行归零**（防死配置，L158）。典型：剑士 reset 4 段节奏循环、精确卡段（L170）。
- **进入条件**：连段技结算后 count 达 max，且 `max_combo_behavior=reset`（L152-154；链级属性示例 L278）。
- **关键数据字段**：`count==max`、`hold=false`、`at_max=true`（运行期推导）、`pending_derivations` 含 eq=max 派生项（L158）、`max_combo` 为链级属性（L152、L190）。
- **退出**：eq=max 派生施放（L158）→ 派生中；下一次连段技 → count 归零重打（L154）→ 回连段中重新积累。

### 1.6 到顶-hold（at_max_hold）

- **语义**：count==max 且链行为 hold（蓄力型）。**保持最大不再增长，普通技能不打断**（L156-157）；hold 是 combo==max 时的**被动态**：仅当连段在最大值时才免疫打断（L163）；派生技消耗连段 → combo<max → **立即退出免疫**（需重新攒回顶，防"一次到顶永久免打断"无代价永动机，L164-165）。典型：法师/盾卫"攒满连段 → 大招消耗 → 重新攒"（L157）。
- **进入条件**：连段技结算后 count 达 max，且 `max_combo_behavior=hold`（L152-153、L156）。
- **关键数据字段**：`count==max`、`hold=true`、免疫判定仅当 count==max（L163）、`pending_derivations`、大招 consume 必填（L74、L399）。
- **退出**：派生技消耗 N 段 → count<max → 立即退出免疫，落回"连段中/空闲"重新攒（L157、L164）。

### 1.7 状态转移一览【细化补充：汇总，行号同各节】

```
空闲 ──连段技+1──→ 连段中 ──条件满足──→ 段数达标 ──自选派生──→ 派生中 ──保留──→ 连段中
  ↑                   ↑   ↑   │                                  └─consume─→ 连段中/空闲
  │ 清零路径           │   └─── 到顶(reset/hold)：count==max ──────┘
  └─普通技能/打断/逃跑成功/战斗结束(清零) ──→ 空闲
  reset 到顶：下一次连段技归零重打 → 连段中
  hold 到顶：被动态免疫；派生消耗 → count<max → 退出免疫 → 连段中/空闲
```

---

## ② 状态数据结构（combo_state 字段级）

### 2.1 持久化字段（入战斗快照，定稿 §9.4 存档示例 L333-343；快照归属见 §12 L382）

| # | 字段 | 类型 | 域 / 默认 | 持久化 | 来源行号 | 语义 | 关联状态 |
|---|------|------|-----------|--------|----------|------|----------|
| 1 | `chain_id` | string \| null | null | 是（存档） | L336 | 当前活跃链 ID；null=无链（按"无链=普通技能"降级，L343）。连段技使用即注册当前链（L189） | 空闲=null；其余=链ID |
| 2 | `chain_name` | string \| null | null | 是（存档） | L337 | 链名冗余存储，防热重载失效（L403） | 全状态 |
| 3 | `count` | int | 0 | 是（存档） | L338 | 当前段数，域 0..max_combo（max_combo 为链级属性，L152、L190）；随结算变化：+1（L51）/ +0（L52）/ +N（L44）/ 归零（L45、L53、L57-59、L112）/ 消耗减 N（L74） | 空闲=0；连段中/达标=1..max-1；到顶=max |
| 4 | `hold` | bool | false | 是（存档） | L339 | 到顶 hold 标记：count==max 且 behavior=hold；被动态免疫打断判定依据（L163） | 到顶-hold=true；其余=false |
| 5 | `step_index` | int | -1 | 是（存档） | L340 | 链步骤进度（最近执行步骤索引），支撑循环互派生形态机（L97-103） | 空闲=-1；其余≥0 |

> 存档示例（对齐 L334-342）：`{"combo_state": {"chain_id": "...", "chain_name": "...", "count": 3, "hold": false, "step_index": 2}}`

### 2.2 运行期 / 瞬态字段（不持久化，战斗快照内派生或计算）

| # | 字段 | 类型 | 域 / 默认 | 来源行号 | 语义 | 说明 |
|---|------|------|-----------|----------|------|------|
| 6 | `state` | enum（6 值，见① 1.0） | `idle` | ① 本文件 | 当前状态机状态 | 【细化补充】建议由 chain_id/count/hold 即时推导，不落档双写（避免存档与运行态不一致）；仅用于显示与日志 |
| 7 | `pending_derivations` | StepRef[] | [] | L92-93 | 当前快照满足条件的派生步骤列表（多派生全部可用、玩家自选，L93）；施放后不重评（L92） | 每次指令处理评估时重建（L92、L329） |
| 8 | `max_combo` / `max_combo_behavior` | int / enum | 链配置 | L152-153、L190、L278 | 链级属性：技能引用链获得；behavior=reset/hold 决定到顶状态归属（⑤/⑥） | 不存于 combo_state，避免双源 |
| 9 | `armor_active` | bool | false | L47、L311、L386 | 技能使用期间免疫打断（行动开始 → 本次结算完成），打断判定依据（L385） | 瞬态，派生/技能执行窗口内为 true |
| 10 | `last_clear_reason`（反馈） | enum \| string | null | L235-236 | 连段清零原因，供消息播报："连段中断！（原因：使用了普通技能）"（L235）/ 派生成功"🔥 完美连段！追斩·变"（L236） | 仅反馈用，不参与判定 |

### 2.3 生命周期约束（字段级绑定）

- **快照归属**：combo_state 入战斗快照（3.18），随战斗结束（任何原因）清零（L59、L182、L382）。
- **中断恢复**：combo_state 随快照还原（"连段 3/4 保持中"，L183）；复活不携带连段（L184）。
- **跨战斗**：默认战斗结束清零；"跨战斗蓄力"开关默认关、内容包可开（L213）。
- **链删除**：进行中战斗按"无链=普通技能"降级，不报错（L343）。
- **条件引用**：评估引用战斗快照字段 + statuses.json + stats.json + 自定义注册字段；求值失败默认"条件不满足"（安全失败不崩战斗，L328-329）。
- **作用域**：连段标签仅作用于玩家技能（怪物侧 P1 预留 AI 策略字段，L210）；多段攻击 hits>1 按施放次数计（1 次=1 段，L211）。

---

*引用底稿：《技能派生系统设计定稿》v1.0（408 行）；配套：《细化_1c2_combo配置与打断霸体.md》。*
