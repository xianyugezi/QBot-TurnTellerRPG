# 任务书 · 批1·路B：注册/注销 + 状态/角色（message 样板）

> 2026-09-12 · 消息模板重构战役 · 执行者：子 agent（本任务书 + fragment 为唯一指令源）

## 0. 一句话

把「注册/注销 + 状态/角色」49 条模板接入新全量表（文本已由主 agent 定稿在 fragment），
清掉旧分区对应键，处理 3 处代码拼接/硬编码，修测试，全量跑绿。

## 1. 环境与纪律

- 主仓：`/root/QBot-TurnTellerRPG`（**只读参考，禁止改动**）。
- 你的工作区：**`/tmp/tpl_b1b`**（git worktree，分支 `tpl-b1b`）。一切改动只在这里。
- Python：`/root/QBot-TurnTellerRPG/.venv/bin/python`。命令都在 `/tmp/tpl_b1b` 内执行。
- 隔离自检（先跑）：`cd /tmp/tpl_b1b && PYTHONPATH= /root/QBot-TurnTellerRPG/.venv/bin/python -c "import qbot_rpg; print(qbot_rpg.__file__)"` → `/tmp/tpl_b1b/...`
- 测试：`cd /tmp/tpl_b1b && /root/QBot-TurnTellerRPG/.venv/bin/python -m pytest tests/unit -q -rN`
- git：worktree 内 commit（分支 tpl-b1b）。**不要 push、不要动 main。**

## 2. 规范

完整规范：`/root/QBot-TurnTellerRPG/docs/消息模板重构/00_方案与规范_v1.md`。
要点：结构化行 ≤14 全角（28 半角当量）；少｜多换行；免斜杠指令；零装饰 emoji；大数值防爆宽。

## 3. 文本（已定稿，逐字应用，不要改写）

**权威文本 = `/tmp/tpl_b1b/docs/消息模板重构/_fragments/b1b.json`**（49 条；`templates` 段）。
其中 30 条标注「维持（已合规）」= 文本不变、只是**迁移到新表**；19 条为重做文本。
`notes` 段有每条一句话说明。**直接按 fragment 逐字写入，不要自行改动措辞。**

## 4. 工作清单（顺序执行）

### 4.1 写表（qbot_rpg/core/templates/template_table.json）
- `templates` 下新增 fragment 全部 49 个 key（用 python 脚本插入，indent=2、ensure_ascii=False）；
- `meta.prose_keys` 本批无新增（保持 `{}`）。

### 4.2 清旧分区（两处文件、四类动作）

**A. `qbot_rpg/core/templates/base.py` 删除以下 28 键**（DEFAULT_TEMPLATES 与 PLACEHOLDER_WHITELIST 两处）：
```
register_gate, already_registered, unregister_confirm, unregister_done,
status_level, status_exp, status_exp_only, status_max_hint, status_no_attr,
status_attr, status_attr_resource, status_location, status_target, status_effects,
status_imprints, status_equip_empty, status_equip_header, status_equip_line,
role_header, role_level, role_job, role_exp, role_exp_only, role_max,
role_attr, role_attr_resource, role_attr_detail, role_attr_detail_resource
```
- 其中 `status_equip_line`：**先 grep 复核零引用**（若真有引用，改为 tpl_of 接表并把文本从 fragment 补入；无引用则直接删=死键）。
- base.py 其余键（help_*/bag_*/shop_*/list_tail）**保留不动**。

**B. `qbot_rpg/core/templates/register_rem_tpl.py` 删除以下 21 键**（两处）：
```
register_name_too_long, register_name_bad_chars, register_dup_name,
register_job_not_found, register_job_recommended, register_reserved_hint,
register_success_prefix, register_success_welcome, register_success_job_loc,
register_success_recommended, register_success_attr_title,
register_success_attr_plain, register_success_attr_resource,
register_success_hp, register_success_mp, register_success_atk, register_success_dfn,
register_success_next, register_args_too_many, register_args_missing, register_auto_name
```
- `register_success_prefix`：**不迁表（死键）**，直接删除（先 grep 复核零引用）。
- shop_* 键（本文件后半）**保留不动**。

### 4.3 代码接线（3 处）

**A. `qbot_rpg/commands/unregister_commands.py`（硬编码常量化 → 表化）**：
- 顶部 `from qbot_rpg.core.templates import tpl_of` 改为：
  `from qbot_rpg.core.templates import DEFAULT_TEMPLATES as _ALL_TPL, tpl_of`
- 4 个模块常量改为**表别名**（保住测试导入与 import 兼容；文本唯一源在新表）：
  ```python
  TPL_UNREG_CONFIRM = _ALL_TPL["unregister_confirm"]
  TPL_UNREG_OK = _ALL_TPL["unregister_done"]
  TPL_UNREG_NONE = _ALL_TPL["unregister_no_player"]
  TPL_UNREG_AGAIN = _ALL_TPL["unregister_nothing"]
  ```
- `cmd_unregister` 内直接 return 常量处改为 tpl_of：
  未注册 → `tpl_of(ctx, "unregister_no_player")`；
  未确认 → `tpl_of(ctx, "unregister_confirm", {"name": name})`（已有，确认仍可用）；
  确认删档 → `tpl_of(ctx, "unregister_done", {"name": name})`。
- `TPL_UNREG_AGAIN` 的实际消费点：全仓 grep（可能在装配/runner 层）——若他层直接用常量文本，保持别名引用即可（文本已随表更新）；若他层也需模板渲染，报告说明，不要大改。

**B. `qbot_rpg/commands/register_commands.py`（列表拆行）**：
- `avail = " ".join(_available_jobs(ctx)) or "?"` → `avail = "\n".join(_available_jobs(ctx)) or "?"`
  （配合新模板 `register_job_not_found` 的 `{list}` 多行形态）。

**C. `qbot_rpg/commands/status_commands.py`（少｜）**：
- `effects_line`：`" ｜ ".join(parts)` → `"、".join(parts)`；
- `imprints_line`：`" ｜ ".join(segs)` → `"、".join(segs)`。
- `status_target` 模板已是三行形态（{name}/{hp_cur}/{hp_max}/{round} 占位不变），无需改代码。

### 4.4 内容包覆盖同步
- `content/veinborn/templates.json`：`register_success_next` 更新为：
  ```
  下一步：
  发 任务 领取 1 接见面礼
  发 位置 看出口，进入 下
  发 怪物 锁定 1 开战
  ```
  （JSON 字符串内用 \n；`job_switch_success` **不要动**。）
- `content/test_demo/templates.json`：**最小同步**——本批涉及的 key 若有旧值，更新为与主表一致；
  其余 500 余条不动（整包收敛另批处理）。同步哪些：跑全量测试后按失败定位，或直接扫描该文件与本批 key 的交集。

### 4.5 自校验
- `cd /tmp/tpl_b1b && /root/QBot-TurnTellerRPG/.venv/bin/python scripts/check_template_width.py` → 0 FAIL；
- `python -m pytest tests/unit/test_template_table.py -q` → 全绿；
- 实测渲染（推荐）：`rm -f /tmp/b1b.db && QBotRPG_DB_PATH=/tmp/b1b.db python scripts/qq_group_sim.py 注册 测试员 脊剑士` 看注册面板实际输出。

### 4.6 测试
- `python -m pytest tests/unit -q -rN` 全量跑；
- 修【本批范围】旧断言（test_register_commands / test_register_default_job / test_unregister_commands /
  test_status_commands / test_basic_commands（role 相关）/ 其他因 register_gate 等被引用到的），
  含 `scripts/` 内嵌冒烟断言；**非本批失败不动，列出报告**。
- 断言更新原则：以**新文本**为准逐条改（可从 fragment 取），不做等价放宽（除非逻辑上确需）。

### 4.7 硬编码扫描（本批范围）
- grep `register_commands.py / unregister_commands.py / status_commands.py / basic_commands.py`（role 段）
  的玩家可见中文串（排除注释/docstring），列清单：已迁 / 应迁（建议 key）/ 豁免（理由）。
- 已知：unregister 的 4 个 TPL 常量与 status/register 的拼接属本批处理；其余如 error 文案若属玩家可见也要列出。

### 4.8 提交 + 报告
- commit（1-3 个，如：文案+表 / 代码接线 / 测试同步），**fragment 与任务书一并提交**。不要 push。
- 报告按 §5 格式全文内联。

## 5. 报告格式（回复必含）

```
【fragment】路径 + 已提交短hash
【变更文件】列表 + 每个一句话
【测试】全量 tests/unit：X passed / Y failed / Z skipped（failed 列表或「无」）
【宽度校验】checker 输出原文（0 FAIL）
【实测渲染】注册/状态 输出样例（如跑了）
【死键复核】register_success_prefix / status_equip_line 的 grep 结果
【硬编码扫描】清单
【未完成/存疑】如实（无则「无」）
【commit】短hash + 标题（全部）
```

## 6. 中止纪律

如遇中断：优先 ① fragment 已含全文（已在盘）② 表/分区/测试改动 commit ③ 如实报告。
不谎报完成度、不跳过提交、不动范围外文件。
