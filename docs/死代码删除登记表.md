# 死代码删除登记表

> **用途**：记录「哪些死代码被删了 / 为什么敢删 / 哪些复核后保守没删」，供后人追溯。
> 每条给**基线 file:line** + **复核结论**；删前一律独立复跑，**不以历史报告结论直接执行**。
>
> **依据**：`批73_死代码_风险分类.md`（§1 总览：绿 49 / 黄 21 / 红 12；§2 绿清单；
> §3 黄清单；§4 红清单；§5 死类·空视图 6；§6 空转分支 3）+ `审计4_勿增实体_死实体与空转.md` §3。
> **基线**：本批起于 `54f716c`（批72 末）；分类文档写于批71 基线 `fbac7be`，本表 file:line 以**删除前实测**为准。
>
> **纪律**：
> 1. **只删 🟢 绿**；🟡 黄（21）与 🔴 红（12）一律不碰（红清单写明「删了会打挂动态装载/
>    扩展点契约/唯一消费链/掩盖缺陷」）。
> 2. **删前逐条独立复核**：跨文件调用 0 + 无动态引用（`getattr`/`globals()`/`__all__`/
>    注册表/字符串方法名/测试/脚本/桥接/内容包/文档）。
> 3. **判不准 → 不删**，移入 §三「改判清单」（**宁留勿删**）。
> 4. 不得改动 `tests/unit/test_editor_batch66_fixes.py`（批67 护栏）；不许顺手重构；
>    发现新问题 → 登记不动手。

---

## 一、批73 总览

| 项 | 条数 | 去向 |
|---|---|---|
| 🟢 绿 · 直删 | **47** | §二 删除清单（分 5 批，每批全量 pytest 0 failed） |
| 🟢 绿 · 复核后**改判为黄**（保守不删） | **2** | §三 改判清单（M7 契约文档点名） |
| §5 死类 · `Spawner` | **1 类** | §四 已删（随 §2.4 三条桩方法） |
| §5 空视图 · `ai`/`hidden`/`env_event`/`log_card` | 4 | §四 保留（处置建议=保留，未加注释） |
| §5 `SpawnManager`（生产零注入） | 1 | §四 保留（红级纪律：verify_m3 按名断言） |
| §6 空转分支 1/2/3 | 3 | §五（分支1 登记不动手；分支2 保留销号；分支3 独立小批） |
| **绿清单合计** | **49** | 47 删 + 2 改判 |

> **一句话**：49 条绿里 **47 条直删**、2 条因 M7 装配契约落点**保守改判黄**；
> `Spawner` 死类整壳删除，`SpawnManager` 与四空视图按处置建议保留。

---

## 二、删除清单（47 条，逐条 file:line + 复核结论）

> 「复核结论」= 对每条独立复跑的结论。公共复核（本批 47 条**全部**满足）：
> **① 跨文件调用 0**（AST 全语境扫描 `def`/`call.name`/`call.attr`/`attr`/`name`/`kwarg`/`import`
> 零非定义命中，覆盖 `qbot_rpg`/`tests`/`scripts`/`qbot_rpg_bridge`/`content` 823 个 `.py`）；
> **② 无动态引用**（`getattr`/`globals()`/注册表/字符串方法名 零命中；不在任何 `__all__`）；
> **③ 全仓整词 grep（含 `docs/**` 与非 .py）零业务消费**（仅历史审查报告/CTB 考古快照提及）。
> 下表只列**该条特有的**结论；标「同公共」者无额外发现。

### 2.1 批 73-A① · §2.1 前 12 条（commands/content 私有辅助）

| # | 基线 file:line | 名称 | 复核结论 |
|---|---|---|---|
| 1 | `qbot_rpg/commands/achievement_commands.py:80` | `_titles_of` | 同公共（私有，零引用） |
| 2 | `qbot_rpg/commands/basic_commands.py:502` | `_base_header` | 同公共（私有，零引用） |
| 3 | `qbot_rpg/commands/battle_commands.py:1667` | `_item_action` | 同公共（私有，零引用） |
| 4 | `qbot_rpg/commands/enhance_commands.py:165` | `_slot_quality` | 同公共；函数体仅 `return 'normal'` 常量桩 |
| 5 | `qbot_rpg/commands/forge_commands.py:1374` | `_join_node_args` | 同公共（私有，零引用） |
| 6 | `qbot_rpg/content/dungeon_models.py:164` | `_maps_with_entrances` | 同公共；docstring 称「反向 R2 校验靶」但该 R2 未接线（登记 §六） |
| 7 | `qbot_rpg/content/field_meta_pack.py:801` | `_display_field` | 同公共；同文件已有同类内联构造，未被复用 |
| 8 | `qbot_rpg/content/hot_reload.py:107` | `ReloadResult.ok_and_clean` | 同公共（属性方法，零引用） |
| 9 | `qbot_rpg/content/job_models.py:401` | `_state_policy_children` | 同公共；官方单源 `STATE_POLICY_CHILDREN = state_policy_fields()`，本条为绕路副本 |
| 10 | `qbot_rpg/content/loader.py:61` | `PackLoadError.pack_errors` | 同公共；调用方直接用 `.errors` |
| 11 | `qbot_rpg/content/quest_models.py:471` | `QuestDef.reward_entries` | 同公共；校验器用内联实现，未调用它 |
| 12 | `qbot_rpg/content/quest_models.py:485` | `QuestDef.has_reward_alias_conflict` | 同公共；同上 |

### 2.2 批 73-A② · §2.1 后 11 条（core 私有辅助/查询方法）

| # | 基线 file:line | 名称 | 复核结论 |
|---|---|---|---|
| 13 | `qbot_rpg/core/alchemy_core.py:223` | `AlchemyCore._pp_refresh` | 同公共；`pp_refresh` 由 `:732 _pp_budget_payable` 另路直读，删访问器不影响结算 |
| 14 | `qbot_rpg/core/battle.py:1603` | `BattleEngine._transform_policy_report` | 同公共；`_transform_events` 另经 `:1651`/`:3570` 消费。CTB `01_asset_inventory.md:112` 有考古行——该文档**基线=9ef3ce2 的只读快照**（自述「不含业务改动/行号以 9ef3ce2 为准」），非活契约，不构成保留理由 |
| 15 | `qbot_rpg/core/battle.py:5888` | `BattleEngine._json_roundtrip` | 同公共；类末方法，连孤立段落注释一并清。CTB 考古行同理（见 #14） |
| 16 | `qbot_rpg/core/codex.py:94` | `_item_def_type` | 同公共（私有，零引用） |
| 17 | `qbot_rpg/core/condition_engine.py:322` | `_num_of` | 同公共（私有，零引用） |
| 18 | `qbot_rpg/core/ctb_scheduler.py:255` | `CTBScheduler.peek_events` | 同公共（只读事件缓冲查询，零引用） |
| 19 | `qbot_rpg/core/dungeon.py:171` | `DungeonSession.with_rest_count` | 同公共（不可变替换方法，零引用） |
| 20 | `qbot_rpg/core/effects.py:1133` | `DamagePipeline._max_hp` | 同公共（私有，零引用） |
| 21 | `qbot_rpg/core/marks.py:200` | `MarksManager._def_is_obj` | 同公共（私有，零引用） |
| 22 | `qbot_rpg/core/temper.py:93` | `_nonneg_int` | 同公共（私有，零引用） |
| 23 | `qbot_rpg/core/transform.py:275` | `_norm_bool` | 同公共（私有，零引用） |

### 2.3 批 73-B · §2.2 未导出「公共形态」方法 11 条 + `stats_*` 整族 9 条

| # | 基线 file:line | 名称 | 复核结论 |
|---|---|---|---|
| 24 | `qbot_rpg/world/game_world.py:193` | `GameWorld.move_to_map` | **R1 红线操作**：同名顶层 `world/movement.py:359 move_to_map()` 是活跃代码（3 个测试直调），42 条同名命中**全属该函数**；**只删 GameWorld 类内 `NotImplementedError` 桩**，`git diff` 已核对未触 `world/movement.py` |
| 25 | `qbot_rpg/world/battle_boundary.py:345` | `DeathPenaltyConfig.has_any_loss` | 同公共（1 行表达式属性，零引用） |
| 26 | `qbot_rpg/core/combo.py:289` | `ChainConfig.step_from` | 同公共；已做 §2.5 四条确认，全空 |
| 27 | `qbot_rpg/core/combo.py:292` | `ChainConfig.step_to` | 同上；同节 `involves`/`at_max` 仍在，段落头保留 |
| 28 | `qbot_rpg/core/ctb_scheduler.py:221` | `CTBScheduler.tiebreak_rule` | 同公共；**连带**清理 `ctb_scheduler` 对 `CTB_TIEBREAK_RULE` 的孤儿 import（常量本体在 `ctb_rules.py` 保留，`test_ctb_scheduler.py` 直引未动） |
| 29 | `qbot_rpg/core/forge_tree.py:221` | `ForgeTreeEngine.tree_of` | 同公共；**分类文档记 :215（漂移 +6）**，实测删除前 :221 |
| 30 | `qbot_rpg/core/job_slots.py:520` | `_rearranged_active_order` | 同公共；孤立函数级注释（:518–519）随函数一并清 |
| 31 | `qbot_rpg/core/marks.py:424` | `MarksManager.resolve_ids` | 同公共；`formula_view` 为等价读取路径 |
| 32 | `qbot_rpg/core/runes.py:393` | `_iter_rune_entries` | 同公共；**连带**清理 `runes.py` 孤儿 `typing.Sequence` import |
| 33 | `qbot_rpg/storage/connection.py:176` | `Database.integrity_ok` | 同公共（属性方法，零引用） |
| 34 | `qbot_rpg/web/api.py:1325` | `catalog_module_names` | 同公共；web 面板直接用 `FRAMEWORK_MODULE_CATALOG`（`:981`），全仓（含 HTML/JS/JSON）零按名引用 |
| 35 | `qbot_rpg/content/models.py:594` | `EnemyDef.stats_hp` | **整族 9 条一起删**（§2.3 方案①）：`getattr(obj, f"stats_{k}")` 全仓零命中；连带同步同文件 3 处 docstring（:530/:553/:558）免指向已删访问器 |
| 36 | `qbot_rpg/content/models.py:598` | `EnemyDef.stats_mp` | 同 #35 |
| 37 | `qbot_rpg/content/models.py:602` | `EnemyDef.stats_str` | 同 #35（docstring :558 同步） |
| 38 | `qbot_rpg/content/models.py:606` | `EnemyDef.stats_int` | 同 #35 |
| 39 | `qbot_rpg/content/models.py:610` | `EnemyDef.stats_con` | 同 #35 |
| 40 | `qbot_rpg/content/models.py:614` | `EnemyDef.stats_spr` | 同 #35 |
| 41 | `qbot_rpg/content/models.py:618` | `EnemyDef.stats_foc` | 同 #35 |
| 42 | `qbot_rpg/content/models.py:622` | `EnemyDef.stats_agi` | 同 #35 |
| 43 | `qbot_rpg/content/models.py:626` | `EnemyDef.stats_luk` | 同 #35 |

### 2.4 批 73-B/D · §2.4 未实现桩 4 条（`Spawner.refresh_map/catch_up_offline/refill_world_stock` 随 §5#1 死类一并删）

| # | 基线 file:line | 名称 | 复核结论 |
|---|---|---|---|
| 44 | `qbot_rpg/world/game_world.py:258` | `GameWorld.is_boss_alive` | 同公共；`raise NotImplementedError(_NOT_IMPL_MSG)`，零引用 |
| 47 | `qbot_rpg/world/spawn.py:34` | `Spawner.refresh_map` | 同公共；随 `Spawner` 死类删除（见 §四） |
| 48 | `qbot_rpg/world/spawn.py:37` | `Spawner.catch_up_offline` | 同上 |
| 49 | `qbot_rpg/world/spawn.py:46` | `Spawner.refill_world_stock` | 同上 |

> **未删的 §2.4 两条**：#45 `try_consume_world_stock`、#46 `list_stores` → **改判为黄**，见 §三。

---

## 三、改判清单（保守没删 · 2 条）

> 依据本批红线「**凡复核后判不准的 → 不要删，移入改判清单（宁留勿删）**」。
> 两条均在分类文档 §2.2/§2.4 标为绿，但独立复核新增了**分类文档 §0.3 未覆盖的契约文档落点**。

| # | 基线 file:line | 名称 | 改判 | 原因（复核新证据） |
|---|---|---|---|---|
| 45 | `qbot_rpg/world/game_world.py:265` | `GameWorld.try_consume_world_stock` | 🟢→🟡 **不删** | `docs/细化/细化_M7_装配层契约.md:63` 的 **ctx 字段来源映射表**明确写 `world_stock / world_sold_out` → `callable/dict` → `game_world.world_stock / try_consume_world_stock`（消费方 shop）；同文件 `:168` 又把它列入 GameWorld「真实 API」。**删 = M7 装配契约指向空**（分类文档 §0.3 声称「docs 仅 6 条写成接口」，未覆盖 M7 细化契约）。按红区纪律「被 M7 契约文档点名」→ 至少须**先改契约文档**再谈删 |
| 46 | `qbot_rpg/world/game_world.py:288` | `GameWorld.list_stores` | 🟢→🟡 **不删** | 同上 `docs/细化/细化_M7_装配层契约.md:168` 列为 GameWorld「真实 API」；方法 docstring 自述「细化_5a 编辑器/运营页只读，M-y 实装；预留签名」——是**细化文档承诺的预留接口**，非纯内部细节 |

> **后续处置**：若产品/M7 侧确认这两条不再需要，应走**黄流程**（先改 `细化_M7_装配层契约.md`
> §1.3 映射表与 §5.1 API 清单 → 再删代码）。本批登记不动手。

---

## 四、§5 死类 / 空视图处置（6 类）

| # | 对象 | file:line | 分类文档处置建议 | 本批动作 | 说明 |
|---|---|---|---|---|---|
| 1 | `Spawner` 类 | `qbot_rpg/world/spawn.py:28-47`（`__all__:20`） | **删** | ✅ **已删** | 连同 `__all__` 条目、私有 `_NOT_IMPL_MSG`（仅服务三桩）、`DEFAULT_OFFLINE_CAP_HOURS`（仅作 `catch_up_offline` 默认参）一并清；模块 docstring 与 `world/__init__.py` 模块清单改指 `SpawnManager`。删后全仓 `grep -w Spawner` = 0 |
| 2 | `SpawnManager`（生产零注入） | `qbot_rpg/world/spawn.py:80` | **不要删** | ✅ **保留** | 红级纪律：`scripts/verify/verify_m3.py:221/238` 有 `getattr` 按名存在性断言（`refresh`/`initial_spawn`）；删前必须先动 verify 脚本。属**玩法语义**问题（接线 or 改脚本），不在死码批 |
| 3 | 空视图 `ai` | `qbot_rpg/content/field_meta.py:3453` | **保留 + 注释** | ✅ **保留** | `ModuleMeta` 注册面：删会改编辑器对「包若声明该模块」的处理；当前 10 包不用 ≠ 永久不用 |
| 4 | 空视图 `hidden` | `qbot_rpg/content/field_meta.py:3454` | **保留 + 注释** | ✅ **保留** | 同上 |
| 5 | 空视图 `env_event` | `qbot_rpg/content/field_meta.py:3456` | **保留 + 注释** | ✅ **保留** | 同上 |
| 6 | 空视图 `log_card` | `qbot_rpg/content/field_meta.py:3458` | **保留 + 注释** | ✅ **保留** | 同上 |

> **关于「+ 注释」**：分类文档建议给四空视图加 `# UNUSED-VIEW(2026-09)` 注释。本批**未加**——
> 批73 立项范围 =「**只删 🟢 绿**」且纪律要求「diff 只减不增、不许顺手重构」；四视图处置的
> 主体是「**保留**」，注释为可选增补。**登记为后续批**（若产品明确裁定编辑器不做扩展视图，
> 再走「先改 `module_catalog.py` 排除说明与面板回落逻辑」的黄流程）。

---

## 五、§6 空转分支处置（3 处）

| 分支 | 位置 | 分类文档处置建议 | 本批动作 | 原因 |
|---|---|---|---|---|
| 1 · `spawn_manager.alive_monsters` 优先分支（生产恒不进入） | `qbot_rpg/world/game_world.py:212-214` | **接 or 删，必须二选一** | ⛔ **不做（登记后续批）** | 任务纪律「建议『接/改』的不要顺手做」；且其「删」分支要求**先改 `verify_m3.py:221/238/321-325`** 并按玩法语义回退（与 §5#2 `SpawnManager` 强绑定）。属玩法决策，不在死码批 |
| 2 · `formula.json` 展示层字段（整段无 reader） | `qbot_rpg/content/field_meta.py:1402 FORMULA_FIELDS` | **保留，销号**（已有 `_UNIMPLEMENTED` 守卫） | ✅ **保留 · 销号** | 框架已自认并守卫（`:1290 _UNIMPLEMENTED`、`:1429-1434 help`「兼容保留键…formula_loader 不消费」）；删会砍掉编辑器表单而与包数据不一致 |
| 3 · `enemy_act` 过期注释指向的已删接口 | `core/pvp.py:376` free 模式真 bug + `battle.py:5338` docstring 行号 + verify 脚本 | 独立小批（**非死代码**） | ⛔ **不做（登记独立派单）** | 分类文档明示不含在死码批：`enemy_act`/`end_turn` 是**故意保留的壳**，`tests/unit/test_battle_engine.py:171-178` 用 `pytest.raises(NotImplementedError)` **正面断言其存在**。修 free 模式属行为修复需对拍 |

---

## 六、顺手发现 / 登记不动手（新问题，未动手）

| 项 | 证据 | 建议 |
|---|---|---|
| `dungeon_models` 反向 R2 校验未接线 | `_maps_with_entrances`（已删）docstring 自述「反向 R2 校验靶」，但该 R2 全仓无接线点 | 若仍需该规则，另立「副本入口校验」小批 |
| 四空视图可选注释 | 见 §四 | 产品裁定后再走黄流程 |
| `SpawnManager` 生产零注入 | `assembly/bootstrap.py:64` 不注入 `spawn_manager` → `alive_monsters` 分支恒不进入 | 并入 §六 分支1 的玩法决策 |
| `forge_tree.tree_of` 行号漂移 | 分类文档记 :215，删除前实测 :221 | 分类文档行号以本表为准（其余 48 条一致） |

---

## 七、验收证据（批73）

```bash
# 1) 全量测试（每批一次；删测试相关代码后计数未降，因所删均为无调用代码）
pytest tests/ -q -o addopts=""        # → 9034 passed, 10 skipped, 0 failed（与基线一致）

# 2) ruff（改动文件零新增；全仓对 qa_ctb_baseline_ruff.txt 零新增）
ruff check .                          # 改动文件：All checks passed!

# 3) 双尺子复算（种子 20260919）→ 零变化
python scripts/batch45_measure.py     # 与基线逐字节一致
python scripts/batch55_dual_ruler.py --json   # 与基线一致

# 4) 换包验收 12 包全 PASS
python scripts/editor_verify_packs.py # → 12 个包 · PASS 12 · FAIL 0

# 5) 工作树干净
git status --porcelain                # → 空

# 6) 逐条复核命令（把 <name> 换目标名）
grep -rn --include=*.py "\b<name>\b" qbot_rpg tests scripts qbot_rpg_bridge content
grep -rIn -w "<name>" qbot_rpg tests scripts content docs *.md   # 含非 .py / docs
```

> 删除分 5 批提交（`73-A①` 12 / `73-A②` 11 / `73-B` 11 / `73-B·stats` 9 / `73-B·D` 4+1 类），
> 每批一次全量 pytest，**全程 0 failed、9034 passed 不降**。
