# 审查报告：M6 F5 热重载原子性设计（组A：机制设计合规）

- 审查对象：`docs/实现层规划文档.md` F5 热重载原子性（L3436-3439）、F4 校验器接线（L3431-3434）、§9 风险 L3496、G3 故障注入 L3460-3463、M6 里程碑 L3481
- 定稿对照：`docs/审查参考/RPG回合制框架设计文档.md` §15.5（L1616-1618）/§4.4（L763-778）/§15.16（L1637）；`docs/审查参考/开发规则文档.md` §2.4（L172-178）/§1.3（L71-92）/§2.5（L180-187）/§四.5（L313-326）
- 已实现参考：`qbot_rpg/content/hot_reload.py`、`loader.py`、`validator.py`、`registry.py`、`data/battle.py`、`core/battle.py`；细化契约 `docs/细化/细化_3e2_热重载契约.md`
- 门控档位：**full**（用户指定）；方式：**纯静态审查**，未运行任何命令/脚本/验证；所有运行行为结论标注「静态推导」。
- 结论：**P0 = 0，P1 = 3，P2 = 7**

---

## 一、总述

F5 设计的**实现要点（L3438）**与定稿（框架 §15.5、§4.4、规则 §2.4）逐条对照为**忠实转写，无直接冲突**；定稿行号/章节引用**全部真实存在，无编造**；F4/§9/M6/G3 跨文档衔接**整体一致**。核心问题集中在"**设计文本停留在定稿复述层，未下钻到机制层**"：三个 P1 缺漏（增量 vs 全量校验口径未仲裁、攒批统一触发的完成机制未定义、旧局旧配置的会话层接线未设计）在 M6 落地时会留出实现自由度，存在半套配置/混合版本漏检风险。

---

## 二、① 缺漏维度

### 应覆盖 X 但未覆盖 清单

**G1（P1）增量校验口径：未写"增量=加载/解析增量、校验仍全量"；且细化 TRG-3 与 F2④/实现三方口径冲突未仲裁。**
- F5 L3438 只写"按文件 mtime 增量校验，只重载变更模块"。
- 冲突证据：
  - `细化_3e2_热重载契约.md` L78 TRG-3 原文："未变更模块复用上一份 registry 数据，**不做 IO、不重校验**"；
  - 同文档 L99-100 F2④："④ 全量校验：以「新配置整体」跑完整校验器…**校验对象是替换后的完整包，不是仅校验 Δ 文件**"（ATO-3 L118 同义）；
  - `loader.py` build_pack docstring L186-188 明确裁决："**整包校验仍全量重跑（引用关系跨模块，跳过会漏悬空引用）**"。
- 影响：同一细化契约内部 TRG-3（增量/不重校验）与 F2④（全量校验）自相矛盾；F5 文本沿用"增量校验"未仲裁。若 M6 实现者按字面只校验变更模块，跨模块悬空引用（如改 items 引用已删 effect、enemies 引用已删 action）将漏检进入运行态，违反"绝不半套配置运行"（规则 §2.4 L178）。F5 验收①（写入非法 JSON→回退）只覆盖语法错误，无法暴露此漏洞（非法 JSON 任何口径都拦）。
- 修复建议：F5 L3438 改为"按文件 mtime 增量**加载/解析**（未变更模块复用解析结果），**校验器对新配置整体全量重跑**（引用完整性跨模块，跳过会漏悬空引用）"；并同步修正细化 TRG-3 L78"不重校验"措辞为"不重新解析/不做 IO，校验仍全量"。

**G2（P1）攒批统一触发的完成机制未定义：服务器侧无法判定"全部写完"，"全部文件写完统一触发重载"不可强制。**
- F5 L3438 与定稿同句照抄"全部文件写完统一触发重载"（框架 L1616、规则 L175、细化 ATO-2 L117），但**无完成信号设计**（无 manifest 最后写/版本戳/编辑器批提交信号）。
- 已实现参考 `hot_reload.py` L217-233 `_detect_changes` 为**逐文件** mtime+size+hash 签名比对，「静态推导」：3 秒轮询窗口内若编辑器批量保存 A、B 两文件跨过轮询边界，服务可能先以"Δ={A}"（A 新 / B 旧）触发一次重载并通过校验后提交——即提交了 ATO-7（L122）禁止的"混合版本态"；随后 B 落盘再重载，若 B 移除 A 新引用的目标 → 红拦 → 回退，回退目标是**上一份混合态**。此为混合版本可被观察到的残留风险，靠全量校验缓解但不能消除。
- 修复建议：F5 增加完成机制（至少二选一）——(a) 明确以"轮询节拍内检测到的全部变更集 Δ 为一批，一次管线全包构建"作为批语义（当前实现口径）；(b) 编辑器批保存时把 manifest 作为最后写文件当提交标记，检测到 manifest 变更才触发整批。二者择一写入设计，杜绝"边写边重载/半批重载"。

**G3（P1）旧局旧配置的会话层接线未设计：验收③无设计支撑，且实现侧 `_backup_snapshot()` 无任何消费方。**
- F5 L3438 只写"进行中对局持旧快照（ID+名称冗余）、新对局用新配置"，**未写会话层如何取得/持有旧配置**：对局创建时获取时机、引用计数隔离（细化 D-05 L53）、按旧配置结算路径、generation 世代校验（自检 B L216）、引用计数归零回收。
- 实现参考：`hot_reload.py` L344-346 `_backup_snapshot()` 已定义（docstring"对外供旧局旧配置结算引用"），**静态检查：全仓库无调用方**（grep 确认仅定义处出现）；`registry.py` 有 snapshot/restore/from_snapshot/resolve_name，`data/battle.py` 有 CombatantSnapshot（含解析后数值），但会话层↔watcher 的接线点在 M2/M3 未落地、F5 也未指定接线契约。
- 影响：M6 验收③"进行中对局持旧配置（快照冗余断言）"无设计支撑，M6 实现者无从接线。
- 修复建议：F5 增加一条接线要点：对局创建时经 `watcher._backup_snapshot()`（或 `registry.snapshot()`）取旧 registry 快照 + 引用计数 +1，对局结束/30 天回收 -1；旧会话经快照的解析后数值+ID+名称冗余结算（见 G5）；新对局解析当前 watcher.registry。

**G4（P2）registry 快照的保存/恢复时机与快照内容未写。**
- F5 L3438 只写"回退上一份 registry 快照"，未写：快照在**每次成功提交后**滚动入队、N=2 档（当前有效+上一份，细化 D-04 L52/实现 `deque(maxlen=2)` L81）、回退动作在失败瞬间以指针级替换完成（registry.py from_snapshot L148-159）、快照内容=完整注册表+各模块原始数据+schema_version+manifest（SNAP-4 L153）。
- 修复建议：F5 补一句"每次成功重载后保留 N=2 档校验通过快照（当前+上一份），失败时以指针级替换原子恢复"。

**G5（P2）删除降级边界未写清：回退 vs 降级的分界、降级三维度之外的核心数值。**
- F5 L3438"删除配置按'无效果/无链/无印'降级不报错"未界定：
  - 被**仍在加载的模块**引用的删除 → 新配置整体校验 R-4 红拦 → **回退**（不降级）；只有被**旧会话快照**引用的删除才走降级。两种出口未写明。
  - 降级仅覆盖效果/派生链/印记三维；被删怪物/物品的**核心数值**（atk/max_hp 等）须靠会话快照携带的解析后状态结算（`data/battle.py` CombatantSnapshot L32-43 已含），"ID+名称冗余"（OLD-2 L184）本身不足以支撑"按旧配置结算"（框架 3.18 L344）。F5 未说明快照内容须含结算所需解析值。
- 修复建议：F5 补写"删除分界：破坏新配置整体引用 → 回退；仅旧会话引用 → 按无效果/无链/无印降级；会话快照须含 ID+名称冗余 + 结算所需解析后状态"。

**G6（P2）原子写细节与失败处置未写。**
- 临时文件名（`.json.tmp`）、同目录同文件系统（跨设备 rename 非原子，细化 ATO-1 L116）、写方归属（编辑器/导入器/GM，细化 F2① L91）均未入 F5 设计文本；rename 失败/临时文件写失败的处理（视为未变更不触发？服务不崩？）未写。
- 修复建议：F5 补"原子写由编辑器/导入器/GM 落盘方执行（临时文件+同目录 rename），写失败不产生半写文件、服务侧按未变更处理"。

**G7（P2）人话提示内容规格未写；防空转/失败节流未入设计。**
- "人话提示"应含原因+正确用法+下一步（细化 SNAP-3 L152），F5 未写；实现已有"连续失败≥3 自动暂停轮询转手动 /reload"（BLK-5 L209 / hot_reload L295-300），F5 设计与验收均未提及，M6 复用时缺依据。
- 修复建议：F5 补一句"人话提示含原因+正确用法+下一步；连续失败达阈值自动暂停自动轮询转手动"。

---

## 三、② 错误维度

### 3.1 与定稿 §15.5 / §4.4 / §2.4 逐条对照（无冲突确认）

| F5 L3438 要点 | 定稿出处 | 对照 |
|---|---|---|
| 先写临时文件再原子 rename | 框架 §15.5 L1616；规则 §2.4 L175 | 一致 |
| 全部文件写完统一触发重载 | 框架 §15.5 L1616；规则 §2.4 L175 | 一致（完成机制缺失见 G2） |
| 按文件 mtime 增量校验，只重载变更模块 | 框架 §15.5 L1618；规则 §2.4 L176 | 一致（增量口径未仲裁见 G1） |
| 新模块同样纳入 mtime 监控 | 框架 §4.4 L775；规则 §2.4 L176 | 一致 |
| 校验失败 → 回退上一份 registry 快照 + 人话提示 | 框架 §15.5 L1617；规则 §2.4 L178 | 一致；F5 漏"校验通过"限定（定稿为"上一份**校验通过**的 registry 快照"），语义偏差轻微 → P2 措辞项 |
| 进行中对局持旧快照（ID+名称冗余）、新对局用新配置 | 框架 §4.4 L777；规则 §2.4 L177 | 一致（会话层接线缺失见 G3） |
| 删除配置按"无效果/无链/无印"降级不报错 | 规则 §2.4 L177；框架附 A.5 L1887 | 一致（边界缺失见 G5） |

**确认：F5 与定稿 §15.5/§4.4/§2.4 无任何冲突**——F5 是逐条忠实转写。

### 3.2 删除配置降级 与 热重载失败回退 是否矛盾 —— 确认不矛盾，但分界未写明
- 两者互补而非互斥：删除若破坏**新配置整体**的引用 → 校验 R-4 红拦 → **回退**（服务继续用含被删配置的旧 registry）；删除若只被**旧会话快照**引用 → 新配置校验通过 → **降级**（无效果/无链/无印）。
- F5 同时列出两项但未写分界（即 G5），存在实现者误以为"删除一律降级不报错"从而绕过 R-4 红拦的风险。建议在 F5 补一句分界。

### 3.3 与 F4 校验器接线衔接 —— 一致，两处措辞口径需澄清（P2）
- F4（L3433）"红拦 → 该模块不注册 → 启动/重载失败 → 回退上一份校验通过的 registry 快照"与 F5"校验失败 → 回退"衔接一致，且 F4 已含整包回退兜底。
- 措辞 a：F4"该模块不注册"易被读成"逐模块部分注册"；实现为整包 `check_pack` 任一红拦 → `PackLoadError` → 整包不发布（`loader.py` L252-260）。因 F4 后续有整包回退，不构成冲突，建议改为"该模块（及整包）不注册"。
- 措辞 b：F4"每模块必经 validator"在实现中为**整包一次性 `check_pack`（内部逐模块校验）**，而非逐模块调用——按字面逐模块调用会重复报错、破坏"一次给全量"（`loader.py` L213-215 P1-1 注释同义）。建议 F4 补"聚合校验：一次 check_pack 覆盖全部声明模块"。

### 3.4 定稿依据标注精度（P2）
- F5 L3437 定稿依据将 §4.4 标注为"（mtime 增量/旧局旧配置）"；§4.4 实际为"文件 **mtime/哈希检测**"（L774），"增量"一词出自 §15.5（L1618）。属轻微标注不精确，非编造。建议改为"§4.4（mtime/哈希检测 + 旧局旧配置）、§15.5（增量校验）"。

---

## 四、③ 幻觉维度 —— 确认无幻觉

### 定稿引用核查表（全部真实存在）
| 引用 | 出处 | 核查 |
|---|---|---|
| 【框架】§15.5 热重载原子性 | 框架 L1616-1618（技术要点 #5） | 真实 |
| 【框架】§4.4 mtime/旧局旧配置 | 框架 L774-777（加载流程第 4 步） | 真实 |
| 【规则】§2.4 热重载硬约束 | 规则 L172-178 | 真实 |
| 【规则】§1.3 结构断言收敛 validator | 规则 L71-92（L92"禁止散落 if in data"） | 真实 |
| 【规则】§四.5 故障注入表（热重载失败行） | 规则 L313-326（L321） | 真实 |
| 【框架】§15.16 测试/故障注入 | 框架 L1637（技术要点 #16） | 真实 |

### 机制幻觉核查
- F5 所述全部机制（临时文件+rename、mtime 增量、registry 快照回退、旧局旧配置 ID+名称冗余、删除按无效果/无链/无印降级）**均在定稿原文存在**（框架 §15.5/§4.4、规则 §2.4），无编造 rename 流程或 mtime 判定细节。
- 无"编造行号"：上述六处引用逐行核验通过。

---

## 五、④ 跨文档维度

| 关联点 | 内容 | 核查 |
|---|---|---|
| F4（L3431-3434）↔ F5（L3436-3439） | 回退 registry 快照语义一致 | 一致（措辞见 3.3） |
| §9 风险 #6（L3496）↔ F5 | "原子写 + 快照回退 + 旧局旧配置（F5），校验失败绝不半套运行（§2.4）" | 一致 |
| G3 故障注入（L3460-3463）↔ F5 验收① | "写入非法 JSON 触发重载 → 回退旧 registry 不崩"；F5 验收①显式"（故障注入 G3 复用）" | 一致（良好前向引用） |
| M6 里程碑（L3481）↔ F5 验收 | M6 verify"热重载失败回退不崩"覆盖验收①②，**未含验收③"进行中对局持旧配置（快照冗余断言）"** | 缺项 → **P2**：M6 verify 列应补"旧局旧配置快照冗余断言" |
| 细化 TRG-3 ↔ loader 实现 ↔ F5 | "不重校验" vs "全量重跑" vs "增量校验"三方口径 | 冲突未仲裁 → P1（G1） |

**M6 verify 缺验收③（P2）**：建议 M6 里程碑行（L3481）补"旧局旧配置（快照冗余断言）"verify 项，使 F5 三项验收在 M6 verify 列全量可勾选。

---

## 六、问题分级清单

### P1（3 项）
1. **增量 vs 全量校验口径未仲裁（G1）** — `实现层规划文档.md` L3438；细化 TRG-3 L78 ↔ F2④ L99/loader.py L186-188 冲突；修复：F5 明确"增量=加载/解析、校验对新配置整体全量重跑"，修正 TRG-3 措辞。
2. **攒批统一触发完成机制缺失（G2）** — L3438；修复：以"轮询节拍内变更集 Δ 为一批，一次管线全包构建"或"manifest 作最后写提交标记"择一入设计。
3. **旧局旧配置会话层接线未设计（G3）** — L3438/验收③ L3439；`hot_reload.py` L344-346 `_backup_snapshot` 无消费方（静态检查）；修复：F5 补对局创建/结束的旧快照获取与引用计数、结算路径。

### P2（7 项）
1. 快照保存/恢复时机与 N=2 档、快照内容未写（G4，L3438）。
2. 删除降级边界与"回退 vs 降级"分界未写（G5，L3438；关联 M6 verify）。
3. 原子写细节与失败处置未写（G6，L3438）。
4. 人话提示规格 + 防空转/失败节流未入设计（G7，L3438/验收①）。
5. F4"该模块不注册/每模块必经 validator"措辞与整包聚合校验口径（L3433）。
6. F5 定稿依据 §4.4 标注"mtime 增量"用词不精确（L3437）；"回退上一份 registry 快照"缺"校验通过"限定（L3438）。
7. M6 verify 列未含 F5 验收③（L3481）。

---

## 七、无问题维度确认

- **错误维度**：F5 与定稿 §15.5/§4.4/§2.4 **无逐条冲突**（对照表见 §3.1）；删除降级与失败回退**不矛盾**（见 §3.2）；F4 接线衔接**一致**（§3.3）。
- **幻觉维度**：六处定稿引用**全部真实**；机制无编造（§四）。
- **跨文档维度**：F4、§9、M6、G3 **一致**（唯一缺项为 M6 verify 未含 F5 验收③，P2）。
- **F5 验收标准质量**：三项验收（非法 JSON 回退/G3 复用、mtime 单模块断言、旧局快照冗余断言）均为可执行、有断言项，符合 G1"verify 必须可执行且有断言"（L3452）。

---

## 八、Top 3 问题

1. **增量口径未仲裁（P1）**：细化 TRG-3"不重校验"与 F2④"全量校验"自相矛盾，F5 沿用"增量校验"——按字面实现将漏检跨模块悬空引用，违反"绝不半套配置运行"。
2. **攒批统一触发无完成机制（P1）**：服务器按逐文件 mtime 轮询无法判定"全部写完"，3 秒窗口内可能提交 A 新/B 旧混合版本，弱化 ATO-7"半套禁止"。
3. **旧局旧配置会话层接线未设计（P1）**：F5 验收③无设计支撑，实现中 `_backup_snapshot()` 无消费方，M6 无从落地"进行中对局持旧快照"。
