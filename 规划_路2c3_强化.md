# 规划 · 路2c3 强化系统（实现层）

> 性质：实现层原子任务清单。每条任务含四要素：**任务名 / 定稿依据 / 实现要点 / 验收标准**，全部可追溯到定稿。
> 定稿来源：《强化系统设计定稿 v1.0》（下文简称「强化」）+《指令分隔符统一规范》（分隔）+ 实现层规划文档（C21/V11/T-M4-05）+ 职业熟练度与生活系统设计定稿 v1.0（边界）+ 功能联动优化分析（孤岛现状）。
> 任务数：15 条 = 配置数据 4（A）+ 结算引擎 4（B）+ 指令 3（C）+ 边界/可选钩子 2（D）+ 校验与呈现 2（E）。
> 原子性约定：每条任务 = 一个可独立实现、独立验收的单元；跨系统机制（品质注册表/事务幂等/幸运属性来源/强化石产出）只立项一次，其余任务引用之，不重复实现。

---

## 0. 覆盖对照表（用户要求 ↔ 任务）

| 覆盖项 | 任务 |
|---|---|
| 强化曲线（等级数消耗+金币） | A2 / A3 |
| 成功率与失败分级（低不变 / 高掉级 / 碎=内容包选项默认关） | B1 / B2 |
| 强化上限（品质四档决定） | A4 |
| /强化 <装备>+N | C1 |
| /强化信息（+N / 成功率 / 上限） | C2 |
| /强化保护（保护石） | C3 |
| 转移（内容包选项默认关） | D1 |
| 原子操作防双扣 | B4 |
| 熟练经验 | D2 |
| （附带）level_gated 装备等级门槛 / 装备卡显示 / 校验器 / 编辑器 | A1 / E2 / E1 / E2 |

---

## A. 配置与数据层（enhance.json）

### A1. enhance.json 加载与默认配置落地
- **定稿依据**：强化 §九（enhance.json 全量字段：settings/success_curve/cost/values/protect_stone；settings 默认值：max_by_rarity＝normal 5/fine 8/epic 10/legendary 12、fail_tier_split=3、shatter_mode=false、shatter_chance=0、shatter_max=5、luck_affects=true、transfer_allowed=false、level_gated=false，上限值不变）；§一 边界（品质四档引用 items.json 唯一注册表，跨系统不重复定义）；§八（level_gated 开启时 +N 需装备等级 ≥ 门槛）；实现层规划文档 C21（enhance.json 依赖 items/stats/luck）。
- **实现要点**：
  - 加载器：路径 `life/enhance.json`（内容包可覆盖）；缺文件 = 全默认配置兜底（系统可用）。
  - 字段默认值逐项兜底（上列值）；品质键必须 = items.json 四档枚举（normal/fine/epic/legendary），拒绝整数/R 口径等其它编码。
  - protect_stone 引用 items.json 物品 ID（可空）；stone_tiers 引用的强化石物品需在 items.json 存在。
  - 全部开关（luck_affects / transfer_allowed / level_gated / shatter_mode）一次落地，供后续任务读取；level_gated=true 时强化上限追加装备等级门槛判定（门槛表内容包配）。
- **验收标准**：缺文件/缺字段全兜底不崩；品质键非法被拒；四个开关默认值与定稿逐项一致；level_gated 开/关两用例。

### A2. 强化成功率曲线表（解析 + 查表）
- **定稿依据**：强化 §4.1（基础成功率表 +1 90% → +10 25%，内容包可配任意档位；等级越高成功率越低，经典递减）；§10.1 #1 #2（曲线单调不增；档位覆盖 0→max_by_rarity 最大值）。
- **实现要点**：
  - success_curve[] 按 to 升序建索引；运行时按目标等级 N 查表得基础率。
  - 缺档（内容包只配到 +8 但传说上限 +12）：加载期黄提示「曲线未覆盖至上限」，运行期按末档兜底。
  - 超上限查表不触发（A4 上限拦截前置）。
- **验收标准**：默认表 1→90…10→25 解析正确；+N 查表命中；缺档兜底 + 黄提示；非法档（非递增、负值）加载报错。

### A3. 强化消耗计算（强化石三档 + 金币）
- **定稿依据**：强化 §五（强化石=炼金产出三档：低级 +1~+3 / 中级 +4~+6 / 高级 +7~+10；数量消耗 = 强化等级 × stones_per_level（+1 用 1 个、+2 用 2 个…可配）；金币 = 强化等级 × coin_per_level（默认 100）；商店卖基础强化石=金币回收口，高级石商店不卖或限量）；§10.1 #4（石档位引用 items.json 存在性 + 档位覆盖连续）。
- **实现要点**：
  - 消耗按**目标等级 N**（本次 `/强化 铁剑+N` → 目标 N+1）：石数 = (N+1) × stones_per_level；金币 = (N+1) × coin_per_level。
  - 石档位选择 = 目标等级所在 tier（stone_tiers.levels 区间），tier 解析为具体强化石物品 ID。
  - 检查顺序：石数（含正确档位）→ 金币余额 → 不足提示带来源（「缺 低级强化石×2（来源：炼金合成/商店）」）→ 通过才进入 B4 结算事务。
- **验收标准**：+3→+4 用中级石、+6→+7 用高级石；数量随目标等级线性递增；金币 = 目标等级 × 基础费用；不足拦截提示含来源；档位区间未覆盖目标等级 → 加载期黄提示。

### A4. 强化上限（品质四档注册表）
- **定稿依据**：强化 §六（上限 = 装备品质决定：普通 +5 / 精良 +8 / 史诗 +10 / 传说 +12，上限值不变；到达上限 `/强化` 提示「已达强化上限」；强化数值每级 fixed 或 % 由 enhance.json values 提供接口）；§九（max_by_rarity 键 = items.json 四档品质，上限值不变）；§八（level_gated 开启时 +N 需要装备等级 ≥ 门槛，如 +5 需装备 Lv20）。
- **实现要点**：
  - 上限查表：装备品质（items.json rarity 字段）→ max_by_rarity[rarity]。
  - 拦截**先于结算**（框架实现审查确认序）：当前 +N ≥ 上限 → 提示「已达强化上限」，零消耗返回。
  - level_gated=true 时追加：目标 +N 对应装备等级门槛不达标 → 拦截并提示所需等级。
- **验收标准**：四品质各 1 用例（+5/+8/+10/+12 到顶）；达顶指令零消耗且文案「已达强化上限」；level_gated 门槛拦截用例。

---

## B. 结算引擎（无指令依赖的纯逻辑层）

### B1. 成功率结算（基础率 + 幸运修正）
- **定稿依据**：强化 §4.1/§4.2（实际成功率 = 基础成功率 +（√我方幸运 − √对方幸运）%，对方=0 时即 +√我方幸运%；幸运来自角色属性 stats.json 自定义 luck 属性；luck_affects 开关默认开，内容包可关；显示「成功率 60%（基础 55% + 幸运 5%）」）。
- **实现要点**：
  - 基础率 = A2 查表（目标等级）。
  - luck_affects=true 时：修正 = round(√luck_me − √luck_opp)（对方=0 → +√luck_me）；实际率 = 基础率 + 修正，按 [0,100] 夹取（实现默认，防负值/超 100 显示异常）。
  - 拆分显示供 C1/C2 使用：「成功率 {实际}%（基础 {基础}% + 幸运 {修正}%）」/ 修正为负 → 「基础 {基础}% - 幸运 {修正}%」。
  - 随机判定：rng < 实际率 → 成功。
- **验收标准**：幸运修正公式单测（luck=25 → +5%）；clamp 0/100 两用例；luck_affects=false → 修正恒 0；拆分显示文案正确。

### B2. 失败分级（低不变 / 高掉级 / 碎=内容包选项默认关）
- **定稿依据**：强化 §三（低等级段 +0→低段上限（默认 +3）失败 = 装备不变无惩罚；高等级段（低段上限以上，默认 +4+）失败 = 强化等级 −1；fail_tier_split 可配；碎装备 shatter_mode 默认 off + shatter_chance 默认 0%、我们不开；校验器碎率超默认 5% → 黄警告「碎率高挫败感强」）；§二（失败提示惩罚类型：❌ 失败！强化等级 -1（铁剑+5 → +4））。
- **实现要点**：
  - 失败后分级：当前 +N ≤ fail_tier_split → 无惩罚（等级不变，文案「强化失败，装备保持不变」）；> fail_tier_split → 等级 −1（不低于 +0）。
  - shatter 钩子：shatter_mode=false 不参与判定；=true 时高段失败额外掷 shatter_chance（默认 0 → 永不触发），命中 → 装备碎（移出背包）+ 特有文案。
  - 保护石介入点（C3）：保护场景失败且本应有惩罚 → 先扣保护石抵消惩罚（等级不变），再落账。
- **验收标准**：+1 失败装备不变；+5 失败 −1（文案含「强化等级 -1」）；fail_tier_split=4 时 +4 失败不变；shatter_mode=false 连续 100 次无碎；开且 5% 概率用例可复现；碎后装备移出背包。

### B3. 强化数值加成应用（flat / percent · 装备卡）
- **定稿依据**：强化 §六（强化数值每级可配：武器攻击 +固定值或 %；防具防御 +固定值或 %；具体值游戏设计阶段定，enhance.json 只提供配置接口）；§九 values（weapon_atk_per_level: flat 5 / armor_def_per_level: flat 3）；§10.2（装备卡显示当前 +N 与上限）；§一 边界铁律（强化加数字、锻造管形态，词条类效果已砍）。
- **实现要点**：
  - 加成注册表：当前等级 N × values[部位词条] → 最终值 = base + N×flat 或 base × (1 + N×percent%)。
  - 接入战斗结算与面板的属性来源（items/stats）：装备总攻击/防御 = 白值 + 强化加成；**不新增任何词条/形态字段**（附魔已砍）。
  - 装备卡展示：{装备名}+N 与强化加成明细（供 E2 复用）。
- **验收标准**：flat/percent 两类型计算用例；武器/防具两部位各 1 例；装备卡显示 +N 与上限；词条层无改动（无效果系统新条目断言）。

### B4. 原子结算事务（防双扣 · 一条消息）
- **定稿依据**：强化 §二（原子操作：一条指令出结果，零会话，免疫热重载/双扣；成功率随机故禁批量）；实现层规划文档 V11（购买/强化/奖励结算防双扣：SQLite 事务 + message_id 幂等 + version；指令处理以 message_id 为幂等键）+ T-M4-05（/强化 禁批量 *）；路2b 规划 C5 同款「事务 + 幂等键」模式。
- **实现要点**：
  - 单事务顺序：扣石 → 扣金币 → 判定（B1/B2）→ 写回等级（保护路径含扣保护石）→ 存档；任一步失败整体回滚（不扣款不扣物不写等级）。
  - 幂等：指令处理以 message_id 为幂等键，重发同 id 返回已结算结果，不二次扣费。
  - 零会话铁律：结算上下文全量自包含，不依赖会话状态；热重载/切包后不残留半状态。
- **验收标准**：事务失败用例整体回滚（金币/石头/等级三态一致）；同 message_id 重发不双扣（幂等断言）；重载前后结算连续正确；扣费与结果同一条消息闭环。

---

## C. 指令层

### C1. /强化 <装备> 指令（原子结算一条消息）
- **定稿依据**：强化 §二（`/强化 铁剑` → 铁剑+1；`/强化 铁剑+5` → +6；⚙️ 行显示成功率｜幸运修正；成功显示数值变化；失败显示惩罚类型；低段失败「强化失败，装备保持不变」）；§10.3（/强化 <装备> 强化 +1，原子，需确认材料后直接结算）；指令分隔规范（铁剑+5 的 + 为等级标记保持；强化禁批量 *，批量=连点爆装风险）。
- **实现要点**：
  - 解析：`/强化 铁剑+5` → 物品名「铁剑」+ 现等级 5（目标 6）；`/强化 铁剑` → 目标 1；+ 后非数字 → 解析错误提示；出现 `*` 批量语法 → 拒绝「强化不支持批量（成功率随机，批量=连点爆装风险）」。
  - 管线：A4 上限拦截 → A3 消耗检查（不足带来源）→ B4 事务 → 单条消息输出；挂接框架频率控制（主文档 7 章防刷屏，连点限频）。
  - 回复模板：`⚙️ {装备}+{N} → +{N+1}（成功率 {p}%｜幸运修正 {d}）` → `✅ 成功！{词条} {a} → {b}` / `❌ 失败！强化等级 -1（{装备}+{N} → +{N-1}）` / 低段「强化失败，装备保持不变」。
- **验收标准**：裸名/带 +N 两用例；带 * 被拒且提示与分隔规范一致；达顶文案零消耗；成功数值变化与失败惩罚类型文案逐字对齐定稿示例；材料不足提示含来源；单条消息完成（零会话断言）。

### C2. /强化信息 指令（+N / 成功率 / 上限 / 消耗）
- **定稿依据**：强化 §10.3（/强化信息 <装备> 查看当前 +N / 成功率 / 上限）；§4.2（成功率显示含幸运拆分）；§五（消耗预览）。
- **实现要点**：
  - 解析：物品名（可带 +N，以实际当前等级为准）；输出：当前 +N / 下一级成功率（基础 + 幸运拆分）/ 上限（当前品质）/ 下一级消耗（石 + 金币）。
  - 达顶时显示「已达强化上限（传说 +12）」。
- **验收标准**：四字段齐全；成功率含幸运拆分；达顶显示上限档；物品不存在/未强化提示正确。

### C3. /强化保护 指令（保护石）
- **定稿依据**：强化 §3.3（保护石：强化失败不掉级，消耗 1 颗，失败时消耗；来源 = 炼金产出 / 商店高价 / 活动奖励，可配；高等级段堆装备的保险）；§10.3（/强化保护 <装备>）。
- **实现要点**：
  - 管线同 C1，附加：检查背包保护石（protect_stone 物品 ID）≥ 1，不足拦截提示。
  - 消耗时机（实现默认语义）：失败且该失败将产生惩罚（掉级/碎）时消耗 1 颗，惩罚豁免（等级不变/不碎）；成功或低段无惩罚失败不消耗（无惩罚可免，保护对象不存在）。
  - 产出配置归炼金/商店/活动路线（本路只消费物品引用）；protect_stone 为空 → /强化保护 返回不可用提示。
  - 回复标注：「保护石已消耗（免去掉级）」/「成功！保护石未消耗」。
- **验收标准**：高段失败扣 1 石且不掉级；成功不扣石；低段失败不扣石；无石拦截；protect_stone 为空不可用；消耗与等级写回同事务（B4 复用断言）。

---

## D. 边界与可选钩子（内容包选项 · 默认关）

### D1. 强化转移（内容包选项 · transfer_allowed 默认关）
- **定稿依据**：强化 §七（转移机制 content pack 可配 transfer_allowed: false 默认关；开启后 = 强化等级可转移到同部位新装备，消耗转移石/金币可配；我们自己的数据包不开放 → 装备淘汰 = 重新强化（强化石消耗循环 = 经济出口）；校验器：转移开关 + 转移消耗配置一致性）。
- **实现要点**：
  - 实现配置口与校验一致性，**不实现**转移指令/转移逻辑（默认关，无入口）。
  - transfer_allowed=true 时校验：转移消耗（转移石物品引用存在 / 金币）配置一致；开但无任何消耗 → 黄提示。
  - 编辑器与文档标注「我们不开」（转移关闭即本路最终形态）。
- **验收标准**：默认关下无任何转移指令入口（路由表断言）；开 + 合法配置通过校验；开 + 缺消耗黄提示；配置钩子存在（内容包可配项断言）。

### D2. 强化熟练经验（定稿边界登记 · 默认不产出）
- **定稿依据**：强化 §一（强化 = 数值堆叠层，无职业入口/无熟练出口；边界铁律：强化只加数字）；职业熟练度与生活系统设计定稿 §2.1（熟练经验来源 = 制作（合成/炼金/锻造）+ 采集 + 战斗，**不含强化**）；功能联动优化分析（「强化无职业、无熟练经验」= 已验证现状，强化定稿职业列：—）。
- **实现要点**：
  - 强化结算（成功/失败/保护/转移任一）**不写 proficiency 熟练值**；不新增强化 exp_sources 配置。
  - 全文登记开放项：「强化产出熟练经验」= 超出强化定稿的新需求；若拍板，走 proficiency.json exp_sources 扩展（另立路线实现，不在本路）。
  - 校验器默认不因「强化无熟练经验」告警（现状即定稿，非缺失）。
- **验收标准**：任意强化结算后 proficiency 熟练值不变（自动化断言）；开放项登记在规划/代码注释可见；无强化经验相关新配置产出。

---

## E. 校验与呈现

### E1. 强化校验器（全量规则）
- **定稿依据**：强化 §10.1 七条（①曲线单调不增 ②档位覆盖 0→max_by_rarity 最大值 ③碎装备模式：碎率超默认参考上限仅警告「碎率高挫败感强，确认？」；碎率 0 时无需开启 ④强化石档位引用 items.json 存在性 + 档位覆盖连续 ⑤转移/等级门槛开关与配置一致性 ⑥max_by_rarity 与数值表类型一致（flat/percent）⑦max_by_rarity 键须为 items.json 四档品质枚举，不得出现整数/R 口径）。
- **实现要点**：
  - 七条规则逐条实现：①按 to 升序 rate 单调不增检查 ②曲线覆盖 0→四档最高上限（默认传说 12）③shutter 开且 chance > shatter_max(5) → 黄警告；开且 chance=0 → 黄提示（无需开启）④stone_tiers 物品存在 + 区间连续 ⑤transfer_allowed/level_gated 与消耗/门槛配置一致性 ⑥values 各条目 type ∈ {flat,percent} 并与 max_by_rarity 各档兼容 ⑦max_by_rarity 键枚举黑名单（整数/其它编码直接报错）。
  - 全部警告级黄提示，不阻断加载；加载报告聚合输出。
- **验收标准**：七条各 1 违规用例被正确检出；警告文案含「碎率高挫败感强，确认？」；合法配置零告警；品质键非法被拒（#7）。

### E2. 编辑器与装备卡（强化设置页 · CSV · 显示）
- **定稿依据**：强化 §10.2（强化设置页：成功率曲线表 / 失败惩罚分级 / 碎装备开关 / 幸运开关 / 上限表 / 消耗表；CSV：成功率曲线表导出；装备卡：显示当前 +N 与上限）。
- **实现要点**：
  - 设置页六区编辑（读改写 enhance.json 同源）；失败分级 fail_tier_split 可配。
  - CSV 导出 = success_curve 表（to, rate 两列）。
  - 装备卡集成 B3 显示：{装备名}+N（上限徽标）+ 强化加成行。
- **验收标准**：六区读写回环；CSV 导出内容与配置曲线一致；装备卡正确显示 +N 与上限。

---

## 实施顺序建议（依赖拓扑）

1. A1 → A2 → A3 → A4（配置/数据层：加载 → 曲线 → 消耗 → 上限）；
2. B1 → B2 → B3（结算引擎：成功率 → 失败分级 → 数值应用），B4 事务包裹前三步与写回；
3. C1 → C2 → C3（指令：强化 → 信息 → 保护，均消费 B4 事务与 A/C 数据层）；
4. D1 / D2 独立：默认仅登记/配置钩子，无引擎实现，可并行落地；
5. E1 随 A 各数据任务完成后统一接入（加载期校验）；E2 随 C 指令后接入装备卡与设置页。

## 不变量（跨系统硬约束）

- 品质四档唯一注册表 = items.json（normal/fine/epic/legendary），max_by_rarity 只引用不重复定义；
- 结算防双扣统一走「SQLite 事务 + message_id 幂等 + version」模式（B4，与购买/奖励发放 V11 同款）；
- 强化禁批量 `*`（成功率随机，批量 = 连点爆装风险）；+N 的 + 为等级标记保持；
- 强化只加数字（攻击/防御 flat 或 %），不产生任何词条/形态/熟练经验；附魔已砍不复活；
- 强化石产出链路归炼金/商店（本路只消费物品引用，不重复实现产出）。