# 实现层规划 · 路2a 地图副本（QBot-TurnTellerRPG）

> 性质：实现层规划（新仓库从零重做，每功能可追溯定稿）
> 依据定稿（3 份，均位于 /root/docs_archive/RPG框架项目/）：
> - 《副本系统设计定稿》v1.0.1 → 简称【副本】
> - 《时间天气系统设计定稿》v1.0 → 简称【时间天气】
> - 《怪物模块设计定稿》v1.0.3 → 简称【怪物】（换区部分 §4.4 为本路线核心引用）
> 跨路线依赖：
> - 路1《规划_路1_战斗怪物.md》：T33 combo_state 生命周期、T40 PV 门禁、T42 条件行动引擎、T46 阶段表与 BOSS 编排、T48 掉落三类与 reward 入账、T49 图鉴 lore 与 codex_state、T22 变量大全互译（条件键）、T08 回合时序状态机——本路线换区/战斗续接/子任务奖励直接复用，不重复实现。
> - 路3《规划_路3_框架指令数据.md》：E7 世界内容六文件 schema（maps/dungeon/quest）、C1-C6 指令解析（/进入 /休息 /时间 /天气 登记）、F1-F4 loader/校验器接线、A4 存储基座（快照/副本会话持久化）——本路线只定义扩展字段语义，装载与校验走路3 框架。

---

## 〇、旧实现教训与防回归总则（路2a 的三面镜子）

本路线对应旧实现的三条死因，以「接线任务 + 回归防线」显式处置：

| 旧教训 | 死因 | 本规划对策 |
|---|---|---|
| 定时器推周期 | 季节/时段/天气用后台定时器刷新，重部署后漂移、跨群不同步 | M32/M34 懒计算+变化检测钩子（零定时器）+ M43 回归①（探针断言：周期值只能由锚点公式得出，无定时器驱动） |
| 天气真随机 | 抽签用真随机，同一时刻不同群/重启后结果不一致 | M37 确定性 seed（池键排序+tick）+ M43 回归②（同 tick 同池两次计算必同值） |
| 快照断裂 | 战斗中断续玩丢 combo_state/换区上下文，续玩=重打 | M27 快照完整性 + M43 回归③（中断→续玩状态字段断言：ai_state+combo_state+换区上下文一致） |

**铁律：周期值禁止定时器驱动；天气抽签必须确定性；任何续玩路径禁止丢状态。**

---

## 一、任务编号与批次依赖

- 编号：M01–M43，分 10 章；每条任务四字段：【任务名】【定稿依据:文件+章节】【实现要点】【验收标准】。
- 建议实施批次（依赖序，可并行组内任务）：
  1. 批次 0 数据骨架：M01、M07、M11、M16、M21、M31（装载器与配置全落地，依赖路3 E7/F 组）
  2. 批次 1 时间引擎：M32–M36（依赖 M31）
  3. 批次 2 天气引擎：M37–M41（依赖 M31、M32；M40 依赖路1 T22 互译表/路3 F2 注册表）
  4. 批次 3 地图行走：M02–M06（依赖 M01；M05 隐藏通道依赖 M40 条件键）
  5. 批次 4 刷怪：M08–M10（依赖 M32/M34 时间钩子、M38 天气池）
  6. 批次 5 副本两型与子任务：M17–M20、M22–M23（依赖 M01/M16；M20 依赖路1 T46）
  7. 批次 6 换区追击：M12–M15（依赖 M11 + 路1 T40/T42/T33）
  8. 批次 7 安全区/快照/死亡：M24–M30（依赖 M16 + 路1 战斗会话 + 路3 A4/C 组）
  9. 批次 8 集成回归：M42–M43（依赖全部）
- 路2a 完成总判据：M01–M43 全部验收通过，且 M42 端到端 + M43 三条回归全绿。

---

## 二、章1 网状地图（maps.json 通道）M01–M06

### M01 maps.json 通道四方向字段与装载
- 【定稿依据】【副本】§一 地图结构（每地图编辑 4 个方向：上/下/左/右的通向地图）；【时间天气】§三（该三字段同落 maps.json 图行）
- 【实现要点】maps.json 每图顶层新增 `exits: {up/down/left/right: map_id}`（每方向可空）；装载器（路3 F1）合并校验：方向引用地图存在、无指向自身；manifest 无新增模块名（无独立文件）。
- 【验收标准】默认地图配置可装载；exits 引用不存在的地图红拦（人话报错）；空方向 = 该方向无通道。

### M02 双向通道判定与行走
- 【定稿依据】【副本】§一 通道结构（双向 = 地图1↔地图2 互相有通道）
- 【实现要点】行走判定 = 当前图该方向 exits 存在；双向语义 = 目标图反向 exits 亦存在（两图互指）；行走 = 玩家位置切换 + 地图上下文切换（该图怪物场/采集/天气池/场地效果随之生效）。
- 【验收标准】双向通道可来回走；位置切换正确；目标图上下文（怪物/天气）按目标图重新解析。

### M03 单向通道与反向拦截提示
- 【定稿依据】【副本】§一 通道结构（单向 = 地图1→地图2 只有一边）
- 【实现要点】只配一边时仅单向可通行；逆向行走拦截并给出明确文案（不静默失败）。
- 【验收标准】单向边逆向 /进入 被拦且提示；正向通行正常；通道矩阵不因单向产生环校验误报。

### M04 捷径（单向门回环）与追击路径计算
- 【定稿依据】【副本】§一 通道结构（捷径 = 绕路连通：地图1→地图2 单向，地图2→地图3→地图1 绕回）+ §一 示例（熔岩坑道捷径、单向门）
- 【实现要点】捷径 = 通道矩阵表达的单向回环，无特殊字段；提供「当前区域 → 候选区域」最短可用路径函数（追击判定用，M13）；用于 /地图 类概览可标注「捷径」。
- 【验收标准】示例图（入口/回廊/核心/BOSS 房 + 坑道单向门 + 矿洞绕路）落成 maps 配置并通过；最短路径函数在含单向图上的结果正确。

### M05 隐藏通道（条件可见/可走）
- 【定稿依据】【副本】§一 通道结构（隐藏通道 = 满足条件才显示/可走，可选）
- 【实现要点】exits 单方向条目可选 `condition` 字段（复用条件引擎 {var:...}，见 M40）；条件不满足 → 该方向不可走且概览隐藏（或显示"？？？"）；满足 → 解锁提示。
- 【验收标准】条件不满足时 /进入 该方向被拦；满足后放行并提示「隐藏通道开启」；未满足时概览不泄露该方向存在（可配）。

### M06 /进入 <方向> 指令接线与地图切换钩子
- 【定稿依据】【副本】§一/§二（换区追击走 /进入 <方向>，空格分隔）；怪物 §4.4（/进入 <方向>，框架通道系统）；【时间天气】§三（怪物行同图加载）
- 【实现要点】指令解析（空格分隔 + 方向字面量 上/下/左/右，遵循《指令分隔符统一规范》）；切换钩子：离开图钩子（时段边界怪物移除判定 M09）+ 进入图钩子（补刷 M08、该图天气池刷新展示）；一次行走合并一条消息输出。
- 【验收标准】/进入 上|下|左|右 按通道矩阵切换地图；非法方向/无通道明确提示；每次行走触发「离开+进入」两钩子且顺序正确。

---

## 三、章2 刷怪（spawn / 补刷 / 时段）M07–M10

### M07 地图怪物配置与初始 spawn
- 【定稿依据】【时间天气】§三（maps.json 怪物行：enemy/count/respawn_minutes/active_time）；【怪物】§一（enemies.json 怪物定义）
- 【实现要点】maps.json monsters 行 → 玩家进入地图时按 count 生成场上怪物列表（enemy 引用 enemies.json）；场上怪物按地图维度存储（切图保留原图场）。
- 【验收标准】按配置数量生成；引用的敌人 ID 不存在红拦；切图后原图怪物列表保留。

### M08 补刷（懒计算，按时间差）
- 【定稿依据】【时间天气】§一 1.3（懒计算对齐框架 3.6 补刷：玩家操作时按时间差补刷，零定时器）+ §三（respawn_minutes 字段）
- 【实现要点】每次指令/进入地图时，按当前时间与上次刷新时间之差折算补刷：场上存活不足 count 时按 respawn_minutes 间隔补至上限（上限 = count）；不存定时器、不存刷新历史。
- 【验收标准】离线 1 小时回图，怪物补至配置上限；无任何指令时零后台任务；补刷数量 = floor(时间差/respawn_minutes) 截断到缺口。

### M09 时段/季节限定出没与边界移除
- 【定稿依据】【时间天气】§三（怪物行 seasons/periods，空 = 全年/全天）+ §4.3（3.16 出没时段边界规则同样适用：季节/时段结束 → 场上怪物移除，「对方逃跑了」）
- 【实现要点】怪物行限定过滤 = active_time（现实钟点）与 seasons/periods（游戏周期）AND 叠加，全部满足才出没；周期边界变化（M34 钩子）产出怪物存在性变化：新增补刷/移除在场怪并提示「对方逃跑了」。
- 【验收标准】非限定季节/时段不刷该怪；时段边界把已在场限定怪移除并提示；季节边界同理；三条件 AND 语义正确。

### M10 天气权重刷新（只影响刷新，不驱逐在场）
- 【定稿依据】【时间天气】§4.3（weather_weights 作用于刷新计时：有效刷新间隔 = respawn_minutes/weight；0 = 该天气不刷；只影响刷新/补刷，不驱逐已在场怪物）+ §1.5（天气/季节不驱逐战斗中怪物）
- 【实现要点】刷新计时按当前图当前天气（M38）计算折算；在场怪物永不被天气驱逐（仅 M09 时段/季节边界驱逐）；weight 仅作用于补刷结算。
- 【验收标准】weight ≥1 时同时间差补刷更快；weight=0 的天气下不补刷但已在场怪保留（不驱逐）；战斗中天气变化不影响当前战斗怪物。

---

## 四、章3 换区追击 M11–M15

### M11 zone_change 配置与装载
- 【定稿依据】【副本】§二（换区条件每 BOSS 独立可配；未配置 = 不换区）+ §四 配置示例（zone_change: {pv_recover: 0.5}）；【怪物】§4.4（换区触发语义）
- 【实现要点】BOSS 引用行支持 `zone_change: {trigger:{type,value}, targets:[区域...], pv_recover}`（落 enemies.json 或 dungeon.json，见定稿示例）；装载器合并校验：trigger 类型 ∈ 枚举（hp_below/阶段切换/特定行动后/自定义）、targets 引用地图存在、pv_recover ∈ [0,1]。
- 【验收标准】无 zone_change 的 BOSS 不触发换区；targets 引用不存在红拦；pv_recover 越界红拦；trigger 枚举非法红拦。

### M12 换区触发与随机候选逃跑提示
- 【定稿依据】【副本】§二（血量阈值/阶段切换/特定行动后/自定义；BOSS 随机逃到候选之一；提示「BOSS 逃向了 XX 区域」）；【怪物】§4.4（残血才会换区，逻辑闭环）
- 【实现要点】战斗每回合结算后检查 trigger（hp_below 等，复用路1 T42 条件行动引擎语义）；触发时从 targets 等概率随机选一（随机种子入战斗会话可重放）；广播逃跑提示含目标区域名 + 「防护值恢复了一半」预告。
- 【验收标准】残血阈值（如 <30%）触发；不残血不触发；候选多个时随机选一且可重放；未配置候选 = 不换区。

### M13 追击路径行走与错失窗口
- 【定稿依据】【副本】§二（玩家 /进入 <方向> 走通道追击，网状路径选择；地图熟悉度 = 追击资源：熟悉=快追到，不熟=绕路/错过窗口）+ §三 3.1（探索版提前熟悉）
- 【实现要点】换区后 BOSS 位置 = 目标区域；玩家逐区 /进入 追寻（M06）；错失窗口判定可配（如：换区后 N 步内未到达候选区 → 提示「追丢了」，BOSS 可再次换区或进入决战触发点）；追击路径上按配置遭遇精英/守门怪；探索版子任务完成度可作为「熟悉度」提示来源（信息即资源，不硬约束）。
- 【验收标准】走最短路径可追到并进入 M14 续战；绕路触发错失窗口提示；窗口参数（步数/路径偏差）可配；追击途中精英遭遇按配置发生。

### M14 换区后战斗续接（残血保持 + PV 半恢复 + 开场技）
- 【定稿依据】【怪物】§4.4（血量保持残血不重置；PV 恢复一半向下取整；换区后第一回合开场技）+ §7.1 ④（battle_start 触发含换区后第一回合）；【副本】§二（血量保持残血 + PV 恢复一半 + 开场技）
- 【实现要点】追到后**不新建战斗实体**：同一战斗会话续接，BOSS HP 保持残血原值；PV 恢复 = floor(PV×zone_change.pv_recover)（默认 0.5，向下取整），提示「防护值恢复了一半」；追到后第一回合触发开场技（battle_start 特殊行动）。
- 【验收标准】续接后 BOSS 血量与换区前一致（不重置）；PV 恢复至 50% 档且向下取整；第一回合为开场技；无 pv_recover 配置则 PV 不恢复；换区 PV 半恢复不产生重复掉落的副产品。

### M15 离开副本重置界定（非战斗离开）
- 【定稿依据】【副本】§二 退出规则（非战斗状态离开副本区域 = 副本重置，下次满状态重打；战斗进行中退出 = 快照续玩走 M27）
- 【实现要点】离开判定 = 非战斗 + 已离开副本区域（不依赖任何计时器）；重置 = 清副本会话（进度/BOSS 状态/子任务进度）；恢复入口明示「战斗中断可续玩，离开副本将重置」。
- 【验收标准】非战斗离开后重进 BOSS 满状态且入场限制正常结算；战斗中断离开不触发重置（走 M27）；文案在恢复入口可见。

---

## 五、章4 副本两型（探索版 / BOSS 版）M16–M20

### M16 dungeon.json 两型结构装载
- 【定稿依据】【副本】§四（type: dungeon_explore / dungeon_boss；maps/boss/difficulty/recommended_level/entry_item/entry_limit/safe_zone/rest_per_dungeon/rest_auto/checkpoints/death_policy/zone_change/drops）
- 【实现要点】dungeon.json schema 按定稿全字段落地（路3 E7 schema 扩展）；两型共字段 + BOSS 型独有字段（boss/zone_change/checkpoints/death_policy）；缺省值齐全（缺省 = 入口安全区、手动休息、checkpoint 回退、BOSS 保留等）；装载器校验引用。
- 【验收标准】两种 type 均可装载；缺省字段有文档化默认值；boss/entry_item/checkpoints 等引用不存在红拦；探索型配置 boss 字段 → 黄提示忽略。

### M17 探索/BOSS 地图共用与 BOSS 房守门怪
- 【定稿依据】【副本】§四 地图共用说明（两组副本引用同一组地图 ID；探索版地图配置完整保留；BOSS 版 = 同一地图 + 额外 BOSS 房 + 守门怪；地图机制两副本共用）
- 【实现要点】副本 maps = maps.json 的 id 引用列表；BOSS 房 = 单独地图 ID 仅 BOSS 版引用；守门怪配置（BOSS 房前/路径上）；场地效果（落石等）字段两型共用不复制。
- 【验收标准】探索版不含 boss 房；BOSS 版含 boss 房 + 守门怪；两型共享地图配置（同一地图 ID 一处定义两处引用）；改地图配置两型同步生效。

### M18 探索版副本流程（练习赛）
- 【定稿依据】【副本】§一（探索版 = 同一地图的练习赛，无 BOSS：熟悉通道/捷径/机制，为正式赛准备）+ §三 3.1（地形/通道/机制/怪物分布/宝箱/机制预告学习）
- 【实现要点】探索版无 BOSS、无换区；完整走图无战斗压力；通关条件可配（完成全部/部分子任务 或 走完全图）；子任务驱动探索（M21-M23）；机制预告（如「前方有灼热气息」）在探索中展示。
- 【验收标准】无 BOSS 地图可全通；通关判定按配置达成；机制预告文案在对应区域展示；探索版不触发换区/不产生 BOSS 掉落。

### M19 BOSS 版流程（正式赛 + 入场限制）
- 【定稿依据】【副本】§一（BOSS 版 = 一场完整战斗：进入 → 探索推进 → 精英遭遇 → BOSS 换区 → 决战）+ §四（difficulty/recommended_level/entry_item/entry_limit）
- 【实现要点】流程编排 = 进入（入场限制校验：entry_item 消耗/entry_limit 每日次数，可配 null=不限）→ 探索段（可跳过）→ 精英遭遇 → BOSS 战 → 换区追击（M11-M14）→ 决战；推荐等级展示（提示不拦截）。
- 【验收标准】完整流程可走通；入场限制生效（无钥匙/超次数拦截）；推荐等级在入口展示；流程状态机各阶段转换正确。

### M20 BOSS 三阶段机制与换区联动
- 【定稿依据】【副本】§五（阶段 1 100-60% 常规 / 阶段 2 60-30% 狂暴 + 换区 PV 恢复一半 / 阶段 3 30-0% 绝境：大招循环 + 印记反制 + 连招锚点加速）；【怪物】§十（BOSS：PV 200-500、行动表 4-6 招、特殊行动 2-4 个）
- 【实现要点】阶段表按血量阈值（复用路1 T46 阶段表引擎）；阶段切换 = 行动表强化/新增招/换区联动；阶段 2 才允许换区触发（与 M11 trigger 可配叠加）；阶段 3 大招循环 + 印记反制（依赖路1 T35-T38 印记）。
- 【验收标准】三阶段血量边界（100-60/60-30/30-0）正确触发；阶段切换有提示；换区只发生在配置阶段；阶段 3 大招循环与印记消除博弈可发生。

---

## 六、章5 副本子任务五形式 M21–M23

### M21 子任务五形式配置（quest.json 引用）
- 【定稿依据】【副本】§3.2（五形式：探索/收集/清剿/机制学习/情报，落 quest.json）
- 【实现要点】dungeon.json subquests 列表引用 quest.json 任务；类型字段五值：explore（探索）/collect（收集）/clear（清剿）/mechanic（机制学习）/intel（情报）；进度跟踪挂在副本会话内（M30）。
- 【验收标准】五形式均可配置并按类型解析；引用不存在的 quest 红拦；同一条目缺 type → 红拦。

### M22 五形式进度判定
- 【定稿依据】【副本】§3.2（到达指定区域 / 采集地图特产 N 个 / 击败该图精英 / 触发地图机制 N 次 / 解锁图鉴情报）+ §3.1（机制：落石/陷阱/机关/场地效果）
- 【实现要点】explore = 到达指定区域计数；collect = 采集点采集计数（与采集系统联动）；clear = 击败指定精英计数；mechanic = 触发地图机制计数（落石/陷阱/机关）；intel = 图鉴解锁进度（codex_state，路1 T49）；进度随副本会话持久化，退出续玩不丢。
- 【验收标准】各形式完成条件可达成并结算；进度跨退出/重启保留；intel 形式与图鉴解锁状态一致。

### M23 子任务奖励与可选性
- 【定稿依据】【副本】§3.2（奖励：经验/金币/素材/副本入口道具/图鉴进度）+ §八 风险表（子任务可选：奖励驱动，不做也能打 BOSS）
- 【实现要点】奖励条目走统一 reward 管线入账（路1 T48）；子任务完成度不阻塞 BOSS 战入口（可选）；完成奖励与通关奖励分账（副本 §六）。
- 【验收标准】奖励正确入账（经验/金币/素材/道具/图鉴）；未完成子任务不影响 BOSS 版进入；重复挑战奖励重置语义按副本 §六。

---

## 七、章6 安全区 / 休息 M24–M26

### M24 安全区定义与可用性判定
- 【定稿依据】【副本】§2.1（安全区 = 进入副本的起始区（入口区）+ dungeon.json 标记 safe_zone:true 的特定区域；副本内唯一恢复点）+ §四（safe_zone 缺省 = 入口区，可配多区域）
- 【实现要点】dungeon.json safe_zone 数组，缺省 = [入口区]；/休息可用性判定 = 当前位置 ∈ 安全区 且 非战斗 且 非 BOSS 房。
- 【验收标准】仅安全区可用 /休息；多安全区配置生效；BOSS 房/普通区域/战斗中被拒绝并提示。

### M25 /休息 副本内语义（恢复 / 冷却缩减 / 次数限制）
- 【定稿依据】【副本】§2.1（HP/MP 部分恢复默认各 20% 可配倾向百分比；冷却缩减：当前冷却中技能/特效冷却 −N 回合默认 3；次数：计入 /休息 每日 3 次/天可配 0=不限随存档持久化；rest_per_dungeon 每副本上限；rest_auto 进入自动结算一次默认关）；语义对齐效果系统定稿 §3.1（副本 §2.1 引用）
- 【实现要点】恢复数值走配置（百分比倾向）；冷却缩减作用于战斗冷却表；每日次数随存档持久化并按日界重置；rest_per_dungeon/rest_auto 两个可配项落地。
- 【验收标准】20% 恢复正确；冷却 −3 回合生效；每日第 4 次被拦；rest_per_dungeon=1 时同副本第 2 次被拦；rest_auto=true 进安全区自动结算一次。

### M26 休息 ≠ 离开副本（不重置、快照保留）
- 【定稿依据】【副本】§2.1（安全区休息不改变位置、不退出副本、不触发重置；副本进度/BOSS 血量/快照 ai_state+combo_state 全程保留）+ §二（仅非战斗状态离开副本区域触发重置）
- 【实现要点】/休息 不进入离开判定（与 M15 互斥）；休息后的继续推进不受影响。
- 【验收标准】休息后副本进度、BOSS 血量、快照原样；不触发重置；休息后 BOSS 房状态未受休息影响。

---

## 八、章7 快照续玩 / 退出 / 死亡 M27–M30

### M27 战斗快照续玩（ai_state + combo_state 全保留）
- 【定稿依据】【副本】§二 退出规则（战斗进行中退出/超时/锁屏 = 战斗快照续玩，ai_state+combo_state 全保留，套内也可续）+ §六（中途退出保存进度，下次从换区点继续）
- 【实现要点】战斗会话序列化：ai_state（我方）+ combo_state（连段）+ BOSS 状态（HP/PV/阶段/换区上下文）+ 战斗场地；恢复入口明示「战斗中断可续玩，离开副本将重置」；续玩 = 原会话恢复继续（套内/跨重进均可）。
- 【验收标准】中断后重进，回合/连段/HP/PV/换区上下文与中断前逐字段一致；超时/锁屏同路径；续玩后战斗正常推进无状态残留。

### M28 非战斗离开重置与明示文案
- 【定稿依据】【副本】§二 退出规则（非战斗状态离开副本区域 = 副本重置，下次满状态重打；恢复入口明示）
- 【实现要点】离开判定 = 非战斗 + 已出副本区域；重置 = 副本会话与 BOSS 状态清空；入口文案常驻展示。
- 【验收标准】非战斗离开后重进满状态重打；战斗中断离开不触发重置（与 M27 分工明确）；明示文案在入口/恢复处可见。

### M29 副本内死亡处理（复活点 / checkpoint / BOSS 状态 / 虚弱）
- 【定稿依据】【副本】§2.2（死亡 ≠ 战斗中断；复活点默认副本入口可配；三档惩罚：checkpoint 回退（默认，未配置 auto 降级重置）/ 重置 / 无惩罚；掉落/素材/已领奖励保留；boss_state: keep（默认）/reset；复活后虚弱 1 分钟可配，禁入非安全区含 BOSS 房）
- 【实现要点】死亡结算入战斗败北路径（路1 T08 死亡判定衔接）；按 death_policy 结算；checkpoints 列表记录区域快照（已清区域/BOSS 血量回退）；虚弱状态（框架 3.12 语义）期间禁入限制；复活点即死亡后安全区可 /休息（次数限制 M25）。
- 【验收标准】默认 = 入口复活 + checkpoint 回退 + BOSS 残血保留；三档可配且各自行为正确；checkpoint 未配置时回退自动降级为重置；虚弱期间进 BOSS 房被拦；死亡后主动离开 = 重置。

### M30 副本进度会话持久化
- 【定稿依据】【副本】§六（中途退出保存进度，下次从换区点继续）+ §2.1（快照 ai_state+combo_state 保留）；【时间天气】§六（对齐框架 3.7 存档：每指令处理/战斗结算后事务保存，含 content_pack_id+version）
- 【实现要点】副本会话（当前区域/已清区域/子任务进度/换区上下文/BOSS 状态/安全区休息次数）随玩家存档持久化（路3 A4）；清理时机 = 通关/重置；存档含 content_pack_id+version 防跨包串档。
- 【验收标准】重启后副本会话可恢复（从换区点/checkpoint 继续）；通关与重置均正确清理；跨内容包不读错会话。

---

## 九、章8 时间引擎（四季五时段 / 锚点 / 懒计算）M31–M36

### M31 time_cycle 配置与系统总开关
- 【定稿依据】【时间天气】§二（enabled/season/period/weather/broadcast/combat 全段）+ §0 用户原话（季节默认 7 天整数≥1；时段默认 60 分整数≥30；天气默认 60 分整数≥30）+ §8.1 V1-V3
- 【实现要点】settings.json 新增 time_cycle 段（全局唯一配置，3.8 全局世界）；enabled=false → /时间 /天气 提示未启用、条件键 [季节]/[时段]/[天气] 失效；V1-V3 硬校验（season_days ≥1、period_minutes/weather_minutes ≥30 均为整数，人话报错）。
- 【验收标准】默认配置（7 天/60 分）可装载；非法值红拦人话报错（「季节天数要填整数，最少 1 天」等）；总开关关闭后条件键与查询指令全部失效提示。

### M32 锚点与三周期懒计算
- 【定稿依据】【时间天气】§1.3（锚点 ANCHOR = 2000-01-01 00:00:00 UTC+8；season_idx/period_idx/weather_tick 整除公式；不存历史、不跑定时器）+ §9.1（默认参数时间轴示例）
- 【实现要点】ANCHOR 常量 + 三个纯函数：season_idx = floor((now−ANCHOR)/(season_days×86400))%4、period_idx = floor((now−ANCHOR)/(period_minutes×60))%5、weather_tick = floor((now−ANCHOR)/(weather_minutes×60))；任何时刻可重算；缓存索引仅供变化检测，不参与计算。
- 【验收标准】单位测试：默认配置下 2026-08-16 为夏；指定时刻三值与手算一致；跨日/跨季/跨年不溢出；全仓无定时器驱动周期值（探针）。

### M33 三周期独立推进与对齐规则
- 【定稿依据】【时间天气】§9.2（各自整除、共锚不共界、不重置不补算、边界重合=自然现象）+ §1.3（配置变更即重排：旧缓存失效自动重算 + 黄提示「时间配置已变更，周期重新铺排」）
- 【实现要点】三周期互不读写对方状态（实现零耦合）；时段跨季节边界不重置不补算；改配置 → 后续周期按新配置推进 + 黄提示；播报合并上限 3 条、顺序固定（此条 M34）。
- 【验收标准】45 分钟时段配置下换季时刻时段不重置；同一时刻多周期同时变化自然发生；改 season_days 后重算正确且黄提示出现。

### M34 变化检测钩子与懒广播
- 【定稿依据】【时间天气】§1.3/§1.5/§5.2（每条指令处理前 check_changes(player, map_id)；广播默认关、懒模式；播报顺序固定 季节→时段→天气；离线跨多周期只播最新；不打断战斗、战斗结算后补播；跨群去重）
- 【实现要点】指令钩子比较世界缓存索引与公式重算值产出变化列表；broadcast.enabled 默认 false（仅记录不播报）；战斗中变化不打断（战斗会话每回合开始重读当前时段/天气，仅数值与状态行提示，播报挂战斗结算后）；time_state 去重跨群不重复。
- 【验收标准】一次指令最多合并播报 3 条且顺序固定；离线跨多周期只播最新值；战斗中被变化不中断战斗；开启广播后按配置播报且跨群不重复。

### M35 world_state.time_state 存档
- 【定稿依据】【时间天气】§六（season_idx/period_idx/weather_tick/map_weather_seen；3.7.1 持久化语义表「② 非会话持久状态」新增行；每指令处理/战斗结算后事务保存；缺失字段缺补默认/多忽略）
- 【实现要点】字段落 world_state.time_state；map_weather_seen 惰性增长（仅记录访问过的图）；换包迁移 = 字段级（缺补默认/多忽略）；去重 = 比较缓存索引与重算值相等不播。
- 【验收标准】重启后缓存索引恢复且与重算一致；map_weather_seen 只含访问过的图；跨内容包迁移不崩。

### M36 /时间 /天气 查询指令
- 【定稿依据】【时间天气】§5.1（当前季节距下次 X 天 / 时段距下次 X 分钟 / 天气距下次 X 分钟；未注册提示「请先 /注册」；只报状态不教规则；/天气 得当前图天气 + 生效池标注覆盖/默认 + 距下次变化 + 天气联动摘要不教具体数值）
- 【实现要点】指令表（路3 C 组）登记 2 行；/时间 输出三行（季节/时段/天气 + 倒计时）；/天气 读当前图生效池（覆盖/默认标注）+ 该图联动摘要（有天气修正的怪物/采集点提示，不教数值）。
- 【验收标准】输出格式与定稿 §5.1 示例一致；倒计时计算正确；未注册玩家被拦提示；/天气 对覆盖池图标注「使用本图天气池」。

---

## 十、章9 天气引擎（等概率 / 条件键）M37–M41

### M37 天气池等概率确定性抽签
- 【定稿依据】【时间天气】§1.4（每次变化从生效池等概率抽一个；seed = sha256(生效池键列表排序 + str(weather_tick))；确定性伪随机跨群/进程/重启一致）+ §二（default_pool 非空、键唯一、键+中文名+emoji 齐全）+ §8.1 V4
- 【实现要点】map_weather(map_id, weather_tick) = 生效池[pick(seed)]；等概率抽签（权重 v1 不做，预留）；池任一 tick 计算确定性；V4 硬校验（池非空、键唯一、键+名齐全）。
- 【验收标准】同 tick 同池两次计算同值；重启后抽签序列一致；池增删 → 序列整体重排 + 黄提示；空池/重复键红拦。

### M38 地图天气池覆盖
- 【定稿依据】【时间天气】§三（maps.json 顶层 weather_pool；缺省/空数组 = 用默认池；元素必须 ∈ 注册键，V5 硬拦）+ §1.3（天气取值 = 生效池抽签）
- 【实现要点】每图可选 weather_pool（只引用默认池已注册键）；map_weather 优先覆盖池，无覆盖 → 默认池；V5 校验「该图天气『雪』没有在默认天气池里注册」。
- 【验收标准】覆盖图取覆盖池值；未覆盖/空数组图用默认池；引用未注册键红拦；同一 tick 不同图可不同天气（变化时刻全局一致）。

### M39 天气消费方联动
- 【定稿依据】【时间天气】§4.2（采集点 weather_mods：rate_mult/rarity_shift，0=该天气不出）+ §4.3（怪物 weather_weights 已进 M10）+ §4.1（钓鱼 seasons/periods 过滤 + 鱼王事件可选 weather 条件）+ §4.5（图鉴 lore condition 复用条件引擎）+ §4.6（战斗天气修正 combat.weather_mult：默认关、每回合开始读天气、不改 formula 本体）
- 【实现要点】各消费方引用三键零新增机制（配置组合，M40）；weather_mods 作用于采集出率与稀有度档位平移（clamp 4 档）；lore condition 按当前图天气判定显示；combat.weather_mult 默认 false，开启后每回合开始读当前天气、战斗中途变化下回合生效 + 状态行一次提示，不打断战斗；不改伤害公式本体。
- 【验收标准】雨天采集出率 ×1.5 且稀有度 +1（配置后）；天气限定的 lore 情报按天气显示/隐藏；战斗天气修正关闭时伤害与公式完全一致；开启后倍率按当前图当前天气生效。

### M40 条件引擎三键注册（season / period / weather）
- 【定稿依据】【时间天气】§4.0（互译表新增 3 行：{var:"season"/"period"/"weather", param:X}；季节/时段为全局值，天气按玩家当前所在图取值；消费方零新增机制）+ §1.2（键格式英文小写、季节五时段枚举固定写死，防碎片化）
- 【实现要点】条件引擎注册表（路1 T22 变量大全/路3 F2 注册表）新增三键；枚举固定：spring/summer/autumn/winter、dawn/noon/dusk/night/midnight；weather 上下文 = 玩家当前所在图（{var:"item_count"} 同构）；互译表中英文登记；V6 校验消费方枚举引用 ∈ 注册表。
- 【验收标准】任务/NPC/签到活动/副本事件/成就可引用三键判定正确；[天气:X] 按所在图判定（不同图同条件结果不同）；非法枚举（如「旱季」）红拦；三键在条件编辑器可选取。

### M41 校验器 V1-V8 汇总接线与黄提示
- 【定稿依据】【时间天气】§8.1（V1-V8 硬校验：V1 season_days ≥1 / V2 period_minutes ≥30 / V3 weather_minutes ≥30 / V4 池非空键唯一 / V5 地图池引用 ∈ 注册键 / V6 消费方枚举引用 / V7 combat mults 键 ∈ 注册集 / V8 broadcast.template 占位符）+ §8.2（黄色提示：365 天/1440 分/池 >12 种/单种池恒定/无消费方引用/池配置变更重排）
- 【实现要点】校验规则接入路3 F3/F4 校验器管线（红拦五类 + 黄提示 + 人话模板）；V1-V8 全落地；黄提示五条落地（不拦截）；编辑器 UI 属设计层工具（时间天气 §七），本路线仅提供字段元数据（表单驱动的数据层输入），UI 不在本路线范围。
- 【验收标准】V1-V8 各自触发正确人话报错；黄色提示触发但可保存；消费方非法引用全网红拦；字段元数据可供编辑器页生成表单。

---

## 十一、章10 集成与回归 M42–M43

### M42 路2a 端到端集成冒烟
- 【定稿依据】【副本】§一（整体体验单元：进入 → 探索推进 → 精英遭遇 → BOSS 换区 → 决战；探索版 = 练习赛）+ 【时间天气】§一（每条指令前时间钩子）
- 【实现要点】全链路脚本：进 BOSS 版（入场限制）→ 可选子任务 → 精英 → BOSS 战（三阶段）→ 残血换区 → /进入 追击 → 续战（残血+PV 半恢复+开场技）→ 决战 → 通关奖励；探索版全通；全程时间/天气钩子生效（时段边界移除/天气补刷）。
- 【验收标准】两条副本路径（探索版/BOSS 版）端到端脚本跑通；随机种子固定可重放（换区去向/抽签可复现）；时间/天气在全程各指令处正确取值。

### M43 回归防线（零定时器 / 确定性 / 快照完整性）
- 【定稿依据】【时间天气】§1.3（零定时器）、§十二 风险表（确定性抽签为懒计算刚需）；【副本】§二（快照全保留 ai_state+combo_state）
- 【实现要点】三条回归断言：① 周期值只能由锚点公式得出（探针：全仓无定时器写入周期值）；② 同 tick 同池两次 map_weather 同值、重启不重抽；③ 战斗中断 → 续玩，ai_state+combo_state+换区上下文逐字段一致。
- 【验收标准】三条回归探针全绿；任何新功能合入前必须通过 M43 三条回归（对齐路1 T53 铁律）。

---

## 十二、路2a 完成总验收

- M01–M43 全部验收通过（每任务【验收标准】逐条核对）。
- M42 端到端（探索版 + BOSS 版）冒烟全绿；M43 三条回归全绿。
- 覆盖清单核对：网状地图（M01-M06）· 刷怪 spawn/补刷/时段（M07-M10）· 换区追击残血+PV 半恢复+血量不重置（M11-M15）· 副本探索/BOSS 两型（M16-M20）· 子任务五形式（M21-M23）· 安全区 /休息（M24-M26）· 快照续玩（M27-M30）· 时间引擎四季五时段/锚点/懒计算（M31-M36）· 天气引擎等概率/条件键（M37-M41）· 集成回归（M42-M43）。