# 任务书 · 专项·引擎文案2 接线（表已就绪，只差代码改写）

> 2026-09-12 · 消息模板重构战役 · 执行者：子 agent
> **前情**：上一路已把 73 个新键写好并合入本工作树的表（`spec_env2.json`，表 786 条），
> 但**代码改写未做**。本任务 = 只做「把引擎/壳层的硬编码文案换成表键调用」+ 测试同步 + 全量 + commit。
> 文案**已定稿在表里**，你**不要**改文案（除非发现明显排版问题，需在报告里说明理由）。

## 1. 先看这些（只读，别猜）

- `docs/消息模板重构/_fragments/spec_env2.json` —— 73 键 + `notes`（逐键写了旧硬编码出处/调用链），
  这是**键 ↔ 位置映射的唯一来源**。
- `docs/消息模板重构/00_方案与规范_v1.md` —— 排版口径。

## 2. 要做的事

1. 按 fragment `notes` 逐处把**硬编码玩家可见中文字面量**替换为 `tpl_of(ctx, "<键>", {...})`：
   涉及文件（以 notes 为准）：`qbot_rpg/core/alchemy_battle.py`、`qbot_rpg/core/alchemy_core.py`、
   `qbot_rpg/core/alchemy_deep.py`、`qbot_rpg/core/alchemy_harvest.py`、`qbot_rpg/core/alchemy_helper.py`、
   `qbot_rpg/core/alchemy_settle.py`、`qbot_rpg/core/investigate.py`、`qbot_rpg/core/hidden_trigger.py`、
   `qbot_rpg/commands/achievement_commands.py`（「状态：」拼接行）。
2. **引擎纯函数拿不到 ctx 时**：允许「表默认值 + 调用方注入覆盖」两级（参考 `core/environment_lore.py`
   已落地的写法：给函数加可选 `ctx` 形参，内部 `tpl_of(ctx, ...)`，无 ctx 时用 `DEFAULT_TEMPLATES[key]`），
   但报告里要逐处写清是哪一种。
3. 测试同步：旧文案逐字断言改新文案（**禁止**把文案改回硬编码）。已知断言点（以 grep 为准）：
   `tests/unit/test_alchemy_battle.py`、`test_alchemy_helper.py`、`test_resource_commands.py`、
   `test_alchemy_settle.py`、`test_hidden_trigger.py`、`test_investigate.py`、
   `test_alchemy_harvest.py`、`test_alchemy_commands.py` 等。
4. 自校验：`scripts/check_template_width.py`（0 FAIL）+ `pytest tests/unit/test_template_table.py
   tests/unit/test_template_table_final.py tests/unit/test_emoji_discipline.py -q` +
   定向（上面各模块）+ **全量** `pytest tests/ -q -o addopts=""`（报计数）。
5. `ruff check <改动文件>` 干净；commit（`feat(专项·引擎文案2): …`）。**禁止 push / 禁止动 main**。

## 3. 报告（回复正文内联）
- 「旧硬编码（文件:行）→ 新表键」对照表（逐处）；
- 改动文件清单；checker 输出；定向+全量计数原文；**仍保留兜底常量的清单**；commit hash；
- 未完成/存疑（如实）。
