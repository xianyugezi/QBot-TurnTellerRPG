# 深度打造 · 设计文档待裁决合订本（2026-09-19）

> 来源 5 份设计文档（均在 `/root/deliverables/`）：
> `符文系统_实现口径.md`（12 条）· `深炼金相性接入_设计.md`（11 条）· `装备被动_实现口径.md`（12 条）·
> `淬炼与分解回收_实现口径.md`（8+ 条）· `打造_材料与图纸内容草案.md`（9+ 条）。
>
> **说明**：标 ✅ **已裁定**的 = 主 agent 依你"数值口径你决定 / 你判断就行"的授权直接定（**全部可配、随时可改**）；
> 标 🟡 **需你拍板**的 = 属**玩法形态/经济规模**，不该由实现者默认。
> 你只需看 🟡 那部分（约 15 条），✅ 部分当作"我们已经这么干了，你不满意就说"。

---

## 一、总览

| 组 | 主题 | 条数 | 何时必须定 | 需你拍板 |
|---|---|---|---|---|
| **A** | 经济规模与材料密度（cap / 材料 / 精粹） | 12 | **批 44（淬炼回收）与数值校准批前** | 🟡 4 条 |
| **B** | 符文玩法口径 | 12 | 批「符文」前 | 🟡 5 条 |
| **C** | 装备被动口径 | 12 | 批「被动」前 | 🟡 3 条 |
| **D** | 深炼金侧相性玩法 | 11 | 细分炼金时 | 🟡 3 条 |
| **E** | 工程口径（实现者已默认） | ~8 | — | ✅ 0 条 |

---

## A · 经济规模与材料密度（🟡 4 条需你定）

| # | 争点 | 我的建议 | 为什么需要你 |
|---|---|---|---|
| **A-1** 🟡 | **cap 语义与量级**（决策记录 N3 的"品质预算"口径 vs B 路"材料价值 1.5 倍"口径，**彩档相差 20 倍**） | 取 **`cap = 等级承载成本 + 品质预算`**，暂定 **铜 900 / 银 2100 / 金 4200 / 彩 8600** | **直接决定全系统经济规模**；且需你答一句："**彩档要不要保证品质 10 可达？**"（要 → 12 种材料下需 ≈19500 cost 或**扩充彩档材料表**） |
| **A-2** 🟡 | **每档材料密度**（样例里金档只有 2 种 → **金档品质天花板 q5 反而低于银档 q6**，不单调） | **每档 ≥6 种**，价格跨度覆盖该档 cap | 材料表是你内容侧的活；密度比数值旋钮更决定天花板 |
| **A-3** 🟡 | **精粹是否可交易 / 可否重置 / 分解是否返还相性材料**（三问） | **不可交易**（绑定货币）· **可重置但不返还**（避免"一次误点毁毕业装"）· **不返还相性材料**（返还原型材料已等价返还相性） | 影响 RMT 风险与挫败感；且"可重置"会改变"每件投入"的经济含义 |
| **A-4** 🟡 | **是否引入"材料合成"（低阶合高阶）** | **建议引入**，作为公用合成层的子用法（能补 A-2 的密度） | 会改变经济与 cap 语义，需你点头 |

**✅ 已裁定（我定的默认）**：精粹载体=**货币**（仿 gem，天然不可交易）· 两套 rate **分键**（`decompose_rate`=材料回收 / `essence_rate`=精粹产出，**语义相反不可合键**）· 分解走**新建实例级路径**（现 `/分解` 按 `item_id`，**分解不了 uid 实例**）· 淬炼落 `enhance.json` 的 `temper` 段 + 新引擎 `core/temper.py` · 淬炼状态挂 `ItemInstance` 新字段（**必须逐处核对 6 个构造点**）· 品质经验标度 `quality_exp_scale=10` · 件数边际按公式（step 0.25，第 5 件 0 收益）· 相性互动 冲突 −0.25 / 增幅 +0.15 · 经验互动新增 `exp_mult` 字段（保持唯一源）

**实现者揪出的 3 处上游不自洽（已修正并标注）**：B 路"件数边际 n=5→3.75"与其公式矛盾（实得 2.5）· B 路品阶基数与阈值不同标度（需乘 scale）· N3 cap≈4200 与 B 彩档≈86000 量级差 20 倍。

---

## B · 符文（🟡 5 条需你定）

**✅ 已定的工程口径**：孔位/镶嵌**复用 `core/jewel.py`**（不用新造）· 三阶符文**独立刻度**（≠既有四档品质）· 3 合 1 **复用既有执行器形状**（禁跳级/原子提交）· 镶嵌状态**挂 `uid` 落档容器**（挂 `EquipmentSlot` 会丢）· **副手失活零额外工作**（批38 已留接口）。

| # | 争点 | 我的建议 |
|---|---|---|
| **B-1** 🟡 | **阶与等级的对应**（无门槛 / 需装备等级 / 按图纸档决定开孔数） | 无门槛（三阶只看符文自身）+ 孔数按图纸档/品质，**两者分开** |
| **B-2** 🟡 | **3 合 1 成功率与失败处理** | **必成**（对齐既有 `_exec_jewel`），消耗项（金币/材料）可配 |
| **B-3** 🟡 | **三孔是"一律全开"还是"逐步开 1→2→3"** | 按你的原话"**固定三个孔位**"→ **一律三孔全开** |
| **B-4** 🟡 | **"同符文跨装备类型差异"的类型键** | 用 `items.type`（比 slot 更贴"武器/护甲/饰品"的语义）+ **default + 覆盖**声明形状 |
| **B-5** 🟡 | **符文与既有装饰珠是否共用孔位** | **共用同一孔位数组（一槽一物、互斥）**——避免两套孔位真相源 |
| **B-6** | 三阶"偏向性"的机制定义 | ✅ 建议=**偏向某相性**（直接对接批38 相性层，复用做装备那套） |
| 其余 | 拆卸无损 / 3合1 身份（同 id 递阶 vs 换 id） / 绑定交易 / 战斗中禁插拔 | ✅ 建议：**无损拆（沿用 SOCK-03）** · **同 id 递阶** · **沿用绑定** · **沿用战斗内禁插拔** |

**两处真机制缺口（实现时必补）**：**"重伤"（治疗削减）无承载**（heal 动作不查状态减益）· **层数型增益不生效**（`_aggregate_boost` 只按状态实例求和、**不乘 stacks**）。

---

## C · 装备被动（🟡 3 条需你定）

**✅ 已定**：**不新造机制**——被动定义复用 `traits.json`、行为复用 `effects.json`、装备模板加 `passives` 引用字段、"相性影响变更"写成 `affinity_variants` 并**直接调 `affinity.resolve_affinity_effect`**；**打造时求值、冻进实例**（呼应"独一无二"）。

| # | 争点 | 我的建议 |
|---|---|---|
| **C-1** 🟡 | **被动可否叠加 / 同名如何处置** | **默认不叠加**（`traits.group` 互斥组内取一）——防数值失控 |
| **C-2** 🟡 | **相性变更是否可逆**（脱下再穿 / 重铸） | **实例冻结后不可逆**（仅"重铸/再打造"时重掷）——保住"独一无二" |
| **C-3** 🟡 | **变更后原被动是否彻底替换** | **彻底替换**（语义最干净、展示最清楚） |

**其余 ✅**：副相性能否单独作条件（建议改 `resolve_affinity_effect` 支持 `"|<sub>"`，**但那是公共 API、深炼金共用 → 改前我会先评估**）· 变更=确定性替换（非随机）· 变体目标必须在 `traits.json` 注册（强校验）· 禁止自引用与环。

**两处真缺口（实现时必补）**：`traits` **目前零战斗消费点**；`event_dispatcher` **无 owner 过滤**（触发式被动会全局误触发）。

---

## D · 深炼金侧相性（🟡 3 条需你定）

**✅ 好消息**：**通用层不用改** —— `core/affinity.py` 四个接口**原样**就支撑"材料相性 → 主副相性 → 效果取值"主干（已实测跑通）。

| # | 争点 | 说明 |
|---|---|---|
| **D-1** 🟡 | **相性 → 药剂效果走哪种口径**：强度型 / 附加型 / 成功率·品质型 | 三者可并存；**附加型复用打造的池模型**（同一套池、加作用域区分） |
| **D-2** 🟡 | **要不要动通用层加"池条目作用域"**（区分药剂词条 vs 装备词条） | 我建议**加，但做成可选参数、缺省零行为变化** |
| **D-3** 🟡 | 炼金侧玩法细节（相性影响哪些产出、多少） | **等你的炼金细化**，我不替你定 |

**炼金侧 4 处真缺口（带 file:line）**：材料相性没进炼金快照 · 池条目 `weight` 无消费者 · 结算算出的 `scaled_effects` 不落实例 · 使用路径只读物品定义不读实例（→"强度型"无处落地）。

---

## E · 你只需要"知道"的（✅ 已按建议默认，随时可改）

1. **品质维度**：新增正交维度（10 级 × 6 色 × 4 图纸档），不动既有四档品质注册表
2. **强化上限**：按你的"替换"决定 —— 用 6 档（+3…+18）替换现有 4 档，**旧装备按旧档兼容读取**
3. **指令名**：`/精造`（`/打造` `/铸造` `/图纸` 已被占用）
4. **随机流**：玩家级 `rng_state` 落档 + 逐指令推进（同一玩家反复打造不会走出同一条序列）
5. **装备实例**：加落档 `uid`（已迁移 v2→v3），同 id 两件随机装备**不再串**
6. **副手**：部位设置页开关（默认关；开启后单手作副手 50% + 五类失活）
7. **装备占比 60%**：装备预算 8→18（白值 7 / buff 5 不动），**怪物侧同步校准 + 斩杀回合数对照**（独立可回滚批）
8. **合成公用层**：三层架构 + 三条启用路径（只合成 / +炼金 / +打造），"只启用合成"逐字段等于既有 simple 档

---

## F · 我建议的下一步

1. **你先回 🟡 那 15 条**（A 组 4 + B 组 5 + C 组 3 + D 组 3）——**可以只回关键的（A-1 cap 与彩档 q10、A-3 精粹三问、B-1~B-5）**，其余我都已按建议默认；
2. 同时我继续推进**不需要这些裁决的批**：**强化 6 档替换（含迁移兼容）** → **数值校准（装备占比 60%）** → 这两批口径已定；
3. 你回完 A/B 组，我就开 **符文批** 与 **淬炼回收批**；D 组等你炼金细化。
