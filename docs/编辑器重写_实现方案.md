# 内容编辑器 · 实现方案（终端极简风 · 分批实施）

> 用户 2026-09-13 拍板：**用「终端极简风」写**（样式稿 `s3/e3`）；**分多批、一个功能一个功能来**；
> 技能条目必须有**派生界面**（派生条件 / 派生技能 / 派生消耗）。
> 需求侧唯一来源：`docs/编辑器重写_需求与约束.md`（第〇节「通用工具」原则优先于一切）。
> 本文件 = 实施侧唯一来源：架构、批次划分、每批验收门禁。**不写进本方案的功能一律不做**。

## 一、不变量（每批都必须满足，违反即回退）

1. **通用性**：编辑器不为任何内容包定制。模块列表来自 `manifest.modules`，字段与分组来自字段元数据
   （`qbot_rpg/content/*_models.py` 的 `FieldMeta` / `FieldMetaTable`）。判断标准：**换一个包，编辑器零改动可用**。
2. **元数据驱动渲染**：字段类型 → 控件的一对多映射写在一处（见 §三），业务模块不得新增特例分支。
3. **面向非技术用户**：字段中文名；必须有人话提示；新功能要有引导，不静默。
   · **三栏并列**：①模块分类栏 ②条目列表栏 ③大编辑面板——前两栏各自独立（不混排分类名与条目名），
     合计仍窄（约 350–400px），编辑面板占其余全部宽度（用户 2026-09-13：是两栏不是两段）；
     装备归入物品 = **模块层级**由包元数据声明（`物品 ▸ 装备`），编辑器不写死归并关系。
4. **纯配置优先**：内容侧不新增硬编码；模板/文案可被内容包覆盖（沿用游戏侧既有机制）。
5. **落盘安全**：任何写盘都走原子写 + 备份；写前校验；不破坏包（校验不过不落盘）。

## 二、形态与技术选型

| 项 | 选型 | 说明 |
|---|---|---|
| 宿主 | 复用原编辑器形态：本地 HTTP 服务（FastAPI）+ 单页前端，`scripts/editor_host.py` 起服 | 默认 `127.0.0.1:8090`；公网默认关（隧道/反代另配） |
| 前端 | 单页静态页（零构建、零 CDN） | 终端极简风样式 token 独立成文件，便于换肤 |
| 鉴权 | 保留原语义：首次访问引导设密 → 登录；会话内存态（重启重登） | 机主 / GM 只读预览的权限级 |
| 落盘 | 沿用 `qbot_rpg/content/atomic_store.py`（原子写 + 变更条目形态） | 不新写存储层 |
| 校验 | 复用现有校验器 + `FieldMetaTable`（不手写字段清单） | 红拦/黄提示语义不变，编辑器只做展示 |

## 三、元数据 → 界面的映射（唯一实现点）

**需补的元数据维度（本方案第 1 项实际改动）**：现有 `FieldMeta` 有 type/required/default/ref_target/enum/范围/概率，
**缺「分组/分区」**。补一个可选维度（如 `group: str` 或 `section: str`），缺省时按「模块默认分组」兜底。
这样才能做到「页签 = 元数据声明的分组」而不是编辑器写死。

字段类型 → 控件（一对一映射表，集中实现）：

| FieldMeta.type | 控件 | 备注 |
|---|---|---|
| `str` | 单行文本 | 长文本用 `str` + 模块声明 `multiline`（或 `text`） |
| `int` / `float` / `number` | 数字输入（步进按类型） | 有 `range_min/max` 时显示为提示，不硬拦 |
| `bool` | 开关/复选 | |
| `enum` | 下拉（选项来自 `enum`） | |
| `ref` | **引用选择器**（可搜、显示目标 `ref_target` 的名称） | 引用目标来自 `ref_target` |
| `list` | **可增删行表格**（列 = 元素字段元数据；行尾删除 + 「+ 添加一行」） | 列表元素若为 `obj`，列名取元素字段元数据 |
| `obj` | 折叠字段组 | |
| `map` | 键值对表格（可增删） | |
| `formula` | 表达式输入 + 实时求值预览 | |
| 条件类（派生条件等） | 条件行编辑器（左 = 来源/主体，中 = 比较，右 = 阈值） | 通用形态；具体键名由包的元数据声明 |

> **批 2 已实现范围**：上表**标量控件**（单行文本 / 多行文本 `str` + `multiline` / 数字 / 布尔 /
> 枚举下拉 / 引用下拉）已可编辑。**批 4 已把 `list` 升级为可编辑**：元素为 obj/标量 → 可增删行表格
> （`listtable`），元素为 ref → 引用多选（`reflist`）；`obj`（除列表内联对象）与 `map` 仍只读展示。
> **批 5 补齐上表的后两行**：`FieldMeta.editor` 显式声明 → `condition`（条件行编辑器）/
> `maptable`（键值对表格，可增删行、键值可引用）；未声明 `editor` 的 `obj`/`map` 仍只读（既有契约不变）。
> 映射集中在 `qbot_rpg/web/api.py`（`_WIDGET_BY_TYPE` + `_EDIT_BY_WIDGET` + `list_control` + `control_for`，
> 见 §四 批 2/批 4/批 5 实现锚点），业务模块不得新增特例；长文本另有「含换行 / 超长」启发式兜底
> （元数据未声明 `multiline` 时）。

## 四、分批实施（一批一功能，每批独立验收）

> 进度：批 0 ✅ 令牌+组件总览（`/root/editor_design/`）｜批 0.5 ✅ 三栏骨架 + 三栏重画（s3/e3）｜批 1 ✅ 只读骨架｜批 2 ✅ 编辑与保存｜批 3 ✅ 分区页签（元数据驱动）+ 视觉小修｜批 3.5 ✅ 字段中文名并列显示 + 各模块字段名表 + V8 修复｜批 4 ✅ 列表型字段（可增删行表格）+ 引用型字段（引用选择器/引用多选）＋ 实机 UX 四修（空列表加行/表头中文名不缩写/单元格控件外观/列宽跨列）｜批 4.5 ✅ 嵌套子字段中文名（递归到 `element.children`）+ 余量模块字段名补全｜批 4.6 ✅ 字段说明气泡（悬停/点击中文名看解释：自动拼装 + 元数据人工 `help`）＋ 实机三修（类型按实际值推断 / settings+maps/quest/jobs/npc/shop 说明覆盖 / 关闭按钮与避让定位）｜批 5 ✅ 技能派生界面（元数据「关联分区」+ 条件行编辑器 + 派生技能引用选择器 + 派生消耗/数值覆盖键值对表格）＋ 实机验收（改派生条件→保存→`skill_chains.json` 真变化→回退复原）｜批 5.1 ✅ 列表字段「块状换行」布局（用户点名：派生步骤表加一层换行；V13/V14 随块布局消除，纯标量列表仍用表格）｜批 5.2 ✅ 视觉细则清零（V1–V6 / V9–V11 销项）｜批 6 ✅ 新增/删除条目（建议 ID 即时唯一性 + 元数据默认值 + 被引用黄提示 + 自动备份/回退）+ 检索（当前模块按名称/ID + 跨模块全局检索分组跳转；复用批2 落盘链路）

> **批 2 实现锚点**（代码位置与调用链，便于后续批次与复核）：
> · 控件映射**唯一实现点** = `qbot_rpg/web/api.py`：`_WIDGET_BY_TYPE`（type→形态）+
>   `_EDIT_BY_WIDGET` / `control_of` / `is_editable_widget` / `editable_form`（形态→编辑控件，含 multiline→textarea）；
>   前端只读 `descriptor.control` 渲染，不认字段类型。
> · 草稿态 = 前端（`static/index.html` 内联纯 JS 模块 `EditorDraft`，标记块 `EDITOR_DRAFT_BEGIN/END`）：
>   改动→草稿、未保存计数、取消清空、切换条目/模块/包与关窗守卫（`canLeave`）。
> · 写链路 = `qbot_rpg/web/editor_ops.py`：
>   `validate_entry` = `api.entry_slot` → 补丁 → `check_pack`（现有唯一校验入口，红拦/黄提示）；
>   `save_entry` = 红拦不落盘 → `atomic_store.backup_modules`（.bak）→ `atomic_store.write_modules`（原子写）
>   → 回读 `check_pack` 复核，失败自动 `restore_modules_from_backup`；
>   `rollback_module` = 从 .bak 原子恢复；`module_backup` = 回退入口可见性。
> · 权限位 = 沿用 `permission_store` 角色：机主(owner) 可编辑 / GM(gm) 只读预览；
>   只读身份在 `editor_ops.require_edit` 前置拒绝（Forbidden 403），不依赖前端隐藏按钮；
>   批 2 不做登录页，启动参数 `--role` + 顶栏开关（`/api/session/role`）表达。
> · 宿主编排 = `scripts/editor_host.py`（`/api/session`、`…/validate`、`…/save`、`…/backup`、`…/rollback`、`/api/pack/{p}/refs/{target}`）。

> **批 3 实现锚点**（分区页签完全元数据驱动 + 视觉小修）：
> · 分组元数据维度（`qbot_rpg/content/models.py`）：`FieldMeta.group`（批1）+ `ModuleMeta.field_groups`
>   （键→组）+ `ModuleMeta.group_order`（**顺序**，缺省按首次出现）+ `ModuleMeta.group_labels`
>   （**显示名**，缺省回退组键）——三者都只影响展示，不参与任何校验判定。
> · 分组声明（`qbot_rpg/content/field_meta.py`）：`SKILLS_*`（批1）+ 批3 新增
>   `ENEMIES_/ITEMS_/EQUIPMENT_/MAPS_/QUEST_GROUP_DEFS`（各 2~4 组；只加元数据、零新增业务字段；
>   覆盖真实内容包里常见但 fields 未登记的键：desc/monsters/exits/ai/phases/rewards 等）。
> · 页签数据源 = `qbot_rpg/web/api.py::_group_summary`：输出 `{name, label, count}`；
>   顺序 = `group_order` → `field_groups` 声明序 → 字段首现序；**元数据声明了但本条目无字段的组也保留**
>   （count=0 → 前端空态）；`DEFAULT_GROUP`（「默认」）仍是唯一兜底（模块无声明时单组）。
> · 前端页签 = `static/index.html` 内联纯 JS 模块 `EditorGroups`（标记块 `EDITOR_GROUPS_BEGIN/END`，
>   node 可直接执行）：`view(groups, fields)` 出有序页签（含 label/计数/empty）、`switchGroup` 只切视图索引；
>   默认只显示第一组，切组**不触碰草稿**（草稿按字段键持有 → 跨组编辑不丢、未保存计数不变）。
> · 视觉小修：双列网格同一行两列**基线对齐**（`.row` align-items:start + `.rlab` min-height:var(--h-ctl)
>   + `.rows2` stretch → 长提示只向下撑高本行）；顶栏 nowrap + 省略号 + 窄屏（≤900px）隐藏来源标注
>   （三段信息不挤压换行）；空值文案统一为 `EMPTY_TEXT`（`index.html` 唯一字面量）。
> · 回归测试 = `tests/unit/test_editor_group_tabs.py`（顺序/显示名/兜底/空分组/真实条目计数 +
>   node 执行 EditorGroups&EditorDraft 证「切组保草稿」+ 前端接线/视觉 CSS 断言）。

> **批 3.5 实现锚点**（字段中文名并列显示 + 各模块字段名表 + V8 修复）：
> · 显示形态 = `index.html` 内联纯 JS `fieldLabelHtml(label, key)`（标记块
>   `EDITOR_FIELD_LABEL_BEGIN/END`）：元数据有中文名（label ≠ key）→「中文名 +
>   弱化键名」（`.fkey` = `--text-5` + `--fs-11`）；无中文名 → 只显示原始键（现状，不造词）。
>   三处标签渲染（主表单行 / 列表表头 / 对象子字段）统一走该函数，框架不认任何业务字段名。
> · 字段中文名表 = `qbot_rpg/content/field_meta.py`（与 `SKILLS_FIELD_LABELS` 同位置）：
>   本批 `ENEMIES/ITEMS/EQUIPMENT/MAPS/QUEST_FIELD_LABELS`，余量补
>   `EFFECTS/STATUSES/MARKS/SKILL_CHAINS/ACTION/JOBS_FIELD_LABELS`；
>   经 `_decorate_field_meta(fields, groups, labels)` 叠加——只补 label/group，
>   type/required/default/enum/children 原样保留，字段自带 label 优先。
>   同批修正批3 误把「组显示名」当字段中文名传参（enemies 的 stats/actions/drops）。
> · V8 根因 = `selectOptions` 按对象取候选 `o.id/o.name`，而枚举候选是纯字符串
>   （`FieldMeta.enum`）→ 读成 undefined：候选渲染为空、当前值恒判「不在候选内」；
>   修法 = 候选按形态归一（字符串→只显示值；对象→「名称（标识）」）后再比较/选中。
> · 回归测试 = `tests/unit/test_editor_field_labels.py`（字段名表补全/契约不变 +
>   node 执行 `fieldLabelHtml` 与 `selectOptions` 证并列渲染与 V8 修复 + 框架无业务字段名）。

> **批 4 实现锚点**（列表型字段 → 可增删行表格；引用型字段 → 引用选择器/引用多选；嵌套值保存链）：
> · 控件映射（仍集中在 `qbot_rpg/web/api.py` 唯一实现点）：`_EDIT_BY_WIDGET["list"]`
>   由 `readonly` 升级为 `("listtable", True)`；新增 `list_control(fm)` 依据**元素元数据**
>   分流——`element.type == "ref"` → `reflist`（引用多选），其余 → `listtable`（可增删行表格）。
>   `obj`（除列表内联对象）与 `map` 本批仍 `readonly`；前端只认 `descriptor.control`。
> · 列表列元数据来源 = `_list_columns(fm, rows)`：元素 `obj` 且有 `children` → 逐子字段出列
>   （label/type/enum/ref_target/default 全带）；元素为标量 → 单列 `value`（列名兜底「值」）；
>   无元素元数据 → 按实际行键推断（现状兜底）。列描述新增 `control/enum/number_step/default`
>   供**单元格编辑**。值缺失/null 的列表也按元数据出列（空表仍可「+ 添加一行」）。
>   元素声明 `obj` 但未声明子字段、且无数据行可推断键 → **不出列**（前端如实提示补元数据，
>   不臆造 `value` 键破坏对象行形态）。
> · 新增行默认值 = `_row_default(elem, scalar)`：obj 元素只写声明了 `default` 的子字段；
>   标量元素用元素 default。`scalar_element` 由 `_is_scalar_element` 判定（obj → False）。
> · 引用合法性（**黄提示，不红拦**）：`_ref_valid(view, ref_target, value)`（空值不算非法）；
>   标量 ref → 描述符 `ref_valid`；元素 ref 的列表 → `invalid_refs` + 元素元数据 `ref_target`
>   回填到字段（候选目标在 element 上）；obj 行内 ref 子字段 → `invalid_cells`。
>   保存校验仍由现有 `check_pack`（R-4）决定，编辑器不新写规则、不改变既有校验语义。
> · 草稿/前端 = `index.html` 内联纯 JS 模块 `EditorList`（`EDITOR_LIST_BEGIN/END`：
>   `addRow/delRow/setCell/cellValue`，增行用 `row_default` 初始化）与 `EditorRef`
>   （`EDITOR_REF_BEGIN/END`：`addRef/delRef/filter/status/label`）；DOM 层
>   `listTableEdit/refListEdit` 按 `control` 渲染，行内改动 `EditorDraft.set(draft, key, 整数组, f.value)`
>   → 整表/整列表作为一个字段键进草稿，与批2 未保存计数/脏标记天然联动（数组用 deepSame 比对）。
> · 样式 = tokens.css 令牌 + `index.html` 组件 03/04（`.ltable-edit/.ldel/.ladd/.refsel/.refvals/.refval/.rf-list/.ref-it`）。
> · 保存链**零改动**（复用批2）：`_apply_patch` 深拷贝任意嵌套值 → `check_pack` → 备份 → 原子写 → 回读；
>   列表/对象/引用多选整体作为字段值落盘，回退照旧从 `.bak` 原子恢复。
> · 回归测试 = `tests/unit/test_editor_list_ref.py`（列元数据/默认行/控件分流/非法引用标记 +
>   node 执行 EditorList&EditorRef&EditorDraft 证增删改入草稿 + 嵌套落盘/备份/回退 + 红拦不落盘）。

> **批 4 UX 修复锚点**（主 agent 实机实测四问题；只修展示/交互层，保存链与校验语义零改动）：
> · 问题1 空列表无「+ 添加一行」：`listTableBody` 在「元素 obj 未声明 children 且无数据行」
>   时提前 return，跳过统一追加的按钮。修法 = 去掉提前 return；无列时仍出「操作」列，
>   新行可删（`index.html` `listTableBody`）。回归 `tests/unit/test_editor_batch4_ux.py`。
> · 问题2 表头只有原始键且被压成缩写：根因 = 元素子字段无 `label` + `.ltable` 用
>   `table-layout:fixed;overflow:hidden`。修法 = ①元数据层给 `ACTION_ENTRY_CHILDREN`/
>   `SPECIAL_ACTION_CHILDREN`（含 trigger/post_state 孙字段）/`CHAIN_ENTRY_CHILDREN`/
>   `CHAIN_NODE_CHILDREN` 补**展示层 label**（含数据实有的 `desc`），校验契约不动；
>   ②前端表格 `table-layout:auto` + 最小列宽 96px + 表头换行 + 外包 `.ltable-scroll`
>   横向滚动（`--sb-size/--sb-thumb` 细滚动条令牌），不再裁切。
> · 问题3 单元格无控件外观：readonly（对象/映射）与嵌套 list 列裸露无边框 JSON。修法 =
>   `.ro-cell` 有框有底只读块（嵌套列表不再误渲染成可编辑文本框）；text/number 补
>   `placeholder=EMPTY_TEXT`（V3 空值口径）；表格内 `.cin/.cta` 补 `--bg` 底。
> · 问题4 右表贴边 + 两列不齐：`.rows2/.row` 改 `minmax(0,1fr)`；列表/引用列表字段
>   （只按后端 `control` 判定）加 `.row.wide` 跨双列独占整行；「操作」列补右侧内边距。
> · 回归测试 = `tests/unit/test_editor_batch4_ux.py`（node 执行真实 `listTableBody`/
>   `listCell`/`fieldRow` + 元数据中文名 + CSS 令牌断言）。

> **批 4.5 实现锚点**（嵌套子字段中文名补全 + 余量模块字段名；只加展示层 label，校验语义零改动）：
> · 问题 = 列表表头/对象组头仍有裸键：`enemies.phases[].threshold/enter_action/broadcast`、
>   `drops.*`、`ai.*`、`parts.*`、`skills.level.*`、`jobs.transform.*`、`maps.*`、
>   `quest.conditions.*`、`effects/statuses/marks` 的 children、`forge/enhance/slots` 等。
> · 递归装饰（`qbot_rpg/content/field_meta.py` 唯一实现点）：`_decorate_field_meta(..., child_labels)`
>   新增第 4 参 = 嵌套中文名表 `{子键: 中文名 | {孙键: …} | FieldMeta}`；`_decorate_one`/
>   `_decorate_tree` 递归到 `FieldMeta.children` 与 `element.children`（list 元素对象同覆盖）。
>   已声明键只 `replace(label=…)`（type/required/enum/children 全保留）；真实内容包出现但未登记的
>   键补 `soft_label=True` 纯展示子字段（validator 对 soft_label 立即 return → **校验语义零变化**）。
>   容器自身中文名用保留键 `_label` 表达（如 `{"battle": {"_label": "战斗掉落", …}}`）。
> · 命名依据（不造新词）：`stats.json`（hp 生命 / mp 法力 / …）、`docs/m2_shared_contract`
>   （enemies 八段 + AI / phases 阶段表）、`docs/m3_shared_contract`（zone_change 换区）、
>   `docs/m13_6a~6c`（skills/jobs/skill_chains）、`docs/veinborn/03_schema_修正稿.md`、
>   既有 `*_FIELD_LABELS`、`data/gear_stats.GEAR_LABELS_ZH`；术语冲突按需求 §六
>   （mp 法力 / con 体质 / mag 法强 / pv 防护值）。
> · 覆盖统计（对基线 HEAD 的字段树逐节点 diff）：**226** 个原有裸键补中文名、**398** 个真实
>   键新增纯展示节点、覆盖 **34** 个模块、递归深度 **4**；仍无中文名的展示节点 **8**（`items.elements`
>   的 8 个元素键本身即中文：地/水/火/风/雷/晶/月/无）。**动态键空间**（`ai.states.<状态名>` /
>   `settings.slot_defs.<部位>` / `consume_marks.<印记>` / `element_req.<元素>` / `job_tier_map.<档位>`
>   / `currencies.<货币>` / `settings.events.<事件名>` / `exp_curve.<等级>` 等）、**里程碑门禁钉住的键**
>   （`jobs.growth.atk/dfn`、`skill_chains.job_scope`）与**触发 M43① 零定时器扫描词的键**
>   （`settings.contest` 下的排期子段）**保留原始键不猜**，登记于批报「待确认清单」。
> · 余量模块顶层字段名：新增 `MANIFEST/TRAITS/RECIPE/PROFICIENCY/SLOTS/STATS/SETTINGS/CONDITIONAL_FIELD_LABELS`；
>   `npc/shop/checkin/dungeon/achievements/formula` 原表已带中文名。enemies 补 AI 引擎依赖段
>   `ai/phases/rewards/zone_change`、maps 补真实地图段（`monsters/exits/mechanics/gate_guard/
>   gather_points/dungeon_entrances/camp/...`）、settings 补真实顶层段（`assistant/battle/
>   command_aliases/contest/ctb/events/exp_curve/level_cap/quest_board`）——均为 soft_label 纯展示宽字段。
> · 回归测试 = `tests/unit/test_editor_batch45_nested_labels.py`（递归装饰契约 + 真实数据反查
>   嵌套键中文名 + 列表表头/对象组头中文 + soft-only 断言 + 框架无业务字段名）；
>   硬计数断言随本批更新（`tests/unit/test_field_meta_inject.py`）。全内容包泛型校验报告
>   （errors/warnings）与基线逐包 diff **完全一致**（附录 /tmp 校验日志）。


> **批 4.6 实现锚点**（字段说明气泡：悬停/点击中文名看解释；只加展示维度，校验语义零改动）：
> · 用户诉求（2026-09-13）：「点击对应中文名/鼠标悬停会显示关于这个字段的详细解释
>   （范围数值填多少、百分比还是数值型、字段具体功能）」。形态 = **自动拼装 + 人工补充**，
>   不新增外部依赖、不写死任何业务字段名。
> · **元数据新维度**（`qbot_rpg/content/models.py` `FieldMeta` 尾部默认值）：
>   `help: str`（一句话说明该字段干什么；缺省空 = 未撰写，不报错）+ `unit: str`
>   （数值单位：% / 点 / 回合 / 次 / 段 / 行动条 / 级 / 层；无单位留空）。
>   只影响说明文案与「数值还是比例」判定，**不改 type/required/default/enum/children/校验规则**。
> · **人工说明表**（`qbot_rpg/content/field_meta.py` 唯一数据源）：
>   `SKILLS_/ENEMIES_/ITEMS_/EQUIPMENT_/EFFECTS_/STATUSES_/MARKS_/SKILL_CHAINS_/ACTION_/SETTINGS_FIELD_HELP`
>   + 嵌套 `*_CHILD_HELP`（形态与 `*_CHILD_LABELS` 平行：str 或 `{_help, 子键…}`）。
>   覆盖 10 个必做模块的**全部顶层字段**（skills 42 / enemies 31 / items 39 / equipment 31 /
>   effects 31 / statuses 14 / marks 12 / skill_chains 10 / action 36 / settings 19，共 265 条人工说明）；
>   装备词条键说明按 `data/gear_stats.py` 注册表口径批量生成（白值=点 / `_pct`=百分点 / 战斗直读键）。
>   命名依据 = stats.json / docs/m2·m3·m8·m13 契约 / 字段定义处注释，**不造词、不瞎写**。
> · **装饰扩展**：`_decorate_field_meta(..., helps, child_helps)` / `_decorate_one` / `_decorate_tree`
>   增加 help 维度（递归到 `children` / `element.children`）；说明**只作用于已声明键**，
>   不新增字段节点；字段自带 help 优先；缺省（无 helps）行为与批4.5 完全一致。
> · **自动拼装**（`qbot_rpg/web/api.py`，唯一实现点）：`help_card(key, fm)` 输出结构化卡片，
>   字段名（中文+原始键）、类型语义（`_type_semantic`：文本/数字/布尔/枚举/引用/列表/对象/
>   映射/公式）、**数值还是比例/百分比**（`_scale_semantic`：probability → 比例；unit=`%` →
>   百分比；其他 unit → 「数值（单位：X）」；数值无单位且区间 0~1 → 比例；其余数值 →
>   「数值（未标注单位）」；非数值 → 不适用；未登记 → 未标注）、建议范围（`_range_semantic`，
>   `zero_unlimited` → 「0 = 不限」）、默认值（`_default_text`：布尔是/否、复合值 JSON）、
>   是否必填、枚举候选、引用目标；**判不出来如实说「未标注」**。
>   描述符（`_descriptor`）与列表列（`_column`）都带 `unit` / `help` / `help_card`。
> · **前端交互**（`static/index.html` 内联纯 JS `EditorHelp`，标记块 `EDITOR_HELP_BEGIN/END`，
>   node 可直接执行）：`helpLines/helpCardHtml/helpTitle` 渲染；状态机 `helpHover/helpUnhover/`
>   `helpClick/helpKey`（悬停开、点击固定/再点关、Esc 关）；`helpPlace` 定位（字段行下方 →
>   上方 → 右 → 左，均不与正在编辑的字段矩形重叠；窄窗口水平收进视口）；
>   `helpTriggerHtml` 给字段中文名加 `tabindex/role=button/aria-expanded/aria-label`；
>   DOM 接线 `bindHelp`（悬停/点击/键盘/点空白/Esc/窗口缩放重定位）。
>   三处字段标签渲染（主表单行 / 列表表头 / 对象子字段）统一经 `helpTriggerHtml`，框架不认业务字段名。
> · **样式**（`static/index.html` 组件 · tokens.css 令牌）：`.help-card` 深底 `--bg` + 细边框
>   `--line-strong` + 等宽 `--font-mono` + 直角；`max-height:45vh` + `overflow:auto` +
>   细滚动条令牌（`--sb-size/--sb-thumb`）；`.flabel.helpable` 悬停/聚焦高亮 + `--focus` 环。
> · 回归测试 = `tests/unit/test_editor_batch46_help.py`（元数据维度/说明表覆盖与无幻键/装饰契约零变化/
>   自动拼装各分支/描述符接线/node 执行 EditorHelp 的状态机·拼装·定位·转义·aria + 框架无业务文案）。
>   批4 UX 回归的 node 桩补 `helpTriggerHtml` 透传（标签外层新增触发器，本批只验证标签与控件）。

> **批 4.6 补实现锚点**（2026-09-13 实机三修；只动展示层，校验语义零变化）：
> · **问题① 类型判定**：根因 = 批4.5 起「只有中文名、无类型依据」的纯展示节点由
>   `_soft_display` 默认 `type="str"` 生成，`help_card` 只读 `fm.type`、拿不到实际值。
>   修法：`field_meta._soft_display` 默认 type 改空（=未登记）；`web/api.py` 新增
>   `_infer_value_kind`/`_registered_type`/`_resolve_type`（数值细分整数/小数 + `type_source`），
>   `_scale_semantic(fm, value)` 按「登记类型 or 实际值」判数值/比例（值落 0~1 → 疑似比例；
>   判不出 → 未标注而不是不适用），`help_card(key, fm, value)` / `_descriptor` / `_column` /
>   `_list_columns` 全链贯穿实际值；前端类型行显示「· 元数据未登记，按实际值推断」。
> · **问题② 内容覆盖**：`SETTINGS_CHILD_HELP`（currencies/death_penalty/slot_defs/alchemy/
>   forge/fishing/assistant/battle/contest/ctb/quest_board）+ `MAPS/QUEST/JOBS/NPC/SHOP` 的
>   `*_FIELD_HELP`（顶层 22/17/11/15/15 全覆盖）与 `*_CHILD_HELP`；battle 段补登记引擎白名单
>   键 `stun_enabled/backstab_bonus/crit_cond_low_hp`。依据 = 引擎默认表/契约/定稿，未注册的
>   `time_cycle/economy` 段不臆造（见批报存疑项）。
> · **问题③ 交互**：卡片右上角 × 关闭按钮（`aria-label`、Esc/点空白保留），`helpPlace`
>   候选顺序改「上方 → 右 → 左 → 下方」且任何候选都不压被编辑字段行，视口放不下时取
>   「不压字段且可见面积最大」者；`.hc-t/.hc-v` 换行保护，防长键名/长文案溢出。
> · 回归：`tests/unit/test_editor_batch46_help.py`（98→新增推断/覆盖/交互断言）、
>   `tests/unit/test_editor_list_ref.py`（无元数据列按值推断控件）。

> **批 5 实现锚点**（技能派生界面：派生条件 / 派生技能 / 派生消耗；用户点名项）：
> · **关联分区（元数据声明驱动）**：`ModuleMeta.associations: Tuple[AssociationMeta, ...]`
>   （`qbot_rpg/content/models.py`）声明「本模块条目 ↔ 其他模块条目的外键」——`module`（相关条目
>   所在模块）+ `field`（外键字段路径，支持点路径与列表通配 `steps[].to`）+ `label`/`editable`/
>   `local_field`（本条目用于匹配的字段，缺省 id）。`qbot_rpg/content/field_meta.py` 的
>   `SKILLS_ASSOCIATIONS` 为 skills 声明两条：`skill_chains.trigger_skill`（可编辑）与
>   `skill_chains.steps[].to`（只读摘要 + 跳转）。**编辑器零业务模块/字段名**（api.py/index.html
>   只读声明渲染）；换包/换模块改声明即可，代码零改动。
> · **关联分区渲染** = `qbot_rpg/web/api.py::_association_sections`（`_walk_path` 路径匹配 +
>   `_local_values`）→ `entry_detail` 新增 `associations`/`association_count`；可编辑分区对每个
>   相关条目用**该模块自己的字段元数据**出完整表单（`_entry_base`+`_build_fields`+`_group_summary`），
>   只读分区只出 id/名称/命中路径。前端 `assocPaneHtml`/`registerForm`：每个相关条目 = 一个独立
>   **表单上下文**（`state.forms`，各自草稿），页签标签/说明来自声明；「打开」跳转到相关模块条目。
> · **跨模块落盘**：就地编辑保存 = `POST /entry/{相关模块}/{相关条目}/save`（同一批2 链路：
>   `editor_ops.save_entry` → 现有校验 → 备份 → 原子写 → 回读/回退）；改派生链 = 写 `skill_chains`，
>   **不新写存储/校验、不绕过 batch2**。`formURL(ctx, suffix)` 按上下文拼 URL，主表单与关联分区共用。
> · **条件行编辑器（通用控件）**：`FieldMeta.editor="condition"` 显式声明（覆盖 type→控件默认映射，
>   映射唯一实现点仍是 `web/api.py::control_for`）。数据面参考实现 = `api.condition_rows`/
>   `condition_value`（键名不写死：一级 `{主体:{比较符:值}}`、两级 `{主体:{键:{比较符:值}}}`、
>   列表值 = 逻辑组合 `{and/or:[{条件}…]}`；深于两级整体只读保留不丢数据）；前端
>   `EditorCondition`（`static/index.html` 标记块 `EDITOR_CONDITION_BEGIN/END`，node 可直接执行）
>   与之**同构**（批5 测试逐条比对 71 条真实条件 + Python 行→JS 序列化等价）。主体声明
>   = `ConditionSubject`（`label`/`key_ref`/`value_ref`/`ops`/`combine`），`CHAIN_CONDITION_SUBJECTS`
>   声明 7 个主体；元数据缺省时按实际值形态推断并在控件里标明「元数据未声明主体」。
> · **派生技能引用选择器**：`skill_chains.trigger_skill` 与 `steps[].from/to` 由 `str` 升为
>   `ref, ref_target="skill"`（复用批4 引用选择器；`steps` 是 soft_label 容器 → 泛型校验语义
>   不变，`trigger_skill` 顶层引用启用 R-4）。**派生消耗** = 步骤 `consume`（数字）+ 技能
>   `consume_marks`（`FieldMeta.editor="maptable", key_ref="mark"` → 键引用选择器）；
>   **数值覆盖** = 步骤 `variant_override`（`maptable`）。
> · **键值对表格**：`FieldMeta.editor="maptable"`（`map` 的默认映射仍只读，显式声明才可编辑 →
>   既有 `editable_form("map")==readonly` 契约不变）；前端 `EditorMap`
>   （`EDITOR_MAP_BEGIN/END`）+ `mapBodyHtml`/`mapCell`，可增删行、键可引用。
> · 回归测试 = `tests/unit/test_editor_batch5_derive.py`（元数据声明/装饰保留 + 条件全量 round-trip
>   + 合成包验证关联分区零硬编码 + 列表通配匹配 + 临时目录落盘/回退/红拦 + node 执行
>   EditorCondition/EditorMap 与 Python 同构 + 条件行/关联分区/单元格接线 + 通用性护栏）。

> **批 5.1 实现锚点**（列表字段「块状换行」布局；用户点名：派生步骤表加一层换行）：
> · 用户原话（2026-09-13）：「把这类表格之类的加一层换行吧，放到连段步骤这个类目标题底下，
>   不然那里空了一大块，一方面是不好看，另一方面是浪费空间」。
> · **判定依据（元数据，唯一实现点 `qbot_rpg/web/api.py`）**：新增
>   `_NESTED_CONTROLS = (condition, maptable, readonly, listtable)` 与
>   `is_nested_control(control)`；列描述 `_column` 新增 `nested`（该列的值是否承载嵌套结构）；
>   字段级 `_descriptor`（widget=list）新增 `block_layout = 任一列 nested`。判定**只依据控件形态**
>   （§三 映射表的产物），不认任何业务字段名；元数据未登记类型时沿用批4.6「按实际值形态推断
>   control」的口径（对象值 → readonly），因此缺元数据也能兜底。
> · **块状布局结构（前端）**：`listTableBody` 命中 `block_layout` → `listBlockBody`/`listBlockHtml`
>   （新增于 `listTableEdit` 之后），逐条成块：`.lblock > .lb-head > .lb-scalars > .lb-scalar`
>   （标量列横向排布；`scalarCssClass` 只按 control 分宽度档：ref/text 宽、number/bool 窄、
>   textarea 整行）+ `.lb-nests > .lb-nest > .lb-nest-b`（嵌套字段各自换行成块、占满容器宽度）。
>   只读视图同一判定（`listTable` → `listBlockTable`）。
> · **能力不变**：增删步骤（`data-ladd`/`data-ldel`）、行内编辑（`data-lcell`）、引用候选
>   （`data-lref`）、条件行编辑器（`data-condcell`）、键值对表格（`data-mapcell`）全部沿用批4/批5
>   接线；跨模块保存链（校验 → 备份 → 原子写 → 回退）零改动。**纯标量列表**（如怪物 `actions`）
>   仍用可增删行表格，不强制块布局。
> · **样式（tokens 驱动）**：`.lblocks/.lblock/.lb-*` 落在 `index.html` 组件 CSS；块分隔 = 外层
>   `--line-strong` 细边框 + 左侧 `--bw-accent` 色条 `--line-accent-dash`，嵌套块之间 `--line-faint`；
>   `.lb-nest-b` 宽度 100%，嵌套只读块取消 `.ro-cell` 的 260px 上限、嵌套表格 `width:100%`
>   → 不再有右侧大片空白；仍放不下时由既有 `.ltable-scroll` 细滚动条兜底（不再出现 9 列挤一屏 5 列）。
> · **销项**：需求 §五 V13（条件编辑器撑高行高、同行基线错位）、V14（9 列宽表横向滚动、首屏只见
>   前 5 列）随块状布局消除；V12（引用下拉文本被裁）由 `.lb-scalar.ref` 宽档缓解。
> · 回归测试 = `tests/unit/test_editor_batch51_blocks.py`（列 nested / block_layout 判定 + 全包扫描
>   「block_layout 恒等于存在 nested 列」+ 合成包任意字段名零硬编码 + 缺类型按值兜底 + node 执行
>   `listTableBody`/`listBlockBody`/`listTable` 证块结构·接线·纯标量回退表格 + CSS 令牌与宽度断言）。

> **批 5.2 实现锚点**（视觉细则清零 V1–V6 / V9–V11；只动展示层，校验/保存链零改动）：
> · **V1 字段网格**：`api.help_card` 新增 `hint = _hint(fm)`；前端 `helpLines` 增「提示」行、
>   `helpTriggerHtml` 支持 `view_note`（只读视图说明）并入卡片副本；`fieldRow` 删除行内
>   `fieldHint`（连同 `hintBits`/`hintHtml` 与 `.hint` CSS 一并移除），只读说明改由 `view_note`
>   进卡 → 有无提示的行高一致。
> · **V2 顶栏**：新增 `packOptionText`/`packOptionTitle`/`syncPackTitle`（完整名称/标识/版本/
>   模块数挂 `title`）；CSS `@media(max-width:760px)` 先舍品牌字样，包名/状态可收缩省略号、
>   身份切换固定宽。
> · **V3 空值口径**：新增常量 `INPUT_EMPTY="未填写"`；`editControl`（text/number/textarea）与
>   `listCell`（text/number）统一占位符；只读/列表空值仍走 `EMPTY_TEXT`，下拉未选保留
>   「（未选择）」。
> · **V4 回退真禁用**：新增纯函数 `rollbackDisabled(backup,editable,busy)` + `refreshRollbackButton()`
>   唯一出口；`refreshBackup`（含失败分支）/`setBusy`/`selectModule`（清 `state.backup`）/
>   空模块/`renderRoleTag` 统一调用；CSS `.btn.danger:disabled` 灰边灰字（tokens）。
> · **V5 元数据横幅**：`renderDetail` 横幅改分段（字段数/分组数/可编辑 + `.src` 来源路径）；
>   `.meta{flex-wrap:nowrap;overflow:hidden}` + `.meta .src` 省略号 + `title="元数据来源：…"`。
> · **V6 计数口径**：新增 `moduleCountLabel`（含子项 →「N（含子项）」+ 悬停「本模块 + 子项」明细；
>   叶子 → 裸计数）；`renderModules` 用之，栏头对层级包标注「计数含子项」。
> · **V9 列表区块副标题**：新增 `labeledKey(label,key)`「中文（键）」；后端
>   `_path_field_label`（支持 `steps[].to` 列表通配）+ `_association_sections` 补
>   `field_label`/`local_field_label`；前端副标题/空态/命中行统一走它。
> · **V10 列表引用列**：引用列 `.ref` 宽度档（170px）、`select.cin`/表格内下拉预留箭头区
>   （`padding-right: --sp-9`）、`refSelectTitle`（下拉 `title` = 「当前：名称（标识）」+ 非法提示）、
>   `refValChips` 恒有 `title` 全文 + `.mono2` 省略号；只读表单元格加 `title` 全文。
> · **V11 列表表头**：新增 `headerLabelHtml`（只留中文 + `title="中文（键）"`），表格表头与
>   块状布局标签统一改走它；主表单行/对象子字段仍保留批3.5「中文名 + 弱化键名」。
> · **通用性**：所有新逻辑只按控件形态/元数据字段（control / own_count / label）判定，不含任何
>   内容包模块名/业务字段名。
> · 回归测试 = `tests/unit/test_editor_batch52_visual.py`（后端 hint/label + node 执行 7 个纯函数
>   + 静态 CSS/结构断言，23 用例）；并同步批4/批51/字段标签/宿主用例的既有断言。

> **批 6 实现锚点**（新增/删除条目 + ID 生成 + 检索；写入仍复用批2 唯一链路）：
> · **ID 规则元数据（新增维度，尾部默认值）**：`ModuleMeta.id_rule`（`""` 自动 / `"slug"` /
>   `"prefix_seq"`）+ `id_prefix`（缺省用 `kind`，再缺省用模块名）。规则集中实现于
>   `qbot_rpg/web/api.py::slugify` / `id_rule_spec` / `suggest_entry_id`（前缀+递增序号保证不重复，
>   slug 取不出 ASCII 时**不臆造拼音**）。只影响「建议 ID」，ID 合法/唯一仍由既有校验器
>   （R-5 命名空间唯一 + `key_regex`）裁定，不新写规则。
> · **新增条目**（模块级）：`GET …/module/{m}/new`（`new_entry_detail`：建议 ID + 元数据 `default`
>   初始化字段 + 分组；ID 单独渲染不进字段网格）｜`GET …/suggest_id?name=`｜
>   `POST …/module/{m}/id_check`（`check_entry_id`：空/非法/同模块或同命名空间重复 → 红）｜
>   `POST …/module/{m}/entry`（`editor_ops.create_entry`：唯一性红拦 → `check_pack` → 备份 →
>   原子写 → 回读复核，失败自动回退）。`api.new_entry_slot` 给出新条目槽位（list=追加下标 /
>   map·object=键），写链路的红黄作用域据此收敛到新条目自身。前端 `+ 新建条目`（只读身份禁用）+
>   可编辑建议 ID + 「按名称生成」+ 即时唯一性状态（本地索引）+ 人话说明（`ID_HINT`）。
> · **删除条目**：`GET …/entry/{m}/{id}/refs`（`api.reference_scan`：按字段元数据 `ref` 递归 +
>   关联声明路径，二者皆元数据驱动）｜`POST …/entry/{m}/{id}/delete`
>   （`editor_ops.delete_entry`：被引用检查 → 备份 → 原子写 → 回读复核）。**被引用默认不拦**：
>   仅容忍「指向被删条目的 R-4 引用缺失」（`_is_dangling_to`），其余红拦照常阻断/回退；
>   引用者以黄提示人话列出（`reference_warnings`：模块显示名「条目名」的「字段中文名」引用了它）。
>   前端「删除条目」按钮 + 二次确认（删哪个 / 不可撤销 / 可回退）+ 引用者清单。
> · **检索**：`GET …/entry-index`（`api.entry_index`：全包条目按模块分组，含 namespace）——
>   一次拉取，前端 `EditorEntry.searchIndex/filterEntries` 本地过滤（<200ms，见批报实测）；
>   中栏搜索框作用域写明「当前模块 · 按名称/ID」；新增全局检索框，结果按模块分组、点击
>   `jumpTo` 跳转并切模块。中栏过滤与全局检索互斥（有全局查询即显示分组结果）。
> · **一致性**：`refreshAfterMutation`（新建/删除后）= `loadModules`（左栏计数/层级）+
>   `loadEntries`（中栏条数）+ `loadEntryIndex`（索引）→ 再选中新条目；页签计数随条目详情重画；
>   新建态用独立草稿 `state.newDraft`（退出/保存后主草稿指针还原），主表单保存/校验按钮在新建态
>   置灰，取消/离开有未保存守卫。
> · **顺带修复（实机复现）**：`api._read_json` 读取缓存原先只按 `(path, mtime_ns)` 作键——原子写
>   （`os.replace` 新文件）在同一时间戳刻度内可能与上一版 mtime 相同 → 读到旧内容，导致
>   「删除 → 回退」的回读复核假阴性。改为 `(path, mtime_ns, size, ino)` 作键。
> · 回归测试 = `tests/unit/test_editor_batch6_crud_search.py`（元数据/ID 规则 + 新增落盘/默认值/
>   红拦/只读 + 被引用扫描/删除/回退 + 全包索引 + node 执行 `EditorEntry` 过滤·分组·唯一性·
>   人话 + HTTP 端点 + 通用性护栏，39 用例）。
> · 实机证据（临时内容根 `/tmp/ed_b6_live`，非仓库包）：建议 ID → 新建（文件出现 `live_probe`）→
>   删除（文件消失）→ 回退（文件复原）；删除被引用条目 `cw_delay` → 4 条引用者黄提示、默认不拦
>   → 回退复原；GM 只读身份新增/删除 403。

> 每批都必须：① 代码 + 测试 ② **实机截图发用户**（终端极简风） ③ 台账登记 ④ 全量测试绿 + 校验器绿

- **批 0 · 设计令牌定稿**：把定稿样式（终端极简风 s3/e3）抽成样式 token 文件（色/字/间距/控件态），
  出 1 张「组件总览图」（按钮/输入/页签/表格/提示/空态）供你确认 → 之后所有批都照它画。
- **批 1 · 只读骨架**：宿主起服 + **内容包选择器** + **三栏骨架**（第1栏=模块分类导航【支持层级：物品 ▸ 装备】、
  第2栏=当前模块条目列表【只名字/滚动条/无翻页】、第3栏=大编辑面板，只读渲染，面板顶部标注元数据来源）
  + 条目详情**只读**渲染（字段类型 → 只读展示；面板顶部标注「字段由元数据生成 · N 字段 · M 分组」）。
- **批 2 · 编辑与保存**：可编辑控件 + 草稿态（未保存改动提示）+ 校验 + **原子写落盘** + 自动备份 + 回退。
- **批 3 · 分区页签**：按元数据分组渲染页签（默认只显示第一组），分组顺序由元数据决定。
- **批 4 · 列表型与引用型字段**：可增删行表格（列表）+ 引用选择器（ref，含搜索与名称显示）。
- **批 5 · 技能派生界面**（你点名）：派生链编辑——「触发技 → 步骤（**派生条件** / **派生技能** / **派生消耗** / 数值覆盖）」；
  实数据形态：`skill_chains.json` 的 `{trigger_skill, steps:[{from,to,condition{count|self_marks|target_marks},consume,mode,variant_override}]}`
  ——**通用实现**（引用字段 + 条件行 + 数值覆盖组），不写死"派生"这个业务词以外的东西。
- **批 6 · 增删条目 + 检索**：新增/删除条目、ID 生成规则、搜索/过滤。
- **批 7 · 终验**：全量回归 + 实机抽查（多包切换验证「换包零改动」）+ 文档定稿 + 使用说明。

## 五、不做的事（明确排除）

- 不做富文本编辑、不做多人协同、不做版本 diff 视图；不复刻已删除的旧编辑器目录结构（新写）。
- 不把任何内容包的专属业务结构（属性表/套装/词条/buff 表）写进编辑器框架——它们只作为元数据声明的示例。
- 不在本方案外"顺手"加功能；需要就新开一批并先更新本文件。
