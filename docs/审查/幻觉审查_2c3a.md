# 幻觉审查报告 · 细化_2c3a_强化数值曲线.md

- **审查对象**：`/root/QBot-TurnTellerRPG/docs/细化/细化_2c3a_强化数值曲线.md`（356 行，wc -l 实测 356 ✅ 与头部自述一致）
- **依据定稿**：`/root/docs_archive/RPG框架项目/强化系统设计定稿.md`（v1.0，235 行，wc -l 实测 235 ✅ 与细化 L4/L356 自述一致，全文逐行核对）
- **核对方法（四查）**：①行号引对——每条引用定位到有实义文本的行，核查空行/代码块边界陷阱；②内容一致——引用原文与声明逐字比对（数值/语义不得改写）；③补白显式标注——自创字段/规则/默认值必须标【补白/工程补白】或细化建议；④数值有依据——每个数字可指到定稿原文或显式补白出处。
- **核对范围**：§0 键值索引 8 行、§1 消耗曲线（1.1-1.5 + 逐级/累计表 12 行）、§2 成功率（2.1-2.4 + 幸运公式）、§3 上限（3.1-3.4 + 数值表）、§4 失败分级（4.1-4.4）、§5 字段表 D-1~D-13 + D-3b/D-3c 共 15 行 + 校验器映射 V1-V7、§6 验收用例 TC-01~25 共 25 条、§7 接缝审计 S1-S4、§8 行号回查表 8 行——共 66 处行号引用逐条回查；外部引用点 5 处（细化_0 R-04 ×4、细化_1a、细化_2c2a、转移 L127/L160）grep 实证；数值 40+ 处（累计三角形数 78/7800、√ 运算、clamp 示例、线性累计）全部重算。

---

## 总体结论

**P0 = 1 条 / P1 = 1 条 / P2 = 9 条**

**评分：C**（P0 存在必改项）

**一句话**：全文 66 处定稿行号引用**全部真实且指向正确实义行**（零空行/零边界引用，密度高、30+ 处逐字吻合），R-04 定案引用与《细化_0》逐字一致，§5 有总括补白声明、数值算术全部验证通过；但 §4.1 失败分级的「段判定基准」与「掉级后果值」存在与定稿 L42/L54/L155 实质冲突（+4→+5 失败应为掉到 +3，文中两处写成「掉到 +4」且「目标等级」公式自相矛盾），另有 D 编号错位 3 处、推导规则漏标补白 8 处——修订后可升 B。

---

## P0（严重幻觉：编造 / 与定稿实质冲突）— 1 条

### P0-1　失败分级段判定基准 + 掉级后果值与定稿 L42/L54/L155 实质冲突（+4→+5 失败应掉到 +3，文中两处写成「掉到 +4」）
- **位置**：细化 L200（段判定公式）、L201（边界示例）、L313（TC-19 期望值）
- **声明**：
  - L200：`fail_tier_split = 3：目标等级（含）≤ 3 属低段（失败不变）；目标等级 ≥ 4 属高段（失败 -1）`
  - L201：`+3→+4 失败：+3 是低段上限（在低段 → 不变）；+4→+5 失败：+4 已在低段以上 → 掉到 +4。即"目标等级 ≤ split 不变，目标等级 > split 掉级"`
  - TC-19（L313）：`高段失败掉级 | +4 目标失败 → 维持 +3（+3→+4）；+5 目标失败 → 掉到 +4；不掉至 0 以下`
- **核对**：
  - ① 段判定基准自相矛盾：定稿 L53-54 按**当前等级**分段（`低等级段（+0 → 低段上限，默认 +3）：失败=不变` / `高等级段（低段上限以上，默认 +4+）：失败=-1`），L155 注释同口径（`// +3 以下失败不变，+4 起失败掉级`）。细化 L200 换成**目标等级**口径（目标 ≥4 即高段）——按此口径 +3→+4 失败应掉级（当前 +3 → +2），与该文档自己的 L201 首例（`+3 是低段上限 → 不变`）和 TC-19 首半句（`+4 目标失败 → 维持 +3`）**直接矛盾**。
  - ② 掉级后果值错误：定稿 L42 反例严格执行当前等级 −1：`❌ 失败！强化等级 -1（铁剑+5 → +4）`（命令为 /强化 铁剑+5 → +6，当前 +5 失败 → +4）。按此，+4 强化到 +5 失败 → **掉到 +3**。细化 L201 与 TC-19 两处均写「掉到 +4」——无论按「当前−1」还是「目标−1」口径都无法成立（当前−1 得 +3；目标−1 得 +4=当前=未掉级，与「掉级」语义及定稿 L42 矛盾）。
  - ③ 细化自身 L205 才是正确口径：`高段失败：当前 +N → +(N−1)`（+4→+5 失败 → +3）——与 L201/TC-19 的「掉到 +4」互相打架。
- **证据**：定稿 L42 `❌ 失败！强化等级 -1（铁剑+5 → +4）`、L53-54（低段 +0~+3 不变 / 高段 +4+ 掉 1 级）、L155 `"fail_tier_split": 3, // +3 以下失败不变，+4 起失败掉级`；细化 L205 `高段失败：当前 +N → +(N−1)（一次 1 级…）`。
- **建议**：统一为「按**当前等级**分段：当前 ≤ split 失败不变，当前 > split 失败当前 −1」；L200 公式删「目标等级」改「当前等级」；L201 第二例与 TC-19 期望值改为「+5 目标失败 → 掉到 +3（+4→+5）」；并删去 L201 结尾「目标等级 ≤ split 不变，目标等级 > split 掉级」的错误归纳（该归纳与其首例所列 +3→+4 不变相悖）。若实现按 TC-19 写测试，断言「掉到 +4」将与按 L42 的实现（+3）必冲突，属落地必错项。

---

## P1（明显问题：行号引错 / 内部编号冲突 / 补白漏标）— 1 条

### P1-1　D 编号错位 3 处（D-4 被同时指认为 luck_affects / 成功率曲线 / shatter_max）
- **位置**：细化 L104、L240、L242
- **声明**：
  - L104：`默认表按目标等级索引（success_curve[].to，D-4）`
  - L240（D-2 行）：`0–max(maxByRarity) | 与 D-4 曲线、D-1 上限一致`
  - L242（D-3b 行）：`0–100；>D-4 即 shatter_max 仅警告`
- **核对**：§5.1 字段表自定编号 **D-4 = `settings.luck_affects`**（L244，bool 型幸运开关）——不是成功率曲线，也不是 shatter_max。三处引用语义全部错位：L104/L240 所述曲线应为 **D-10（success_curve[]）**；L242 所述 shatter_max 应为 **D-3c**（L243）。属文档内部编号冲突，实现者按编号挂校验/取默认值时会被误导。
- **证据**：细化 L244 `| D-4 | settings.luck_affects | true | bool | …`；L250 `| D-10 | success_curve[] | …`；L243 `| D-3c | settings.shatter_max | 5 | …`。
- **建议**：L104/L240 的「D-4」改「D-10」；L242 的「>D-4」改「>D-3c（shatter_max）」。

---

## P2（轻微问题：行号偏移 / 引用过弱 / 取舍未标注 / 补白不齐）— 9 条

### P2-1　「已达上限零消耗、零随机」推导规则未标补白
- **位置**：细化 L84（§1.5 表首行）、L167（§3.2）
- **声明**：`提示"已达强化上限"；零消耗、零随机（材料金币均不退不扣）｜出处 L115`；`不扣强化石、不扣金币、不掷随机（对应 TC-06/14）`
- **核对**：定稿 L115 = `到达上限：/强化 提示"已达强化上限"`——仅覆盖提示文案；「零消耗」为合理推导但定稿未述，正文未标【补白/细化建议】（对比 L85/86 同类推导标了「细化建议」）。L13 引 L6 亦无此语义。属推导值漏标补白。

### P2-2　「失败不返还消耗材料」未标补白
- **位置**：细化 L88（§1.5 失败行）、L207（§4.2）
- **声明**：`失败 | 按 §4 分级惩罚，不返还消耗材料 | L38 / L42 / L53-54`；`失败仍消耗已扣材料（L38 / L42：失败提示惩罚类型），不退还`
- **核对**：L38/L42 为失败示例文本、L53-54 为分级规则，均只涉及「惩罚类型/等级变化」，**不含材料不退还语义**；「不返还」为推导（由「消耗」概念引申），未标补白。引用列仅支撑「失败提示惩罚类型」。

### P2-3　「先扣强化石再扣金币」消耗顺序自创规则未标
- **位置**：细化 L78（§1.4）
- **声明**：`消耗顺序：先扣强化石再扣金币；任一不足即拒绝（见 TC-04/05）`
- **核对**：定稿 §五（L97-106）未定义消耗顺序，属实现层取舍；TC-04/05 只验证「不足即拒绝」，未验证顺序。未标【工程补白】。

### P2-4　clamp 0/100 与 rand 判定判据未标工程补白
- **位置**：细化 L119（公式块）、L138-141（§2.4 边界表）
- **声明**：`final_rate = clamp(rate, 0, 100)`；`幸运使率 < 0 → clamp 到 0 / > 100 → clamp 到 100`；`rand(0,100) < rate 成功，边界含下限`
- **核对**：定稿 L87 只有公式本体（基础率 + √差），无 clamp 无判定式；「保留 1 位小数」在 L117 已显式标「细化定取舍」✅，但 clamp 与 rand 判定同为自创口径却未标补白。另 L123「开关关闭时修正恒为 0」有 L89 支撑可接受。

### P2-5　「保护石未配置（空）时 /强化保护 不可用」细化建议未标
- **位置**：细化 L227（§4.4）
- **声明**：`未配置（空）时 /强化保护 指令不可用（提示未开放）`
- **核对**：定稿 L188 仅注保护石物品 ID「可空」，未定义空值时指令行为；规则为细化自创（§7-S4 同口径标为「提示未开放（软降级）」待确认），正文未标【细化建议】。

### P2-6　门槛表「非降序」校验判据自创未标补白
- **位置**：细化 L177（§3.3）
- **声明**：`开启时校验配置存在且非降序（校验器 5 关联，L202）`
- **核对**：L202 = `5. 转移/等级门槛开关与配置一致性`——只要求「开关与配置一致性」；「非降序」为细化自创判据，未标补白（同 2c2d 先例 V3 跳档判据同类问题）。

### P2-7　TC-25「本次消耗（石 N+金币 M）」超出定稿 L216 内容
- **位置**：细化 L324（TC-25）
- **声明**：`/强化信息 | 展示 当前 +N / 成功率（含幸运拆分）/ 上限 / 本次消耗（石 N+金币 M）与说明（L216）`
- **核对**：定稿 L216 = `/强化信息 <装备>  # 查看当前 +N / 成功率 / 上限`——只含 当前+N/成功率/上限，「本次消耗」与「幸运拆分」为细化扩展内容（幸运拆分有 L90/L131 依据，本次消耗无依据），且引用列以 L216 涵盖之，引用略过强。属引用过弱/扩展内容未标。

### P2-8　L35「定稿 D-8」编号体系误植（D-x 是本文档字段编号，非定稿编号）
- **位置**：细化 L35（§1.1）
- **声明**：`默认 stones_per_level=1，定稿 D-8`
- **核对**：定稿全文无 D-x 编号体系（D-x 为本档 §5.1 自建字段表编号）；「定稿 D-8」暗示定稿以 D-8 标注该字段。数值 1 本身真实（定稿 L177 `"stones_per_level": 1`），仅是编号归属措辞错误；L47 同型引用（D-7）未加「定稿」前缀则无此问题。

### P2-9　L356「SEAM S1-S4 待确认项」与 §7/R-04 已定案状态不一致
- **位置**：细化 L356（结尾）
- **声明**：`附带 SEAM S1-S4 待确认项`
- **核对**：§7 表内 S1/S2 已标「✅ 已定案（细化_0 R-04）」（L334-335），R-04 原始决议亦明确「关闭『待人工确认』」；仅 S3/S4 待确认。结尾将 S1-S4 整体列为「待确认项」为过时口径，易误导读者以为曲线缺口仍未定案。属状态标注失实（轻微）。

---

## 核对亮点（加分项）

1. **行号引用密度高且全真**：全文 66 处行号引用（含区间）逐条回查，**0 处空行、0 处代码块边界（```）引用、0 处起点偏移**——包括最容易出错的 L34-45 流程代码块、L176-182 JSON 区段引用，全部落在有实义文本的行上（对比 2c2d 审查发现的两处空行引用）。
2. **引用内容逐字一致率极高**：L6/L16/L28/L87/L88/L89/L90/L103/L105/L113-114/L115/L139/L141/L142/L154/L155/L198-204/L215/L216 等 30+ 处与定稿原文逐字吻合；§0 键值索引 8 行出处全部落在正确行。
3. **R-04 外部引用完全真实**：L105/L263/L296/L334/L335 四处「细化_0 R-04」与《细化_0_仲裁决议汇总.md》R-04 行（L19）及第四章决议全文（L68-77）逐字一致（默认补 +11 20%/+12 15%、high 延至 [7,8,9,10,11,12]、内容包更高上限须自配齐档位曲线、V2 硬拦+TC-12 拒绝加载）——无把自创补白伪称兄弟文档定值的情况（本档未借用任何兄弟文档字段值）。
4. **数值全部可重算验证**：消耗累计三角形数（78/7800 ✅）、TC-08 √ 运算（√36=6→+6.0pp；√4−√16=−2→−2.0pp ✅）、TC-09 clamp 示例 ✅、TC-17 线性累计（+3×5=15、+4×3=12 ✅）、+12 累计 +60（12×5 ✅）、档位映射（低/中/高按目标等级 1-3/4-6/7-10 ✅）——算术零错误。
5. **补白纪律部分良好**：L117 保留 1 位小数标「细化定取舍」、L85/86/L124/L206 推导规则标「细化建议」、§5 L233 有总括声明（「可配范围」为细化建议，均不改变定稿默认值语义）、§7 跳出点声明「不改变定稿既定默认值」——同类处理在本次 P2 中已逐条正反举证。
6. **计数自洽**：头部自述 235 行/25 条用例与 wc 及 TC-01~25 逐项加总完全一致；校验器 7 条（V1-V7 ↔ 定稿 L198-204）一一对应；字段表 15 行无重复编号（除 D-4 引用错位外）。

---

## 结尾核对基准说明

- 核对基准：定稿 235 行全文逐行通读（read_file 分页 1-235 全量读入），细化 356 行全文通读（1-356 全量读入）；66 处行号引用 + 5 处外部引用点 + 40 处数值逐一验证。
- 标级口径对齐《幻觉审查_2c2d.md》：P0=编造/与定稿实质冲突（必改）、P1=行号引错/内部编号冲突/补白漏标、P2=行号偏移/引用过弱/取舍与结构改写未标注。
- 本档未发现虚构定稿行、未发现把推导冒充原文（推导均有定稿支撑或已标细化建议，仅漏标处列入 P2）、未发现兄弟文档伪引用（R-04/细化_1a/细化_2c2a 全部实证存在且口径相符）。
- 唯一必改项集中在失败的「段判定基准」与「掉级结果值」（P0-1）：涉及定稿 L42 示例的语义解释，请实现侧与策划确认「当前−1（+4→+5 失败 → +3）」后统一修订 L200/L201/L313 三处表述。

---

## 补遗（2026-08-18 12:20 · 轮 13 合并记录）

1. **P0-1 修复已落地**：12:08:53 对 L200/L201/TC-19 三处完成修改（当前等级口径、+4→+5 失败掉到 +3、TC-19 三子例含定稿 L42 示例），来源为并行审查轮（deleg_975a3112 同批派发）；git diff 可查。本报告撰写于修复前，保持原判定（C）与 P0=1 计数。
2. **并行审查补充发现 P1（我方漏检，父复核已确认定稿 L15 原文）**：§0 核心判据引用「L16」实为 **L15**（定稿 L15=「强化 = 给装备堆数值（+N）…」，L16 为「低等级失败无惩罚…」行）。本文 L12 与 §8 行号回查表 L345 两处已当场修正为 L15。
3. **并行审查 P2×10 查重**：与本文 P2×9 主体重叠——D 编号错位为同一根因（并行版记 D-3b/D-2 误指 D-4 两条，本文记 D-4→D-10/D-3c 一条）；补白漏标条目（失败不返还/达顶零消耗/硬拦截分级/碎判定序/clamp/rand）与本文 P2-1~P2-6/P2-8 一一对应。无实质性新待办，轮 30 按本文清单执行。