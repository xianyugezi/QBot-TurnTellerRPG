# 细化规划：2a1b 通道规则与刷怪配置

> 依据（三份定稿，行号引用格式：`文件简称§L{n}`）：
> - 副本系统设计定稿.md（简称 **副本**）→ /root/docs_archive/RPG框架项目/副本系统设计定稿.md
> - 怪物模块设计定稿.md（简称 **怪物**）→ /root/docs_archive/RPG框架项目/怪物模块设计定稿.md
> - 时间天气系统设计定稿.md（简称 **时间天气**）→ /root/docs_archive/RPG框架项目/时间天气系统设计定稿.md
> 作用域：2a1b = 网状地图通道规则（双向/单向/捷径/隐藏）+ 刷怪配置 spawn 表 + 时段联动出没条件；实现层细化，全部语义可追溯定稿行号；定稿未显式定义的字段/默认值以 **【工程补白】** 标注，遵循"只建议不限制"总纲。

---

## 〇、追溯总览

| 子系统 | 定稿落点 | 关键行号 |
|---|---|---|
| 网状地图结构 | 副本 §1 | L37/L41（网状）、L46–47（4 方向编辑） |
| 通道类型（双向/单向/捷径/隐藏） | 副本 §1 | L48–51 |
| 换区追击走通道 | 副本 §二 + 怪物 §4.4 | 副本 L76–77；怪物 L94–95 |
| 移动指令 | 怪物 §一 | 怪物 L13 |
| 刷怪 spawn 字段 | 时间天气 §三 | L140–148、L208–210 |
| 时段/季节/天气出没条件 | 时间天气 §4.3 | L206–211、L235 |
| 出没时段边界（3.16） | 时间天气 §4.3 | L209（对齐 3.16） |
| 条件引擎新键 | 时间天气 §4.0 | L176–182 |

---

## 一、通道规则（maps.json `exits`）

### 1.0 总则（网状结构）

| 规则 | 内容 | 定稿引用 |
|---|---|---|
| **R1** | 地图结构 = **网状**：多区域互相连通（节点+边，方向制），非线性单线 | 副本§L40–41（❌线性 → ✅网状，节点+边，方向制） |
| **R2** | 每地图编辑 **4 个方向**（上/下/左/右）各通向其他地图；**未配置的方向 = 死路（不可通行）**，玩家 /进入 该方向提示无通道 | 副本§L46–47（每地图编辑 4 个方向）；副本§L156（哪里能走哪里不能走） |

### 1.1 通道类型（`mode` 枚举）

| 规则 | 内容 | 定稿引用 |
|---|---|---|
| **R3** | **双向** `bidirectional`：地图1↔地图2 互相有通道，两端均可通行 | 副本§L48（双向 = 地图1↔地图2 互相有通道） |
| **R4** | **单向** `one_way`：地图1→地图2 只有一边；反方向 /进入 判定无通道、拒绝通行 | 副本§L49（单向 = 地图1→地图2 只有一边） |
| **R5** | **捷径 = 拓扑属性、非独立通道模式**：绕路连通 =「地图1→地图2 单向」+「地图2→地图3→地图1 绕回」两条以上边组合实现；实现层以单向边+回程边布线，不新增 `shortcut` mode | 副本§L50（捷径 = 绕路连通，地图1→地图2 单向，地图2→地图3→地图1 绕回）；副本§L43（熔岩坑道 = 捷径，单向门） |
| **R6** | **隐藏通道** `hidden`：满足条件才显示/可走，可选配置，不配 = 无隐藏 | 副本§L51（隐藏通道 = 满足条件才显示/可走（可选）） |

### 1.2 隐藏判定：条件打开（回答"走过一次解锁 or 条件打开"）

> **结论：定稿口径 = 条件打开，不是"走过一次解锁"。** 副本§L51 仅定义「满足条件才显示/可走」，通篇未定义"走过即解锁"持久态；故实现不引入通道解锁状态存档。

| 规则 | 内容 | 定稿引用 |
|---|---|---|
| **R7** | 隐藏通道判定 = **条件引擎条件满足** 才显示且可走；不满足 = 不显示 + 不可走（双层拒绝：显示隐藏 + 移动拒绝） | 副本§L51 |
| **R8** | 隐藏条件复用**条件引擎新键**：`{var:"season"}` / `{var:"period"}` / `{var:"weather"}`（及互译 [季节:X]/[时段:X]/[天气:X]） | 时间天气§L176–182（条件引擎新键互译表）；时间天气§L215–217（副本事件/dungeon.json 事件牌组同引用新键，零新机制） |
| **R9** | 隐藏通道条件可引用玩家进度（如 quest.json 子任务完成度），作为"地图熟悉度"的学习解锁渠道（探索版子任务：探索/收集/清剿/机制学习/情报） | 副本§L163–169（子任务形式，含"机制学习"）；副本§L156（熟悉通道/捷径是探索目标） |
| **R10** | 地图熟悉度 = 追击资源：熟悉（已解锁/已知路径）→ 捷径直追；不熟悉 → 绕矿洞岔路，错过追击窗口/遇精英 | 副本§L77（地图熟悉度 = 追击资源）；副本§L58–59（熟悉直追 vs 不熟绕路） |

### 1.3 通道与移动 / 换区 / 副本状态衔接

| 规则 | 内容 | 定稿引用 |
|---|---|---|
| **R11** | 移动指令：`/进入 <方向>`（上/下/左/右，空格分隔）；`/进入 <序号>`（数字 = 序号，不带 `*`） | 怪物§L13（指令分隔统一规范）；副本§L76（玩家 /进入 <方向> 走通道追击） |
| **R12** | 换区追击使用**同一套通道系统**：怪物逃跑换区 → 提示"逃到了 XX 地图" → 玩家 /进入 <方向> 走通道；换区后 BOSS 血量残血保持 + PV 恢复一半 + 开场技 | 怪物§L94–95（逃跑换区 → 玩家走地图通道去找，框架通道系统）；怪物§L97–99（残血保持/开场技）；副本§L78（残血 + PV 恢复一半 + 开场技） |
| **R13** | 通道成功移动 = 位置变更；安全区 /休息 不改变位置；非战斗状态移动**离开副本区域** → 副本重置 | 副本§L104（安全区休息 ≠ 离开副本，不改变位置）；副本§L81（非战斗状态离开副本区域 = 副本重置） |

### 1.4 配置 schema（maps.json `exits`）

```json
{
  "id": "molten_corridor",
  "exits": {
    "up":    { "to": "molten_entrance",    "mode": "bidirectional" },
    "down":  { "to": "molten_lava_tunnel", "mode": "one_way" },   // 单向门（捷径布线，副本§L43/L49）
    "left":  { "to": "molten_core",        "mode": "bidirectional" },
    "right": { "to": "molten_mine",        "mode": "hidden",
               "condition": { "var": "subquest_done", "op": "eq", "param": "learn_mechanic" } }
  }
}
```

字段说明与校验：

| 字段 | 类型 | 必填 | 说明 | 引用 |
|---|---|---|---|---|
| `exits.<方向>` | object | 否 | 4 方向键 `up/down/left/right`；缺省方向 = 死路 | 副本§L47、L156 |
| `exits.<方向>.to` | string | 是 | 目标地图 ID，须存在（引用检查） | 怪物§L295–299（引用检查校验器精神）；副本§L270（maps.json 副本地图链） |
| `exits.<方向>.mode` | enum | 是 | `bidirectional` / `one_way` / `hidden` | 副本§L48–51 |
| `exits.<方向>.condition` | object | hidden 时必填 | 条件引擎表达式（§1.2 R7–R9）；双向/单向不可配置 | 副本§L51；时间天气§L176–182 |

校验器：① `to` 地图 ID 存在（硬拦）；② `mode` 枚举合法；③ `hidden` 必带 `condition`（硬拦）；④ 双向一致性提示（A→B 双向而 B→A 单向 → 黄提示"双向不对称"）。

---

## 二、刷怪配置（maps.json `spawn` 表）

### 2.0 归属与层级

| 规则 | 内容 | 定稿引用 |
|---|---|---|
| **R14** | 刷怪行挂在 **地图级（maps.json）**，非副本级：副本按 `maps` 引用地图，刷怪配置自然继承 | 时间天气§L131/L208（maps.json 怪物行）；副本§L191/L207（maps maps 引用）；副本§L229（两组副本引用同一组地图 ID） |
| **R15** | 探索副本与 BOSS 副本**共用**同一地图刷怪配置（地图机制/怪物分布两副本共用） | 副本§L229–232（地图共用：怪物/采集/宝箱/机制完整保留） |

### 2.1 字段级 schema（spawn 行）

```json
// maps.json 地图 spawn 数组（时间天气§L140–148 样例原样对齐）
"spawn": [
  {
    "enemy": "幽灵",                       // enemies.json 引用（id 或 name）
    "count": 5,                            // 该行同时在场数量上限
    "respawn_minutes": 10,                 // 刷新间隔（分钟）
    "active_time": { "from": "20:00", "to": "06:00" },  // 现实钟点出没窗口（3.6 保留）
    "seasons": ["autumn", "winter"],       // 季节限定（空 = 全年）
    "periods": ["night", "midnight"],      // 时段限定（空 = 全天）
    "weather_weights": { "fog": 2, "rain": 0.5 }        // 天气出现率倍率（默认 1；0 = 该天气不刷）
  }
]
```

字段语义（逐行追溯）：

| 字段 | 类型 | 默认 | 语义 | 引用 |
|---|---|---|---|---|
| `enemy` | string | 必填 | enemies.json 引用（id/name 均可）；取值对应怪物结构 id/name/area | 时间天气§L141；怪物§L19–22（id/name/tier/area） |
| `count` | int | 【工程补白】1 | 该 spawn 行**同时在场数量上限**（刷怪上限之一；击杀才可补刷） | 时间天气§L142 |
| `respawn_minutes` | int | 必填 | 刷新间隔（分钟），≥1；被 `weather_weights` 变速（R26） | 时间天气§L143、L210 |
| `active_time` | {from,to} | 否（空=全天） | 现实钟点出没窗口，3.6 语义保留（"20:00-06:00" 跨夜写法沿用） | 时间天气§L144、L28（现实钟点层保留不动） |
| `seasons` | string[] | 否（空=全年） | 季节限定过滤：值 ∈ 四季枚举 `spring/summer/autumn/winter` | 时间天气§L145、L40 |
| `periods` | string[] | 否（空=全天） | 时段限定过滤：值 ∈ 五时段枚举 `dawn/noon/dusk/night/midnight` | 时间天气§L146、L41 |
| `weather_weights` | {天气:倍率} | 否（默认 1） | 天气出现率倍率；**0 = 该天气不刷**；作用于刷新计时（R26） | 时间天气§L147、L210 |

### 2.2 出没语义规则（与上限）

| 规则 | 内容 | 定稿引用 |
|---|---|---|
| **R16** | `seasons`/`periods` 为**限定过滤**，语义与 `active_time` 对齐并叠加：**全部条件满足才出没**（AND） | 时间天气§L209（语义与 active_time 对齐并叠加，全部满足才出没） |
| **R17** | **出没时段边界（对齐框架 3.16）**：季节/时段结束 → 场上怪物**移除**，提示"对方逃跑了" | 时间天气§L209（3.16 出没时段边界规则同样适用） |
| **R18** | `weather_weights` **只影响刷新/补刷，不驱逐已在场怪物**：天气变化本图已有怪不受影响 | 时间天气§L210（只影响刷新/补刷，不驱逐已在场怪物） |
| **R19** | 天气/季节**不打断战斗**：战斗会话内每回合开始重读当前时段/天气（懒计算），"对方逃跑了"仅发生于非战斗/地图展示场景 | 时间天气§L91（变化不打断战斗，懒计算，天气/季节不驱逐战斗中怪物） |
| **R20** | `active_time`（现实钟点层）与 `seasons`/`periods` 五时段键**并存正交**（AND 叠加）：前者管"几点现实时间"，后者管"游戏周期第几段" | 时间天气§L235（并存正交，AND 叠加）；时间天气§L19（三套周期互相独立） |
| **R21** | 地图级**在场怪物总量上限** `max_alive`（聚合所有 spawn 行）：【工程补白】默认 10，可配——防止多 spawn 行聚合刷爆地图 | 定稿未显式定义 · 工程补白（依据"只建议不限制"总纲） |
| **R22** | **rank 权重**：怪物稀有度档 = 怪物本体 `tier`（`normal`/`elite`/`boss`），非 spawn 字段；精英怪 = `tier: elite` 刷怪行（网状图中精英遭遇）；多 spawn 行间的出现比例权重 `spawn_weight`（可选，默认 1）【工程补白】 | 怪物§L21（tier normal/elite/boss）；副本§L43（岔道/矿洞 绕路/宝箱/精英）；时间天气§L147 倍率语义（权重调速同理） |
| **R23** | 刷新判定公式：常规 → 每 `respawn_minutes` 分钟补 1 只（受 `count` 上限约束）；带天气权重 → 见 R26 | 时间天气§L143、L210 |

### 2.3 校验器

| 规则 | 内容 | 引用 |
|---|---|---|
| **R24** | `enemy` 引用必须存在（enemies.json id/name 解析，硬拦） | 怪物§L295–299（引用检查校验器精神：行动/掉落 ID 引用存在） |
| **R25** | `seasons` ∈ 四季枚举、`periods` ∈ 五时段枚举（硬拦）；`weather_weights` 的 key ∈ 注册天气集（硬拦） | 时间天气§L40–41（枚举写死）；时间天气§L166（校验器硬拦，引用不存在）；时间天气§L230（mults 键 ∈ 注册天气集） |
| **R26** | `count`/`respawn_minutes` 非负整数，`respawn_minutes ≥ 1`；`weather_weights` 值为非负可配（0 = 不刷） | 时间天气§L147、L210 |

---

## 三、时段联动（出没条件判定链）

### 3.1 判定链（全 AND，缺一不出）

```
出没 = active_time(现实钟点窗口, 3.6) 满足
     AND seasons 满足（空=全年恒真）        [时间天气§L145/L209]
     AND periods 满足（空=全天恒真）        [时间天气§L146/L209]
     AND weather_weights[当前图当前天气] ≠ 0 [时间天气§L147/L210]
```

| 规则 | 内容 | 引用 |
|---|---|---|
| **R27** | 出没判定 = 上述全 AND 叠加；`seasons`/`periods` 空数组/缺省 = 全年/全天（恒真） | 时间天气§L145–146（空=全年/全天）、L209（全部满足才出没） |
| **R28** | **有效刷新间隔 = `respawn_minutes / weight`**：weight ≥ 1 刷更快，0 < weight < 1 更慢，weight = 0 = 该天气下不刷 | 时间天气§L210（有效刷新间隔 = respawn_minutes / weight） |
| **R29** | **出没边界移除（3.16）**：季节/时段窗口结束 → 场上怪物移除"对方逃跑了"；仅影响存在性，不计入战斗结算 | 时间天气§L209（对齐 3.16）；时间天气§L91 |
| **R30** | **天气不驱逐**：天气变化只调刷新间隔，已在场怪（含战斗中）不驱逐、不打断 | 时间天气§L210、L91（天气/季节不驱逐战斗中怪物，与 3.16 刻意区分） |

### 3.2 条件引擎键（隐藏通道 & 其余消费方复用）

| 规则 | 内容 | 引用 |
|---|---|---|
| **R31** | 新键：`{var:"season",param:X}`(四季) / `{var:"period",param:X}`(五时段) / `{var:"weather",param:X}`(玩家**当前所在图**的当前天气，上下文绑定) | 时间天气§L178–180 |
| **R32** | 季节/时段为**全局值**，天气按**玩家所在图**取值（上下文绑定）；隐藏通道、副本事件、任务/NPC 活动窗口统一复用（零新机制） | 时间天气§L182（上下文绑定）；时间天气§L215–217（事件活动窗口引用新键） |

---

## 四、验收测试用例（TC-x）

> 编号规则：TC-01～07 移动/通道，TC-08～09 刷怪基础，TC-10～12 时段，TC-13～17 天气/叠加/校验。均在探索副本（同一组地图）内执行。

| # | 用例名 | 前置 | 操作 | 预期 | 引用 |
|---|---|---|---|---|---|
| **TC-01** | 双向通道移动 | 位于入口区 A | `/进入 右` 至 B，再 `/进入 左` | 两次均成功，位置在 A↔B 间往返 | 副本§L48、副本§L76 |
| **TC-02** | 单向通道不可逆 | 位于熔岩坑道（单向门入） | `/进入 <反向>` | 拒绝：提示无通道/不可走，不改变位置 | 副本§L49 |
| **TC-03** | 未配置方向=死路 | 地图某方向未配出口 | `/进入 <该方向>` | 拒绝：提示无通道 | 副本§L47/L156 |
| **TC-04** | 捷径绕路连通 | 网状图含单向捷径 + 绕回路径 | 沿捷径直达 + 原路绕回 | 捷径单向前进可达目标；经第三区绕回可达起点（闭环） | 副本§L50、L43 |
| **TC-05** | 隐藏通道·条件未满足 | condition（如未完成机制学习子任务） | `/进入 <隐藏方向>` + 地图展示 | 不显示该方向；/进入 被拒 | 副本§L51 |
| **TC-06** | 隐藏通道·条件满足 | condition 满足（子任务完成了/对应时段） | 地图展示 + `/进入 <隐藏方向>` | 通道显示且可进入 | 副本§L51；时间天气§L178–180 |
| **TC-07** | BOSS 换区追击走通道 | BOSS 触发换区（hp_below 30） | 提示追向 → `/进入 <方向>` 追击 | 提示"逃向了XX区域"（候选随机一）；走通道位置变更；追上后残血 + PV 恢复一半 + 开场技 | 副本§L75–78；怪物§L94–99 |
| **TC-08** | 刷怪基本出没 + count 上限 | spawn 行 `count:5` 全天 | 反复进入/待机 | 该怪在场 ≤ 5；不超上限 | 时间天气§L142 |
| **TC-09** | 击杀后按 respawn_minutes 刷新 | spawn `respawn_minutes:10` | 清光后等待 | 10 分钟后在场数恢复（受 count 限制） | 时间天气§L143 |
| **TC-10** | 时段限定出没 | spawn `periods:["night","midnight"]` | 白天时段进入该图 | 该怪不出现（时段之外） | 时间天气§L146、L209 |
| **TC-11** | 季节限定出没 | spawn `seasons:["autumn","winter"]` | 夏季进入该图 | 该怪不出现（季节之外） | 时间天气§L145、L209 |
| **TC-12** | 出没边界移除（3.16） | 夜→晨时段切换，怪在场 | 时段窗口结束 | 场上怪移除，提示"对方逃跑了"；不记战斗结算 | 时间天气§L209、L91 |
| **TC-13** | 天气权重变速 | `weather_weights:{"fog":2}`，respawn=10 | 雾天清光后计时 | 有效刷新间隔 = 10/2 = 5 分钟（刷更快） | 时间天气§L210 |
| **TC-14** | 天气权重 0 = 不刷 | `weather_weights:{"storm":0}` | 雷雨天进入 | 该怪不刷新/不出现；已在场怪不驱逐 | 时间天气§L147、L210 |
| **TC-15** | 天气变化不驱逐战斗中怪 | 战斗中天气变更 | 战斗中一轮后结算 | 战斗不中断；"对方逃跑了"不触发（仅时段边界触发） | 时间天气§L91、L210 |
| **TC-16** | 出没条件 AND 叠加 | `active_time 20-06` + `periods night/midnight` + `seasons autumn` | 半满足（仅时段满足） | 不出现——全 AND；全部满足才出没 | 时间天气§L209、L235 |
| **TC-17** | 校验器拦截 | 配置 `enemy:"不存在"` / `seasons:["monday"]` / `weather_weights:{"雷暴"}` | 加载/保存配置 | 硬拦报错：enemy 引用不存在 / 枚举非法 / 天气未注册 | 怪物§L295–299；时间天气§L40–41/L166 |

> 附录：细化_2a1b 之外的延伸（不实现、仅记录）：① 双向一致性为黄提示非硬拦（允许作者刻意不对称）；② `spawn_weight`（R22）、`max_alive`（R21）为工程补白可配项，默认值不影响定稿语义；③ 隐藏通道"走过解锁"方案经核对**未采用**（副本§L51 仅条件打开口径）。
