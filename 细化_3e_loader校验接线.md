# 细化_3e_loader校验接线（内容包加载管线 · 校验二分法红拦/黄提示 · 只建议不限制 · 热重载原子性 · 数据包校验器接口 · 验收测试）

> 版本：v1.0 · 类型：实现层细化契约（大厂模式：加载管线 / 校验分级 / 只建议不限制边界 / 热重载状态机 / 模块校验清单 / 验收测试）
> 追溯依据（**严格引用策划案，引用到行号 Lxx**）：
> - 《开发规则文档.md》v1.0（2026-08-15）→ 简称【规则】（**主依据**，全篇行号引用，本文档全部行为条款以 Lxx 为准）
> - 《RPG回合制框架设计文档.md》→ 简称【框架】（架构落点：content 层 loader/validator/registry 三件套、热重载 15.5 节）
> - 《细化_3a_架构分层契约.md》→ 简称【3a】（content 层定位与依赖方向：content 仅依赖 data，零 NoneBot import）
> - 实现层规划《规划_路3_框架指令数据.md》对应条目（任务级验收对齐）
> 范围：QQ 回合制 RPG 框架**数据加载器与校验器**的实现层细化——加载全流程（manifest→各模块→校验→挂载，阻断式抛错）、
> 校验二分法（红拦 5 类 / 黄提示若干）、只建议不限制原则的**不拦清单**、热重载（检测/原子替换/快照回退/旧局旧配置）、
> 每模块校验规则清单、验收测试用例（TC-01~TC-30，共 30 条）；
> 不含具体战斗/商店/任务等系统语义（分别见对应细化契约），不含字段元数据表本身的扩展流程（见【规则】2.2）。
> 变更纪律：任何偏离【规则】之处必须写入 §0.3 决策记录并注明理由+影响用例，禁止静默改语义。

---

## 〇、契约总则

### 0.1 术语表

| 术语 | 定义 | 依据 |
|---|---|---|
| 内容包 | 纯 JSON 配置 `content/<pack_id>/`，由 Web 编辑器生成、玩家 git pull 更新；**JSON 是运行时公共 API** | 【规则】L129 |
| manifest.json | 内容包入口文件：`name` / `version`（语义化）/ `schema_version`（整数）/ `author` / `modules`（启用的模块文件清单，不含 .json 后缀） | 【规则】L133 |
| loader | 加载器（`content/loader.py`）：按 manifest 声明顺序读文件 → 交校验器 → 挂载注册表 | 【规则】L41-42 |
| validator | 校验器（`content/validator.py`）：**全部结构断言收敛于此**，禁止散落各处 `if "xxx" in data` | 【规则】L92 |
| registry | 注册表（`content/registry.py`）：ID 全局唯一、效果家族先注册、原子快照替换 | 【规则】L41-42/L136/L175 |
| 红拦（红色拦截） | 5 类真正的非法值（类型错误/负数/非数字/引用不存在/结构错误）→ **拒绝加载** | 【规则】L152-157 |
| 黄提示（黄色提示） | 数值大小/概率/组合强度等 → **不拦截、可加载**，仅提示确认 | 【规则】L159-163 |
| 阻断式抛错 | 任一红拦 → 整包拒绝挂载，抛 `PackLoadError`（聚合全部红拦），**绝不半套配置运行** | 【规则】L178 |
| 热重载 | 编辑器保存 → mtime/哈希检测 → 原子替换 + 快照回退；3 秒自动检测或 `/重载` 手动 | 【规则】L174-178 |
| 旧局旧配置 | 进行中对局持旧 registry 快照（ID+名称冗余存储），新对局用新配置 | 【规则】L177 |

### 0.2 一句话契约

> 内容包加载 = **五段管线**：A 读 manifest → B 按【规则】L136 顺序加载模块 → C 过校验器（红拦=拒绝、黄提示=放行）
> → D 挂载 registry（ID 注册 + 原子快照替换）→ E 通知热重载；**任一红拦整包阻断并抛聚合异常（人话报错，L165-168），
> 黄提示只进报告不阻断**；热重载全程遵循「先写临时文件再原子 rename、全部写完统一触发、失败回退上一份校验通过的 registry 快照、
> 进行中对局持旧快照」，**绝不半套配置运行**（L175/L178）。

### 0.3 决策记录（ADR，均为对定稿空白的显式收敛）

| 编号 | 决策 | 理由 | 影响用例 |
|---|---|---|---|
| D-01 | 校验输出统一为 `ValidationReport`（errors + warnings 双列表），errors 非空即阻断；**阻断式抛错 = 一次抛全量红拦**（不逐条抛、不第一条即停），保证作者一次改完所有问题 | 【规则】L165「报错必须人话」要求可行动；一次给全量清单减少作者往返（小白用户编辑场景）；逐条抛会造成反复保存-报错循环 | TC-10 / TC-11 |
| D-02 | 阻断发生在**挂载之前**：校验 → 挂载两阶段严格分离，校验失败不产生任何注册表写入；`register_pack()` 仅在 `ValidationReport.ok` 时被调用 | 【规则】L178「绝不半套配置运行」——半挂载状态（部分 ID 已注册）是热重载回退漏洞的主要来源 | TC-11 / TC-23 |
| D-03 | 黄提示**聚合为一条消息**发送（去重 + 同模块合并），且措辞过【规则】L170 红线检查（禁止「必须/强制/上限/封顶/拒绝」类文案） | 【规则】L159-163 黄提示定位是「确认是故意设计？」；一条聚合消息满足 QQ 防刷屏约束（【规则】L495/L514） | TC-20 / TC-21 |
| D-04 | registry 维护**双快照**：`registry.snapshot()`（校验通过的整包 registry 快照，热重载回退用）+ 对局级快照（战斗中 ID+名称冗余，旧局旧配置用）；两者生命周期不同 | 【规则】L175（回退 registry）与 L177（对局持旧快照）是两个不同层级的需求，混用一个快照会互相污染 | TC-25 / TC-27 |
| D-05 | 加载/校验全量走 `asyncio.to_thread`（>50ms 操作），**禁止阻塞事件循环**；热重载轮询为独立后台任务 | 【规则】L106「所有 >50ms 操作（内容包校验、CSV 导入、zip 打包、文件 IO）必须 asyncio.to_thread」 | TC-01 / TC-22 |
| D-06 | `PackLoadError` 属领域异常（content 层），携带结构化错误清单，人话文案由 commands 层统一翻译；**validator/loader 不拼用户文案** | 【规则】L117「引擎层抛领域异常…由 commands 层捕获翻译成人话；禁止引擎层拼用户文案」；3a R4 同款铁律 | TC-10 |

---

## 一、loader 加载流程（manifest → 各模块 → 校验 → 挂载；阻断式抛错）

### 1.1 五段管线总图

```
pack_dir/
  ├── manifest.json ──► A 读 manifest（校验自身结构）──┐
  │                                                     │
  ├── effects.json ──┐                                  │
  ├── statuses.json  │                                  │
  ├── marks.json     ├─► B 按声明顺序读模块（L136）───► C 校验器 ──► D 挂载 ──► E 热重载生效
  ├── skill_chains   │      （缺失文件→黄提示继续）       │   红拦→抛错    （registry 原子替换）
  ├── action.json    │                                  │   黄提示→放行
  └── 其余模块 ──────┘                                  ▼
                                                   ValidationReport
```

对应【规则】L131-136（manifest 与模块声明）、L41-42（loader/validator/registry 三件套）。

### 1.2 A 阶段：manifest 解析（硬约束）

- 入口校验：文件必须存在且为合法 JSON（否则红拦「结构错误」）；`manifest.json` 本身缺文件 = 整包不可用，直接阻断。
- 必填字段检查（【规则】L133）：`name`（str）、`version`（语义化版本 str）、`schema_version`（int）、`author`（str）、`modules`（str 数组，不含 .json 后缀）。
  - 缺 `name`/`version`/`schema_version`/`modules` 任一 → 红拦「必填字段缺失」（L157 第 5 类结构错误）。
  - `schema_version` 非整数 → 红拦「类型错误」（L153）。
- `modules` 数组语义（【规则】L134-135，硬约束）：
  - **声明才加载**：`modules` 里没有的文件即使磁盘存在也**不加载**（防误启用）；此项为静默跳过 + 调试日志，**不是报错**（L135）。
  - **声明但缺失**：文件不存在 → 黄提示「没找到 xxx.json……旧包照常玩」，**不拒绝**（L135/L163）。
  - 空 `modules`（合法）→ 加载空包，各系统全部不启用（L134 向后兼容，旧包零改动可跑）。

### 1.3 B 阶段：模块加载顺序（硬约束，结构依赖）

按【规则】L136 注册顺序，**顺序即依赖**：

```
effects → statuses → marks（效果家族先注册）
→ skill_chains → action → 其余（items / equipment / traits / enemies / maps / stats / formula …）
```

- 效果家族（effects/statuses/marks）被 traits/items/skills/enemies 引用，**必须先入 registry**（L136）。校验器在引用存在性检查（红拦第 4 类，L156）中依赖此顺序：引用目标所属模块若未声明，按「引用不存在」红拦处理（因为该效果家族实际不会注册）。
- 同一模块内部按文件内条目顺序注册；ID 冲突 → 红拦（见 §2.1 第 5 类 + 【规则】L168 示例「已经有一个叫『铁剑』的物品了」）。

### 1.4 C 阶段：校验（阻断式抛错）

- 每模块解析为结构化数据后立即过该模块的校验规则（清单见 §5.2）；所有模块校验完成后汇总 `ValidationReport`。
- **阻断规则**：`report.errors` 非空 → 抛 `PackLoadError`（携带全部红拦结构化清单），整包不挂载（D-01/D-02）。
- **放行规则**：仅剩 `report.warnings` → 正常挂载，黄提示由调用方（commands / 编辑器）聚合展示（D-03）。
- 校验器不得有副作用（不写文件、不改 registry）；纯函数输入 `(module_name, parsed_data, registry_meta) → 校验片段`。

### 1.5 D 阶段：挂载（registry 原子替换）

- `register_pack(pack: Pack) -> None`：仅在 `report.ok` 时调用（D-02）。
- 原子性：新包注册表整体构建完成后**一次性替换**内存引用（`self._registry = new_registry`），期间世界/引擎读旧引用不受影响（【规则】L175 原子性要求）。
- 注册内容：ID → 定义对象 映射（物品/效果/状态/印记/技能/怪物/属性…）+ ID 全局唯一校验 + 名称冗余（显示名，供旧局快照用，L177）。

### 1.6 执行模型与性能（硬约束）

- `load_pack()` / `check_pack()` 整体走 `asyncio.to_thread`（>50ms，L106）；热重载轮询任务常驻后台。
- 校验复杂度 O(总条目 × 引用数)，引用存在性用预构建 ID 集合查表（O(1)）；单包全量校验目标 <2s（小白编辑保存后等待感知）。
- 禁止校验期间持有任何锁跨 await；registry 读写用单写锁（与 storage 单写队列同哲学，【规则】L107）。

### 1.7 错误模型（人话报错，硬约束）

- `PackLoadError`（领域异常，D-06）携带：`errors: tuple[PackError, ...]`，每条含 `module` / `field` / `kind`（五类红拦之一）/ `detail`（结构化参数）。
- 人话模板由 commands 层按【规则】L165-168 生成，示例：
  - 引用完整性：「『巨剑』引用的部位『双手武器』不存在，请先在部位页面添加」（L166）
  - 部位互斥环：「你的武器、盾牌、副手互相排斥形成了一个圈，谁也装不上，请删掉一条互斥」（L167）
  - ID 唯一：「已经有一个叫『铁剑』的物品了，换个名字吧」（L168）
- 黄提示模板示例（L160）：「生命值 999999，超出常见区间，确认是故意设计？」
- **绝不静默吞异常**：任何加载/校验异常必须 log（【规则】L114）；用户可见路径必须人话 + 下一步（L115）。

---

## 二、校验器分级（红拦 / 黄提示）

### 2.1 红拦：拒绝加载（仅限 5 类真正的非法值，硬约束）

来源：【规则】L152-157。**红拦清单是封闭集合**——任何新规则若不属于以下 5 类，必须降为黄提示或进 §3.1 不拦清单，否则违反总纲（【规则】L7/L148/L372）。

| # | 红拦类别 | 判定口径 | 依据 |
|---|---|---|---|
| R-1 | 类型错误 | 字段类型与元数据表不符：数字填成文字 / NaN / null / 数组填成对象等 | 【规则】L153 |
| R-2 | 负数 | 数值字段 < 0（int/float 均可为负的字段）；布尔/字符串/枚举字段不适用 | 【规则】L154 |
| R-3 | 非数字 | 应为数字的字段填了非数字内容（字符串数字「12」算类型错误 R-1；NaN/Infinity 属 R-1 或 R-3 均可，统一归 R-3） | 【规则】L155 |
| R-4 | 引用不存在 | ref 字段指向的 ID（效果/属性/元素/印记/物品/怪物/部位…）在对应注册表/键空间中不存在 | 【规则】L156/L144 |
| R-5 | 结构错误 | JSON 格式坏 / 必填字段缺失 / 死配置（条件 min>max、reset 模式 eq=max、battle+revert 矛盾等结构性矛盾） | 【规则】L157 |

R-5 死配置判定口径（【规则】L157 示例展开）：
- 区间类：`min > max`、`min < 0`（负数已归 R-2）、`lower > upper`。
- 模式矛盾类：`reset.mode == "eq"` 但 `reset.value != max`（L157 示例「reset 模式 eq=max」）；`battle + revert` 同挂（互斥语义同时开启）。
- 必填缺失类：元数据表标记 required 的字段缺失（含 `manifest` 顶层字段，见 §1.2）。
- 互斥环类：部位互斥声明成环（L167 示例）——`equipment.json` 专项，见 §5.2。
- 结构错误**不包含**「数值太大/太小」——那是黄提示（L160），见 §3.1。

### 2.2 黄提示：不拦截、可加载

来源：【规则】L159-163。全部黄提示**只进 `report.warnings`**，不影响 `report.ok`。

| # | 黄提示类别 | 判定口径 | 依据 |
|---|---|---|---|
| Y-1 | 数值大小 / 范围越界 | 数值超出「常见区间」（区间来自字段元数据表 range 列，**提示用**）：生命值 999999 等 | 【规则】L160 |
| Y-2 | 概率高低 | 命中率/触发率/掉落率等概率字段过高（>1 或 >95%）或过低（<5%），提示确认 | 【规则】L161 |
| Y-3 | 组合强度 | 多效果组合理论强度异常（如某技能同时带超高倍率+无消耗+必中），仅提示 | 【规则】L161 |
| Y-4 | 上限可配 0=不限 | `crit.cap` / `max_stack` / 触发上限等上限字段=0 → 解读为不限，**不报错**；上限字段缺失 → 默认 0=不限 | 【规则】L161/L375 |
| Y-5 | 软标注字段 | `difficulty` / `playstyle` 等软标注字段**永不红色报错**（任意值放行） | 【规则】L162 |
| Y-6 | 模块缺失 | modules 声明但文件缺失：「没找到 effects.json……旧包照常玩」 | 【规则】L135/L163 |
| Y-7 | 未注册键空间 | 消费方引用 stats.json 未注册的属性键 → 提示（仅查键空间，见 §5.2） | 【规则】L146 |
| Y-8 | 自定义字段 | `x_` 前缀字段一律放行（仅查引用存在性） | 【规则】L145 |

### 2.3 分级判定总表（实现用速查）

| 判定问题 | 答案 | 处理 |
|---|---|---|
| 是 5 类红拦之一吗？ | 是 | errors + 阻断 |
| 是 Y-1~Y-8 之一吗？ | 是 | warnings + 放行 |
| 都不属于？ | — | **默认放行**（总纲兜底：不明确非法即合法，L7/L148） |

> 兜底原则（硬约束）：**红拦清单封闭、黄提示清单开放**——未列入任何清单的字段/值默认放行，不得默认拦截。这是【规则】L7「总纲遵循：《设计总纲：只建议不限制》」在代码层的直接落地。

### 2.4 人话报错模板（红拦 & 黄提示共用）

- 模板结构：【规则】L115「原因 + 正确用法 + 下一步建议」；格式细节沿用《细化_3d_消息模板规范》。
- 红拦示例见 §1.7（L166-168）；黄提示示例见 L160。
- **提示语措辞红线**（硬约束，L170）：黄提示文案**禁止**出现「必须 / 强制 / 上限 / 封顶 / 拒绝」类字样；红拦文案允许「拒绝」但优先用「请先…/ 换个名字吧」等行动导向措辞（L166-168 示例语气）。

---

## 三、只建议不限制原则（不拦什么）

### 3.1 不拦清单（全部放行，仅可能黄提示）

来源：【规则】L148-163 + L145-146。以下内容**永不做红拦**：

1. **数值大小**：任何数值字段的具体大小（生命值 999999、攻击力 10 万、倍率 500%……）——只黄提示「确认是故意设计？」（L160）。
2. **概率高低**：命中率 0.1% 或 100%、触发率 0、掉落率 99%——概率是作者设计自由度，只黄提示（L161）。
3. **组合强度**：多效果/多技能/多装备叠加后的理论强度（一回合秒杀组合、无限连段组合）——只黄提示（L161）。
4. **上限数值本身**：`crit.cap`、`max_stack`、`max_combo`、触发上限等上限字段填任何值（含 0）——**0 = 无上限，合法语义**（L161/L375）；上限字段缺失 = 默认 0=不限。
5. **软标注字段**：`difficulty` / `playstyle` 等软标注字段任意值——永不红色报错（L162）。
6. **模块缺失**：manifest 声明但文件缺失——黄提示「旧包照常玩」，不拒绝（L135/L163）。
7. **未声明的多余文件**：磁盘存在但未在 modules 声明——不加载、不报错（防误启用，L135）。
8. **`x_` 前缀自定义字段**：一律放行（仅查引用存在性）（L145）。
9. **未注册的属性键引用**：消费方用到 stats.json 未注册键——黄提示不拦截（L146）。
10. **数值语义的「强/弱」判断**：任何关于「这个配置会不会太强/太弱/太无聊」的推断——超出校验器职责，交给作者与玩法测试（L7 总纲精神）。

### 3.2 拦与不拦的判定标准（统一口径）

> **唯一判定问题：这个值会让游戏「加载不起来 / 运行期必崩 / 引用悬空」吗？**
> - 会（JSON 坏、类型错、负数、引用空、结构死锁）→ 红拦（L152）。
> - 只是「看起来不对劲，但跑得起来」→ 黄提示（L159-163）。
> - 连「不对劲」都算不上 → 放行，校验器不发表意见。

### 3.3 安全例外（不受只建议不限制覆盖，硬约束）

下列安全红线**独立于校验二分法**，命中即拒绝加载（【规则】L443「安全例外不受只建议不限制覆盖」）：

| 例外 | 判定 | 依据 |
|---|---|---|
| JS 公式 AST 黑名单 | formula 字段含 `constructor / __proto__ / Function / eval / globalThis / process / require / fetch` 等黑名单标识符或 `new Xxx` 表达式 → **拒绝加载**（红拦，安全例外） | 【规则】L448 |
| 公式资源上限 | 公式长度 >4KB、结果 >1KB、求值超时 10ms 配置被改坏 → 拒绝加载或兜底 0 | 【规则】L449 |
| zip 导入 | zip slip（`../`/绝对路径）、条目数/总大小超限 → 拒绝导入 | 【规则】L487 |

> 安全例外是唯一允许在 5 类红拦之外加红拦的通道；新增安全例外必须走【规则】9.1 定稿流程 + 评审清单 A 项（L348-352）。

---

## 四、热重载（检测 / 原子替换 / 快照回退 / 旧局旧配置）

### 4.1 触发检测（硬约束）

来源：【规则】L174。
- 自动：后台轮询任务每 **3 秒** 检测一次各包目录文件 **mtime + 哈希**（哈希兜底 mtime 精度不足场景）。
- 手动：`/重载` 指令（GM 权限，【规则】L457）立即全量检测。
- 新模块文件（modules 声明后新增）同样纳入 mtime 监控（L176）。

### 4.2 原子替换（硬约束）

来源：【规则】L175。
- **编辑器写盘侧**：所有文件先写临时文件（同目录 `.tmp`）再原子 `rename`（编辑器侧约定，loader 侧不强制）；**全部文件写完统一触发重载**，禁止逐个文件生效。
- **loader 侧**：热重载 = 重新走 §1.1 五段管线（A→B→C→D）；新 registry 构建完成后一次性替换内存引用（§1.5），期间旧引用继续服务（D-04）。
- zip 导入路径（【规则】L487）：解压到临时目录 → 路径白名单/条目上限 → manifest 校验 → 全量校验通过 → 原子替换 + 触发重载。

### 4.3 增量校验（硬约束）

来源：【规则】L176。
- 按文件 mtime 增量：mtime 未变的模块跳过**重新解析**，但**整包校验仍全量执行**（引用关系是跨模块的，跳过单模块校验会漏悬空引用）。
- 折中口径：mtime 未变模块复用其**解析结果与单模块校验结果**（缓存），引用存在性检查仍全量重跑（O(1) 查表，成本可忽略）。
- 变更模块列表供黄提示/日志使用：「已重载：effects.json、items.json」。

### 4.4 快照回退（硬约束）

来源：【规则】L175/L178。
- registry 维护 `snapshot()` / `restore(snapshot)` 双接口（D-04）：`snapshot()` 深拷贝当前生效 registry（含全部 ID→定义映射 + 名称冗余）。
- 热重载流程：
  1. `old = registry.snapshot()`
  2. 走五段管线；任何红拦或 IO 异常 → **中断**
  3. 失败：`registry.restore(old)` + 黄提示/人话错误「重载失败，已回退到上一份可用配置」（L178/L321 故障注入断言点）
  4. 成功：新 registry 生效，旧快照保留为「上一份可用」以备下次回退
- **绝不半套配置运行**：失败路径保证 registry 与热重载前逐字节一致（L178），服务不崩（L321）。

### 4.5 旧局旧配置（硬约束）

来源：【规则】L177。
- **进行中对局持旧快照**：战斗/任务/调合等对局在开始时引用当前 registry 的定义（ID + 名称冗余），热重载后该对局继续用**旧定义**结算，新对局用新配置。
- 快照内容：ID + 名称（显示名）冗余存储——重载后旧局内展示仍用旧名，引用仍按旧 ID（L177/L186「引用按 ID 存储、显示按名字」）。
- **删除配置降级**：热重载删除了某效果/链/印记 → 进行中旧局按「无效果/无链/无印」降级处理，**不报错**（L177）。
- 存档侧：存档含 `content_pack_id + version`（L183），读档时按存档版本决定迁移路径（L182-184），与热重载无关的独立链路。

### 4.6 并发与一致性

- 热重载与进行中操作并发：registry 读不加锁（内存引用原子替换），写（替换）用单写锁（§1.6）。
- 热重载期间禁止打开编辑器写盘窗口与重载窗口重叠（编辑器保存侧：行级冲突检测，【规则】L469）；loader 侧以「最后完成的整包校验」为准。
- 校验失败回退时，若编辑器同时又在写新文件 → 下次轮询（≤3s）重新检测，幂等重试，无死锁。

---

## 五、数据包校验器接口（每模块校验规则清单）

### 5.1 接口定义（content/validator.py，零 NoneBot，仅依赖 data 层）

```python
# qbot_rpg/content/validator.py —— 校验器唯一落点（【规则】L92 结构断言收敛于此）
@dataclass(frozen=True)
class PackError:          # 红拦条目
    module: str           # 模块文件名（无后缀），如 "effects"
    field: str            # 字段路径，如 "items.0.effects.1"
    kind: str             # R-1..R-5（§2.1 五类之一）
    detail: dict          # 结构化参数（供 commands 层拼人话，§1.7）

@dataclass(frozen=True)
class PackWarning:        # 黄提示条目
    module: str
    field: str
    kind: str             # Y-1..Y-8（§2.2 八类之一）
    detail: dict

@dataclass(frozen=True)
class ValidationReport:
    errors: tuple[PackError, ...]
    warnings: tuple[PackWarning, ...]
    @property
    def ok(self) -> bool: return not self.errors   # 红拦为空才可挂载（D-02）

def check_pack(modules: dict[str, object], meta: FieldMetaTable) -> ValidationReport:
    """整包校验：逐模块过 §5.2 规则表；errors/warnings 全量收集（D-01）。纯函数，无副作用。"""
```

```python
# qbot_rpg/content/loader.py —— 五段管线（§1.1），阻断式抛错
class PackLoadError(Exception):
    """阻断式聚合异常：携带全部红拦（report.errors），由 commands 层翻译人话（D-06）。"""
    def __init__(self, report: ValidationReport): ...

async def load_pack(pack_dir: Path) -> Pack:
    """A→B→C→D 全流程（to_thread 内执行，§1.6）；report.errors 非空 → 抛 PackLoadError；否则挂载并返回 Pack。"""
```

```python
# qbot_rpg/content/registry.py —— ID 注册 + 原子快照替换 + 双快照（D-04）
class Registry:
    def snapshot(self) -> RegistrySnapshot: ...       # 深拷贝生效 registry（回退用）
    def restore(self, snap: RegistrySnapshot) -> None: ...  # 回退（热重载失败路径，§4.4）
    def resolve(self, id: str, kind: str) -> Def | None: ...  # 引用查表（红拦 R-4 用，O(1)）
```

```python
# qbot_rpg/content/hot_reload.py —— 3s 轮询 + /重载 手动触发 + 原子替换（§4）
class HotReloadWatcher:
    async def run(self) -> None: ...          # 3s 轮询 mtime/哈希（L174）
    async def reload(self, pack_id: str) -> ReloadResult: ...  # 快照→管线→替换/回退（§4.4）
```

### 5.2 每模块校验规则清单

> 通用规则（所有模块适用）：R-1 类型错误（按元数据表类型枚举 int/str/bool/enum/ref/obj/list/map/formula，【规则】L143）、
> R-2 负数（数值字段）、R-3 非数字、R-4 引用存在性（ref 字段按元数据表「引用目标」列查，L144）、R-5 必填缺失；
> ID 唯一（全模块同类型 ID 不得重复，L168 人话示例）；`x_` 前缀字段一律放行（L145）；软标注字段永不红拦（L162）。

| 模块文件 | 红拦（R-1~R-5 之外的本模块专项） | 黄提示（Y-1~Y-8 之外的本模块专项） | 依据 |
|---|---|---|---|
| manifest.json | 必填字段缺失/类型错（§1.2）；`modules` 条目非字符串 | 无（manifest 不产生黄提示） | 【规则】L133 |
| effects.json | 效果 ID 重复；`effects[].type` 非元数据表枚举值（类型错误）；效果引用 status/mark 的 ID 不存在 | 效果数值（倍率/时长/层数）异常大 | L136/L143/L156 |
| statuses.json | 状态 ID 重复；`max_stack` 为负数（R-2）；引用 effect ID 不存在 | `max_stack = 0` → 提示「0=不限，确认？」（合法放行）；时长过大 | L161/L375 |
| marks.json | 印记 ID 重复；引用 status/effect 不存在 | 印记层数/概率异常 | L136/L156 |
| skill_chains.json | 链 ID 重复；链内节点引用 skill/action 不存在；链成环（A→B→A）→ 结构错误 R-5 | 链长/组合强度异常（Y-3） | L136/L157/L161 |
| action.json | 动作 ID 重复；`action[].type` 非枚举；引用 skill/effect 不存在 | 动作消耗/冷却异常 | L136/L143 |
| formula.json | 公式段缺失（必填 R-5）；公式**含 AST 黑名单标识符 / new 表达式 → 安全例外红拦**（§3.3）；公式长度 >4KB | 公式结果异常（恒 0/恒满） | L448/L449 |
| items.json / equipment.json | 物品 ID 重复（L168 人话）；`equipment` 部位引用不存在（L166 人话）；**部位互斥成环 → 结构错误 R-5**（L167 人话）；price/atk 等数值为负 | 价格/数值异常大；组合强度（一身神装） | L156/L157/L160/L166-168 |
| traits.json | 特质 ID 重复；引用 effect/status/skill 不存在 | 特质概率/层数异常 | L136/L156 |
| enemies.json | 怪物 ID 重复；引用 trait/effect/action 不存在；`hp/atk` 为负 | 怪物数值/掉落率异常（Y-1/Y-2） | L156/L160/L161 |
| maps.json | 地图 ID 重复；引用 enemy 池/副本 ID 不存在；区域区间 min>max（R-5 死配置） | 刷怪概率、通道强度 | L156/L157 |
| stats.json | 属性键非小写 snake_case（结构错误，与 L66/L193 一致）；键与元数据表类型冲突 | 未注册键被消费方引用 → 提示（Y-7，键空间见 L146） | L66/L146/L193 |
| npc.json（如声明） | NPC ID 重复；引用 item/task 不存在 | 好感度/价格异常 | L156/L193 |

### 5.3 字段元数据表消费（唯一数据源，硬约束）

- 校验规则**不手写字段清单**：全部字段的 名称/类型/默认值/范围/引用目标 从字段元数据表读取（【规则】L140「编辑器表单、CSV 列头、JSON Schema、校验规则四处共用一张元数据表」）。
- 元数据表缺失某字段 → 该字段按「未知字段」处理：非 `x_` 前缀未知字段默认放行（总纲兜底，§2.3），不红拦。
- 新字段 = 元数据表加一行（L142），校验器零代码变更即生效；校验器代码只实现「规则引擎」（五类红拦 + 黄提示判定框架），不包含任何具体字段名（L140 防漂移）。

### 5.4 与编辑器 / CSV / Schema 的同步

- 编辑器保存前**必须调 `check_pack`**：红拦 → 保存失败并展示全部错误（D-01 一次给全量）；黄提示 → 弹确认后允许保存（【规则】L373「即改即生效，无导出导入」）。
- CSV 导入列头按元数据表生成（L140）；导入后过同一校验器，红拦拒绝导入。
- JSON Schema 由元数据表生成（L140），validator 与 Schema 校验结果必须一致（测试对齐，TC-28 覆盖思想）。
- 内容包提交 git 前必须 `validator.py` 全绿（【规则】L123/L194）。

---

## 六、验收测试用例（TC-01 ~ TC-30，共 30 条）

> 测试规范依据：【规则】4.1 单元测试（content 纯逻辑，不联网不启 NoneBot，L283）、4.5 故障注入（L321/L326）、4.6 fixtures（tests/fixtures/packs/：合法包/坏引用包/缺模块包/旧 schema 包各一，L332）；覆盖门槛 content/ ≥80%（L333）。

### 6.1 坏包拦截类（TC-01 ~ TC-11，共 11 条）

| 编号 | 用例 | 步骤 | 断言 |
|---|---|---|---|
| TC-01 | JSON 语法坏 → 阻断 | 写入 `items.json` 为非法 JSON（截断/缺括号），`load_pack()` | 抛 `PackLoadError`，`kind == R-5`；registry 无任何新 ID；`asyncio.to_thread` 内执行（不阻塞事件循环，D-05） |
| TC-02 | 类型错误 → 阻断 | `items[].price = "很贵"`（数字填成文字） | 红拦 R-1；人话文案含「请填写数字」类下一步（§1.7 模板） |
| TC-03 | 负数 → 阻断 | `enemies[].hp = -100` | 红拦 R-2；包不挂载 |
| TC-04 | 非数字 → 阻断 | `crit.cap = NaN` / `Infinity` | 红拦 R-3（NaN 归 R-3 口径 §2.1） |
| TC-05 | 引用不存在 → 阻断 | `items[].effects[0]` 指向未注册 effect ID | 红拦 R-4；人话「『巨剑』引用的效果『xxx』不存在，请先添加」（L166 模板） |
| TC-06 | 必填字段缺失 → 阻断 | `manifest.json` 缺 `modules`；`effects` 条目缺必填字段 | 红拦 R-5（必填缺失）；manifest 缺字段时**整包不可用** |
| TC-07 | 死配置 min>max → 阻断 | 条件/区间字段 `min=10, max=5` | 红拦 R-5（死配置，L157 示例） |
| TC-08 | 部位互斥环 → 阻断 | 武器↔盾牌↔副手互斥成环 | 红拦 R-5；人话「互相排斥形成了一个圈…请删掉一条互斥」（L167） |
| TC-09 | 未声明文件不加载 | 磁盘有 `npc.json` 但 manifest 未声明 | 加载成功（不报错）；`registry.resolve(npc 任意 ID)` 返回 None；调试日志记录跳过（L135） |
| TC-10 | 阻断式抛错一次给全量 | 构造同时含 3 处红拦的包 | 单次 `load_pack()` 抛错携带 **3 条** errors（D-01）；人话消息聚合 3 条，格式统一（§1.7） |
| TC-11 | 全红拦一个不挂载 | 合法包已挂载后，写入含红拦的新版包触发加载 | 旧 registry 完全保留（快照对比逐 ID 一致）；新包 0 个 ID 注册（D-02 半挂载禁止） |

### 6.2 黄提示放行类（TC-12 ~ TC-21，共 10 条）

| 编号 | 用例 | 步骤 | 断言 |
|---|---|---|---|
| TC-12 | 数值大小仅提示 | `enemies[].hp = 999999` | 加载成功；warnings 含 Y-1「超出常见区间，确认是故意设计？」（L160 措辞） |
| TC-13 | 概率高低仅提示 | 命中率 100% / 掉落率 0.1% | 加载成功；warnings 含 Y-2 |
| TC-14 | 组合强度仅提示 | 技能同时高倍率+无消耗+必中 | 加载成功；warnings 含 Y-3；**不阻断**（L161） |
| TC-15 | 上限 0=不限 | `crit.cap = 0`、`max_stack = 0`、上限字段缺失 | 均加载成功；0 与缺失都按「不限」语义；warnings 含 Y-4（L161/L375） |
| TC-16 | 软标注字段任意值 | `difficulty = "???!"`、`playstyle = 3` | 加载成功；**永不红拦**（L162） |
| TC-17 | 模块缺失仅提示 | modules 声明 `effects.json` 但文件缺失 | 加载成功；warnings 含 Y-6「没找到 effects.json……旧包照常玩」（L163） |
| TC-18 | 未注册键空间仅提示 | 效果引用 `stats.json` 未注册属性键 | 加载成功；warnings 含 Y-7（L146） |
| TC-19 | x_ 自定义字段放行 | `x_my_field` 任意类型值（引用存在则查） | 加载成功；引用不存在的 x_ 字段内引用 → 按 R-4 查（L145） |
| TC-20 | 提示语措辞红线 | 触发 Y-1~Y-8 全部分支，收集全部提示文案 | 无任何「必须/强制/上限/封顶/拒绝」字样（L170 硬约束，正则断言） |
| TC-21 | 黄提示聚合一条 | 触发 10 条黄提示 | 对外展示为**一条**聚合消息（D-03）；含去重与同模块合并 |

### 6.3 热重载类（TC-22 ~ TC-30，共 9 条）

| 编号 | 用例 | 步骤 | 断言 |
|---|---|---|---|
| TC-22 | mtime 检测即时生效 | 修改 `items.json` mtime/内容 → 等 ≤3s 轮询 | 新定义生效（新对局 `registry.resolve` 返回新对象）；无需重启（L174） |
| TC-23 | 非法 JSON 回退快照（故障注入） | 写入非法 JSON 触发热重载（L321 场景） | 服务不崩；registry 与重载前快照逐字节一致（§4.4）；人话提示「已回退」（L178/L321） |
| TC-24 | 增量只重载变更模块 | 修改 1 个文件，其余 5 个不动 | 日志/内部计数：仅 1 模块重新解析（§4.3 mtime 增量，L176）；引用检查仍全量通过 |
| TC-25 | 旧局旧配置 | 战斗进行中热重载 items.json | 进行中对局结算用**旧定义**（ID+名称冗余断言，L177）；新开对局用新定义 |
| TC-26 | 删除配置降级 | 热重载删除某 effect/mark/skill_chain | 旧局按「无效果/无链/无印」降级继续，不抛异常（L177）；新局引用该 ID → 红拦（引用不存在） |
| TC-27 | 全部写完才触发 | 编辑器写 3 个文件，仅写完 2 个时检查 | 重载未触发（registry 未变）；第 3 个写完（原子 rename 完成）后 ≤3s 内统一生效（L175） |
| TC-28 | /重载 手动触发 + 全量校验 | 改 1 个文件后执行 `/重载`（GM） | 立即重载（不等 3s）；重载后必过完整校验器（L178）；黄提示记录在案可接受 |
| TC-29 | 安全例外：公式黑名单拒绝 | formula 字段含 `eval` / `constructor` / `new Function` | 红拦（安全例外，L448），**不受只建议不限制覆盖**（§3.3）；含 `setTimeout` 同样拒绝 |
| TC-30 | 冒烟：合法包全链路 | 对 fixtures 合法包跑 `load_pack` + 模拟一局战斗（锁定→攻击→结算） | 全流程无红拦；战斗结算正常（【规则】L285 冒烟测试）；对 fixtures 坏引用包/缺模块包/旧 schema 包分别断言预期行为（L332） |

---

## 附：规则速查表

| 场景 | 一句话规则 | 出处 |
|---|---|---|
| 加载 | manifest → 按 L136 顺序 → 校验 → 挂载；任一红拦整包阻断 | §1 / L131-136 |
| 红拦 | 仅 5 类：类型错误/负数/非数字/引用不存在/结构错误，清单封闭 | §2.1 / L152-157 |
| 黄提示 | 数值/概率/强度/上限 0=不限/软标注/缺模块/未注册键/x_ 字段，清单开放 | §2.2 / L159-163 |
| 不拦 | 数值大小/概率高低/组合强度/上限数值一律不拦（可能黄提示） | §3.1 / L148-163 |
| 提示措辞 | 禁「必须/强制/上限/封顶/拒绝」；报错必须人话 | L170/L165-168 |
| 热重载 | 原子写 + 快照回退 + 旧局旧配置 + 增量校验，绝不半套运行 | §4 / L174-178 |
| 安全例外 | 公式 AST 黑名单 / zip 导入红线不受只建议不限制覆盖 | §3.3 / L443-453/L487 |
| 测试 | content 单测零 NoneBot + 故障注入回退 + fixtures 四类包，覆盖 ≥80% | §6 / L283/L321/L332-333 |
