# 任务书 · 专项·引擎层直出文案 第一批（中性文本池 / 成就尾行）

> 2026-09-12 · 消息模板重构战役 · 执行者：子 agent（本任务书 + `docs/消息模板重构/00_方案与规范_v1.md` 为唯一指令源）
> 背景：迁移批把「模板表」做成了唯一文案源，但**引擎/壳层仍有玩家可见中文字面量**直出。
> 本批清第一批（台账 02_遗留登记 #38/#39 已登记），目标 = 让这些文案也由模板表驱动（纯配置、内容包可覆盖）。

## 1. 范围（3 处，逐一处理）

1. `qbot_rpg/core/environment_lore.py`
   - L88 `DEFAULT_AMBIENT_TEXT = "四周一片寂静，并没有特别的发现。"`
   - L101-106 `_NEUTRAL_POOL`（4 条中性环境句，`_NEUTRAL_POOL[rng.randrange(...)]` 随机取）
2. `qbot_rpg/core/investigate.py`
   - L93 `DEFAULT_GENERIC_TEXT = "四周一片寂静，并没有特别的发现。"`
   - L96 `DEFAULT_CONFIRM_TEXT = "这里没有新的发现。"`（同类，一并处理）
3. `qbot_rpg/commands/achievement_commands.py`
   - L153 `tip="输入 /成就信息 <N> 查看详情"`（**仍带斜杠**；`Tip:` 前缀由 list_render 负责）

## 2. 达标口径

- **文案进模板表**：新增键（建议 `env_neutral_pool_1..4` / `env_ambient_default` / `env_investigate_generic`
  / `env_investigate_confirm` / `ach_list_tail_tip`，命名可自定但需成体系）；`content/test_demo/templates.json` 同步。
- **主路径走表**：渲染/取数路径经 `tpl_of(ctx, key)` 取文案（ctx 从装配层传入；若某函数确实拿不到 ctx，
  允许在该层保留兜底常量，但**必须**让调用方能注入 ctx 覆盖，并在报告里说明该兜底）。
- **排版口径**：结构化行 ≤14 全角；错误行 ❌+原因；**免斜杠**（`发 成就信息 <序号>` 这类写法）；
  中性环境句属介绍类，允许自然折行（登记 prose_keys）。
- 池化文案（`_NEUTRAL_POOL`）保留「多条随机」语义——改为从模板表读多条（键名后缀 `_1.._4`），
  随机逻辑不变。
- 既有测试同步；**不得**为过测试把文案改回硬编码。

## 3. 工作清单

1. 先落 fragment：`docs/消息模板重构/_fragments/spec_env1.json`（`{"rail":"spec_env1","templates":{...}}`）→ commit。
2. 合表：`python3 scripts/tpl_merge_fragments.py docs/消息模板重构/_fragments/spec_env1.json`
3. 改代码：三处硬编码改为表驱动（含 ctx 注入链）；`content/test_demo/templates.json` 同步。
4. 自校验：`scripts/check_template_width.py`（0 FAIL）+ `test_template_table` + `test_emoji_discipline`
   + 定向（investigate / environment_lore / achievement 相关用例）+ **全量** `pytest tests/ -q`（报计数）。
5. `ruff check <改动文件>` 干净；commit。**禁止 push / 禁止动 main**。

## 4. 报告（回复正文内联）
- fragment 路径 + 新键文案 JSON 全量；三处「旧硬编码 → 新模板键 → 调用链」对照表；
- 改动文件清单；checker 输出；定向+全量计数原文；**仍保留的兜底常量清单**（如有）；commit hash。
