# 细化规划·1f 怪物 AI 状态机

> 依据：《怪物行动 AI 设计定稿 v1.0.1》（2026-08-16），下文简称《定稿》。
> 范围：本文件为 1f（怪物 AI 状态机）细化规划，覆盖六节：① AI 决策流程状态机 ② 套间评估 8 级规则 ③ 意图预告 ④ 行动表解析 ⑤ chains 连招链 ⑥ 验收测试用例。
> 引用纪律：一切规则条目标注《定稿》出处；《定稿》未显式声明、由本细化补充的口径一律标注【细化】。
> 硬约束（《定稿》〇）：1v1 回合制 / QQ 文字 / 一轮一条消息 / 碎片化 / 小白优先 / 只建议不限制。

---

## ① AI 决策流程状态机

### 1.1 两级状态模型【细化分层，锚定《定稿》§三/§四/§八】

《定稿》§三 边界声明：**phases 阶段表 = 配置载体**（HP 阈值/每阶段行动表/转换演出），**ai.states 状态机 = 运行时行为状态**（巡逻/战斗/狂暴/濒死 + enter_action/权重修正）；阶段表管"血量进度→行动表切换"，状态机管"行为模式→权重与专属行动"；阈值声明只写 phases 单处（60/30 双阈值已收敛）。

本细化据此拆为两级：**行为态**承载行为模式（§三），**执行态**承载"套的执行生命周期"（核心规则 1/2/7 的套内门语义需要显式执行状态承载，快照 ai_state 提供承载字段【§八 A.5】）。

#### 行为态（ai.states，配置载体，4 标准态 + 可配自定义【§三】）

| 状态 | 进入条件（示例） | 效果 | 出处 |
|---|---|---|---|
| patrol 巡逻 | 战斗前/默认；battle_start 后转战斗 | 待机 | §三 / §二④ |
| normal 战斗 | 战斗开始 | weight_mod {}（无修正） | §三 |
| enraged 狂暴 | hp_below 50（条件表达式） | weight_mod {attack:1.5, defense:0.5}；enter_action=roar；exclusive_actions=[fire_burst] | §三 / §九 |
| dying 濒死 | hp_below 15 | weight_mod {attack:2.0}；enter_action=death_rattle；exclusive_actions=[final_strike] | §三 / §九 |

行为态规则（全部引自《定稿》）：
- 状态切换 = 条件表达式，与条件行动同一套表达式体系【§三】
- 切换时机 = 套间（打完一套再切，套内不切）【§三 / 核心规则 1】
- 阈值只写 phases 单处，状态机不重复声明血量阈值【§三 边界声明】
- enter_action 进强制队列（强制队列见② L2）【§三 / §四 步骤1】
- phase_changed 触发可联动状态机转换【§三 边界声明 / §二⑨】
- 状态专属行动 exclusive_actions 为该状态白名单【§三】

#### 执行态（运行时，快照 ai_state 承载【§八 A.5】，4 态）

| 状态 | 含义 | 行为 | 出处 |
|---|---|---|---|
| idle 套间 | 无套在途 | 执行套间评估 L1-L7（② 节） | §四 |
| in_chain 套执行 | 连招链 / 单随机行动执行中 | 只走链推进/行动结算，套内门开启（跳过 L1-L7） | §四 套内门 / 核心规则1 |
| charging 蓄力中 | 蓄力行动跨回合进行 | 蓄力结算，套内门开启；蓄力可被打断；armor=true 霸体免疫打断 | 核心规则7 / §八 |
| downed 倒地/起身 | 被击倒（被击倒类效果【细化】） | 起身动作 → 下一回合评估 get_up 条件行动 | §二③ |

### 1.2 行动选择流程（决策管线总图）

```
┌───────────────────────────────────────────────┐
│ 回合开始 → 套内门（L0）                          │
│   · 在途链 / 蓄力中？→ 只走链推进 / 蓄力结算 → 结束 │
│   · 无在途 → 套间评估：L1 状态机切换 → L2 强制队列   │
│     → L3 条件行动 → L4 连锁队列 → L5 状态专属      │
│     → L6 随机行动表 → L7 兜底普攻                  │
│   产出行动 → 进入执行态（in_chain / charging）      │
└───────────────────────────────────────────────┘
```
（对应《定稿》§四"套内门 + 步骤 0-6"结构，编号映射见②。）

### 1.3 关键场景状态语义

- **行动选择**：idle 态套间评估 L1-L7，产出行动后进入执行态【§四】。
- **在途链**：chain_queue + chain_pos 快照字段承载【§八 A.5】；入队后套内确定性执行，中途不被条件打断【核心规则2】；战斗中断快照续玩（套内可续）【§六.3】。
- **蓄力**：action.json charge_* 字段（登记于 4.2.1）；蓄力跨回合 = 同一套【核心规则1】；蓄力可被打断，armor=true 霸体免疫【核心规则7 / §八】。
- **残血狂暴**：血量跨 phases 阈值（§六.1 阶段2 狂暴期）→ 套间联动切换 enraged，权重修正 + 专属行动生效【§三 边界声明 / §六.1】。
- **起身**：downed → 起身 → 下一回合套间评估 get_up 条件行动（触发类型 ③）【§二③】。

---

## ② 套间评估 8 级规则（每级评估口径 / 优先级）

说明：《定稿》§四 给出"套内门 + 步骤 0-6"共 8 个决策点；本细化编号为 **L0-L7 全序**，逐级给出评估口径与优先级（L0=套内门，L1-L7 对应 §四 步骤 0-6）。

| 级 | 名称 | 评估口径 | 优先级 | 产出 / 去向 | 出处 |
|---|---|---|---|---|---|
| **L0** | 套内门 | chain_queue 非空 ∨ 蓄力进行中？ | 闸门（最高，先于一切） | 是 → 只走链推进/蓄力结算，跳过 L1-L7；否 → 进入套间评估 | §四 / 核心规则1、7 |
| **L1** | 状态机切换 | transitions 条件表达式求值（套间先切） | 1 | 切换行为态；enter_action 入强制队列；weight_mod 生效 | §三 / §四 步骤0 |
| **L2** | 强制队列 | 开场技 / 脚本 / enter_action 队列非空？ | 2 | 执行队首行动；若引链则入 chain_queue | §四 步骤1 / §三 |
| **L3** | 条件行动 | 13 类触发匹配（once / max_triggers / trigger_cooldown 过滤） | 3（priority 降序，同级随机） | 单次锚点行动，或经 chain_ref 链入队 | §二 / 核心规则5 |
| **L4** | 连锁队列 | chain_queue 非空？（只由顶层 chains 表驱动） | 4 | 链节点 chance roll：成功入队 → 套内确定性执行；失败断链+冷却 | §四 步骤3 / §八 / 核心规则2 |
| **L5** | 状态专属行动 | 当前行为态 exclusive_actions 非空？ | 5 | 白名单内选行动 | §三 |
| **L6** | 随机行动表 | probability=1 行动池；hungry 保底先查；cooldown 过滤 | 6（最低参与档） | P = weight × 条件修正 × 状态修正 归一化选一 | §一 / 核心规则4、6 |
| **L7** | 兜底普攻 | L0-L6 均无产出 | 7（兜底） | 默认普攻 | §四 步骤6 |

优先级总则：
- 条件行动**永远**套间评估；多个条件满足：priority 降序，同级随机【核心规则5】。
- 套内门开启时一切评估跳过（套内只走链推进/蓄力结算，玩家预判稳定）【核心规则1】。
- L1-L7 逐级短路：高优先级级有产出即停，不落入低优先级级【§四 结构】。

---

## ③ 意图预告（intent 字段 / 3 类基础预告以外的预告类型）

### 3.1 intent 字段（运行时快照【§八 A.5】+ action.json 预告字段【§八 4.2.1】）

《定稿》登记：快照 ai_state 含 `intent`；action.json AI 字段含 `preview / preview_chain / reveal_condition`。本细化定义 intent 运行时结构【细化】：

```json
"intent": {
  "level": 1,              // 预告分级 L1/L2/L3【§五】
  "category": "charge",    // L1 类别：charge 蓄力 / gather 积蓄能量 / stance 攻击架势【§五】
  "action_id": "doomsday_breath",
  "name_revealed": false,  // 图鉴解锁后为 true【§五】
  "chain_preview": ["tail_sweep"],  // L3 连锁预演（图鉴解锁后显示）【§五】
  "progress": "1/2"        // 蓄力进度【§五 消息模板】
}
```

### 3.2 预告分级（图鉴分级【§五】）

| 级 | 内容 | 可见性 |
|---|---|---|
| L1 意图类别 | 【蓄力】/【积蓄能量】/【攻击架势】 | 永远公开（小白读招底线） |
| L2 招名 | 行动名 | 图鉴解锁后显示；未解锁 = ？？？（"它在蓄力……（？？？）"） |
| L3 连锁预演 | "似乎要接【尾扫】" | 图鉴解锁后显示 |

### 3.3 3 类基础预告（L1 意图类别）以外的预告类型

| 预告类型 | 内容 | 分级 | 出处 |
|---|---|---|---|
| 蓄力进度 | "正在积蓄火焰能量…（1/2）"（置顶） | L1 | §五 消息模板 |
| 状态切换播报 | "🔥 熔岩霸王龙 狂暴化！！" | 公开 | §五 消息模板 |
| 连锁预演 | "似乎要接【尾扫】" | L3 | §五 |
| 中断恢复播报 | "连招 火球→【尾扫】→吼叫（2/3 段）" | 公开（快照恢复） | §五 / §十 |
| 换区逃亡提示 | "逃向 XX 区域" | 公开 | §六.3 |
| 阶段转换演出 | 转换演出回合（剧情时刻） | 公开 | §六.1 |
| 虚弱窗口提示 | 灭世龙息被打断后的虚弱窗口【细化：承接 §九 玩家博弈】 | 公开 | §九 |

### 3.4 绑定规则

- preview / preview_chain / reveal_condition 由 action.json 声明【§八 4.2.1】；reveal_condition 控制 L2/L3 的解锁条件【细化：锚定图鉴/教学解锁体系】。
- 一轮一条消息合并排版：预告置顶 → [状态行] [行动反馈] [连锁排队]【§五】。

---

## ④ 行动表解析（action / probability / weight 归一化）

### 4.1 字段语义（《定稿》§一）

| 字段 | 语义 | 默认 | 出处 |
|---|---|---|---|
| action | 行动 ID（引用 action.json） | — | §一 |
| weight | 基准权重（归一化后 = 概率） | 兜底值 | §一 |
| condition | 条件权重修正（如 pv_broken 时 ×2） | null | §一 |
| cooldown | 行动冷却 | 0 | §一 |
| hungry | 饥饿保底：连续 N 回合没选则强制选 | 无 | §一 |
| probability | 锚点开关：0=锚点（只被链/条件/状态机触发）；1=参与随机池（**数值不参与概率计算**，写其他正值等价 1） | **0** | §一 / 核心规则4 |

### 4.2 归一化规则（核心规则6：概率一律 = weight 归一化）

1. **随机池** = 全行动表中 probability=1 且不在冷却中的行动【核心规则4 / §一】。
2. **概率公式**：P(a) = (w_a × c_a × s_a) / Σᵢ∈池 (w_i × c_i × s_i)
   - w = weight 基准权重；c = condition 条件修正；s = 状态修正（weight_mod，如狂暴 attack×1.5 / defense×0.5【§三】）。
3. **示例（《定稿》§九 阶段1）**：爪击 40 / 火球 30 / 尾扫 20 / 吼叫 10（probability=1）→ 归一化 40% / 30% / 20% / 10%。狂暴态下（攻击类 ×1.5、防御类 ×0.5，tag 归属为示例假设【细化】）：爪击 60 / 火球 45 / 尾扫 30 / 吼叫 5 → 归一化 ≈ 42.9% / 32.1% / 21.4% / 3.6%（状态修正后重算）。
4. **hungry 保底**：随机池抽取前，先查连续 N 回合未选中的行动 → 强制选（随机偏差保护）【§一】。
5. **概率=0 全表 = 纯脚本 BOSS**：只走链/条件，不进随机【§一】。
6. **校验器**：概率总和为 0 → 黄提示；类型错误/负数/结构错误 → 硬拦（只建议不限制）【§八】。
7. **权重档位建议**：核心机制 100+ / 阶段 50-99 / 常规条件 10-49 / 连招锚点 0-9【§一】。

---

## ⑤ chains 连招链（chain C 模型 / 套内稳定 / 打断=套完结）

### 5.1 载体与节点 schema（连招载体唯一化【§八】）

- **顶层 chains 表（enemies.json）= 连招唯一载体**；链节点 schema `{action, chance, role, armor}`：
  - action = 行动 ID 引用（action.json）；chance = 触发 roll（建议 ≥80%）；role = chain / finisher；armor = 霸体免疫打断【§八】。
- **历史写法废弃**：action.json `action.chain` 字段、special_actions 内嵌 `chain` 数组 = 历史写法（兼容解析旧配置，新配置一律顶层 chains；after_action 触发保留"单次锚点行动"语义，需连招的用 `chain_ref` 引用链 ID）【§八 / §二】。
- **决策管线（§四 步骤3）**：chain_queue **只由顶层 chains 表驱动**，删除"条件行动内嵌链"入队路径【§八】。

### 5.2 chain C 模型：触发 roll + 套内确定性【核心规则2】

1. 链节点 chance：触发时 **roll 一次**决定是否入队（建议 ≥80%）。
2. 入队后 **套内确定性执行**（火球 roll 成功 → 尾扫必来，中途不被条件打断）。
3. roll 失败 = **断链**（该链进入冷却）。

### 5.3 套内稳定【核心规则1】

- 套 = 连招链 = 一套 / 单随机行动 = 长度 1 套 / 蓄力跨回合 = 同一套。
- 套内：只走链推进 / 蓄力结算，**不评估条件/状态/权重**（玩家预判稳定）。
- 套间：一套结算后回合间隙，评估条件行动 / 状态机切换 / 权重修正。

### 5.4 打断 = 套完结【核心规则3】

- 玩家打断技打断连招 → 该套视为**完结**；下一回合 BOSS 进入**随机流程**（不继续原套）。
- 蓄力可被打断；armor=true 节点霸体免疫打断【核心规则7 / §八】。
- 博弈语义：打断 = 牺牲行动换拆套；收益 = 避开尾扫；代价 = BOSS 随机化更难读【核心规则3 / §七】。

### 5.5 连招骨架与校验【§六.2 / §八】

- 骨架：起手（50-70%）→ 锚点（25-40%）→ 收尾（≥70%）；固定骨架：锚点→收尾必接（chance ≥80%）；随机血肉：起手选择/中间穿插每次不同。
- 校验器：接续概率 <60% → 黄提示【§八】。

---

## ⑥ 验收测试用例

共 20 条（TC-01 ~ TC-20），分组覆盖 ①-⑤ 与《定稿》§十 验收标准。

### 状态机（TC-01 ~ TC-05）

| ID | 用例名 | 前置 | 步骤 | 期望 | 依据 |
|---|---|---|---|---|---|
| TC-01 | 套间切换 + enter_action | 阶段2 开启，HP 55% | 玩家行动 → BOSS 打完一套（HP 降至 49%）→ 下一回合 | 套间 L1 评估 hp_below 50 成立 → 切 enraged → enter_action roar 入强制队列（L2 先执行吼叫）；攻击权重×1.5 生效 | §三 / §四 / 核心规则1 |
| TC-02 | 残血狂暴权重修正 | enraged 态 | 随机池 爪击40/火球30/尾扫20/吼叫10 | 攻击类×1.5、防御类×0.5 后归一化重算（≈42.9%/32.1%/21.4%/3.6%） | §三 / 核心规则6 |
| TC-03 | 濒死处决 | HP 16% | 一套打完 HP→14% | 套间切 dying → death_rattle 强制 → final_strike 仅 dying 态可出（exclusive） | §三 |
| TC-04 | 阈值单源 | phases 阶段2 阈值 60 | 修改 phases 60→55 后运行 | 阶段表与状态机联动阈值同步变化（状态机不重复声明血量阈值） | §三 边界声明 |
| TC-05 | 起身触发 | BOSS 处于 downed | 起身动作完成 → 下一回合 | 套间评估 get_up 条件行动（once=true 仅触发一次） | §二③ |

### 套间评估 8 级（TC-06 ~ TC-09）

| ID | 用例名 | 前置 | 步骤 | 期望 | 依据 |
|---|---|---|---|---|---|
| TC-06 | 评估优先级序 | 开场技在强制队列 + hp_below 条件满足 | 套间评估 | L2 强制队列先于 L3 条件行动执行 | §四 步骤1/2 |
| TC-07 | 同级随机 | 两个 priority=50 条件同时满足 | 套间评估 L3 | 随机选一触发（不双触发） | 核心规则5 |
| TC-08 | 套内不评估 | 在途链 火球→尾扫，尾扫前 HP 跨 30% | 套执行回合 | 不触发 hp_below 条件、不切状态、权重不重算，尾扫照常执行 | 核心规则1 / §四 套内门 |
| TC-09 | 兜底普攻 | 无强制/条件/链/专属，随机池为空 | 套间评估 L0-L6 无产出 | L7 默认普攻 | §四 步骤6 |

### 意图预告（TC-10 ~ TC-12）

| ID | 用例名 | 前置 | 步骤 | 期望 | 依据 |
|---|---|---|---|---|---|
| TC-10 | L1 永远公开 | 灭世龙息蓄力中 | 回合消息生成 | 置顶显示【蓄力】+ 进度（1/2）；未解锁也显示类别 | §五 |
| TC-11 | 图鉴分级 | 尾扫未解锁 | 火球后生成连锁预演消息 | L2 招名=？？？、L3 不显示；解锁后显示"似乎要接【尾扫】" | §五 |
| TC-12 | 中断恢复 | 战斗中断于连招 2/3 段 | 10 秒内恢复 | 快照 ai_state（chain_pos/chain_queue/charge/intent）恢复；消息显示"连招 火球→【尾扫】→吼叫（2/3 段）" | §五 / §八 A.5 / §十 |

### 行动表（TC-13 ~ TC-15）

| ID | 用例名 | 前置 | 步骤 | 期望 | 依据 |
|---|---|---|---|---|---|
| TC-13 | 权重归一化 | 阶段1 行动表 40/30/20/10 | 长时间统计随机选行动 | 比例趋近 40% / 30% / 20% / 10% | §一 / §九 |
| TC-14 | 锚点开关 | 火球 probability=0（仅链触发） | 随机池抽取 100 回合 | 随机池永不直接出火球；链触发照常入队；全表 0 = 纯脚本 BOSS（只走链/条件） | 核心规则4 / §一 |
| TC-15 | hungry 保底 | 大招 hungry=3 | 连续 3 回合随机未选 | 第 3 回合强制选大招 | §一 |

### 连招链（TC-16 ~ TC-18）

| ID | 用例名 | 前置 | 步骤 | 期望 | 依据 |
|---|---|---|---|---|---|
| TC-16 | chain C roll | 熔岩连招 火球(chance 0.8)→尾扫(chance 1.0) | 火球触发回合 roll | roll 成功 → 尾扫必接（套内确定性）；roll 失败 → 断链 + 链冷却，不接尾扫；接续 chance<60% 时校验器黄提示 | 核心规则2 / §八 |
| TC-17 | 打断=套完结 | 尾扫预告已出 | 玩家打断技命中 | 套完结 → 打断演出 → 下一回合 BOSS 进入随机流程（不继续原套） | 核心规则3 |
| TC-18 | 蓄力跨回合 | 灭世龙息蓄力 2 回合 | 蓄力期间玩家行动 + HP 跨阈值 | 套内门开启：不评估条件/状态/权重，蓄力照常结算；armor=true 霸体免疫打断，非霸体可被打断 | 核心规则1、7 / §九 |

### 综合与验收（TC-19 ~ TC-20）

| ID | 用例名 | 前置 | 步骤 | 期望 | 依据 |
|---|---|---|---|---|---|
| TC-19 | 换区流程 | HP 29%（hp_below 30） | 触发换区 | 提示"逃向熔岩坑道/矿洞" → 玩家追击 → 决战；换区后血量保持残血 + PV 恢复一半 + 开场技；退出副本区域=重置；战斗中断=快照续玩（套内可续） | §六.3 |
| TC-20 | 验收四判据 | 完整 BOSS 战 | 玩家通关 | 玩家能说出"火球后接尾扫"（可读性）；多次挑战起手/穿插有差异（差异性）；读招有四选一选项（应对面）；打断有拆套 vs 随机化取舍（博弈深度）；精英教过的机制在 BOSS 出现（先教后考）；一轮一条消息看完预告+反馈（QQ 适配） | §十 |

---

## 附：引用索引

| 《定稿》条目 | 本细化节 |
|---|---|
| 〇 核心规则 1-8 | ①②④⑤⑥ |
| 一 随机行动表 | ② L6 / ④ |
| 二 条件行动（13 类触发） | ② L3 / ① downed |
| 三 AI 状态机（含边界声明） | ① / ② L1、L5 |
| 四 决策管线（套内门 + 0-6） | ① 1.2 / ② 8 级映射 |
| 五 意图预告与 QQ 适配 | ③ |
| 六 BOSS 编排（阶段/连招/换区/教学/压迫） | ① 残血狂暴 / ⑤ 5.5 / ③ 3.3 |
| 七 博弈体验 | ⑤ 5.4 / ③ 3.3 |
| 八 数据结构与登记（chains 载体唯一化 / 快照 A.5 / 校验器） | ⑤ / ① 1.1 / ④ 4.2 |
| 九 示例：熔岩霸王龙 | ④ 4.2 示例 / ① 行为态 / ⑥ TC |
| 十 验收标准 | ⑥ TC-20 |

*细化结论：状态数 8（4 行为态 + 4 执行态）；验收用例 20 条（TC-01 ~ TC-20，6 组）。*
