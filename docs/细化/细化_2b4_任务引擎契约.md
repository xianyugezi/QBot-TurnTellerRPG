# 细化_2b4_任务引擎契约（quest.json 数据模型 · 三原语条件引擎 · 统一奖励发放 · 防刷任务板 · 验收测试）

> 版本：v1.0 · 类型：实现层细化契约（大厂模式：数据模型 / 原语引擎 / 统一发放 / 防刷边界 / 验收测试）
> 追溯依据（**严格引用策划案，引用到行号 Lxx**）：
> - 《任务系统设计定稿.md》v1.0.4（2026-08-16，quest.json · 条件驱动版）→ 简称【任务】（主依据，全篇行号引用）
> - 《NPC 系统设计定稿.md》v1.3 → 简称【NPC】（4.0/4.1/4.3 统一条件引擎与互译表主表，经【任务】L20/L37/L39-44 转述引用）
> - 变量体系 v1.2 → 简称【变量】（中文变量键 ↔ 英文条件键互译，主表落 NPC 4.3，任务侧速查见【任务】L47-58）
> - 实现层规划《规划_路3_框架指令数据.md》对应条目（指令归属与验收对齐）
> 范围：quest.json 字段级 schema、三原语条件引擎（值型/累计型/事件型）求值语义、统一 reward 条目解析与发放、防刷机制（每日上限/接取上限/完成即移出 active）、任务板交互（主线置顶/接取序号/交付/双板仲裁）、验收测试；
> 不含编辑器 UI（【任务】§七 预留）、NPC 候选任务表全量结构（本契约只定义 quest 侧 `npc` 字段衔接，NPC 侧挂载按【任务】§3.2 由 NPC 定稿承接）。
> 变更纪律：任何偏离【任务】之处必须写入 §0.3 决策记录并注明理由+影响用例，禁止静默改语义。

---

## 〇、契约总则

### 0.1 术语表

| 术语 | 定义 | 依据 |
|---|---|---|
| 条件任务引擎 | 任务 = 条件组合（变量体系驱动）——不枚举类型，任何新任务形态零新增代码 | 【任务】L10/L21 |
| 三原语 | 判定 100% 由统一 conditions 数组驱动，仅 3 种原语：值型 / 累计型 / 事件型 | 【任务】L19/L25-36 |
| 统一条件引擎 | `{var, op, value, param}` 四键条件条目（NPC 4.0），中文变量键经别名映射互译为英文条件键 | 【任务】L20/L39-44 |
| type | **仅展示标签 / 编辑器模板**（collect/deliver/slay/explore/intel…），非判定依据 | 【任务】L18/L97 |
| reward 统一条目 | 物品 `{item,count}` / 货币键值 `{coins\|gem\|exp\|rep:N}` / 组合数组三型，**四系统（任务/签到/NPC/怪物掉落）共用同一发放器** | 【任务】L108-126 |
| quest_active | 进行中任务表（接取的任务+当前进度，ID+名称冗余，非会话无锁无超时，热重载降级提示） | 【任务】L204-207/L350 |
| quest_daily | 每日防刷表（今日完成数/接取数/重复衰减计数，每日懒计算重置，默认 05:00） | 【任务】L208-210/L182 |
| daily_limit | 每日完成上限（防刷），默认 ≤10，可配 0=不限 | 【任务】L184 |
| accept_limit | 同时接取上限，默认 ≤5，可配 0=不限 | 【任务】L183 |
| main 主线 | 常驻任务标记（main:true，不刷新不移除，/任务 置顶） | 【任务】L138/L135/L187 |

### 0.2 一句话契约

> 任务引擎 = 由 quest.json 配置、**判定 100% 由 `conditions` 数组三原语（值型/累计型/事件型，`{var,op,value,param}`）驱动**的任务系统；
> type 只是展示标签、零新增代码；奖励走 **reward 统一条目**（物品/货币键值/组合数组，内联串为解析器等价的序列化糖），发放器唯一实现＝框架 reward 解析器（任务/签到/NPC/怪物掉落四系统共用）；
> 防刷靠 `board.daily_limit ≤10`（0=不限）+ `board.accept_limit ≤5`（0=不限）+ 完成即移出 quest_active（主线常驻除外），每日 05:00 懒计算重置；
> 任务板 `/任务`＝主线置顶 + 板上任务 + NPC 支线，`/接取 N`（序号，不带 *）、`/交付 N` 闭环；委托板独立走 `/委托`（§3.4 仲裁）。

### 0.3 决策记录（ADR，均为对定稿语义的显式收敛，不违反定稿任何一行）

| 编号 | 决策 | 理由 | 影响用例 |
|---|---|---|---|
| D-01 | **字段命名收敛**：【任务】只给语义/数值约束、未给字段名的属性本契约收敛为：顶层 `main`（布尔，L138）、`daily`（布尔=板槽简写，L181-182）、`repeatable`（布尔/对象，L185）、`npc`（对象，L143-151）；板级 `board.accept_limit`（L183）/ `board.daily_limit`（L184）；奖励字段沿用定稿之名 `reward`（L100），`rewards`（复数）为等价别名（解析器等价，同给异值→黄提示） | 定稿板规则章节只给了数值（≤5/≤10）与默认关，字段名需收敛一份供实现直接落地 | TC-01 / TC-03 / TC-18~23 |
| D-02 | **多条件语义 = 数组全与（AND）**：conditions 数组排序后逐条求值，**全部为真才判定完成**；不引入 OR/组合算子（要 OR 语义拆成多个候选任务，对齐 §3.2 差异化发任务哲学） | 【任务】L19「判定 100% 由统一 conditions 数组驱动」未给算子；全与最简、碎片化友好 | TC-12 |
| D-03 | **求值失败安全降级**：条件求值异常（变量未注册 / 事件未触发过 / param 维度缺失）= 默认**不满足**（false），不崩溃、不阻塞指令，仅面板黄提示 | 对齐一体化条件引擎在商店 open_condition 的「求值失败默认不满足」惯例（商店 L339/L500），【任务】§八 校验器黄提示体系 | TC-31 |
| D-04 | **完成结算单事务**：发放奖励（框架 reward 解析器，L125）+ main_progress 计数（L61）+ 移出 quest_active（L205）+ 记 quest_daily（L209）全部在**一个 SQLite 事务**内；重复交付携带旧会话快照→幂等不二次发放 | 【任务】L125 发放器唯一实现＋碎片化友好（L204-206 非会话）；对齐商店原子结算防双扣惯例 | TC-17 / TC-21 |
| D-05 | **内联串 = 序列化糖**：`exp=50,coins=80,item:铁矿*3` ≡ `[{exp:50},{coins:80},{item:"铁矿",count:3}]`，解析器等价展开；存储与校验以结构化条目为准，导入器自动展开 | 【任务】L100/L124 明示「解析器等价」 | TC-02 |
| D-06 | **timed = {deadline, penalty}**：deadline 现实时间懒计算（L102/L349②），接取时明确倒计时；penalty 默认无（可配扣物/仅提示），超时未交付按 penalty 处理并移出 active | 【任务】L102「deadline 现实时间懒计算」+ L349「可配无惩罚」 | TC-30 相关、边界用例 |
| D-07 | **默认值兜底（漏配=合理默认，绝不报错）**：type=collect｜consume=false｜reward=空｜board=每日默认板｜timed/unlock_chain/zone/filter/bonus/npc=null｜main=false｜daily=false｜repeatable=false｜daily_limit=10｜accept_limit=5 | 【任务】L11「极简字段+默认值兜底」+ L92-106 字段表「默认」列 | TC-01 |

---

## 一、quest.json 字段级 schema

### 1.1 文件根与结构总览（字段树）

```
quest.json                            // 文件根（【任务】L73-89 极简字段 + 默认值兜底）
└── [任务] ×N                          // 17 顶层字段（下表 1.2）
    ├── conditions: Cond[]            // 判定条件数组（4 键原语 {var,op,value,param}，§二）
    ├── reward: str|list              // 统一奖励条目（§三，内联串=糖）
    ├── board: BoardConf              // 任务板配置（5 key，§1.3）
    ├── timed: {deadline,penalty}     // 限时修饰符（2 key，D-06）
    └── npc: NpcGrant                 // NPC 差异化发任务（3 key，§1.4）
字段数：顶层 17 ＋ board 5 ＋ npc 3 ＋ timed 2 ＝ 27 叶子字段；
另：条件原语 4 键（{var,op,value,param}，3 型）＋ 奖励条目 3 型（物品/货币键值/组合）。
```

**字段计数口径**：顶层 17 字段（下表逐列清点）＋ `board` 子对象 5 key（slot/refresh/limit/accept_limit/daily_limit）＋ `npc` 子对象 3 key（id/conditions/priority）＋ `timed` 子对象 2 key（deadline/penalty）＝ **27 叶子字段**。`conditions` 条目 4 键（三原语 3 型，§二）；`reward` 条目 3 型（§三）。

### 1.2 顶层字段表（17 字段）

| # | 字段 | 类型 | 必选 | 默认 | 取值/约束 | 语义 | 依据 |
|---|---|---|---|---|---|---|---|
| 1 | id | string | ✅ | 自动生成 | 全局唯一 | 任务唯一 ID，被 unlock_chain / 副本 subquests 引用 | 【任务】L94 |
| 2 | name | string | ✅ | — | 任意文本 | 任务名 | 【任务】L95 |
| 3 | desc | string | 可选 | "" | 任意文本 | 描述 | 【任务】L96 |
| 4 | type | string | 可选 | `"collect"` | 任意标签（collect/deliver/slay/explore/intel…） | **展示标签/编辑器模板，非判定依据**——判定全由 conditions 驱动，引擎对 type 零分支 | 【任务】L18/L97 |
| 5 | main | bool | 可选 | false | true/false | **主线标记**：常驻不刷新不移除，/任务 置顶（接收时不占 accept_limit 亦可配） | 【任务】L138 |
| 6 | conditions | list | ✅ | [] | Cond[]，三原语 `{var,op,value,param}` | **判定条件数组**；全与（D-02），数组为空=接取即完成 | 【任务】L98/L19 |
| 7 | consume | bool | 可选 | false | true=交付消耗（deliver）/false=计数（collect） | 交付语义收敛一个布尔：true 交付时扣物出包，false 只计数不扣物 | 【任务】L99/L81 |
| 8 | reward | str 或 list | 可选 | "" | 统一 reward 条目（物品/货币键值/组合）或内联串；别名 `rewards` 等价（D-01） | 奖励配置，经框架 reward 解析器发放（§三） | 【任务】L100 |
| 9 | board | obj | 可选 | 每日默认 | §1.3 五键 | 任务板（槽位/刷新/上限/防刷） | 【任务】L101/L180-187 |
| 10 | timed | obj | 可选 | null | `{deadline,penalty}`（D-06） | 限时修饰符：deadline 现实时间懒计算，接取时明确倒计时，penalty 默认无 | 【任务】L102/L349 |
| 11 | unlock_chain | string | 可选 | null | 前置任务 ID | 链式任务（前置完成任务后才可接取） | 【任务】L103/L85 |
| 12 | zone | string | 可选 | null | dungeon.json / maps.json 的 ID | 地图/副本关联：副本子任务限定用（zone 限定，不占板槽位）；校验器引用检查 | 【任务】L104/L86/L325 |
| 13 | filter | obj | 可选 | null | `{quality:"精良",...}` | 交付过滤（品质/特性/数量要求），交付时逐项校验 | 【任务】L105/L299 |
| 14 | bonus | obj | 可选 | null | `{condition:{var,op,value,param},mult:N}` | 条件倍率：满足加分项→奖励×N（如无伤交付×1.5） | 【任务】L106/L300 |
| 15 | npc | obj | 可选 | null | §1.4 三键 | NPC 差异化发任务（§3.2 集成）：标记由哪个 NPC 触发、发任务条件与优先级 | 【任务】L141-154（命名收敛 D-01） |
| 16 | daily | bool | 可选 | false | true/false | 每日板标记简写 = `board:{slot:"daily",refresh:"daily"}`；与 board 对象同给 → 互斥黄提示 | 【任务】L181-182（收敛 D-01） |
| 17 | repeatable | bool/obj | 可选 | false | false=完成即移出不可再接；true=可重复；obj=`{decay:N,cap:M}`=重复衰减 | 重复/衰减开关：false 为主线外普通任务的默认；decay 对应重复衰减奖励递减（默认关） | 【任务】L185/L187（收敛 D-01） |

> 引擎统一性铁律：**type 只作为展示标签/编辑器模板，判定 100% 由 conditions 驱动，任何新任务形态零新增代码**【任务】L18-21/L97。

### 1.3 board 任务板子对象（5 key，默认值兜底全部可配）

| # | 字段 | 类型 | 默认 | 取值/约束 | 语义 | 依据 |
|---|---|---|---|---|---|---|
| 1 | slot | enum | `"daily"` | `daily` \| `weekly` \| `event` | 板槽位：daily=每日刷新板、weekly=周板、event=活动板（多板） | 【任务】L181 |
| 2 | refresh | enum | `"daily"` | `daily` \| `weekly` \| `once` | 刷新模式，每日懒计算刷新，重置时刻默认 05:00 可配（与签到/商店同刻对齐） | 【任务】L182 |
| 3 | limit | int | 0 | ≥0；0=不限 | 板上限：本周期该任务最多上架/完成次数（示例 limit:1/3） | 【任务】L101/L302/L314 |
| 4 | accept_limit | int | 5 | ≥0；**0=不限**；**默认 ≤5** | 同时接取上限（板级防刷）：玩家同时进行中的任务数上限 | 【任务】L183 |
| 5 | daily_limit | int | 10 | ≥0；**0=不限**；**默认 ≤10** | 每日完成上限（防刷总闸）：今日完成计数达上限即拒绝交付/领奖 | 【任务】L184 |

**default 板兜底**：漏配 board = `{slot:"daily", refresh:"daily", limit:0, accept_limit:5, daily_limit:10}`（D-07）。
**层级**：板级 accept_limit/daily_limit 为全局默认；quest 级可不配并继承板默认——防刷口径以 quest_daily 当日计数为准（§四）。

### 1.4 npc 差异化发任务子对象（3 key，§3.2 收紧）

| # | 字段 | 类型 | 默认 | 取值/约束 | 语义 | 依据 |
|---|---|---|---|---|---|---|
| 1 | id | string | — | npc.json 的 NPC ID | 发起 NPC 引用（/与 NPC 对话 触发条件匹配→发布） | 【任务】L143-145（收敛 D-01） |
| 2 | conditions | list | [] | 统一条件引擎 `{var,op,value,param}`（采集熟练<3 ≡ `{var:prof_level,op:lt,value:3,param:"采集"}`） | **发任务条件**：玩家与 NPC 互动时按满足条件发布不同任务（差异化支线） | 【任务】L149-151/L148 |
| 3 | priority | int | 0 | ≥0，小者先 | 多候选匹配顺序：匹配**第一个满足条件的任务**发布；不同玩家/不同状态→不同任务 | 【任务】L145-146 |

> 衔接：NPC 侧挂多个候选任务 + 各自条件，本字段为 quest 侧回指；`/与 NPC 对话` → 按条件匹配发布（【任务】L178）。
> 与任务板区别：板=每日刷新通用任务；NPC=按条件差异化的支线（事件牌组）【任务】L153/L156-169。

### 1.5 校验器与默认值兜底（边界契约）

**硬拦（拒绝加载，4 类）**：引用不存在（物品/怪物/变量/zone 地图副本）/ 类型错误 / 负数 / 结构错误【任务】L276。
**黄提示（不拦可加载）**：条件永假（如 gain_count:铁矿 ≥ 999）/ 奖励超基准 / 链式任务死链 / 重复衰减异常 / 板上限冲突 / 条件键非互译表键（旧 `{type,var,op,value}` 结构=「旧格式，建议迁移」）/ main_progress 主线链可达性【任务】L277-279。
**默认兜底（D-07）**：type=collect｜reward=空｜timed/unlock_chain/zone/filter/bonus/npc=null｜true 布尔全 false｜daily_limit=10｜accept_limit=5【任务】L92-106 默认列。

---

## 二、三原语引擎（值型 / 累计型 / 事件型判定语义）

### 2.1 求值管线

```
/任务 进度查询 / 事件结算点 / NPC 互动
 → 读取玩家当前状态（快照，非会话）
 → 依序求值 conditions[] 逐条 evaluate（三原语归一开关，§2.3）
 → 全部为真（AND）→ 判定可交付/完成（D-02）
 → 条件求值失败 → 默认不满足（false，不崩，D-03）
```

判定 100% 由统一 conditions 数组驱动（【任务】L19）；条件全部基于统一条件引擎 `{var,op,value,param}`（【任务】L20）。

### 2.2 三原语判定语义表

| 原语 | var 表达式形态 | param 维度 | 读取来源 | 判定语义 | 依据 |
|---|---|---|---|---|---|
| **值型**（当前值比较） | 状态键：`level` / `item_count`(＋param) | param=目标物品 ID | 当前状态快照（等级/背包数量等） | 读当前值，op 比较 value，立即判定 | 【任务】L27-29 |
| **累计型**（长线计数） | 长线键：`gain_count` / `kill_count` / `codex` / `dungeon_clear` / `reputation` / `main_progress` | param=目标维度（物品/怪物/副本/板 ID，缺省=全局） | 玩家存档 **longline_counters**（跨任务复用，任务只读不重复计数） | 读累计计数，op 比较 value；长期成立 | 【任务】L30-32/L201-203/L204 |
| **事件型**（事件触发） | **固定前缀 `[事件:事件名]`**（如 `[事件:落石]` / `[事件:dungeon_clear]`） | param=目标（事件作用目标，可空） | 事件触发计数表（NPC/副本/地图机制 on_* 写入） | 事件触发次数 ≥ value（默认 1）即满足；一旦满足长期成立 | 【任务】L33-35/L58 |

**var 表达式三态归类**（互译表【任务】L47-58）：
- **状态值**：`level`、`item_count`（当前背包快照）
- **累计计数**：`gain_count` 累计获得、`kill_count` 累计击杀、`codex` 图鉴完成度、`dungeon_clear` 副本通关次数、`reputation` 声望等级（读 reputation_state，§五）、`main_progress` 主线进度
- **事件计数**：`[事件:X]`（封面 `[事件:事件名]` 原语，映射 on_*("目标")）

### 2.3 op 双写等价表（双写等价，全文示例用英文 op）

| 符号写法 | 英文写法 | 语义 |
|---|---|---|
| `>=` | `ge` | 大于等于 |
| `>` | `gt` | 大于 |
| `<=` | `le` | 小于等于 |
| `<` | `lt` | 小于 |
| `=` | `eq` | 等于 |
| `!=` | `ne` | 不等于 |

依据：【任务】L37「op 符号双写等价：`>=` ≡ ge ｜ `>` ≡ gt ｜ `<=` ≡ le ｜ `<` ≡ lt ｜ `=` ≡ eq ｜ `!=` ≡ ne（全文示例统一用英文 op）」。
校验器：op 非枚举 → 硬拦类型错误【任务】L268/L277。

### 2.4 param 目标维度语义

- **带参泛化引用**：`item_count` / `gain_count` / `kill_count` / `dungeon_clear` / `prof_level` / `reputation` ＋ param:目标（物品/怪物/副本/职业/板 ID），param 为**精确目标键**（exact 匹配）【任务】L47-57。
- **全局无参**：`level` / `codex` / `main_progress` 无 param；`reputation` param 缺省=全局（板 ID 缺省=全局声望）【任务】L57。
- 示例：`[背包:铁矿] ≡ {var:item_count, param:"铁矿"}`；`[累计击杀:岩皮鼬] ≡ {var:kill_count, param:"岩皮鼬"}`【任务】L42。
- **别名互译**：中文变量键经显式别名映射互译为英文条件键；唯一权威主表=NPC §4.3 互译表；编辑器显示中文、存储与校验一律英文条件键【任务】L43-44。

### 2.5 main_progress 取值语义（v1.0.4 补定义）

- **默认语义**：已完成 main:true 主线任务计数（done 计数）——随任务完成结算递增，落 longline_counters（主线任务完成数）【任务】L61。
- **可配语义**（内容包二选一或组合，编辑器下拉）：①整数序号（当前主线章节序号，沿 unlock_chain 链推进=链上已解锁最深序号）；②完成状态数组（done 状态数组取值=已 done 计数或当前章节号）【任务】L62-64。
- 示例：`[主线进度≥3] ≡ {var:main_progress, op:ge, value:3}`【任务】L65。
- 校验器：main_progress 条件与主线链可达性互查（黄提示「主线进度≥N 但主线链不足 N 个任务」）【任务】L66。

---

## 三、奖励统一条目解析（四系统共用发放器）

### 3.1 条目 schema（三型，统一 · 任务/签到/NPC/怪物掉落四系统共用）

```json
// ① 物品条目（引用 items.json）：{ "item": "ID", "count": N }
{ "item": "铁矿", "count": 3 }
// ② 货币/数值键值条目：{ "coins" | "gem" | "exp" | "rep": N }
{ "coins": 80 }   // 金币（settings 货币键空间）
{ "gem": 3 }      // 宝石
{ "exp": 50 }     // 经验（数值直入账）
{ "rep": 20 }     // 声望（可选模块；rep = reputation 值入账 → reputation_state，§五）
// ③ 组合数组：多条目按序入账
"reward": [ { "exp": 50 }, { "coins": 80 }, { "item": "铁矿", "count": 3 } ]
```

依据：【任务】L112-120。
- 物品键固定 `item`（引用 items.json）；货币键 coins/gem 走 settings 货币键空间；exp/rep 为数值直入账【任务】L123。
- **内联键值串（`item:铁矿*3` / `exp=50,coins=80`）= 同一解析器的序列化糖**——解析器等价展开为条目数组，存储与校验以结构化条目为准（旧内联串由导入器自动展开）【任务】L124/D-05。
- **发放器唯一实现 = 框架 reward 解析器**（物品入包/货币入账/exp/声望）——签到 rewards、NPC give_item、怪物掉落只配条目不另立发放逻辑【任务】L125。
- 等价写法：签到 items[]{id,count} 的 id 键 ≡ 物品条目 item 键；怪物掉落条目在本 schema 上扩展概率/条件/数量区间字段【任务】L126。

### 3.2 逐键入账管线（框架 reward 解析器）

```
解析 reward（内联串 → 条目数组，D-05）
 → 依序逐条入账（组合数组按序）【任务】L119
   ├─ exp   → 经验数值直入账
   ├─ coins / gem → settings 货币键空间入账
   ├─ rep   → reputation_state 累计值入账（声望=进度门槛，不入货币表）【任务】L118/L226
   └─ item  → items.json 引用，count 个入包
 → 全部入账 + 防刷计数 + active 移出 在同一 SQLite 事务（D-04）
 → 回复模板：/交付 2 →「✅ 交付完成：铁矿×3（+exp50/coins80），今日已完成 N/10」
```

- 发放顺序即数组顺序（【任务】L119「多条目按序入账」）。
- **rep 落点**：reputation 值存玩家存档 reputation_state（值+等级+来源板 ID），reward 条目 rep=reputation 值入账（**不入货币表**）；任务系统只声明「完成委托任务 +N（板配置）」，不另立存储【任务】L226。
- **声望双源归一**：任务板 rep 奖励 与 委托板交付评价并入同一 reputation_state，不另立双套【任务】L223。
- 发放器唯一性铁律：**只配条目不配实现**，四系统共用同一入账管线【任务】L108/L125。

### 3.3 边界

- **超基准黄提示**：单任务 reward 总值超出内容包基准 → 黄提示（不拦）【任务】L277。
- **发放幂等**：完成结算单事务＋会话快照幂等，重复交付不二次发放（D-04）【任务】L125/L205。
- 声望模块默认关：禁 rep 条目时 rep 被忽略并黄提示（可配）【任务】L218「默认关（内容包按需开）」。

---

## 四、防刷机制（每日上限 / 接取上限 / 完成即移出）

### 4.1 三表存储（进度存储，三表设计）

| 表 | 内容 | 生命周期 | 依据 |
|---|---|---|---|
| **longline_counters**（框架级） | 累计击杀/累计获得/图鉴进度/副本通关——任务只读（跨任务复用），存于玩家存档，任务系统不重复计数 | 永久累计，不随任务重置 | 【任务】L201-203 |
| **quest_active**（进行中） | 接取的任务+当前进度（ID+名称冗余，对齐快照原则）；非会话（无锁/无超时，碎片化友好）；热重载：删配置降级提示不报错 | 接取→完成/放弃移出 | 【任务】L204-207/L350 |
| **quest_daily**（每日防刷） | 今日完成数/接取数/重复衰减计数 | 每日懒计算重置（默认 05:00，与签到/商店同刻对齐） | 【任务】L208-210/L182 |

### 4.2 防刷规则

| 编号 | 规则 | 阈值 | 触发行为 | 依据 |
|---|---|---|---|---|
| F-1 | **每日完成上限 daily_limit**：今日完成数（quest_daily）达上限 | 默认 **10**，0=不限 | 拒绝交付/领奖：提示「今日任务已完成 10/10，明早 5 点刷新」 | 【任务】L184/L209 |
| F-2 | **同时接取上限 accept_limit**：quest_active 行数达上限 | 默认 **5**，0=不限 | 拒绝接取：提示「同时最多进行 5 个任务（当前 5）」 | 【任务】L183/L209 |
| F-3 | **完成即移出 active**：交付/领奖成功后该任务从 quest_active 删除；非 repeatable 不可再接。**主线 main:true 除外**（常驻；所属子任务完成仅推进进度不移除本体） | — | 完成→移出→/任务 不再显示（主线置顶仍显示） | 【任务】L205/L187/L138 |
| F-4 | **重复衰减**（repeatable={decay,cap}，默认关）：同一任务重复完成奖励递减 | 默认关 | 第 2 次起 奖励×decay，至 cap 下限 | 【任务】L185/L209 |
| F-5 | **重置**：quest_daily 每日懒计算重置（05:00 可配），完成数/接取计数/衰减计数清零 | 05:00 | 跨天首个操作惰性补算 | 【任务】L182/L208-210 |

### 4.3 防刷结算事务（D-04）

```
/交付 N 校验通过
 → BEGIN TRANSACTION
 → ① 发放 reward（框架解析器，§三）
 → ② main_progress 计数（主线 done 计数 +1，若主线） 【任务】L61
 → ③ 移出 quest_active                                          【任务】L205
 → ④ quest_daily.完成数 +1 / 衰减计数更新                       【任务】L209
 → COMMIT  任一失败 ROLLBACK（不发放/不移除/不计数）
幂等：会话快照防重复发起（框架 3.18 快照原则，见【任务】L206/L350）
```

---

## 五、任务板（主线置顶 / 接取序号 / 交付 / 双板仲裁）

### 5.1 指令闭环（【任务】L171-178）

| 指令 | 行为 | 依据 |
|---|---|---|
| `/任务` | 查看任务列表：**主线置顶** + 板上任务 + NPC 支线 | 【任务】L173 |
| `/接取 3` | 接取 3 号任务（**序号不带 `*`**；`*`=已接取/不可接取不计序号） | 【任务】L174 |
| `/任务信息` | 查看已接任务进度（三原语进度逐条显示） | 【任务】L175 |
| `/交付 2` | 交付 2 号任务（consume=true 扣物出包；自动计数任务只需 /交付 领奖） | 【任务】L176/L81 |
| `/放弃 2` | 放弃任务（可配惩罚，默认无） | 【任务】L177 |
| `/与 NPC 对话` | 与当前地图 NPC 互动（触发条件匹配 → 发布差异化支线，§1.4） | 【任务】L178 |

### 5.2 列表排版（主线置顶 + 序号）

```
/任务
━━ 主线（常驻） ━━
1. [主线] 特制武器        // main:true 置顶，不标 *
2. [主线] 锻造试炼
━━ 每日板上任务 ━━
3. 收集铁矿       进度 12/20      [今日 3/10]
4. 清剿熔岩甲虫   进度 0/3
━━ NPC 支线 ━━
5. 打造武器       铁匠·发任务条件命中
请发送 /接取 <序号> 接取任务（* 为已接取）
```

- **主线置顶**：main:true 常驻、置顶、不移除【任务】L135/L173/L187；板上任务按 slot 槽位展示；NPC 支线列尾。
- **接取 N 序号**：`/接取 N` 按展示序号命中（列表序号即指令参数）；不带 `*` 意为未接取的可接【任务】L174。

### 5.3 板规则（默认值兜底，全部可配【任务】L180-187）

- 槽位：daily 槽（每日刷新）+ 可选 weekly/event 槽（多板，§1.3）【任务】L181。
- 刷新：每日懒计算刷新（重置时刻默认 05:00 可配，与签到/商店同刻对齐）【任务】L182。
- 接取上限：同时接取 ≤5（可配 0=不限）＝ board.accept_limit【任务】L183。
- 每日完成上限：≤10（防刷，可配 0=不限）＝ board.daily_limit【任务】L184。
- 重复衰减：同一任务重复完成奖励递减（默认关）＝ repeatable【任务】L185。
- 声望：可选模块（见【任务】§五）【任务】L186。
- 主线：常驻不移除（main:true）【任务】L187。

### 5.4 双任务板指令归属（v1.0.4 仲裁，修复缺漏批2C）

- **`/任务` = 玩家任务板**（主线 + quest.json 板槽任务 + NPC 支线）；本节指令（/接取 /交付 /放弃 /任务信息）默认作用于玩家任务板【任务】L191。
- 炼金委托板（profession.json quest_board）= **独立板**：指令改 `/委托`（或 `/任务板 <板名>`，内容包可配别名），不占用 /任务 路由【任务】L192。
- 冲突仲裁：裸 `/任务` `/接取` `/交付` 归玩家任务板；委托板操作必须带板前缀（`/委托 查看` `/委托 接取 3` `/委托 交付 <委托> <道具>`）【任务】L193。
- 落点说明：职业熟练度与生活系统 §3.1 / 炼金定稿指令表中的 `/任务` 指令行建议同步改为 `/委托`（本批未触碰，交后续批次/父 agent 协调登记）【任务】L194。

---

## 六、验收测试用例

### 覆盖矩阵（31 用例 / 6 类）

| 需求类别 | 定稿行号 | 用例区间 | 条数 |
|---|---|---|---|
| ① schema 解析与默认值兜底 | L92-106、L124-126 | TC-01 ~ TC-05 | 5 |
| ② 三原语判定引擎 | L25-37、L47-66 | TC-06 ~ TC-12 | 7 |
| ③ 奖励统一条目 | L108-126、L119 | TC-13 ~ TC-17 | 5 |
| ④ 防刷机制 | L183-184、L198-210 | TC-18 ~ TC-23 | 6 |
| ⑤ 任务板交互 | L171-194 | TC-24 ~ TC-28 | 5 |
| ⑥ 校验器与边界 | L273-280、L349-350 | TC-29 ~ TC-31 | 3 |
| **合计** | — | TC-01 ~ TC-31 | **31** |

### ① schema 解析与默认值兜底

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-01 | 最小配置 `{id,name,conditions:[...]}` 无其它字段 | 默认值兜底（D-07）成功加载：type=collect｜consume=false｜reward 空｜board=每日默认板(limit 0)｜timed/unlock_chain/zone/filter/bonus/npc=null｜main=false｜daily=false｜repeatable=false｜daily_limit=10｜accept_limit=5，绝不报错 | L92-106/L11 |
| TC-02 | `reward:"exp=50,coins=80,item:铁矿*3"` 与结构化 `[{exp:50},{coins:80},{item:"铁矿",count:3}]` 各配一任务 | 解析器等价展开（D-05）：两种写法入账结果完全一致；存储/校验均以结构化条目为准 | L100/L124 |
| TC-03 | 一条配 `reward`，一条配 `rewards`（复数别名） | 解析器等价（D-01）：同一任务两种写法行为一致；同给且异值 → 黄提示「奖励字段重复」 | L100 |
| TC-04 | 配自定义标签 `type:"gather_rare"`（不在预设 collect/deliver/slay/explore/intel） | **不报错、不新增判定分支**：type 纯展示/模板标签，判定只由 conditions 驱动 | L18/L97/L19 |
| TC-05 | `zone:"nonexistent_dungeon"` 引用不存在的副本 ID | 校验器硬拦「zone 引用不存在」，该任务拒绝加载并提示 | L104/L276 |

### ② 三原语判定引擎

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-06 | 值型 `{var:level, op:ge, value:10}`：LV10 / LV9 玩家各查 | LV10 → 满足（可交付）；LV9 → 不满足；判定读当前状态快照 | L29 |
| TC-07 | 值型 `{var:item_count, op:ge, value:20, param:"铁矿"}`：背包铁矿 20、背包铁矿石 20 | 铁矿 20 → 满足；铁矿石 20 → 不满足（param 精确目标维度，不同名不算） | L28/L42/L50 |
| TC-08 | 累计型 `{var:gain_count, op:ge, value:30, param:"铁矿"}`：玩家历史累计获得铁矿 30 | 满足；计数读 longline_counters（跨任务复用，任务只读不重复计数） | L31/L202 |
| TC-09 | 累计型 `{var:kill_count, op:ge, value:3, param:"熔岩甲虫"}`：击杀过 3 只熔岩甲虫 | 满足；击杀计数读 longline_counters；与击杀累计共享计数 | L31/L52/L334 |
| TC-10 | 事件型 `{var:"[事件:dungeon_clear]", op:ge, value:1, param:"熔岩洞窟"}`：通关熔岩洞窟后查询 | 事件触发计数 ≥1 → 满足；未通关 → 不满足；var 固定前缀 `[事件:事件名]` | L34-35/L58 |
| TC-11 | 同一判据分别写 `op:">="` 与 `op:"ge"`（双写等价） | 两写法产出完全一致的真/假（`>=`≡ge，`!=`≡ne 等六对） | L37 |
| TC-12 | conditions 两条 `[{var:level,op:ge,value:10},{var:item_count,op:ge,value:20,param:"铁矿"}]`：仅满足其一 | **全与判定**（D-02）：必须两条同时满足才完成；任一不满足不回显示完成 | L19 |

### ③ 奖励统一条目

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-13 | `reward:[{exp:50}]` 完成交付 | exp 数值直入账 +50 经验 | L117/L123 |
| TC-14 | `reward:[{coins:80},{gem:3}]` 完成交付 | coins/gem 走 settings 货币键空间入账：金币 +80、宝石 +3 | L115-116/L123 |
| TC-15 | `reward:[{item:"铁矿",count:3}]` 完成交付 | 物品按 items.json 引用入包 3 个铁矿 | L113/L123 |
| TC-16 | `reward:[{rep:20}]` 完成交付（声望模块开） | rep=20 声望值入**reputation_state**（值+等级+板 ID），**不入货币表**；声望=进度门槛非支付货币 | L118/L226 |
| TC-17 | 组合数组 `[{exp:50},{coins:80},{item:"铁矿",count:3}]` 按序入账；发放中途（如货币满）抛异常 | 单事务（D-04）：全成或全不成，无部分入账；重复交付带旧快照 → 幂等不二次发放 | L119/L125/L120 |

### ④ 防刷机制

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-18 | 默认 daily_limit=10：当日已完成 10 个每日任务再交付第 11 个 | 拒绝交付/领奖并提示「今日任务已完成 10/10，明早 5 点刷新」；quest_daily 完成计数不再增加 | L184/L209 |
| TC-19 | 配 `daily_limit:0`（0=不限） | 每日完成数不受限，防刷闸关闭 | L184 |
| TC-20 | 默认 accept_limit=5：同时进行中 5 个任务再 `/接取` 第 6 个 | 拒绝接取并提示「同时最多进行 5 个任务（当前 5）」；quest_active 行数保持 5 | L183/L209 |
| TC-21 | 交付成功后该普通任务再 `/接取 N`（非 repeatable） | **完成即移出 active**：该任务已从 quest_active 删除，不可再接（提示「任务已完成」）；/任务 中不再出现 | L205/L187/L138 |
| TC-22 | main:true 主线任务完成后查看 /任务 | 主线**常驻不移除、仍在置顶**显示（可重复查看/推进下一环）；主线链推进 main_progress | L138/L187/L61 |
| TC-23 | `repeatable:{decay:0.5,cap:1}` 同一任务重复完成 3 次 | 第 2 次起 奖励×0.5、第 3 次 ×0.25 至 cap 下限 1；默认 repeatable=false 时无此行为 | L185/L209 |

### ⑤ 任务板交互

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-24 | 玩家发送 `/任务`（含主线 2 个 + 板上 3 个 + NPC 支线 1 个） | 列表**主线置顶**，其次板上任务、NPC 支线；均带序号；主线带 `[主线]` 标记不标应付序号 | L173/L135 |
| TC-25 | `/接取 3` 接取列表 3 号任务 | 成功入 quest_active，/任务 列表中该序号变 `*`（已接取标记，不再参与可接序号计数） | L174 |
| TC-26 | `/交付 2`：2 号任务 consume=true（交付扣物）与 consume=false（计数领奖）各一 | consume=true：校验背包扣物数量后发放奖励；consume=false：计数达标直接领奖（/交付 即可），不扣物 | L176/L81 |
| TC-27 | `/放弃 2` 放弃进行中任务 | 任务从 quest_active 移除；默认无惩罚（可配惩罚生效时按配置扣） | L177 |
| TC-28 | 玩家发送裸 `/任务`、`/接取 3`、`/交付 2` 与 `/委托 接取 3` | 裸指令全部路由到**玩家任务板**；委托板必须带板前缀（/委托 接取 3），裸指令不触碰委托板——双板仲裁成立 | L190-194 |

### ⑥ 校验器与边界

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-29 | 条件写成旧格式 `{type,var,op,value}` | 黄提示「旧格式，建议迁移」不拦加载；编辑器按互译表迁移为 `{var,op,value,param}` | L279 |
| TC-30 | 玩家 05:00 后第 1 次操作（昨日完成 3 个 / 接取 2 个） | 懒计算重置（D-05 无关，为板规则）：quest_daily 今日完成数/接取数清零可再刷；time 对齐签到/商店 | L182/L208-210 |
| TC-31 | 条件引用未注册事件 `[事件:future_patch]` 求值 | **求值失败 → 默认不满足**（D-03），不崩溃、不阻塞 /任务 打开；编辑器黄提示「事件未注册」 | L19/L277 |

---

## 附：系统衔接一览（对齐【任务】§六 全系统联动）

| 系统 | 衔接点 | 依据 |
|---|---|---|
| 变量体系 | 任务条件全用变量（新登记 4 类：累计获得 gain_count/累计击杀 kill_count/图鉴 codex/声望 reputation）——变量体系第一非战斗消费者；互译表主表落 NPC 4.3 | 【任务】L22/L255/L43 |
| 炼金委托板 | 委托任务 = deliver + filter（品质）+ bonus（倍率）；指令=/委托 独立板（§3.4 仲裁）；声望产出同源并入 reputation_state；collect 不消耗、收购走 deliver 出口闭环 | 【任务】L250 |
| 副本子任务 | 副本 subquests 引用 quest ID（zone 限定，不占板槽位） | 【任务】L251/L104 |
| 图鉴 | intel 任务：[图鉴完成度]（codex）条件 / 情报解锁奖励（落点 codex_state） | 【任务】L252 |
| 商店/签到 | 声望商店解锁（门槛读 reputation_state）/ 签到联动奖励；签到 rewards 与任务 reward 共用同一发放器 | 【任务】L253/L125 |
| 掉落 | 收集任务与掉落联动（[累计获得]→gain_count）；collect 计数不扣（不抢材料） | 【任务】L254 |
| NPC 定稿 | 统一条件引擎 {var,op,value,param}（4.0）/op 双写（4.1）/互译表主表（4.3）；NPC 差异化发任务（§3.2） | 【任务】L20/L37/L43/L143-151 |
| 时间引擎 | 每日 05:00 懒计算重置（与签到/商店同刻对齐）；timed 限时 deadline 现实时间懒计算 | 【任务】L182/L102/L349 |
| 存档（3.7） | longline_counters 主计数器 / quest_active / quest_daily 三表；reputation_state 声望落点 | 【任务】L201-210/L226 |
| 编辑器 | 变量面板结构化条件表单（非 JS）+ 统一 reward 条目表单 + 板配置 + unlock_chain 下拉 + 校验器 | 【任务】L259-268/L277-279 |

*细化依据：《任务系统设计定稿.md》v1.0.4 全篇（quest.json · 条件驱动版）+ NPC 定稿 v1.3（4.0/4.1/4.3 条件引擎与互译表，经转述）+ 变量体系 v1.2（别名映射）+ 设计总纲（只建议不限制）。所有字段、原语、规则、用例行号可逐条回查；对定稿只给语义未给字段名的属性和默认值，已在 §0.3 决策记录显式收敛登记（D-01~D-07）。*

*本契约统计口径：**顶层 17 字段 ＋ board 5 ＋ npc 3 ＋ timed 2 ＝ 27 叶子字段**；三原语 3（值型/累计型/事件型）；奖励条目 3 型；验收用例 **31**（TC-01 ~ TC-31）。*
