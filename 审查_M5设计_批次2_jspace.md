# 审查报告：M5 设计层·批次2（细化_5e_战斗战报格式 vs 前缀/数值层/模板规范/代码）· j-space 静态审查

> 审查日期：2026-08-27（会话内）· 审查方式：**纯静态审查**（本环境无 bash 沙箱，未运行任何命令/脚本/验证，全程仅用 read/grep/glob；所有运行行为结论一律标注【静态推导】）
> 审查对象（主）：`docs/细化/细化_5e_战斗战报格式.md`（read 计数 **430 行**；任务声明 429 行，差 1 行为末尾换行差异，不计问题）
> 权威定稿（仓库 `docs/审查参考/` 副本，行号以此为基准）：
> - `消息前缀功能设计定稿.md`（181 行，主依据【前缀】，全篇行号引用逐条核对）
> - `战斗数值层设计定稿.md`（383 行，主依据【数值层】，全篇行号引用逐条核对）
> - `细化_3d_消息模板规范.md`（348 行，横切规则消费方 D-01~D-05）
> 开发规则：`docs/审查参考/开发规则文档.md`（L509 状态行规则）
> 已实现渲染模块（静态读代码）：`qbot_rpg/core/message_format/battle_render.py`（50 行，M1 骨架）· `panel_render.py`（114 行）· `qbot_rpg/core/battle.py`（1841 行，引擎结算文案）
> 辅助证据：`审查_M5设计_批次1_jspace.md`（P2-3 开发规则 L509 状态行规则未覆盖——针对 3d）

---

## 〇、结论摘要

| 级别 | 数量 | 一句话 |
|---|---|---|
| **P0** | **0** | 无文档内部自相矛盾到阻断实现/测试级别的缺陷；本批未发现与已实现渲染直接冲突 |
| **P1** | **1** | 【数值层】行号引用**系统性 -2~-5 偏移**（含核心军规4/BREP 依据列/TC 矩阵/衔接表约 40 处，部分指向无关内容），文档头注「严格引用策划案定稿，引用到行号 Lxx」承诺失效 |
| **P2** | **9** | 开发规则 L509 状态行规则未覆盖；「幻觉审查_5e」编造来源；先手行降级示例跨文档不一致；16 行上限构成口径漂移；模板计数分解不自洽；「6 条军规 vs 五条军规」计数不一致；TC-11 MP 消耗区间自相矛盾；引擎内部文案与 BREP-02 句式不一致（观察项）；木桩分页引用 L340 语义错位 |
| 继承（不重复计分） | — | 3d 的「16 行折叠 vs 定稿分两条发送」关系未声明（批次1 P2-2）对 5e 同样适用（5e 只消费 3d D-03，未补声明），本批仅点明 |

**一句话总评**：5e 对【前缀】的全部行号引用（约 25 处）**逐条核对真实、精确、语义一致**，干净；对【数值层】的引用则**系统性整体偏移**（回合死亡判定顺序区块整体 -4，技能倍率/难度分层/MP 经济/木桩展示等区块 -2~-5，个别指向无关内容如 L164 空行/L230 luck.max_mod/L332 blocked 字段），这是本批最重大缺陷——语义承接全部正确（时序、内容、数值均对），但行号锚点集体失效，与文档自述的「严格引用到行号」纪律冲突。模板注册表 25 条编号连续、TC 矩阵 27 用例计数自洽；5 条 ADR（D-5A~E）全部显式标（补白），无冒充定稿/裁决。

---

## 一、维度① 缺漏——「定稿要求 X 但细化无 Y」清单

### 1.1 P2-1 开发规则 L509 战斗状态行规则未覆盖（与批次1 P2-3 同型）

- **定稿要求 X**：开发规则文档 L509「战斗状态行：默认显示前 5 个状态，超出追加『还有 N 个状态』（防一条消息塞满）」。
- **细化有 Y**：5e 有 BREP-08（状态与资源行差分行）、D-5D（状态行三轴可选展示）、BREP-01（末行操作提示/状态行），但 grep 5e 全文「前 5 个状态 / 还有 N 个状态 / 状态数」→ **零匹配**（仅 TC-26 的「前 5 条来源条目」指木桩明细分页，非状态行）。
- **判定**：L509 的「状态数默认前 5 + 还有 N 个状态」是与「5 条/页」「16 行上限」同级的防刷屏横切规则，批次1 已判 3d 未覆盖（P2-3），5e 作为战斗战报契约**同样未覆盖**。5e 的 BREP-08/D-5D 管理的是「资源轴差分/全量展示」，与 L509 的「状态条目数量上限」是两回事，不可相互替代。
- **修复建议**：在 §1.3 长度控制或 BREP-08 处增补一行「战斗状态行：状态数默认 ≤5，超出追加『还有 N 个状态』」，引用开发规则 L509（或对齐 4f RUL-13 L155/L338）。

### 1.2 【前缀】L25-31 验收示例逐字对齐——基本对齐，先手行有非降级改写（见维度② 2.2）

### 1.3 【数值层】L40-68 回合死亡判定顺序渲染承接——语义完整承接 ✓（行号偏移见维度② 2.1）

### 1.4 §八 伤害构成统计木桩明细——内容完整覆盖 ✓（行号偏移见维度② 2.1）

- BREP-25 + §6.3 + TC-26/27 覆盖：一行摘要 + 折叠明细 + 5 条/页分页 + 占比降序 + 普通战斗默认不展示，与数值层 8.2（L347-350）逐项对应。内容覆盖完整 ✓。行号锚点偏移 ✗（见 2.1）。

### 1.5 全覆盖确认（无问题）

- **判定顺序 L14-17**：5e L61/L139 引用真实命中（L14 标题/L15 围栏/L16 顺序/L17 围栏）✓
- **单回合单条 / 前缀只加首行 / 前缀不计入长度**：5e 军规1/3 对【前缀】L17/L34/L82/L170/L97-99/L176 引用全部真实 ✓
- **技能倍率预算 / BOSS 40-60 回合 / 多资源轴 / 打断读招**：语义承接完整（内容正确），仅行号偏移（见 2.1）✓（语义层）
- **TC 覆盖矩阵 27 用例自洽**：①6+②5+③4+④5+⑤3+⑥4=27 ✓，TC-01~27 连续 ✓

---

## 二、维度② 错误

### 2.1 P1-1 【数值层】行号引用系统性偏移（本批核心缺陷）

5e 对【数值层】的行号引用**整体比实际文件小 2~5 行**（回合死亡判定顺序区块整体 -4），并有个别指向无关内容。文档头注 L4-6 承诺「严格引用策划案定稿，引用到行号 Lxx/段落号」，实际锚点大面积失效。代表性子集：

| 5e 声称 | 实际（数值层文件） | 判定 |
|---|---|---|
| 军规4 L74：回合开始 dot（L45） | ① 回合开始 = **L49** | -4 偏移 |
| 军规4：回合开始即死（L46-48） | ② 死亡判定 = **L50-52** | -4 偏移 |
| 军规4：先手行动（L49） | ③ 行动阶段先手（含连段）= **L53** | -4 偏移 |
| 军规4：击杀判定（L50-53） | ④ 击杀判定 = **L54-57** | -4 偏移 |
| 军规4：后手行动（L54-57） | ⑤ 后手行动判定 = **L58-61** | -4 偏移 |
| 军规4：回合结束 tick（L58） | ⑥ 回合结束 tick = **L62** | -4 偏移 |
| 军规4：互杀判定（L59-63） | ⑦ 同回合互杀 = **L63-67** | -4 偏移 |
| 军规4：战斗结束结算（L64-65） | ⑧ 战斗结束判定时机 = **L68-69** | -4 偏移 |
| 头注 L5：回合死亡判定顺序 L40-68 | 实际区块 = **L44 标题 / L49-71 内容** | 整体 -4 |
| 头注 L5：连段多段 L49-53 / 击杀判定 L50-53 | 连段多段=L53；击杀判定=L54-57 | 偏移/错位 |
| BREP-03 L94：miss 依据 L21-22 | miss → 伤害 0 = **L24**（L21-22 是「### 公式总览」+围栏） | -2 偏移 |
| BREP-07 L98：技能 MP 随倍率 L164 | 技能 MP 随倍率 = **L168**；L164 为空行 | -4 偏移，指向空行 |
| BREP-08 L99：多资源轴 L147 | 多资源轴管理 = **L151**（L147 是 BOSS 战 40-60 回合） | -4 偏移 |
| BREP-19 L110：互杀 L59 | ⑦ 互杀 = **L63**（L59 是「目标未死→正常执行」） | -4 偏移 |
| BREP-20 L111：掉落经济 L64/L167 | 掉落经济 = **L171**（L167 是药剂回复） | -4 偏移 |
| BREP-22 L113：派生封顶 L129/L230 | 派生累计 ≤1.5× = **L133**；L230 是 luck.max_mod | 错位 |
| BREP-25 L116：木桩 L339-343（L340 分页） | 8.2 展示=**L344-351**；L340 是按来源聚合，**分页规则在 L348** | 内容错位 |
| 五章 L278：每段独立取整 L128 | 多段每段独立取整 = **L132**（L128 空行） | -4 偏移 |
| 六章 L325：回合数基准 L135-137/L143 | 3-8/10-15/40-60 = **L139/L140/L141** | -4 偏移 |
| §6.3 L331：占比降序 L332/L339 | 按来源聚合占比排序 = **L340**；L332 是 blocked 字段 | 指向无关内容 |
| 衔接表 L428：快照 L64-67 | 快照只落回合边界 = **L70-71**（L64-67 是互杀判定基准） | 错位 |

**影响面**：军规4（渲染时序锚点）、§1.1 追溯总览（L61-67）、BREP-01~25 注册表依据列（L92-116）、§2/§3/§4/§5/§6 正文引用、TC 覆盖矩阵 27 条定稿依据列、附衔接表——合计约 **40 处**同型偏移。其中语义全部正确（时序、数值、机制承接无误），故不构成内容性错误；但任何按行号回溯定稿的审校/实现/测试都会扑空。

**根因推测【静态推导】**：数值层 L40（取整策略）+ L42（怪物防御率配置化 · R-09 回写，拍板 2026-08-18 与 5e 同日）为后插内容，将回合死亡判定顺序区块从 L40-68 下推至 L44-72；5e 应基于旧版数值层写作，未随仓库版本同步。L164/L128/L332/L230 等指向空行/无关字段的引用则不完全是版本漂移，需逐一人工重核。

- **修复建议**：按「声称 vs 实际」对照表全篇重核约 40 处【数值层】行号（建议以本报告 2.1 表为起点），并注明版本对齐声明（5e 头注补「数值层 v1.1.1 + R-09 回写后行号」）。L164→L168、L332→L340、L230→L133 等需重点改。

### 2.2 P2-2 先手行降级示例非降级改写（跨文档不一致，见维度④ 4.1）

- 【前缀】L29 原示例：`🔥 你施放火球术！造成 18 伤害（史莱姆 7/25）`
- 3d 的 D-01 降级口径（3d L206）：`✅ 你施放火球术，造成 18 伤害（史莱姆 7/25）`（去 🔥、去「！」）
- 5e 的 §1.5 骨架示例（L126）与 TC-07（L373）期望输出：`✅ 你施放火球术攻击史莱姆，造成 18 伤害（史莱姆 7/25）`——**新增「攻击史莱姆」且将「！」改为「，」**，超出 D-01 降级范围（D-01 只授权 emoji→✅/❌，未授权增删文本）。5e BREP-02 模板本身含 `{目标}` 占位符（`你{攻击动作}{目标}`），代入即产生「攻击史莱姆」——是模板设计语义，但 5e 在 §1.5 将其标注为「【前缀】L29 降级」，与 3d 对同一示例的降级口径冲突。
- **修复建议**：统一 5e 与 3d 的降级口径——要么 5e 骨架示例改回 `✅ 你施放火球术，造成 18 伤害`（BREP-02 模板允许 `{目标}` 可选），要么在 ADR 注明「BREP-02 模板化转写含目标补全」而非仅称「降级」。

### 2.3 25 条模板注册表内部自洽性

- **编号**：BREP-01~25 连续、无跳号、无重号 ✓；§1.4 表头「全表 25 条」与 L118「合计 25 条」一致 ✓
- **P2-3 计数/归类不自洽（L118）**：`模板数合计：25 条（骨架 1 + 玩家行动 7 + 怪物行动 5 + 结算 6 + 连段 2 + 开始/结束/明细 4）`——实际 BREP-23/24/25（开始/结束/明细）只有 **3 条**，**BREP-09 操作提示行**在分解中无家可归。正确应为「骨架1 + 玩家行动8（含操作提示行09） + 怪物5 + 结算6 + 连段2 + 开始/结束/明细3 = 25」，或「…+操作提示1+开始/结束/明细3」。当前「玩家行动 7」与「开始/结束/明细 4」错配。
- **P2-4 军规计数不一致**：L30「25 条模板注册表 + **6 条总则军规**」vs §1.2 L69「**五条军规**」——0.2 列 6 条（含每段伤害一行、超长折叠），1.2 列 5 条（合并口径），计数口径不统一。

### 2.4 TC-11 P2-5 MP 消耗区间自相矛盾（文档内部）

- TC-11（L377）：场景「施放治疗术（**MP 30→15**，HP 0→30）」= MP 消耗 **15**，断言却写「MP 消耗落在小技区间 **5-10**/ml 语义一致」——消耗 15 属于大招区间（15-25），与断言矛盾。
- 同源问题：§2.3 示例（L170）`（MP 15/60）` 若从 30 扣到 15 = 消耗 15，落在 15-25（大招），但 §2.3 声称「小技 5-10」；而 BREP-07 示例（L98）`（MP 22/60）` 消耗 8 落在 5-10 ✓（且注明「幻觉审查_5e 修正原 15」）。
- **修复建议**：统一治疗术示例为消耗 8（MP 30→22）或 5-10 区间内数值；TC-11 改「MP 30→22」；「/ml」疑为笔误，删去。

### 2.5 其余行号引用抽查（无问题）

5e 对【前缀】全部引用逐条核对真实且语义一致：L22/L43（默认格式）、L34/L82/L170（前缀首行/只加合并首行/验收）、L17/L97-99/L176（不计入长度）、L48/L62/L100（prefix_max_len 截断）、L35/L46/L90-92（空称号三态）、L80/L83/L152（仅玩家回复/不影响解析/渲染层登记）、L28-31/L29/L30/L31（验收示例）、L41-49/L56-62（settings 段）、§四 L64-76（占位符）。零偏移 ✓。对【模板规范】引用 D-01~D-05（L42-46）/L171/L201/L202-209/L194-196/L196/L134/L81/L82 全部真实 ✓。

---

## 三、维度③ 幻觉——「声称 vs 实际 grep 结果」对照

### 3.1 P2-6 「幻觉审查_5e」——声称存在但 grep 零匹配

| 声称 | 实际 grep 结果 |
|---|---|
| BREP-07 说明（L98）：「（MP 消耗 8，对齐小技 5-10 区间；**幻觉审查_5e 修正原 15** 与区间矛盾）」 | grep 全仓库「幻觉审查_5e」→ **零匹配**（仅见 `幻觉审查_5b.md`、`幻觉审查_5c.md`、5a2 提及 `幻觉审查_5a2`；**无 5e 的幻觉审查档案**）。`docs/审查/` 下无 5e 幻觉审查文档 |

- 语义上「MP 消耗 8 对齐小技 5-10」本身正确（数值层 L168），且 BREP-07 示例已用 8（L98），修正方向对；但**引用一个不存在的审查档案作为权威修正来源 = 编造来源**。若确有对细化_5e 的幻觉审查但未落盘，应改为「本契约自校」或引用实际审查文档；若无，删除该表述。
- **修复建议**：删「幻觉审查_5e 修正原 15」或改为「经数值层 L168 对齐小技区间自校」。

### 3.2 补白标注纪律确认（无问题）

- D-5A~D-5E 五条 ADR（L41-47）全部显式标（补白）/【工程补白】，理由均指向定稿**真实空白**：BREP 编号消费 3d D-05 ✓、排版符号豁免引用 3d L208 ✓、每段伤害一行引用数值层 8.1 ✓（内容真实，仅行号 L128/L314/L319 中 L128 偏移）、状态行三轴引用多资源轴 ✓（行号 L147 偏移至 L151）、意图预告句式 ✓（行号 L284-285 偏移）。**未发现把工程补白写成「定稿已定义/裁决已授权」的冒充** ✓（5e 未声称任何裁决/拍板/决议，grep「裁决|拍板|决议|仲裁」→ 零匹配，无编造仲裁权威）。
- **注意**：ADR 依据行号同样受 2.1 偏移影响（D-5D 引 L147、D-5E 引 L149/L284-285），需随全篇重核。

### 3.3 把行为写成定稿原文

- 军规4 将渲染行序表述为「=【数值层】L40-68 回合死亡判定顺序的执行序」——语义是承接判定顺序（合理），行号偏移（2.1），未改写定稿语义本身 ✓。

---

## 四、维度④ 跨文档一致性（5e vs 3d D-01~D-05 消费）

| 横切规则 | 3d 口径 | 5e 消费 | 判定 |
|---|---|---|---|
| D-01 emoji 降级只留 ✅❌ | ✅（成功）/❌（失败）两标记；禁用清单 L201；降级示例 L202-209 | 军规2 同口径；禁用清单引用 L201 ✓ | **一致**（但降级示例文本见 P2-2 不一致） |
| D-02 列表分页 5 条/页 | 每页最多 5 条；页脚 TPL-08（L134/L81） | §1.3/L84 复用 5 条/页 + 页脚 TPL-08；BREP-25 明细分页 | **一致**（行号 L340 偏移见 2.1） |
| D-03 折叠 16 行上限 | 「一条消息总渲染行数上限（**含前缀/正文/页脚**）默认 16 行」（3d L171） | 「一条消息总渲染行数超上限（默认 16 行，**含前缀/正文/折叠行**）」（5e L83） | **同源但构成口径漂移**（P2-7：3d 含「页脚」、5e 含「折叠行」——页脚 TPL-08 与折叠行 TPL-09 是两物；16 行上限数字同源，建议 5e 对齐「含页脚」或注明差异） |
| D-05 模板编号 | TPL-xx（14 条） | BREP-xx（25 条），D-5A 明确消费 D-05；页脚/折叠行引用 3d TPL-08/TPL-09 | **一致**（BREP 为战斗侧独立注册表，与 TPL 横切模板并存，无冲突） |
| 16 行折叠 vs 定稿「分两条发送」关系 | 3d 已由批次1 P2-2 要求补声明（未补） | 5e 只消费 3d D-03，未补声明 | **继承缺口**（不重复计分，点明） |

- **结论**：D-01/D-02/D-05 消费一致；D-03 的 16 行上限同源但「含页脚 vs 含折叠行」构成口径漂移（P2-7）；先手行降级示例文本冲突（P2-2，见维度②）。

---

## 五、维度⑤ 与已实现战斗渲染衔接（静态读代码）

### 5.1 battle_render.py（50 行，M1 骨架）——无冲突，待实装【静态推导】

- 3 个函数 `render_battle_start/render_battle_round/render_battle_end` **全部 `raise NotImplementedError`**（M1 骨架签名），docstring 明确引用「细化_5e_战斗战报格式（含检测状态行）」+「细化_3d §1.5/§3.1（前缀首行、一轮战斗仍 1 条消息）」。函数签名（开始/每轮/结束）与 5e BREP-23 / BREP-01-22 / BREP-24-25 三段结构对应 ✓，无冲突。待 M1 实装时按 5e 模板落地即可。

### 5.2 panel_render.py（114 行）——与 5e 无冲突【静态推导】

- `render_panel`（状态面板：前缀首行+职业/等级/生命/魔法/货币+九属性简况）、`render_stats_line`、`paginate`（DEFAULT_PAGE_SIZE=5，页脚由渲染侧按 TPL-08 拼装）。与 5e BREP-25 木桩明细分页（5 条/页）经 3d D-02 同源一致 ✓。`render_prefix` 由 prefix_render 提供，与 5e 军规1 前缀首行口径一致 ✓。

### 5.3 P2-8 core/battle.py 引擎内部文案 vs BREP-02（观察项）【静态推导】

- `battle.py` L1497 `ActionOutcome.message = f"{attacker} 对 {target} 造成 {damage.get('final', 0)} 伤害"`——引擎内部字段，句式（`阿伟 对 史莱姆 造成 18 伤害`）与 5e BREP-02（`✅ 你施放火球术攻击史莱姆，造成 18 伤害`）**不一致**。当前 battle_render 未实装、未消费该字段，故**无直接冲突**；但若 M1 渲染器直接透传引擎 message，将输出与 5e BREP-02 不符的句式。**观察项**：5e 应注明「对外战报文案由 battle_render 按 BREP 模板生成，不直接复用引擎 ActionOutcome.message」。
- 其余 battle.py 结算逻辑（⑧ 战斗结束 L681、互杀 L706、后手不反击 L1615）与 5e 军规4/5 语义对齐 ✓【静态推导】。

---

## 六、无问题维度确认

- ✅ 【前缀】全篇约 25 处行号引用逐条核对通过（零偏移，见 2.5）。
- ✅ 【模板规范】D-01~D-05 全部真实，消费关系明确（BREP 为 D-05 战斗侧注册表）。
- ✅ 25 条模板编号连续自洽（01~25 无跳号/重号）；TC 覆盖矩阵 27 用例计数自洽。
- ✅ 5 条 ADR 全部显式标补白、理由指向真实定稿空白；无编造仲裁权威、无把补白冒充定稿。
- ✅ 语义承接完整：回合死亡判定顺序渲染时序（军规4）、木桩明细（BREP-25/§6.3）、连段鞭尸/BOSS 提前结束、互杀基准、结算一次性——内容全部正确。
- ✅ battle_render.py/panel_render.py 与 5e 无冲突（M1 骨架 + 通用分页工具）。
- ⚠️ 未检查项（诚实声明）：5e 头注提及的「技能派生定稿」「设计总纲」未读（非本次审查依据四件套）；战斗引擎对外文案的实际消费链路未跑（渲染器未实装，见 5.3 观察项）。

---

## 七、修复优先级建议

1. **P1-1（最高）**：按 2.1 对照表全篇重核约 40 处【数值层】行号（军规4 逐项改 L45→L49 等；L164→L168、L332→L340、L230→L133、L128→L132、L143→L147、L167→L171 等），并在头注补版本对齐声明。这是文档「严格引用到行号」承诺的修复。
2. **P2-2**：统一 5e 与 3d 的先手行降级口径（删「攻击史莱姆」或改注「模板化转写」）。
3. **P2-1**：补开发规则 L509 状态行「前 5 + 还有 N 个状态」规则（对齐批次1 P2-3 的 3d 修复）。
4. **其余 P2**：删「幻觉审查_5e」编造来源（3.1）；修正模板计数分解（2.3）；统一「6 条 vs 五条」军规计数（2.3）；修 TC-11 MP 区间自相矛盾（2.4）；3d/5e 16 行上限「页脚 vs 折叠行」口径对齐（4.1）；补 16 行折叠 vs 分两条关系声明（继承）；注明引擎 message 不直接对外（5.3）。

---

*本文档由 j-space 全档（full 档：broadcast + introspection + directed-focus）产出；hub = {【前缀】181 行全量行号映射、【数值层】383 行全量行号映射、【模板规范】348 行 D-01~D-05、开发规则 L509、三代码文件、批次1 报告}；全部行号均来自本会话实际 read/grep，未运行任何命令（无 bash 沙箱）；运行行为结论均标【静态推导】。*
