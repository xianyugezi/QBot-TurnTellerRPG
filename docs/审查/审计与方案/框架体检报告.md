# 框架体检报告（「万物皆插件」+「如无必要，勿增实体」· 4 路审计汇总）

> 审计源（各含 file:line 与证据，均在 `/root/deliverables/`）：
> `审计1_万物皆插件_核心引擎.md`（9 条 + 5 组唯一源问题）· `审计2_万物皆插件_编辑器与指令.md`（29 条）·
> `审计3_勿增实体_重复机制.md`（真重复 8 条 + 合理并存清单）· `审计4_勿增实体_死实体与空转.md`（无消费者登记 33 条 + 死代码 83 条）
> **主 agent 已逐路抽验 10 条关键断言**（含"消费点为空"这类硬断言），**全部成立** ✓ → 可信度高。

---

## 一、结论速览

| 维度 | 结论 |
|---|---|
| **编辑器合规度** | **很高** ✓ 字段/模块/分组/预设/枚举/关联/聚合/CSV **全部元数据驱动**；前端**零包名/零包内 id/零模块特例**；`commands/` 里 `if pack ==` **零命中** |
| **框架纯净度** | 仍有 **单包残留**（6 处高危）：云海遗产 + veinborn 专属 id + test_demo 专属面板 id + 写死状态表 |
| **简洁度** | **最大问题是"登记了不生效"**：**33 条**（其中 **4 条特效轴 + 3 条预设**指向它们）→ **对内容作者是欺骗** |
| **真重复** | 8 条（配置段 2 + 代码机制 5 + 死表示 1），其中最严重是**两段 forge 配置已漂移**（实证） |
| **死实体** | 死代码 83 条（含"导出但无调用"24）+ 死类/空视图 6 + 空转分支 3 |

**一句话**：**架构方向是对的（编辑器元数据驱动做得很干净），真正该动手的是"清账"——把登记了却不生效的、某包专属的、重复漂移的，逐条处理掉。**

---

## 二、必须修（P0，共 3 类）

### P0-A · 「登记了却不生效」= 对作者的欺骗（**33 条，最该优先**）
- **`damage_dealt_pct`（X01，标 P0）**：`data/gear_stats.py:267` 定义 → 进 `EFFECT_TO_COMBATANT`（`:465-468`）→ **框架预设 `weapon_dmg`（`content/effect_presets.py:127`）把它发给作者** → **但 `core/` 零读取**（主 agent 实测 grep = 0）⇒ **作者写"弱点增伤"伤害不变**
- **`status_duration_pct` / `status_duration_taken_pct`（X20）**：定义 `:328/:338`、预设（涤净之躯/缠身之甲）→ **`apply_status` 只读 chance/resist/stack_gain/stack_cap，不读时长**（`effects.py:527-534`）⇒ **抗控/延长状态是空承诺**
- **`forge.decompose_rate`**：批57 点名坑，**至今未消费**（真源是 `settings.alchemy.decompose_rate`，`core/gem_wallet.py:190`）⇒ **改 forge 段分解率无效**
- **修法三选一（逐条裁定，不许含糊）**：① **接线**（补消费点）② **删除**（若不打算做）③ **显式标注"未实现"**（若确实要预留，且**不得**由预设发给作者）

### P0-B · 包专属东西进了框架（6 处高危）
| # | 位置 | 问题 |
|---|---|---|
| 1 | `core/battle.py:333-339` | **6 个 veinborn 跃空状态 id 写死**，注释自认"内容侧新增须同步登记本表" → **包想加就得改框架** |
| 2 | `core/forge_sp.py:81-115` | **SP 面板 5 条目写死**；包声明第 6 项**既不生效也不显示** |
| 3 | `core/alchemy_settle.py:92/217` | **`quality_cap_10`（test_demo 专属）写死**用于炼金 → 包改 id 机制静默失效 |
| 4 | `content/validator.py:957-961` | **云海包两键白名单**（`cls`/`break_behavior`），且框架字段表未登记 → **两侧口径分裂** |
| 5 | `commands/basic_commands.py:1959/2423` | **veinborn 印记 id `sword_flow`** 写进框架判定（**两处重复**） |
| 6 | `commands/battle_launch_commands.py:431` | 文案残留 **veinborn NPC「驿站药婆」**，**模板 key 未注册** → 包无法覆盖 |
- **统一修法**：走**包声明驱动**（框架读声明；无声明则走通用兜底），第 4 条可**用 E 系扩展点**彻底移除（这正是当初建 E1/E2/E3 的目的）

### P0-C · 重复配置段已漂移（实证）
- **`forge.json["settings"]` ↔ `settings.json["forge"]`**：`test_demo` 两段**逐字节相同**、`veinborn` **已漂移**（前者缺 `carry_sec`/`set_piece_counts`）→ **同一件事两个源，必然漂移**（主 agent 实测确认）
- **修法**：定**唯一源**（建议 `settings.json`），另一段**废弃 + 校验器给黄提示**，并给迁移说明

---

## 三、建议修（P1）

1. **`core/upgrade.py:883 _rune_tier_of`**：**绕过符文阶唯一源**（`data/runes.rune_tier_of`）自写解析 + **硬编码 `1..3`** → 改为 import 唯一源；
2. **死代码 83 条**（含导出但无调用 24）：**分批清理**，优先"内部无调用且无动态引用"的；**导出符号要先确认插件/测试是否动态引用**；
3. **死类/空视图 6**（`Spawner`、`SpawnManager`(生产零注入)、`ai`/`hidden`/`env_event`/`log_card` 视图）：**要么接要么删**（"以后可能用"不算理由）；
4. **框架预设指向无消费轴 3 条**（`weapon_dmg`/`armor_dur_up`/`armor_dur_down`）：↑随 P0-A 一起处理；
5. **编辑器侧 29 条中的中低项**（内联 `FieldMeta(label/help)` 未下放等，文档已登记为二阶段）。

---

## 四、可接受 / 别乱删（**审计3 §6 专列，防后人误删**）
- 别名与兼容层（`cooldown_reduction_pct`→`cooldown_pct`、`immune_dmg`→`damage_taken_pct`、`heal_amp_pct`→`healing_done_pct`…）：**存在理由明确、且仍可被旧内容使用** → **保留**
- `temper`/`enhance` 分账、`affinity` 与 `effect_axes` 分层、`synthesis` 公用层：**有意分层，不是重复**
- 详见 `审计3 §6 合理并存清单`

---

## 五、修复批次建议（每批独立可验收）

| 批 | 内容 | 依赖 | 风险 |
|---|---|---|---|
| **70** | **「登记了不生效」清账（33 条）**：逐条裁定 接线/删除/标注未实现；**先清 4 条特效轴 + 3 条预设**（对作者伤害最大） | 无 | 低（多为数据/标注） |
| **71** | **包专属残留清理（6 处）**：改包声明驱动；validator 那条用 E 系扩展点移除 | 无 | 中（涉及引擎分支，需对拍） |
| **72** | **重复配置段收敛**：`forge` 单源 + 校验器黄提示 + 迁移说明；`_rune_tier_of` 改唯一源 | 无 | 中（涉及迁移） |
| **73** | **死代码清理（83 条分批）**：先清无动态引用的内部死代码 | 无 | 低-中（需确认动态引用） |

**每批硬纪律**：**零行为变化对拍**（不改玩法）+ 护栏测试 + 全量 0 failed + 工作树干净 + 批67 的 27 个护栏不许动。

---

## 六、给决策的一页清单（你挑）
1. **P0-A 的三选一策略**：对"登记了不生效"的 33 条，**我建议**：**4 条特效轴 → 接线**（它们是特效体系的核心卖点）· **`forge.decompose_rate` → 删除**（真源在 alchemy）· **其余逐条按"有没有玩法价值"裁定**（我给逐条建议表）
2. **P0-B 的 6 处**：全部改包声明驱动（云海那条用扩展点移除）——**要不要现在就做**
3. **P0-C 的 forge 单源**：选 `settings.json` 为准？（另一段废弃 + 黄提示）
4. **死代码 83 条**：**清 or 留**（我建议清"内部无调用"的，留"导出但可能被插件用"的并加注释）
5. 要不要我把这四批**排进执行序**、逐批派 dsh 做（照旧每批独立验收）
