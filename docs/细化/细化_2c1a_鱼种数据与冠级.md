# 细化 · 2c1a 鱼种数据与冠级（fishing.json schema · 六档判定 · 鱼王 · 图鉴 · 校验器）

> 依据（严格引用）：`/root/docs_archive/RPG框架项目/钓鱼玩法设计定稿.md`（下称【钓鱼】，v1.0.1 · 138 行）
> 对应任务：规划_路2c1_钓鱼.md T02（fishing.json 鱼种数据）/ T03（校验器）/ T11（冠级六档）/ T12（纯收藏）/ T13（codex 记录）/ T14（图鉴展示）/ T15（鱼王事件）
> 定位：**钓鱼 = 内容包玩法包，机制零新增纯配置**【钓鱼】L5；本细化只定义内容数据（fishing.json）的字段契约、冠级判定算法、鱼王接线、图鉴落库、校验器与验收用例，不含流程指令（见 2c1b 细化）与职业熟练度（见 2c1c 细化）
> 编号规则：字段表以 F-xx 编号；校验规则以 V1-V4（硬）/ W1（黄）编号；验收用例 TC-01 起连续编号

## 0. 一句话定位（可追溯）

| 维度 | 定稿要求 | 引用 |
|---|---|---|
| 内容数据独立文件 | 鱼种数据放 fishing.json（内容数据），不在 profession.json | 【钓鱼】L81 |
| 字段语义 | 大小/重量区间/稀有度/时段/钓点/饵偏好/图鉴文案 | L81 / L104 |
| 冠级生成 | size% / weight% 独立均匀生成，数值 = 区间百分位线性插值 | L29-30 |
| 六档判定 | 阈值 reverse=5 / silver=85 / gold=95 可配，判定顺序写死 | L32-43 / L75 |
| 纯收藏 | 冠级只作图鉴记录与社交展示，不进任何数值 | L46-48 / L132 |
| 鱼王事件 | 金闪鱼讯 → 鱼王 BOSS 战（enemies 引用），30%×日窗口 2 | L21 / L79 / L134 |
| 图鉴落库 | codex_state 鱼条目扩展：计数/历史最佳/最小/逆金冠 | L49-58 |
| 校验器 | 4 项硬校验 + simple 黄提示 | L62-67 / L122 |

---

## 一、fishing.json 鱼种字段级 schema

> 依据：【钓鱼】§三 L81-88、§五 鱼种池 CSV 列 L104、§七 登记要求·fishing.json L123；稀有度枚举对齐 §四 70/25/5 L94。
> 结构：`fishing.json` 顶层含 `species`（鱼种数组）与可选 `king`（鱼王表，见 §三）。每条鱼一对象，键名英文小写。

### 1.0 文档级字段（fishing.json 顶层）

| # | 字段 | 类型 | 必填 | 语义 | 引用 |
|---|---|---|---|---|---|
| D-01 | `schema_version` | string | 是 | 数据版本号，如 `"1.0"`；加载器校验 | —（对齐框架配置规范） |
| D-02 | `species` | array<Fish> | 是 | 鱼种池，≥1 条；全空则校验器 V1 硬错（防空池） | 【钓鱼】L81 |
| D-03 | `king` | array<KingEvent> | 否 | 鱼王讨伐表（enemies 引用），空=无鱼王；仅 full 模式生效 | L21 / L79 |

### 1.1 鱼种行字段（Fish）

| # | 字段 | 类型 | 必填 | 默认 | 语义 | 引用 |
|---|---|---|---|---|---|---|
| F-01 | `id` | string | 是 | — | 鱼种唯一键，英文小写蛇形（如 `silver_carp`）；全文件唯一定稿示例 | 【钓鱼】L84 |
| F-02 | `name` | string | 是 | — | 中文名（如 `银鳞鲤`） | L84 |
| F-03 | `rarity` | enum | 是 | `normal` | 基础稀有度：`normal` / `rare` / `gold`（对齐 70/25/5 体系枚举，满配 54/37/9 由收杆环节换算） | L84 / L94 |
| F-04 | `size_min` | float | 是 | — | 大小下限（cm），百分位 0% 处取值 | L30 / L105 |
| F-05 | `size_max` | float | 是 | — | 大小上限（cm），百分位 100% 处取值；须 ≥ size_min（V1） | L30 / L105 |
| F-06 | `weight_min` | float | 是 | — | 重量下限（kg），百分位 0% 处取值 | L30 / L105 |
| F-07 | `weight_max` | float | 是 | — | 重量上限（kg），百分位 100% 处取值；须 ≥ weight_min（V1） | L30 / L105 |
| F-08 | `seasons` | array<enum> | 否 | `[]` | 季节偏好：`spring`/`summer`/`autumn`/`winter`；空数组=全年不限 | L85 / L123 |
| F-09 | `periods` | array<enum> | 否 | `[]` | 五时段偏好：`dawn`/`noon`/`dusk`/`night`/`midnight`；空数组=全天不限 | L86 / L123 |
| F-10 | `hours` | array<string> | 否 | `["00:00-24:00"]` | 现实钟点（24h 区间，可按逗号多段）；3.6 语义保留兼容 | 【钓鱼】L87 |
| F-11 | `spots` | array<string> | 是 | — | 钓点 id 列表，引用 maps.json 采集点（时段+稀有度标记素材取该点）；至少 1 个 | 【钓鱼】L15 / L81 |
| F-12 | `preferred_bait` | array<string> | 否 | `[]` | 对口饵 id 列表，引用 profession.json `fishing.bait_ids`（炼金 recipe）；对口饵稀有 +8% / 金 +2% | L16 / L66 / L74 |
| F-13 | `codex_text` | object | 否 | — | 图鉴文案（含大小/重量单位 cm/kg 语义） | L60 / L81 |
| F-14 | `king` | object \| null | 否 | `null` | 该鱼对应的鱼王讨伐配置（见 §三 表 K-xx）；null=无鱼王；simple/off 模式引用触发 W1 黄提示 | L21 / L79 / L67 |

### 1.2 图鉴文案对象（F-13 `codex_text`）

| # | 字段 | 类型 | 必填 | 语义 |
|---|---|---|---|---|
| C-01 | `desc` | string | 是 | 图鉴描述文案（叙述性，不含判定公式） |
| C-02 | `unit` | enum | 是 | 单位制：`cm-kg`（大小 cm / 重量 kg）；首版固定该值，预留扩展 |
| C-03 | `best_mask` | string | 是 | 展示模板，示例：`{name} Lv{lv} · 最大 {best_size}cm/{best_weight}kg · {best_crown} · 逆金冠×{reverse_crown_count}`，占位符见 §四 |

### 1.3 示例鱼种行（单测夹具基准）

> 依据：【钓鱼】L84-88。以下为 T02 单测夹具内置示例行，同时是 CSV 双向导出的基准。

```json
{
  "id": "silver_carp",
  "name": "银鳞鲤",
  "rarity": "normal",
  "size_min": 10.0, "size_max": 60.0,
  "weight_min": 0.3, "weight_max": 5.0,
  "seasons": ["spring", "summer", "autumn"],
  "periods": ["dawn", "noon", "dusk"],
  "hours": ["00:00-24:00"],
  "spots": ["map_laketown:pier_01"],
  "preferred_bait": ["饵_蚯蚓"],
  "codex_text": { "desc": "鳞片泛银光的鲤，黄昏时最活跃。", "unit": "cm-kg",
    "best_mask": "{name} · 最大 {best_size}cm/{best_weight}kg · {best_crown} · 逆金冠×{reverse_crown_count}" },
  "king": null
}
```

### 1.4 时段/季节匹配语义（消费方约定）

- `seasons`/`periods` 为**偏好白名单**：与当前世界周期（时间天气定稿 §4.1 语义）命中即候选；**空数组 = 不限制**，任何季节/时段都可 (【钓鱼】L85-86 / L123)。
- `hours` 为硬时钟约束：当前钟点不在任一流区间 → 该鱼种此轮不可出（保留 3.6 兼容，默认全天）。
- 命中集为空时由流程层（2c1b 细化）给空态文案，数据层不兜底。
- CSV 双向导入导出列固定为：`id/name/rarity/size_min/size_max/weight_min/weight_max/seasons/periods/hours/spots/preferred_bait/codex_text`（对齐【钓鱼】L104）。

---

## 二、冠级六档（阈值可配 · 判定顺序写死 · 线性插值 · 纯收藏）

> 依据：【钓鱼】§2.1 L27-30、§2.2 L32-44、§2.3 L46-48、§四 冠级概率 L95、§三 `crown_thresholds` L75。

### 2.1 百分位生成与数值插值

- 每次出鱼独立生成两个百分位：`size%`、`weight%` ∈ [0,100)，**均匀分布**（分布可配，v1 固定均匀）【钓鱼】L29。
- 具体数值 = 区间百分位**线性插值**【钓鱼】L30：

```
size   = size_min   + (size_max   - size_min)   × (size%   / 100)
weight = weight_min + (weight_max - weight_min) × (weight% / 100)
```

- 边界：`size%=0` → `size_min`；`size%=99.99…` → 趋近 `size_max`（百分位不封 100，见 T11 边界用例）。
- 阈值来源：`profession.json.fishing.crown_thresholds`（默认 `reverse=5 / silver=85 / gold=95`，可配）【钓鱼】L75；运行时一次读取，出鱼过程中不重读。

### 2.2 六档判定表（阈值参数化）

> 依据：【钓鱼】L32-41。括号内为默认阈值（reverse=5 / silver=85 / gold=95）展开式；概率基于均匀分布理论值【钓鱼】L95。

| 档位 | 判定（阈值参数化 r=reverse, s=silver, g=gold） | 概率（默认值，均匀分布） | 引用 |
|---|---|---|---|
| **逆金冠** | `size% < r 且 weight% < r`（两者都到最小极值区） | 0.25%（=0.05²） | 【钓鱼】L36 / L95 |
| **普通** | 其余（r~s 为主区间） | ~72.0% | L37 / L95 |
| **银冠** | `size% ≥ s 或 weight% ≥ s`（之一达银冠级） | ~17.0%（=2×0.10×0.85） | L38 / L95 |
| **大银冠** | `size% ≥ s 且 weight% ≥ s`（都达银冠级、未达金冠级） | ~1.0%（=0.10²） | L39 / L95 |
| **金冠** | `size% ≥ g 或 weight% ≥ g`（之一达金冠级） | ~9.5%（=1−0.95²−0.00025） | L40 / L95 |
| **大金冠** | `size% ≥ g 且 weight% ≥ g`（都达金冠级） | 0.25%（=0.05²） | L41 / L95 |

### 2.3 判定顺序（写死，不可配置）

```
逆金冠 → 大金冠 → 金冠 → 大银冠 → 银冠 → 普通
```

- 顺序语义（严格按序短路）【钓鱼】L43：
  1. 先判 **逆金冠**：仅在 `size%<r 且 weight%<r` 时成立——混合极端（如 size≥g 且 weight<r）**不**判逆金冠；
  2. 再判 **大金冠**（双 ≥g）→ **金冠**（单边 ≥g，含一边 ≥g 一边 <r 的混合极端按金冠显示）→ **大银冠**（双 ≥s 未达 g）→ **银冠**（单边 ≥s）→ 否则 **普通**。
- 范围线（用户原话，只作文档注释，不写进公式文本）：
  `最小 - 逆金冠 - 5% - 普通 - 85% - 银冠 - 95% - 金冠 - 最大`【钓鱼】L44。
- 边界语义：`size% == 5` → **非**逆金冠（阈值是严格小于 `<`）；`size% == 85` → 达银冠级（`≥`）；`size% == 95` → 达金冠级（`≥`）。见 TC-08/09 边界用例。

### 2.4 纯收藏约束（不参与任何数值）

- 冠级**只做图鉴记录与社交展示**，不影响鱼获价值 / 经验 / 出售价【钓鱼】L46-48；目的为防数值倾斜【钓鱼】L132。
- 实现约束：结算层（2c1b）计算奖励/经验时**不读取冠级字段**；差分测试保证同鱼种同大小重量不同冠级 ⇒ 价值/经验/售价完全一致（TC-12）。
- 大金冠/逆金冠全收集为炫耀物提示，不设硬门槛【钓鱼】L99。

---

## 三、鱼王事件（金闪鱼讯 → BOSS 战引用 enemies）

> 依据：【钓鱼】§1 M7 L21、§2.2 判定 L32（金闪属猛烈鱼讯档）、§三 `king_event` L79、§四 钓鱼王 L98、§八 L134。
> 完整鱼讯/等待/收杆流程属 2c1b 细化；本节只定义**数据接线与触发契约**。

### 3.1 鱼王表（fishing.json `king` 数组，KingEvent）

| # | 字段 | 类型 | 必填 | 默认 | 语义 | 引用 |
|---|---|---|---|---|---|---|
| K-01 | `id` | string | 是 | — | 鱼王事件键（唯一） | 【钓鱼】L79 |
| K-02 | `species_id` | string | 是 | — | 关联鱼种 `fishing.json.species[].id`；校验器检查存在（V4 关联） | L79 / L66 |
| K-03 | `enemy_id` | string | 是 | — | 鱼王 BOSS 引用 `enemies.json` 实体（复用怪物/BOSS 引擎） | L21 |
| K-04 | `hint` | string | 否 | `"金闪"` | 金闪标记文案：猛烈鱼讯携带"金闪"（鱼王预告） | L21 / 2c1b T07 |
| K-05 | `window_daily` | int | 否 | `2` | 每日讨伐窗口次数（超出当日不再触发） | 【钓鱼】L79 |
| K-06 | `chance` | float | 否 | `0.3` | 单次触发概率（30%） | L79 |
| K-07 | `enabled` | bool | 否 | `true` | 事件总开关 | L79 |

> 顶层 `crown_thresholds` 之外，`king_event` 段位于 profession.json `fishing` 段（T01 已在配置骨架落位）；本节表 K-05/06/07 为**引用**该段默认值并作为鱼王表行的可配覆写——两处之一为准（覆写优先级：鱼王表行 > profession.json）。

### 3.2 触发契约

- 触发链路：出鱼轮 → 判定命中某鱼种的 `king` 行且 `chance` 命中 → 该轮鱼讯升级为**猛烈 + 金闪** → 进入 BOSS 战（enemies 引擎）【钓鱼】L21 / L110。
- 触发控制：`enabled=false` 时不触发；当日已触发 `window_daily` 次后不再触发【钓鱼】L79 / L134。
- 讨伐胜利计 1 次鱼王事件次数，参与图鉴全亮（钓鱼王称号条件，含鱼王事件 2 次）【钓鱼】L98（归属 2c1c 细化）。
- **mode 约束**：`simple` / `off` 模式下鱼王路径不可达；配置期若 simple 模式存在 `king` 数据 → W1 黄提示不阻断【钓鱼】L67。

---

## 四、图鉴记录（codex_state 鱼条目扩展）

> 依据：【钓鱼】§2.3 L46-60、§七 登记要求·codex_state L120 / 图鉴 L126。

### 4.1 codex 条目字段（codex_state，type=fish）

| # | 字段 | 类型 | 初始 | 更新规则 | 引用 |
|---|---|---|---|---|---|
| G-01 | `caught_count` | int | 0 | 每次捕获 +1 | 【钓鱼】L52 |
| G-02 | `best_crown` | enum | — | 玩家获取过的最优标志中**最大**者：`big_gold` > `gold` > `big_silver` > `silver` > `normal`（顺序即 §2.3 标注序） | L53 / L59 |
| G-03 | `best_size` | float | 0 | 记录最大尺寸（cm）极值 | L54 |
| G-04 | `best_weight` | float | 0 | 记录最大重量（kg）极值 | L54 |
| G-05 | `min_size` | float | ∞语义 | 记录最小尺寸（cm）极值（逆金冠记录） | L55 |
| G-06 | `min_weight` | float | ∞语义 | 记录最小重量（kg）极值（逆金冠记录） | L55 |
| G-07 | `reverse_crown_count` | int | 0 | 获得过逆金冠次数，每次 +1 | L56 |

> 结构示例（与定稿 L50-58 一致）：

```json
"fish_carp": {
  "caught_count": 12,
  "best_crown": "gold",
  "best_size": 45.2, "best_weight": 3.8,
  "min_size": 12.1, "min_weight": 0.4,
  "reverse_crown_count": 1
}
```

### 4.2 更新规则（确定性）

- `best_crown`：比较优先级 `big_gold > gold > big_silver > silver > normal`；逆金冠**不计入** best_crown 优先级链，单独走 `reverse_crown_count`（逆金冠是极值收藏不是等级）【钓鱼】L53 / L59。
- `best_size/best_weight/min_size/min_weight`：按每次出鱼的实际数值与存量极值取 max / min 更新【钓鱼】L54-55。
- `reverse_crown_count`：每次逆金冠出鱼 +1，并刷新 min 记录【钓鱼】L56。
- 未捕获过的鱼种无条目；首获即创建条目并点亮图鉴【钓鱼】L125。

### 4.3 图鉴展示（type=fish）

- 查看鱼时标注：最优冠级（best_crown）+ **逆金冠单独标注** `逆金冠×N`【钓鱼】L59。
- 示例格式：`鲤鱼 Lv3 · 最大 45.2cm/3.8kg · 金冠 · 逆金冠×1`【钓鱼】L60。
- 冠级规则由图鉴标注自然揭示，**不写判定公式、不教攻略**【钓鱼】L113。

---

## 五、校验器规则

> 依据：【钓鱼】§2.4 L62-67、§七 登记要求·校验器 L122、规划 T03。硬校验在配置加载期执行并记录日志/启动报告，定位到具体字段。

| # | 级别 | 规则 | 失败行为 | 引用 |
|---|---|---|---|---|
| V1 | **硬** | 鱼种区间：`size_min ≤ size_max` 且 `weight_min ≤ weight_max`（逐鱼，定位字段） | 加载失败，报字段路径 | 【钓鱼】L64 |
| V2 | **硬** | 冠级阈值合法：`0 < reverse < silver < gold < 100`（即默认 5/85/95 满足，可配但须满足序） | 加载失败 | L65 |
| V3 | **硬** | 定向饵引用鱼种存在（双向）：`preferred_bait[]` 每个 bait_id 存在于 `fishing.bait_ids`；任一"定向饵"recipe 的 `fish_target` 指向的鱼种存在于 fishing.json | 加载失败，报缺失 id | L66 |
| V4 | **硬** | `mode` 为三态枚举 `full`/`simple`/`off`；非枚举值硬错（而非静默） | 加载失败 | L67 / 规划 T01 |
| V5 | **硬** | 鱼种 `id` 全局唯一（含 `king[].species_id` 解析） | 加载失败 | L84（id 唯一语义） |
| V6 | **硬** | `seasons`/`periods`/`hours`/`rarity` 枚举成员合法；`spots` 引用 maps.json 采集点存在且非空 | 加载失败 | L85-87 |
| W1 | **黄** | `simple`（及 `off`）模式下存在 fish `king` / king 表 / 其他 full 专属字段引用 → 提示"simple 模式不生效"，不阻断加载 | 提示并记录日志 | L67（附 K K.2） |

> V-W 关系说明：【钓鱼】§2.4 仅列 4 行硬校验（区间/阈值/饵引用/mode，即本文 V1-V4）+ simple 黄提示（W1）；V5/V6 为 id 唯一性与枚举/引用完整性，属同节"校验"语义的最小必要扩展，文档中显式标注为扩展以免与定稿 4 行混淆（对齐规划 T03"4 项硬校验各有独立失败用例"——T03 验收只强制 V1-V4 各自独立失败用例）。

---

## 六、验收测试用例

> 编号 TC-01 起连续；断言均自动化可判定。覆盖矩阵：A 鱼种数据 4 例、B 冠级判定 5 例、C 纯收藏 2 例、D 鱼王 3 例、E 图鉴记录 3 例、F 校验器 5 例，共 **22** 例。

### 覆盖矩阵

| 需求类别 | 定稿章节 | 用例 | 条数 |
|---|---|---|---|
| A 鱼种数据 schema | §三 L81-88 / §五 L104 | TC-01 ~ TC-04 | 4 |
| B 冠级六档判定 | §2.1-2.2 L29-44 | TC-05 ~ TC-09 | 5 |
| C 冠级纯收藏 | §2.3 L46-48 / §八 L132 | TC-10 ~ TC-11 | 2 |
| D 鱼王事件 | §1 M7 L21 / §三 L79 / §八 L134 | TC-12 ~ TC-14 | 3 |
| E 图鉴记录 | §2.3 L49-60 | TC-15 ~ TC-17 | 3 |
| F 校验器 | §2.4 L64-67 | TC-18 ~ TC-22 | 5 |
| **合计** | — | TC-01 ~ TC-22 | **22** |

### A 鱼种数据

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-01 | 加载 `silver_carp 银鳞鲤` 示例行（L84-88 全字段） | 字段齐全可加载：id/name/rarity/size_min/size_max/weight_min/weight_max/seasons/periods/hours/spots/preferred_bait/codex_text 全部按 §1.3 落库 | 【钓鱼】L84-88 |
| TC-02 | 鱼种配 `seasons:[]`、`periods:[]`（空=不限） | 时段匹配层对任意季节/时段该鱼种均候选，不被过滤 | L85-86 / L123 |
| TC-03 | CSV 导出→导入（示例行） | 导出 13 列与导入后数据逐字段一致（双向无损）；`codex_text` 为单 JSON 单元格 | L104 |
| TC-04 | 鱼种配 `hours:["06:00-18:00"]`，当前 20:00 | 当前钟点不命中 → 该鱼此轮不可出；`00:00-24:00` 全天命中 | L87 |

### B 冠级六档判定

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-05 | 种子 42 / 2026 跑 N=100000 次六档判定（默认阈值 5/85/95） | 频率收敛：逆金冠≈0.25%、大金冠≈0.25%、金冠≈9.5%、大银冠≈1.0%、银冠≈17.0%、普通≈72.0%（±0.5pp） | 【钓鱼】L95 |
| TC-06 | 线性插值：`size_min=10, size_max=60`，size%=50 | `size = 10 + 50×0.5 = 35.0`；weight 同理按区间插值 | L30 |
| TC-07 | 边界 4.9% vs 5.0%（r=5）：size%<5 且 weight%<5 vs size%=5 | 4.9 组合判逆金冠；任一边 5.0 不判逆金冠（严格 `<`） | L36 / L43 |
| TC-08 | 边界 84.9% vs 85.0%（s=85）、94.9% vs 95.0%（g=95） | 84.9 不达银冠、85.0 达银冠；94.9 不达金冠、95.0 达金冠（`≥` 含边界） | L38-41 |
| TC-09 | 混合极端：size%=97（≥95）且 weight%=3（<5） | 判定顺序生效 → **金冠**（非逆金冠、非大金冠；逆金冠须双 <5） | L43 |
| TC-09b | 阈值改为 10/80/90（可配）：size%=88、weight%=92 | 按新阈值判：88≥80 且 92≥90 → 大金冠（阈值可配生效，非写死 5/85/95） | L75 |

### C 纯收藏约束

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-10 | 同鱼种同 size%同 weight%，仅最小属性红/金冠路径不同（构造不同冠级但同数值） | 结算引擎对两者给出**完全一致**的价值/经验/售价（差分=0）；冠级仅供展示 | L48 |
| TC-11 | 大金冠/逆金冠全收集提示 | 无任何硬性解锁门槛或数值加成，仅为图鉴炫耀物提示 | L99 |
| TC-11b | `energy.enabled=false` 下出鱼 | 无能量条参与出鱼计算（能量字段不影响冠级与收杆） | L78 |

### D 鱼王事件

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-12 | 鱼王命中：某鱼种 `king.chance=0.3`，种子 2026 抽样 | 长期触发率≈30%（±3pp）；触发瞬间鱼讯为猛烈+金闪 | L79 / L21 |
| TC-13 | 当日已生效触发 2 次再触发 | 第 3 次被 `window_daily=2` 拦截，不再进 BOSS 战 | L79 / L134 |
| TC-14 | `king.enemy_id` 解析 | 引用 enemies.json 实体成功进入 Boss/怪物引擎；`mode=simple` 时该路径不可达且加载期出 W1 黄提示 | L21 / L67 |

### E 图鉴记录

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-15 | 序贯捕获序列：普通→金冠→大金冠→银冠 | `best_crown` 依次升级且最终 = `big_gold`（大金冠>金冠>大银冠>银冠>普通 优先序） | L53 / L59 |
| TC-16 | 逆金冠出鱼 1 次 + 之后再捕获普通大个体 | `reverse_crown_count=1`；`min_size/min_weight` 刷新为历史最小；`best_crown` **不计逆金冠**（仍为后续较大者） | L55-56 / L59 |
| TC-17 | 展示格式：`{name} Lv3 · 最大 45.2cm/3.8kg · 金冠 · 逆金冠×1` | 按 `codex_text.best_mask` 输出逐字段一致；图鉴界面无判定公式文本（检索"94.9/85.0"类词汇为空） | L60 / L113 |

### F 校验器

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-18 | 构造 `size_min=60 > size_max=10`（及 weight 同理） | 加载失败，报错定位到具体鱼种字段（V1） | L64 |
| TC-19 | 构造阈值 `reverse=95, silver=85, gold=5`（乱序） | 加载失败（V2：须 0<r<s<g<100） | L65 |
| TC-20 | 构造 `preferred_bait:["饵_不存在"]`（及"定向饵 fish_target 指向不存在的鱼种"） | 加载失败，报缺失引用（V3 双向检查） | L66 |
| TC-21 | `mode:"bogus"` 非法枚举 | 加载**硬**失败而非静默 fallback（V4）；仅 full/simple/off 通过 | L67 |
| TC-22 | `mode:"simple"` 但表格含 `king` 数据 | V1-V4 仍通过，W1 黄提示"simple 不生效"且加载成功（不阻断） | L67 |
| TC-22b | `species` 空数组 / 鱼种 `id` 重复 / `spots` 引用缺失 | 加载失败（V5/V6 扩展校验） | L84 / L104 |

---

## 七、登记与对接（落点确认）

| 落点 | 内容 | 引用 |
|---|---|---|
| fishing.json | 本细化 §一 schema（鱼种行 seasons/periods 字段补登记时间天气定稿 §4.1） | 【钓鱼】L123 |
| codex_state | §四 鱼条目冠级记录扩展 | L120 |
| profession.json | `fishing.crown_thresholds` / `king_event` 段（T01 骨架复用，本细化引用） | L119 / L75-79 |
| 校验器 | §五 V1-V4 硬校验 + W1 黄提示（顶格 T03） | L122 |
| 图鉴 | codex `type=fish` 枚举 + 展示 | L126 |
| 编辑器 | 鱼种池 CSV 列（§1.4）+ 冠级阈值滑条 + 大小重量预览（走 §2.2 判定） | L105-106 |

---

*设计依据：钓鱼玩法设计定稿 v1.0.1（138 行，全篇已读）+ 规划_路2c1_钓鱼.md（19 原子任务）——本文对应 T02/T03/T11-T15 数据与判定部分；流程/收杆/熟练度见 2c1b/2c1c 细化*
