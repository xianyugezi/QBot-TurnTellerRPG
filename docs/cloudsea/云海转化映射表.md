# 云海猎团 → QBot 引擎逐项映射表（含源码锚点）

> 硬约束：战斗 100% 沿用现有引擎，零新增战斗模块；云海全部落新内容包 + 新指令文件。
> 源码锚点为 035c973（M13 收官）时的行号/接口，动手前先 grep 复核。

---

## 一、机制 → 现有引擎逐项映射

| # | 云海机制 | 映射为 | 源码锚点 | 动作类型 |
|---|---|---|---|---|
| 1 | 九层伤害公式 | 现有 10 乘区（damage.py） | qbot_rpg/core/damage.py（hit_rate L338 / crit_prob L267 / block_rate L369 / defense_factor L386 / pierce_pct L400 / elem_factor L424） | 纯口径（内容包 weakness/type_affinity 对齐） |
| 2 | 七属性 E | enemies.weakness + formula | content/test_demo/enemies.json `weakness` 字段 | 数据 |
| 3 | 部位易伤 H | 状态/effect 挂 damage_mult + pierce | qbot_rpg/core/effects.py（mitigation/dot 等 L0）；content/test_demo/effects.json | 数据 + 1 部位状态注册 |
| 4 | 异常九态积蓄条 | **marks 印记**（max_stack/duration/触发 effects） | content/test_demo/marks.json 结构；qbot_rpg/core/marks.py（MarksManager） | **数据**（9 异常 = 9 marks 条目） |
| 5 | 异常衰减/每场上限 | 挂 M13 resource_axis tick | qbot_rpg/core/resource_lifecycle.py tick_round_end L1836（battle 挂点） | ≈20 行衰减 hook |
| 6 | 双压力轴（压制/潮压） | **M13 resource_axis 两轴** | qbot_rpg/core/resource_axis.py；content/test_demo/stats.json（rage/element_energy 先例） | 数据（stats.json 注册 2 轴） |
| 7 | 14 职业专属资源 | **M13 job + resource_axis**（数值型轴） | qbot_rpg/content/job_models.py；resource_axis_models.py | 数据（jobs.json/stats.json） |
| 8 | 14 职业动作表/战技/绝技 | **M13 skills**（kind/power/hits/cooldown/mp_cost/effects + 派生链） | qbot_rpg/content/skill_models.py；qbot_rpg/core/combo.py（resolve_skill/should_reject/派生） | 数据（skills.json/skill_chains.json） |
| 9 | 狂战士形态值 | **已有 berserker 先例**（transform 引擎） | qbot_rpg/core/transform.py；content/test_demo/jobs.json berserker | 数据（直接抄先例） |
| 10 | 五流派/本命加成 | skills job_form/job_restrict + equipment 武器类型 | skill_models.py（job_form/revert_form 等挂点）；M13 批14 | 数据 |
| 11 | 意图预告 | 怪物 AI（M2）意图 + 战报意图行 | qbot_rpg/core/battle.py（monster AI decide）；ai 配置 | 数据 |
| 12 | 阶段状态机 | **dungeon_boss 多阶段先例**（换敌人实体） | qbot_rpg/core/dungeon_boss.py | 数据 + 少量接线 |
| 13 | 部件破坏/倒地窗口 | 部位状态清零 → effect 触发（倒地 = damage_mult ×1.5 状态） | effects.py 触发链；battle.py | ≈30 行部位状态注册 + 数据 |
| 14 | 瞬御押注 | guard + 条件技能（被拒机制） | combo.py should_reject；battle guard | 数据（技能配置） |
| 15 | 共生灵指令串 | effects proc/L2 容器 | qbot_rpg/core/effects.py execute_proc_action L1618；M13 season_events 同款 | 数据（proc 条目） |
| 16 | 同行兽被动 | passive 槽 + 战报图标 | M13 skill_slots passive/trigger；battle_render | ≈10 行 + 数据 |
| 17 | 托管/跨天 | session_mgr + battle snapshot（M13 快照续战） | qbot_rpg/core/battle.py to_snapshot/from_snapshot | 少量 |
| 18 | 四选一 | 技能位 basic/active + 战报选项行 | M13 skill_slots_battle.py（行动位过滤）；battle_commands | 数据 |
| 19 | 符文三层加工 | 复用 M9 forge 强化链（新 equipment 类目） | qbot_rpg/core/forge*.py；equipment.json | 数据 |
| 20 | 武器强化/套装/超限 | M9 锻造 + equipment | 同上 | 数据 |
| 21 | 浮空遗迹/素材本 | dungeon 配置（新 dungeon JSON） | qbot_rpg/core/dungeon.py | 数据 |
| 22 | 蚀潮巨兽窗 | 定时事件（cron/事件引擎） | qbot_rpg/core/environment_events.py 先例 | 数据 + 少量 |
| 23 | 丰饶符 | checkin 每日发放 + 掉落倍率配置 | qbot_rpg/core/checkin.py；settings.json | 数据 |
| 24 | 三币（金标/公会勋/航券） | 新货币字段（不与现有金币互扰） | 数据层 | 数据 + 少量 |
| 25 | 主线/任务/章节 | quest + maps（第 1 章 MVP） | content/test_demo/quest.json/maps.json | 数据（体力活） |

---

## 二、需要新写的代码清单（合计 ≈60–100 行 + 1 指令包）

| 文件（建议） | 内容 | 行数 |
|---|---|---|
| qbot_rpg/core/part_status.py（或并入 effects 注册） | 「部位状态」类型：独立 HP 桶、清零触发破坏事件/倒地窗口 | ≈30 |
| qbot_rpg/core/marks_decay.py（或挂 resource_lifecycle） | 异常积蓄衰减 tick + 每场上限饱和 | ≈20 |
| qbot_rpg/core/pet_passive.py（或并入 skill_slots） | 同行兽常驻 passive 挂接 | ≈10 |
| qbot_rpg/commands/cloudsea_commands.py | /接任务 /出发 /1-4 /局面 /剥取 /港 等新指令集（纯新增） | 新文件（400+ 行含模板） |
| qbot_rpg/core/templates/cloudsea_tpl.py | emoji 八段战报模板分区 | 新文件 |
| content/cloudsea/ | 全部数据表（manifest + jobs/skills/stats/marks/effects/enemies/quest/maps/equipment/…） | 纯数据 |

---

## 三、明确不做的事（约束承诺）

1. **不改** battle.py 回合制调度（不做 ATB）
2. **不改** damage.py 乘区结构（不做新乘区，只用现有 weakness/pierce/type_affinity 映射）
3. **不改** combo.py/effects.py/marks.py 既有行为
4. **不改** 现有 content 包（demo_lv15/test_demo 零改动）
5. **不改** 现有指令与白名单（云海指令词面与现有零重叠）
6. 云海新模块全部**可选加载**：不注册 manifest 即不存在

---

## 四、MVP 范围（对齐设计稿 10 章）

设计稿 MVP = 第 1 章「云海之下」（潮位一 ★1–3）闭环：
进群 1 条指令 → 3 步打第一场 → 一场 Boss（部件破坏 + 1 次倒地）→ 剥素材 → 回港强化一次 → 解锁下一浮岛

对应转译批次：
1. 14 职业数值卡 → jobs/skills/stats（可先 6 首发职业）
2. 首发巨兽（崩岭岩犀·灰岗等）+ 部位 marks + 九异常 marks
3. 武器/装备（首发 6 把）+ 符文（forge 复用）
4. 任务/地图（第 1 章 9 条任务）+ 教学序列
5. 新指令集 + 战报模板
6. 丰饶符/日常（checkin 复用）
7. verify_cloudsea 探针 + 全仓回归 + dsh 审查

---

## 五、九期 hook 台账（207 框架引擎小改包 · 2026-09-13 · 逐处锚点与行数登记）

> §三承诺逐条维持：回合调度/乘区结构/combo-effects-marks 既有行为/现有包/现有指令词面全部零改动——本批全部为**可选加载的 hook 面**（注册表空/键缺失 → 既有行为零变化）。锚点行号为 4ae3eaa 基础上本批落盘后 `grep -n` 复核值。

| # | 项 | 文件：锚点 | 新增行数（含注） | 说明 |
|---|---|---|---:|---|
| 1 | skill_or_any 并集惰性缓存 | content/validator.py：`_all_ref_ids_cache`（init ≈L382）／`_register_id` 失效（≈L444）／`_check_ref` 使用（≈L1966） | ≈10 | 性能修复①（12MB 包引用校验热路径） |
| 2 | skills_fields 模块级缓存 | content/skill_models.py：`_SKILLS_FIELDS_CACHE`＋`skills_fields()` 包装＋`_build_skills_fields()` 改名（≈L388） | ≈20 | 性能修复②（V-11 逐条目热路径） |
| 3 | snapshot 浅拷贝 | core/battle.py：`to_snapshot`（≈L3541 `copy.copy`） | ≈6 | 性能修复③（边界落快照＋即落 JSON 不变式） |
| 4 | boss_state 快照继承 | core/battle.py：`boss_state()` 视图（to_snapshot 前 ≈L3531）＋三键约定 | ≈14 | break_slots/stamina/ailment_buildup（211/214 写入） |
| 5 | phase_changed 继承 hook | core/monster_phases.py：`inherit_boss_state()`（文末） | ≈47 | stack_retain/control_mult 比例化携带；阈值 T 侧归 214 |
| 6 | condition 数组连乘＋权重 clamp | engine/condition_engine.py：`clamp_weight`＋`condition_weight_product`（文末，WEIGHT_MIN/MAX=1/100 同源 `$C.POOL.WEIGHT_MAX`） | ≈50 | AND 门控×权重连乘；消费 212 |
| 7 | 链长校验 | content/validator.py：`_check_chain_cycle` 前置（≈L2006，Y-8 chain_length_exceeded，上限 4 同源 `$C.POOL.CHAIN_MAX`） | ≈6 | 黄提示不红拦 |
| 8 | 押注判定钩子 TRIGGER_PROC_HOOK | core/battle.py：模块级 `TRIGGER_PROC_HOOKS`＋`register_trigger_proc_hook`（≈L67 后）＋`_dispatch_trigger_procs`＋start_turn `round_head` 派发（≈L1867） | ≈60 | 空表零行为变化；215/221 消费 |
| 9 | 错位印记 | **零引擎代码**——marks 数据驱动（MarksManager.evaluate 通用），印记条目归 210 content/cloudsea | 0 | 映射表 #4 marks 通道 |
| 10 | 段级 effect hook（G1B） | core/battle.py：段展开 per_segment_effects（≈L2191）＋结算循环段内 extend（≈L2917） | ≈22 | hits 逐段累加积蓄；opt-in 键 |
| 11 | period 周期器（G1B） | core/battle.py：`_tick_field_periods`＋tick_round_end 挂点调用（≈L3390） | ≈34 | field_periods→period_events；寒潮 3×1,000 类 |
| 12 | 冷却口径换算函数（G1B） | core/battle.py：`unified_cooldown_turns`（≈L68）＋冷却表写点接线（≈L2400） | ≈18 | 玩家 N/怪 N+1；raw=生成器已偏移 |
| 13 | **战术四件套接线（213）** | **新文件** core/cloudsea_tactics.py（199 行）＋tests/test_cloudsea_tactics.py（116 行，12 用例全绿） | ≈199（测试另计） | 态势 PostureMomentum＋反扑门控 proc／钉位 PinnedStreak／扇区 position_bonus（背击 +0.30 双向＋裁决 #10 三例外短路＋#20 灰度名单）／响应槽 ArmedQueue（FIFO+容量+回合衰减）——**battle.py/damage.py 零 diff**，全部经 #8 TRIGGER_PROC_HOOKS 注册面（`register_cloudsea_tactics` 显式装配口，不装配＝零行为）；共享数字由装配层注入（缺省系裁决草案值），零新 `12` 号键 |
| 14 | **战报八段终案（215）** | core/monster_intent.py：`build_intent` 扩 keyword-only 四字段 stage_shift/windup/target_ref/damage_tier（≈L59–64 签名＋≈L113–133 ext 并入，缺省 None 不入 dict＝既有形态零变化，adef 数据声明＞显式参数缺省）＋core/templates/battle_tpl.py：DEFAULT_TEMPLATES/PLACEHOLDER_WHITELIST 各追加五新 key（stage_shift_line/windup_unknown/windup_ready/merge_summary/mention_line，文末追加）＋**新文件** core/templates/cloudsea_emoji.py（54 行，D-01 emoji 白名单注册口＋校验器，未注册恒通过）＋**新文件** qbot_rpg_bridge/cloudsea_sender.py（127 行，30s 合并窗/摘要行/@白名单 4 类 boss_first·urgent·personal·system，纯调度零 IO）＋tests/test_cloudsea_215.py（10 用例；相关既有 235 passed 零破坏）＋**跨平台修复**：qbot_rpg_bridge/__init__.py（≈L138–163）与 plugin.py（≈L172–197）部署诊断日志 /tmp 写死 → /tmp 不可用回退 logs/、再不可用静默（Windows import 即炸既有 bug，日志落点语义不变） | ≈181 新文件＋≈35 扩展 | 红线核验：回合调度/乘区/combo-effects-marks/现有包/指令词面零 diff；215 渲染接线与 D-01 emoji 名单数据面归 216/210 消费 |
| — | **合计** | | **≈287＋199＋181＋35** | 207 主包 600–800 行预估含测试与后续 208–210 接线；实际落码 ≈287 行；213 新文件 199 行、215 新文件 181 行（模块 127＋emoji 54）与扩展 35 行另列（测试不计入纪律线）；「总量 ≤800 行」纪律线评估点＝245 收官批（排期三版变更已预注 G5A 0 hook 补偿） |

> 台账口径：九期框架侧累计行数随 211/212/213/215 批追加登记；「总量 ≤800 行」纪律线评估点＝245 收官批（排期三版变更已预注 G5A 0 hook 补偿）。

---

## 六、行动值→回合换算映射节（批次 209 · 2026-09-13）

> **换算律（九期裁决 #1＝方案 B）**：设计稿保留行动值叙述轴（`12` §B `ACTION_VALUE_FULL`=1,000，零改动），转译层机械换算——**N×1,000 行动值 ≡ N 回合；X 个出手序列 ≡ X 回合**。速度频率收益（出手比 1.3–1.6）不搬运，速度流职业数值重校授权归 211（裁决 #2）。本节为**纯映射文档**，零引擎改动（0 行 hook）。

### 6.1 四型→框架原语

| 设计稿表述型 | 语义 | 框架原语（QBot 通道） | 转译示例 |
|---|---|---|---|
| ① 周期触发 | 每 N×1,000 行动值触发一次 | `tick_round_end` N 回合计数器（同族＝207 #11 period 周期器 `field_periods→period_events`） | 「每 3×1,000 行动值寒潮爆发」→每 3 回合回合末触发 |
| ② 持续窗口 | 维持 N×1,000 行动值 | marks `duration: N`（turns 计） | 「充盈态持续 2×1,000」→marks turns: 2 |
| ③ 次数限制 | N×1,000 行动值内至多 X 次 | 局内 turn 计数窗（battle 回合计数） | 「4×1,000 内至多 2 次」→前 4 回合内 ≤2 次 |
| ④ 窗口延长 | 延长 N×1,000 行动值 | control_state `turns +N` | 「束缚延长 1×1,000」→控制态 turns +1 |

### 6.2 G1B 耗时四型接口（预留，215D–H 落细目）

| 耗时型 | 换算 | 框架口径 |
|---|---|---|
| 前摇 N | ≡ 预告 N 回合后落地 | ❓/🌀 分级渲染归 215（intent windup 扩展字段） |
| 冷却「施放后 N 回合不可用」 | ≡ N 回合 | `unified_cooldown_turns`（207 #12：玩家 N／怪 N+1，raw=生成器已偏移） |
| 持续 duration | 施加回合算第 1 回合 | marks turns；tick 挂回合末（全局回合末统一时点，211 增量裁决） |
| 间隔每 N 回合 | ≡ 型①同族 | tick_round_end N 回合计数 |

### 6.3 消费与对账

- **消费批**：211（战斗常量落地）／212（资源轴 tick）／215D–H（耗时块逐招换算）／216／221（action.json/skills 转译逐条机械换算）。
- **对账基线**：设计稿全库「行动值」词面域分布统计由 `生产日志/九期/批次209_断言.py` 落档（告警口径＝本节存在性＋词面基线快照，供 216/221 转译后零残留对账）；零墙钟、零现实时间词（衔接 215H 游戏日模型）。

---

*本文件与《云海接入可行性报告.md》《云海设计稿索引与数值速查.md》配套。*

---

## 六、九期工具与内容包文件台账（208 对账管线 · 2026-09-13 · 非引擎 hook，纯新增工具/数据）

> §三承诺逐条维持（零引擎代码改动）；本批产物＝设计稿 12 号常量表 → 框架 JSON 片段的对账管线，供 210 内容包骨架（settings/formula/stats 首版）消费。

| # | 文件 | 内容 | 行数/规模 |
|---|---|---|---|
| 1 | scripts/parse_doc12.py | 三段式管线（解析→映射→出片）＋双向断言 A1–A6（--check）＋解析报告（--report） | 283 |
| 2 | docs/cloudsea/doc12_mapping.yaml | 映射规则：点分路由 6＋逐键覆盖 13（SYMBIONT 族→BREAK 域，全库引称语料定 canonical）＋排除项 | 33 |
| 3 | docs/cloudsea/fragments/（39 域片＋_manifest） | 框架 JSON 片段：561 键（explicit 72/dotted 23/override 13/packed_inherit 32/citation 91+citation_majority/fallback 86 未引称旗标）；A1–A6 全 PASS | 40 文件 |

> 管线口径：解析行 511（打包行展开后 561 键）；值保真＝出片 raw 与 12 号源行逐字一致；幂等重放过；未引称键回退节主域并旗标（fallback 86），审计面在 --report。

## 七、九期资源轴三缺口 hook 台账（批次 212 · 2026-09-13 · 逐处锚点与行数登记）

| # | 文件 | 内容 | 行数 |
|---|---|---|---:|
| 1 | qbot_rpg/content/resource_axis_models.py | opt-in 3 字段读取器（tick_per_round/tick_floor/on_full）＋3 缺省常量＋resource_axis_fields 10→13 键 | 42 |
| 2 | qbot_rpg/core/resource_lifecycle.py | tick_round_end 实装（tick_per_round 应用＋clamp [tick_floor, max]＋frozen_sides 被控跳过＋on_full 满槽收集返回 "on_full_fired"）＋effective_max（max_mod 承载，0=不限 mod 不生效）＋_gain_scalar/_gain_pool 负向下限 clamp 0 | 89 |
| 3 | content/cloudsea/axes.json | 云海 21 轴注册数据（玩家 17＋怪 4；_meta 口径＋210 并入登记）——纯数据非 hook | 222 |
| 4 | tests/unit/test_cloudsea_axes_212.py ＋ test_resource_axis_models.py 契约 13 键更新 | 三缺口/max_mod/幂等降级 13 用例＋10 键契约测试演进 | 181 |

> **三缺口口径**：①tick 自然增长＝注册段 `tick_per_round`（正增长/负衰减，clamp [tick_floor, max]；风缆 `WIND_PER_TURN`=2、潮压 `SURGE.GAIN_PER_ROUND`=+2 即此场景）；②负向增减＝apply_gain 负值 clamp 下限 0（气力「只出不进」03 §M3.2B）；③满槽 on_full＝注册段 `on_full` proc 引用，tick 后达生效上限收集事件，装配层按 M13 season_events/proc 消费（引擎只收集不执行）。max_mod＝`effective_max(axis, mod)`（诗人乐章 3→4／德鲁伊孢子 3→5／御风风缆 3→4／潮压威胁缩放／气力 SCALE_BY_TIER 五档场景）。
> **condition_engine 承载＝零改动**：var=resource 既有四键条件＋[我方资源:X]/[对方资源:X] 别名直接承载「态势达标」（POSTURE.VALUE_RANGE 全队共享）／「气力尽」（[对方资源:cs_stamina] ≤0）——纯数据条件，无新 var、无新引擎代码。
> **opt-in 红线自证**：无 tick_per_round/on_full 字段的轴 tick 零增减（幂等不变）；battle.py 回合调度/damage.py 乘区/combo-effects-marks 既有行为/既有 content 包（veinborn 等）/指令词面零 diff（213 提交内 battle.py +24 行系其 tactics 注册面，非本批）；axes.json 为 cloudsea 包首件，210 收尾并 manifest。**hook 引擎行数 42＋89＝131**（累计 207≈287＋213≈199＋212≈131 ＝ 617 ≤ 800 纪律线 ✓）。测试：定向 13 用例＋资源面回归全绿（test_resource_snapshot 1 失败经基线 worktree 复证＝Windows 既有非本批）。


---

## 七、211 战斗常量与公式落地台账（批次 211 · 2026-09-13 · 逐处锚点与行数登记）

> §三承诺维持：回合调度/乘区结构/damage.py/既有包/指令词面零改动——formula/statuses/effects 三 JSON 为**纯数据**（引擎未装配即零引用）；battle.py 仅 207 同款 opt-in 挂点（键缺失零操作）。

| # | 项 | 文件：锚点 | 行数 | 说明 |
|---|---|---|---:|---|
| 1 | formula.json 战斗常量层 | content/cloudsea/formula.json（新建） | 41 | defense_k 400／rng [0.92,1.08]／crit 0.45·0.4·1.50–2.20／xa_singleton 1.60·0.60／h_weakness_expansion 断言锚／speed_neutral F=1.0——全键引设计稿 `12` 号，零自拟 |
| 2 | statuses.json 九态 | content/cloudsea/statuses.json（新建） | 99 | 04 §M4.1 T/D/持续/递增/每场上限逐字；出手序列≡回合（209 §六）；effect_ids 引 effects |
| 3 | effects.json X 单例合成态 | content/cloudsea/effects.json（新建） | 295 | 36 条：xa_add 5＋独立乘子 4＋九态触发效果拆分＋玩家四态＋共生灵四口＋养成＋规则条（递增封顶×3.0/饱和/阶段遗产/TIDE_MULT/回合末衰减） |
| 4 | 衰减/递增封顶 hook 本体 | qbot_rpg/core/cloudsea_ailment.py（新建） | 99 | CloudseaAilmentState 数值面状态机：apply_buildup（触发/递增 min(base×g^n, base×3.0)）／tick_round_end（A−=D）／饱和伤害类豁免（DAMAGE_KIND）；控制类接线归 213 |
| 5 | battle.py 挂点 | qbot_rpg/core/battle.py：tick_round_end 区调用行（≈L3447）＋`_tick_cloudsea_ailments` 方法（≈L3672） | ≈26 | 207 `_tick_field_periods` 同款：`cloudsea_statuses` 键缺失 → 零操作；事件流写 `ailment_events` |
| — | **本批合计** | | **≈560** | 数据 JSON 435＋hook 99＋挂点 26；框架代码侧净增 ≈125（hook＋挂点），累计 287＋125＝**≈412 / 800 纪律线** |

> 消费链：213 控制类接线读 `ailment_events`＋statuses control 字段；214 阶段遗产读 phase_retain 规则条；216 action.json 异常值 V 走 apply_buildup；235G 轴承载三分承载规格与 effects 正交。

---

## 七、九期引擎扩展台账续（214 阶段状态机 · 2026-09-13 · 内容线1）

> 207 hook 面的首次设计消费（inherit_boss_state）＋ monster_ai 阶段写端扩展。§三承诺维持：battle.py 回合调度／damage.py 乘区／combo-effects-marks 既有行为／现有包／指令词面零 diff（本批唯一引擎改动面＝monster_ai.py 阶段写端与条件内建，非红线清单文件）。

| # | 项 | 文件：锚点 | 新增行数（含注） | 说明 |
|---|---|---|---:|---|
| 1 | 行为计数器 | core/monster_ai.py decide() 头（≈L153） | ≈15 | seq_count（每次 decide +1，阶段切换清零＝「激怒满 8」阶段内语义）＋damage_taken 事件/量双记（快照 HP 回落检测） |
| 2 | 阶段写端扩展 | core/monster_ai.py（≈L173） | ≈50 | 条目 no/enter_when（非 HP 触发，list=OR）/from/latch/weakness/mods；changed 时 inherit_boss_state 携带 boss_state（207 hook 设计消费）＋动态弱点换装（weakness_base 首切备份）＋phase_mods 数据契约（211 消费，只写不读）；纯阈值旧配置走 PhaseTable 原路径零变化（有 no 键才进扩展解析） |
| 3 | 三内建条件 | core/monster_ai.py _eval_condition（≈L719） | ≈22 | seq_count／damage_taken（mode=count|amount）／stamina_empty（轴面缺数据 → False，与 212 解耦）＋_cmp_num 通用比较（≈L773） |
| 4 | 扩展解析器 | core/monster_ai.py _phase_entry／_resolve_phases_extended（≈L784/796） | ≈75 | config 序 last-match；HP 换算同 PhaseTable 收敛 3；03 §M3.11 灰岗四阶段直落样例＝content/cloudsea/phases.json（bulk 模板归 219） |
| 5 | 定向测试 | tests/test_cloudsea_phases214.py | 9 例 | 非 HP 触发/HP 阈值/上位直切/latch 驻留/inherit 携带/动态弱点/三条件/旧路径零变化/受击检测——与 monster_ai 既有三单测同跑全绿 |
| — | 合计 | | **≈162** | 九期框架侧累计 ≈287+162=449 行（≤800 纪律线内，245 收官复核） |

---

## 八、方位适配映射节（215C · 2026-09-13 · 内容包数据契约，零引擎 hook）

> 方位系统 v0.6 引擎已实装（combat_position/reposition/position_match/position_bonus）；215C 落的是 schema 与常量面。设计稿单一出处＝`22` 号 §十一（五字段＋marks 五族）＋`12` 号 §AD `C.POS`（11 键）＋§Q `ZONE.ELEVATION`＋§L 职业新技数值组四行。

| # | 方位机制 | 映射为 | 源码锚点 | 动作类型 |
|---|---|---|---|---|
| 1 | attack_zone 五字段 schema | action.json/skills 逐招字段（216/221 数据） | content 包 schema（`22` §十一 全枚举） | 数据 |
| 2 | position_rule 够不着打空 | 引擎 v0.6 rule_permits（battle.py 行动资格检查既有） | qbot_rpg/core/position.py＋battle.py position_rule 消费点（L2205 区） | 既有零改动 |
| 3 | position_match weight_mod | 引擎 position_match 权重面（v0.6 既有） | qbot_rpg/core/position.py | 数据（族级画像 215A） |
| 4 | 仰攻 UPWARD_MOD −0.20/−0.10 | 内容包预处理层（伤害三层预处理包，裁决 #16 damage.py 零改动承诺） | content/cloudsea formula/effects 数据面 | 数据（211 落地形态对齐） |
| 5 | 背击 +0.30 双向＋例外三例 | position bonus 既有面＋例外短路（F08/F09/F11） | position.py bonus 判定＋marks 态查询 | 数据＋内容包 marks 条目 |
| 6 | target_pull 扩展枚举 | pull_close/pull_away/lift_air（裁决 #13 enemy 路径放行 ≤20 行） | effects reposition 原语（engine 既有通道） | ≈≤20 行（随 216 落，另行台账登记） |
| 7 | 态类 marks 五族 | marks.json 五条目（rolling/clinging/burrowing/decoy/falling_window） | content/cloudsea/marks.json（212 statuses 同构） | 数据（215A/216 落） |
| 8 | 微跃帧 1 拍 | air 状态 1 tick（air_policy 既有） | battle.py settle_air_policy | 数据 |
| 9 | 夜曲声域衰减 0.8/0.6 | 诗人增益随相对扇区衰减（增益面预处理） | 内容包 effects 数据（221 诗人落卡） | 数据 |

---

*本文件与《云海接入可行性报告.md》《云海设计稿索引与数值速查.md》配套。*

---

## 八、九期工具台账续（216 动作池转译 · 2026-09-14 · 内容线1 · 非引擎 hook）

| # | 文件 | 内容 | 规模 |
|---|---|---|---|
| 1 | scripts/parse_actions.py | 动作池四册三形态解析（骨架上紧凑/卡式三册/独有招表）→ content/cloudsea/actions.json（1261 招）＋actions_manifest.json；内建对账 A1–A5（1261 分册恰尽/id 唯一/必备键/方位覆盖/幂等） | ≈265 |
| 2 | content/cloudsea/actions.json | 动作库 1261 条：id（ska/skb/ind/dif/uq＋序）／name／source／owner／ctype／intent_mark（❓🌀）／hits（×N 段）／resolve_raw／refs（$C 键）／follow／break_disp／pos 五字段（215A 标注面；独有招 222 卡方位随每怪绑定表，建 enemies.json 时自表列取）／kind=basic／power=1.0 缺省（逐怪绑定归 217–218） | 1261 条 |
| — | 口径注 | 耗时四键（charge_turns/cd/hits/duration 恒写出）归 215E 增量（G1B 在途）；逐怪 actions 引用表与权重归 217–218；调用侧 schema=content/test_demo/action.json 同构超集 | |

---

## 九、九期工具台账续（217 怪物转译管线 v1 · 2026-09-14 · 内容线1 · 非引擎 hook，跨轮增量）

| # | 文件 | 内容 | 规模 |
|---|---|---|---|
| 1 | scripts/parse_enemies.py | 五源合流管线 v1（L1 生态册名册／L2 挂配置／L3 抗性表 A 具名／L4 行为性破坏铺开分片一／L5 独有招绑定表方位→L6 组装）→ content/cloudsea/enemies_tide1.json（105 只）＋enemies_tide1_manifest.json | ≈190 |
| 2 | content/cloudsea/enemies_tide1.json | 潮位一分片 105 只（终盘 6/常规 23/空 44/杂 32）：hp 缺省演算 BASE_EHP×STAR_COEF×LAYER（具名 Boss 走详设 EHP，灰岗 12000）／抗性表 A 具名命中 2／破坏绑定 27 怪（251 分片一）／独有招引用 100% 解析回链 actions.json | 105 只 |
| — | 增量待续 | 增量二＝潮位二至七分片＋抗性五规则默认生成器＋七维 stats 派生＋weakness 三级默认策略；增量三＝pv/部位阈值（219 部位全量输入后） | |

## 十、装备技能聚合器台账（222 · 2026-09-14 · 共通线 · 引擎可选工具＋纯数据）

> 可选加载纯函数模块（不 import 战斗引擎；不注册即不存在），27 技三档结构注入与 gear_skill 条件判定；per-tier 实值＝`18_/07` 词面域由 223 装备全量以 `$C.SKILL.*` 回填。

| # | 项 | 文件 | 行数/量 | 说明 |
|---|---|---|---|---|
| 1 | 装备技能聚合器 | qbot_rpg/core/gear_skill_aggregator.py（新） | ≈120 | load_tiers／resolve_tier（件数阈值单调达档）／aggregate_gear_skills（档②③派生注入 derive_only + 质变亲和降档：非王骸/巨兽套装档③不注入，§三 三规则） |
| 2 | 27 技三档数据 | content/cloudsea/skill_tiers.json（新） | 27×3 | 速查 §三 六线 27 技恰尽；base_skill 待 223 接线（聚合器安全跳过）；三规则白名单 MUTATION_AFFINITY |
| — | 合计 | | ≈120 行＋数据 | 零红线文件 diff；skill_chains 面归 222 后继/223 派生链 |

## 八、云海指令冲突矩阵与接线台账（批次 231 · 2026-09-14）

> 框架既有词面全集 96 个（commands/*.py 的 `*_CMD` 常量程序化提取）× 云海指令面逐一核对。红线：不改既有指令词面——`合成` 系**复用**（框架炼金 SYNTH_CMD 既有引擎，云海配方库经 content/cloudsea/ 数据面接入，零新注册零词面改动）。

| 云海词面 | 框架对照 | 判定 | 处置 |
|:--:|:--|:--:|:--|
| `合成` | SYNTH_CMD=「合成」（alchemy_commands） | **复用** | 零新注册；配方库经 content/cloudsea/ 数据面（235H 并账扩容） |
| `帮` | 框架「帮助」两字词 | 零撞 | 新注册 HELP_CLOUDSEA_CMD（云海速查面板＋单指令用法） |
| `预` | 无 | 零撞 | 新注册 PRESET_CMD（预案串 spec；库数据随 232 灌） |
| `局面` | 无 | 零撞 | 新注册 SITUATION_CMD（缺口引擎①局面聚合：HP/意图/潮压/气力一行式，读 battle_snapshot fail-safe） |
| `挂` | DELEGATE_CMD=「委托」（quest_board 任务委托，异语义） | 零撞 | 新注册 HANG_CMD（缺口引擎②挂 N 托管：N∈[1,10] 解析＋回执；autoplay 执行面归 234） |
| `撤` | ABANDON_CMD=「放弃」（炼金会话放弃，异语义） | 零撞 | 新注册 RETREAT_CMD（缺口引擎③弃撤退：两段式确认） |
| `1/2/3/4/绝` | 框架快捷系统（ctx["shortcuts"] 玩家自定义表） | 预置层 | 数字快捷绑定表 DIGIT_BINDINGS（1→攻击/2·3→技能/4→防御/绝→绝技），经 ctx["shortcut_exec"] 钩子转发；战斗句柄装配归 234 |

> **接线文件**：qbot_rpg/commands/cloudsea_commands.py（新增指令文件，映射表 §二 清单内）——10 词面注册（5 新＋5 数字）＋6 handler 纯函数（parsed+ctx→str）＋register_cloudsea_commands 壳（shop/alchemy 模式）；make_context 装配层注入。定向测试 `tests/unit/test_cloudsea_commands_231.py` 13 用例全绿（注册面/冲突矩阵复扫/五 handler/数字钩子）。**红线自证**：框架 96 词面零触碰、battle.py/damage.py/combo-effects-marks/既有 content 包零 diff；战斗状态只读公开快照语义（battle_state），零交叉 import。注记：局面聚合的资源段读取沿用 212 轴 ID（cs_surge/cs_stamina）；`预` 的预案库与 `挂` 的 autoplay、数字位实际转发三处执行面归 234 异步底盘（231 指令面/解析/参数校验已实装）。
