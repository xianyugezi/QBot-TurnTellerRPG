# 幻觉审查报告·《细化_1c1c_到顶与清零.md》

> 审查对象：`/root/QBot-TurnTellerRPG/docs/细化/细化_1c1c_到顶与清零.md`（182 行）
> 对照定稿：`/root/docs_archive/RPG框架项目/技能派生系统设计定稿.md`（408 行，v1.0·2026-08-14）
> 审查方法：通读细化全文 → 抽取全部声明（规则/用例/行号/引文/数值）→ 回定稿逐行核对（①行号引对 ②内容一致 ③补白显式标注 ④数值有依据）
> 核对范围：细化文中所有 Lxx 引用共 **60+ 处**，逐一回查定稿原始行。

---

## 结论摘要

| P0（硬伤幻觉） | P1（关键补白未标注/可能误导实现） | P2（轻微标注缺失/推导未标） |
|:---:|:---:|:---:|
| **0** | **2** | **3** |

**评分：B**
**一句话：行号引用 100% 真实准确、内容与引文零冲突、数值全部有据，仅 2 处关键补白与 3 处推导性描述未按文档自述规范标注【细化补充】。**

---

## 一、核对通过项（抽样列证，全部属实）

### ① 行号引用核对（全部正确，无一落空）

| 细化声明 | 定稿实际内容 | 判定 |
|---|---|---|
| L16、L152：max_combo 链级属性 | L16"职业通过 max_combo 与到顶行为（reset/hold）形成节奏差异"；L152"max_combo：链级属性（连段上限，内容包配）" | ✅ |
| L154-155：reset 归零重打/剑士格斗家 | L154"reset 循环型：到顶后下一次连段技归零重打（节奏循环）"；L155"→ 剑士/格斗家（节奏循环，精确卡段）" | ✅ |
| L158：到顶当回合先判 eq=max 再归零 | L158"到顶当回合：先判定 eq=max 派生再执行归零（防死配置）" | ✅ |
| L156-157：hold 保持最大不增长/法师盾卫 | L156"hold 蓄力型：到顶后保持最大（不再增长，普通技能不打断）"；L157"→ 法师/盾卫（攒满连段→大招消耗→重新攒）" | ✅ |
| L163-165：hold 免疫被动态/消耗退出免疫 | L163"hold 是 combo==max 时的【被动态】"；L164"派生技消耗连段 → combo<max → 立即退出免疫"；L165"防'一次到顶永久免打断'" | ✅ |
| L399：大招消耗量必填 | L399 风险表"hold 无代价循环｜消耗退出免疫（5.2）+ 大招消耗量必填" | ✅ |
| L168-174：四职业预置 | L170-173 剑士4/reset、法师5/hold、刺客2/reset、盾卫3/hold，数值逐字一致 | ✅ |
| L43-44、L51-53：四标签行为 | L43"combo_preserve…+0 不清零（喘息技）"；L44"combo_push…+N 不清零（N 可配 1-2）"；L51-53 对应 +1/+0+N/归零 | ✅ |
| L22、L28-29、L69-71：派生是链上节点 | L22 核心循环"保留连段/推进连段（喘息续链）"；L28"派生不是终点是链上节点"；L69"派生技使用后【保留连段链】"；L71 连斩→追斩·变示例 | ✅ |
| L31、L254-259：分级放出 P0/P1 | L31"默认只含【连段技】一种标签，保留/推进为进阶内容"；L257"保留连段/推进连段标签"在 P1 名下 | ✅ |
| L54-59：打断面规则逐条 | 防御不打断/道具不打断/指令被拒不改连段/逃跑成功清空失败保留/被控制保留/死亡胜利失败战斗结束清零 | ✅ |
| L203-204：冷却降级/指令被拒 | L203"冷却中派生技：自动降级为原技能+连段保留（防链卡死）"；L204"指令被拒：不耗回合不改连段" | ✅ |
| L112、L116、L385：打断判定/霸体免疫 | L112"打断其连段（连段清零）"；L116"使用期间免疫打断"；L385"打断技→目标 combo 清零（除非霸体）" | ✅ |
| L141、L143：引文逐字一致 | L141"⚡ 盾击打断连斩！连段中断"；L143"连段中断！（原因：使用了普通技能）" | ✅ |
| L182-184：战斗结束清零/中断恢复/复活 | L182"战斗结束（任何原因：胜利/失败/死亡/逃跑/怪物丢失/中断回收）→连段清零"；L183"连段 3/4 保持中"；L184"复活不携带连段" | ✅ |
| L213：跨战斗蓄力开关 | L213"默认战斗结束清零，可配'跨战斗蓄力'开关（默认关，内容包可开）" | ✅ |
| L238：大招溢出补偿 | L238"目标死亡时连段未消耗 → 转少量经验/下场起始+1（可配）" | ✅ |
| L277-278、L288：配置字段落点 | JSON 中 max_combo:4 / max_combo_behavior:"reset" / consume:0 逐行存在 | ✅ |
| L310、L353、L371：tag 枚举/编辑器下拉 | L310 注释"combo/combo_preserve/combo_push/interrupt/armor/无"；L353 校验枚举；L371"技能卡下拉（combo/preserve/push/interrupt/armor）" | ✅ |
| L332-343、L382：combo_state 快照 | L332"combo_state 入战斗快照 3.18"；L336-340 chain_id/chain_name/count/hold/step_index；L337"冗余存储防热重载失效"；L343"链删除时按'无链=普通技能'降级（不报错）" | ✅ |
| L355-357：校验器死配置三条 | L355"min/max 与 max_combo 矛盾"；L356"eq 段数>max_combo"；L357"reset 模式 eq=max（提示改用 hold 或 min-max）" | ✅ |

### ② 内容一致性
- TC-TOP-01~07 / TC-DEF-01~05 / TC-END-01~07 共 19 条：前置条件、步骤、期望均与定稿对应规则吻合（如 TC-TOP-02"先判派生再归零，顺序反则永不触发"=L158；TC-END-01"战斗内资源不跨战斗残留"=L59"（战斗内资源）"）。
- 细化内自洽：7+5+7=19 条与头部声明一致；行 182 统计"L16-L399"确为到顶/喘息/清零规则行覆盖域。
- 头部声明"依据 v1.0（2026-08-14）"与定稿 L3 一致。

### ③ 显式标注核对（合规项）
- §1.1 判定顺序回合粒度收敛（L27）、§1.2 hold 到顶豁免 L53 特例（L43-44）、§2.2 喘息不免疫对齐 1c2 TC-ARM-04（L96）、§2.3 combo_push_amount 字段收敛（L106）、TC-TOP-05 特例标注——均已打【细化补充】，符合文档自述纪律。

### ④ 数值有据核对
- 剑士4/reset、刺客2/reset、法师5/hold、盾卫3/hold ← L170-173 逐字有据。
- combo_push N ∈ {1,2} ← L44"（N 可配 1-2）"有据。

---

## 二、问题清单

### P1（2 条）——定稿未明但被当作定稿事实断言，未标注【细化补充】，可能误导实现/配置

**P1-1｜§1.1 L24-25 + TC-TOP-03："提示不拒绝加载，内容包自行决策"**
- 细化原文："校验器对 reset 模式 `eq=max` 出死配置提示，建议改用 hold 或 min-max（L357）——**提示不拒绝加载**，内容包自行决策"；TC-TOP-03 期望栏同样写"不拒绝加载"。
- 定稿核对：L357 仅说"提示改用 hold 或 min-max"，**未规定死配置的加载行为（拒绝 or 放行）**；L197"可加载"与 L359"超限警告不拒绝"均只针对**倍率警告**（6.3 节），不覆盖死配置检测。细化将"不拒绝加载"当作既定规则陈述。
- 定性：关键校验行为补白未标注。虽与定稿"校验器只提示防崩"（L32）的整体软校验哲学相容、不至于与定稿冲突，但属"策划案未明处"却未打【细化补充】标签，违背文档自己规定的标注纪律。
- 建议：补标【细化补充】，或引用 L32/L359 作为依据。

**P1-2｜§3.2 L133 + TC-END-07："大招溢出补偿默认不启用"**
- 细化原文：§3.2"按配置转少量经验 / 下场起始 +1（**可配项，默认不启用**）"；TC-END-07 期望"默认不启用"。
- 定稿核对：L238 仅写"（可配）"，**未规定默认启用与否**；定稿只有 L213 跨战斗蓄力明确"默认关"。细化两处直接断言"默认不启用"，无依据且未标注。
- 定性：默认值补白未标注。影响内容包默认配置决策。
- 建议：补标【细化补充】或删除"默认不启用"表述。

### P2（3 条）——推导性/实现层描述未标注，风险低

**P2-1｜§3.3 L137："清零动作挂在战斗结算收尾（回合结束 tick 后）"**
- 定稿 L382 仅"随战斗结束清零（6.1）"、L384 给出回合时序（…→ 回合结束 tick），**未指定清零挂点**。细化直接给出"回合结束 tick 后"的实现时序，属实现层补白未标注。
- 建议：补标【细化补充】。

**P2-2｜§2.2 L94："到顶（hold）时使用喘息技保持 max 不变"**
- 定稿无直接语句，系 L156（到顶保持最大）+ L43-44（喘息不清零）的推导组合；结论正确但与 L157"普通技能不打断"并列为规则陈述时未标注为推导。
- 建议：补标【细化补充】。

**P2-3｜§1.3 字段表第 5 行："combo_state.hold 与链配置 max_combo_behavior 同源冗余"**
- 定稿仅对 `chain_name` 标注"冗余存储防热重载失效"（L337、L403），`hold` 字段的冗余语义属细化自述，未标注。
- 建议：补标【细化补充】或删减。

### 查无 P0
- 无一行号落空、无一处引用语句不存在、无一条规则与定稿抵触、无一处引文失真、无一个预置数值无据。

---

## 三、可选改进（非问题）

- L33 回合粒度示例画框内"① 指令处理：快照 count 3 → 结算成功 → count=4（达顶）"与定稿 L92"条件评估时基于当前战斗状态快照评估一次"衔接良好，建议示例中显式标注该依据（L92），增强可追溯性。
- 定稿 408 行正文中"到顶/喘息/清零"相关行号下限为 L16、上限为 L399，行 182 统计表述准确，无需改动。

---

*审查完·逐行核对 60+ 处 L 引用与 19 条用例，证据如上。*