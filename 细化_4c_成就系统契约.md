# 细化_4c_成就系统契约（achievements.json 数据模型 · 三原语解锁条件 · 统一奖励与称号 · 隐藏成就 · 验收测试）

> 版本：v1.0 · 类型：实现层细化契约（大厂模式：数据模型 / 解锁条件 / 统一发放 / 状态持久化 / 隐藏成就 / 验收测试）
> 追溯依据（**严格引用策划案，引用到行号 Lxx**）：
> - 《任务系统设计定稿.md》v1.0.4 → 简称【任务】（§2.1 统一 reward 条目 L108-126；条件三原语 L19/L25-36；op 双写 L37；条件键别名映射/互译表 L39-58；发放器唯一实现 L125）
> - 《怪物模块设计定稿.md》v1.0.3 → 简称【怪物】（§九 图鉴情报 L216-230；**图鉴完成度 = 收集控目标** L229；codex_state 落点 L230；校验器 L300）
> - 《单机向体验包设计定稿.md》v1.0 → 简称【单机】（F2 图鉴全收集阶梯 L40-46；里程碑五档 L42；codex 分册 L43；100% 专属奖励 L44；90% desc 软锚 L45；**收集闭环：100% 图鉴含隐藏要素条目** L46；联动总表 L77-79；数值 L86；校验 L96；事件注册表预置 L113；⛩️ 仪式感揭示 L69；不提供 /秘密 L72）
> - 《方案存档/全量重叠检查/新功能B_成就称号.md》→ 简称【成就方案】（作者配置版拍板 L4/L14；统一条件引擎 L15；check 达成检测 L16；统一 reward 条目+称号型 L17；三路称号合一 L18；边界声明 §五 L103-113；字段表 L39-49；称号型条目 L51-59；判定与发放 L61-67；指令登记 L126）
> - 《方案存档/神鱼内容包规划.md》→ 简称【神鱼】（**万家灯火即神鱼 = 隐藏成就** L20；羁绊胜过追寻 L31）
> - 《时间天气系统设计定稿.md》→ 简称【时间天气】（条件引擎新键 L176-182；消费方统一复用含成就 L213-218）
> - 《职业熟练度与生活系统设计定稿.md》→ 简称【熟练度】（§3.6 称号佩戴 L129-148：1 槽/titles 注册表/[称号] 前缀 L144/title_state 存档 L147）
> - 《实现层规划文档.md》→ 简称【规划】（C29 achievements.json 成就墙 L157；reward 统一 schema ④称号型+消费方含成就+入账顺序 L180；持久化语义五分类 ⑤成就称号 L190）
> 范围：achievements.json 字段级 schema、解锁条件四类映射（图鉴完成度/事件计数/物品收集/等级，全部复用统一条件引擎）、统一 reward 解析器与称号联动、成就状态持久化（玩家存档 achievement_state）、隐藏成就（神鱼支线→万家灯火即神鱼 类型）、验收测试用例。
> 不含：编辑器 UI（【成就方案】附 I.5 预留）、消息前缀渲染细节（消息前缀功能设计定稿承接）、NPC 4.3 互译表主表本身（主表落 NPC 定稿）。
> 变更纪律：任何偏离定稿之处必须写入 §0.4 决策记录并注明理由+影响用例，禁止静默改语义。

---

## 〇、契约总则

### 0.1 追溯总览表

| 子系统 | 定稿落点 | 关键行号 |
|---|---|---|
| 统一条件引擎（三原语） | 【任务】§一 / 【成就方案】L15 | L19/L20/L25-36/L37/L47-58 |
| 统一 reward 条目 | 【任务】§2.1 / 【成就方案】L17/L49/L51-59 / 【规划】L180 | L108-126/L123-125 |
| 图鉴完成度（收集控目标） | 【怪物】§九 / 【单机】F2 | 怪物 L216-230/L229/L230；单机 L40-46/L86 |
| 收集闭环（100% 含隐藏要素） | 【单机】F2 / 联动总表 | L46/L77-79/L106 |
| 称号三来源 + 佩戴 | 【成就方案】§三 / 【熟练度】§3.6 | 成就方案 L71-86；熟练度 L129-148 |
| 隐藏成就（万家灯火型） | 【神鱼】 / 【单机】仪式感揭示 | 神鱼 L20/L31；单机 L69/L72/L37-38 |
| 持久化（achievement_state） | 【成就方案】L61-67/L112 / 【规划】L190 | 成就方案 L66/L112 |

### 0.2 术语表

| 术语 | 定义 | 依据 |
|---|---|---|
| 成就墙 | 作者配置的长期目标墙——达成检测（条件满足即达成，一次性），可配小奖励与称号 | 【成就方案】L21/L108-109 |
| 达成检测（check） | 触发类型唯一值：条件满足即达成；**不引入事件监听型**（与任务事件型条件区分） | 【成就方案】L16/L47 |
| 三原语 | 判定 100% 由统一 conditions 数组驱动，仅 3 种原语：值型 / 累计型 / 事件型 | 【任务】L19/L25-36 |
| 统一条件引擎 | `{var, op, value, param}` 四键条件条目（NPC 4.0），中文变量键经别名映射互译为英文条件键 | 【任务】L20/L39-44 |
| 统一 reward 条目 | 物品 `{item,count}` / 货币键值 `{coins\|gem\|exp\|rep:N}` / 组合数组 / **称号型 `{title:ID}`（仅成就使用）**，任务/签到/NPC/怪物掉落/成就共用同一发放器 | 【任务】L108-126 / 【成就方案】L17 |
| 称号型条目 | 奖励第四型：`{ "title": "称号ID" }`——发放器将称号写入 title_state 可佩戴列表，**不扩散到任务/签到/NPC/掉落** | 【成就方案】L17/L56 |
| achievement_state | 成就达成状态存档（达成集合+时间戳+可配重复计数），对齐 3.7.1 ③ 长线计数语义：**只增不减** | 【成就方案】L66 |
| title_state | 已拥有称号集合 + 当前佩戴称号 ID（玩家存档 3.7，换包三选一复用） | 【成就方案】L84 |
| hidden 成就 | 隐藏成就标记：列表不显示 / 显示锁定，达成瞬间揭示并展示结局文本 | 【神鱼】L20 |

### 0.3 一句话契约

> 成就系统 = 由 achievements.json 配置的**达成检测墙**：解锁条件 100% 复用统一条件引擎三原语（值型 / 累计型 / 事件型，`{var,op,value,param}`），不另立条件格式；
> 奖励走统一 reward 条目（物品/货币键值/组合数组 + 成就侧独有的称号型 `{title}`），发放器唯一实现＝框架 reward 解析器（与任务/签到/NPC/怪物掉落同管线）；
> 达成状态落玩家存档 achievement_state（**只增不减**、达成时间戳），读取 longline_counters / codex_state / reputation_state 等既有长线数据、**不重复计数**；
> 与任务互补不重叠（任务=接取后推进的流程；成就=纯达成检测）；
> 隐藏成就（隐藏标记）不在 /成就 列表公开，达成瞬间揭示结局文本——案例：神鱼内容包「万家灯火即神鱼」（完成全部神鱼相关支线解锁）。

### 0.4 决策记录（ADR，均为对定稿语义的显式收敛，不违反定稿任何一行）

| 编号 | 决策 | 理由 | 影响用例 |
|---|---|---|---|
| D-01 | **condition 单对象 → conditions 数组**：【成就方案】L46 字段名 `condition`（单对象）收敛为 `conditions`（数组，与【任务】L98 同构，支持组合条件）；`condition` 单对象 = 单元素数组的等价别名（解析器等值，同给异值→黄提示） | 万家灯火型成就需「完成全部支线」多条件，单对象放不下；对齐任务引擎 conditions 数组惯例【任务】L19/L98 | TC-20 |
| D-02 | **多条件语义 = 数组全与（AND）**：conditions 数组逐条求值，**全部为真才达成**；不引入 OR/组合算子（要 OR 语义拆成多个成就或事件型计数器） | 对齐本族任务引擎同款决策（细化_2b4 D-02），一套引擎零特例 | TC-01/TC-18/TC-20 |
| D-03 | **求值失败安全降级**：条件求值异常（变量未注册 / 事件未触发过 / param 维度缺失）= 默认**不满足**（false），不崩溃、不阻塞 /成就 指令，仅编辑器黄提示 | 对齐 2b4 D-03 同款惯例 + 【单机】L37 可达性检查体系 | TC-18/TC-21 |
| D-04 | **达成结算单事务**：发放奖励（框架 reward 解析器）+ 达成时间戳写入 achievement_state + once 标记翻转（+ 可配 repeat 计数）全部在**一个 SQLite 事务**内；message_id 幂等不二次发放 | 对齐 2b4 D-04 / 3f R-21 同款原子结算惯例 | TC-07~TC-10/TC-12 |
| D-05 | **内联串 = 序列化糖**：`exp=500,coins=1000` ≡ `[{exp:500},{coins:1000}]`，解析器等价展开；存储与校验以结构化条目为准 | 【任务】L100/L124 明示「解析器等价」，成就侧沿用（【成就方案】L49 同 schema） | TC-22 |
| D-06 | **隐藏成就默认 = 显示锁定**：`hidden.mode` 默认 `locked`（/成就 列表显示「？？？」锁定行，序号计数占位）；可配 `hide`（列表完全隐藏不占序号）。【神鱼】L20 给了两个口径（"成就列表不显示/显示锁定"），收敛默认 locked——保留悬念且让玩家知道还有东西（探索惊喜系统【单机】L5 的折中：不提供 /秘密【单机】L72，但保留可见锁定行） | 定稿双口径需收敛一个默认展示行为，可配项覆盖另一口径 | TC-14/TC-15 |
| D-07 | **授予时机 = 结算点钩子**：图鉴点亮 / 事件触发 / 物品获得 / 升级等**结算点**逐批求值本模块已注册、未达成的成就条件（check 型）；不轮询、不做后台任务 | 对齐 3f R-21「每一次图鉴点亮事件结算时检查补全」；碎片化友好 | TC-01/TC-03/TC-16 |
| D-08 | **隐藏成就揭示纪律**：`reveal_text` 仅在达成瞬间输出一次（⛩️ 一次性揭示卡片语义）；未达成时任何路径（/成就 列表、/成就信息、任何指令回显）**不渲染** reveal_text，防剧透 | 【单机】L69 一次性揭示 + L72 不提供 /秘密（探索感完整保留）+ 【神鱼】L31 结局文本的仪式感 | TC-14/TC-16/TC-17 |
| D-09 | **默认值兜底（漏配=合理默认，绝不报错）**：conditions=[]（空=接取即达成，黄提示不拦）｜trigger=check｜once=true｜hidden=false｜reward=[] | 【成就方案】L39-49 字段表「默认」列 + 【任务】L11 极简字段哲学 | TC-19 |

---

## 一、achievements.json 字段级 schema

### 1.1 文件根与结构总览（字段树）

```
achievements.json                       // 文件根（【成就方案】§二 L27-37 + 附 I.2 元数据）
└── [成就] ×N                           // 8 顶层字段（下表 1.2）
    ├── conditions: Cond[]              // 达成条件数组（4 键原语 {var,op,value,param}，§二）
    ├── reward: str|list                // 统一奖励条目（§三；内联串=糖；可含称号型 {title}）
    └── hidden: bool|obj                // 隐藏成就标记（false 或 {mode,reveal_text,clue_ref}，§五）
字段数：顶层 8 ＋ hidden 子对象 3 ＝ 11 叶子字段；
另：条件原语 4 键（{var,op,value,param}，3 型）＋ 奖励条目 4 型（物品/货币键值/组合/称号）。
```

**字段计数口径**：顶层 8 字段（下表逐列清点）＋ `hidden` 子对象 3 key（mode/reveal_text/clue_ref）＝ **11 叶子字段**。`conditions` 条目复用三原语 4 键（值型/累计型/事件型，§二）；`reward` 条目 4 型（§三）。

**文档总览（计数）**：字段 **11 项**（顶层 8 ＋ hidden 3）· 条件原语 **3 型**（4 键）· 奖励条目 **4 型** · 规则 **13 条**（ACH-01 ~ ACH-13，分布见各节规则表）· 验收用例 **22 例**（TC-01 ~ TC-22，见 §六）。

### 1.2 顶层字段表（8 字段）

| # | 字段 | 类型 | 必选 | 默认 | 取值/约束 | 语义 | 依据 |
|---|---|---|---|---|---|---|---|
| 1 | id | string | ✅ | 自动生成 | 全局唯一 | 成就唯一 ID，被 title 注册/日志记录引用 | 【成就方案】L43 |
| 2 | name | string | ✅ | — | **≤20 字** | 成就墙展示名（隐藏成就锁定态不渲染明文，见 §五） | 【成就方案】L44 |
| 3 | desc | string | 可选 | "" | **≤50 字** | 成就墙展示描述；**90% 档阶梯成就允许明示「全收集还有更深处」——唯一明示例外**（软锚） | 【成就方案】L45 + 【单机】L45 |
| 4 | conditions | list | ✅ | [] | Cond[]，三原语 `{var,op,value,param}`（【任务】L98 同构） | **达成条件数组**；全与（D-02）；单对象别名 `condition` 等价（D-01）；数组为空=接取即达成（黄提示不拦） | 【成就方案】L46（收敛 D-01/D-02）+ 【任务】L19 |
| 5 | trigger | enum | 可选 | `"check"` | **仅 check** | 达成检测：条件满足即达成；配置其它值=硬拦结构错误 | 【成就方案】L16/L47 |
| 6 | once | bool | 可选 | true | true/false | true=达成后不再检测（默认）；false=可重复达成/重复发小奖励（成就通胀，黄提示） | 【成就方案】L16/L48 |
| 7 | hidden | bool/obj | 可选 | false | false 或 `{mode,reveal_text,clue_ref}`（§1.3） | 隐藏成就标记：true≡`{mode:"locked"}`；含 reveal 文案时强制 hidden | 【神鱼】L20（隐藏成就 hidden 标记） |
| 8 | reward | str/list | 可选 | [] | 统一 reward 条目数组（物品/货币键值/组合/称号型）或内联串（D-05），别名 `rewards` 等价（同给异值→黄提示） | 成就达成奖励，经框架 reward 解析器发放（§三） | 【成就方案】L17/L49 + 【任务】L100/L124 |

> 引擎统一性铁律（ACH-01 / ACH-04）：**达成判定 100% 由 conditions 三原语驱动，触发类型仅 check（达成检测）**——成就不引入监听型触发，与任务事件型条件在条件语法层面共用、在触发机制层面区分【成就方案】L15-16/L21。

### 1.3 hidden 隐藏成就子对象（3 key）

| # | 字段 | 类型 | 默认 | 取值/约束 | 语义 | 依据 |
|---|---|---|---|---|---|---|
| 1 | mode | enum | `"locked"` | `locked` \| `hide` | locked=列表显示「？？？」锁定行（占序号、不渲染 name/desc）；hide=列表完全隐藏（不占序号） | 【神鱼】L20（两口径收敛 D-06） |
| 2 | reveal_text | string | "" | 任意文本；hidden 时**建议必填**（缺失黄提示） | 达成瞬间揭示文本（结局文案，如「灯火即神鱼」），一次性输出（D-08） | 【神鱼】L20/L31 |
| 3 | clue_ref | list | [] | lore/日志/地图文本引用列表 | 线索引用（三层线索漏斗第一二层载体），**每隐藏成就 ≥1 条**硬提示软拦 | 【单机】L38（精神延伸，L38 原文约束隐藏要素，隐藏成就=隐藏要素之一，见 §五.4） |

### 1.4 校验器与默认值兜底（边界契约）

**硬拦（拒绝加载）**：引用不存在（标题 title 未注册 / 物品未注册 / 变量或事件未注册）/ 类型错误（trigger 非 check / 结构错误）/ 负数。【成就方案】L58 + 【任务】L276（硬拦 4 类精神类推）。
**黄提示（不拦可加载）**：条件永假（如 gain_count:铁矿≥999）/ 奖励超基准 / once=false 通胀提示 / conditions 为空 / hidden 缺 reveal_text / condition/rewards 重复别名冲突。【成就方案】L58 + 【任务】L277 + 【单机】L37。
**默认兜底（D-09）**：trigger=check｜once=true｜hidden=false｜reward=[]｜reward 内联串自动展开（D-05）。

### 1.5 加载与模块开关

- 作者配置版：**非自动生成**；`achievements` 模块**不声明 = 不加载**（manifest 可选，向后兼容）【成就方案】L4/L14。
- 阶梯模板一键插入（25/50/75/90/100 + 称号）由编辑器提供【单机】L93，模板产物即本 schema 的标准成就（§二.2 示例）。
- 单机向体验包随内容包整体开关（settings），**不单独设档**【单机】L118。

---

## 二、解锁条件（全部复用条件引擎）

### 2.1 求值管线与结算点钩子（ACH-04 / ACH-13）

```
结算点钩子（D-07）：图鉴点亮 / 事件触发 / 物品获得 / 升级 / 任务完成 等结算完成
 → 读玩家当前状态（快照，非会话）
 → 逐条求值【已注册且未达成】成就的 conditions[]（三原语归一开关，§2.4）
 → 全部为真（AND，D-02）→ 达成 → 触发达成结算（§3.2 单事务）
 → 条件求值失败 → 默认不满足（false，不崩，D-03）
```

- 判定 100% 由统一 conditions 数组驱动（【任务】L19），条件全部基于统一条件引擎 `{var,op,value,param}`（【任务】L20）——**成就不另立条件格式**【成就方案】L15。
- 触发类型仅 check（达成检测）：每次结算点钩子都重查未达成成就，条件满足即达成【成就方案】L16。
- 判定口径与任务区分：任务=接取后推进（quest_active 三表）；成就=**纯达成检测**（achievement_state 只增不减）【成就方案】L112。

### 2.2 四类解锁条件映射表（任务点名的四类，全部复用既有条件键）

| 类别 | 条件键（var） | param 维度 | 示例 | 读取来源 | 依据 |
|---|---|---|---|---|---|
| **图鉴完成度** | `codex` | 缺省=总册；`param:"monster"/"fish"/"item"/"craft"`=分册 | `{var:codex, op:ge, value:50}`｜`{var:codex, op:ge, value:100, param:"monster"}` | 玩家存档 codex_state（四册加权求值，见 §二.3） | 【任务】L54 + 【怪物】L230 + 【单机】L42-43 |
| **事件计数** | `[事件:事件名]`（固定前缀原语） | param=事件目标（可空） | `{var:"[事件:神鱼支线完成]", op:ge, value:8}`｜`{var:"[事件:隐藏发现:蚀月之狼]", op:ge, value:1}` | 事件触发计数表（NPC 4.3.2 事件注册表写入；单机预置 [事件:环境事件:ID]/[事件:隐藏发现:ID]） | 【任务】L33-35/L58 + 【单机】L59/L113 + 【神鱼】L20 |
| **物品收集** | `gain_count`（累计获得）/ `item_count`（当前背包） | param=目标物品 ID（精确匹配） | `{var:gain_count, op:ge, value:100, param:"铁矿"}`｜`{var:item_count, op:ge, value:20, param:"神鱼图"}` | longline_counters（累计，只增不减）/ 当前背包快照 | 【任务】L28/L31-32/L50-51 |
| **等级** | `level` | 无 | `{var:level, op:ge, value:30}` | 当前状态快照（等级） | 【任务】L29/L49 |

### 2.3 全部可复用条件键汇总（一套引擎零特例）

| 条件键 | 说明 | 依据 |
|---|---|---|
| level / item_count / gain_count / kill_count / codex / dungeon_clear / main_progress / prof_level / reputation | 任务系统已注册条件键，成就直接消费（互译表主表落 NPC 4.3） | 【任务】L47-58 |
| `codex` param 分册（monster/fish/item/craft）+ 总册加权（权重可配，默认等权） | 图鉴完成度条件支持分册；每册 100% 专属称号（怪物博士/鱼类大师/物品收藏家/制造行家）+ 总册 100% 收藏家 | 【单机】L43 |
| `[事件:环境事件:ID]` / `[事件:隐藏发现:ID]` | 单机向体验包预置事件（事件注册表 NPC 4.3.2），可被隐藏成就/阶梯前置引用 | 【单机】L59/L113 |
| [季节] / [时段] / [天气]（var:season/period/weather） | 时间天气条件引擎新键；消费方统一复用：任务/副本事件/dungeon 事件牌组/任务/NPC/**成就**；求值失败默认不满足 | 【时间天气】L176-182/L213-218（跨文件对齐：细化_2a4c L112「成就｜achievements.json 条件｜时间天气 §L215」） |

### 2.4 求值语义

| 编号 | 规则 | 语义 | 依据 |
|---|---|---|---|
| ACH-02 | **全与（AND）** | conditions 数组逐条求值，全部为真才达成；任一不满足不回显示达成 | D-02 + 【任务】L19 |
| ACH-03 | **求值失败=不满足** | 变量未注册/事件未触发过/param 维度缺失 → false，不崩溃不阻塞 | D-03 + 【单机】L37 |
| ACH-09 | **只读不计数** | 成就读取 longline_counters / codex_state / reputation_state 等既有长线数据，**不重复计数**（图鉴进度/累计获得/击杀等由原系统记账） | 【成就方案】L111/L112 |
| — | **跨档跳升逐档授予** | 图鉴完成度从 45% 直接到 52%：只授予 50% 档成就，25% 档此前已授；逐档检查不漏授不重授 | 【单机】L86（阶梯间隔）+ 对齐 3f R-21 |
| — | **op 双写等价** | `>=`≡ge｜`>`≡gt｜`<=`≡le｜`<`≡lt｜`=`≡eq｜`!=`≡ne，全文示例用英文 op | 【任务】L37 |

---

## 三、奖励（统一 reward 解析器 · 称号联动）

### 3.1 奖励条目 schema（四型，统一 · 任务/签到/NPC/怪物掉落/成就共用发放器）

```json
// ① 物品条目（引用 items.json）：{ "item": "ID", "count": N }
{ "item": "世界之书", "count": 1 }
// ② 货币/数值键值条目：{ "coins" | "gem" | "exp" | "rep": N }
{ "coins": 1000 }   // 金币（settings 货币键空间）
{ "gem": 3 }        // 宝石
{ "exp": 500 }      // 经验（数值直入账）
{ "rep": 20 }       // 声望（可选模块；rep = reputation 值入账 → reputation_state）
// ③ 组合数组：多条目按序入账
"reward": [ { "exp": 500 }, { "coins": 1000 }, { "item": "世界之书", "count": 1 } ]
// ④ 称号型条目（仅成就系统使用，不扩散到任务/签到/NPC/掉落）：{ "title": "称号ID" }
{ "title": "t_collector" }   // 发放器将称号写入 title_state 可佩戴列表
```

- ①②③ 依据：【任务】L112-120（§2.1 统一 reward 条目 schema）；④ 依据：【成就方案】L17/L51-59 + 【规划】L180（reward 统一 schema ④ 称号型 `{title: ID}`）。
- 物品键固定 `item`（引用 items.json）；货币键 coins/gem 走 settings 货币键空间；exp/rep 数值直入账【任务】L123。
- **内联键值串（`exp=500,coins=1000`）= 同一解析器的序列化糖**，解析器等价展开为条目数组（D-05）【任务】L124。
- **发放器唯一实现 = 框架 reward 解析器**——签到/NPC/怪物掉落/副本首通/成就只配条目不另立发放逻辑【任务】L125 + 【规划】L180。

### 3.2 逐键入账管线（ACH-06 / ACH-07）

```
达成（§2.1 结算点钩子判定通过）
 → BEGIN TRANSACTION
 → ① 解析 reward（内联串 → 条目数组，D-05）
 → ② 依序逐条入账（组合数组按序【任务】L119；【规划】L180 顺序：exp → 货币 → 物品 → 称号）
      ├─ exp → 经验数值直入账
      ├─ coins / gem → settings 货币键空间入账
      ├─ rep → reputation_state 累计值入账（声望=进度门槛，不入货币表）【任务】L118/L226
      ├─ item → items.json 引用，count 个入包
      └─ title → 称号写入 title_state 可佩戴列表（titles 注册表引用校验）【成就方案】L56/L64-65
 → ③ 达成时间戳写入 achievement_state + once 标记翻转（§四）
 → COMMIT  任一失败 ROLLBACK（不发放/不标记）
幂等：message_id 快照防重复发起（D-04，对齐 3f R-21）
→ 回复模板：「🏅 成就达成：图鉴过半！+exp500/coins1000」（隐藏成就走 §五 揭示卡片格式）
```

### 3.3 称号联动（三来源合一 · 佩戴槽 · 消息前缀）

| 来源 | 授予方 | 配置落点 | 进入可佩戴列表时机 | 依据 |
|---|---|---|---|---|
| ① 职业王称号 | 职业熟练度 §2.3（图鉴全亮，按职业独立） | proficiency.json tier_names[6] / titles 自动生成（source: king） | 图鉴全亮授予时 | 【成就方案】L75 |
| ② 品评会冠军 | 职业熟练度 §3.3 | profession.json contest.reward.title | 夺冠结算时 | 【成就方案】L76 |
| ③ **成就称号** | 本契约（achievements.json reward ④称号型） | achievements.json reward `{title}`（source: achievement 自动注册） | **成就达成发放时** | 【成就方案】L77 |

- titles 注册表：proficiency.json titles 列表 `{id, name, icon, source, desc}`（source: king/contest/achievement/custom）【成就方案】L81-82，成就称号校验器硬拦 title 引用不存在【成就方案】L58。
- 佩戴槽：**1 个（替换式）**；指令 `/称号 查看` `/称号 佩戴 <N>`【成就方案】L19/L126。
- 显示：消息前缀 `[称号]` 占位符（未佩戴渲染为空；王称号「群内称号展示」统一收编进消息前缀，不另立展示通道）【成就方案】L20/L83。
- 存档：title_state = 已拥有称号集合 + 当前佩戴称号 ID（玩家存档 3.7，换包三选一复用）【成就方案】L84 + 【熟练度】L147。
- 与王称号关系：佩戴体系只负责「拥有/展示/替换」，不改变王称号授予逻辑与称号加成【成就方案】L85。

### 3.4 边界

- **发奖失败不吞整次**：条目级失败（物品不存在）→ 黄字跳过该条不中断，其余条目照常入账（对齐【规划】L180「发奖失败（物品不存在）黄字跳过不吞整次」）；但**事务完整性**指达成标记与槽位写入（D-04：全成或全不成，部分条目失败按规划口径跳过为黄字语义，不 ROLLBACK 已入账条目）。
- **幂等**：达成结算单事务＋会话快照幂等（D-04），重复结算点不二次发放、不重复写时间戳。
- **超基准黄提示**：单成就 reward 总值超出内容包基准 → 黄提示（不拦）【任务】L277（精神类推）。
- once=false 成就：可重复达成、重复发小奖励（作者自担通胀，编辑器黄提示）【成就方案】L16/L48。
- 声望模块默认关：禁 rep 条目时 rep 被忽略并黄提示（可配）【任务】L218（精神类推）。

---

## 四、成就状态持久化（玩家存档）

### 4.1 achievement_state 存档结构（E-01 收敛）

```
achievement_state（玩家存档段，框架 3.7 持久化语义总表 ⑤ 成就称号【规划】L190）
├── unlocked: { <成就ID>: <达成时间戳 ISO8601> }   // 达成集合（只增不减，不随周期重置）
├── repeat_count: { <成就ID>: N }                   // once=false 成就重复达成计数（可配，默认缺省=无）
└── （隐藏成就达成后：unlocked 照常记录；/成就 列表由 locked 翻转为已达成展示）
```

- 存储承接：achievement_state 为**成就系统独占段**（不并入 longline_counters 键位——达成是离散事件集合而非计数标量；重复计数走 repeat_count）。【成就方案】L66「达成时间戳入 achievement_state（对齐 3.7.1 ③ 长线计数语义：只增不减，不随周期重置）」。
- 对齐 3f R-21 原子性：达成状态写入与奖励发放**同一事务**（D-04）。
- **存档字段先登记后引用**（【规划】L190）：achievement_state / title_state 的字段登记由框架 3.7 持久化语义总表承载，本契约引用不重复造表。

### 4.2 只增不减与幂等（ACH-08）

| 编号 | 规则 | 语义 | 依据 |
|---|---|---|---|
| ACH-08 | **只增不减** | unlocked 集合只插入、不删除、不随每日/每周周期重置；达成时间戳永不回退 | 【成就方案】L66/L112 |
| ACH-07 | **达成幂等** | once=true：已达成成就从待检集合移除，重复结算点不再检测、不再发放；once=false：仅 repeat_count+1 并重发小奖励 | D-04 + 【成就方案】L16/L48 |

### 4.3 热重载与降级

- 热重载：成就配置删除/修改 → 已达成成就的**存档字段保留**（unlocked 时间戳不因配置删除而丢失），/成就 列表不再显示该条目并附降级提示「配置已移除」（对齐任务侧「删配置降级提示不报错」惯例，任务 L207/L350，精神延伸）。
- 配置变更（conditions 收紧/放宽）：只影响**未达成**成就的后续判定；已达成成就保持达成态（尊重「只增不减」）。

### 4.4 存档登记纪律

- 存放位置：玩家存档（3.7）；换包三选一语义对齐 title_state 同款（【成就方案】L84）。
- 任何新增持久化字段须先登记（【规划】L190），本契约不引入存档新类，仅登记 achievement_state 段。

---

## 五、隐藏成就（神鱼支线 → 万家灯火即神鱼 类型）

### 5.1 语义与两种展示模式（ACH-10）

- **隐藏成就 = 带 hidden 标记的成就**【神鱼】L20：达成条件对外不可见，/成就 列表不显示或显示锁定；达成瞬间揭示并展示结局文本（D-06/D-08）。
- 两种列表模式（§1.3）：
  - `locked`（默认）：/成就 列表显示「？？？」锁定行，占序号、不渲染 name/desc/reveal_text；已达成后翻转为正常展示（名称+达成时间戳）。
  - `hide`：/成就 列表完全隐藏该行（不占序号，成就总数也按可见计数）。
- **不提供 /秘密 列表指令**【单机】L72：任何「列出全部隐藏成就/隐藏要素」类指令不注册（未知指令按 3c 默认回复），探索感完整保留。
- 与隐藏要素的关系：隐藏成就是隐藏要素体系的收束点之一（【神鱼】L20 直接点名成就系统 hidden 标记），其揭示仪式复用 ⛩️ 一次性揭示卡片格式【单机】L69/L104。

### 5.2 案例：万家灯火即神鱼（神鱼内容包 P0，完整配置）

```json
{
  "id": "ach_lantern_fish",
  "name": "万家灯火即神鱼",
  "desc": "？？？",
  "conditions": [
    { "var": "[事件:神鱼支线完成]", "op": "ge", "value": 8 }   // 全部神鱼相关支线完成（事件计数原语；value=内容包支线总数，示例 8 为【补白】）
  ],
  "trigger": "check",
  "once": true,
  "hidden": {
    "mode": "locked",
    "reveal_text": "灯火即神鱼——回看冒险日志，一路上同行的人已组成神鱼的轮廓。",
    "clue_ref": ["log:渔歌线", "lore:说书人", "map:码头"]
  },
  "reward": [ { "title": "t_lantern_fish" }, { "coins": 999 } ]
}
```

- 依据：【神鱼】L20（完成**全部神鱼相关支线**后解锁；成就列表不显示/显示锁定；达成瞬间揭示并展示结局文本「灯火即神鱼」；成就系统 hidden 标记）+【神鱼】L31（羁绊胜过追寻——达成时揭示结局文本：完成全部支线的玩家回看冒险日志，发现路上的人组成神鱼的轮廓）。
- 达成链路（对齐【单机】L77-79 联动总表句式）：

```
神鱼支线逐条完成（渔歌线 / 海贼王线 / 说书人线 …）→ [事件:神鱼支线完成] 计数 +1
→ 第 8 条完成结算点钩子 → 条件满足 → ⛩️ 成就揭示卡片：万家灯火即神鱼！
→ 展示 reveal_text「灯火即神鱼——…」→ 奖励发放（称号 t_lantern_fish 进 title_state / coins 入账）
→ 冒险日志记录（milestone 类，对齐【单机】L50）→ /成就 列表该行翻转为已达成展示
```

### 5.3 揭示仪式纪律（D-08）

- **一次性揭示**：达成瞬间输出 `⛩️ 前缀 + 成就名 + reveal_text` 特殊格式；状态翻转后不再出（对齐【单机】L69「一次性揭示卡片：…状态翻转后不再出」）。
- **不提前渲染**：未达成时任何路径不输出 reveal_text / 不渲染 hidden 成就的 name/desc 明文（D-08）——锁定行只显示「？？？」。
- 隐藏成就的线索走三层线索漏斗（环境文本泛暗示 / 图鉴 lore 定向线索 / 玩家主动操作确认【单机】L29-33），`clue_ref` 仅服务校验器与编辑器（作者视角回放【单机】L95），不参与玩家展示。

### 5.4 校验器（可达性 / 线索引用，ACH-10）

| 检查项 | 层级 | 规则 | 依据 |
|---|---|---|---|
| 条件永假检测 | 黄提示 | 隐藏成就引用了未注册事件/条件键、或条件组合逻辑上永假 → 「无发现路径=可能永不可发现」 | 【单机】L37 |
| 线索引用检测 | 硬提示软拦 | 每隐藏成就 clue_ref ≥1 条（lore/日志/地图文本引用）——缺失可保存但标注 | 【单机】L38（精神延伸：L38 原文约束隐藏要素，隐藏成就=隐藏要素收束点之一） |
| 揭示文案必填 | 黄提示 | hidden=true 且 reveal_text 为空 → 提示「隐藏成就建议配置达成揭示文案」 | 【神鱼】L20/L31（语义需要，标注补白） |
| 作者视角时间轴预览 | 编辑器 | 模拟作者配置的隐藏链能否被玩家发现（玩家不可见） | 【单机】L95 |

---

## 六、验收测试用例（22 例 TC-01 ~ TC-22）

### 覆盖矩阵（22 用例 / 5 类）

| 需求类别 | 定稿行号 | 用例区间 | 条数 |
|---|---|---|---|
| ① 解锁条件（四类：图鉴完成度/事件计数/物品收集/等级） | 【任务】L28-35/L47-58；【怪物】L230；【单机】L42-43 | TC-01 ~ TC-06 | 6 |
| ② 奖励统一条目（数值/物品/称号/组合） | 【任务】L112-126；【成就方案】L17/L51-59；【规划】L180 | TC-07 ~ TC-10 | 4 |
| ③ 状态持久化（只增不减/幂等/热重载） | 【成就方案】L61-67/L112；【规划】L190 | TC-11 ~ TC-13 | 3 |
| ④ 隐藏成就（锁定/隐藏/揭示/校验） | 【神鱼】L20/L31；【单机】L37-38/L69/L72/L96 | TC-14 ~ TC-18 | 5 |
| ⑤ schema 与校验器（默认值/别名/枚举/引用） | 【成就方案】L39-49/L58；【任务】L276-279 | TC-19 ~ TC-22 | 4 |
| **合计** | — | TC-01 ~ TC-22 | **22** |

### ① 解锁条件

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-01 | 图鉴完成度全局：`{var:codex, op:ge, value:50}`；玩家图鉴 45%→52%（跨档跳升） | 50% 档成就达成；25% 档此前已达成不重授；52% 不触发 75% 档；完成度读 **codex_state**（怪物模块不重复计数） | 【单机】L42/L86；【怪物】L230 |
| TC-02 | 图鉴分册 param：`{var:codex, op:ge, value:100, param:"monster"}` vs 总册 100% | 怪物册 100% → 仅怪物册成就达成；总册未满（其它册缺）→ 总册 100% 成就（收藏家）不达成；分册条件与总册条件互不干扰 | 【单机】L43 |
| TC-03 | 事件计数：`{var:"[事件:神鱼支线完成]", op:ge, value:8}`；完成第 8 条支线瞬间 | 第 7 条时不满足；第 8 条完成结算点 → 达成；计数由事件注册表写入，成就只读 | 【神鱼】L20；【任务】L33-35/L58 |
| TC-04 | 物品收集（累计）：`{var:gain_count, op:ge, value:100, param:"铁矿"}` | 历史累计获得铁矿 ≥100 → 达成；读 longline_counters，不重复计数 | 【任务】L31-32/L51 |
| TC-05 | 物品收集（当前背包）：`{var:item_count, op:ge, value:20, param:"神鱼图"}`；背包 20 神鱼图 / 19 神鱼图残片 | 20 → 达成；19 → 不满足（param 精确目标维度，不同名不算）；消耗后背包 19 → 已达成成就不回退（只增不减） | 【任务】L28/L50 |
| TC-06 | 等级：`{var:level, op:ge, value:30}`；LV30/LV29 | LV30 → 达成；LV29 → 不满足；读当前等级快照 | 【任务】L29/L49 |

### ② 奖励统一条目

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-07 | `reward:[{exp:500},{coins:1000},{gem:3}]` 达成 | exp 数值直入账 +500；coins/gem 走 settings 货币键空间 +1000/+3 | 【任务】L114-117/L123 |
| TC-08 | `reward:[{item:"世界之书",count:1}]` 达成 | 物品按 items.json 引用入包 1 件（唯一物品标记可配） | 【任务】L112-113/L123 + 【单机】L44 |
| TC-09 | `reward:[{title:"t_collector"}]` 达成（收藏家称号） | 称号写入 **title_state** 可佩戴列表；/称号 查看可见；佩戴后消息前缀 [称号] 渲染；title 未注册 t_fake → 校验器硬拦不加载该成就 | 【成就方案】L56/L64-65/L58 |
| TC-10 | 组合数组 `[{exp:500},{coins:1000},{item:"世界之书",count:1},{title:"t_collector"}]` 按序入账；其中 item 条目引用不存在的物品 | 按序入账 exp→coins→物品→称号；item 不存在 → **黄字跳过该条**，其余照常入账；达成标记与已入账条目同事务提交；重复结算点幂等不二次发放 | 【任务】L119；【规划】L180；D-04 |

### ③ 状态持久化

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-11 | 达成「图鉴过半」后跨每日重置（05:00）再查 | 达成时间戳留存、不随周期重置；/成就 保持已达成展示、不重复触发检测 | 【成就方案】L66/L112 |
| TC-12 | once=true 成就：达成后同结算点再次触发（如再次获得同类物品） | 不再检测、不再发放、时间戳不变（幂等）；once=false 成就配置重复达成 → repeat_count+1 并重发小奖励 | 【成就方案】L16/L48；D-04 |
| TC-13 | 热重载：已达成成就配置从 achievements.json 删除 | /成就 不再显示该条目（降级提示「配置已移除」）；unlocked 存档字段保留；已发奖励不回收 | 【规划】L190；对齐任务热重载降级（任务 L207/L350，精神延伸） |

### ④ 隐藏成就

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-14 | 隐藏成就（mode:locked）未达成：玩家打开 /成就 列表 | 显示「？？？」锁定行（占序号）；**不渲染** name/desc/reveal_text 明文；/成就信息 <N> 同样只显示锁定态 | 【神鱼】L20；D-06/D-08 |
| TC-15 | 隐藏成就（mode:hide）未达成 | 列表**完全隐藏**该行，不占序号；可见成就总数不含该行；/秘密 类指令不注册（未知指令默认回复） | 【神鱼】L20（口径二）；【单机】L72 |
| TC-16 | 达成瞬间（万家灯火型）：完成最后一条神鱼支线 | 结算点钩子满足 → ⛩️ 揭示卡片（前缀+成就名）+ reveal_text「灯火即神鱼——…」一次性输出；奖励入账；冒险日志记录（milestone 类） | 【神鱼】L20/L31；【单机】L69/L50/L104 |
| TC-17 | 隐藏成就达成后再次：打开 /成就 / 再次触发相同事件 | 列表翻转为已达成展示（名称+达成时间戳）；揭示文本不重复输出（状态翻转后不再出）；重复结算点不重发 | 【单机】L69；【神鱼】L20 |
| TC-18 | 校验器：隐藏成就条件引用未注册事件 `[事件:不存在]` / clue_ref 为空 | 条件永假 → 黄提示「无发现路径=可能永不可发现」（不拦）；clue_ref 空 → 硬提示软拦；编辑器作者视角时间轴可回放该链 | 【单机】L37-38/L95/L96 |

### ⑤ schema 与校验器

| 编号 | 场景 / 输入 | 期望断言 | 定稿依据 |
|---|---|---|---|
| TC-19 | 最小配置 `{id,name,conditions:[...]}` 无其它字段 | 默认值兜底（D-09）成功加载：trigger=check｜once=true｜hidden=false｜reward=[]；name 超 20 字/desc 超 50 字 → 黄提示 | 【成就方案】L39-49 |
| TC-20 | 一条配 `condition`（单对象），一条配 `conditions`（数组） | 解析器等值（D-01）：同一条件两种写法行为一致；同给且异值 → 黄提示「条件字段重复」 | 【成就方案】L46（收敛 D-01） |
| TC-21 | `trigger:"event"`（非 check 值） | 硬拦结构错误，该成就拒绝加载并提示「仅支持 check 达成检测」 | 【成就方案】L16/L47 |
| TC-22 | `reward:"exp=500,coins=1000"` 内联串 vs 结构化数组 | 解析器等价展开（D-05）：两种写法入账结果完全一致；存储/校验以结构化条目为准 | 【任务】L100/L124 |

---

## 七、数据与配置落点速查

| 落点 | 内容 | 依据 |
|---|---|---|
| achievements.json | 成就墙配置（顶层 8 字段 + hidden 3 key；conditions 三原语；reward 4 型含称号型） | 【成就方案】§二；【规划】C29/L157 |
| 玩家存档 achievement_state | 达成集合 unlocked（时间戳）+ repeat_count | 【成就方案】L66；【规划】L190 |
| 玩家存档 title_state | 已拥有称号集合 + 当前佩戴称号（成就称号/王称号/品评冠军三来源并集） | 【成就方案】L84；【熟练度】L147 |
| proficiency.json titles | 称号注册表 {id,name,icon,source,desc} | 【成就方案】L81-82 |
| codex_state | 图鉴完成度（四册+总册加权），成就只读 | 【怪物】L230；【单机】L43 |
| longline_counters | 累计获得/击杀/事件计数等，成就只读 | 【任务】L201-203 |
| settings 货币键空间 | coins/gem 入账；rep 走 reputation_state 不入货币表 | 【任务】L123/L226 |
| 事件注册表（NPC 4.3.2） | 预置 [事件:环境事件:ID] / [事件:隐藏发现:ID]；承载隐藏成就事件计数条件 | 【单机】L113/L59 |
| 指令表 | /成就 [页数]（5 条/页，分页语法 7.4）/ 成就信息 <N> / 称号 / 称号 佩戴 <N> | 【成就方案】L126 |
| message_prefix | [称号] 占位符渲染成就称号（settings message_prefix 段） | 【成就方案】L20/L90-98 |

---

## 附录：不实现、仅记录

1. **零配置自动成就层**：建议稿 #4 原提「零配置自动成就层」被用户拍板否决（成就=作者配置版）——本契约不实现自动生成成就，仅记录【成就方案】L4/L14。
2. **成就达成群广播**：定稿未定义成就达成推送群公告/分享卡通道——不引入新展示通道（消息前缀 [称号] 为唯一常驻展示位；冒险日志里程碑记录为唯一历史记录位），仅记录，内容包可后续另立评审【单机】L50/L105。
3. **成就墙排序规则**：定稿未定义 /成就 列表排序——默认按配置顺序（JSON 数组序）展示，已达成置顶（可配），不引入排序算法配置。

---

*细化依据：《任务系统设计定稿.md》v1.0.4（§2.1 统一 reward L108-126 / 三原语 L19/L25-36 / 互译表 L47-58）+《怪物模块设计定稿.md》v1.0.3（§九 图鉴情报 L216-230）+《单机向体验包设计定稿.md》v1.0（F2 图鉴全收集阶梯 L40-46 / 联动总表 L77-79 / 仪式感揭示 L69 / 校验 L96）+《方案存档/新功能B_成就称号.md》（成就墙与称号佩戴作者配置版）+《方案存档/神鱼内容包规划.md》（万家灯火即神鱼 L20/L31）+《实现层规划文档.md》（C29/L157、L180、L190）+ 同族兄弟细化（2b4 条件引擎与统一奖励口径、3f 收集闭环承接、2c1c 图鉴 codex_state、2a4c 时间天气条件键消费方）。所有字段、原语、规则、用例行号可逐条回查；对定稿只给语义未给字段名的属性和默认值，已在 §0.4 决策记录显式收敛登记（D-01~D-09）。*

*本契约统计口径：**顶层 8 字段 ＋ hidden 3 ＝ 11 叶子字段**；条件原语 3 型（4 键 {var,op,value,param}）；奖励条目 4 型（物品/货币键值/组合/称号）；规则 13 条（ACH-01 ~ ACH-13）；验收用例 **22 例**（TC-01 ~ TC-22）。*