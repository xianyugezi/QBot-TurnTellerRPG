# 深度打造系统 · 决策记录（用户拍板 2026-09-19 + 主 agent 数值裁定）

> 上游：`/root/deliverables/深度打造系统_方案_待决策.md`（三路汇总方案）、`/root/deliverables/打造系统_原案_20260919.md`（需求源头）、
> `打造系统_{A,B,C}_*.md`（三路明细）。
> 本文件 = **实现批的唯一决策依据**：与本文冲突者以本文为准。

---

## 一、H 组硬岔路 · 用户已拍板

| # | 决定 | 用户原话/口径 | 落地含义 |
|---|---|---|---|
| **H1** | **深度打造 = 打造家族的"深度层"**（关系同「深度炼金 : 炼金系统」） | 「深度打造和打造系统的关系就和深度炼金和炼金系统的关系一样」 | 参照炼金定稿三层结构（`合成→炼金→深度炼金`，共用 `recipe.json`）：**共享基础设施**（材料/套装/强化数值层/词条键空间/孔位镶嵌/实例化/分解回收）+ **独立功能层**（自己的模块与入口，不复用 forge 的"无随机"段） |
| **H2** | **品质新增正交维度** | 「按建议来」 | 新增 `品质等级 1~10` × `颜色 6 档（白绿蓝紫橙红）` × `图纸档 铜银金彩`；**不动**既有唯一品质注册表（common/uncommon/rare/legendary，`field_meta.py:891`、`core/quality.py:80`、`m9_shared_contract.md:210`） |
| **H3** | **强化上限：替换现有的** | 「替换现在的」 | 用**打造装备的 6 档上限**（+3/+6/+9/+12/+15/+18，由品质决定）**替换**既有 `max_by_rarity`（5/8/10/12）；**须给既有内容的迁移/兼容口径**（旧装备按旧档读取，新装备按新档），迁移写进报告 |
| **H4** | **装备实例加落档唯一键 `uid`** | 「按建议来」 | `ItemInstance` 与 `EquipmentSlot` 增 uid（`data/item.py:28-39`、`data/player.py:57-69`）+ **存档迁移**（旧档补发 uid）+ 全链路（装配/战斗桥/寄存/交易）按 uid 引用 |
| **H5** | **随机流沿用引擎既有做法** | 「按建议来」 | `rng_state` 持久化 + 逐指令推进（照 `core/battle.py:5316,5409-5424` 的既有实现；**不得**再用 `random.Random(qid)` 每指令重建） |
| **H6** | **新指令换新名** | 「按建议来」 | `/打造`、`/铸造` 已是 `/锻造` 别名、`/图纸` 已被占用 → 深度打造主入口用**新名**（建议 `/精造`，最终名可在批 1 定，**不与既有指令撞车**） |
| **H7** | **副手 = 部位编辑页上的"是否启用副装备"可选项** | 「在部位数量编辑的页面加入一个是否启用副装备的可选项，如果启用就是副手设计，不启用就不是，让用户可以根据自己的数据库实际情况选择」 | **副手是配置开关**（在 `settings.slot_defs` 的编辑界面/元数据里给"启用副装备"选项）：**启用** → 出现副手部位与副手规则（单手作副手 50% + 指定加成失活）；**不启用** → 行为与本系统引入前**完全一致**（默认关闭，回归零影响）。开关与副手规则必须**包声明驱动、不硬编码** |

## 二、数值口径 · 用户授权主 agent 裁定
> 用户原话：「**数值口径你决定吧，反正又不是硬编码，到时候根据需要改就行了。**」

裁定原则：**全部做成可配（包声明/元数据），给默认值 + 可调范围**；文档写明每个数的依据与影响。

| # | 项 | 裁定 |
|---|---|---|
| **N1** | 品质经验模型 | 采用 B 路提案：`单次经验 = Σ(材料品阶基数 × 相性加成系数 × 件数边际 × 互动乘子) × 全局衰减`；cost 约束 = `总 cost 上限(按图纸档缩放) + 每材料 cost`；**参数全部落包声明** |
| **N2** | 防退化两条约束 | **必做**：① 图纸声明**等级带 + 主材料槽**；② **cost 上限随图纸档缩放**（否则出现"低阶便宜材料填满预算"的单一最优解） |
| **N3** | 总 cost 上限量级 | 默认钉在"**刚好到 10 级 + 少量冗余**"（B 路推：10 级阈值≈4040，上限≈4200，超投空间 ~4%）；**按图纸档缩放**，全部可配 |
| **N4** | 品质系数乘区 | **0.80–1.50**（面板总倍率 0.795–1.075），在"装备这一份预算"内分配；可配 |
| **N5** | 分解回收率 | 先占位：`精粹 = 投入价值 × k1(0.30–0.40) × k2(图纸档 0.40–0.60) × β^色序(β=0.5–0.7)`，与既有 `decompose_rate`（0.40→0.65）并存；标"待实际数据校准" |
| **N6** | 品质经验粒度 | 按原案"**每次打造独立**"：单次打造内累计、打造结束后清零（**不跨件累计**） |

## 三、用户新增的两条要求（本轮补充）

### 补充 1 · **装备占比 40% → 60%**
- 用户原话：「把装备占比在不改变其他数值的情况下提高到 60%，其他地方你看需要怎么调整就安排一下就行了。」
- **主 agent 裁定（执行口径）**：
  - 现状基线：面板 = `白值 7 : 装备 8 : buff 5`（合计 20 单位）→ 装备占 **40%**（依据 `docs/审查参考/阶段一数值模型v2:32` 及三路报告引用）。
  - **保持白值与 buff 的绝对数值不变**（这是"不改变其他数值"的直接含义），**放大装备贡献**至 60%：
    `装备 8 → 18`（18/(7+18+5)=**60%**），即**装备预算 ×2.25**；白值 7、buff 5 不动。
  - **连锁调整（授权范围内，主 agent 安排）**：面板总功率由 20 → 30 单位（+50%）⇒ **怪物侧与战斗数值层按同一比例重校**（目标：**保持既有斩杀回合数与手感不变**），按战斗数值层定稿的"三档推演（无buff下限/半buff中值/满buff上限）"口径重算，用既有**蒙特卡洛/斩杀回合**验证工具做**修前 vs 修后对照**。
  - **实施落点**：① 装备数值来源（词条/强化/品质乘区）的**总额度参数**上调配平；② 战斗数值层的怪物侧基线同步；③ **全部参数化**，不写死。
  - **独立成批**（数值校准批），与功能批解耦，便于回滚。

### 补充 2 · **材料相性共享给深度炼金**
- 用户原话：「材料相性这个设定可以共享给深度炼金用，相性影响药剂效果之类的。」
- **主 agent 裁定**：**相性是通用机制**，不属打造私有 →
  - 落点为**通用层**（如 `core/affinity.py` + 包声明数据），**打造与深度炼金共用同一套相性定义/池/互动规则**；
  - 深度炼金的消费方式：**相性 → 药剂效果**（效果强度/附加效果/成功率等，具体口径在炼金侧细化时定，本系统只提供**通用相性 API**）；
  - **不得**在两处各写一套相性（唯一源）；深炼金侧的接入点留出**稳定接口**，等其细化时使用。

---

## 四、批次计划（执行序 · 每批可独立验收）

| 批 | 范围 | 依赖 | 状态 |
|---|---|---|---|
| **批 38** | **地基**：① H6 命名解冲突（新指令名 + 别名冲突检查）② H7 副手**开关**（部位设置页可选项 + 开关关闭时行为零变化）③ 槽位角色/手数 字段登记（`role` / `handedness`，包声明驱动）④ **相性通用层骨架**（`core/affinity.py` + 数据落点 + 校验 + 编辑器可见，先不接打造流程） | 无 | **已完成（2026-09-19，地基；打造流程未接）** |
| 批 39 | **H4 实例 uid + H5 随机流推进**（含存档迁移与回归） | 批 38 | 待 |
| 批 40 | 图纸 + 材料 + 打造主流程（等级加权 / cost / 品质经验 / 概率表落数据） | 批 39 | 待 |
| 批 41 | 属性与套装词条随机抽取 + 相性池（通用池/专属池/联动覆盖） | 批 40 | 待 |
| 批 42 | 强化：**替换为 6 档上限** + 每 4 级特殊词条 + 迁移兼容 | 批 40 | 待 |
| 批 43 | 符文附魔（三孔/三阶/3合1/按装备类型差异） | 批 39 | 待 |
| 批 44 | 淬炼 + 分解回收（精粹） | 批 40 | 待 |
| 批 45 | **数值校准批**（装备占比 60% + 怪物侧同步 + 斩杀回合对照） | 与功能批解耦，可随时插 | **已完成（2026-09-20；页脚串「批45 · 装备占比校准」，见 §十）** |
| 批 46 | 深度炼金侧相性接入（相性 → 药剂效果） | 相性通用层（批 38）+ 炼金侧细化 | 待 |

---

## 五、批 38 地基登记（实现批的落点冻结）

### 5.1 指令名登记（H6 解冲突）

**选定名：`/精造`**（深度打造主入口；**本批只登记、不在 Router 注册任何空壳指令**——真指令等批 40 实现主流程时随 `commands/` 新模块注册）。

**登记口径**（全部包声明驱动、不硬编码）：
- 主入口名固定为 `精造`（无参数字符串常量落在批 40 的新命令模块里，不落框架公共表）；
- 批 40 注册时须同时：① `router.register(CommandSpec("精造", handler=...))`；② 把 `"精造"` 加进 `commands/parsers.DEFAULT_WHITELIST`（否则 S5 前缀匹配静默不响应，见 `parsers.py` 既有 P-04/P-05 裁决）；③ 跑 `assembly/router_setup.check_consistency`（白名单 ↔ 注册双向一致）+ 指令计数/契约测试。
- 本批**不加白名单**：避免"看着像功能其实没接线"（登记在文档，接线在批 40）。

**别名冲突检查（全量扫描证据，2026-09-19，基线 `52d38a8`）**：

扫描方法（可复跑）：用 `assembly/router_setup.REGISTER_GROUPS` 装配框架 Router（`make_context=None`）→ `router.names()` 取全量注册名、逐个 `spec.aliases` 取内置别名；再取 `commands/parsers.DEFAULT_WHITELIST` 与各 `content/<包>/settings.json` 的 `command_aliases`。

| 检查项 | 结果 |
|---|---|
| 框架注册指令名 | **110 条**（含 `/打造` `/铸造` `/图纸` `/锻造` `/锻造树`） |
| 框架内置别名（`CommandSpec.aliases`） | **1 条**：`领取任务 → 任务` |
| 包声明别名（`settings.command_aliases`） | veinborn：`{任务: 领取任务}`；其余 8 包均无 |
| `parsers.DEFAULT_WHITELIST` | **112 条**（含 `打造`/`铸造`/`图纸`） |
| `精造` 是否已是注册名 / 内置别名 / 白名单项 | **否 / 否 / 否** |
| 以 `精` 开头的既有指令名 | **0 条**（无同首字撞车） |
| 以 `精造` 为前缀的既有指令名 | **0 条**（不会被 S5 最长前缀匹配吞掉，也不吞既有名） |
| 与 `精造` 共享尾字 `造` 的既有名 | `打造` `铸造` `锻造` `锻造树`（**均非 `精造` 前缀**，无吞噬关系） |

**结论**：`/精造` 与既有指令名、内置别名、包别名、白名单**零冲突**；原拟的 `/打造` `/铸造`（= `/锻造` 别名，`commands/forge_commands.py:2248-2252`）与 `/图纸`（= 锻造树进度，`commands/forge_commands.py:1585`）确认不可用。

> 证据原始输出留存于本批报告；复跑命令见批 38 报告「①」节。

### 5.2 副手开关命名与落点（H7）

| 项 | 值 |
|---|---|
| 开关落点 | `settings.equipment_offhand`（obj，settings 段顶层） |
| 开关字段 | `enabled`（bool，**默认 false**） |
| 数值折算字段 | `single_hand_scale`（number，默认 `0.5`；**可配、不硬编码**） |
| 槽位角色字段 | `settings.slot_defs.<部位>.role`（enum `main`/`offhand`，缺省 `main`） |
| 手数字段 | `items`/`equipment` 条目 `handedness`（enum `one_hand`/`two_hand`，缺省未声明） |
| 引擎归一入口 | `core/equipment.py::EquipmentEngine`（`slot_defs` 解析处） |
| 关闭语义 | 开关关闭 → 副手部位不生效、无折算、无失活；**行为与本系统引入前逐字段一致**（对拍） |

### 5.3 相性通用层落点（补充 2）

| 项 | 值 |
|---|---|
| 引擎 | `qbot_rpg/core/affinity.py`（纯函数、通用层，**打造与深度炼金共用**） |
| 数据段 | `settings.affinities` / `settings.affinity_pools` / `settings.affinity_linkage` / `settings.affinity_reactions`（settings 顶层，非 forge 私有） |
| 材料/图纸相性声明 | `items`/`equipment` 条目 `affinities`（obj：`{相性id: 数值}`） |
| 对外单一查询 | `resolve_available_entries(config, main, sub)` → 给定主/副相性 → 可用词条集合 |
| 深炼金接入点 | `accumulate_affinity` + `rank_affinities` + `resolve_available_entries` + `resolve_affinity_effect(effect_table, main, sub)`（稳定 API，本批不消费；见批 38 报告「④ API 一览」） |

---

---

## 六、用户补充（2026-09-19 二次）：**「合成」提升为打造与炼金的公用系统**

> 用户原话：「把"合成"作为一个**打造和炼金的公用系统**，用户们**可以不选择启用打造和炼金，只启用合成**，也就是说不管是**合成装备还是合成药剂合成材料**之类都可以走这个。然后再分别是**炼金（深度炼金）**和**打造（深度打造）**。因为我感觉打造没必要再分出来一个类似合成的东西独立出来，本来就是极度简化用的，干脆合一块对玩家来说应该更简单。」

### 主 agent 可行性判定：**方便**（现有零件 ≈ 80%，只需归位，不需重写）
| 需求 | 现有依据 | 结论 |
|---|---|---|
| 合成是公用层 | 炼金定稿 `:18`「第 1 层【合成】（跨职业）」、`:5`「**合成（跨职业）→ 炼金（职业专属）→ 深度炼金（大师解锁）**」、`:36`「合成/炼金/深度炼金（三层生产）」 | ✅ 定稿本就把合成定为**跨职业公用层** |
| 玩家可**只启用合成** | 总纲 `:2719`「可裁剪模块声明：`settings.json` 各模块段 `mode` 字段（full/simple/off）」；`:2720`「炼金 `alchemy.mode` —— full 三层漏斗 / **simple 仅合成层（投料合成，无炼金层/深度炼金/职业等级/特性/能量条）**」 | ✅ **现成机制**，"只启用合成"= simple 档 |
| 合成载体 | `core/synthesis.py`（引擎，已消费 `synth_allowed`）· `/合成` 指令已注册（`alchemy_commands.py:165 SYNTH_CMD`，在 parsers 白名单内）· `recipe` 模块（框架目录 `module_catalog.py:83`「合成配方：材料与产出」） | ✅ 零件齐 |
| 打造不再自带"简化合成" | 原批 40 计划"打造自带基础合成" | ✅ **改为复用公用合成层 → 更省事** |

### 目标架构（三层 + 三条启用路径）
```
合成（公用层：装备 / 药剂 / 材料 皆可走；极度简化的投料产出）
  ├─ 炼金  → 深度炼金   （职业专属 → 大师解锁；走 recipe + alchemy 三层漏斗）
  └─ 打造  → 深度打造   （本方案；走 recipe 公用合成 + 打造深度层）
```
- **三条启用路径（互相独立，包声明驱动）**：①`只启用合成` ②`合成 + 炼金(深度炼金)` ③`合成 + 打造(深度打造)`；也可三者全开。
- **深度打造的基础合成调用公用层**（`recipe` + `core/synthesis.py`），自己只做**深度层**（图纸/材料相性/随机属性与套装词条/品质经验/强化词条/符文/淬炼）。

### 落地动作（归位，非重写）
1. **指令归位**：`/合成`（及 `/投料` 等合成族）从 `alchemy_commands` **提到公用层**（新模块或既有 `recipe` 的指令组），**保持指令名与行为不变**（回归对拍）；
2. **启用粒度**：`mode` 三态扩展为**三条路径的启用矩阵**（合成 / 炼金 / 打造 各自开关），**缺省 = 现状**（不启用打造时零变化）；
3. **打造复用公用合成**：深度打造的"投料→基础产出"走 `recipe`/`synthesis`，**不新造第二套合成**；
4. **编辑器**：「合成」作为**独立可启用模块**（`recipe` 已在框架目录）；打造/深度打造在其之上；
5. **红线（回归）**：既有炼金三层漏斗口径**不得破坏**——"只启用合成"必须**逐字段等于**现有 `alchemy.mode=simple` 行为。

### 对批次计划的影响
- **批 38**（进行中：命名/副手开关/槽位角色/相性通用层）**不受影响** ✓
- **新增「批 39 · 合成层归位」**（插在原批 39 之前）：指令归位 + 三条启用路径 + 编辑器模块化 + **回归对拍（simple 档零变化）**
- 原「批 40 打造主流程」范围收窄为**深度层**（基础合成改为调用公用层）
- 后续批号顺延。

### 批 39 落地登记（2026-09-19 · 已完成；页脚批次串 =「批39 · 合成公用层」）

| 落地项 | 落点 | 说明 |
|---|---|---|
| 指令归位 | `commands/synth_commands.py`（新） | `/合成` 从 `alchemy_commands` 提层；名称/参数/文案/行为不变；`alchemy_commands` 仅再导出 |
| 装配 | `assembly/router_setup.REGISTER_GROUPS` | `synth_commands` 先于 `alchemy_commands`（唯一注册，防同名双注册） |
| 启用矩阵（唯一推导点） | `core/craft_paths.py`（新） | `alchemy.mode` 三态推导合成/炼金/深度炼金 + `settings.deep_craft.enabled`（obj+enabled，默认关）推导打造；`mode_matrix`/`path_matrix`/`resolve_paths`/`path_enabled`/`layer_denied_message` |
| 指令壳门禁 | `commands/alchemy_commands.py` | `/炼金` `/深度炼金` `/即时调合`：`simple`→仅合成层、`off`→已关闭、`full`/缺省→放行（行为零变化） |
| 编辑器 | `content/field_meta.py` / `content/validator.py` / `content/module_catalog.py` | `settings.deep_craft` 字段登记（中文名/说明/默认关）+ `recipe`（配方）可启用、指向 `settings.alchemy.mode`；段结构/类型红拦 + 「打造已开但合成层关」黄提示 |
| 公用合成 API | `core/synthesis.py` | `base_output` / `requirements_of` / `preview_base_output`（给定投入 → 基础产出，只读）；调用边界见 `docs/批39_合成公用层接口.md` |
| 回归对拍 | `tests/unit/test_batch39_synth_relocation.py` | 提层前后 18 用例 canonical sha256 逐字节一致；`simple` 与现有档逐字段一致 |

> **红线落实（口径说明）**：既有代码仅实现 `alchemy.mode=off` 的合成层拒绝，**未实现 `simple`
> 门禁**（预存缺口）。本批按总纲 `:2720`「simple 仅合成层」落地 `simple` 的炼金层/深度炼金层
> 门禁；合成层（`/合成`）输出与提层前**逐字节一致**（对拍证据见上表）。`full` 与缺省档对
> 整个三层漏斗行为零变化。

### 批 40 落地登记（2026-09-19 · 已完成；页脚批次串 =「批40 · 实例uid与随机流」）

> 范围 = H4 实例唯一键 + H5 随机流推进（**两块地基**，不实现打造流程）。原表「批 39 = H4/H5」
> 因批 39 插入「合成公用层」而顺延为本批。

| H | 落地项 | 落点 | 说明 |
|---|---|---|---|
| **H4** | 实例 uid 生成 | `data/item.py` | `ItemInstance.uid`（`new_item_uid()` = `uuid4().hex`，唯一生成口径单源；`__post_init__` 缺省补发；`compare=False` 保持结构等价语义） |
| **H4** | 槽位落档身份 | `data/player.py` | `EquipmentSlot.uid` **回指** `ItemInstance.uid`（不是自生成 id） |
| **H4** | 存档迁移 | `storage/migrations.py` | `DB_SCHEMA_VERSION 2→3` + `migrate_v2_to_v3`（纯函数 `backfill_instance_uids`：inventory 补发、equipment 槽回指匹配行；幂等/无损/非法行跳过）。**新库直接具备**：`ensure_meta` 空库直写 3 + 新建实例天然带 uid（本步纯 JSON 级，无 DDL） |
| **H4** | 全链路按 uid | `core/equipment.py` / `core/inventory.py` / `commands/{use,gift,basic}_commands.py` / `assembly/runner.py` | 装配/卸下/聚合/副手判定按槽 uid 精确取行（空 uid 才回落首匹配）；背包 `remove_item(uid=)`/`find_by_uid`；使用/赠送精确取件；dict→实例归一保留 uid |
| **H5** | 随机流推进 | `core/rng_state.py`（新）+ `assembly/context.py:1714` + `assembly/runner.py` | 玩家级 `persistent_state.rng_state` 落档 + 逐指令推进；**照 `core/battle.py:5316,5409-5424` 既有做法**（`list(getstate())` + 递归归一 `setstate`），`battle.py` 零改动 |
| **H5** | 注入点兼容 | `assembly/context.py` | 无落档状态时 `ctx["rng"]` 原样返回 `rng_factory(qid)`（测试注入确定性 RNG 身份不变） |
| 回归 | 测试 | `tests/unit/test_batch40_instance_uid.py` / `tests/unit/test_batch40_rng_stream.py` | 同 id 两件不串（前后对照）；迁移无损/幂等/新库直具备；连续两次随机不同/重载可续/采集对拍；战斗 rng_state 续流逐值一致（多种子） |


### 批 41 落地登记（2026-09-19 · 已完成 A/B/D；C 留批 42；页脚批次串 =「批41 · 打造主流程」）

> 范围 = 图纸 / 材料 / 打造主流程（等级加权 · cost · 品质经验 · 品质抽取）。基础合成
> 100% 走公用层（`core/synthesis.py` 的 `preview_base_output` / `base_output` /
> `requirements_of` / `resolve_recipe`），打造只做深度层；随机用玩家级随机流（批40）。

| 节 | 落地项 | 落点 | 说明 |
|---|---|---|---|
| **A** | 参数段默认值 + 字段元数据 | `content/deep_craft_settings.py`（新） | `settings.deep_craft` 补 `craft_rules` / `quality_colors` / `quality_exp_by_color` / `blueprint_grades` / `quality_draw_table`（13 行共享阶梯）；图纸/材料字段（中文名/类型/范围/枚举/引用/必填） |
| **A** | items 字段承载图纸与材料 | `content/field_meta.py` | **图纸是 items 的一种**（不新造平行物品体系）：`blueprint_*` 13 字段 + 材料 `material_level` / `material_quality` / `craft_cost`；并入既有分组（不开新组） |
| **A** | 校验（引用/枚举/范围） | `content/validator.py` | 图纸档/产出/部位/配方引用存在性；材料等级/成本/品质；等级带 min≤max；随机条数 ≤2；概率阶梯顶档行（红仅行 10+）与行和提示 |
| **B** | 深度层引擎（纯函数） | `core/deep_craft.py`（新） | 等级 `Σ(材料等级×占比)`（角色权重按实际投入归一 → 完全同级=该级）；品质经验 `Σ(品阶基数×相性系数×件数边际)×种类奖励×互动乘子×全局衰减`；品质等级阈值；品质抽取（图纸档行偏移）；cost（工费+每种附加+Σ材料 cost）；`plan_craft` |
| **B** | 主入口 `/精造` | `commands/deep_craft_commands.py`（新） | `/精造 学习 <图纸>` / `/精造 <图纸>`（预览）/ `/精造 <图纸> <材料>*<数量>,…`；投料校验与基础产出走公用层；产出装备实例（带 uid）入 `inventory_instances` + 快照落 `persistent_state.deep_craft_*`；**文案全走模板表** |
| **B** | 接线 | `commands/parsers.py` / `assembly/router_setup.py` / `assembly/context.py` | 白名单 + 自由参数 + REGISTER_GROUPS（check_consistency 双向一致）+ `learned_blueprints` 持久化 |
| **B** | 防退化 N2 | `core/deep_craft.py::plan_craft` | ① 图纸等级带 + 主材料槽（不满足 → **拒绝**，口径：硬拒绝并给人话提示）；② cost 上限按图纸档缩放（超限拒绝） |
| **D** | 文案与页脚 | `core/templates/template_table.json` / `web/static/index.html` | 新增 16 条 `deep_craft_*` 键；页脚批次串同步 |
| **C** | 属性与套装词条抽取 + 相性池 | **留批 42** | 随机属性/随机套装词条/被动的相性变更与 `resolve_available_entries` 池抽取**未实装**；`blueprint_random_*` / `blueprint_passive` / `blueprint_fixed_set_affix` 字段**已登记与校验但引擎暂不消费**（先登记后接线，同批38 先例） |

### 批 41 待裁决项（原案/定稿未写清；本批取工程补白口径并显式标注）

| # | 项 | 本批口径 | 依据/影响 |
|---|---|---|---|
| G-1 | 图纸「学习」是否消耗图纸物品 | **消耗 1 张**（`/精造 学习`） | 原案 §12 只说「学习后」；未落可配开关 |
| G-2 | 装备实例的「需求等级」落档字段 | 打造等级写入快照 `deep_craft_last` / `deep_craft_instances`；实例本体无 `required_level` 字段 | A 路 T-9 建议 `use_level`/实例 `required_level`，H 组未落 → 待裁决 |
| G-3 | 品质经验的**互动乘子**幅度 | `Σ(结算后相性)/Σ(投入相性)`，clamp 到 `affinity_reactions` 声明区间（默认 [0.75, 1.15]） | 原案 §12 只说冲突/增幅，未给幅度 |
| G-4 | 等级加权「占比」口径 | **角色权重按实际投入的角色归一**（只投主材料 → 主材料独占 100%） | B 路 §1.1 自证示例（主35×60%+20/10/5→26.5）与其「自由槽按件数均分」不自洽 → 本批取自洽口径 |
| G-5 | 品质经验 **10 级可达性** | B 路默认参数（阈值 1080 / 品阶基数 [1,1.6,2.6,4.2,7,11] / slot_decay 0.25）下满投入 ≈323，**达不到 10 级** | 阈值/基数需按实际掉落经济校准（原案 §6 要求 1~10 级） |
| G-6 | 概率阶梯行窗口 | 银/金/彩品质等级 1~10 → 行 2~11 / 3~12 / 4~13 | B 路 §3.1「铜表上移 1/2/3 行」；用户原表 11/12/13 行与之一致 |
| G-7 | 基础合成的熟练经验 / 合成图鉴 | 深度打造落地**未走** `SynthesisEngine.synthesize`（其标准版产出契约 `quality=标准/traits=[]` 不可改写，见批39 接口 §三红线）→ 熟练经验与合成图鉴未入账 | 待裁决是否补一条「深度层熟练入账」接口 |
| G-8 | 顶档颜色判定 | 框架按 `quality_colors` **最后一项**为顶档（位置约定） | 校验器「红仅行 10+」据此；包改序需同步 |
| G-9 | cost 上限/下限量级 | 采用 B 路推演默认（铜 600 / 银 3900 / 金 18000 / 彩 86000；下限默认关） | N3 与 §1.6 均标「待实际数据校准」 |

### 批 41 边界说明（诚实登记）

- **C 节未实装**：随机属性 0~2 / 随机套装词条 0~2 / 装备被动受相性变更 / 相性池
  `resolve_available_entries` 抽取 —— 留批 42；相关字段已登记与校验，引擎暂不消费。
- **基础合成的熟练/图鉴未入账**（G-7）：投料校验与基础产出定义 100% 走公用层，但落地
  为深度层私有（实例须带 uid/品质/词条），未调用 `synthesize`。

### 批 42 落地登记（2026-09-19 · 已完成；= 批41 C 节补做；页脚批次串 =「批42 · 打造属性与相性池」）

> 范围 = **属性/套装词条随机抽取 + 相性池**（批41 §C 未实装部分）+ 装备被动相性变更。
> 随机一律走批40 玩家级随机流；池抽取一律走 `affinity.resolve_available_entries`
> （唯一入口，不得自己算池）；数值/概率/条数一律包声明；不注册新指令。

| 节 | 落地项 | 落点 | 说明 |
|---|---|---|---|
| **C-1** | 词条计划引擎（纯函数） | `core/deep_craft.py`（新函数 `resolve_main_sub` / `draw_count` / `plan_affixes` / `resolve_passives`） | 固定项照抄；随机条数图纸声明优先（回落 `craft_rules.random_*_count`）；随机内容按 `stat` / `set_affix` 载荷键从池抽（权重、不放回、同键去重） |
| **C-1** | `plan_craft` 集成 | `core/deep_craft.py::plan_craft` | 新增 `affinity_config` / `trait_defs` 入参；成功计划返回 `affinity` / `fixed_stats` / `random_stats` / `stats_bonus` / `fixed_set_affixes` / `random_set_affixes` / `set_affixes` / `passives`；词条抽取置于 quality 之后（批41 首位随机语义不变） |
| **C-2** | 实例新字段 + 构造点透传 | `data/item.py`（`affinities` / `set_affixes` / `passives`）+ `storage/repository.py` + `assembly/runner.py` + `commands/basic_commands.py` | 6 处非测试构造点逐处核对（清单见 `docs/深度打造_实现说明.md` §七）；序列化 `asdict` 自动含新字段 |
| **C-3** | `/精造` 接线 | `commands/deep_craft_commands.py` | 传 `affinity_config=settings` + `trait_defs=ctx["traits"]`；产物实例写词条/相性；快照附主副相性与随机词条 provenance；成功文案新增 `{main}/{sub}/{affixes}/{passives}` 占位 |
| **C-4** | 声明形状与门禁 | `content/field_meta.py` + `content/validator.py` | 池词条新增 `value` / `set_affix` 载荷键；池词条类型/范围、`blueprint_passive`→trait 引用、`blueprint_random_set_affix_pool`→池引用（门禁只增不减，未改既有 label/help/group） |
| **C-5** | 端到端 | `tests/unit/test_batch42_craft_affixes.py` / `..._e2e.py` | 池命中 / 无相性抽不出（负向）/ 联动覆盖专属池 / 副相性参与 / 条数边界 0 与 2 / 固定照抄 / 确定性 / 同 id 两件互不污染 / 构造点往返 / 批41 对拍 |

**批 42 待裁决项**（原案/定稿未写清；工程补白口径，详见实现说明 §六）：

| # | 项 | 本批口径 | 依据/影响 |
|---|---|---|---|
| C-1 | 图纸相性在相性累计中的位置 | 图纸相性作为**前置贡献**，其后按材料投入顺序 | 原案 §8 未写位置；影响互动触发顺序 |
| C-2 | `blueprint_random_set_affix_pool` 语义 | 在 `resolve_available_entries` 结果上按 `_pool` **收窄**（守「池查询唯一入口」红线） | 声明池不可达 → 抽不出 |
| C-3 | 固定/随机属性数值缩放 | `value` **照抄**，`value_rule` 暂不消费 | 原案 §3「数值由品质、等级决定」未落公式 |
| C-4 | `requires_affinity` 多相性列表 | 引擎支持、字段元数据限 `str`（门禁不减）；「主+副同时要求」用联动覆盖池 | 如需包内写列表须重定字段门禁 |
| C-5 | 套装词条 ↔ `forge_sets` 族 id 映射 | **未接**：既有套装结算按锻造节点 id 计件，打造产物无节点 id；`INS-07 set_tag` 未实现 | 产物套装词条暂不计入套装激活（缺口已登记） |

### 批 42 边界说明（诚实登记）

- **套装结算未接通**（C-5）：产物 `set_affixes` 已冻结进实例，但既有 `forge_sets` /
  `forge_set_skills` 不在打造链路上识别它；建议复用 `equip_mods._worn_defs` 按实例
  套装词条归并族件数（待裁决映射后接线）。
- **属性数值缩放未落**（C-3）：随机属性只取池词条 `value`；「品质/等级决定基础数值」
  待公式/元数据定稿。
- **强化词条（原案 §7）不在本批**：相性池已按「一个池同时服务随机属性/套装词条/强化
  词条」预留 `effect_ref` / `value_rule` 键，强化批消费。



---

## 七、用户补充（2026-09-19 三次）：**投入概率暴击**（解彩档 q10 可达性）+ 其余按建议默认

> 用户原话：「**cap 量级和彩档：可以加个投入时概率暴击的设计。其他按建议来吧，反正你别写成硬编码就行，不写死后续完全可以根据实际情况再调整。**」

### 裁定
1. **彩档品质 10 可达性 → 用「投入概率暴击」实现**（**不**把 cap 拉到 ≈19500，**不**强制扩充彩档材料表）：
   - 投料时按**概率**触发暴击；暴击给**额外品质经验**（倍率可配），使高档位图纸有机会冲破常规经验上限达到品质 10；
   - **全部参数化**：暴击概率、暴击倍率/加成、适用档位（或全档）、是否影响品质等级判定 — 一律**包声明可配**，缺省值给保守口径；
   - 需要：引擎（`core/deep_craft.py` 品质经验计算处）× 编辑器（参数可见）× **模板文案**（暴击提示）× 测试（**注入确定性 RNG 验证"暴击/不暴击"两条路径**，且**不 flaky**）；
   - 建议默认（可调）：暴击概率 = 低档更高、高档更低（如 铜 15% / 银 10% / 金 7% / 彩 5%）；暴击品质经验倍率 = ×2（先占位，待实测校准）。
2. **cap 量级**：采用合订本 A-1 建议 —— `cap = 等级承载成本 + 品质预算`，暂定 **铜 900 / 银 2100 / 金 4200 / 彩 8600**（全部可配）。
3. **A-2 材料密度**：每档 **≥6 种**（价格跨度覆盖该档 cap）——内容侧执行。
4. **A-3 精粹三问**：**不可交易**（绑定货币）· **可重置但不返还** · **分解不返还相性材料**。
5. 其余所有 🟡 项暂按合订本「我的建议」默认执行，**一律可配**，后续按实际情况调整（用户明确授权）。

### 批次影响
- 新增**批 44 · 投入概率暴击**（依赖批 41/42 的打造链路，插在强化批之后）
- 数值校准批（装备占比 60%）顺延为批 45；符文/淬炼/深炼金接入相应顺延
- **批 45 已完成（2026-09-20）**：装备面板倍率 2.25 + 怪物数值倍率 1.5（con 补偿 1.0828）；
  三档斩回对照见 `docs/批45_装备占比校准_测量报告.md` 与 §十。

---

## 八、批 43 落地登记（2026-09-20 · 已完成；页脚批次串 =「批43 · 强化六档与特殊词条」）

> 范围 = **H3 上限替换**（品质等级 6 档 +3/+6/+9/+12/+15/+18 替换 `max_by_rarity`
> 5/8/10/12）+ **原案 §7 每 4 级特殊词条**（相性池抽取，跨度可配）+ **词条族注册**
> （`data/gear_stats.py` 唯一源；冷却缩减占位）。
> 随机一律走批40 玩家级随机流（`ctx["rng"]`）；池抽取一律走
> `affinity.resolve_available_entries`（唯一入口，不得自己算池）；数值/档位/跨度一律包声明。

| 节 | 落地项 | 落点 | 说明 |
|---|---|---|---|
| **B** | 上限表替换（H3） | `content/enhance_models.py` + `content/veinborn/enhance.json` | `max_by_rarity` → `max_by_quality_level`（品质等级 → 上限）+ `legacy_quality_level_by_rarity`（旧档桥接）；曲线/石档延至 +18 |
| **B** | 上限引擎（纯函数） | `core/enhance_affix.py::resolve_cap` | 品质等级优先；旧档桥接；顶档封顶；`max(查表值, 当前等级)` 绝不降级 |
| **B** | 调用点替换 | `commands/enhance_commands.py::_max_for_quality`（+ `_quality_level_of_row`） | 三处调用（候选行 / `/强化` / `/强化信息`）统一走新函数 |
| **C** | 每 N 级词条 | `core/enhance_affix.py::special_affix_count` / `plan_special_affixes` | 应得条数 = `enhance_level // span`（span 包声明，默认 4）；只补差额、不重复键 |
| **C** | 池载荷键 | `data/affinity_keys.py::ENTRY_PAYLOAD_ENHANCE_AFFIX`（`enhance_affix`） | 唯一源落 data 层（core 抽取 + content 校验共用）；值须 ∈ `gear_stats` 键空间（validator 硬拦） |
| **C** | 实例字段 + 构造点 | `data/item.py`（`quality_level` / `enhance_affixes`）+ `repository` / `runner` / `basic_commands` / `deep_craft_commands` | 6 处构造点逐处核对（清单见实现说明 §七） |
| **A** | 词条族注册 | `data/gear_stats.py` | 4 个 `_pct` 键（回复强化/减益概率/增益概率/弱点伤害）+ 1 个占位键（冷却缩减）；`GEAR_PLACEHOLDER_KEYS` 显式不接引擎 |
| **D** | 编辑器/文案 | `content/field_meta_child_structure.py` + `content/veinborn/field_meta.json` + `template_table.json` | 上限表/桥接/跨度可见；新词条中文名+说明；强化结果文案走模板（`enhance_affix_gain` / `enhance_info_affix_row`） |
| **E** | 测试 | `tests/unit/test_batch43_enhance_tiers.py`（29 例）+ `test_enhance_commands.py` 回归改写 | 逐档上限 / 旧档兼容 / 4-8-12 / 跨度可配 / 无相性抽不出 / 占位不接引擎 / 曲线石档对拍 |

#### `max_by_rarity` 调用点清单（替换 / 桥接逐处）

| # | 位置 | 角色 | 批43 处理 |
|---|---|---|---|
| 1 | `commands/enhance_commands.py::_max_for_quality` | 上限唯一读取点 | **替换**：读 `max_by_quality_level` + 桥接；签名增 `quality_level`/`cur` |
| 2 | `commands/enhance_commands.py::_resolve_worn`（候选行） | 歧义候选显示上限 | **替换**：补品质等级与当前等级 |
| 3 | `commands/enhance_commands.py`（`/强化` 达顶守卫 / `/强化信息`） | 达顶 + 展示 | **替换**：同上；上限经 `max(查表, cur)` 保底 |
| 4 | `content/enhance_models.py`（`DEFAULT_MAX_BY_RARITY`/V7/V6/V2/V4） | 默认锚点 + 校验 | **替换**：`DEFAULT_MAX_BY_QUALITY_LEVEL` + 桥接表 + `special_affix_span`；V2/V4 覆盖 1..MaxR（默认 18） |
| 5 | `content/veinborn/enhance.json` | 包声明数据 | **替换**：删 `max_by_rarity`；新增上限表/桥接/跨度；曲线与石档延至 +18 |
| 6 | `content/field_meta_child_structure.py` + `content/veinborn/field_meta.json` | 编辑器结构/文案 | **替换**：删 `max_by_rarity` 节点；新增三键（含中文名/说明） |
| 7 | 测试 `test_enhance_commands.py` / `test_batch28_d6_transfer_removed.py` | 既有断言 | **替换**：新上限口径 + 旧档兼容断言（旧档普通 +5 → 上限 +6） |

#### 旧档 → 新档映射（工程补白 · 待裁决）

| 旧品质 | 旧上限 | 桥接品质等级 | 新上限 | 说明 |
|---|---|---|---|---|
| normal | 5 | 2 | 6 | 取「新上限 ≥ 旧上限」的最小档 |
| fine | 8 | 3 | 9 | 同上 |
| epic | 10 | 4 | 12 | 同上 |
| legendary | 12 | 4 | 12 | 同上（恰等于，不重复抬档） |

- **兼容口径（关键）**：旧装备**无 `quality_level`** → 走上表桥接读上限；其既有
  `enhance_level` **原样保留**（不降级、不清零，`resolve_cap = max(表值, 当前等级)`）；
  旧档装备可正常穿戴/展示/强化到新上限（报告附实证）。
- **待裁决**：桥接只取最小非降档（legacy 未声明时引擎回落默认表）；若策划希望旧
  legendary 获得更多上限空间（如 → 5）可改包声明表，引擎零改动。

#### 批 43 待裁决项 / 边界（诚实登记）

| # | 项 | 本批口径 | 依据/影响 |
|---|---|---|---|
| H3-1 | 品质等级 > 6（7~10）的上限 | 已声明档位之外 → **顶档封顶**（+18） | 原案 §7 只给 6 个上限值；待策划补 7~10 档 |
| H3-2 | 成功率曲线 / 石档 13~18 的数值 | 延续单调不增（12/10/8/6/5/4）；high 石档延至 18 | 原案/决策记录未给；**待实际数据校准** |
| H3-3 | 旧档桥接映射 | 最小非降档（见上表） | 决策记录仅要求「旧装备可读、不降级」 |
| A-1 | 词条池 `min_level` 取哪个等级 | 用**强化目标等级**（`to_level`）过滤 | 实例无装备等级字段；待裁决 |
| A-2 | 强化失败掉级时已得词条 | **保留**（不清词条） | 若需随掉级回收须另定；当前保「不变砖」 |
| A-3 | 强出一条词条后的随机序列消耗 | 走 `ctx["rng"]`（批40 玩家级流） | 同批40 H5；roll 亦已切玩家流（见下） |

**词条族承载口径与缺口（截至批43）**：

| 词条族 | 键 | 承载口径 | 状态 |
|---|---|---|---|
| 百分比加成 | 既有 `_pct`（atk_pct/dfn_pct/…） | 复用 `route_bonus_into` → `attributes.bonus["pct"]` | ✅ 既有链路复用 |
| 回复强化 | `heal_amp_pct` | `_pct` 拆层 + 聚合 + 展示 | ⚠️ 战斗治疗乘区**未接**（缺口） |
| 减益概率提升 | `debuff_chance_pct` | 同上 | ⚠️ 效果命中/施加概率**未接**（缺口） |
| 增益概率提升 | `buff_chance_pct` | 同上 | ⚠️ 同上（缺口） |
| 弱点伤害增加 | `weakness_dmg_pct` | 同上 | ⚠️ 弱点伤害乘区**未接**（缺口） |
| 冷却缩减 | `cooldown_reduction_pct` | **占位**：仅登记 + 展示，`route_bonus_into` 显式跳过 | ⛔ 明确不接引擎（原案 §7 要求） |

> 缺口建议：四族已可**存储/序列化/聚合/展示**；专属消费点应在战斗数值层接线
> （回复/弱点伤害 → 伤害/治疗结算；减益/增益概率 → 效果施加判定），建议独立成批。
> 本批**不为交差硬造**消费点。

---

## 九、批 44 落地登记（2026-09-20 · 已完成；页脚批次串 =「批44 · 投入暴击」）

> 范围 = **§七 用户三次裁定的「投入概率暴击」**：彩档品质 10 可达性用它实现（**不**拉高
> cap、**不**强制扩材料表）；落点 = 批41/42 已实现的 `quality_exp` / `plan_craft`。
> 随机一律走批40 玩家级随机流；概率/倍率/适用档一律包声明；门禁只增不减；
> **不暴击路径与现状逐字段一致**（缺省关 → 零随机消费）。

### 9.1 落地项

| 节 | 落地项 | 落点 | 说明 |
|---|---|---|---|
| **A** | 暴击判定引擎（纯函数） | `core/deep_craft.py::_resolve_quality_exp_crit` / `_apply_exp_cap` | 判定期在 `quality_exp` 内（品质等级判定前）；概率逐档可配、倍率逐档可配、适用档可配、可选 `additive_exp`/`affects_quality_level`/`rolls_per_craft`/`exp_cap`/`applies_to` |
| **A** | 主流程集成 | `core/deep_craft.py::quality_exp` / `plan_craft` | `quality_exp` 增 keyword-only `rng`/`grade`/`thresholds`；等级判定用 `exp_for_level`；计划新增 `quality_exp_base`/`crit`/`crit_mult`/`crit_chance` |
| **A** | 参数默认值 + 字段元数据 | `content/deep_craft_settings.py` | `craft_rules.quality_exp_crit` 默认值（**缺省关**）+ 12 个子字段元数据（中文名 + 说明卡，写清「替代拉高 cap 实现彩档 q10」） |
| **B** | 校验器 | `content/validator.py::_check_quality_exp_crit` | 结构/类型/范围/枚举红拦；启用但概率全 ≤0 → 黄提示 **Y-17**（只增不减） |
| **C** | 模板文案 + 接线 | `core/templates/template_table.json` / `commands/deep_craft_commands.py` | 新键 `deep_craft_crit_gain`（禁 emoji；`{crit}` 已登记冻结集）；命中时成功文案**追加**该行，未命中零变化；快照留痕 `crit`/`crit_mult`/`quality_exp_base` |
| **D** | 测试 | `tests/unit/test_batch44_crit.py`（23 例）/ `..._e2e.py`（4 例） | 两条路径（贴数值）/ 概率统计 1000 次 / 按档不同（15/10/7/5% + 边界）/ 回归对拍 / 编辑器可见 / 校验器 |
| **E** | 页脚批次串 | `web/static/index.html` | →「批44 · 投入暴击」，14 处批次串断言同步 |

### 9.2 参数一览（键名 / 默认值 / 范围）

| 键 | 默认 | 范围 | 说明 |
|---|---|---|---|
| `enabled` | `false` | — | 总开关；**缺省关**（不声明 = 零变化、不消费随机数） |
| `grades` | `[]` | 图纸档 id | 适用档；空 = 全档，可只开高档 |
| `chance_by_grade` | 铜 .15 / 银 .10 / 金 .07 / 彩 .05 | 0~1 | §七 建议值（低档更高、高档更低） |
| `chance_default` | `0.05` | 0~1 | 档未声明时的概率 |
| `mult_by_grade` | 四档 = `2.0` | ≥0 | §七 建议值（×2 占位，待实测校准） |
| `mult_default` | `1.0` | ≥0 | 档未声明时的倍率（1.0 = 不加成） |
| `additive_exp` | `0.0` | 任意 | 命中时的固定加经验 |
| `applies_to` | `quality_exp` | `quality_exp` | 作用目标 |
| `affects_quality_level` | `true` | — | false = 仅展示经验，等级按暴击前 |
| `rolls_per_craft` | `1` | 1~2 | 每次打造掷数 |
| `exp_cap` | `last_threshold` | `none`/`last_threshold`/数值 | 生效档位最终经验封顶 |
| `rng_stream` | `player` | `player` | 随机流标记 |

### 9.3 §七 裁定完成情况

| §七 裁定 | 状态 | 证据 |
|---|---|---|
| 彩档品质 10 用「投入概率暴击」实现 | ✅ | E2E：彩档常规 QE 991.65（q9）→ 暴击 ×2 封顶 1080 → **q10**；概率 5% |
| **不**拉高 cap | ✅ | `blueprint_grades[].cost_cap` 默认值**未改**（铜 600 / 银 3900 / 金 18000 / 彩 86000，仍为批41 值） |
| **不**强制扩材料表 | ✅ | 未新增任何材料字段/条目；24 种材料表由内容侧自理 |
| 全部参数化、可调 | ✅ | 12 个包声明参数（见 9.2） |
| 引擎 × 编辑器 × 模板 × 测试 | ✅ | 见 9.1 各节 |
| 注入确定性 RNG 验证两条路径、不 flaky | ✅ | 单测 `_FakeRng` 强制暴击/不暴击；概率统计固定种子 ±0.02（实测 ≤0.005） |
| 不改品质概率表 13 行阶梯 | ✅ | `quality_draw_table` 默认值**未改** |

### 9.4 待裁决 / 边界（诚实登记）

| # | 项 | 本批口径 | 依据/影响 |
|---|---|---|---|
| **R5** | 暴击参数落点 | 裁定 = `craft_rules.quality_exp_crit`（与 `interaction` 同级子对象） | 草案 v2 §5 R5「由批44 定」；与品质经验参数内聚，引擎零额外读取 |
| **R1** | 金档暴击倍率 | 沿用 §七「×2 占位」→ **金档 7% 亦可达 q10**（619.2×2=1238.3 ≥1080） | 若策划希望 q10 为彩档专属 → 把 `mult_by_grade.金` 改 `1.7`（包声明，引擎零改） |
| — | `enabled` 缺省值 | **本批取 `false`**（草案 §3.2 示例写 `true`） | 红线「不暴击路径必须与现状逐字段一致」+ 缺省保守：示例的 `true` 是**内容声明示例**；框架缺省若为 true，会对未声明包引入随机消费 → 改变既有品质抽取序列。翻开关即得草案建议值（15/10/7/5% ×2） |
| — | `mult_default` 缺省 | `1.0`（草案值） | 自有档位 id 的内容包须显式声明逐档倍率，否则只记暴击标记不加成（文档已警示） |
| — | 暴击与 `interaction`/`global_decay` 的顺序 | 先算完整 QE0（含互动/衰减），再乘暴击倍率 | 保证「不暴击 = 现状」；暴击是 QE 的最终乘区 |

### 9.5 边界说明（诚实登记）

- **`exp_cap` 作用于「生效档位」的最终经验**（命中与否都适用）：启用暴击且设
  `last_threshold` 时，生效档位的 QE 不超过阈值末项；未启用档位完全不受影响。
- **暴击不改等级 / cost / 相性累计**（原案 §5/§12 口径不变），仅改品质经验与由其决定的
  品质等级（`affects_quality_level=false` 时连等级也不改）。
- **随机数只在生效且概率 >0 时消费**；启用后 `rolls_per_craft` 个**全部消费**（次数确定，
  同种子同结果可复现）。

---

## 十、批 45 落地登记（2026-09-20 · 已完成；页脚批次串 =「批45 · 装备占比校准」）

> 范围 = **§三 补充 1**（用户 2026-09-19「把装备占比提高到 60%，其他数值不动」+ 主 agent 执行口径）。
> 目标：装备预算 `8→18`（×2.25）、白值 7 与 buff 5 绝对数值不动、面板总功率 `20→30`；
> 怪物侧同比例重校，**保持既有斩杀回合数与手感不变**；全部参数化、独立可回滚。
> 对照报告：`docs/批45_装备占比校准_测量报告.md`；复跑脚本：`scripts/batch45_measure.py`（种子 20260919）。

### 10.1 落地项

| 节 | 落地项 | 落点 | 说明 |
|---|---|---|---|
| **A** | 装备面板倍率（唯一落点） | `core/equipment.py::aggregate_bonus`（`:789`）+ `core/panel_budget.py` | 读时聚合乘「面板轴」`atk/dfn/hp`（flat + 对应 `_pct`）；**不改落档实例/内容数值**，改回 1.0 即回滚 |
| **A** | 装备定义→实例数值 | `data/gear_stats.py::extract_bonus`（不改） | 倍率不在此层，落档仍为原始值（可回滚/可对拍） |
| **B** | 怪物数值倍率 | `commands/battle_launch_commands.py::_enemy_combatant`（`:145`） | `hp/atk ×1.5`；`con` 仿射补偿 `(con+K)×def_factor−K`（保斩回）；其余键不动 |
| **C** | 参数默认值 + 字段元数据 | `core/panel_budget.py`（缺省 7:8:5 / 1.0）+ `content/field_meta.py` | `settings.panel_budget` / `settings.monster_scaling` 中文名 + 说明卡（一号原则） |
| **D** | 校验器 | `content/validator.py::_check_panel_budget` / `_check_monster_scaling` | 段结构/类型/负值红拦；三份全 0 → 黄提示 Y-16；`def_factor` 超带 → 黄提示 Y-17 |
| **E** | 内容包校准值 | `content/veinborn/settings.json` | `panel_budget.equip_stat_mult=2.25`、`monster_scaling={hp 1.5, atk 1.5, def_factor 1.0828}` |
| **F** | 测试 | `tests/unit/test_batch45_panel_calibration.py`（14 例） | 占比 40/60% / 只作用面板轴 / 缺省回归对拍 / 怪物倍率 / **斩回不变式** / 编辑器 / 校验器 |
| **G** | 页脚批次串 | `web/static/index.html` | →「批45 · 装备占比校准」，15 处批次串断言同步（旧串「批44 · 投入暴击」列入残留断言） |

### 10.2 参数一览（键名 / 默认 / 校准 / 范围）

| 键 | 默认 | veinborn 校准 | 范围 | 说明 |
|---|---|---|---|---|
| `panel_budget.white` | 7 | 7（冻结） | ≥0 | 面板预算·白值份（占比分母；不动） |
| `panel_budget.equip` | 8 | 8（冻结） | ≥0 | 面板预算·装备基准份；有效 = ×倍率 = 18 |
| `panel_budget.buff` | 5 | 5（冻结） | ≥0 | 面板预算·buff份（不动） |
| `panel_budget.equip_stat_mult` | 1.0 | **2.25** | ≥0 | 装备面板倍率（占 60% = 18/(7+18+5)） |
| `monster_scaling.hp_mult` | 1.0 | **1.5** | ≥0 | 怪物生命倍率（+50%，面板总功率同比例） |
| `monster_scaling.atk_mult` | 1.0 | **1.5** | ≥0 | 怪物攻击倍率（+50%） |
| `monster_scaling.def_factor` | 1.0 | **1.0828** | ≥0（建议 0.5–2.0） | 防御仿射补偿 `(con+K)×f−K`；按斩回不变式反解 `R/1.5` |
| `monster_scaling.def_k` | 100 | 100（缺省） | ≥0 | 防御系数常数 K（与战斗数值层 `defense.k` 同源） |

### 10.3 修前 vs 修后对照（种子 20260919；详见测量报告）

| 项 | 修前 | 修后 | 比值 |
|---|---|---|---|
| 装备预算份 | 8 | 18 | ×2.25 |
| 预算占比（装备） | **40.00%** | **60.00%** | — |
| 白值 atk/dfn/hp | 360 / 251 / 808 | **360 / 251 / 808** | 1.00（不动） |
| buff 半档 atk | 128.57 | **128.57** | 1.00（不动） |
| 装备 atk（原始→生效） | 486 → 486 | 486 → **1093.5** | ×2.25 |
| 中值面板 atk/dfn | 974 / 527 | 1582 / 872 | ×1.624 / ×1.655 |
| 黑脉熊 斩回（无/半/满） | 8 / **7** / 6 | 8 / **7** / 7 | 中值一致 |
| 黑晶巨魔 斩回 | 23 / **20** / 18 | 22 / **20** / 19 | 中值一致 |
| D主1·群山嚎主 斩回 | 27 / **24** / 21 | 26 / **24** / 22 | 中值一致 |
| 三怪 MC 中值斩回 | 3.955 / 10.607 / 13.372 | 3.954 / 10.605 / 13.367 | ≈1.000 |

### 10.4 边界 / 诚实登记

- **预算单位 +50% vs 实测面板**：设计口径面板总功率 `7:8:5 → 7:18:5` 即 `20→30`（+50%）；
  但真实毕业装（7 槽）装备 atk = 486 > 模型 8 份名义值 411 → **实测中值面板 atk 974→1582（+62.4%）**。
  怪物 HP/atk 按**设计 +50%**取（1.5），差额由 `def_factor` 补偿吸收——**中值档斩回逐只完全一致**。
- **三档偏差**：中值（主校准轴）**完全一致**；无buff 档装备收益占比更高 → 斩回略降（Boss 27→26、
  精英 23→22，普通 8→8）；满buff 档 buff 摊薄装备收益 → 斩回略升（+1）。9 格中 6 格一致、3 格 ±1，
  原因 = buff 填充度改变「装备在面板中的实际占比」；单倍率无法同时锁三档（已如实登记）。
- **既有存档迁移口径**：倍率在**聚合时**生效；已穿戴玩家需下一次**穿戴/重算**（穿脱任一件）才反映新倍率；
  新建/新穿戴直接生效。默认不配置 → 1.0，既有行为零影响。
- **不改动**：白值 7（`stats.json` base/growth）、buff 5 语义、品质概率表、暴击参数、既有战斗公式结构。


---

## 十一、批 46 落地登记（2026-09-20 · 已完成；页脚批次串 =「批46 · 符文地基」）

> 范围 = `/root/deliverables/符文系统_实现口径.md` **§五 43-A · 符文地基**（数据 + 孔位复用 +
> 镶嵌状态落档），**不接战斗效果**（1 阶数值落 `gear_stats`、2/3 阶战斗接线 = 后续批 43-B/D）。
> 上游：原案 §9 符文附魔（L37-38）；决策依据 H1（孔位镶嵌 = 共享基础设施）/H2（品质正交）。

### 11.1 落地项

| 节 | 落地项 | 落点 | 说明 |
|---|---|---|---|
| **A** | 符文定义解析层（纯引擎） | `core/runes.py`（新） | 三阶**独立刻度** `rune_tier_of`（不读 quality，H2）；`by_equip_type` default+覆盖；`effects` 声明；`socket_count_of`；3 合 1 纯函数 `resolve_rune_upgrade` |
| **A** | 符文数据模块登记 | `content/veinborn/runes.json`（`[]`）+ `loader._KIND_FOR_MODULE["runes"]="rune"` + `field_meta` ModuleMeta(kind=rune, ns=rune_lib) + `module_catalog` + `ctx["runes"]` | list 模块，含「跨装备类型差异表」→ 独立成册（不塞 items 表）；编辑器可见（一号原则） |
| **A** | 字段元数据 + 编辑器 | `content/field_meta.py`（`runes_fields` + `settings.rune_sockets`）+ `content/veinborn/{manifest,field_meta}.json` | 阶范围 1-3；by_equip_type（动态键空间）；effects 引用 effects 注册表；孔位缺省段可见可配 |
| **A/B** | 专项校验器 | `content/rune_models.py`（新，`validate_runes` RUNE-01~07）+ `validator` 分派 | 阶枚举红拦 / default 缺失红拦 / stats 自造键红拦 / effects 悬空引用红拦 / family 必填 / 未知类型黄提示 |
| **B** | 镶嵌/拆卸引擎入口 | `core/jewel.py`（`mount_rune`/`unmount_rune`/`active_rune_sockets`/`rune_socket_defs`） | 复用孔位骨架；**绕过**珠的 `slot_accepts` 槽级门票；与装饰珠**共用同一孔位数组**（一槽一物、互斥） |
| **B** | 镶嵌状态落档 | `data/player.py::persistent_state["rune_sockets"]`（键 = `ItemInstance.uid`）+ `assembly/context.py` 镜像 + `storage/migrations.py::backfill_rune_sockets` | 挂 uid（**不挂 `EquipmentSlot`**——卸装会丢，`core/equipment.py:47-49` P1-2）；自由 dict 缺补，**不需** DB schema 升级 |
| **C** | 3 合 1 执行器 | `core/upgrade.py`（`UPGRADE_SUBTYPES` + `_exec_rune` + `_rune_tier_of`） | 复用 `_exec_jewel` 形状：3×同阶同 id → +1 阶、**必成**、**禁跳级**、`_commit` 原子提交；档位走独立三阶解析（不读 quality 序号） |
| **D** | 测试 | `tests/unit/test_batch46_runes.py`（28 例） | 数据/校验/落档随实例/迁移幂等/3 合 1/装饰珠逐字段对拍/副手继承/6 构造点静态审计 |
| **D** | 页脚批次串 + 文档 | `web/static/index.html` + 14 处断言 | →「批46 · 符文地基」；旧串「批45 · 装备占比校准」列入残留断言；字段元数据门禁重定基线 `1e2f68c → 0d55330` |

### 11.2 玩法默认（全部可配、可改）

| 项 | 默认 | 落点 | 说明 |
|---|---|---|---|
| 阶位刻度 | `tier ∈ {1,2,3}` | 符文定义 `tier` | **独立**于既有 quality 四档（H2）；按定义无等级门槛 |
| 3 合 1 成功率 | **必成** | `_exec_rune` | 对齐既有 `_exec_jewel`；禁跳级（产出阶必须 == 输入阶 + 1） |
| 孔位数 | **三孔全开** | `settings.rune_sockets.default_count`（默认 3） | 已登记 `slots.json` 用登记数组（共用）；未登记 → 缺省 N 孔 |
| 差异类型键 | `items.type` + default+覆盖 | `by_equip_type` | 命中类型条目**顶层浅覆盖** default（`stats` 整块覆盖）；未命中回落 default |
| 与装饰珠关系 | **共用同一孔位数组**（一槽一物、互斥） | `slot_defs` | 珠占用槽 → 符文 `slot_full`；符文不新增第二套孔位 |
| 系统总闸 | `settings.deep_craft.enabled`（默认关） | `JewelSystem.runes_enabled` | 口径 §二.5：无需新总闸；关闭时镶嵌/拆卸/升阶全拒（零行为） |
| 副手失活 | **零额外工作继承** | `offhand_inactive`（唯一判定点） | `active_rune_sockets` 副手折算件 → `[]`（同珠口径） |

### 11.3 边界 / 诚实登记

- **不改既有炼金/珠镶嵌行为**：`JewelSystem.mount`/`unmount`/`slot_accepts`/`battle_trigger_*` 零改动；
  新增符文方法为**追加**。对拍：`tests/unit/test_jewel.py` 既有例全绿 + 本批「珠镶嵌逐字段对拍」
  （加/不加 `rune_sockets` 结果 dict 完全一致）。
- **无 DB schema 升级**：口径 §二.4 明示 `persistent_state` 为自由 dict → 无需 v3→v4。落地为
  「`backfill_rune_sockets` 唯一补缺口径 + 装配路径（`make_context`）惰性挂回」——**新库直具备**
  （新玩家装配即得空容器）、**旧档幂等补**（已有状态零改动）。**不改 storage codec 读写** →
  `codec_roundtrip`/`db_roundtrip`「逐字段一致」不变量零变化（batch40 测试全绿）。
- **`ItemInstance` 6 处非测试构造点**：4 处 uid 透传（`commands/basic_commands.py:1572`、
  `assembly/runner.py:720`、`core/equipment.py:772`、`storage/repository.py:194`）；2 处合法无 uid
  （`commands/shop_tx.py:181` 全新购买实例 → `__post_init__` 补新 uid；`core/equipment.py:778`
  旧实例无 uid 字段的 TypeError 兜底）。**静态审计测试** `test_item_instance_construction_sites_audit`
  断言「恰好 6 处、uid 缺失仅限这两个合法落点」。
- **差异表覆盖语义**：命中类型条目**浅覆盖** default（口径 §二.1 示例 armor_body 只给 `dfn`、不带
  default 的 `atk`，即「按类型给一份完整效果」）。若某类型只想微调单键，需在该条目里写全。
  此语义为工程补白（口径未逐字写死合并粒度），可改。
- **本批不做**（留后续批，口径 §五 43-B~D）：1 阶数值落 `gear_stats`/`aggregate_bonus` 消费段、
  2 阶「重伤」与层数型增益两处机制缺口、2/3 阶战斗接线、拆卸/交易/战斗中插拔指令壳（沿用既有珠口径）。
- **待裁决沿用**（口径 §四 Q1~Q12 未定）：Q5 类型键用 `items.type`（本批默认，可切 `slot`）、
  Q7 三阶「偏向性」机制（`bias` 字段只登记不解释）、Q8 3 合 1 输入/输出身份（本批同 id 输入、
  产出由配方声明，支持显式 `rune_tier` 回退）、Q9 孤儿清理（分解/出售入口未清理 `rune_sockets[uid]`，
  登记为待办）、Q11 战斗中插拔（沿用珠禁插拔）。

---

## 十二、批 47 落地登记（2026-09-20 · 已完成；页脚批次串 =「批47 · 符文数值与类型差异」）

> 范围 = `/root/deliverables/符文系统_实现口径.md` **§三 效果承载建议 + §五 43-B**（1 阶数值
> 落键空间 + 跨装备类型差异生效）。上游：原案 §9 R2/R4；决策依据 H1（孔位镶嵌 = 共享基础设施）。
> 依赖批46 地基（定义解析 / 孔位复用 / uid 落档），**不改战斗核心**。

### 12.1 落地项

| 节 | 落地项 | 落点 | 说明 |
|---|---|---|---|
| **A** | 1 阶数值求值（差异解析唯一处） | `core/runes.py::rune_stats_of` / `sum_rune_stats` | default + `items.type` 覆盖（顶层浅覆盖，`stats` 整块替换）；键白名单 `GEAR_NUMERIC_KEYS`（唯一源）；布尔/非数值/0/占位键丢弃；差异解析**只在此处**发生，不展开成多份 |
| **A** | 键空间口径（**零新增键**） | `data/gear_stats.py`（docstring 段） | 1 阶符文**复用**既有 FLAT/PCT/COMBAT 键族（口径 §三.2「禁止自造键」）；本批新增词条键 = **0**；上限=编辑器提示域，非引擎钳制 |
| **B** | 符文数值贡献段（唯一聚合入口） | `core/equipment.py::aggregate_bonus`（新增段）+ `EquipmentEngine(runes/items/jewel)` | 读 worn uid → `jewel.active_rune_sockets`（**唯一孔位读取入口**，副手失活自动继承）→ `rune_stats_of` → 同一 `route_bonus_into` flat/pct 路由（**不新开第二套聚合**） |
| **B** | 生产接线 | `commands/basic_commands.py::EquipmentEngineAdapter`（+`_equip_engine` 兜底）+ `assembly/context.py` | 注入 `ctx["runes"]`/`ctx["items"]` + `JewelSystem(settings)`；缺数据源/总闸关 → 零贡献 |
| **C** | 孔位互斥对向收口 | `core/jewel.py::mount` + `_rune_slot_taken` / `_worn_uid` | 批46 只做「符文→珠」方向；本批补「**珠→符文**」方向：珠 `mount` 命中同件符文占用 → `slot_full`；两方向共用 `_rune_slot_taken`（**一处判定**）；解析不到穿戴 uid 不误拦（既有珠行为逐字段一致） |
| **D** | 测试 | `tests/unit/test_batch47_runes_numeric.py`（19 例） | 求值/装-卸前后数值/**跨类型两组数值**/未声明类型回落 default/双向互斥/副手失活/无符文逐字段对拍/适配路径/校验器键空间 |
| **D** | 页脚批次串 + 文档 | `web/static/index.html` + 15 处断言 | →「批47 · 符文数值与类型差异」；旧串「批46 · 符文地基」列入残留断言；字段元数据门禁重定基线 `0d55330 → 本批末提交` |

### 12.2 玩法默认 / 口径（全部可配、可改）

| 项 | 本批口径 | 落点 | 说明 |
|---|---|---|---|
| 差异类型键 | `items.type` | `EquipmentEngine._equip_type_of` | 口径 §四 Q5 本批**取默认项①**；未声明该类型 → 回落 default；可切 `slot` |
| 覆盖合并粒度 | 顶层浅覆盖（`stats` 整块替换） | `core/runes.resolve_by_equip_type` | 沿用批46 R-2 语义（口径 §二.1 示例：armor_body 只给 dfn） |
| 符文数值键 | 复用既有键族，**零新增** | `data/gear_stats.py` | flat=点 / pct=百分点 / COMBAT 按既有封顶；自造键由校验器 RUNE-05 红拦 |
| 副手符文 | **不生效**（空） | `jewel.active_rune_sockets` | 复用批38 闸门零额外分支；主手件不受影响。**注**：口径 §五 43-B 验收③曾写「按 `offhand_scale` 折算」——与本批「副手失活=不生效」冲突，**以任务口径（失活继承）+ 批46 已实装的 `active_rune_sockets → []` 为准**，此处如实登记口径差 |
| 与装饰珠关系 | 共用同一孔位数组，**双向**互斥 | `slot_defs` + `_rune_slot_taken` | 先装者占用，后装者 `slot_full` |
| 系统总闸 | `settings.deep_craft.enabled` | `JewelSystem.runes_enabled` | 关闭 → 符文贡献零（即便行仍在） |

### 12.3 边界 / 诚实登记

- **不改战斗核心**：本批只接 `aggregate_bonus`（读时聚合）+ 孔位互斥判定；`core/battle.py`、
  `core/effects.py` 零改动。2 阶「重伤」（治疗削减）与层数型增益两处机制缺口、2/3 阶战斗接线
  = 后续批 43-D。
- **无新增 `data/gear_stats` 键**：1 阶符文数值**全部**落既有 FLAT/PCT/COMBAT 键；「新注册词条键
  一览」= 0（复用语义见 `data/gear_stats.py` docstring 批47 段）。若日后符文需专属词条 → 仍在本表
  加一行（唯一源），不得在符文定义/引擎自造。
- **回归对拍**：`EquipmentEngine` 缺 runes/items/jewel 或符文行为空 → `aggregate_bonus` 与基线
  **逐字段一致**；`JewelSystem.mount`/`unmount` 在「无穿戴 uid / 无符文占用」时结果 dict 逐字段不变。
- **不改 `content/veinborn/runes.json`**（仍 `[]`）：符文为内容包数据，测试一律内存/临时内容根构造，
  不写真实内容包（纪律）。1 阶数值内容配置由后续内容批填充。
- **孤儿 `rune_sockets[uid]`** 未清理（沿用批46 Q9 登记）。


---

## 十三、设计原则（用户 2026-09-20 拍板）：**双向修正轴优先**

> 用户原话：「**受伤的减治疗效果其实可以是加强治疗效果填复数这种设计**」「**补充：记得多做成这种双向修正轴**」

**原则**：装备/效果的特效**优先做成「一条参数轴 + 双向取值」**，而不是"减 X"与"增 X"各造一套专门机制。

**例**（用户给的起点）：
- **治疗修正轴** `healing_received_mult`：`0.5` = 重伤（减疗 ≤50%）；`1.0` = 原样；`1.5` = 强化治疗。
  → **一个轴覆盖"重伤"与"增疗"两种玩法**，配置即可，不新增专门 debuff/buff。

**应优先通用化的轴（候选，供设计批扩充）**：
| 轴 | 语义 | 取值方向 |
|---|---|---|
| `healing_received_mult` | 受到的治疗量 | <1 减疗 / >1 增疗 |
| `damage_dealt_mult` / `damage_taken_mult` | 造成/承受伤害 | 双向 |
| `cooldown_mult` | 冷却时长 | <1 缩短 / >1 延长（**与"冷却缩减"占位键的关系**：缩减排期时统一到乘法轴） |
| `duration_mult` | 状态/效果持续 | 双向 |
| `stack_gain_mult` / `stack_cap_delta` | 层数获取速度 / 层数上限 | 双向 / 增减 |
| `action_speed_mult` | 行动条速度 | 双向 |
| `cost_mult` | 资源消耗 | 双向 |
| `drop_rate_mult` / `exp_mult` | 掉率/经验 | 双向 |

**要求**：
1. 设计批（本次搜集的整理阶段）**必须优先输出"双向轴"版本**；确需单向的，说明为什么不能双向；
2. 轴的**取值范围/上下限可配**（包声明），不写死；
3. 轴进 `data/gear_stats.py`（词条键唯一源）或对应通用层，**不许各玩法自造**；
4. 与既有机制**重叠的先归并**（例：既有 `cooldown_reduction_pct` 占位 → 统一为乘法轴时一并处理）。

---

---

## 十四、批 48 落地登记（2026-09-20 · 已完成；页脚批次串 =「批48 · 符文特殊效果」）

> 范围 = `/root/deliverables/符文系统_实现口径.md` **§五 43-C + 43-D**（3 合 1 端到端 +
> 2/3 阶特殊效果与战斗接线），含口径 §〇 结论 7 的**两处机制缺口**（重伤治疗削减 / 层数型增益）。
> 上游：原案 §9 R6/R7/R8；决策依据 H1（孔位镶嵌 = 共享基础设施）/H2（品质正交）；
> 依赖批46 地基 + 批47 数值/差异。**首次改动战斗核心（`core/battle.py`）+ `core/effects.py`。**

### 13.1 落地项

| 节 | 落地项 | 落点 | 说明 |
|---|---|---|---|
| **A** | 重伤（治疗削减）通道 | `core/effects.py` | 缺口①承载**选择候选②**：在 `heal` 计算处读状态聚合（`status_stat_modifier_sum` 唯一收口，语义键 `HEAL_TAKEN_STAT`）——**复用既有 `stat_modifier` 状态动作族**，不新造 L0 动作/第二套减益；`v×(1+heal_taken/100)`，下限 0，封顶复用 S6（可配） |
| **A** | `_aggregate_boost` 收口下沉 | `core/effects.py` / `core/battle.py` | 原 battle 内联遍历下沉为纯函数 `status_stat_modifier_sum`（`_status_raw_of` 为其唯一源）；battle 只保留 S6/S7 封顶 → 既有路径逐字段不变 |
| **B** | 层数型增益修正 | `core/effects.py` | 缺口②：聚合按实例 `stacks` **乘算**（`_stacks_of` 归一：缺失/0/非法 → 1）；上限 = 状态包声明 `max_stack`（未声明回落 `config.stack_default_max`）→ 双重可配；再叠 S6 `cap_boost` |
| **C** | 2/3 阶战斗接线 | `core/runes.py` / `core/rune_battle.py`（新，组合层）/ `core/equipment.py` / `core/event_dispatcher.py` / `core/battle.py` / `commands/battle_launch_commands.py` | `rune_effect_refs_of`（差异解析后的 effects 引用）+ `core/rune_battle.active_rune_effect_refs`（ctx 级取数；**拆独立模块避免 `runes ↔ equipment` 模块级环**，G0 TC-03 绿）+ `EquipmentEngine.active_rune_effects`（同一 worn 枚举 + 同一 `active_rune_sockets`）+ `dispatch_event(extra_candidates=…)`（缺省 None 零破坏）+ `BattleEngine._rune_candidates` / `_dispatch_event` 合并符文候选；`_player_combatant` 注入 `rune_effects`（无符文不新增键） |
| **D** | 3 阶偏向性符文 | `core/runes.py` / `core/equipment.py` / `content/rune_models.py` / `content/field_meta.py` | R-8：`bias={affinity,bonus_pct}`，宿主**主/副相性**（`core/affinity.rank_affinities`）命中 → 该符文数值 ×(1+bonus_pct/100)；RUNE-07 由「待裁决 note」升级为形状校验 |
| **E** | 3 合 1 端到端 + 闸 | `core/upgrade.py` | `resolve_upgrade_recipe` 解析 `synth_allowed`；`_exec_rune` 新增闸①`synth_allowed=false` → `runes_deep_locked`、闸②`settings.deep_craft.enabled` 非 true（且 ctx 带该段）→ `runes_disabled`；1→2→3 逐阶、禁跳级、满阶拒、失败原子回滚 |
| **F** | 测试 / 文档 | `tests/unit/test_batch48_runes_effects.py`（33 例）+ `docs/深度打造_实现说明.md` §十二 + 本文件 + CHANGELOG + 页脚批次串（html + 14 个测试文件断言） | 重伤前后 / 层数逐次 / 2 阶接线 / 3 阶偏向 / 3 合 1 / 回归对拍 |

### 13.2 缺口承载选择与理由（口径 §〇 结论 7 / 任务要求）

| 缺口 | 选择 | 理由（为何不改/如何改引擎） |
|---|---|---|
| ① 重伤（治疗削减）无承载 | **在 heal 计算处读状态聚合**（候选②），语义键 `heal_taken` | 复用**既有** `stat_modifier` 状态动作族 + 既有 `apply_status` 叠层/持续/衰减/驱散全链；不新增 L0 动作类型（候选① `heal_modifier` 会与 `stat_modifier` 双轨）。聚合收口与 battle 的 boost 聚合**同一个** `status_stat_modifier_sum` → 无双实现 |
| ② 层数型增益不生效 | **修正 `_aggregate_boost`（×stacks）** | 语义上「n 层 = n×值」是 S3 stack 框架的定义；改动面**仅限** `stack_frame="stack"` 且带 `stat_modifier` 的状态——已核实**既有内容包与测试中此类状态 = 0**（`content/*/statuses.json` 无 `stack_frame:stack`）；非 stack 框架 `stacks` 恒 1 → 乘子 1，逐字段对拍一致（测试 F 组提供批48 前算法逐字复制基准） |
| ② 附带：同来源不叠层（S5） | **符文接线侧区分来源**（`侧@行动序号`），**不改 `apply_status`** | 既有 S5「同来源同侧只保留一个」是引擎既有语义（既有测试 `test_c3_stack_to_max_stack` 明写「叠层需不同来源」）。改 S5/S3 次序会改引擎行为；符文接线属新增代码，把「每次攻击=一次独立施加」的来源区分放在接线侧 → 引擎风险最小（回归零改动） |

### 13.3 玩法默认 / 口径（全部可配、可改）

| 项 | 本批口径 | 落点 | 说明 |
|---|---|---|---|
| 重伤削减值 | 内容包状态声明（`value`，百分点） | `stat_modifier{stat:"heal_taken"}` | 引擎只给上下限（S6 `max_boost_pct` 默认 ±100） |
| 层数上限 n | 状态 `max_stack`（缺省回落 `stack_default_max`） | 状态定义 | 引擎不写死；S6/S7 为第二道封顶 |
| 每次攻击触发 | 符文引用条目 `trigger="action_start"` | `runes.json` effects 引用 | 触发时点枚举 = `EVENT_POINTS`；计数上限沿既有（10/行动、99/场） |
| 效果容器是否带 trigger | **不写**（只写在符文引用条目） | `effects.json` | 防全局事件分派对所有单位触发（框架无内容包业务名） |
| 3 阶偏向 | `{affinity, bonus_pct}`，命中主/副相性 → 数值 ×(1+bp/100) | `runes.json` bias | Q7 默认口径（任务指定「偏向某相性」，对接批38）；工程补白 R-8 |
| 3 合 1 门槛 | `synth_allowed` / `settings.deep_craft.enabled` | `core/upgrade.py` | ctx 无 `settings`/`deep_craft` 段不额外收紧（批46 兼容） |
| 3 合 1 成功率 | 必成（沿用批46） | `_exec_rune` | 对齐 `_exec_jewel`；Q2 待裁决 |

### 13.4 边界 / 诚实登记

- **本次改战斗核心**（`core/battle.py`、`core/effects.py`）：`_aggregate_boost` 行为变更为
  「×stacks」，已给**逐字段回归对拍**（批48 前算法逐字复制为基准，覆盖 single/dual/level_based、
  int/百分号串/负值/bool/越界、单/多状态混合 → 全等；仅 stacks>1 按设计放大）。`dispatch_event`
  新增 `extra_candidates` 参数缺省 None → 既有调用零变化。
- **PvP 未接符文效果**：`core/pvp._combatant_of` 的 combatant 不注入 `rune_effects`（本批只接
  PvE `/锁定` 链路，含 `dummy_commands` 复用同函数）；PvP 符文是否生效 → **待裁决**（口径未写）。
- **3 合 1 无专属指令壳**：本批补执行器端到端 + 系统闸；`/符文升阶` 指令名需先做 H6 命名冲突
  扫描 → **未做，登记待办**（内容包 `recipe.json` 配 `subtype=rune_upgrade` 后即可经既有
  upgrade 执行器消费）。
- **效果容器 `trigger` 未进校验器**：`trigger` 是否 ∈ `EVENT_POINTS` 目前**不校验**（content 不得
  import core；枚举跨层引用未做 data 化）→ 登记待办；未知 trigger 的引用不会触发（安全空转）。
- **偏向仅放大数值**：3 阶 `bias` 不放大 `effects`（是否按相性切换/增强效果 → **待裁决**）。
- **`content/veinborn/runes.json` 保持 `[]`**：测试一律内存构造，不写真实内容包（纪律）；
  2/3 阶符文内容配置由内容批填充。
- **架构分层**：`active_rune_effect_refs` 不能放 `core/runes.py`（`core.equipment` 顶层 import
  `core.runes` → 会成 `runes ↔ equipment` 环，G0 TC-03 红），故拆 `core/rune_battle.py` 组合层
  （core → core，无人反向 import）；`scripts/check_architecture.py` 已复跑 **ARCH-OK**。
- **字段元数据门禁**：`bias` 子字段登记变更后跑 `scripts/compare_field_meta_migration.py`
  → **0 差异**（runes 模块当前 0 条目 → `detail/runes` 不入快照，门禁不受影响；无需重定基线）。

## 十五、批 49 落地登记（2026-09-20 · 已完成；页脚批次串 =「批49 · 测试 flake 根治」）

**任务**：擦除 `tests/unit/test_m125_formula_assembly.py::test_conftest_wrapper_still_works`
全量偶发失败（隔离跑绿）。

**根因（已复现，非猜测）**：仓库 `tests/` 是 **PEP 420 隐式命名空间包**（无 `tests/__init__.py`）。
该用例体内 `from tests.conftest import load_formula_params` 走 import 系统；当 `sys.path` 上存在
同名 **regular package** 时，规范规定「regular package 优先于 namespace package」，解析落到宿主包。
本仓 DSH 宿主自带 `/usr/local/lib/hermes-agent/tests/__init__.py`（该目录下 `conftest.py` 无
`load_formula_params`），于是全量跑（宿主路径入 `sys.path`）偶发：

```
ImportError: cannot import name 'load_formula_params' from 'tests.conftest'
             (/usr/local/lib/hermes-agent/tests/conftest.py)
```

隔离跑（无宿主路径）→ `tests.conftest` 命中本仓 → 绿：与「全量偶发、隔离稳过」的现象一致。
历史同类修复见 `scripts/verify/verify_m0.py`（P2-3：改由 REPO 直推 fixtures 路径，不 import
`tests.conftest`）；另有 `docs/消息模板重构/_taskbooks/批6_路R/P.md` 记录「并发可能出现
`tests.conftest` 解析偶发红」。

**修法**：`tests/conftest.py` 增 `conftest_formula_loader` fixture（返回 conftest 内同一个薄包装
函数），用例改经该 fixture 注入、**不再运行期 import `tests.conftest`**（并加「薄包装 == 生产
读取器」同源对拍，断言更严）；附静态回归用例 `test_no_runtime_import_of_tests_conftest`
（扫描 tests 树禁该 import 模式复发）。不跳过、不 xfail、不放宽断言。页脚批次串 + 14 个测试
文件断言同步为「批49 · 测试 flake 根治」，旧串「批48 · 符文特殊效果」列入残留断言（门禁更严）。


## 十六、批 50 落地登记（2026-09-20 · 已完成；页脚批次串 =「批50 · 特效轴地基」）

**任务**：**批 50 · 特效轴地基**（P + 小 K，**不含任何消费点**）。口径 =
`特效整理设计_1_修正轴全集.md` §2/§3 + `特效整理设计_3_落点与分期.md` §1.0/§1.2
+ §二「批48」（该设计文档的批号是**旧编号**，与本仓库「批48 · 符文特殊效果」无关；
本批叫**批50**）+ 本文件 **§十三「双向修正轴优先」**（用户拍板）。

### 16.1 落地项

1. **键族**（`qbot_rpg/data/gear_stats.py`）：新增 **EFFECT 档** `GEAR_EFFECT_KEYS`，
   由逐轴登记表 **`EFFECT_AXIS_SPECS`（唯一源）** 派生。本批只登记轴全集定级
   **P0/P1 的 15 条轴**（P0 8：X17/X16/X02/X01/X27/X21/X23/X24；P1 7：
   X20×2/X22/X30/X34/X35/X03），**不一次全上**。`GEAR_NUMERIC_KEYS`
   = FLAT + PCT + COMBAT + **EFFECT**；`GEAR_DISPLAY_KEYS` 自动覆盖。
2. **每轴唯一消费点**：`EFFECT_AXIS_SPECS[*].consumer`（+ `consumer_note` 写收口范围与
   归并事项）。**空 consumer = 不得登记**，由 `tests/unit/test_batch50_effect_axes.py`
   钉死；未登记项见 §16.4。
3. **归并/别名**（轴全集 §2.1）：`legacy_alias` 声明 `heal_amp_pct`→`healing_done_pct`(+1)、
   `immune_dmg`→`damage_taken_pct`(−1)、`weakness_dmg_pct`→`damage_dealt_pct`(+1)、
   `cooldown_reduction_pct`→`cooldown_pct`(−1)、`debuff_chance_pct`/`buff_chance_pct`→
   `status_chance_pct`(+1)。**旧键自身链路本批不动**（零行为变化）。
4. **声明段** `settings.effect_axes`：每轴 `min / max / default / display（mode + 中文名 +
   双向说明）/ stack / legacy_alias`；`normalize_effect_axes()` 做**读时参数化**
   （缺省表 ⊕ 包声明），范围与默认值**可配、不写死**（对齐 §十三 与
   `core/panel_budget` 的「包声明驱动 + 缺省恒等」风格）。
5. **`route_bonus_into` 例外**：EFFECT 键按 **COMBAT 同规则留 flat、不进属性 pct 层**
   （理由与 `pierce_pct` 同：否则 `healing_received_pct` 被拆成
   `pct["healing_received"]` 误算/丢失）。
6. **`combatant_updates` 桥接行**（`EFFECT_TO_COMBATANT`）：把 EFFECT 键接入战斗桥，
   **本批只桥接、不求值**（战斗侧零读取点）；只映射**非零项** → 缺省 0 / 未配置
   → 输出与引入前**逐字段一致**；封顶一律 `None`（钳制归包声明 + 消费点，引擎不写死）。
7. **校验器**（`qbot_rpg/content/validator.py`）：
   `_check_effect_axes`（settings 分派）+ `_check_effect_axis_values`
   （items/equipment/runes 分派，独立钩子、不依赖 settings 是否存在）——
   **越界红拦**（default 越界 / min>max / 内容取值越界，R-2）、
   **未知轴黄提示**（Y-18，不硬拦）、**键名不得与面板三轴 stem 冲突红拦**（R-4，
   stem 取自 `data.gear_stats.PANEL_AXIS_STEMS`——唯一源上移 data 层，
   `core/panel_budget.PANEL_AXIS_KEYS` 由它派生，值/语义不变）。
8. **字段元数据 + 编辑器**：`settings.effect_axes` 段登记（中文名 + 说明）；
   items/equipment 词条表纳入 EFFECT 键，字段元数据（区间可负、单位、中文名、双向说明）
   全部由登记表派生（`_gear_effect_field`），**零前端改动**即可见。

### 16.2 零行为变化（对齐 §十三 与批45 的 `mult==1.0` 不动原则）

- **缺省即恒等**：EFFECT 键默认 0（= ×1.0）；未声明 `settings.effect_axes` → 缺省表；
- **逐字段对拍**（`tests/unit/test_batch50_effect_axes.py`）：
  `route_bonus_into` / `combatant_updates` 对既有键与**批50 之前的参考实现**逐字段相等；
  `combatant_updates({}) == {}`；`_combatant_of` 未配置时 combatant 无任何轴键；
  **一场真实战斗的结算快照**（同种子两次）归一化实例 uid 后逐字段一致，且快照不含轴键；
- 既有内容包过校验器**零新增红/黄**（真实 `content/veinborn` 复跑）。

### 16.3 未完成 / 存疑（如实登记）

- **本批没有任何消费点**（红线要求）：15 条轴目前**写了不生效**——但与
  `cooldown_reduction_pct` 的区别是每条轴都已写明「唯一消费点（待接）」，
  接线批次照此落地即可；编辑器与校验器已可用。
- **编辑器没有「滑条」控件**：仓库既有数字控件是 `type="number"` 输入框
  （`index.html`），**不存在字段滑条**；本批的「滑条/输入」按「沿用既有控件风格」
  取**既有数字输入**，范围经说明卡「建议范围」行展示（`_range_semantic(fm)`）。
  新增滑条属新前端代码，本批不做（若需要请拍板）。
- **P0/P1 中 4 条未登记**（lifesteal / reward_mult / stack_potency / buildup）：原因见 §16.4；
  其中 `buildup_rate_pct` 属**缺消费点**，须先立「异常累积段」。
- **`< -100` 治疗反转成伤害**属待裁决（默认关）；`stack_mode` 的 `mult` 聚合口径待裁决；
  本批只在声明里保留 `stack` 枚举与说明，不实现。

### 16.4 未登记的 P0/P1 轴及原因

| 未登记项 | 原因 |
|---|---|
| `lifesteal_pct` / `drain_to_resource`（X12，P1） | 吸血两条并存路径（`battle.absorb_hp` + `effects.lifesteal`）须先归并 → **无唯一消费点**；归并即改动既有行为 |
| `reward_mult{scope}`（X43，P1） | 一条轴多 scope 实例，声明形状待裁决；消费点分散三处 → 无唯一收口 |
| `stack_potency_pct`（X25，P1） | 消费点 `_aggregate_boost` 现按状态实例求和、**不乘 stacks**，接轴须先改聚合本身（M），与符文 2 阶共用 |
| `buildup_rate_pct`（X26，P1） | **缺消费点**：异常累积段（buildup）尚不存在 → 按「无消费点的轴不得登记」不登记 |
| `action_recovery_pct`（A-2 / X29） | 轴全集 §3 定级 **P2**（超范围）+ 须归并 enrage/fatigue 状态硬编码倍率 |
| `action_speed_pct`（X28） | 设计裁定与 X29 二选一，**只留 A-2** → 永不作为独立轴登记 |
| `roll_mode` / `pierce{target}` / `stack_mode` | 聚合规则与方向参数，**不是数值轴**；穿透是既有键扩展 |
| I01 / I02 / I04 | 接线类基础设施（触发归属 / 条件变量 / 套装阈值），非数值轴 |

### 16.5 验收证据（复跑）

```bash
# 批50 单测（29 例）
pytest tests/unit/test_batch50_effect_axes.py -q -o addopts=""
# 全量
pytest tests/ -q -o addopts=""
# 架构 / ruff / 字段元数据门禁
python scripts/check_architecture.py
.venv/bin/ruff check .
PYTHONPATH= python scripts/compare_field_meta_migration.py
```

- 字段元数据迁移门禁：本批为**纯新增**（settings 段 +1、items/equipment 词条 ×15、标签/说明）
  → 按门禁语义 **soft 新增允许且逐条列出**；**硬差异（删除/改值）= 0**，
  `settings.effect_axes` 段为新增条目，`settings` 未配置计数 48 → **49**（既有断言同步）。
- 页脚批次串 →「批50 · 特效轴地基」，14 个测试文件断言同步，旧串列入残留断言。

---

## 十七、批 51 落地登记（2026-09-20 · 已完成；页脚批次串 =「批51 · 触发归属与事件补点」）

**任务**：**批 51 · 触发归属与事件补点**（K，P0 缺口；**装备被动系统的前置**）。口径 =
`特效整理设计_3_落点与分期.md` §二「批 49」（该设计文档的批号是**旧编号**——设计里的
「批48」= 仓库批50「特效轴地基」，设计里的「批49」= **本批批51**）
+ `特效整理设计_1_修正轴全集.md` §3 P0 第 1 项（I01 触发时点 ＋ 归属过滤）
+ `装备被动_实现口径.md` §3.3 方案 A + N-5。

### 17.1 落地项

1. **归属过滤**（`core/event_dispatcher.py`）：`_iter_candidates` / `dispatch_event`
   新增两个**只作用于纯效果事件分支**的作用域参数：
   - `owner_effect_ids` = 本侧**拥有**的 effect id 集（方案 A 的 `active_effect_ids`）；
   - `claimed_effect_ids` = 本场**被任一持侧认领**的 effect id 全集（**实现优化**：
     未被认领的效果视为全局效果、对任意侧照常触发，直接对冲口径 §批49 风险栏
     「owner 过滤过严会静默吞掉既有全局触发」）。
   两者皆缺省 → **全库扫描旧行为**（零变化基线）。
2. **事件补点 `on_kill`**（`core/event_dispatcher.py` + `core/battle.py`）：
   `EVENT_POINTS` 补 `on_kill` = **我击杀敌**（派发给**击杀者侧**）；与既有 `death`
   = **任一侧死亡**（派发给**死者侧**）语义相反、方向不同。两者都在唯一死亡判定点
   `_death_check_side` 派发，由 `dead_mark` 门控 → 每次击杀恰好一次；同归于尽 /
   战斗已结束 → 不派发 `on_kill`。覆盖「连段套中击杀」与「BOSS 死亡立即结束」两条路径。
3. **枚举唯一源下沉 data 层**（`qbot_rpg/data/event_points.py`）：
   `EVENT_POINTS` 唯一源由 `core/event_dispatcher` 迁到 `data/event_points`，
   原路径**原样再导出**（`from qbot_rpg.core.event_dispatcher import EVENT_POINTS` 不变）。
   理由：content 层校验器要按分层契约 `content → {data}` 校验 `trigger` 取值域，
   **不得**反向 import core（与批50 把 `PANEL_AXIS_STEMS` 落 data 层同一取舍）。
   既有 16 点**原样保序**在前的，`on_kill` 追加在末尾。
4. **校验器**（`qbot_rpg/content/validator.py`）：新增 `_check_effect_trigger_points`
   （effects / runes 两处声明面）：**未知时点黄提示 Y-19**（拼错/自造/未来时点 = 永不触发，
   不硬拦）、**非字符串红拦 R-1**、缺省/空串放行。
5. **装备触发型效果的最小接线**（`core/equip_mods.py` + `commands/battle_launch_commands.py`
   + `data/gear_stats.py`）：
   - `data/gear_stats.OWNED_EFFECT_IDS_KEY` = combatant 侧归属集**键名唯一源**；
   - `core/equip_mods.worn_passive_effect_ids`：已穿戴件实例 `passives`（批40 `uid`
     精确定位；`_worn_defs` 复用副手失活过滤）→ `traits.effects` → effect id 集
     （保序去重；**只读、不落缓存**）；
   - `_player_combatant` 有装备被动才写该键（无 → **不新增键**，既有 combatant 逐字段一致）；
   - `core/battle._owner_scope`：**任一侧声明即本场启用归属**（owner ∪ 未认领全局），
     两侧都无 → `(None, None)` = 旧行为。
6. **文档**：`深度打造_实现说明.md` §十四（作者向：时点/归属/条件怎么写、
   `death` vs `on_kill` 对照表、与被动系统的边界）+ 本登记。

### 17.2 零行为变化（红线）

- **未声明归属 → 与现状逐字段一致**（对拍测试）：`owner=None, claimed=None` 时
  `_iter_candidates` 候选与「本批之前的全库扫描」在**既有 16 个时点**上逐条一致；
  `dispatch_event` 的 side_effects 与快照后态亦逐字段一致（
  `tests/unit/test_batch51_trigger_ownership.py`）。
- **装备接线缺省不新增任何键**：无 `passives` → `_player_combatant` 不写归属键 →
  战斗侧 `_owner_scope` 返回 `(None, None)` → 全库扫描；且 `_combatant_of` 的
  逐键断言不受影响（既有键集不变）。
- **`on_kill` 是新增派发点，不是新增副作用**：无内容声明 `trigger=on_kill` 时
  候选为空 → `[]`；既有 16 时点的派发点**一处未动**。
- **复用既有机制**：`actions` 容器 / `trigger` / `overrides` / `chance` 三态 /
  次数与链深上限 / 状态与印记体系 / `condition_engine` 门控，全部沿用，未新开容器。
- **框架零内容包业务名**：新增代码只有 `owner`/`claimed`/`effect id`/`passive` 等
  框架词；测试只用匿名 id（`fx_*` / `t_*` / `s_*`）与内存 registry，**不写真实内容包**。

### 17.3 未完成 / 存疑（如实登记）

- **其余缺失时点未补**（`on_struck` / `on_block` / `on_crit` / `on_interrupt` / `on_cc` /
  `on_synergy`）：口径 §I01 写「优先只补 `on_kill`」，且每个时点都需要**新的派发点**，
  而新派发点 = 行为改动，须单独批次 + 战斗结算对拍。**本批登记为待裁决**：
  是否一次性补齐（含各自派发点）由用户拍板。
- **条件变量未扩**（`self_hp_pct` / `self_resource_pct` / `is_crit` / `is_kill`）：
  口径 §批49 范围③列为同批项，本批按「P0 缺口优先 + 不越界」**未做**（属 I02）；
  触发型效果目前可用既有 `condition_engine` 变量集写条件。
- **被动声明字段未登记**：`ItemInstance.passives` 字段存在（批40/41）但 `items`/`equipment`
  的 `passives` **内容字段与校验**（`field_meta` / ref→trait）未登记——属被动系统首批；
  本批接线只**消费**实例上已有的 `passives`。
- **`claimed` 口径为可优化实现**：口径文档只写方案 A 的单集合；本批加了「未认领 =
  全局」的第二集合，用于对冲「静默吞掉全局触发」风险。若用户希望**严格**「任一侧声明归属
  后全局效果一律不触发」，把 `claimed_effect_ids` 传 `None` 即可（引擎已支持），
  属一行改动 → **待裁决**。
- **归属集在敌方侧未接线**：敌人定义有 `traits` 字段，但本批只接玩家侧
  （`_player_combatant`）；敌方归属属后续批（与怪物被动/词缀同批）。

### 17.4 验收证据（复跑）

```bash
# 批51 单测（31 例：归属不串 / on_kill 两侧 / 装备端到端 / 校验器 / 16 时点对拍 / 结算快照）
pytest tests/unit/test_batch51_trigger_ownership.py -q -o addopts=""
# 事件分派 + 战斗接线 + 符文/装备回归
pytest tests/unit/test_event_dispatcher.py tests/unit/test_event_dispatcher_battle.py \
       tests/unit/test_batch48_runes_effects.py tests/unit/test_gear_wiring.py -q -o addopts=""
# 全量
pytest tests/ -q -o addopts=""
# 架构 / ruff / 字段元数据门禁
python scripts/check_architecture.py --path .
.venv/bin/ruff check .
PYTHONPATH= python scripts/compare_field_meta_migration.py
```

- 字段元数据门禁：本批**不改任何内容字段元数据**（只新增 data 层常量与校验）→
  与批50 基线**逐字段一致**（新增 0 / 删改 0）。
- 页脚批次串 →「批51 · 触发归属与事件补点」，全部批次串断言同步，旧串列入残留断言。

---

## 十八、批 52 落地登记（2026-09-20 · 已完成；页脚批次串 =「批52 · 治疗与承伤双向轴」）

**任务**：**批 52 · 治疗 / 承伤双向轴接线**（M + K，P0 核心玩法）。口径 =
`特效整理设计_3_落点与分期.md` §二「批 50」（该设计文档的批号是**旧编号**——设计里的
「批48」= 仓库批50「特效轴地基」，设计里的「批50」= **本批批52**；仓库已有的
`tests/unit/test_editor_batch52_visual.py` 是**编辑器批52**，与本批同名不同域）
+ `特效整理设计_1_修正轴全集.md` §5-D2（X17/X16）/ §5-D3（X02）
+ `特效强度预算_设计.md` §四（红线守护清单）。

### 18.1 落地项

1. **受疗轴 `healing_received_pct` 接线（唯一收口）**：`core/effects.heal_apply`
   （`effects.py:1760`）——批48 重伤状态通道（`stat_modifier{stat: heal_taken}`，经
   S6 `cap_boost` 封顶）与新轴**同轴相加** → 合计按 `settings.effect_axes` 声明区间钳制
   → `round(基础 × (1 + 受疗/100))`。**双向**（<0 减疗 / >0 增疗）；**负治疗**：
   合计 ≤ −100 时治疗转伤害（返回负值，下界由包声明，默认 `min=-200` 允许反转）。
2. **出疗轴 `healing_done_pct` 接线 + 归并旧键**：同一收口按 **source（施疗侧）** 二次取值；
   旧悬空键 `heal_amp_pct`（批43 登记、消费点为 0）在战斗桥
   `data.gear_stats.combatant_updates(flat, pct=None)` 中按 `axis += sign × 旧值`
   归并进本轴（仅 pct 层旧键；`immune_dmg` 等 COMBAT 旧键由其自身消费点换算，不在此并入）。
3. **多源一次改全（唯一收口纪律）**：5 处 heal 源全部经 `heal_apply`——
   `heal` L0 原子（`effects.py:2050`）/ absorb_heal（`:1303`）/ regen（`:1350`）/
   lifesteal（`:1994`）/ `battle.absorb_hp` 吸血（`battle.py:4852`）。负治疗时扣血下钳 0。
4. **承伤轴 `damage_taken_pct` 接线（收敛口径 D3(b)）**：`battle._damage_taken_mult`
   （`battle.py:4120`）= **承伤乘区唯一求值处**：
   `mult = 1 + (damage_taken_pct − clamp(immune_dmg,0,100)) / 100`（**减伤/易伤一轴**，
   旧键别名只乘一次、不双计）；读时按声明区间钳制。调用点替换既有 `immune_dmg` 块
   （`battle.py:4744`），作用于防御/格挡/乱数之后的 raw。
5. **声明段接线**：`settings.effect_axes` → `assembly/context.py:1858` →
   `commands/battle_launch_commands.py:509` → 引擎配置 → `EffectRuntime.config`
   （治疗收口读它钳制）；`data/gear_stats.EFFECT_AXES_KEY` + `effect_axis_value`
   （`:553`）是唯一读点辅助，**不写死区间**（§0.2 R-5）。
6. **文档**：`深度打造_实现说明.md` §十五（作者向：三条轴怎么写、负治疗、多源清单、
   红线复算表）+ 本登记 + `CHANGELOG`。

### 18.2 承伤收敛口径的实现方式与对既有行为的影响（本批核心裁决）

**实现方式**：`damage_taken_pct` 为主乘区；`immune_dmg` 作为其负半轴别名
（`pct = −immune_dmg`）在**同一个函数**内求值并只乘一次（`_damage_taken_mult`）；
`_status_damage_mult`（倒地 `damage_mult`）经 `include_status` 形参可在同一函数内并入。

**对既有行为的影响（对拍证据）**：

| 组合 | 旧行为 | 本批 | 结论 |
|---|---|---|---|
| 仅 `immune_dmg=25` | ×0.75 | ×0.75 | **逐值一致**（旧链路保留） |
| `damage_taken_pct=-25` | （不存在） | ×0.75 | 新增 |
| 无 immune / 无轴 + 破位 status | `int(raw×broken×status)` | `int(raw×broken×1.0×status)` | **逐位一致**（status=1.0 时恒等） |
| 破位 + 非零 `immune_dmg` | `int(raw×broken×status)` 后 `round(×(1−imm))` | `int(raw×broken×(1−imm)×status)` | **可能相差 −1 点**（同一次截断，见下） |
| 两者并存（−25 与 imm25） | （不存在） | ×0.5（同轴相加） | 非 ×0.5625（不双计） |

- **收敛实现**：status `damage_mult` 与主乘区**在唯一求值处 `_damage_taken_mult` 内合并**
  （`include_status=broken_hit` 门控沿用既有破位窗口），破位路径一次整型截断：
  `int(raw × broken × mult)`。**唯一受影响组合 = 「破位 + 非零 `immune_dmg`」**，因截断序
  改变可能少 1 点（实测 raw∈[50,200]、broken=status=1.5、imm∈{10,25,33,50} 的 148 个组合，
  差值绝对值恒为 1）。其余全部逐位一致。
- **为什么不违反一号红线**：`immune_dmg` 是本轴的**负半轴别名**（`pct = −immune`）——用
  `immune_dmg` 就是「以旧写法配置了承伤轴」；**真正的「不配置轴」= 无 `immune_dmg` 且无
  `damage_taken_pct`**，此时 `mult` 恒等（含 status=1.0 时逐位一致），全仓内容包对素材键
  使用量为 0 → 实机与三档斩回逐字段零变化（§18.3）。
- `effects` 的 `mitigation` 阶段按批50 `consumer_note` 明文（`gear_stats.py:207-212`）
  **不动**：它只做减伤、与护盾 / 保底伤害交织，混入易伤会错。故本批的「收敛」=
  `damage_taken_pct` + `immune_dmg` 归并为**唯一乘区**；`mitigation` / status `damage_mult`
  保持**条件实例**（既有门控与乘序不变）。

### 18.3 零行为变化（红线 C1）与红线复算

- **缺省对拍**：`heal_apply` 未配置轴 → 原值；`_damage_taken_mult` 无轴无免疫 → `1.0`；
  同种子战斗结算快照逐字段一致且不含任何特效轴键（`test_batch52_heal_damage_axes.py` D 组）。
- **旧键不突变**：`combatant_updates(flat)`（缺省不传 pct）与批51 及此前逐字段一致；
  `route_bonus_into` 未改一行（`heal_amp_pct` 仍落 `pct["heal_amp"]`）。
- **斩杀回合复算**（`scripts/batch45_measure.py`，种子 20260919，修前 = eb21d7d）：
  普通 8/7/7、精英 22/20/19、Boss 26/24/22（无/半/满档）**逐格 0 变化**；
  `equip_share = 60.00%` 不变。三条轴**全在生存/受疗侧，不改玩家输出** → C4/C5/C8 通过。
- **C6/C7（档位红线 / 生存轴专项）**：工具口径不含生存轴，且**全仓内容包对特效键
  使用量为 0** → 实机档位不变、Boss ≥40 回合不被压缩。**潜伏风险报告**：
  `特效强度预算_设计.md` §1.6 的典型生存 build 在实机口径把 Boss 压到 ≈39–41（贴死下沿）；
  本批只接线不填预设，缓解建议 = 批53 启用 `effect_budget` 上限 / 方案 C 怪物侧上浮，
  **在内容开始用特效词条之前先立闸**。**本批不擅自调任何数值平衡。**

### 18.4 未完成 / 存疑（如实登记）

- **`mitigation` 阶段仍独立**（未并入承伤乘区）：这是**有意**取舍——批50 `consumer_note`
  明文「不得动 effects 的 `mitigation` 阶段」（与护盾 / 保底伤害交织）。status `damage_mult`
  已并入唯一乘区（见 18.2）；若后续要连 `mitigation` 一起收敛，需先重画护盾/保底的结算序，
  属结构改动，**另立批次**。
- **`healing_received_pct` 的「反转开关」以声明下界表达**（默认 −200 允许反转）：
  批50 轴登记卡的 help 曾写「是否开放反转属待裁决（默认关）」；本批按用户口径
  「放开负治疗（下界 −200 = 治疗转伤害）」实现为**配置驱动**——不想要反转的包把
  `settings.effect_axes.healing_received_pct.min` 声明为 `-100` 即可。**待裁决**：
  是否需要一个显式布尔开关（当前无）。
- **absorb_heal（护盾吸收回血）/ regen 无明确施疗方**：只吃受疗轴、不吃出疗轴
  （施疗方不可知）。若后续要给出疗，须在 `defenses.absorb` / `regen` 上登记来源侧。
- **`lifesteal` L0 动作纳入收口**（设计 4 源清单未列它，但它是 heal）：施疗方 = 受疗方 =
  `attacker`，与 `absorb_hp` 同口径。若设计认为不该纳入，去掉该行即可（一行）。
- **轴值施加源仍限装备/符文/状态**：状态侧只有既有 `heal_taken`（受疗）；
  「施疗削弱」「易伤」目前只能来自装备/符文轴键，**没有对应的状态动作**——需要时另立批次。

### 18.5 验收证据（复跑）

```bash
# 批52 单测（20 例：受疗双向三例 / 负治疗 / 出疗归并 / 承伤双向 / immune 不双计 / 零变化）
pytest tests/unit/test_batch52_heal_damage_axes.py -q -o addopts=""
# 批48/50/22 回归
pytest tests/unit/test_batch48_runes_effects.py tests/unit/test_batch50_effect_axes.py \
       tests/unit/test_batch22_gear_combat.py tests/unit/test_batch22_effect_switches.py -q -o addopts=""
# 全量（0 failed）
pytest tests/ -q -o addopts=""
# 红线复算（修前 vs 修后）
python scripts/batch45_measure.py --json
# 换包验收 + ruff
python scripts/editor_verify_packs.py
ruff check .
```

- 字段元数据门禁：本批**不改任何内容字段元数据**（只新增 data 层函数与 core 侧消费）
  → 与批51 基线逐字段一致（新增 0 / 删改 0）。
- 页脚批次串 →「批52 · 治疗与承伤双向轴」，全部批次串断言同步，旧串列入残留断言。

---

## 十九、批 53 落地登记（2026-09-20 · 已完成；页脚批次串 =「批53 · 时序与资源轴」）

### 19.1 范围与落点

批50 登记 15 条特效轴，批52 接治疗/承伤 4 条；本批接**时序 / 资源 / 结算**余下 7 条，全部复用
**既有唯一消费点**（不新开乘区/管线）：

| 轴 | 消费点 | 侧 / 口径 |
|---|---|---|
| `cooldown_pct` | `battle.py` 技能冷却起算（α3 同乘区相加后一次缩放） | 行动者 |
| `status_chance_pct` | `effects.apply_status` R1 命中判定 | source（施加者） |
| `status_resist_pct` | `effects.apply_status` resist 判定（扩展 `resist_table`，加算百分点） | target（承受者） |
| `stack_gain_pct` | `effects.apply_status` stack 框架增量 | source |
| `stack_cap_delta` | `effects.apply_status` `max_stack`（加算，下钳 ≥1） | target |
| `action_bar_shift` | `battle._after_actor_action` → `hasten_actor`/`delay_actor` | 行动者（正提前/负延后） |
| `resource_cost_pct` / `resource_gain_pct` | `resource_axis` 既有门禁/扣款/入账（新增 `mult` 形参） | 行动者 |
| `crit_damage_pct` | `battle._resolve_damage_action` 会心乘区（crit_roll 后等比缩放） | 攻击者 |

### 19.2 关键裁定

1. **冷却占位旧键激活（与 §三 C-05 的偏差，用户口径优先）**：C-05 建议旧键「未声明 alias 即不
   生效」以绝对避免旧内容突变；用户 2026-09-20 明确要求「旧键仍可用且不双计」→ 本批**激活**。
   实现取最小冲击面：`route_bonus_into` 契约**不变**（占位键仍不进 flat/pct，批43/批50 回归
   断言继续绿），新增 `route_legacy_aliases_into_flat` 在**聚合入口**换算一次
   （`reduction_p ⇔ cooldown_pct −p`）。**证据**：全仓内容包对 `cooldown_reduction_pct` 使用量
   为 0（仅测试构造）→ 现网内容零突变。
2. **状态抵抗用「加算百分点」而非 `×mult`**：X22 原文写 `×mult`，但既有管线是
   `effective = hit_rate × (100 − resist)/100` 的**减法**；对 `resist=0` 的乘法恒等（装备轴
   形同无效）。故按用户在任务书中点名的「**扩展既有 resist_table**」落成加算百分点（0..100 钳制），
   与 X05 `hit_pct` 同族。**登记待裁决**：若口径坚持 `×mult`，需另改 `apply_status` 的抵抗模型。
3. **层数两轴的侧**：`stack_gain_pct` 取 source（施加者 build 让 debuff 叠得快）、
   `stack_cap_delta` 取 target（持有者抗上限）。口径未写侧别，按语义最小自洽取值并登记。
4. **溢出转层换算率**：`>100%` 的溢出按**每满 100% 折 1 层**（`floor(overflow/100)`），仅 stack
   框架生效；口径未写换算率，取保守确定值，登记待裁决。
5. **层数上限半边**：只接 `apply_status` 的 `max_stack`；`marks.max_stack_of` 半边**不接**（印记
   是独立容器），登记待裁决。
6. **`counter_refund` 不并入 `action_bar_shift`**：并入会改变防反/闪反返还的既有隐性口径 →
   本批只接轴，登记待裁决。
7. **会心倍率与离散档位**：`crit_damage_pct` 等比缩放 `crit_roll` 的三档倍率；`super_crit_lv`
   仍由 `crit_roll` 内部离散叠加，`elem_crit_lv` 只作用元素通道、**不并入**本轴。

### 19.3 待裁决（本批不做，只登记）

| # | 待裁决 | 影响 |
|---|---|---|
| **D2** | `action_speed_mult`（X28）vs `action_recovery_mult`（X29）**二选一** | 二者数学互为倒数，并存 = 指数速度。用户未裁决 → 本批**不登记/不接线/不写测试** |
| D-new-1 | `status_chance_pct` 溢出换算率 / `stack_gain_pct` 是否支持「向上取整可配」 | 影响层数型 build 上限 |
| D-new-2 | `X22` 抵抗用 `×mult` 还是「加算百分点」 | 影响高抵抗 + 高命中双 build 的表达 |
| D-new-3 | `stack_cap_delta` 是否覆盖 `marks.max_stack_of` | 影响依赖印记层数的 build |
| D-new-4 | `counter_refund` 是否收编为 `action_bar_shift` 实例 | 影响防反/闪反节奏口径 |
| D-new-5 | `debuff_chance_pct` / `buff_chance_pct` 是否需要 `scope={debuff,buff}` 分治 | 影响「只提减益命中」的 build |

### 19.4 验收证据（复跑）

```bash
# 批53 单测（A~G 共 25 例）
pytest tests/unit/test_batch53_timing_resource_axes.py -q -o addopts=""
# 全量（0 failed）
pytest tests/ -q -o addopts=""
# 红线复算（修前 vs 修后；--json 逐字节一致）
python scripts/batch45_measure.py --json
# 换包验收 + ruff（对 qa_ctb_baseline_ruff.txt 零新增）
python scripts/editor_verify_packs.py
ruff check .
```

- 字段元数据门禁：本批**不改任何内容字段元数据**（只改 data 层函数 / core 消费点 / 文案
  的 `consumer_note`，不进 `display.help`）→ 与批52 基线逐字段一致（新增 0 / 删改 0）。
- 页脚批次串 →「批53 · 时序与资源轴」，16 处批次串断言同步（旧串「批52 · 治疗与承伤双向轴」
  仅保留在 §十八 / CHANGELOG 的**历史条目**中，非断言）。

---

## 二十、批 54 落地登记（2026-09-20 · 已完成；页脚批次串 =「批54 · 预设集与结算轴」）

口径：`特效整理设计_2_框架预设包.md` §1/§2/§4 + `特效整理设计_1_修正轴全集.md`
§3 P1 第 16 项（X43）+ 既有机制 `content/entry_presets.py`（批17）。实现说明见 §十七。

### 20.1 范围与落点

| # | 交付 | 落点 |
|---|---|---|
| 1 | **框架特效预设集**（26 条 · 设计 §1.1~§1.3） | 新增 `qbot_rpg/content/effect_presets.py`（唯一源 `FRAMEWORK_EFFECT_PRESETS`） |
| 2 | **复用既有预设机制**（不新造一套） | `entry_presets.framework_presets` 追加 `effect_presets_for` 投影；同 `merge_entry_presets` / 同 `framework_preset_key_errors` / 同 `web/api.new_entry_detail` |
| 3 | **编辑器可见** | 模块 `equipment`「新建条目」预设下拉（零前端改动：走既有 `newPresetHtml`） |
| 4 | **校验**（越界红拦 / 引用轴存在） | `effect_presets.effect_preset_errors`（纯函数；轴须在册、三档取值须在有效区间） |
| 5 | **结算/奖励轴核查** | **不登记、不接线**（结论见 §20.2-4），理由 + 证据进实现说明 §17.8 |

### 20.2 关键裁定

1. **宿主模块 = `equipment`**：特效预设面向「有 `slot` 的装备条目」；`items` 模块的**六种
   通用品类预设逐字段不变**（批17/18 契约不破）。预设只声明在框架代码，**不写进任何内容包**。
2. **同族两方向 = 两条预设**：同 `conflict_group` + 反 `direction` → 互斥；**执行仍走既有
   装备互斥链**（`equipment.validate_slot_exclusions` / `excludes`），**不新增互斥引擎**
   （防「预设级互斥」与「装备级互斥」两套并存）。
3. **档位 = 编辑器落 `normal`，`advanced`/`rare` 仅声明**：设计 §1.0「档位只改数值、不改轴」；
   「按档掷取」属掉落/强化系统，本批**不接线**（登记待裁决 D-54-1）。
4. **结算 / 奖励轴 `reward_mult{scope}` 不登记（如实报告，不为交差硬造）**：
   核查既有消费点后确认**无可复用的唯一收口**——
   ① 标量（`coins`/`gem`/`rep`）在 `core/reward._SCALAR_KEYS` 的 `dispatch_reward`，被
   任务/签到/NPC/钓鱼/成就/PvP **共用**；② 战斗 `exp` 在 `settle_battle_rewards` 走
   `LevelUpEngine.gain_exp`（**不走** `dispatch_reward`）；③ 掉率在 `roll_death_drops`。
   且扁平数值键空间无法承载 `scope` 而不「造多键」（违反设计「勿造三键」明文）或新造
   「对象型轴」（违反「复用既有机制」）→ **不登记**（与批50 §13.7 登记一致）。
5. **数值全部取设计档位示意**（`_mult → _pct` 精确换算 `pct=(mult-1)*100`：×1.10⇔+10、
   ×0.85⇔−15、×0.50⇔−50），**不擅自调任何数值平衡**（红线）。设计未给值的预设**不臆造**：
   `healing_done_pct`（施疗侧）轴已登记但设计无具名预设 → 不为它造预设名。
6. **未纳入的预设族**（诚实登记，见实现说明 §17.9）：需 **效果条目 / 条件门控 / 未登记轴 /
   布尔枚举**的族（proc / 定向增伤 / 反伤 / 免死 / 命中闪避 / 回复速率 / 异常积蓄 / 套装 /
   持久成长 / 属性转化 / 处决 …）本批**不做**；`action_speed`（D2 二选一）**不登记**。

### 20.3 待裁决（本批不做，只登记）

| # | 待裁决 | 影响 |
|---|---|---|
| **D-54-1** | 预设**档位掷取**（`advanced`/`rare` 由掉落/强化/打造哪一环节选、如何映射 `quality_level`） | 决定「档位」能否从声明变成实机掉落档 |
| **D-54-2** | `reward_mult{scope}` 的 `scope` 承载形状（**多键** vs **对象型轴**）+ 是否只作用于战斗结算 | 决定贪婪/苦修流能否落地（本批如实报告不登记） |
| **D-54-3** | `weapon_wound`（命中降**目标**受疗）等「把轴挂到目标」的预设走 `on_hit` 效果条目 | 决定减疗压制流的目标侧表达 |
| **D-54-4** | `weapon_dmg` 的 `scope`（物理/元素/技能/DoT/弱点）走效果条目条件还是轴键分名 | 决定定向增伤 build 的表达 |
| **D-54-5** | `conflict_group` 是否升级为**可执行**约束（现为声明式，执行靠装备 `excludes`） | 决定「同族互斥」能否在预设选择期即时红拦 |

### 20.4 验收证据（复跑）

```bash
# 批54 单测（A~H 共 28 例）
pytest tests/unit/test_batch54_effect_presets.py -q -o addopts=""
# 全量（0 failed）——基线 8700 passed / 12 skipped
pytest tests/ -q -o addopts=""
# 红线复算（本批不改战斗/数值；--json 修前 vs 修后逐字节一致）
python scripts/batch45_measure.py --json
# 换包验收 + 字段元数据门禁 + ruff（对 qa_ctb_baseline_ruff.txt 零新增）
python scripts/editor_verify_packs.py
PYTHONPATH= python scripts/compare_field_meta_migration.py
ruff check .
```

- 字段元数据门禁：本批**不改任何内容字段元数据**（新增的是框架预设声明模块，不改
  `field_meta` 的字段表）→ 与批53 基线逐字段一致（新增 0 / 删改 0）。
- 页脚批次串 →「批54 · 预设集与结算轴」，全部批次串断言同步（旧串「批53 · 时序与资源轴」
## 二十、批 55 落地登记（2026-09-20 · 已完成；页脚批次串 =「批55 · 特效强度预算」）

### 20.1 范围与落点

批45 把装备占比校到 60%（`panel_budget` + `monster_scaling`），但 `PANEL_AXIS_KEYS` **只覆盖
atk/dfn/hp**；批50~53 接线 20+ 条特效轴后，特效会抬高真实战力却**不进 `equip_share`**。
本批按 `docs/特效强度预算_设计.md` §三推荐落地：**方案 B（另立上限，主）+ 方案 A1（度量，
只报不改）+ 方案 C（`monster_scaling` 备用杠杆，缺省 1.0）**；A2（预算池式）**不采用**。

| # | 落点 | 动作 | 缺省语义 |
|---|---|---|---|
| 1 | `data/gear_stats.py` | `EFFECT_BUDGET_KEY` / `EFFECT_AXIS_WEIGHTS`（坐标权表）/ `DEFAULT_EFFECT_BUDGET` / `EFFECT_AGGREGATES`·`GATE_MODES`·`UNKNOWN_MODES` / `normalize_effect_budget` / `effect_values_of`（旧键别名换算）/ `effect_equiv` / `effect_cap_pct` / `check_effect_budget` | 缺段 → `enabled=false` |
| 2 | `core/panel_budget.py` | **再导出**上述符号（调用方入口不变）+ `effective_equip_share`（A1）；`DEFAULT_MONSTER_SCALING` 增 `effect_hp_mult` / `effect_atk_mult` | 1.0（恒等） |
| 3 | `commands/battle_launch_commands._enemy_combatant` | 方案 C 两键与 `hp_mult` / `atk_mult` **相乘** | 缺省 1.0 → 逐字段一致 |
| 4 | `content/validator.py` | `_check_effect_budget`（段：结构/类型/枚举/负值红拦 + `calib=0` 黄提示 Y-20）+ `_check_effect_budget_entries`（条目级越界两态） | 缺段放行 |
| 5 | `content/field_meta.py` | `settings.effect_budget` 中文名 + 8 子字段说明卡 | 元数据可配 |
| 6 | `scripts/batch45_measure.py` | `--effects` 夹具（典型生存 build）+ A1/闸/特效后斩回列 | **缺省输出逐字节一致** |
| 7 | `scripts/batch55_dual_ruler.py` | 双尺子复算（工具尺 + 定稿档位尺 + 精确翻转阈值） | 只读 |
| 8 | `docs/批55_特效强度预算_测量报告.md` | 双尺子对照 + 越档判定 + A1 登记 | — |

**数据层落点原因**（落点 1）：`content/validator` 需读本段做内容校验，而架构矩阵
`content → {data}` **禁止 content→core**（对齐批50 把 `PANEL_AXIS_STEMS` 落 data 层的同一取舍）。
`core/panel_budget` 再导出，core/commands 调用方入口不变。

### 20.2 关键裁定

1. **越界行为 = `gate_mode` 三态**（按报告 §三.2 推荐）：`off` 静默 / **`warn` 黄提示（缺省，
   上线初期观察）** / `red` 红拦（拒绝）。条目级配额 = `cap_equiv_pct`（无档位语境）。
   **「削减上限」落在既有 `settings.effect_axes` 每轴上下钳**（批50/52），本批不新增钳制。
2. **缺省参数**（报告 §三.2）：`cap_equiv_pct=8`、`tier_mult={normal:1.5, elite:1.0, boss:0.625}`
   → 上限 **12%/8%/5%**；`aggregate=geometric`；`unknown_axis=warn`；`report_effective_share=true`。
3. **A1 = 只报数不改数**：`effective_equip_share = e/(pool+u)`，与 `equip_share` 并列；
   `effect_budget` 缺省不存在 → 不计算（`enabled=false` → 聚合器返回 0）。
4. **权表是坐标表不是加法定律**：`等效% = 轴值 / calib × 校准等效%`；按 §2.2 求和聚合。
   求和口径**保守**（把减伤与吸血的生存收益线性相加、高估叠加）→ 闸偏严；可被内容包
   `settings.effect_budget.axis_weights` **逐键覆盖 / 追加**（不写死）。
5. **方案 C 真正可用但缺省不存在**：两键与既有倍率相乘，改回 1.0 即回滚；本批**不配置**。
   报告 §三.1 明确「不单独作为主方案」——本批只留应急通道。
6. **A2 不采用**：报告 §2.1 实测「每一行都过度补偿」（Boss +1~+15 回合）且会把
   `equip_stat_mult` 变成随内容变化的函数 → 不实现 `mode` / `pool_scope` 键。

### 20.3 双尺子结论（本批核心证据）

| 尺子 | 口径 | 修前 | 本批缺省 | 典型生存 build | 判定 |
|---|---|---|---|---|---|
| 工具（batch45） | 相对仪器，公差 ±1 回合 | 7/20/24 | 7/20/24（**零变化**） | 7/19/22 | Boss **Δ−2 超公差** |
| 定稿档位 | 绝对红线 3-8/10-15/40-60 | — | — | 普通 4.43~4.64 ✅ / 精英 10.08~10.55 ✅ / **Boss 38.69~40.52** | **Boss 越档** |

- **明确结论：Boss 越档成立**（典型生存 build 把 Boss 压到 ≈39–41，跌破 40 下沿）；
  主因是**生存轴**（防御回合→攻击回合），冷却仅 −1.5 回合。
- **闸生效后**：Boss 上限 5% 综合等效 ⇒ worst-case 实机 ≥ **45.77** ∈ 40-60 ✅。
- **A1 登记（红线 C3）**：典型 build 真实装备占比 **43.73%**（< 50% 显式登记）。

### 20.4 待裁决（本批不做，只登记）

| # | 待裁决 | 影响 |
|---|---|---|
| **BV-1** | 红线 C8：典型 build 输出 +6.8% **远超** Boss 翻转阈值 +0.67%，是否重解 `monster_scaling.def_factor`（`R/1.5`） | 本批按一号红线**不擅改**既有数值；若内容启用特效轴须按 §四 C8 重解并重跑双尺子 |
| BV-2 | 特效是「点缀」还是「第三种支柱」：cap 是否维持 8%（会把「减伤25+吸血15+冷却×0.8」判超限） | 报告 §5-2：若视为正常毕业 build 则须选 ≥12%（几何）或 ≥20%（乘积） |
| BV-3 | 「60%」是面板口径还是总战力口径 | 报告 §5-1：本批保持面板口径纯净 + A1 常驻登记 |
| BV-4 | 权表「求和」保守口径是否改为按轴 max / 非线性 | 影响闸松紧；当前保守偏严 |

### 20.5 验收证据（复跑）

```bash
# 批55 单测（A~G 共 24 例：缺省零变化 / 越界两态 / A1 / 别名 / 校验器 / 编辑器 / 双尺子）
pytest tests/unit/test_batch55_effect_budget.py -q -o addopts=""
# 全量（0 failed）
pytest tests/ -q -o addopts=""
# 工具尺缺省零变化对拍（逐字节一致）+ 特效夹具
python scripts/batch45_measure.py --json
python scripts/batch45_measure.py --effects
# 双尺子一次跑齐
python scripts/batch55_dual_ruler.py --json
# 换包验收 + ruff（对 qa_ctb_baseline_ruff.txt 零新增）
python scripts/editor_verify_packs.py
ruff check .
```

- 字段元数据门禁：本批新增 `settings.effect_budget` 段（settings 条目 +1，未配置计数
  49→50）→ 重定基线 `b9df5c7 → f1e942e`（本批文档提交），对拍 0 差异。
- 页脚批次串 →「批55 · 特效强度预算」，全部批次串断言同步（旧串「批53 · 时序与资源轴」
  仅保留在 §十九 / CHANGELOG 的**历史条目**中，非断言）。


---

## 十五、特效轴 D 组裁决（用户 2026-09-20 拍板）

| # | 争点 | 用户裁决 | 落地含义 |
|---|---|---|---|
| **D1** | 轴的主口径：倍率 vs 百分点 | **百分点** | 与现实现一致（15 条轴均为 `_pct`，默认 0，如 `healing_received_pct: -50`）；倍率口径 `mult = 1 + pct/100` 数学等价，**编辑器显示层**可后续提供换算提示 |
| **D2** | 行动速度 vs 行动后摇（互为倒数，二选一） | **行动速度** | 实现 **`action_speed_pct`**（CTB 分母方向），**不实现** `action_recovery_pct`（`.effect_axes` 中该轴不得登记，避免指数级加速） |
| **D3** | 承伤乘区：叠乘 vs 收敛 | **按建议（收敛）** | `damage_taken_pct` 作主乘区，与 `immune_dmg` 归并；`mitigation` 阶段**保持独立**（批52 已实现并登记取舍） |
| **D4** | 治疗轴下界：负治疗 / 过量治疗 | **按建议（两者都做）** | 负治疗**已实现**（下界 −200 = 治疗转伤害，配置驱动）；**`overheal`（过量治疗）需补**——口径见 `特效整理设计_1_修正轴全集.md` 的 E5，**设计未写清的部分进待裁决，不得臆造** |
| **D5** | 奖励轴下界 | **按建议（钳 0）** | 掉率/经验/货币类轴下钳 0（负掉率无意义）；接线时落实并给证据 |
| **D6** | 光环/场域 | **按建议（只登记不做）** | 不做半成品；团队内容立项时连同多单位模型一起做 |

**附加要求（沿用）**：全部轴**可配**（包声明 `settings.effect_axes`）；**缺省零变化**；动战斗数值的批**必须复算斩杀回合双尺子**（工具尺 + 定稿档位尺），越档**只报告不擅调**。

---

## 二十一、批 56 落地登记（2026-09-21 · 已完成；页脚批次串 =「批56 · 速度轴与过量治疗」）

> 依据：本文 §十五 D 组裁决（D2 / D4 / D5）；`特效整理设计_1_修正轴全集.md` X28 / X29 / §4-E5；
> `特效整理设计_3_落点与分期.md` §二「批 51」（旧编号 = 本批批56）。
> 实现说明：`docs/深度打造_实现说明.md` §十八；复跑：`scripts/batch55_dual_ruler.py`。

| 项 | 落地 |
|---|---|
| **D2 行动速度** | 登记第 16 条特效轴 `action_speed_pct`（X28，P1，双向 `-80~400` 百分点，范围由 `settings.effect_axes` 可配）；消费点 = CTB **有效速度**（`battle._ctb_actor_speed`，由 `ctb_rules.time_cost` 分母消费，不新开管线）。**`action_recovery_pct` 不登记、不实现**，进 `DEPRECATED_EFFECT_AXES`；校验器对声明/词条命中者 → **黄提示 `effect_axis_deprecated`**（点名 `action_speed_pct`，不硬拦）。 |
| **D4 过量治疗** | 新增 `settings.overheal = {enabled: bool}`（**不是数值轴**，E5 = 布尔）；缺省/关闭 = 与现状一致（按 `max_hp` 封顶、过量丢弃）；开启 = 按 E5 字面「可否超过最大 HP」= **保留**（HP 可超上限）。5 处治疗落点统一收口 `effects.apply_heal_to_hp`；只对 HP 生效。 |
| **D5 奖励轴** | **核查结论：尚未接线**——奖励类轴（掉率/经验/货币）既未登记为特效轴、也无战斗/结算消费点（`reward._SCALAR_KEYS` 只有 coins/gem/exp/rep）。**无对象可钳 0**，如实登记到「奖励轴」批（登记时在 `EFFECT_AXIS_SPECS` 写 `min: 0`）。**不硬造消费点**。 |
| **零变化** | 不配置新键/新段 → 战斗结算快照、字段元数据、双尺子**逐字段与修前一致**（工具尺 7/20/24；档位尺结论与批55 完全一致）。 |
| **待裁决** | **BV-1**：过量治疗的**上限**（E5 只给布尔，未写「最多超多少」）→ 本批不设额外上限。**BV-2**：过量部分是否**转护盾**、与既有护盾阶段（伤害链②）的先后 → 本批不转护盾、护盾不动。**BV-3**：速度轴在 `effect_budget` 权表中的**等效坐标**（当前未登记 → `unknown_axis` 黄提示）→ 校准批处理。 |
| **验收证据** | 速度：+20 → 有效速度 ×1.2 / `next_ready` 1/1.2、−20 → ×0.8（`tests/unit/test_batch56_speed_overheal.py` A 组）；后摇轴声明 → 黄提示（A6）；overheal 缺省/开启满血治疗对拍（B 组）；双尺子修前=修后（§18.6）；速度对斩回敏感性表（§18.7，只报告不擅调）。 |
| **页脚批次串** | 「批56 · 速度轴与过量治疗」（`index.html` + 全部断言同步；旧串 批55 不残留）。 |

---

## 二十二、批 57 落地登记（2026-09-21 · 已完成；页脚批次串 =「批57 · 淬炼与分解回收」）

> 依据：`/root/deliverables/淬炼与分解回收_实现口径.md`（C-1~C-7 / §2 / §3 / §4 / §5）+
> 用户原案 §10/§14/§12 + 本文 §二 N5 + §三 补充 1 + §十五 D 组（**全部可配**）。
> 实现说明：`docs/深度打造_实现说明.md` §十九；复跑：`scripts/batch57_cycle_sim.py`。

| 项 | 落地 |
|---|---|
| **C-1 淬炼落点** | 配置 = `enhance.json → temper` 段（`enhance_models` 归一/meta/校验 V8~V13）；引擎 = **新 `core/temper.py`**（纯函数）；**不改既有强化成功率/失败段**（`success_curve`/`stone_tiers` 逐字段不变）。 |
| **C-2 状态落点** | `ItemInstance.temper_alloc`（+ `required_level`），**uid 锚定**；增量**幂等物化进 `stats_bonus`** → 面板聚合/战斗桥/展示零改动。 |
| **C-3 与强化分账** | `enhance_level`（强化）与 `temper_alloc`（淬炼）分存；`materialize_temper` **不碰** `enhance_level`（测试 C 组）。 |
| **C-4/C-5 分解回收** | **新实例级路径**：`/分解` 精粹启用时按 uid/名称定位打造实例 → **材料（`decompose_rate`）+ 精粹（`essence_rate`）两套率分键**（语义相反不可合键）；未启用整段跳过（既有 item_id 路径逐字节不变）。 |
| **C-6 84:1 → 两侧同时定** | 产出侧 `V×k1×k2×β^色序`；消耗侧 `cost_per_point.essence=1593`（报告 §4.6）。典型循环（95 分解 ↔ 5 满淬）**产出/消耗 = 0.9999×**（`p=1` 时 1593×）→ 不再是 84:1。 |
| **C-7 β 闸门 → 真闸门** | β 降为**正向**系数（默认 1.2）；真闸门 = **不可逆 + 返还随淬炼量衰减**（`temper_refund=800`、`refund_decay=0.5`，每点返还率 0.423→0.251）。20 轮模拟每轮净变化 **−215,320 < 0**、余额单调递减 → **无套利**；V8 硬拦 `refund > cost`。 |
| **D4 载体** | 精粹 = **货币**（`essence_rate.essence_currency`，默认 `essence`）；**须登记 `settings.currencies`**，否则 R-4 红拦 + 运行期入账被拒。**未改 veinborn 内容包**（缺省零变化）。 |
| **迁移** | `backfill_instance_temper`（纯函数/幂等/无损/版本无关；读侧 `row_to_player` 调用）；`_item_from_dict` 逐字段读回；6 构造点逐处核对（4 处显式带过 + 2 处兜底构造按批42/43 同口径）。 |
| **零变化** | `temper.enabled=false` + `essence_rate.enabled=false` → 引擎不消费、字段默认空、既有 5 段配置逐字段一致、红线复算（`batch45_measure` + `batch55_dual_ruler`）**修前=修后逐字节一致**。 |
| **待裁决（按建议默认，可改）** | **D1** 精粹不可交易；**D2** 可重置不返还（`reset_allowed=true`）；**D3** 分解不返还相性材料；**D6** 报告建议 0.0 → 本批按主 agent ② 改可配衰减返还（默认 0.5×消耗×衰减）；**D11** `forge.decompose_rate` 不动（保留待裁决）；**D13** 指令名不改（复用 `/分解` 实例分支，`/淬炼` 壳待裁决）。 |
| **未接线（如实登记）** | `/淬炼` 独立指令壳本批未做（引擎/状态/配置/校验齐备；D13 指令名待裁决）；`grade_of` 逐档系数因实例未存图纸档而默认走 `k2`（预留）。 |
| **页脚批次串** | 「批57 · 淬炼与分解回收」（`index.html` + 全部断言同步；旧串 批56 不残留于断言）。 |

---

## 二十三、批 58 落地登记（2026-09-21 · 已完成；页脚批次串 =「批58 · 示例包数据」）

> 定位：**内容数据落地**——新建独立示例包 `content/zz_craft_demo/`，把「材料表/图纸表草案 v2 +
> 批38 相性四段 + 批54 特效预设 + 批57 淬炼/精粹」落成可打开、可跑通、可照抄改的示例。
> **一号红线：`content/veinborn` 一个字节未改**；**框架代码零改动**。
> 实现说明（用户向）：`docs/深度打造_实现说明.md` §二十。

| 项 | 落地 |
|---|---|
| **包骨架** | `content/zz_craft_demo/`：`manifest.json`（9 模块）+ `field_meta.json`（中文模块/字段名）+ `settings.json`。 |
| **相性四段** | 4 相性（`lunar`/`frost`/`ember`/`verdant`）+ 11 池 + 1 联动 + 3 互动（冲突 `ember×frost` / 增幅 `lunar×frost` / 反转 `frost→lunar`）——形状按可开工版 §七。 |
| **打造参数** | `deep_craft.craft_rules`：等级权重 0.6/0.4、floor、同相性 ×1.5、阈值 `[0,25,…,1080]`、`slot_decay=0.25`、`type_bonus=1.03`、`interaction{-0.25,+0.15}`、cost 60+20×种、`quality_exp_crit`（逐档 0.15/0.10/0.07/0.05，×2，`last_threshold`）；`quality_grades`（copper/silver/gold/rainbow，cap 900/2100/4200/8600，偏移 0/1/2/3）；`quality_exp_by_color`（10/16/26/42/70/110）。数值全部来自草案 v2。 |
| **材料/图纸** | **24 材料**（每档 6，字段对齐 `core/deep_craft` 读取键）+ **6 图纸**（铜2/银2/金1/彩1：固定槽/自由槽/等级带/固定属性/随机位/套装词条/被动/相性/配方/产出）。 |
| **特效预设** | 从批54 的 26 条取 **11 条**落成示例装备词条（取 normal 档原值），含 **2 对双方向**：会心 ±10、受疗 ±15。 |
| **最小可玩** | `/精造` 打造（铜剑 §4.1 投料复算：Lv5 / cost 890 / QE 265.2）+ `/分解 <uid>`（材料 0.65 + 精粹）+ `core/temper` 淬炼（上限 `等级×6`、1593 精粹/点）。 |
| **实测证据** | 两次打造 uid/随机词条不同；炎火投料 → 主 `ember` 副 `frost`（随机词条命中 `pool_ember`）；分解 `...×2/×2/×2/×1 + 精粹 75`；淬炼 `atk 12→13`、精粹 200075→198482、越限/精粹不足双向拒绝；无相性 → `requires_affinity` 词条 0 条（负向）。 |
| **校验** | 示例包跑既有校验器（`validate_pack_data`）：**红拦 0 / 黄提示 0**；编辑器 `editor_verify_packs` PASS。 |
| **待确认（如实登记）** | ① `reverse` 为引擎全局按材料 `from` 触发，「只由月蚀石触发」当前 schema 不可表达；② 固定属性 `value_rule` 缩放未实装（§4-R4）→ 示例包补 `value` 示例占位值；③ 草案 `thr` 未登记 → 换 `con`；④ 草案 `blueprint_random_stat_pool` 引擎不读 + 字段表未登记 → 不落（防悬空）；⑤ `material_tags` 未登记但引擎读 → 照用；⑥ `/淬炼` 指令壳批57 未做 → 淬炼走引擎；⑦ `enhance.cost.stone_tiers` 默认石 id 不在 items → 改为真实材料 id。 |
| **页脚批次串** | 「批58 · 示例包数据」（`index.html` + 全部断言同步；旧串 批57 不残留于断言）。 |

---

## 二十四、批 59 落地登记（2026-09-22 · 已完成；页脚批次串 =「批59 · 特效小尾巴」）

> 依据：本文 §十五 **D5**（奖励轴下钳 0）+ §二十一 批56 登记的 **BV-1/BV-2/BV-3** 待裁决；
> `特效整理设计_1_修正轴全集.md` §1.7 X43（`reward_mult{scope}`）/ X28（行动速度）/
> §4-E5（过量治疗）；`特效强度预算_设计.md` §2.2（权表坐标）。
> 实现说明（用户向）：`docs/深度打造_实现说明.md` §二十一。复跑：`scripts/batch45_measure.py`
> + `scripts/batch55_dual_ruler.py`（种子 20260919）。

| 项 | 落地 |
|---|---|
| **D5 奖励轴** | 登记第 17 条特效轴 `reward_mult_pct`（X43，P1，D1 百分点口径 `mult = 1 + pct/100`）：**`min: 0` 下钳 0**（负掉率无意义）；`bridge="settlement"`（**结算期轴，不进战斗桥**）。**消费点待接**（哨兵 `EFFECT_CONSUMER_PENDING`）：候选三处——`reward.dispatch_reward`（coins/gem/rep，被任务/签到/NPC/钓鱼/成就/PvP 共用）、`battle_reward.settle_battle_rewards`（exp 经 `LevelUpEngine.gain_exp`）、`battle_reward.roll_death_drops`（掉落 chance）——**无唯一收口，不为交差硬造**。`scope` 声明形状（多键 vs 对象型轴）仍待裁决。 |
| **BV-1 上限** | `overheal` 段新增 `cap_pct`（最多超出 max_hp 的百分比）/ `cap_flat`（最多超出的点数），同给取**更小**者；**缺省都不给 = 无额外上限**（与批56 现状逐字段一致）。读点 = `effects.apply_heal_to_hp` → `overheal_cap`。 |
| **BV-2 去向** | `overheal.mode ∈ keep|discard` 把选择点**显式表达**（`mode` 优先于兼容的 `enabled`）；`shield`（转护盾）为**未实现保留位**：不生效、回落 `discard`，校验器 **R-1 红拦 + Y-21 黄提示**「未实现」——**不做半成品开关**，既有护盾阶段原样不动。 |
| **BV-3 速度坐标** | `EFFECT_AXIS_WEIGHTS` 登记 `action_speed_pct`（`calib=+20 ⇔ output=+20%`，依据实现说明 §18.7「把 +20% 速度当作输出侧 ×1.2」；可被包声明逐键覆盖）→ `effect_equiv` 不再报 `unknown_axis`。 |
| **m1 flake 根治** | 根因 = `true`/`false` 被 Python 快路径拒 → 每次降级 Node，而 Node vm watchdog 在 30ms 有效预算下以 **~2%~12%** 概率误报 `ERR_SCRIPT_EXECUTION_TIMEOUT` → `runner_fatal` → 公式兜底 0.0（预编译 `vm.Script` / 复用 `vm.createContext` 均不能消除）。修：`_VM_CTX_SLACK_MS` 20→90（有效 100ms；`FORMULA_TIMEOUT_MS=10` 执行预算契约字面值不变）+ 布尔字面量走 Python 快路径。修复后探针 400 次 0 失败。 |
| **零变化** | 不配置新键/新段 → 双尺子**修前 = 修后**（工具尺 7/20/24；档位尺 Boss 越档结论与批55/56 完全一致，**非本批引入**）。 |
| **字段元数据门禁** | 新增 `reward_mult_pct` 字段（soft 新增）+ `settings.overheal` 补 `mode`/`cap_pct`/`cap_flat` 三子字段 → `blocks.stats[@more]` 计数 **+1**；删除 0 / label·help·group 严格键改动 0 → **重定基线** `427047e → ee15c78`（本批字段/元数据提交）。 |
| **页脚批次串** | 「批59 · 特效小尾巴」（`index.html` + 全部批次串断言同步；旧串 批58 不残留于断言，仅存于 §二十三 / CHANGELOG 历史条目）。 |
