# 实现层启动手册（QBot-TurnTellerRPG）

> 2026-08-18 · Reset 后用：新会话直接读本文件即可无缝继续实现层开发
> 使用：reset 后说"读《实现层启动手册.md》开始实现层"或直接按本手册执行

## 一、项目状态（设计层 100% 完成）

| 资产 | 数量 | 位置 |
|---|---|---|
| 定稿（设计事实来源） | 39 份 | `/root/docs_archive/RPG框架项目/*.md` |
| 细化契约（实现直接依据） | 72 份 | `/root/QBot-TurnTellerRPG/细化_*.md` |
| 覆盖审计（防缺漏） | 8 份 | `覆盖审计_A~H.md` |
| 幻觉审查（防编造） | 72 份+总结论 | `幻觉审查_*.md` + `幻觉审查总结论.md` |
| 仲裁决议（8 条已裁决） | 1 份 | `细化_0_仲裁决议汇总.md` |
| 测试体系规划 | 1 份 | `细化_5d_测试体系总纲.md`（15 脚本/1441 条 TC） |

**仓库**：GitHub `xianyugezi/QBot-TurnTellerRPG`（分支 main，push 走 mihomo 代理 7890）

## 二、固定协作团队（5 角色）

| 角色 | 承担 | 说明 |
|---|---|---|
| ① 实现组 | Hermes 子 agent（每里程碑 2-3 并行，delegate_task） | 按细化写码 |
| ② 代码审查 | dsh 固定对话 | 角色卡 `审查角色_初始化.md` + 调用模板 |
| ③ 测试验收 | Hermes 子 agent（独立于实现组） | verify_m0~m6 + 门禁 |
| ④ 定稿对照员 | dsh/子 agent | 功能合入前核对定稿出处 |
| ⑥ 进度收口 | Hermes 主 agent（本会话） | 调度/落盘/冲突上报用户拍板 |
| ~~⑤ 数值验证~~ | ⏸ 延后 | 数据包阶段启用 |

**流水线**：实现组写码 → ④定稿对照 → ③测试门禁 → ②dsh 审查 → ⑥汇总给用户拍板 → 合入

## 三、里程碑计划（M0-M6）

| 里程碑 | 内容 | 依据细化 | verify 脚本 |
|---|---|---|---|
| M0 框架骨架 | 分层架构/存储 layer/loader 校验 | 3a/4a/3e/3e2 | verify_m0.py |
| M1 战斗核心 | 伤害公式/效果拦截链/连段/印记 | 1a/1b/1c/1d/1g | verify_m1.py |
| M2 怪物体系 | 怪物八段/AI 状态机/掉落 | 1e/1f/1g4 | verify_m2.py |
| M3 地图副本时间 | 网状地图/换区/副本两型/时间天气 | 2a 系（9 份） | verify_m3.py |
| M4 指令系统 | 交互（NPC/商店/任务/签到）+基础指令 | 2b/4f/3c/3d | verify_m4.py |
| M5 生活生产 | 钓鱼/锻造/强化/炼金/熟练度 | 2c 系（19 份） | verify_m5.py |
| M6 数据框架 | 编辑器/GM/图鉴/成就/PVP | 5a/5b/4c/4d/4e/6 系 | verify_m6.py |

**门禁**：每里程碑 verify 全绿 + dsh 审查通过 → 才进下一步（细化_5d G0~G7）

## 四、启动第一步（M0 骨架）

1. 建实现目录 `qbot_rpg/{core,world,storage,content,data,commands,web}`（细化_3a 分层）
2. 实现存储层（细化_4a：7 表 SQLite/事务/幂等/回收）
3. 实现 loader+校验器（细化_3e/3e2：五段管线/红黄分级/热重载）
4. 实现组 3 路并行（delegate_task），测试组同步写 verify_m0
5. 完成后走流水线：定稿对照 → 测试 → dsh 审查 → 汇报用户

## 五、铁律（每次写码/审查都遵守）

1. **每功能可追溯**：实现里标注 `细化_XX §Y` + 定稿行号；出现设计文档没有的内容=疑点，除非标注【工程补白】
2. **平台无关**：world/storage/content/data 零 NoneBot import；commands 唯一适配器接触点
3. **拦截链必须接线**：旧实现教训——run_l0 全库无调用方。每写一个函数查调用方
4. **数据表非空 + validator 生效**：防旧实现 effects/statuses 空表、校验空转
5. **指令严格对照定稿**：参数/流程/返回模板每项对细化文档指令契约章节
6. **原子防双扣**：购买/强化/合成类 SQLite 事务 + message_id 幂等
7. **dsh 自报不可信**：报告落盘后必须 read_file 核对
8. **行号编造教训**：AI 定稿仅 322 行，L502 编造已修——引用前先验证行号在文件范围内

## 六、关键环境

- **dsh key**：`DSH_OPENCODE_API_KEY`（/etc/environment + /root/.bashrc，2026-08-18 已换新 sk-2HEW...）
- **Hermes key**：sk-gOt...（/root/.hermes/.env:502，备份 .env.bak-2/3）
- **push**：`https_proxy=http://127.0.0.1:7890 git push ...`（mihomo 8live；重启 gateway 会连坐 mihomo，需重拉）
- **子 agent 并行上限**：3（delegate_task）
- **实机环境**：napcat-3124097014（WS 3001，游戏 bot 连接点）/ 3379761086（3002）
