# 规划 · 路2b 交互系统（实现层）

> 性质：实现层原子任务清单。每条任务含四要素：**任务名 / 定稿依据 / 实现要点 / 验收标准**，全部可追溯到四份定稿。
> 定稿来源：NPC 系统设计定稿 v1.3.1（下文简称 NPC）、商店系统设计定稿 v1.0（商店）、任务系统设计定稿 v1.0.4（任务）、签到系统设计定稿 v1.0（签到）。
> 任务数：24 条 = 公共基础 3（A）+ NPC/对话 6（B）+ 商店 6（C）+ 任务 5（D）+ 签到 4（E）。
> 原子性约定：每条任务 = 一个可独立实现、独立验收的单元；跨系统机制（奖励发放/条件引擎/日界）只立项一次（A1/A2/A3），各系统任务引用之，不重复实现。

---

## 0. 覆盖对照表（用户要求 ↔ 任务）

| 覆盖项 | 任务 |
|---|---|
| NPC 发牌员：三策略 / 10 类动作 / 会话路由 / 对话树 ≤2 层 | B3 / B4 / B2 / B5 |
| /对话：列表 / 序号 / 名称 | B1 |
| 商店：5 类型 / stock 0=无限 / 原子防双扣 / 当前商店 | C1 / C2 / C5 / B6 |
| /商店：切换列表（序号 / 名称 / 列表一览 / 翻页） | C4 |
| /购买 / /出售 | C5 / C6 |
| 任务：三原语引擎 / 每日防刷 / 统一奖励条目 / 主线置顶 | D1 / D2 / D4 / D3 |
| /任务：接取 N / 交付 N（+任务信息 / 放弃） | D5 |
| 签到：多表 / 连签 / 补签 | E1 / E2 / E3 |
| /签到（含 [签到:*] 条件键） | E4 |

---

## A. 公共基础层（跨系统唯一实现，先于各系统落地）

### A1. 统一 reward 解析器（发放器唯一实现）
- **定稿依据**：任务 §2.1（统一 reward 条目 schema：物品 {item,count} / 货币键值 coins/gem/exp/rep / 组合数组；"发放器唯一实现 = 框架 reward 解析器"）；NPC §3.3（give_item 经框架 reward 解析器统一入账，NPC 只配条目+repeat 规则）；签到 §3.2（签到/任务共用发放器）+ §十三（"发奖失败兜底：物品不存在黄字跳过，不吞整次签到"）。
- **实现要点**：
  - 单一入口函数 `dispatch_reward(entries, ctx)`，任务（D4）、签到（E2）、NPC give_item（B4）、怪物掉落四条消费方只配条目不写发放逻辑。
  - 条目解析：`{item,count}` → 入包（默认绑定）；`coins/gem` → 货币表（键空间 = settings.json 货币键）；`exp` 数值直入；`rep` → reputation_state（声望=进度门槛，**不入货币表**）。
  - 内联键值串（"exp=50,coins=80,item:铁矿*3"）= 序列化糖：加载/导入时等价展开为条目数组，存储与校验以结构化条目为准；兼容签到 `items[]{id,count}` 的 id ≡ item 键。
  - 组合数组按序入账；任一条目失败（物品引用不存在/数值非法）→ 黄字跳过该条，不中断整批结算。
  - 幂等：结算上下文携带 version/tx id，同一上下文重复调用不重复入账（承接任务/签到防双发、商店防双扣）。
- **验收标准**：任务/签到/NPC 三处调用同一函数（无第二实现）；内联串与数组展开等价；rep 入 reputation_state 不入货币表；物品缺失时黄字跳过、其余正常入账；重复调用幂等。

### A2. 统一条件引擎（{var,op,value,param} · 9 运算符 · 三原语 · 互译表）
- **定稿依据**：NPC §4.0（一套条件引擎全系统复用；var 键空间含签到三键登记；符号双写等价；event 原语等价映射；旧格式兼容）；任务 §一（三原语 + 中文变量键互译）；签到 §四/[签到:*] 三键（[签到:连续天数]/[签到:本月天数]/[签到:今日已签]）+ §十一 登记要求。
- **实现要点**：
  - 结构 `{var, op, value, param}`；9 运算符 gt/ge/lt/le/eq/ne/between/is/not，符号双写 >= > <= < = != 归一；旧 {type,var,op,value} 的 type 忽略（var 归一），黄提示"旧格式，建议迁移"。
  - 谓词求值三类：值型（level/item_count 等读当前值）、累计型（kill_count/gain_count 读 longline_counters，见 D2）、事件型（var 前缀 `[事件:xxx]`，param=目标，读事件计数）；事件名先查事件注册表（预置清单 + 内容包扩展登记），未登记黄提示。
  - var 键空间注册表统一维护：任务类/物品类/状态类/职业类/累计类/时间类/关系类/`[签到:*]`/事件类；any/all/not 嵌套递归求值。
  - 中英文互译表（权威主表 NPC §4.3）：编辑面板显示中文变量键，配置存储与校验一律英文条件键；main_progress 默认语义 = 已完成 main:true 主线任务计数（落 longline_counters）。
- **验收标准**：任务/NPC/签到三处共用同一求值器（单实现断言）；9 运算符 + 符号双写各 1 用例；三原语各类 1 用例；any/all/not 嵌套 1 用例；[签到:*] 三键可被任务/NPC 条件引用并正确求值；互译表抽查 5 条。

### A3. 日界统一与懒计算（重置 05:00 · 商店/任务/签到同刻）
- **定稿依据**：签到 §5.1（懒计算为唯一判定，apscheduler 仅双保险）/§5.2（统一周期键默认 05:00，与 quest_daily 共用配置键，商店/任务/签到三表同刻对齐；凌晨 0-5 点仍算前一天）；商店 §七（last_refresh 惰性补刷、时区 UTC+8、跨周判定）；任务 §3.4（每日懒计算刷新，重置时刻默认 05:00 可配）。
- **实现要点**：
  - 单一日界函数 `today_of(last_key)`：读存档周期键与当前现实时间（UTC+8），重置时刻之后算新一天，0-5 点归属前一天。
  - 惰性补刷：所有周期状态在玩家操作时按时间差补算，不依赖定时器；定时器只做每日计数重置/公告双保险；服务器重启/离线多天不丢不炸。
  - 统一配置键：重置时刻一个配置项同时供商店 refresh、任务 quest_daily、签到结算读取；跨周判定（结束 > 开始）、once 时间窗（未开门/自动下架）复用同一套周期工具。
- **验收标准**：三系统读同一日界函数（无第二实现）；离线 3 天首次操作正确补算（签到 streak/限购/库存/任务每日计数）；凌晨 0-5 点操作归属前一天；重置时刻可配且改一处三系统同步生效。

---

## B. NPC / 对话 / 发牌员

### B1. /对话 指令（列表 / 序号 / 名称）
- **定稿依据**：NPC §二（/对话 查看当前地图 NPC 列表，序号+名字+类型图标；/对话 1 序号快捷；/对话 铁匠·老周 名称对话；命名铁律禁空格、允许 ·/Ⅱ；无 NPC 地图提示"当前地图没有可对话的人"；高频快捷绑定/免前缀；内容包指令别名）。
- **实现要点**：
  - 解析分派：无参 → 列当前地图 NPC（序号+名字+类型图标，格式 "1.🔨铁匠·老周"）；纯数字 → 序号快捷对话；非数字串 → 名称优先精确匹配，失败按序号回退。
  - NPC 源 = maps.json npcs 引用 + npc.json；过滤 visible=false 与条件隐藏项；无 NPC 时按定稿文案提示。
  - NPC 名解析按命名铁律：禁用空格（防解析歧义），接受 ·/Ⅱ 字符。
  - 指令别名挂 settings.json command_aliases（修仙世界可别名"拜访"）；/对话 支持快捷绑定/免前缀（会话路由见 B2）。
- **验收标准**：列表格式与定稿逐字对齐；含 ·/Ⅱ 的名字精确匹配；序号与名称并存时名称优先；无 NPC 地图正确提示；别名与免前缀生效。

### B2. 对话会话状态机与会话路由（写死规则）
- **定稿依据**：NPC §2.1（会话路由写死：纯数字/继续/离开/再见/退出/选择 N → 对话状态机；带指令词"攻击1/使用1/背包2"→ 正常指令解析；"快捷绑定 1=攻击"后对话中发"1"→ 会话路由优先；快捷表仅在无会话上下文生效；离开/再见/退出三词同义；菜单末尾固定"N.离开"）+ §2.4（对话恢复简报：菜单层中断恢复重显菜单 / 子界面层中断注明"上次的『领取补给』未完成，请重新选择" / 长叙述 page 索引续段）。
- **实现要点**：
  - 会话上下文（玩家 × NPC）：状态 = 菜单层 / 子界面层（heal 确认、任务交付确认、领取中）/ 叙述 page 索引；上下文随存档持久化（碎片化续玩不丢）。
  - 路由表硬编码分发：纯数字/继续/三结束词/选择 N → 状态机；带指令词 → 正常指令解析（对话中也能"使用1"吃药再"继续"）；快捷表匹配只在无会话上下文时参与。
  - 结束词统一三词同义；菜单末尾固定 "N.离开"（N=选项数+1，与路由词严格一致）。
  - 中断恢复简报：菜单层重显菜单；子界面层注明未完成项（防误以为操作已完成）；长叙述按 page 续段。
- **验收标准**：对话中"1"选选项、不触发快捷攻击；对话中"使用1"正常解析；离开/再见/退出三词均可结束；中断重进恢复简报正确（菜单层/子界面层两用例）。

### B3. 发牌员抽取状态机（三策略）
- **定稿依据**：NPC §六（dealer 三策略：first_match 默认按序首匹配 / weighted 加权随机 / random 纯随机；抽牌状态机 5 步：visible→牌池构建（按条件过滤）→抽牌→事件交付（对话/物品/任务）→孤寂卡（无牌可抽 = 普通问候，不交付）；去重复用 quest_active/quest_daily 三表，不重复发已完成任务）。
- **实现要点**：
  - dealer 配置解析：strategy 枚举（缺省 first_match）+ 牌池（候选事件列表）；"发牌员"作为 NPC type 之一（§四）。
  - 5 步状态机实现：visible 判定 → 候选牌按条件（A2）过滤构建牌池 → 按策略选中一张（first_match=顺序首个满足；weighted=按权重随机；random=等概率随机）→ 交付走对应管线（对话→B5、物品→give_item/A1、任务→接取/D5）→ 牌池为空走孤寂卡：普通问候，不交付。
  - 去重：复用 quest_active（不重发进行中/已完成）与 quest_daily（repeat=daily 当日已发不重发）。
  - 校验配套（黄提示）：候选互斥无牌可发。
- **验收标准**：三策略各 1 用例（first_match 顺序优先、weighted 出现多样性、random 可复现随机）；条件过滤生效（不满足条件的牌不进入牌池）；无牌 → 孤寂卡不交付；已完成任务不重复发放。

### B4. interactions 十类动作引擎（含一次一物 / 菜单 ≤6 折叠 / repair 降级）
- **定稿依据**：NPC §3.1-3.4（10 类动作 quest/shop/heal/give_item/buff/repair/teleport/intel/tutorial/reply 及子字段表；条件判定：满足执行/不满足提示/可隐藏可置灰）+ §2.3（一次一物：信息类交付后置灰"已听"，已交付标记落玩家存档、不落 30 天回收的会话快照；图鉴=回看出口）+ §3.0（菜单 ≤6 选项，超限折叠"7.更多…";条件提示一行）+ v1.3.1（repair 依赖装备耐久系统，框架未实现 → 当前降级不可用/友好提示）。
- **实现要点**：
  - 动作分发器 10 分支：quest（候选任务+条件匹配 → 接取发布）、shop（shop_refs 打开 + 记录当前商店，见 B6）、heal（cost 扣费 + heal{hp,mp} 恢复，百分比串"100%"按上限）、give_item（A1 发放 + repeat once/daily 语义）、buff（效果注册表引用 + turns）、repair（**降级路径**：选项置灰，触发友好提示"没有需要修理的装备"）、teleport（map 引用 + 可选 cost）、intel（图鉴解锁 + 已听置灰）、tutorial（first_meet 首见）、reply（随机/循环文本）。
  - 公共字段：text（菜单文案）+ condition（A2）；不满足默认显示但提示"需要：等级 ≥10"，作者可配隐藏/置灰。
  - 一次一物：信息类（intel/tutorial 等）交付后标记"已听"（下一轮可选但无新内容"你已经听过了"）；标记落玩家存档（不落会话快照）；情报/线索交付同时写图鉴（回看不死胡同）；功能类不置灰可重复。
  - 菜单渲染 ≤6 项 + 固定"N.离开"，超过折叠二级"7.更多…"。
- **验收标准**：10 类动作各 1 用例（repair 用例 = 置灰 + 友好提示）；条件不满足提示文案正确；已听置灰且重进会话不再交付、存档持久化；情报交付后图鉴可回看；7 个选项时折叠为"7.更多…"。

### B5. 对话树解析（单轮交付 · ≤2 层硬约束）
- **定稿依据**：NPC §三 对话结构（greeting 见面语；单轮交付默认：一次交互最多一条新线索；简单对话树 ≤2 层硬约束防爆炸；选项级 conditions；action 枚举 shop/quest/tutorial/intel/reply；超 2 层校验器提示"对话太深，拆成多 NPC 或事件牌组"）+ §4.5（对话树死循环 >2 层环 = 硬拦）。
- **实现要点**：
  - dialogues 结构解析：greeting（兜底默认）+ options[]（text + action + condition）。
  - 递归渲染限制深度 ≤2：根（greeting）+ 1 层选项；选项下再出选项 = 第 2 层；第 3 层配置 → 校验器黄提示"对话太深"；图结构成环（>2 层环）→ 硬拦拒绝加载。
  - 选项级条件（A2）求值过滤可见选项；action 转发到 B4 动作分发器。
  - 已交付标记（B4 一次一物）作用于对话树选项，防重复喂同一线索。
- **验收标准**：greeting + 选项树正确渲染；选项级条件过滤生效；3 层配置被黄提示；环状引用被硬拦；2 层内全链路可用。

### B6. 当前商店机制（记录 / 清除 / 多店切换 / 购买语境）
- **定稿依据**：NPC §2.2（打开 NPC 商店记录当前商店 = shop_refs，地图级状态；地图内 /商店 直回，中断不丢，无需子界面快照；离图清除 → 默认全局商店）；商店 §八（衔接规则 5 条写死：对话 action:shop 打开 → 记录 shop_refs[0] → 地图内 /商店 直进 → 离图清除回退 normal 默认 → 一 NPC 多店 /商店 2 切换；NPC 购买语境"老周把药水递给你：…"；商人 NPC 默认带 shop 交互，漏配 shop_refs = 打开默认商店不报错；声望折扣可选模块默认关）。
- **实现要点**：
  - 地图级状态 `current_shop`：仅在地图上下文中有效；`/对话 → 看看货物`（action:shop）打开时写入 shop_refs[0]。
  - 地图切换事件清除 current_shop；清除后 /商店 回退 normal 类型全局默认商店。
  - 多店：shop_refs 数组保序；/商店 2 切换第二家（与 C4 序号解析协同）。
  - 购买语境：经 NPC 商店购买成功 → "老周把药水递给你：药水×5（-250 金币）"；经 /商店 直购 → 标准模板（无 NPC 语境）。
  - 商人 NPC 类型默认生成 shop 交互；漏配引用打开默认商店，不报错（默认值兜底）。
- **验收标准**：打开 → 中断 → 地图内 /商店 直回原店；离图后 /商店 回落全局默认；多店序号切换正确；窗口期内两种购买语境外套正确切换。

---

## C. 商店系统

### C1. 商店 5 类型与可见性
- **定稿依据**：商店 §3.1（顶层字段：type/currency/level_required/reputation_required/open_condition/refresh/items/pool/price_fluctuation/visible/desc + 默认值兜底）+ §四（5 类型：normal/npc/reputation/event/blackmarket；type 只影响入口/可见性/刷新默认值，购买/限购/库存逻辑完全同一套；声望商店 visible 默认 true 但商品整体被门槛挡住时列表显示"[需要 信赖]"而非整店消失；黑市 = 配置组合非新引擎）。
- **实现要点**：
  - shop.json 加载与校验：id 唯一、name 禁空格、type 枚举、currency 必须在 settings 货币注册表；缺省全兜底（currency=第一货币、refresh=daily 05:00、门槛 0、price_fluctuation=0）。
  - 可见性判定管线：visible && open_condition（A2）&& 时间窗（once start/end，见 C3）&& 等级/声望门槛；npc 类型经 action:shop 挂载（B6）；event 类型依赖活动时间窗；blackmarket 依赖 open_condition 入口。
  - 声望商店列表显示规则：整店被门槛挡住 → 显示"[需要 信赖]"不整店消失（看得见买不着 = 目标感）；条目级门槛显示于商品标记（C4）。
  - 引擎同一性：5 类型共用购买/限购/库存一套代码路径，类型只影响入口与默认值。
- **验收标准**：5 类型各 1 加载用例；可见性管线各条件拦截正确；声望商店门槛显示"[需要 信赖]"不消失；event 未开门提示"这家店还没开门"；购买逻辑无类型分叉（代码路径断言）。

### C2. 商品条目模型（global/personal · stock 0=无限）
- **定稿依据**：商店 §3.2（条目字段：item/price/currency/scope/stock/refresh/limit/period/reputation_required/min_level/discount/sold_out_once；scope=global 默认 / personal；stock 0=无限；旧 stock/per_player 字段自动映射）+ §六（商品范围：global 全服共享计数存世界状态 key=shop_id+item_id 跨群共享；personal 每人独立计数存玩家存档；限购只管买入，出售不计入限购；周期重置惰性）+ §3.3（价格体系：基准价/覆盖价/出售价/混合支付）。
- **实现要点**：
  - 条目解析默认值兜底：price=items 基准价、currency=商店货币、scope=global、stock=0（0 即无限，不设上限分支）、limit=0 不限、period=day。
  - global：库存计数存世界状态（shop_id+item_id 键）；N 件售罄 → "已售罄（下次补货…）"；sold_out_once=true 售出后永久下架（刷新不恢复）。
  - personal：limit/period 计数存玩家存档（碎片化续玩不丢）；周期重置走 A3 惰性；买入计数、出售不计入。
  - 混合支付 price 对象 {coins:50,gems:5} → 两币同扣；discount 折扣在列表显示原价划线。
  - 旧字段兼容：stock→scope=global；per_player/per_player_period→scope=personal+limit/period。
- **验收标准**：缺省条目 = 基准价+商店货币+无限库存；stock=0 购买不限量；global N 件售罄且刷新回满；sold_out_once 永久下架；personal 限购计数跨天重置且出售不影响；混合支付两币同扣。

### C3. 商店刷新引擎（daily/weekly/once/none · 黑市重抽重浮动）
- **定稿依据**：商店 §七（refresh 四模式：daily 默认 05:00 与 quest_daily 同刻对齐 / weekly / once 时间窗 / none 不刷新；刷新语义三件事同刻发生：①全服库存回满 ②个人限购清零 ③黑市商品重抽；sold_out_once 条目不恢复；实现要点：存档 last_refresh 惰性补刷、跨周判定、时区 UTC+8；once：start 未到="这家店还没开门"、end 已过=自动下架；不配 refresh=永不刷新 + stock 0 = 无限库存无限购最简形态）。
- **实现要点**：
  - refresh 配置解析四模式 + 默认值（daily、hour 默认 5）。
  - 补刷执行（复用 A3 日界/周期工具）：判定 last_refresh → 当前时间跨越周期 → 执行三件事：库存回满（stock 归 N）、个人限购清零（personal 计数清 0）、黑市重抽（pool[] 按配置数量抽 N 上架 + 上架价 = 基准价 × (1 ± price_fluctuation% 随机浮动)；商店本体不变）。
  - sold_out_once 条目从回满清单排除（永久下架）。
  - once 模式判定：未到 start → "这家店还没开门"；过 end → 自动下架（活动商店）；weekly 跨周按 结束 > 开始 判定日期偏移。
- **验收标准**：四模式各 1 用例；离线数天后首次操作正确回满/清零/重抽；黑市价格落在浮动区间且每次刷新重抽；sold_out_once 不随刷新恢复；once 未开门/自动下架两提示正确。

### C4. /商店 列表与切换（当前商店优先 · 序号/名称/列表 · 翻页）
- **定稿依据**：商店 §二（/商店 查看当前商店商品列表，当前商店优先、无则全局默认商店；/商店 2 序号切换（多商店时）；/商店 铁匠铺 名称切换（禁空格）；/商店 列表 可用商店一览（含类型图标/门槛标记））+ §2.1（回复模板：首行 LV{等级}.{玩家名}；商品行 序号.物品名｜商品单价：价格(货币名)；分隔线；底部 当前页：N/M(全部) + Tip"发送'购买+物品名'即可购买商品"；≤10 一屏；置灰不隐藏）+ §9.3（商品列表格式：店名+商品行+玩家余额+操作提示；标记优先级：声望不足/售罄/限购满/折扣（原价划线）；分页"1/3 页：/商店 2 翻页"）。
- **实现要点**：
  - 解析分派：无参 → 当前商店商品列表（当前商店优先，无则 normal 全局默认）；"列表" → 可用商店一览；纯数字/名称 → 切店（多店场景）；名称禁空格、允许 ·/Ⅱ。
  - **定稿双语法消歧（登记为解析器上下文规则）**：/商店 后跟纯数字——若处于"当前商店商品列表已被展示"的上下文 → 解释为翻页（§9.3）；否则（未打开商店或多店选择场景）→ 解释为商店序号切换（§二、§八⑤）。上下文标志随当前商店状态维护。
  - 回复模板逐字实现：首行 "LV{等级}.{玩家名}"（例 LV3.阿明）；商品行 "序号.物品名" + "商品单价：价格(货币名)"；条目间分隔线；底部 "当前页：N/M(全部)" + Tip（紧凑格式提示）；≤10 条一屏。
  - 商品行标记：声望不足（显示所需等级名）/ 售罄 / 限购满 / 折扣（原价划线）；置灰不隐藏（买不起也显示）。
- **验收标准**：模板逐字对齐（LV 行/分隔线/当前页/Tip）；当前商店优先、无则全局默认；多店序号与名称切换；列表一览含类型图标与门槛标记；翻页 ≤10 一屏且消歧规则命中两用例（切店 vs 翻页）。

### C5. /购买 管线（校验链 6 步 · 原子防双扣）
- **定稿依据**：商店 §9.1（购买管线：* 左侧名称精确匹配 → 失败按列表序号 → 再失败别名/x_ 扩展；校验链顺序即提示优先级：①开店②门槛③个人限购④全服库存⑤货币余额⑥数量上限；结算原子操作：扣货币→物品入包（堆叠）→扣库存/记限购→存档 SQLite 事务；会话快照（3.18）幂等：网络重发不双扣）+ §五（结算流程：解析货币键→查余额→扣款入包；余额不足提示差额；持有上限联动可配默认不提示）+ §二（* 连数量，默认 1，上限默认 ≤99 可配；紧凑格式'购买+物品名'双认）。
- **实现要点**：
  - 解析：`/购买 药水*5`（规范）/ `购买+药水`（紧凑无空格）双认；* 数量默认 1、上限默认 99（可配）；名称 → 序号 → 别名/x_ 三级回退。
  - 校验链按定稿顺序实现，命中即返回定稿文案：① 商店存在且可见 → "这家店还没开门"；② 门槛等级/声望（商店级与条目级取更严）→ "声望不足：需要 信赖"；③ 个人限购 → "今日限购 3 个，已买 3 个"；④ 全服库存 → "已售罄（下次补货：明早 4 点）"；⑤ 货币余额（混合支付两币同查）→ "金币不足，还差 20"；⑥ 数量上限 → "最多一次购买 99 个"（提示不拦截）。
  - 结算原子性：扣货币 → 物品入包（堆叠）→ 扣库存/记限购 → 存档，整段 SQLite 事务；任一步失败整体回滚（不扣款不扣库存不入包）。
  - 幂等防双扣：操作携带 tx id 写入会话快照（3.18），网络重发同 id 幂等返回成功结果，不二次扣款。
  - 持有上限联动：货币到上限时产出提示（可配默认不提示）。
- **验收标准**：6 条校验提示逐一命中（构造用例）；事务失败用例整体回滚；同一购买命令重发不双扣（幂等断言）；混合支付两币同扣；紧凑格式与规范格式等价。

### C6. /出售 管线
- **定稿依据**：商店 §9.2（价格 = 基准价 × 出售比率（settings 通用默认 30%）或 items.json sell_price 单条覆盖；出售 = 立刻到账，碎片化无二次确认；大额出售确认可选默认关；禁止出售：绑定物品 bound=true / 任务关键物品 sellable:false 或 x_ 标记 / 数量不足 → "背包里只有 12 个铁矿"；回复"✅ 出售成功：铁矿×20（+240 金币），剩余 1,240 金币"）+ §六（出售不计入限购计数）。
- **实现要点**：
  - 价格解析：sell_price 单条覆盖优先，否则基准价 × 出售比率（默认 30%，settings 可配）。
  - 校验拦截：bound=true 拒绝；sellable:false 或任务关键标记拒绝；持有数量不足提示差额；全部通过后立即到账（物品出包 + 货币入账，同事务）。
  - 回复模板："✅ 出售成功：{物品}×{N}（+{收益} {货币}），剩余 {余额} {货币}"。
  - 可选：大额出售（回收 >N 金币，N 可配）黄色确认提示，默认关。
  - 出售不触碰个人限购计数与全服库存（只收货币）。
- **验收标准**：30% 比率与 sell_price 覆盖各 1 用例；绑定物/任务关键物拒售；数量不足提示准确；出售后背包数量与货币余额正确；限购计数不受出售影响。

---

## D. 任务系统

### D1. 任务条件三原语引擎
- **定稿依据**：任务 §一（三原语：值型（当前值比较）/ 累计型（长线计数 kill_count/gain_count → longline_counters）/ 事件型（var 前缀 [事件:]，param=目标）；"判定 100% 由统一 conditions 数组驱动"；op 符号双写等价；consume 布尔收敛 deliver/collect）+ NPC §4.0（任务全文统一本引擎）。
- **实现要点**：
  - 复用 A2 求值器；任务侧三原语断言：值型读当前值（item_count+param、level 等）、累计型读 longline_counters（只增不减，任务只读不重复计数）、事件型读事件计数。
  - conditions[] 数组 = 逻辑与关系逐条求值；consume 收敛语义：true=deliver（交付时扣物）、false=collect（计数不扣，与掉落联动不抢材料）。
  - 中文变量键经互译表（A2/NPC §4.3）映射英文条件键存储；main_progress 默认语义 = 已完成 main:true 主线任务计数（读 longline_counters）。
- **验收标准**：三原语各 1 用例（收集/击杀/事件）求值正确；consume=true 扣物与 false 不扣两路径；[背包:铁矿] 与 {item_count,param:铁矿} 等价；进度条显示当前值/目标值。

### D2. 任务三表存储与每日防刷
- **定稿依据**：任务 §四（三表设计：① longline_counters 框架级长线计数只增不减、任务只读跨任务复用 ② quest_active 进行中任务表：ID+名称冗余对齐 3.18、非会话无锁无超时、热重载删配置降级提示 ③ quest_daily 每日防刷表：今日完成数/接取数/重复衰减计数；每日重置懒计算默认 05:00）+ §3.4 板规则（接取上限 ≤5、每日完成上限 ≤10（防刷）、重复衰减默认关，全部可配 0=不限）。
- **实现要点**：
  - 三表落玩家存档：longline_counters（累计击杀/累计获得/图鉴进度/副本通关/主线完成数，只增不减）；quest_active（接取任务 + 当前进度，ID+名称冗余，非会话无锁）；quest_daily（今日完成数/接取数/衰减计数）。
  - 防刷执行点：接取时查接取上限（同时接取 ≤5）；完成结算时查每日完成上限（≤10）；超限拦截并提示；上限 0=不限。
  - 每日重置：走 A3 日界懒计算（05:00 与商店/签到同刻），无定时器依赖；重复衰减（奖励递减）默认关。
  - 热重载：删配置 → quest_active 旧条目降级提示不报错。
- **验收标准**：三表字段与语义正确；计数只增不减且跨任务复用；断线/重启后任务与进度不丢；接取/完成超限被拦截；05:00 与签到/商店同步重置（共用 A3 断言）。

### D3. 任务板与主线置顶
- **定稿依据**：任务 §3.1（主线 = 常驻任务列表，随时可接取不消失；/任务 里主线置顶；引导+兜底；实现 main:true 常驻不移除）+ §3.4（/任务 查看任务列表：主线置顶 + 板上任务 + NPC 支线；板规则：daily 槽 + 可选 weekly/event 槽；刷新懒计算；双任务板指令归属仲裁：/任务 = 玩家任务板，炼金委托板走 /委托 独立带板前缀，不占 /任务 路由）。
- **实现要点**：
  - /任务 组装顺序：主线（main:true）置顶 → 板上任务（board 槽位任务）→ NPC 支线（B4 条件匹配后进入 quest_active 的）。
  - 主线任务标记 main:true：不随刷新移除、不设槽位上限；可随时接取。
  - 板槽位与刷新：daily 槽每日懒计算刷新（A3）；weekly/event 槽可配。
  - 路由仲裁：裸 /任务 /接取 /交付 /放弃 归玩家任务板；委托板操作必须带板前缀（/委托 查看/接取/交付），不占用 /任务 路由。
- **验收标准**：列表顺序 = 主线置顶 → 板上 → NPC 支线；主线完成/放弃不移除；/委托 带前缀独立路由正常、裸 /任务 不响应委托板内容。

### D4. 任务统一 reward 条目接入（任务侧）
- **定稿依据**：任务 §2.1（reward 单一 schema：物品条目 / 货币键值 / 组合数组按序入账；内联键值串 = 同一解析器的序列化糖，等价展开；发放器唯一实现 = A1）+ §二（reward 字段：str 或 list，均经解析器展开）。
- **实现要点**：
  - reward 字段双形态解析：结构化条目数组直通；内联串（"exp=50,coins=80,item:铁矿*3"）加载时经 A1 解析器等价展开为条目数组。
  - 发放时点：交付结算时统一入账（主时点）；rep 条目在声望模块开启时入 reputation_state（A1）；bonus（条件倍率奖励 ×N）在满足加分条件时乘算。
  - 任务奖励默认绑定（防套利），与签到/商店奖励同语义。
- **验收标准**：内联串与结构化数组发放入账等价；物品/coins/gem/exp/rep 五类条目各 1 用例；组合数组按序入账；bonus 倍率生效（×1.5 用例）。

### D5. /任务 指令闭环（/接取 N · /交付 N · /任务信息 · /放弃）
- **定稿依据**：任务 §3.4（/任务 查看列表；/接取 3 接取 3 号任务（序号不带 *）；/任务信息 查看已接任务进度；/交付 2 交付 2 号任务（consume=true 扣物；自动计数任务只需 /交付 领奖）；/放弃 2 放弃任务（可配惩罚默认无））。
- **实现要点**：
  - /接取 N：列表序号解析（序号不带 * 数量语法）；重复接取拦截（has_quest 判定）；接取上限检查（D2）；写入 quest_active。
  - /交付 N：交付结算事务 = 条件复核（A2 当前满足）→ consume=true 扣物（原子）→ 发放奖励（A1/D4）→ 置 done；collect/自动计数任务（consume=false）= 直接领奖结算；done 状态幂等：重复交付返回"已完成"，不双发奖励（防双交付）。
  - /任务信息：已接任务条件逐条求值，显示 当前值/目标值（读 longline_counters 与当前值）。
  - /放弃 N：默认无惩罚；释放接取槽位；放弃后任务可重新接取（接取计数相应处理）。
- **验收标准**：接取 → 进度 → 交付全链路 1 用例；consume 扣物与 collect 不扣两路径；重复交付幂等（奖励不双发）；接取超限被拦；放弃释放槽位且无惩罚。

---

## E. 签到系统

### E1. 签到多表加载（loop / monthly / activity）
- **定稿依据**：签到 §3.1（表定义：id/name/type/period/rewards 四通道/makeup/bonus）+ §3.4（多表并存：loop 常驻循环 + monthly 月度 + activity 活动同时生效，一次 /签到 全部结算；活动表 start/end 懒计算判定，未开始/已过期自动停用不报错；bonus 活动加成结算乘算）+ §7 字段元数据（cycle_days 默认 7；monthly 周期=当月天数自动；activity period 必填）+ §二（漏配天数 = 复制第 1 天兜底 + 黄提示）。
- **实现要点**：
  - checkin.json 多表加载：type 枚举（loop/monthly/activity）校验；monthly 周期自动取当月天数；activity 校验 start/end 必填。
  - 四通道定义解析：rewards.daily[]（day 1..N 轮转）/ streak[]（days 里程碑阈值）/ monthly_total[]（签满 N 天，不要求连续）/ makeup。
  - 活动表生效判定：start/end vs 当前时间（A3 周期工具）——未开始/已过期自动停用，不报错；bonus 倍率在结算时统一乘算。
  - daily 缺配天数运行时补全：复制第 1 天条目 + 黄提示"第 3-31 天未配置，已按第 1 天奖励补全"。
- **验收标准**：三类型表同文件并存加载；活动表未开始/过期自动停用；monthly 当月天数正确；漏配日按第 1 天兜底并提示；bonus 倍率生效。

### E2. 签到结算管线（连签 / 月度累计 / 断签 / 幂等）
- **定稿依据**：签到 §3.3（结算管线 6 步写死：①懒计算跨天判定（同天 → "今天已签到"幂等返回；跨天 → streak+1；断签且 reset_on_break → 归 1）②领 daily[day] ③streak 里程碑检查 ④monthly_total 检查（断签不清零，碎片化铁律）⑤写存档：last_date/streak/month_total/signed_days[]/makeup_used ⑥汇总输出）+ §3.2（断签重来 reset_on_break=true 默认开）+ §六（3.18 四项原则：ID+名称冗余 / version 幂等防双发 / 旧配置结算 / 非会话不回收）+ §13 验收（多表并存一次结算各自独立连签；发奖失败兜底黄字跳过不吞整次签到）。
- **实现要点**：
  - 单次 /签到 循环遍历所有生效表，每表独立执行 6 步结算（各自独立连签、独立 day 轮转）。
  - 跨天判定走 A3 日界：同天 → 幂等返回"今天已签到"不重发；跨天 → streak+1；间隔 >1 天且 reset_on_break=true → streak 归 1。
  - daily 领周期内第 day 天（day 从签到状态推导）；streak 达阈值里程碑额外发；monthly_total 达累计天数阈值额外发（不要求连续）。
  - 写存档：last_date / streak / month_total / signed_days[] / makeup_used（按表 ID 分键）。
  - 幂等与兜底：version 幂等防双发；发奖走 A1（单条失败黄字跳过，不吞整次签到）；旧配置结算（新配置下个周期生效）；30 天回收不适用（常驻存档）。
- **验收标准**：连续签到 streak 逐日递增；断签（间隔 2 天）streak 归 1；月度累计断签不清零；同日重复签到幂等返回"今天已签到"不双发；多表一次结算各自独立连签；凌晨 0-5 点归属前一天。

### E3. 补签（可选模块 · 默认关）
- **定稿依据**：签到 §3.2（补签通道：两通道任一满足——补签卡（物品，签到奖励/商店可卖）或货币付费 cost；makeup.enabled 默认关；max_per_month 月上限 0=不限）+ §二（/签到 补签，makeup.enabled 时可用）+ §8 校验（补签开启但无任何费用通道 → 黄提示"补签不花钱？确认"）。
- **实现要点**：
  - makeup.enabled=false 时：/签到 补签 返回不可用提示，不扣费。
  - 开启后：校验费用通道存在（补签卡或 cost 至少其一，否则校验器黄提示）；消耗补签卡或货币 → 回填当日为已签（signed_days[]、today 标记、当日 daily 奖励入账），幂等（同日不可重复补）。
  - 月上限：makeup_used 计数对比 max_per_month（0=不限），达限提示。
  - 补签与连签关系：补签回填当日签到状态，按结算管线自然衔接（不额外制造断签/连签分支）。
- **验收标准**：默认关时指令被拒且不扣费；开启后补签卡与货币两通道各 1 用例；月上限达限拦截；同日补签幂等；无费用通道配置被校验器黄提示。

### E4. /签到 指令与 [签到:*] 条件键
- **定稿依据**：签到 §二（/签到 → 一次完成所有生效表，汇总单条消息输出：今日奖励 + 连签天数 + 进度"8/31" + 各表奖励流水防刷屏；同日重复 → "今天已签到"；/签到 补签；列表模板 5 条/页上限）+ §四（新增 var 键 [签到:连续天数] / [签到:本月天数] / [签到:今日已签]，NPC 条件引擎共用、入 NPC 4.3 键空间）+ §十一 登记要求。
- **实现要点**：
  - 指令路由：/签到（无参）→ E2 全表结算 → 单条消息汇总输出（今日奖励 + 连签天数 + 进度 + 各表流水）；/签到 补签 → E3。
  - 汇总输出限幅：列表模板 5 条/页上限（全局规范）。
  - 条件键注册（A2 键空间）：[签到:连续天数]（读当前表 streak，param 指定表 ID，缺省=全部生效表取最大/主表）、[签到:本月天数]（读 month_total）、[签到:今日已签]（读当日 signed_days 标记）；任务/NPC 条件可直接引用求值。
  - 落点：条件求值读 checkin_state（按表 ID 分键），与 E2 写存档同一来源，无第二状态副本。
- **验收标准**：/签到 汇总单消息输出格式正确（今日奖励/连签天数/进度）；同日重复文案"今天已签到"；三条件键在任务/NPC 条件中引用并求值正确（各 1 用例）；5 条/页限幅生效。

---

## 实施顺序建议（依赖拓扑）

1. A1 → A2 → A3（公共基础，三系统全部下游依赖）；
2. B1 → B2 → B5 → B4 → B3（对话链：列表 → 会话 → 对话树 → 动作引擎 → 发牌员），B6 随 B4 动作 shop 分支；
3. C2 → C3 → C1 → C4 → C5 → C6（条目 → 刷新 → 类型可见性 → 列表/切换 → 购买 → 出售），其中 C4 依赖 B6 当前商店状态；
4. D2 → D1 → D4 → D3 → D5（存储 → 条件 → 奖励 → 板 → 指令闭环）；
5. E1 → E2 → E4 → E3（加载 → 结算 → 指令/条件键 → 补签）；
6. 不变量（跨系统硬约束）：reward 发放仅 A1 一处实现；条件求值仅 A2 一处实现；日界判定仅 A3 一处实现；防双扣/防双发/防双交付统一走"事务 + 幂等键"模式（C5/C6 同款）。