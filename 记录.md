# 记录.md

> 规则 1 通用工作记录：记录每次完成的工作、下一步工作、未完成工作。
> 最后更新：2026-08-28

---

## 〇、待办事件（未完成 · 下次会话优先处理）

### 2026-08-29 · M8 批11-2 装配接线完成（主 agent 收口裁决 3 项冲突后接线）
- **子 agent 发现 3 项冲突并停下等裁决**：
  - **冲突 A（P0）/图鉴 双注册**：codex_commands（M7 BCH-08）已注册「图鉴」+ alchemy_commands（批8-2）又注册 → Router.register 抛 ValueError。**裁决：/图鉴 合并到 codex 分册体系**——alchemy_commands 移除 CODEX_CMD 注册（cmd_codex → 公开 `render_alchemy_codex(ctx)`），codex.py CATEGORIES 加 alchemy 分册（("recipe","item")）+ CATEGORY_ORDER/_CATEGORY_LABELS，codex_commands _CAT_ALIASES 加炼金别名 + cmd_codex 对 alchemy 分册调 render_alchemy_codex（惰性单向 import 防环）；test_codex 全局均值断言 3 册→4 册（合理行为变化）
  - **冲突 B/珠升阶 缺白名单**：**裁决补「珠升阶」**进 parsers.py DEFAULT_WHITELIST（批7-2 已实现指令，缺白名单 S5 静默不响应）
  - **冲突 C/currencies 就地可变**：**裁决非 bug 保留**——ctx["currencies"]=dict(player.currencies) 拷贝是入账桶语义（reward/gem_wallet 就地改桶），runner _player_from_dict(ctx) 重建 Player 时回写落档（M6 已有机制）
- **接线**：router_setup.py 注册 alchemy_commands（30+ 指令；/图鉴 并入 codex）；context.py make_context M8 引擎注入（registry/session_mgr/items/recipe/traits 表 + battle_snapshot/battle_alchemy_engine/upgrade_unlocks/wallet/prof_engine/resolve_player_name/same_group 注入位 + registered 段 **_inventory_hooks + upgrade_unlocks 回填）；test_assembly_router ALL_REGISTERED 加 M8 指令集 + 一致性断言更新（「炼金」已注册移出未注册锚点）
- **验收**：新增 test_m8_assembly.py 7 例 + 全量 pytest 回归全绿（1 skip）+ ruff ✓ + mypy 337 文件 Success
- **commit**：见 git log（feat: M8 批11-2 装配接线）
- **下一步**：批12 验收（verify_m8 全链路冒烟 + 全量回归终检）+ 记录.md 收尾

### 2026-08-29 · M8 批11-1 指令装配完成（单路独占 parsers.py + alchemy_commands.py，22 词白名单补全 + /协力）
- **parsers.py DEFAULT_WHITELIST 补全 22 词**（66 词）：继承超 确认 放弃 调合续 深度炼金 进化 镶核心 加成 成品合成 分解 登记 复制 配方合成 特性合成 挑战 即时调合 教学 协力 种植 收获 收取 技能面板（M8 组注释块；/秘钥 不注册未知指令）
- **/协力 cmd_assist**（+200 行）：GU-44 大师/GU-45 会话中/GU-46 同群（ctx same_group/@ 剥离）；F-15 消耗材料（快照材料链优先 配方兜底）+ 随机加成（确定性种子 _assist_seed 字符加权哈希×1000003+version 防 PYTHONHASHSEED 影响，random.Random 可复现；4 项社交向轻量加成池：品质上限+10/属性刻度+5/连锁效果+1 级/投入次数+1）→ 写会话快照 assist_bonus；M-15 渲染
- **验收**：单测 21 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff ✓ + mypy 336 文件 Success
- **commit**：见 git log（feat: M8 批11-1 白名单补全+协力）
- **下一步**：批11-2 assembly 装配接线（make_context 注入 session_mgr/repo/引擎/hooks + Router 全量注册 + check_all/run_all_tests 接入 + e2e 冒烟）——M8 最后接线

### 2026-08-29 · M8 批10-2 资源循环指令壳完成（单路独占 alchemy_commands.py +190 行，21 测）
- **4 指令壳**（async）：/种植（GU-60 正式 → HarvesterEngine.plant now 由壳层注入 ctx[now]/time.time 兜底 → 透传「已种植〈种子〉，4 小时后可收获」）；/收获（无参 → harvest 透传「收获〈材料〉×N（品质 精良·继承特性：…）」折叠单条）；/代工（**指令名「代工」非「雇工」**：GU-62 精通 → parse_task_spec 键值列表解析（代采=材料*数量,代调=配方*数量）→ HelperEngine.assign 能源道具校验 → 透传「开始代采 矿石*5（消耗 糖果×1）」）；/收取（无参 → collect 透传「收取：矿石×5、药剂×4」空队列提示）
- **主 agent 收口**：修 ruff E501 1 处；helper.py docstring 3 处「雇工」为改名历史说明（合规保留）
- **验收**：单测 21 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff ✓ + mypy 335 文件 Success
- **commit**：见 git log（feat: M8 批10-2 资源循环指令壳）
- **下一步**：批11 装配接线（DEFAULT_WHITELIST 补全 23 词/parsers 白名单/quantity_commands/Router 注册/make_context 注入/引擎 ctx hooks/代工后台 tick 调度）——M8 最后的大批次

### 2026-08-29 · M8 批10-1 资源循环引擎完成（2 路并行）
- **路A** `core/alchemy_harvest.py`（914 行）+ 24 测：HarvesterEngine——plant（GU-60 正式/GU-61 种子 seed 标记+空闲地块 默认 3 可配/F-21 种植 harvest_at=now+定时 默认 4h 可配）；harvest（收全部成熟地块 品质≥种子品质 quality_floor 继承特性 正式1/精通2/专家3 可配 超出丢弃+提示 折叠单条消息）；greenhouse（温室 大师解锁 复制素材 消耗宝石/金币）；plots_of（地块存档）
- **路B** `core/alchemy_helper.py`（810 行）+ 26 测：HelperEngine（**全「代工」语义无「雇工」**）——assign（/代工 设定 代采=材料*数量,代调=配方*数量 键值列表解析 GU-62 精通/GU-63 能源道具糖果馅饼类 队列容量/状态存档）；tick（后台定时产出 离线累积）；collect（/收取 入包 失败留队 空队列提示）；level_of+level_bonus（助手等级 1~5 特性更多/采集品质更高 L464）
- **验收**：单测 50 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff ✓ + mypy 334 文件 Success
- **commit**：见 git log（feat: M8 批10-1 种植收获+代工引擎）
- **下一步**：批10-2 指令壳（/种植 /收获 /代工 /收取）

### 2026-08-29 · M8 批9 战斗即时调合完成（2 路并行 + 主 agent battle.py 收口）
- **路A** `core/alchemy_battle.py`（809 行）+ 22 测：BattleAlchemyEngine——instant_eligible（GU-50~52/54 战斗中/大师/能量/限次 per_battle_limit=1）；carry_ok（GU-53 携带素材不足全拒差异）；resolve（BA-07/08 一步出结果 原子扣减→产出→auto_use 当场结算或入包 二选一 计数自增回写）；intensity（BA-10 强度公式 技能×(1+0.4×冷却数) 可配）；cooldown_of（BA-06 炸弹 3 回合冷却）；read_used/write_used（battle_alchemy_used 顶层键读写）；consume_energy（R-08 关则直通）；**零会话依赖（BA-01 战斗内子流程写死 豁免互斥）**
- **路B** `alchemy_commands.py` 追加 /即时调合（+192 行）+ 14 测：cmd_instant 守卫链 GU-50~54 → 一步出结果（resolve 传 battle_alchemy_used/auto_use/cooldown/use_fn）→ 写回 battle_alchemy_used+1 → M-17 一行渲染（M5 无 emoji 纯文本）
- **主 agent 收口**：battle.py _snap 初始化加 `battle_alchemy_used: 0`（to_snapshot 深拷贝自动带出，中断恢复不清零 新战斗 start 清零）+ `record_alchemy_used(n)` 方法供战斗层持久化
- **验收**：单测 36 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff ✓ + mypy 330 文件 Success
- **commit**：见 git log（feat: M8 批9 战斗即时调合）
- **下一步**：批10 资源循环（/种植 /收获 /代工 /收取——**/雇工 已改名 /代工，2026-08-28 拍板**）

### 2026-08-29 · M8 批8-2 深度炼金指令壳完成（单路独占 alchemy_commands.py +790 行，17 测；子 agent 迭代上限截断，主 agent 补写测试）
- **8 指令壳**（async）：/深度炼金（GU-20 大师「深度未解锁」→ deep_snapshot → acquire(challenge_alchemy) → 扣能量 → 深度面板 6 槽/核心槽/刻度/进化线 + 首次解锁公告教学）；/进化（GU-23 宗师 + GU-24 产出 N 次 合成不计 produce_counts 长线计数 → evolve_unlock 永久解锁「✅ 继承 2 额外槽（6 槽）」幂等 ATO-05）；/镶核心（GU-26 深度会话中 → mount_core 品质上限+20 火适配/核心不匹配拒绝/可换）；/加成（GU-28 宗师 + 限 1 次/调合）；/挑战（GU-47 宗师 + 材料×2 全量 → 挑战会话 challenge 标记）；/图鉴（codex_summary+codex_reward 幂等+king_eligible 全亮→「炼金王」称号 TTL-01，M5 无 emoji 纯文本）；/技能面板（SP 查看 + 解锁=面板项 子词 → skill_panel_unlock）；/教学（目录/机制名/升大师 6 机制预览）
- **主 agent 收口**：补写 test_deep_commands.py（17 例，parse_command 白名单 W 注入 + ctx 顶层 currencies 修正）+ 修 ruff 2 处
- **验收**：单测全绿 + 全量 pytest 回归全绿 + ruff ✓ + mypy 327 文件 Success
- **commit**：deb61ec
- **下一步**：批9 战斗接线（/即时调合 战斗内子流程 + 战斗拦截）

### 2026-08-29 · M8 批8-1 深度炼金+图鉴教学技能面板引擎完成（2 路并行）
- **路A** `core/alchemy_deep.py`（962 行）+ 40 测：DeepEngine——deep_eligible（GU-20 大师）/deep_snapshot（F-06 深度会话快照 6 槽/核心槽/进化线/challenge_alchemy 类型）/evolve_eligible+evolve_unlock（F-07 宗师+炼金产出 N 次 合成不计 永久解锁 幂等 ATO-05）/mount_core（COR-01~03 核心不匹配拒绝 可换 destroy/return）/buff（GU-28 限 1 次）/challenge_check+challenge_settle（F-16 苛刻条件 连锁≥5 且/或 刻度≥2 → 达标上限+10/未达标降级+退 50% 材料 只退一次 ATO-06）
- **路B** `core/alchemy_meta.py`（520 行）+ 16 测：AlchemyMeta——codex_summary（图鉴进度 lit/total/all_lit registry 分母 fail-safe）/codex_reward（成长奖励幂等）/king_eligible（TTL-01 全亮→王称号 与等级解耦）/skill_panel_view+skill_panel_unlock（SP 自选解锁）/tutorial_catalog+tutorial_show（教学目录）/master_announcement（升大师 6 机制预览）
- **主 agent 收口**：修 alchemy_deep.py L917 🏆 非白名单 emoji（M5 拍板→纯文本）+ 同步 2 处测试断言
- **验收**：单测 56 例全绿 + 全量 pytest 回归全绿 + ruff ✓ + mypy 326 文件 Success
- **commit**：e3ef916

### 2026-08-29 · M8 批7-2 珠与合成指令壳完成（单路独占 alchemy_commands.py +498 行，28 测）
- **8 指令壳**（async，守卫→引擎→透传，零 NoneBot 仅 ✅/❌）：/镶嵌（JewelSystem.mount 槽级≥珠档 战斗中拒绝）/拆珠（unmount 无损）/珠升阶（UpgradeEngine _exec_jewel 3×同档同ID+宝石10→+1阶 禁跳级 **无职业硬门槛拍板③**）/成品合成（宗师+宝石10 组合表）/配方合成（专家+宝石5 两配方已学 组合命中 已解锁幂等）/特性合成（宗师+宝石20 同系校验）/登记（AlchemyRegister.register 模板持久化）/复制（大师 未登记拒绝 复制消耗 数量上限提示不拦）
- **工程补白 7 helper**：_upgrade_recipes_by_subtype（升级实例解析 U-S1+subtype 过滤）/组合表自推导+ctx[combos] 覆盖/_mount_auto_slot 自动选槽/_copy_qty 数量兜底等
- **遗留**：8 新指令 parser 白名单（除 镶嵌/拆珠 已在 DEFAULT_WHITELIST）与 quantity_commands 登记归批11A 装配（IF-34）
- **验收**：单测 28 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff ✓ + mypy 322 文件 Success
- **commit**：见 git log（feat: M8 批7-2 珠与合成指令壳）
- **下一步**：批8 深度炼金（/深度炼金 /进化 /镶核心 /加成 /挑战 /教学 /图鉴 /技能面板）

### 2026-08-29 · M8 批7-1 珠系统+复制登记引擎完成（2 路并行）
- **路A** `core/jewel.py`（754 行）+ 29 测：JewelSystem——slot_accepts（BEL-03 槽级→珠档映射 1=普通 2=精良及以下 3=全部）；stack_key（BEL-15 堆叠键=ID+品质档+特性集 特性集排序去重键变分堆）；diminish_mult（BEL-10 同名递减表驱动 1.0/0.5/0.25/0.0 第4颗不叠加）；trigger_limit（BEL-11 战斗触发上限默认 3 可配）；mount/unmount（SOCK-02/03 槽级≥珠档+绑定+无损拆珠+战斗中拒绝）；can_toggle_in_battle（SOCK-05）；供批9 战斗接口 battle_trigger_bucket/record_trigger/trigger_remaining（计数落 battle_snapshot.jewel_triggers 中断不清零）
- **路B** `core/alchemy_register.py`（663 行）+ 17 测：AlchemyRegister——register（DUP-06 登记模板持久化 ctx[registered] 换包同 ID 保留）/is_registered（DUP-02 未登记→「未登记复制」）/copy_cost（DUP-03 ⌊cost.coins×20%⌋ 宝石 费率 gem.复制 缺省 0.2 可配+额外消耗 gem.复制额外）/copy（DUP-01~05 已登记+标准版→数量上限 2147483647 超限提示不拦→原子校验宝石×N+材料×N 全拒差异→快照-回滚→产标准版 add_item）/is_copyable（DUP-04 标准版判定 无 traits/awaken/evolution/core 标记）
- **消费点**：批7-2 指令壳（/镶嵌 /拆珠 → JewelSystem；/珠升阶 → UpgradeEngine._exec_jewel；/成品/配方/特性合成 → UpgradeEngine；/登记 /复制 → AlchemyRegister；门槛 GU-34 大师 由指令壳判）
- **验收**：单测 46 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff ✓ + mypy 321 文件 Success
- **commit**：见 git log（feat: M8 批7-1 珠系统+复制登记引擎）
- **下一步**：批7-2 八指令壳（/镶嵌 /拆珠 /珠升阶 /成品合成 /配方合成 /特性合成 /登记 /复制）

### 2026-08-29 · M8 批6 终态指令壳+宝石货币完成（2 路并行）
- **路A** `alchemy_commands.py` 追加 /确认 /放弃 /调合续 /分解 4 指令壳（+300 行）+ 26 测：cmd_confirm（GU-17 会话守卫 → TraitInherit.check_placement_conflict 复核 → SettleEngine.confirm 透传 message_id 幂等「已结算」M-05）；cmd_abandon（/放弃 材料不结算终态）；cmd_resume（/调合续 GU-18 挂起恢复/已有活跃拒绝）；cmd_decompose（鸭子消费 ctx["wallet"].decompose + inspect.isawaitable async 防御 + *N 数量 + 两段式消息 M-10）
- **路B** `core/gem_wallet.py`（533 行）+ 31 测：GemWallet——decompose（标准版拒绝 REASON_STANDARD_NOT_DECOMPOSABLE/材料×回收率向下取整/宝石平铺基础值 普通1/精良3/史诗8/传说20 拍板① 不乘回收率/两段式消息数据）；decompose_rate（正式0.4→王0.65 可配 见习恒0）；gem_base_value（可配）；grant_gem（currencies.gem 统一入账 分解/挑战/掉落/品评会四来源共用）；is_decomposable（非标准版口径）
- **消费点**：批7B/7C 宝石扣减走 ctx["currencies"]["gem"]（合成 cost.gem 同款），grant_gem 只管入账；标准版标记/常量供登记校验复用
- **验收**：单测 57 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff ✓ + mypy 317 文件 Success
- **commit**：见 git log（feat: M8 批6 终态指令壳+宝石货币）
- **下一步**：批7-1 珠系统引擎（/镶嵌 /拆珠 /珠升阶 /珠特效）+ 复制登记引擎（/登记 /复制）

### 2026-08-29 · M8 批5 特性继承+结算引擎完成（2 路并行 + 主 agent 指令解析防御修复）
- **路1** `core/trait_inherit.py`（~660 行）+ `alchemy_commands.py` 追加 /继承 /继承超 指令壳 + 36 测：TraitInherit——inherit_slots（位上限等级化正式1/精通2/专家3 见习0 + SP 特性位+1 叠加，总上限 6 INH-14/15）；select_traits（F-04 核心：候选清单须来自投料池 INH-01 → PP 逐项扣 普通1/超2 会话重置 INH-09 → 位余量「继承超 3 项」INH-06 → group 互斥同组最多1 INH-10 → repeatable false 不可重复 INH-11 → 负面特性宗师同源附带 INH-12 → 超特性需宗师+第 4 位独占 gold_slot_exclusive TSC-11~13）；apply_to_snapshot 写快照 traits（version+1 step=inherit INH-08）；check_placement_conflict 结算复核（供批6）
- **路2** `core/alchemy_settle.py`（594 行）+ 34 测：SettleEngine /确认 品质结算 9 步管线（async）——verify_snapshot 全量复核 GU-19 → 品质聚合 QualitySystem.aggregate_quality 均值 → 上限叠加 SP/核心/挑战 cap_quality ≤100 QLT-08 → 刻度未达标降级 degrade_quality 最低普通 QLT-10 → 档位+系数 effect_value QLT-04 → 触媒消耗 catalyst_consume CAT-04 → 产出入包 quality=tier+traits → 熟练经验=配方等级×1 → 终态幂等 settle_alchemy(command=settle:confirm 仿 settle_exit_idempotent，调用方不得嵌套 repo.tx)；abandon /放弃（材料不结算零背包改动+终态落键）
- **⚠️ 主 agent 指令解析防御修复（路1 自报两测失败根因）**：parse_command 白名单未含「继承超」独立指令（批11A 装配项 IF-34）→ `/继承超 X` 被解析成 command=继承 + args=["超","X"] → cmd_inherit_super 剥离误并入的「超」固定子词（防御，装配前后均可用）——**批11A 待办：白名单/command_specs 必须补「继承超」**
- **验收**：批5 单测 70 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff check . 全绿 + mypy 314 文件 Success
- **commit**：见 git log（feat: M8 批5 特性继承+结算引擎）
- **下一步**：批6 /确认 /放弃 /调合续 指令壳（消费 settle 引擎）+ 宝石货币 /分解（gem 平铺基础值拍板①）

### 2026-08-29 · M8 批4B 调合指令壳完成（单路独占 alchemy_commands.py，160→999 行 + 32 测）
- **/炼金 cmd_alchemy**（async）：守卫链 GU-05~08（炼金职业见习+ → 能量≥1 仅 energy_enabled=true 关则直通 → 单玩家无活跃会话全局互斥 → 触媒需专家 catalyst_unlock_tier 可配）；三路径：主路径开会话（new_snapshot → session_mgr.acquire(alchemy) → **acquire 成功后**扣能量 1 格[防孤儿会话，§7.1 行1 顺序] → assemble_panel 渲染 M-02 面板）/ 自动子词（AutoFeed.balance 配平 → apply_feed → 面板）/ 批量 ×N（不开会话：max_qty 超限提示不拦 → 材料×N+能量N+金币全量原子校验差异全拒 → batch_quality 平均品质产出 → 熟练经验=配方等级×N）；SessionConflictError 鸭子捕获→已有活跃模板（不误删已有会话）；能量竞态→release 刚建会话防孤儿
- **/投料 cmd_feed**（async）：守卫 GU-10 战斗拦截（先于会话查询「战斗中使用 /即时调合」）→ GU-09 无会话模板「当前没有调合会话，先 /炼金 <配方> 开始」→ apply_feed（槽位/持有校验，追加子词）→ session_mgr.suspend 写库 version 递增 → M-03 反馈渲染（⚗️ 火+7 | 连锁 2 段 | 可继承特性清单/刻度达标）
- **register_alchemy_commands**：追加注册 炼金/投料 两个 CommandSpec，handler 支持 k.get("ctx") 注入（async await 执行，runner _invoke_handler 兼容）
- **主 agent 收口修复**：ruff 5 处 E501（含子 agent 截断遗留）+ mypy 5 处（alchemy_commands L449 elems None 窄化 + test CommandSpec Optional 断言）已清
- **消费点**：批5 /继承（特性池+PP via AlchemyCore）、批6A /确认（品质结算 verify_snapshot+QualitySystem+settle_alchemy 幂等）
- **验收**：单测 32 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff check . 全绿 + mypy 309 文件 Success
- **commit**：见 git log（feat: M8 批4B 调合指令壳）
- **下一步**：批5 特性继承（/继承 /继承超：可继承池/PP/特性位/互斥/负面特性）+ 触媒机制

### 2026-08-29 · M8 批4A 炼金核心·引擎层完成（2 路并行；批4 拆批方案：4A 引擎层 + 4B 指令壳层）
- **路1** `core/alchemy_core.py`（1041 行）+ 51 测：AlchemyCore 调合会话核心引擎——会话快照（配方ID/材料链/连锁/特性池/触媒/PP/步骤/version §7.1 形态）；投料计算纯函数：compute_chain（连锁段数=相邻同属性对数 n-1，count 展开链位，chain_map 超界钳制）/compute_element_scores（8 元素累计+全物入料折算）/check_element_req（达标/缺额多档）；build_feature_pool（素材/金色素材/成品 source 分类特性候选池，专家全物入料 job_tier>=3）；PP 预算（super=2/normal=1 会话重置）；apply_feed（追加/覆盖链→重算→槽位上限 FEED-04「投料超槽位」+材料持有 FEED-05 全拒差异）；catalyst_resolve（type=触媒 校验/注册制提示/方向修饰改属性）；verify_snapshot（FEED-10 确认全量复核）；assemble_panel（面板全结构+刻度悬念分级：第 2 层引导语 vs 大师精确阈值 火 42/45，精通显现 QLT-13）
- **路2** `core/energy_bar.py`（487 行）+ `core/alchemy_auto.py`（421 行）+ 41 测：EnergyBar——懒计算补格（ENG-05 (now-last)/interval 上限封顶 查询即补格 锚点 now）/上限随等级 7 档（见习5→王20 可配）/consume 每炼金 1 格批量按次/安全区 2 倍速 energy_regen_sec_safe=900/默认关直通不干预/存档 energy_current+energy_last_regen_ts 同步；AutoFeed——balance 自动配平（AUTO-02 优先 element_req 达标组合贪心→其次配方基础材料→失败全拒+差异「缺 火药×1」原子不部分入料）/batch_quality 平均品质（复用 QualitySystem 均值口径）/check_quantity 数量上限超限提示不拦
- **工程补白（引擎侧 ~12 项，均显式标注）**：连锁 count 展开链位/chain_map 超界钳制/安全区判定口径/懒计算首锚点缺省/配平贪心启发式/持有封顶/全物入料折算等
- **消费点**：批4B 指令壳（/炼金 开会话→AlchemyCore.new_snapshot+EnergyBar.consume；/投料→apply_feed；/炼金 自动→AutoFeed.balance；批量→batch_quality+BATCH 原子；面板→assemble_panel+bar.current_of）
- **验收**：单测 92 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff check . 全绿 + mypy 308 文件 Success；纯新文件零共享改动
- **commit**：见 git log（feat: M8 批4A 炼金核心引擎层）
- **下一步**：批4B 调合指令壳（/炼金 /投料 /自动 /批量 + 面板渲染，集中在 commands/alchemy_commands.py）

### 2026-08-29 · M8 批3 会话基础完成（2 路并行 + 主 agent 接口归属勘误修复）
- **路3A** `world/session.py` 实装（40→320 行，5 占位方法全 async）+ `SessionView` 冻结 dataclass + `settle_alchemy` 幂等结算封装 + 18 测：acquire（已有活跃→SessionConflictError 全局互斥）/release/get_active/suspend（version 递增）/restore/recycle（recycle_scan 透传无 settle 默认不删）；settle_alchemy 仿 settle_exit_idempotent（idem_claim 快速路径→单事务 delete+write_idem_key，command="settle:{kind}"）；构造器注入 repository（bootstrap.py L61 注入 deps["repo"]）；None repo 时读防御/写抛 RuntimeError
- **路3B** `core/alchemy_session.py`（465 行）+ 49 测：调合会话状态机纯逻辑——§7.1 全部 12 行迁移判定（Event 11 枚举/Action 8/状态 4）+ 非法转移模板（无会话/已有活跃/调合进行中/已有一个调合会话进行中/已结算，中文键+TEMPLATE_MESSAGES 人话）+ version 幂等 terminate_idempotent + 战斗即时调合豁免 instant_ok（in_battle=True 不受互斥）
- **现有调用点 async 适配**（最小化+兼容）：context._battle_session / runner._battle_active / _routing_context 同步→async + await，用 inspect.isawaitable 兼容旧同步 fake（不破坏旧测试）；bootstrap 注入 repo
- **⚠️ 主 agent 接口归属勘误（路3A 真库冒烟发现，已修复）**：IF-06/07 将 upsert_session/delete_session 标在 Repository 名下，实际属 **RepoTransaction**（repository.py L887/L904）——acquire/release/suspend 原直调 repo.upsert_session 会真库 AttributeError；修复=写操作全部经 repo.tx() 事务句柄（settle_alchemy 本就正确走 tx）；单测断言 tx_open_count 同步修正
- **验收**：批3 单测 67 例全绿 + 全量 pytest 回归全绿（1 skip，含 context/runner 适配无回归）+ ruff check . 全绿 + mypy 302 文件 Success
- **commit**：见 git log（feat: M8 批3 会话基础）
- **下一步**：批4 炼金核心（/炼金 开会话 + /投料 链式 + 一键投料批量，3 路并行）

### 2026-08-29 · M8 批2 合成层完成（2 路并行）
- **路2A** `core/synthesis.py`（653 行）+ `commands/alchemy_commands.py`（/合成 指令壳骨架 160 行）+ 38 测（引擎 21 + 指令壳 17）：SynthesisEngine——守卫链 GU-01~03（mode≠off → 配方存在 → 任一制造/资源职业达标[遍历 proficiency 多职业取等级最高者 EXP-03] → synth_allowed[CASC-04 缺省=not master_only，深度配方默认拒「深度未解锁」，true 提示「此深度配方可被合成，将绕过深度炼金玩法」不阻断]）；原子校验 GU-04（材料×n+金币/宝石×n 全量差额，不足全拒+差异提示「缺 水结晶×5 + 金币 30」）；快照-回滚单事务（对齐 shop._snapshot 原子防双扣）；标准版产出 LAY-04a（品质固定「标准」/traits 空/awaken 无/标准珠固定 base_effects）；熟练经验=配方等级×成品数×synth_exp（source='craft'）；合成图鉴点亮（on_codex 注入回调，异常吞掉）；max_qty 缺省 2147483647 超限提示不拦按上限截断（拍板⑤）；不耗能量 LAY-05。指令壳 cmd_synthesis（委托引擎+透传 message+TPL-12+紧凑 `+` 收敛）+ register_alchemy_commands（批11A 装配 make_context 注入点）
- **路2B** `core/upgrade.py`（937 行）+ 32 测：UpgradeEngine——resolve_upgrade_recipe（kind=upgrade 条目→配置，子类型结构推断 U-S1）；execute 按 subtype 分发 4 实例：珠三合一升阶（3×同档同ID+宝石10→档位+1 禁跳级联动 QualitySystem.tier_index+1、无职业硬门槛拍板③）、成品合成（两成品+材料+宝石10 原子）、配方合成（组合表归一 U-C1 命中→永久解锁写 ctx[upgrade_unlocks]、已解锁 already 幂等不重复扣 ATO-05）、特性合成（两同系+宝石20+材料→更高位，同系判定 U-T2：显式系名/同 group/family 字段三级；产出落位复核 group 互斥+repeatable 排除被消耗输入）；_commit/_rollback 原子提交 U-A1（扣货币→扣输入→产出/解锁，失败回滚零副作用）
- **工程补白（引擎侧 ~14 项，均显式标注）**：多职业取等级最高者、synth_allowed 缺省=not master_only、max_qty 超限截断、快照-回滚 ctx 子结构、bound 默认 False、图鉴回调异常吞、子类型结构推断、同系判定三级语义、解锁表落 ctx[upgrade_unlocks] 等
- **消费点**：路2A 指令壳批11A 装配接线（make_context 注入 + 白名单「合成」已在 parsers L112）；路2B 指令壳归批7B（珠升阶）/批7C（成品/配方/特性合成）
- **验收**：单测 70 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff check . 全绿 + mypy 299 文件 Success；纯新文件零共享改动
- **commit**：见 git log（feat: M8 批2 合成层）
- **下一步**：批3 会话基础（SessionManager 实装 + 调合会话状态机，2 路）

### 2026-08-29 · M8 批1 熟练度+品质引擎完成（2 路并行）
- **路1A** `core/proficiency.py`（520 行，+29 测）：ProficiencyEngine——三来源熟练经验 ×倍率入账 → 升级判定（可连跳）→ SP 发放判定链（EXP-06 先入账后升级再发 SP）；job_tier_map 配方区间准入（settings.alchemy 主落点，条目可覆盖）；SP 面板自选解锁（sp_earned/sp_used 双计 + unlocks 计数 + repeatable/max_repeat 校验，拒绝 reason 枚举 panel_not_found/sp_insufficient/not_repeatable/max_repeat_reached）；王称号（codex_all_lit flag 授予，king 条目 id=职业ID，按职业独立不唯一）；称号佩戴 1 槽替换式（equip_title，title_not_owned 拒绝）；存档校验 validate_load（sp_used≤sp_earned）
- **路1B** `core/quality.py`（358 行，+28 测）：QualitySystem——quality_tiers 区间判定（39/40、59/60、79/80 边界）/档位系数 0.8/1.0/1.2/1.5/成品品质分均值 round-half-up 四舍五入（空投料 0 兜底）/批量平均品质档/上限三处叠加 ≤100（extra_cap 由调用方注入，与 1A 完全解耦）/未达标降级（差 N 档降 N 档、最低档封底、降后分数裁剪落档稳定）/防负档 index_to_tier 越界裁剪/区间值兼容 [lo,hi] 与 {min,max}（P-1）
- **工程补白（引擎侧 7 项，均显式标注）**：倍率结果向下取整 int 存档口径 / recipe_level_eligible 签名带 player（读当前档位）/ 降级分数=裁剪到新档区间 / 均值 round-half-up 非银行家舍入 / cap 可达上限=min(配方原上限+加成,100) / 越域裁剪落档防御 / sp_panel 缺省 desc="" —— 消费点已标注：批2 合成经验（gain_prof_exp craft）/ 批4 炼金核心 / 批6A 品质结算（effect_value/cap_quality）/ 批7B 珠档位（tier 快照）/ 批8 技能面板·图鉴（unlock_item/grant_king_title）
- **验收**：单测 57 例全绿 + 全量 pytest 回归全绿（1 skip）+ ruff check . 全绿 + mypy 293 文件 Success；纯新文件零共享改动
- **commit**：见 git log（feat: M8 批1 熟练度+品质引擎）
- **下一步**：批2 合成引擎（原子校验+品质·合成产物，2 路并行）

### 2026-08-29 · M8 批0 数据层完成（3 路并行 + 主 agent 收口接线）
- **3 路产出**：路0A `alchemy_models.py`（RecipeDef/TraitDef/ProficiencyDef + validate_recipes/validate_traits/validate_proficiency，REC/TRT/PRF 35 条，1331 行）+ 48 测；路0B `alchemy_settings.py`（check_settings_alchemy ALC-01~24 + validate_slots，769 行）+ 41 测；路0C test_demo/legal 内容包补 recipe/traits/proficiency/slots + 装饰珠/触媒/炼金材料/成品 + settings.alchemy 段 + gem 货币登记 + `check_m8_fixtures.py` 自检脚本（两包零失败）
- **主 agent 收口接线**：field_meta.py（recipe/proficiency ModuleMeta + traits_fields 扩 rarity/group/repeatable/source 4 键 + items 扩展字段 + SETTINGS_FIELDS alchemy 段 + slots 模块 + NAMESPACES recipe/proficiency_lib）+ loader._KIND_FOR_MODULE 3 条（recipe/proficiency/slots）+ validator._check_module 4 处派发 + settings 分支挂 check_settings_alchemy
- **字段定义迁 field_meta（G0 TC-03 循环修复）**：0B 路字段定义/常量全部迁至 field_meta 统一持有（schema 之家），alchemy_settings 改单向 import 并 re-export——函数级惰性 import 在静态 import 图亦成环，弃用
- **收口裁决 6 项**：① evolve_to.source 枚举=炼金产出/合成产出（中文，细化_2c4b L59 语义；0A 原英文 alchemy/synthesis 弃）② craft/combine 允许 output 声明产物（契约 §1.1 仅「upgrade 必填 output」未禁 craft；0A 原「craft 不得写 output」过严）③ items.rarity 3 档=普通/稀有/金色（契约 §4.1 中文；0B 原英文 normal/rare/gold 弃）④ slots equip_id 引用 items∪equipment（泛型 ref 单一 kind 改 str + validate_slots 跨表查）⑤ REC-14 只做类型校验（synth_allowed=false 是深度配方正确默认，对默认值发警告=噪音；「改 true 提示绕过」运行时语义归批2A）⑥ items.traits 只做 str 结构校验（M2 旧语义防泛型 R-4 误拦存量，深引用归批7 运行时）
- **收口修复**：settings.alchemy gem 费率原为嵌套 `gem` 对象 → 改契约点号键 `gem.分解/复制/成品合成/配方合成/特性合成/珠升阶/复制额外/decompose_formula`（否则 ALC-13 静默落默认值）；test_demo+legal 两包同步；check_m8_fixtures 自检同步点号键断言；test_content L50 / test_pck11 物品集断言改超集方向（legal 增补 M8 炼金物品）
- **验收**：全量 pytest 全绿（1 skip）+ G0 ARCH-OK + ruff `check .` 全绿 + mypy 289 文件 Success + test_demo/legal build_pack 零红拦（新增 test_alchemy_data_layer.py 承载）+ check_m8_fixtures 两包零失败
- **commit**：见 git log（feat: M8 批0 数据层）
- **下一步**：批1 熟练度引擎+品质系统（m8_batch_plan §二，2 路并行）

### 2026-08-29 · M8 设计审查（8 批全过）+ 5 项拍板
- **审查结果**：P0=0 / P1=26 / P2=41（8 批严格串行，报告归档 docs/审查/M8设计审查_20260829/）；无编造行号/冒充裁决；P1 集中跨文档口径不一致 + 定稿内部冲突
- **用户 5 项拍板（2026-08-29）**：
  1. 分解宝石产出 = A 平铺基础值（普通1/精良3/史诗8/传说20，不再乘回收率）+ **做成可配置**（后续按情况调整）
  2. 品质档位键名 = A 统一到定稿键集 common/uncommon/rare/legendary + **档位数可配**（后续加新品质）；定稿 L150"优秀/稀有"旧名废弃
  3. 珠升阶 = A 无职业硬门槛（准入靠槽级控制）
  4. 复制费 = A 只算配方 cost.coins（向下取整）+ **可配置额外消耗**（settings 增可配项）
  5. 数量上限 = 默认拉高到 **2147483647**（int32 max，做成可配置）+ 超限提示不拦（对齐分隔符 L73）
- **落地**：6 份细化（2c4a~2c4f）按拍板统一修订（批1/批2 共 6 路子 agent，抽查验证通过）+ 记录.md + commit 023ab43
- **登记仲裁建议**：品质档键集分裂（R-01 先例）——拍板② 已定统一键集，登记 R-09

### 2026-08-29 · M8 共享契约完成（阶段 3）
- **接口摸底**：docs/m8_接口摸底.md（51 接口落点 + 6 缺口定案：SessionManager M8 实装/battle_alchemy_used 挂快照顶层/recipe·proficiency 四件套/proficiency dict 形态/gem 统一 reward 管线/settings.alchemy 段+专项校验）
- **批次派工**：docs/m8_batch_plan.md（13 实现批 + 1 审查批 ≈ 34 路；/协力 补排期批11A；批9B 依赖批3B/4 标注）
- **共享契约**：docs/m8_shared_contract.md（1758 行 = 4 子文档合并：核心机制 455/指令契约 477/数据与校验 477/战斗资源 323）
- **契约审查 5 路**（A/B1/B2/C/D，P0=1/P1=6/P2=24）：P0-1 IF02 resolve_name 签名编造已修（改真实签名 + resolve_by_name 补白）+ P1 全修（check_pack 锚点 L1793/energy 落点统一/迁移表补 2 行/僵尸回收材料返还/ATO-08 L511/recipe inputs·output 双 schema/GEM-08 误引 settle_currency_drops//协力 排期/2c5b·2c5c 追溯）+ P2 全修；细化_2c4d ATO-08 L509→L511 同步
- **commit**：adcbc5b（19 文件 +4869 行）
- **下一步**：阶段 4 实现（按 m8_batch_plan 批0 数据层开工：recipe/traits/proficiency 模型+校验器四件套 / items·slots·settings 扩展+_check_settings_alchemy / fixtures）

### 2026-08-29 · 机器人 3124097014 重连 + test_demo 实机测试
- **机器人离线**：NapCat 双 qq 进程冲突（636478 旧残留 + 668260 新扫码）已清理干净 + systemd 重启；QQ 需重新扫码（当时用户说晚点弄）→ 扫码后验证登录 + 消息链路
- **test_demo 内容包已生成并部署**（content/test_demo/，17 json：7 图/双 boss 机制/10 物品/任务链 10 任务/6 NPC/3 店/7 天签到）；设计文档在 /root/deliverables/test_demo_world_design.md + test_demo_quest_npc_design.md
- **部署实机待测**：QQ 机器人上线后照任务链实测（注册→签到→交付 Q1→采集→…→双 boss）
- **部署说明文档**（dep-fix 遗留）
- **注册门槛跨指令组不一致**（批5 发现）：basic/register/status/explore/investigate/shortcut/use/unregister 有门槛；battle/dialog/codex/log/shop/checkin/quest 无 → 待修统一
- **部署实测修复已提交**：ctx quest 表注入 + quest_active dict 落档 + 内容包五档断言（commit bc9297f）

## 〇、通用检查设定（长期保留 · 复查检测用）

> 项目检查工具集**不随里程碑清理**，任何会话复查项目状态都可用：
> - **一键快速复查**：`python scripts/check_all.py`（ruff/mypy + 架构 + 内容包 + 单测，十几秒）
> - **全量验收**：`python scripts/check_all.py --full`（M0~M7 门禁 + 覆盖率 + 全量 pytest，~5 分钟）
> - 单独项：`--lint` / `--arch` / `--content` / `--unit` / `--verify N`
> - 检查报告归档 `docs/verify/check_report.md`；工具清单与用法见 `docs/检查工具指南.md`

## 〇、需求池（用户提出 · 待实现）

### 2026-08-28 · 消息模板编辑功能（用户：到时候做一个，我根据自己的需要去调整）
- **需求**：策划/GM 运行时编辑消息模板（文案/格式按自己需要调整，不改代码）
- **设计方向**（详见 方案存档/消息模板编辑功能需求_v1.md）：
  1. 模板数据源化——当前 TPL_XXX 常量分散在指令壳/core → 收敛为可配置数据源（templates.json / GM 表）+ 默认值兜底
  2. 编辑入口——GM 指令（/编辑 模板）+ 内嵌 Web 编辑器（M12 规划）
  3. 运行时热更新——复用 M6 热重载机制（3e2），编辑即生效
  4. 校验——编辑后校验（占位符白名单 / emoji 纪律 / 长度上限）
- **归入**：M12 编辑器/GM 或独立需求（届时拍板）

## 一、已完成（最近优先）

### 2026-08-28 · 通用检查设定 + M7 全部推送 GitHub
- **三大部分全部落地**：①装配层接线（A-01~05：make_context 工厂/context.py + Router 全注册/router_setup.py + processing 驱动+发送出口/runner.py + NoneBot 桥接/qbot_rpg_bridge + world 装配/bootstrap.py）；②NPC 对话接线（N-01~04：/对话 指令壳 dialog_commands.py 33 用例 + 世界层 NPC 挂载 get_npcs + 事件写入 event_bus.py 三表 + 会话持久化 30 天清理）；③3f 单机向体验（F-01~18：/日志 冒险日志+传记+GM 分支、/调查 彩蛋/蹲点/隐藏地图、隐藏要素 hidden_trigger 三模式+保底/日限、图鉴 codex 分册+里程碑五档+100% 三件套、环境 lore/事件+check_m7_content 校验器）
- **BCH-01~09 九批收官**（22d4ec3/ccf983f/de717ed/f897c6d/49d072a/981f6aa/a79498f/6d8d46d/a97d436），全量 pytest 2718 passed + ruff/mypy 全仓绿 + G0 架构 ARCH-OK
- **verify_m7 三段式门禁全绿**（VM7-01~05，6/6 EXIT=0）：装配冒烟 + NPC 端到端 + 3f 21 TC 承载 + 全量回归（run_all_tests EXIT=0 + 覆盖率三目录 ≥80%）+ 门禁诚实化；归档 docs/verify/m7/verify_m7_20260828.md + m7_checklist.md
- **收口修复**：全仓 ruff/mypy 新增问题清理（scripts/tests 52 处 type ignore + unused-ignore 清理）、verify_m7 端到端断言修正（未注册玩家 ctx 无 persistent_state 键）
- **内容包既有问题交接**：demo_full 隐藏地图 lava_tunnel 引用未注册条件键 subquest_done → 永不可达（check_m7_content 抓出，待内容包修复）

### 2026-08-27 · M6 实现层批1 完成（三引擎 + 基础指令，commit 720d7ee）
- **路A 三引擎实装**（35 规则全落地，空壳消除）：core/levelup.py（LVL-01~12，经验入账/跨级/回满/SP 联动/自由加点）、inventory.py（INV-01~11，堆叠合并/拆行/绑定拒移/数量截断/格数上限/到期惰性移除）、equipment.py（EQP-01~12，穿戴校验链/互斥/后装覆盖/卸装回包/聚合重算/互斥环加载拦截/战斗内禁穿脱）——操作对象=玩家状态 dict（对齐 checkin/quest 既有引擎模式），配置构造器注入+默认值兜底（exp_curve=100×lv/level_cap=45/sp_per_level=1）
- **路B 基础指令**：register_commands.py（/注册 REG-01~06，名字校验/缺省职业/重名/幂等/建号数据生成）+ status_commands.py（/状态 STT-01~05 面板五区）+ shortcut_commands.py（/快捷解绑 /快捷列表 SHC-01~05）+ parsers 白名单补「注册」+ basic_commands /装备 换真实引擎适配层 EquipmentEngineAdapter（EQP-12）+ cmd_help 别名显示替换（SHC-04/TC-17）
- **主 agent 收口修复**：status_commands 3 bug（_player_fields 忽略 dict 形态 ctx["player"] / _final_attrs bonus 恒非空吞 resolver 兜底 / prefix_line 空称号格式 - -）；补 test_shortcut_commands.py + test_equip_adapter.py（TC-SHC/TC-EQP 适配层）
- **verify_m5 翻转 11 条 DELAYED**（4f TC-01/02/03/04/06/07/09/10/17/22/23 → pytest 承载；剩 2 条=3d TC-16/17 锻造）——踩坑：execute_code 批量替换 read_file(limit=500) 截断 verify_m5 670→500 行（丢 main 块），git checkout 恢复后重新安全替换
- **验收**：全量 pytest 全绿（1 skip）+ verify_m5 全绿（79 承载+2 DELAYED）+ G0 架构门禁绿
- **下一步**：批2 D2 幂等事务三件套（message_id 幂等键/购买入 repo.tx()/per-player 串行队列）

### 2026-08-27 · M6 批1 dsh 审查 + 修复（commit 86f829e）
- **批1A 三引擎审查**（审查_M6实现_批1A_三引擎_jspace.md）：P0=0 / P1=2 / P2=9，35 规则+18 TC 全落地确认
- **批1B 三指令审查**（审查_M6实现_批1B_三指令_jspace.md）：P0=0 / P1=3 / P2=11，REG/STT/SHC 全部落地确认
- **P1 修复（3）**：①equipment aggregate_bonus 同 item_id 多件双计 → 穿戴行引用机制（_worn_refs/_resolve_worn_row，equip 登记/unequip 移除，精确聚合）②穿装序号按存储序 vs /背包 时间倒序错位 → 适配层 _sorted_inventory 按 acquired_at 倒序 ③/注册 固定子词吞名（/注册 自动 战士 → 注册名变战士）→ fixed_subword 非空一律 TPL-12
- **P2 修复（9）**：印记区 imprints_line（RUL-13/STT-01⑤）/ /帮助 引导版按 4f B6 改注册·状态·背包+豁免前置 / shortcut_max=0 显示不限 / /快捷列表 固定子词 TPL-12 / 命令壳裸吞异常加日志 / _slot_name 支持 slots.json 包装形态 / target 字段缺省整行降级 / exp 归一 / add_item new_rows 键 / D1 TC-LVL-04+TC-INV-06 数值勘误（P2-09）
- **登记项**（潜伏/依赖后续）：P1-2 强化字段丢失（M9 前置数据模型解决）、P1-3 适配层 dict 行接缝（装配层批次7 归一）、P2-01/02/03/04/05/06/07/10/11
- **回归**：全量 pytest 全绿 + verify_m5 全绿（79 承载+2 DELAYED）

### 2026-08-27 · M6 实现层批2 完成（幂等事务三件套，commit 13eb2f8）
- **processing.py**（D2 IDEM+POOL 件套，281 行）：指令处理入口 process_message（idem_claim 只查不插 → 未命中入队 → 队列内单事务 tx.idem_exists 权威判定 + 业务写 + write_idem_key 同事务 → COMMIT → 发送；命中幂等返回不入队；失败整单回滚无孤儿键；私聊 group_id "dm" 哨兵）+ PerPlayerQueue（per-player asyncio.Queue + 单消费者，同玩家 FIFO 按序、异玩家并行、队列空消费者自行退出、任务异常捕获不阻塞后续、close 取消）
- **shop_tx.py**（D2 SEG 件套，367 行）：buy_in_tx/buy_in_open_tx 购买结算整体入 repo.tx()（事务内读玩家行/库存 → 6 步校验链 → 扣减 → tx.upsert_player 写回 + 库存 CAS + ok 才 write_idem_key → COMMIT，失败整单回滚）；world_stock 扁平键 {shop:item: n} 双向转换
- **battle_boundary.py settle_exit_idempotent 实装**（IDEM-8 / 批5A P1-1 收口）：幂等键 (player_qid, "settle:{kind}" 入 command, message_id) + group_id 哨兵兜底；idem_claim 查重 → 单事务 delete_session（连段/战斗资源随会话删除清零）+ write_idem_key → 已结算 False
- **测试**：test_idem_processing（TC-IDEM-01~06 + 补充）+ test_per_player_queue（TC-POOL-01~04 + 排队窗口）+ test_shop_double_pay（TC-SEG-01~05 + 边界 4 例 + 幂等重发）= 26 例全绿
- **主 agent 收口**：修 test_per_player_queue 的 flakiness（「for _ in range(2000): sleep(0)」限次忙等在全量负载下偶发失败 → 改 _wait_until 带超时持续等待，5 处）；多次全量回归全绿
- **验收**：全量 pytest 全绿 + verify_m5 全绿 + G0 架构门禁绿
- **下一步**：批2 dsh 审查 → 批3 D3 热重载接线（P0-1 续战世代绑定修复）

### 2026-08-27 · M6 批2 dsh 审查 + 修复（审查_M6实现_批2_幂等三件套_jspace.md）
- **审查结果**：P0=0 / P1=2 / P2=12；三件套结构性要求全对齐（幂等载体=入口/只查不插/同事务写键/事务内读/双扣两路径）
- **P1-1 修复**：shop_tx `_ctx_inventory_to_player` 同 item_id 多实例回写计数膨胀（A2+B3 合并 5 再留 B3 = 8）→ 合并总量归并首实例、其余移除（总量守恒）；补回归测试
- **P2 修复（5）**：P2-2 幂等重放 ok 语义统一（shop_tx 命中 ok=False+idempotent=True，对齐 processing）+ P2-5 消费者异常/取消 future 兜底 resolve（防 await fut 永久挂起）+ P2-7 测试 sleep→事件同步 + P2-12 补纯货币路径并发用例（无限购商品+余额仅够一次 → 货币恰扣一份）
- **登记批次6/7 必办接缝（9）**：P1-2 线上 /购买 未接事务（cmd_buy 闭包替换 buy_in_open_tx 或走 process_message）+ P2-1 settle 嵌套张力（/逃跑 接线决策二选一）+ P2-3 cleanup_idem_keys 无生产调度 + P2-4 队列无超时（压测补 wait_for）+ P2-6 handler ok=False 须零写 + P2-8 close 未消费队列 + P2-9 dm 哨兵收敛一处 + P2-10 world_sold_out 持久化 + P2-11 world_stock CAS 版本交互
- **回归**：批2 测试 28 例全绿 + 全量回归全绿

### 2026-08-27 · M6 实现层批3 完成（热重载接线 + 续战世代绑定，commit 3abbac2 + a957873）
- **路A WIR 件套**（热重载四项接线）：GmBackend.reload_content（WIR-07 = watcher.reload 同一管线，{ok,summary,failures}）+ 权限分级（WIR-06 机主全部/GM 重载备份恢复/无权限静默）+ ReloadResult 人话翻译（reload_result.py TPL-15~18 四模板逐字 + 禁词扫描）+ ValidationReport 红黄计数（count_errors/group_by_module）+ backup_snapshot 激活（RSM-05）+ resolve_or_degrade 统一降级入口（WIR-14）+ schedule_polling 调度包装（WIR-04，零 apscheduler）
- **路B RSM 件套**（P0-1 续战世代绑定）：battle 快照增 registry_generation（RSM-02/F-RSM-01）+ from_snapshot 增 registry 参数透传（RSM-03）+ 续战入口世代重绑定（snapshot_resume pick_registry_snapshot/rebind_registry_for_snapshot，watcher N=2+backup 双口取档，fallback 降级 + RSM-09 自检告警）+ 存储 payload 内嵌世代往返（RSM-06，无新列）
- **主 agent 收口**（两路迭代上限截断）：补 4 个测试文件（battle 世代/重绑定/翻译/wiring = 45 例）+ 修 M43 threading 违规（gm_commands 线程适配 → 返回 None 由上层转提示）+ 修 reload_content 返回值契约 bug（dict vs ReloadResult）+ no_change 视为成功 + 文档回填（router WIR-18 / m5 WIR-20 / 细化_3d TPL-15~18）
- **验收**：全量 pytest 全绿 + verify_m5 全绿 + G0 架构门禁绿 + M43 全绿
- **下一步**：批3A/3B dsh 审查 → 批4 D4 内容包冒烟

### 2026-08-27 · M6 批3A dsh 审查 + 修复（审查_M6实现_批3A_wir_jspace.md）
- **审查结果**：P0=0 / P1=2 / P2=8；四项接线组件级齐全/权限矩阵一致/四模板逐字/红黄计数出口落地
- **P1 修复（2）**：F2 TPL-17 连续失败 N 恒 1 → reload_content 透传 consecutive_failures；F1 同步桥接 asyncio.run 与 watcher asyncio.Lock 跨循环绑定冲突（3.9 必失败、3.11 恰能跑）→ except RuntimeError 收窄只吞事件循环冲突、其余 re-raise；批次7 装配改 await 为正解
- **P2 修复（4）**：F3 手动 /重载 无变更「0 个模块变更生效」误导 → _commit_success 空变更置 no_change=True 走 TPL-18；F4 红黄计数日志三处格式不统一 → 统一收敛 _log_report_counts；F5 补 caplog 红黄计数断言测试（WIR-12/F4 验收③）；F7 resolve_or_degrade Mapping 分支畸形防护（(None,True) 不抛）
- **登记批次6/7（4）**：F6 WIR-13 对照公式补 FIXED_REGISTER_ORDER+manifest 两段 + verify_m6.py 建（D8 执行）+ F8 run() while+sleep 标注 deprecated（仅用 schedule_polling/poll_once）+ F9 四项接线①②壳层注册（启动调 start/apscheduler 注册）+ F10 TPL-18 自动提示发送出口（+限频不占玩法配额）
- **回归**：批3A 相关测试 90 例全绿 + 全量回归全绿

### 2026-08-27 · M6 批3B dsh 审查 + 修复（审查_M6实现_批3B_rsm_jspace.md）
- **审查结果**：P0=0 / P1=2 / P2=6；RSM 三件套主体落地无缺失、半套禁绝四路解析绑定完整（effects/marks/combo/defenses 均绑 rebind registry）
- **P1 修复（2）**：RSM-01 世代重绑定端到端验收无测试（resume_from_snapshot watcher 主链路/半套禁绝断言缺失）→ 补 4 例端到端测试（exact/none/fallback/skipped 四态 + registry 透传工厂）；RSM-02 none 降级路径（世代滚出 N=2 窗口=半套复发最高频）不告警 + degraded=False → 补告警 + degraded=True（与 fallback 可观测性对齐），skipped 分支保持不告警零影响
- **P2 修复（4）**：RSM-03 Registry.from_snapshot 畸形档 try/except 降级 + RSM-04 snapshot_registry_generation Mapping 防护 + RSM-05 battle start 世代类型防护回落 0 + RSM-07 backup_snapshot 异常补 debug 日志
- **登记批次6/7（2）**：RSM-06 resume 重建引擎不注入 enemy_ai → AI 侧半套登记（装配层按 rebind 档重建 ai_action_lib）+ RSM-08 测试质量小项（turn_boundary 名实相符补 interrupt 路径 / 事件循环模式）
- **回归**：重绑定测试 25 例全绿 + 全量回归全绿 + verify_m5 + G0 全绿

### 2026-08-27 · M6 实现层批4 完成 + 审查修复（内容包冒烟，commit f21de92 + ca44444）
- **路A 冒烟脚本**：scripts/e2e_m6_smoke.py（四步断言矩阵注册门槛→锁定→攻击→结算 + validator 四件套 + 确定性重放 33 断言 + 零 NoneBot）+ test_e2e_m6_smoke.py pytest 包装（子进程 exit0/确定性/ast 零 NoneBot）
- **路B 收尾**：verify_m0 missing_mod 空断言→软放行真实断言（SMK-13，Y-6 statuses + 未声明 npc 不加载 + potion 挂载）+ test_pack_fixtures_matrix（四件套矩阵 TC-SMK-08~12）+ content/ 五档包（demo_blank/lv15/lv30/lv45/full，PCK-01~12）+ docs/verify/m6_smoke.md 归档模板（F-01~05）+ run_all_tests m6 接入声明（置位归 D8）
- **主 agent 收口**（路A 迭代截断）：亲自实现冒烟脚本，修 5 个引擎调用问题（parse_command/回合时序/终局判定/命中 rng/溢出快照）
- **审查修复**（P0=1/P1=2/P2=10）：P0-1 攻击一轮一条死断言（BattlePipeline 缺 sender + 传 dict + except 全吞）→ 按 verify_m5 ④b 重写（start→player_act→dispatch_round，mock.send==1，独立新引擎防状态错乱）；P1-1 missing_mod Y-6 断言补消费；P1-2 五档包 demo_lv15 装配战斗（一处覆盖 SMK-10+PCK-10）；P2-4~7 结算 victory 标志/锁定数值 hp=120/注册后四指令统一/重放差异诊断
- **验收**：冒烟 33 断言全绿 + 全量 pytest 全绿 + verify_m0 + verify_m5 全绿
- **下一步**：批5 D5 故障注入（46 规则 / 26 TC）

### 2026-08-27 · M6 实现层批5 完成（故障注入，commit cacd112）
- **六故障脚本**（tests/fault/fault_inject_*.py，19 例全绿）：crash（发送出口崩溃→业务已提交+键已落→同键重发幂等→settle 二次结算 False）/ save（写失败人话+pending 落→磁盘恢复重放一致）/ reload（非法 JSON→回退旧 registry+TPL-16 人话→恢复可重载）/ formula（死循环/黑名单→兜底 0→条件不满足不崩溃）/ doublepay（并发双买货币只扣一次/限购只+1）/ netdrop（断线重连数据完好+version 不变）
- **storage .pending 暂存队列实装（RW-4/FLT-14/15）**：qbot_rpg/storage/pending.py（PendingQueue .pending.jsonl JSONL 追加写 + fsync + 坏行跳过 + clear）+ repository.save_player 写失败转写 + replay_pending 单事务重放清空 + connection._commit 注入接缝 + SAVE_FAILURE_MESSAGE 人话源（storage 层定位防 G0 反向 import）
- **F-1 核销**（contract_deviations.md L24 标注 M6 已实装）+ test_shop TC-23 补限购计数断言
- **主 agent 收口**（路A 迭代截断）：修 crash_env fixture 建玩家（外键+业务前提）+ crash_env.repo→crash_env 引用 + 幂等重发不等 sended（send=False 不调 sender）+ 补 save_env fixture（repo/db/data_dir/pending 四属性）
- **遇坑**：scnet 模型策略漂移（非 Event 后缀 403 Token Plan）→ delegation.model 切 Flash-0731-Event；路A tests/fault/conftest.py 遮蔽根 conftest 已删
- **验收**：fault 20 例全绿 + 全量回归全绿
- **下一步**：批5 dsh 审查 → 批6 D6 测试体系强化（40 规则 / 30 TC）

### 2026-08-28 · M6 实现层批6 完成 + 双轮审查修复（测试体系强化，commit 7e37942 + 3475b15 + 7ccc887）
- **SED 种子收敛**：tests/conftest.py 增 seed()/seeded_rng()（默认 20260826，派生 offset 工厂）/formula_params（D6 F-FIX-01~27 读取器）三 fixture；六处迁移 + 清单外残留全量清零（A 组 16 处 random_seed= → seed fixture 注入；B 组 8 处 Random(N) → DEFAULT_SEED 常量）；禁裸随机 grep 零命中
- **PRP 性质测试**：test_formula_property.py 4 用例（伤害恒≥1 / 非会心 [0.95,1.05] 闭区间 / 会心率≤95% + 档位倍率 [1.3,2.35] / 确定性回归）各 1000 组 + 端点精确断言
- **validator 标量透传**（FIX-2）：formula 模块顶层数值标量参数（monster_def_rate 等）放行 + bool 红拦收紧 + 边界正/负例测试（防透传吞并安全例外）
- **主 agent 收口**：validator 红拦修复（标量透传）+ 路B 暂驻 fixture 删除（同名冲突清理）
- **审查登记**：D6 §八 FIX-6 采纳 + SED 迁移闭环 + PRP-4 偏离（契约前提不成立 → 抽样域取断言带 + 端点真实带补偿）
- **验收**：PRP 4 用例全绿 + test_content 边界用例 + 全量 pytest 绿 + verify_m5 绿
- **下一步**：批7 D7 质量门禁（38 规则 / 30 TC）

### 2026-08-28 · M6 实现层批7 + 批8 完成（质量门禁 + verify 收官，commit 93f8758~fbfb56b）
- **批7 质量门禁**：coverage 三目录各自 ≥80% 真实核算（core 81.47%/engine 90.52%/content 86.57%）+ 阈值断言 exit≠0 + 报表归档；ruff/mypy 阶段0 快速门（--skip-lint）+ 1145 处存量基线豁免；CHANGELOG 建档（M0-M5 六条目回填 + M6 预留）+ GitHub Actions CI（单形态调 run_all_tests 全量）；双轮 dsh 审查修复（P1 5 项全修：TC-COV-04 假数据载体/基线对账 D8/报表口径/标注核对 等）
- **批8 verify 门禁与承接**：verify_m6.py 两段式门禁——段一 8 项验收单（热重载/冒烟/故障注入/覆盖率/ruff-mypy/validator/清单/CHANGELOG 归档）+ 段二 DELAYED 六组承接（DLY-01~10：显式残留登记 4 项[图鉴/锻造→M11/生活生产] + 转 pytest 承载 6 项 + 到期扫描升级 + verify_m4 批次7-01 翻转）；run_all_tests m6 置位 + 不假绿（缺失 return 1）+ 依赖序 m0→m6 + --fast 生效；四源「接线闭环」统一；M<N> 门禁输出统一；m6_checklist.md 验收单 + m6/ 归档报告 + CHANGELOG ACC-03 欠账回填
- **主 agent 收口修 5 处**：async def 正则 / COVERAGE AnnAssign 解析 / T_id 连字符统一 / in_scan 键 .py / 3d TC 连字符
- **验收**：verify_m6 全绿（12 断言 0 失败）+ run_all_tests 全量 EXIT=0（lint 门 + 四层 pytest + M0-M6 verify + 覆盖率）+ 全量 pytest 全绿

## M6 完整收官（8 批全链）
- **里程碑验收一句**：M6 接线闭环完成——热重载接线（WIR/RSM）+ 内容包冒烟 + 故障注入六类 + 幂等事务三件套 + 测试体系强化 + 质量门禁 + verify 两段式门禁全绿（【总纲】VM6-1 八项验收单逐项通过，m6_checklist.md + m6/verify_m6_20260828.md 归档）
- **commit 链**：批1 720d7ee/86f829e → 批2 13eb2f8/0137a77 → 批3 3abbac2/a957873 → 批4 f21de92/ca44444 → 批5 cacd112/7502a80/f579bef/67db5f9 → 批6 7e37942/3475b15/7ccc887 → 批7 93f8758/b3b4ba5/10729d7/d8d923a → 批8 fbfb56b
- **门禁矩阵**：全量 pytest（2359 passed）+ verify_m0~m6 全绿 + ruff/mypy 快速门 + coverage 三目录各自 ≥80% + run_all_tests 全量 EXIT=0
- **下一步**：M7 交互补全（开新对话负责）



### 2026-08-27 · M6 细化需求完成（总纲 + 8 份子细化文档，12 份审查报告 P 项全量落地）
- **产出 9 份 细化_M6_*.md**（docs/细化/）：①接线闭环总纲（38 规则/13 用例，M6 唯一范围+verify 两段式+8 份子文档注册+P 项映射表+ADR-01~13）②D1 三引擎与基础指令（51/29）③D2 幂等事务三件套（23/15）④D3 热重载接线含 P0-1 续战世代绑定（31/21）⑤D4 内容包冒烟（29/21）⑥D5 故障注入（46/26）⑦D6 测试体系强化（40/30）⑧D7 质量门禁（38/30）⑨D8 verify门禁与承接（34/21，聚合 172 TC）
- **12 份 dsh 审查报告 P 项全量归口**：P0×12 全承接、P1×60 归口 8 子文档、P2×62 逐批收纳；4 份子 agent 自报均经主 agent 独立复核（自检脚本 + 代码行号抽查命中）
- **四源仲裁落地**：M6=接线闭环（F5/F6/G1-G4）+verify_m6 两段式；5a/5b 归 M12、4c/4d/4e/6 归 M11/M13
- **跨档共享决策 ADR-01~13**：内容包冒烟≠服务器冒烟/旧 schema=容忍加载/五档包位置/覆盖率口径 core+engine+content 各自≥80%/幂等载体=指令入口/购买并发不依赖世界锁/伪托 D5 修正等
- **D4 曾一次子 agent 基础设施中断**（会话存储写入失败未落盘），重派成功
- **验收**：9 份全部过 refinement-doc-selfcheck.py（规则/用例/字段声明对账 OK）+ emoji 纪律（仅 ✅❌）
- **下一步**：M6 实现层（按总纲 §六 8 批：批1 三引擎+基础指令 → 批8 verify_m6 收口），每批 2 路并行

### 2026-08-27 · M5 实现层完成（6 批 12 任务全落地，六门禁全绿）
- **6 批 12 任务全部实现完成**：批0 M5-01 前缀接线+ M5-02 校验器 / 批1 M5-03 骨架+玩家行动 01-06 + M5-04 技能状态行 07-09 / 批2 M5-05 怪物行动 10-14 + M5-06 结算连段 15-22 / 批3 M5-07 开始结束明细 23-25 + M5-10 全仓 emoji 降级 / 批4 M5-08 battle_commands 一轮1条接线 + M5-09 背包筛选链+探索壳 / 批5 M5-11 emoji 登记表 + M5-12 verify_m5 门禁
- **新增实现**：qbot_rpg/commands/prefix_wiring.py（前缀消费接线 220 行）+ battle_commands.py（BattlePipeline 一轮1条 795 行）+ explore_commands.py（/进入 /休息 壳）+ basic_commands cmd_bag_filter（筛选链）+ data/emoji_sanitize.py（strip_icon_emoji）+ message_prefix 校验器 MP-1/MP-2 + verify_m5.py（658 行，81 TC=68 承载+13 DELAYED）+ 全局图标登记表.md
- **M5-08/11 曾两次 502 重派**（scnet 瞬时故障，确认未落盘后重派成功）
- **实现中排掉的坑**：G0 架构门禁（strip_icon_emoji engine→core 违规 → 移 data/ 层）；并行写 battle_render.py 冲突（patch-only 约定 + 主 agent 收口对齐）；weather 空模板断言/dialog 漏同步断言/页脚单页不输出断言
- **验收**：全量 run_all_tests 六门禁全绿（M0-M5 + G0 ARCH-OK，2138+ passed）
- **M5 实现完成，进入 dsh 审实现**（用户要求"实现后记得检查"）

### 2026-08-27 · M5 细化规划（shared_contract + batch_plan）+ 细化审查 3 批 + emoji 裁决
- **产出 docs/m5_shared_contract.md**（D1-D5 契约：铁律 11 条 / message_prefix 7 字段 / TPL+BREP 模板注册表 / 已实现-待实现缺口矩阵 / 81 TC 矩阵）+ **docs/m5_batch_plan.md**（4 批 9 任务 M5-01~09）
- **M5 实现缺口分析**：battle_render.py 是最大缺口（BREP-01~25 骨架未实装）；prefix_render/list_render/sender/panel_render/cmd_view M4 已实装勿重做；message_prefix show_on_system/per_channel 消费、筛选链、emoji 静态检查、verify_m5 待做
- **细化审查 3 批**（dsh，报告落盘 审查_M5细化_批次1-3_jspace.md）：批1 D1-D2（P0×0+P1×2+P2×7）/批2 D3-D4（P0×0+P1×1+P2×7）/批3 D5（P0×2+P1×4+P2×5）——共 19 项全处理
- **细化审查抓到的重要契约缺陷**：IF01 render_prefix 契约签名与实现不符（format/group_name/job 错）；ActionOutcome 字段编造（无 rating/damage/outcomes，实际 final_damage/raw_damage/target_hp）；BREP-02 丢 HP 后缀/BREP-09 丢 /最大（P0×2）；校验器规则误标 3h V9~V12（实为【前缀】§九）；emoji 白名单三方冲突
- **用户拍板「不用 emoji」**：除 ✅/❌ 功能性标记 + 排版符号（| → × / 「」【】）外，渲染输出不含任何 emoji——数据型功能图标（物品/NPC/商店/天气/印记 icon、GM 🚫、单机 ⛩️）一律降级纯文本；m4_shared_contract §2.2 豁免条款作废；规划_路3 L171 登记表作废；M5-08 含全仓渲染输出 emoji 降级 + 静态检查（命中=0）
- **契约修正全部落地**：m5_shared_contract/m5_batch_plan 按 3 批报告修（真实签名/真实字段/模板文本/军规5/16 行折叠锚点/TC 映射表/筛选分页断言/空列表/✅❌ 位置约束）+ 5e 行号 L171→L184（模板规范上下文）
- **M5 细化完成，可进实现层**（按 m5_batch_plan 4 批 9 任务派工）

### 2026-08-27 · M5 设计审查（3 批 dsh）+ 用户 3 项拍板 + 全量修复
- **M5 设计审查 3 批**（dsh，报告落盘 审查_M5设计_批次1-3_jspace.md）：
  - 批次1 细化_3d 消息模板规范（P0×1+P1×3+P2×7）：页码越界文档自相矛盾（正文报错 vs M4 裁决夹取）/§8.3 合并策略只承接 1/4/L25「汇总语义」错引 5 处/L46「费用语义」编造 4 处+签到引用链不实
  - 批次2 细化_5e 战斗战报格式（P1×1+P2×9）：【数值层】行号系统性 -2~-5 偏移约 40 处（R-09 后插导致，主 agent 独立验证属实）/「幻觉审查_5e」编造来源/TC-11 MP 区间自相矛盾/先手行降级跨文档冲突
  - 批次3 3h settings + 3b 玩家属性 + 4f 基础指令（P1×3+P2×12）：3h message_prefix 三字段错值（show_on_system/per_channel/empty_title_text）/K 默认冲突//角色 三层契约缺失
- **用户 3 项拍板**：① 属性面板指令名改回 **/角色**（VIEW_CMD=角色）② **K=1 按 2026-08-24 拍板回写**玩家属性方案定稿 L86（0.2→1.0，仓库副本+原始两处）③ /角色 三层展示**无条件全层**（战斗外也显示临时层，B）
- **dsh 误判纠正**：批次3 判「3b K=1 与定稿冲突、代码注释编造仲裁权威」方向反了——K=1 是用户真实拍板（contract_deviations P1-1），实际是**定稿 L86 没回写拍板**；dsh 只 grep docs/ 漏了仓库根 contract_deviations.md
- **修复落地**：主 agent 修 3d（页码夹取回改+TC/合并承接表/L25-L46 错引/发送出口四项/状态行/折叠注记/粘合语法/5条可配/TPL-02-04 澄清）+ 3h（三字段+指针行号）+ 定稿 L86 + /角色 代码更名（basic_commands/parsers/测试/m4 登记）；3 路子 agent 并行修 5e（40+ 处行号实核+9 P2）/3b（ADR-06 K=1+70+ 处行号重对）/4f（补 /角色 指令契约 CMD-09 RUL-35~40 + /查看目标 挂靠）
- **回归：run_all_tests 全绿（exit 0）+ M4 门禁 verify_m4 全绿 + test_basic_commands/test_router 全绿**
- **M5 设计层确认完毕**：3 份细化裁决注记（3d 页码/4f 更名+全层/m4 §2.3 更名），可进 M5 实现层（D1-D5 消息模板渲染 + verify_m5）

### 2026-08-26 · M4 实现层审查（5 批）+ 修复 + 双审查收官
- **实现层审查 5 批**（dsh）：批次1 公共基础+解析（P1×2+P2×10）/批次2 模板+指令接线（P0×1+P1×1）/批次3 NPC（P1×3）/批次4 商店任务（P1×1）/批次5 签到指令组（P1×3）——报告落盘 审查_M4实现_批次1-5_jspace.md
- **修复**（5 组子代理 + 主 agent 收尾）：reward 静默丢奖→skip(item_add_failed)+幂等不封口（P1-1）/parsers-router 双管线 3 处漂移+对拍测试（P1-2）/dialog reply 误判信息类+text[]空白屏+快照 exec_index（P1-3×3）/黑市 listing_count 引擎消费+N 统一（P1-1）/checkin 指令层对齐真实引擎+跨月补签+表 id 限定+bonus mult 键（P0-1+P1-3）/dialog 修复时 e2e 冒烟对齐（npc.json info:true）
- **回归：run_all_tests 1981 passed / 0 failed；M0/M1/M2/M3/M4 五门禁全绿；G0 ARCH-OK；verify_m4 83 覆盖点 82 承载 + 1 DELAYED**
- **M4 正式收官：设计审查（4 批 + 用户 8 项拍板）+ 实现审查（5 批）+ 全部修复落地——交互系统（NPC/商店/任务/签到/指令解析/基础指令/GM）完整可用，可进 M5**

### 2026-08-26 · M4 实现层全部完成（8 批次 25 路，五门禁全绿）
- 批次0 公共基础：reward 发放器（355 行）/统一条件引擎（744 行）/日界（339 行）——唯一实现三铁律
- 批次1 指令解析：parsers（803 行，分隔符 7 种+快捷三模式+裁决①）/sender+list_render（TPL-08 页脚+裁决②夹取）/router（860 行，路由管线+会话路由+GM 禁绑）
- 批次2 NPC：npc_models（1185 行）/发牌员（751 行，裁决④策略+10 类动作+一次一物）/dialog（935 行，裁决③深度可配）
- 批次3 商店：shop_models（1137 行，裁决⑤并存+裁决⑥永不刷新）/shop 引擎（1286 行，原子防双扣+period 驱动）/shop 指令
- 批次4 任务：quest_models（1131 行）/quest 引擎（952 行，三原语+主线置顶）/quest 指令
- 批次5 签到：checkin_models（1163 行）/checkin 引擎（960 行，裁决⑦补签只计+裁决⑧三键表名限定）/checkin 指令
- 批次6 指令组：basic_commands（1109 行）/gm_commands（810 行，权限三级+静默+留痕+禁绑）/校验器接线+4 legal fixtures
- 批次7 集成：e2e_m4_smoke（154 断言六链路，NPC→商店→任务→签到→快捷→翻页夹取，确定性重放）/verify_m4（83 覆盖点 82+1 DELAYED）/run_all_tests 注册 M4 门禁
- **最终回归：run_all_tests 1946 passed / 0 failed；M0/M1/M2/M3/M4 五门禁全绿；G0 ARCH-OK**
- 下一步：M4 审实现（按流程实现后 dsh 审实现）

### 2026-08-27 · M4 设计审查（4 批）+ 用户 8 项拍板落地
- **M4 设计审查 4 批**（dsh）：批次1 指令解析（P0×2+P1×4）/批次2 NPC对话（P1×2）/批次3 商店（P1×4）/批次4 任务签到（P1×2）——报告落盘 审查_M4设计_批次1-4_jspace.md
- **幻觉设计实锤 3 处**：商店 D-07 编造"daily 4 点冲突"+"A3 日界裁决"（全文零匹配）；2b4 D-03 编造"商店 L339/L500 求值失败惯例"；2b1 把"深度可配"（用户拍板过）偷改"≤2 层硬约束"
- **用户 8 项拍板**：①战斗裸数字按定稿（归快捷表）②页码越界夹取最后一页 ③对话树深度恢复可配 max_dialog_depth ④发牌员策略保留细化版 rotate/random/condition ⑤商店库存+个人限购并存 ⑥商店不配 refresh=永不刷新 ⑦补签只计不补发奖励 ⑧[签到:*] 键加表名限定（[签到:loop.连续天数]）
- **回写**：7 份细化裁决注记（3c/3d/2b1/2b2/2b3/2b4/2b5）+ 发放器失败策略统一（条目失败黄字跳过，单事务=簿记原子性）
- **M4 设计层确认完毕，可进实现层**（路 2b 24 任务 + 3c/3d/4f/5b）

### 2026-08-26 · M3 实现层审查修复（4 批）+ 双审查收官
- **实现层审查 4 批**（dsh）：批次1 地图数据层 P0×2+P1×3 / 批次2 时间天气 P1×6 / 批次3 副本换区 P1×1 / 批次4 世界集成 P1×2——报告落盘 审查_M3_批次1-4_jspace.md
- **修复**（三组子代理 + 主 agent 补完）：校验器 _emit 适配（P0 空转/崩溃）、time_query 接 IF08 + 枚举可配适配、校验器声明枚举集、default_pool 链、enabled 门控、单值枚举黄通道、pursue 错失窗口只计偏离可达目标（深图追击可达）、PV 恢复落库+幂等（e2e 双触发暴露二次恢复 bug）、phases/zone_change 形态兼容、DungeonSession 追击字段（with_chase + round-trip）、max_alive 阻塞行计时冻结、虚弱 0 时长、dungeon_entrances 交叉校验、weather_weights 键、rest 默认值（冷却 3/限次 0）、version 防串档
- **fixtures 修正**：legal/maps.json 加世界图 volcanic_path（dungeon_entrances 合法承载）+ rubble_field 移除违规入口（R2 防嵌套）；测试同步（断言对齐新行为/结构）
- **回归：run_all_tests 988 passed / 0 failed；M0/M1/M2/M3 四门禁全绿；e2e 冒烟全绿；G0 ARCH-OK**
- **M3 正式收官：设计层审查（3 批 + 用户 3 项拍板）+ 实现层审查（4 批）+ 全部修复落地——G3 达成，可进 M4 数据框架**

### 2026-08-26 · M3 设计层 dsh 审查（3 批）+ 三项裁决落地（用户拍板）
- **流程纠偏**（用户要求）：以后先 dsh 审设计（缺漏/错误/幻觉）→ 确认 → 细化 → 实现。M3 补做设计审查：批次1 地图层（P0×2+P1×2+P2×12）/批次2 换区副本（P0×0+P1×1+P2×10）/批次3 时间天气（P0×0+P1×1+P2×7）
- **设计审查发现的关键幻觉**：① 2a1a 自造 `rank:gate`/`chance` 字段（定稿无）② exits 三套互斥 schema（2a1a 数组/2a1b 对象/2a1c 裸字符串）③ spawns/spawn/monsters 键名分裂 ④ 季节时段枚举「固定写死」篡改定稿「开放可配」⑤ 出没边界「仅非战斗」与 1g4 冲突 ⑥ PV 恢复满值口径与定稿锚点冲突
- **用户拍板 3 项**：① 键名统一 **monsters**（按定稿）② PV 恢复改**缺失量口径**（补已损失的一半，0=不恢复/1=全恢复）③ **枚举开放可配**（season.enum/period.enum，%len(enum)）
- **实现落地**：map_models 读 monsters 键 + fixtures/测试同步；chase_resume/dungeon_boss PV 缺失量口径（+未破防不降断言）；worldtime 枚举可配（构造解析 enum + cycle_tick %len + season_now 返回自定义键）+ time_validator V1b/V2b enum 校验 + weather_conditions ctx 枚举注入 + 4 个新测试
- **文档回写**：m3_shared_contract 4 处 + 7 份细化裁决注记（2a1a/2a1b/2a1c/2a2/2a4a/2a4b/2a4c）；审查报告 3 份落盘（审查_M3设计_批次1/2/3_jspace.md）
- **实现层审查批次1**（地图数据层）：P0×2（validate_maps _emit 适配断裂生产空转 / time_validator _emit 缺失崩溃）已修 + 回归测试 2 条；P1-3 isdigit 陷阱已修；P1-1/P1-2 待设计裁决对齐后修
- **回归：tests/unit 939 passed / 9 skipped + e2e 冒烟 126 断言全绿 + G0 ARCH-OK**

### 2026-08-26 · M3 批次 8 集成回归 + M3 完整收官（G3 门禁全绿）
- 路V：scripts/verify/verify_m3.py（G3 门禁：81 TC 诚实化覆盖声明 75 承载 + 6 DELAYED 注明理由；关键模块/函数断言；子进程 pytest）+ 自测 exit 0
- 路W：scripts/e2e_m3_smoke.py（M42 端到端冒烟 658 行 126 断言：探索版 S0→S1→S5→S7 41 条 + BOSS 版 S0→S3→S4→S5 83 条（入场限制/守门怪/三阶段/换区追击/续战标记/通关奖励）+ 运行级 2 条（固定 now 确定性重放一致、24 步时间天气钩子））
- 路X：tests/unit/test_m43_regression.py（M43 三回归探针 33 断言：全仓零定时器扫描（threading.Lock 允许清单自证）+ 确定性抽签跨实例同值 + 快照完整性 ai_state/combo_state/换区上下文逐字段一致）
- **G0 架构门禁修复**：收口接线暴露 content→engine 反向依赖（validator→worldtime、weather_validator→engine/weather_conditions）+ world→core（map_graph）——校验函数迁移到 content/time_validator.py、weather_validator 常量本地化、map_graph 移 content 层、check_architecture 矩阵登记 engine 层；ARCH-OK 4 项全绿
- **最终门禁：run_all_tests 全量 951 passed / 0 failed；M0/M1/M2/M3 四门禁全绿（G3 verify_m3：81 TC 中 75 承载 + 6 DELAYED）；M3 完整收官，G3 达成可进 M4 数据框架**

### 2026-08-26 · M3 批次 7 安全区/快照/死亡（M24-M30，3 路并行；首轮 scnet 503 瞬时故障重派成功）
- 路S：world/rest.py（504 行：is_safe_zone 入口区/safe_zone 配置/maps 标记并集；rest_in_dungeon HP/MP 恢复+冷却缩减+次数限制；rest_is_not_exit 休息≠离开保留全部上下文）+ 26 测试 62 断言
- 路T：world/snapshot_resume.py（333 行：resume_from_snapshot 快照续玩 ai_state/combo_state/换区上下文逐字段保留，真实 BattleEngine.from_snapshot 端到端；non_combat_exit 非战斗离开重置+明示文案「战斗中断可续玩，离开副本将重置」）+ 19 测试 107 断言
- 路U：world/dungeon_persist.py（775 行：on_dungeon_death 复活点优先级+虚弱禁入+BOSS 残血保留（死亡≠离开）；save/load/clear_dungeon_session 会话持久化 content_pack 防串档，checkpoint 驱动已清区域回退）+ 28 测试 156 断言
- **回归：tests/unit 923 passed / 9 skipped**（记录：scnet 端点 10:02-10:07 瞬时 503，批次 7 首轮三路全挂，curl 验证恢复后重派成功）
- **M3 功能批次全部完成（0-7）**，剩批次 8（端到端 M42 + 三回归 M43 + verify_m3 81 TC）

### 2026-08-26 · M3 批次 6 换区追击（M12-M15，2 路并行）
- 路Q：world/chase.py（477 行：chase_trigger 触发装配+候选区 sha256 确定性选一、begin_chase 提示「BOSS 逃向了 XX」、pursue 追击走错计数+错失窗口超限 BOSS 回满/离开）+ 26 测试 75 断言
- 路R：world/chase_resume.py（347 行：prepare_resume_battle 续战装配残血保持+PV 半恢复 floor+开场技标记+战斗快照续接；exit_dungeon_reset 离开副本全清重置，战斗中拒绝）+ 13 测试 62 断言（两路已自行对齐续战契约 continue_data）

### 2026-08-26 · M3 批次 5 副本两型+子任务（M17-M23，3 路并行）
- 路N：core/dungeon.py（672 行：DungeonStateMachine 全量 M1-M15 迁移 + DungeonSession 持久化形态（content_pack 防串档）+ explore_run 探索版流程（入口校验先于消耗/走图/通关 CLEARED/离开重置回外部锚点））+ 26 测试 90 断言
- 路O：core/dungeon_boss.py（415 行：BossFlow 入场守门怪检查/phase_for 三阶段阈值可配/应该换区触发链（enabled+threshold+timing 衔接 PhaseTable）/on_chase_continue 续战标记残血保持+PV 半值+开场技）+ 20 测试 50 断言
- 路P：core/dungeon_subquest.py（336 行：五形式判定 ProgressTracker/奖励发放防重复/可选性不阻塞 BOSS）+ 16 测试 59 断言
- **回归：tests/unit 811 passed / 9 skipped**

### 2026-08-26 · M3 批次 4 刷怪（M08-M10，2 路并行）
- 路L：world/spawn.py 实装 SpawnManager（410 行：initial_spawn/refresh 懒补刷——last_kill_time+respawn_minutes 时间差纯计算零定时器、weather 变速/0 不刷、filter_eligible seasons/periods/active_time AND、zone_expire_removal 时段结束移除「对方逃跑了」）+ 40 测试 65 断言
- 路M：world/spawn_weather.py（284 行：weather_weighted_refresh 变速补刷/在场不驱逐、max_alive_guard 聚合上限）+ GameWorld.monster_pool/get_boss 实装（读 spawn 表过滤 + gate_guard）+ 36 测试 74 断言
- **回归：tests/unit 749 passed / 9 skipped**

### 2026-08-26 · M3 批次 3 地图行走（M02-M06，2 路并行）
- 路J：core/map_graph.py（326 行：can_move 双向/单向拦截/隐藏条件门/死路 + bidirectional_consistent 不对称黄提示 + path_exists BFS 捷径寻路）+ 26 断言
- 路K：world/movement.py（430 行：resolve_move 隐藏通道门 + move_to_map 地图切换钩子（map_weather_seen 记录 + R22 双钩子）+ enter_context_route /进入 路由（方向行走/入口名/序号匹配，副本进入批次 5 接线））+ 33 测试 89 断言
- **回归：tests/unit 673 passed / 9 skipped**（记录：test_formula_whitelist_boolean_and_string 偶发失败——JS formula runner 瞬时态，隔离证明与批次无关）

### 2026-08-26 · M3 批次 2 天气引擎（M37-M41，3 路并行）
- 路G：worldtime 追加 IF05/IF08/IF04（map_pool 地图池覆盖/回退默认池、map_weather sha256 确定性抽签 同 tick 跨群重启同值、weather_now）+ 27 用例
- 路H：engine/weather_conditions.py（M40 三键注册：SEASON_KEYS/PERIOD_KEYS 复用 worldtime 单口、eval_condition fail-safe、REGISTERED_KEYS 供 V6）+ engine/weather_consumers.py（M39 联动：weather_mods rate_mult/rarity_shift clamp 4 档/0 不出、combat_weather_mult 默认关、lore 天气判定）
- 路I：content/weather_validator.py（M41 V5-V8 硬校验：地图池引用/消费方枚举/combat mults 键/模板占位符 + 黄提示六条）+ 26 用例 70 断言
- **收口**：validate_weather 接入 check_pack settings 分支；M40 三键由 weather_conditions 承载（任务系统 M5 接线时用 REGISTERED_KEYS/eval_condition）
- **回归：tests/unit 614 passed / 9 skipped**

### 2026-08-26 · M3 批次 1 时间引擎（M32-M36，3 路并行）
- 路D：worldtime.py 追加 IF09/IF10（check_changes 变化检测钩子——顺序固定季节→时段→天气 ≤3 条、离线跨周期只报最新；maybe_broadcast 懒广播——enabled 默认 false、占位符模板 {type,name,emoji,map}、跨群去重 seen 预留）+ 15 用例
- 路E：data/world_time_persist.py（IF11 time_state 存档契约：DEFAULT_TIME_STATE 四字段、load 缺补默认多忽略迁移、save 非原地、mark_map_seen 惰性、cache_indexes_equal 去重）+ 24 用例
- 路F：engine/time_query.py（M36 查询：season_status 天折算/period_status 分钟/weather_status pool_label 覆盖池标注）+ M43 探针 test_time_regression（零定时器源码扫描 + cycle_tick 确定性跨年）
- **回归：tests/unit 506 passed / 9 skipped**

### 2026-08-26 · M3 细化 + 批次 0 数据骨架（地图/副本/时间配置装载）
- **M3 细化交付**：docs/m3_shared_contract.md（9 批次 43 任务权威契约：地图层 8 字段/exits 通道/spawn 行/换区追击/副本 S0-S7 状态机/时间引擎 IF01-IF12/天气引擎/verify_m3 81 TC 矩阵/十条铁律）+ docs/m3_batch_plan.md（批次派工单 0-8）
- **批次 0（3 路并行）**：路A maps 装载（map_models.py 520 行 MapDef/SpawnDef/ExitDef + validate_maps + maps.json fixtures + 68 断言）/ 路B dungeon 两型（dungeon_models.py 523 行 DungeonDef + validate_dungeons + zone_change 校验 + dungeon.json fixtures + 50 断言）/ 路C time_cycle 配置（engine/worldtime.py 272 行 ANCHOR + IF01-07 纯函数 + validate_time_cycle V1-V3 + 71 断言，手算验证 2026-08-16=夏）
- **主 agent 收口**：三 validate 接入 check_pack（maps 专项+泛型并行防误拦/恢复旧 dead_range 检测、dungeon ValidationReport 桥接回填 _Checker、settings 追加 time_cycle）；field_meta 注册 dungeon 模块；loader _KIND_FOR_MODULE 加 dungeon；manifest 补 dungeon；清调试脚本；修并发 id 不对齐（dungeon fixtures molten_* → 实际地图 id rubble_field/crag_den/lava_tunnel + 测试同步）
- **回归**：tests/unit **440 passed / 8 skipped**，legal 包零红拦

### 2026-08-26 · M2 怪物体系全里程碑完成（多批多路，G3 门禁全绿）
- **批次1**（3 路并行）：A1 enemies 八段 schema+Def（models/field_meta，M0 简档兼容）+ A2 校验器 R1-R15+难度模板补全+木桩特例（validator 双轨）+ B1 AI 决策管线（core/monster_ai.py：两级状态机+L0-L7 套间评估+随机池归一化+hungry/cooldown）
- **批次2**（3 路并行）：A3 fixtures 八段化+action.json+1e TC-01~13 转 pytest + B2 条件行动 13 类触发+chain C 连招链（monster_conditions/monster_chains）+ B3 意图预告三级+阶段表（monster_intent/monster_phases）
- **批次3**（2 路并行）：C1 battle.py 怪物行动挂接（enemy_act None→MonsterAI.decide、ai_state 快照往返、打断=套完结、tick 单点递减）+ C2 1g4 战斗世界边界（world/battle_boundary.py：丢失挂起/脱战回血/死亡惩罚/跨群锁/时间线）
- **批次4**（2 路并行）：smoke→pytest 固化（test_monster_ai 76/test_monster_intent_phases 57/test_battle_boundary 24/test_monster_conditions_chains 44）+ verify_m2 门禁（34 TC 诚实化覆盖：29 承载 + 5 DELAYED）
- **主 agent 收口修复**：B1 L5 权重 0 落空（_weighted_pick zero_as_one）；三桩接线→新模块；battle 0 倍率 max(1,) 保底误伤短路（蓄力/起身/吼叫）；派生 cap 只对 _derived 生效（单技能 power 1.6 不封顶）
- **回归**：tests/unit 337 passed / 8 skipped；run_all_tests 全量 356 passed / 0 failed；verify_m0/m1/m2 门禁全绿
- 记录：偶发 1 failed 复跑消失（测试确定性待 dsh 审查重点盯）

### 2026-08-26 · M2 dsh 三批审查（P0=0 / P1=6 / P2 关键项修复）
- 批次1 数据层：P1-1 R15 接续概率 <60% 黄提示实现 + P2-1 pv_recover 枚举 + P2-2 NaN/Infinity 八段零拦截 + P2-4 action probability 去旗标（Y-2 误报）+ P2-7 weight 和=0 黄提示 + P2-9 condition obj 放宽（合法配置误拦）
- 批次2 AI 引擎：P1-1 **phase 写端**（HP→phase 换算接进 decide，phase_changed 联动闭环 TC-04）+ P1-2 **断链冷却 max 保留+起算偏移**（防永久锁链）+ P1-3 **蓄力 intent 统一走 build_intent**（图鉴分级不再被绕过）+ P1-4 chain_queue 剩余段语义登记 + P2-3 enqueue 委托唯一实现 + P2-4 __get_up__ 哨兵 + P2-5 快照残留 + P2-1 docstring
- 批次3 战斗+边界：P1-1 **combo_broken 一次性标记**（决策后清除，防无限触发）+ P2-1 decide 异常回落普攻 + P2-2 打断判定以 chain_queue 为真 + P2-3 lost_pending 快照预留 + P2-4 attack_type 全通道归一化 + P2-5 verify_m2 注解刷新 + P2-6 ratio clamp 防负货币
- 契约登记：m2_shared_contract 补 ai/phases/basic_action/chains.cooldown 键 + enemy.luk→player.lck 桥接 + enforce_mp 边界
- 补回归：tests/unit/test_m2_review_fixes.py（5 用例：R15<60%/phase 写端/冷却不重置/蓄力 intent/combo_broken 清除）
- 最终回归：**361 passed / 0 failed**，verify_m0/m1/m2 三门禁全绿，G3 达成

### 2026-08-25 · 通用规则 ⑱~㉒ 落地核查（架构与代码规范）
- ⑱ 先定架构 ✅：全仓有细化文档 + check_architecture.py 依赖矩阵门禁（TC-03）
- ⑲ 模块化 ✅：五层（core/world/storage/content/data）+ commands/web 高度模块化，职责单一
- ⑳ 文件元信息 ⚠️ 部分符合：每文件 docstring 已有功能描述（快速识别已满足）；标准「文件名/创建时间/作者」块仅**新文件**强制（api_response.py 已带头示范），存量 38 文件不做大规模回填（避免破坏稳定代码，登记待办）
- ㉑ 规范注释 ✅：函数/类均有 docstring（入参/出参/核心逻辑，M0 复查已确认）
- ㉒ 统一返回格式 ✅ **新增** `qbot_rpg/data/api_response.py`：对外接口（web/commands 壳层）统一 {code,msg,data} + ok()/error() 工厂 + to_dict()；内部纯逻辑层按架构定稿保持「领域对象+异常」不强制改造 → test_api_response.py（5 用例）

### 2026-08-25 · 通用规则 ⑭⑮⑯⑰ 落地核查（开发流程与版本控制）
- 规则 ⑭ 分块编写：项目天然按「里程碑 → 模块 → 功能」分块（M0 骨架/M1 战斗/…），无不一次性写完 ✓
- 规则 ⑮ 测试驱动：**已实现模块 100% 有配套测试**（content/storage/data/core 全部 + test_logging_utils 新增）；骨架占位模块（levelup/equipment/worldtime/battle_render/commands 等标注「M1/M3/M4 实装·仅骨架」）无测试为合理豁免，实装时补
- 规则 ⑯ 测验后提交：历次 commit 均在回归全绿后执行 ✓
- 规则 ⑰ 规范提交：提交信息统一 `feat:/fix:/chore:/docs:` 前缀 + 中文说明 ✓

### 2026-08-25 · 通用规则 ⑪⑫⑬ 落地（日志与异常处理）
- 新增 `qbot_rpg/data/logging_utils.py`：统一日志工具（stdlib logging、幂等初始化、RotatingFileHandler 1MB×3、QRP_LOG_DIR 可配、日志落 logs/qbot_rpg.log）
- 接入关键异常路径：loader 红拦阻断前记日志、hot_reload 加载失败/意外异常记日志（快照回退兜底）、storage integrity_check 失败记日志
- 新增 tests/unit/test_logging_utils.py（日志真实落文件 + 幂等）
- 依赖矩阵合规：logging_utils 放 data 层（storage/content 只允许依赖 data）
- 回归：234 passed / 0 failed 全绿

### 2026-08-25 · 通用规则 1~10 落地
- 规则 1：`记录.md` 创建（本文件）
- 规则 8：`requirements.txt` 锁版本（aiosqlite 0.22.1 / pytest 9.1.1 / pytest-asyncio 1.4.0）
- 规则 9：`.env.example` 模板（敏感信息不入库）
- 规则 10：`.gitignore` 补 .env/日志/覆盖率杂项

### 2026-08-25 · M0 复查收官（三批全部完成）
- **M0 复查全貌**：批1（storage+data+架构）P0×0/P1×11 ✅ + 批2（content 六件套）P0×1/P1×12 ✅ + 批3（core+测试）P0×1/P1×8 ✅ —— **合计 P0×2 / P1×31 全部修复**
- **批3 修复**（用户拍板 K=1 + 派生收敛）：
  - 命中率 K 统一 0.2→1.0（HitParams/damage.hit_rate 默认值，3b/1a 同口径）
  - 派生属性 7 函数收敛为委托 damage 参数化版（单一实现，常数可配置）
  - 面板 render_panel 改消费 calc_all_final_attributes（显示值=战斗最终值）
  - verify_m0 门禁诚实化（覆盖率简版标注、MIN_PASS_COUNT=100、_validate_fixtures 带断言）
  - worldtime 骨架按 2a4c IF01~IF12 重建 + M1→M3 修正
  - test_message_format 空洞断言（`or True`）修复
- **回归**：232 passed / 0 failed 全绿（G1/G2）
- **3 个 commit 已生成，未 push**（批1/批2/批3）

### 2026-08-24 · 批1+批2 修复
- 批1：只读池令牌泄漏/坏连接复用/回收丢数据/缓存竞态/迁移链+备份兜底/stack_max/data双轨标注/架构门禁 ImportFrom —— 11 项 P1 全修
- 批2：formula 安全绕过封堵（Unicode 转义 + 方括号构造器链）/monster_def_rate 负值黄提示/type 错注册/map Def.id/integrity 自检 A/BLK-5 暂停 —— 13 项 P0/P1 全修
- run_all_tests.py cwd 路径 bug 顺带修复

### 更早
- M1 战斗核心已完结（verify_m1 G2 全绿）
- 效果系统/炼金系统/NPC 系统等策划案定稿 39 份归档 /root/docs_archive/RPG框架项目/

---

## 二、下一步

1. **push 待推送的 3 个 commit**（需用户提供 GitHub token，走 mihomo 代理 7890）
2. M1 复查（战斗核心）或数据包阶段（数值验证）

---

## 三、未完成 / 待办

| 事项 | 状态 | 备注 |
|---|---|---|
| GitHub push（count 更新：7 commit） | ⏳ 待 token | M0 三批修复 + 规则落地 |
| 覆盖率 ≥80% 硬门禁 | ⏳ M6 恢复（M6 批7·路A 已接入真实核算：verify_m0 §5 / run_all_tests 阶段3；M0-M5 简版口径见 5d §7.4 决策记录 D5） |
| 提交规范差距（CI-06） | ⏳ M6 起 | 历史提交无 scope，M6 起按 <type>(<scope>) 执行（存量不追溯，只校验新增） |
| 3d 模板注册表 TPL-07~11 | ⏳ M5 D3/D4 | 页脚/折叠/✅❌ 标记 |
| worldtime 引擎实装 | ⏳ M3 | 骨架占位已对齐 IF01~IF12 |
| 时间天气/图鉴/NPC 等 | ⏳ M3/M4/M6 | 3f TC-01~20 defer |
| 存量 38 文件元信息块回填（规则 ⑳） | ⏳ 可选 | 现 docstring 已有功能描述；标准块仅新文件强制 |

---

## 四、项目通用规则速查

见用户通用工作规则 10 条（规则 1~10）：记录.md / 下载不等待 / 联网搜索 / 全中文 / 国内镜像 / 保护环境 / 写前检查 / 依赖锁定 / 配置分离 / .gitignore。
