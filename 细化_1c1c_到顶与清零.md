# 细化规划·1c1c：到顶行为与清零（reset/hold 语义 + 连段生命周期）

> 依据：《技能派生系统设计定稿》v1.0（2026-08-14），行号引用制（Lxx，可逐条回查）。
> 范围：① 到顶两模式（reset/hold）语义与配置字段；② 保留连段/推进连段（喘息技）；
> ③ 清零规则（死亡/胜利/失败/战斗结束一律清零）；④ 验收测试用例。
> 策划案未明处标注【细化补充】，供策划拍板（不改变策划案语义，仅收敛实现歧义）。
> 与 1c2（combo 配置与打断霸体）、1c3（连段测试集）同源同规，字段/时序不冲突。
> 验收用例：**19 条**（到顶 reset/hold 7 · 防御续链 5 · 战斗结束清零 7）。

---

## ① 到顶行为两模式：reset（归零重打）/ hold（保持蓄力）

> 总则：`max_combo`（连段上限，链级属性）+ `max_combo_behavior`（到顶行为）共同决定职业节奏差异（L16、L152）。

### 1.1 reset 循环型：到顶后归零重打

**语义（L154-155）：**
- 连段达 `max_combo` 后，下一次连段技**归零重打**——从 0 重新计数，形成节奏循环（L154）。
- 定位：节奏循环、精确卡段（eq 精确触发派生），代表职业剑士/格斗家（L155、L170）。

**判定顺序（L158，防死配置）：**
- 到顶**当回合**：① 先按新快照判定 `eq==max` 派生并执行；② 再执行归零。
- 无此顺序则 `eq=max` 派生永远不可触发（归零先于判定 → 死配置），校验器对 reset 模式 `eq=max`
  出死配置提示，建议改用 hold 或 min-max（L357）——提示不拒绝加载，内容包自行决策。

**细化到回合粒度（【细化补充】：收敛 L154/L158 的执行时刻）：**

```
第 4 次连斩（reset 4 段，存在 eq=4 派生）：
  ① 指令处理：快照 count 3 → 结算成功 → count=4（达顶）
  ② eq=4 派生判定：触发 → 本次技能替换为派生形态执行（L158）
  ③ 回合结算后：count 归零重打（L154）→ 下回合再使用连段技从 0→1
```

**与打断/霸体无耦合：** reset 链无"到顶免疫"（免疫是 hold 的专属被动态，L163）。

### 1.2 hold 蓄力型：到顶后保持最大

**语义（L156-157）：**
- 连段达 `max_combo` 后**保持最大，不再增长**（后续连段技不再 +1）（L156）。
- 定位：攒满连段 → 大招消耗连段 → 重新攒（L157）；代表职业法师/盾卫（L171、L173）。
- 到顶后**普通技能不打断**（L157）——蓄力状态不被普通行动破坏【细化补充：此为 §2.2 无标签普通技能归零
  （L53）在 hold 到顶态的豁免特例，与"攒满→大招消耗"循环一致；未到顶时仍按 L53 归零】。

**免疫被动态（L161-165，防"一次到顶永久免打断"）：**
- hold 免疫仅在 `combo == max` 时生效（被动态）（L163）。
- 派生技 `consume > 0` 消耗连段 → `combo < max` → **立即退出免疫**，需重新攒回顶（L164-165）。
- 大招消耗量必填（校验/内容包约束），杜绝无代价循环（L399）。

### 1.3 配置字段（落点：skill_chains.json §9.1 / 存档 §9.4）

| # | 字段 | 类型 | 必填 | 语义 | 引用 |
|---|------|------|------|------|------|
| 1 | `max_combo` | int | 是 | 连段上限（**链级属性**，内容包配；到顶判定的基准值） | L152 / L277 |
| 2 | `max_combo_behavior` | enum `reset`\|`hold` | 是 | 到顶行为：reset=归零重打；hold=保持最大不增长 | L153 / L278 |
| 3 | `steps[].consume` | int（默认 0） | 否 | 派生消耗连段数：用后连段减 N（hold 大招循环；**大招消耗量必填**；到顶态消耗→退出 hold 免疫） | L74 / L288 / L164 / L399 |
| 4 | `combo_state.count` | int | 存档 | 当前连段数（战斗快照 3.18 字段，恢复/清零判定读它） | L335-341 / L382 |
| 5 | `combo_state.hold` | bool | 存档 | 当前链是否 hold 模式（到顶免疫判定开关；与链配置 `max_combo_behavior` 同源冗余） | L339 / L163 |
| 6 | `combo_state.chain_id` / `chain_name` / `step_index` | string/string/int | 存档 | 活跃链标识与进度（恢复用；name 冗余防热重载失效） | L335-341 |

**校验器要点：** reset 模式 `eq=max` → 死配置提示（L357）；`min/max` 与 `max_combo` 矛盾、`eq > max_combo` → 死配置（L355-356）。

### 1.4 职业预置对照（L168-174）

| 职业 | max_combo | behavior | 设计目的 |
|------|-----------|----------|----------|
| 剑士 | 4 | reset | 节奏循环，精确派生（eq 2/eq 4）（L170） |
| 刺客 | 2 | reset | 快速小循环，低段派生（L172） |
| 法师 | 5 | hold | 蓄力保持，大招消耗连段（L171） |
| 盾卫 | 3 | hold | 防御攒大：防御不打断 → 边防边攒（L173 / L54） |

---

## ② 保留连段 / 推进连段（喘息技，P1 进阶）

### 2.1 标签语义（§2.1，对照表）

| tag | 使用后连段变化 | 语义/定位 | 引用 |
|-----|---------------|-----------|------|
| `combo_preserve` 保留连段 | **+0 不清零** | 喘息技：防御/辅助/回蓝——续链不断 | L43 / L52 |
| `combo_push` 推进连段 | **+N 不清零**（N 可配 1-2） | 喘息技的推进变体：喘一口气还推进段数 | L44 / L52 |
| `combo` 连段技 | +1 | P0 默认标签（对照项） | L42 / L51 |
| （无标签）普通技能 | **归零**（打断自己） | 对照项：喘息技的反面 | L45 / L53 |

**核心定位（L22、L28-29）：**
- 派生系统核心循环中，保留/推进连段是"喘息续链"环节——派生技使用后保留连段链，派生是链上节点
  而非终点（L22、L69-71）；喘息技让玩家在缺资源/想蓄势时不丢进度。
- **分级放出：** 默认内容包（P0）只含【连段技】一种标签；保留/推进为 P1 进阶内容（L31、L254-259），
  小白友好不加重负担。

### 2.2 与到顶/清零的交互规则

- 喘息技**不是清零事件**：任何时刻使用均不清零（L43-44、L52）；到顶（hold）时使用喘息技保持 max 不变。
- 喘息技**不是打断免疫**：使用喘息技期间仍可被对手打断技清零（免疫仅限 armor/霸体与 hold 到顶被动态，
  L47、L116、L163）【细化补充：对齐 1c2 TC-ARM-04】。
- 防御/道具不清零（L54-55）——盾卫"防御攒大"的组成部分（L173）。
- 冷却中派生技：自动降级为原技能 + 连段保留（防链卡死）（L203）。
- 指令被拒（MP/冷却/条件）：不耗回合、不改连段、可反复尝试（L56、L204）。

### 2.3 配置字段（skills.json §9.2）

| # | 字段 | 类型 | 必填 | 语义 | 引用 |
|---|------|------|------|------|------|
| 1 | `tag` | enum | 否 | `combo`/`combo_preserve`/`combo_push`/`interrupt`/`armor`/无；缺省/无=普通技能 | L310 / L353 |
| 2 | `combo_push_amount` | int（默认 1） | 否 | 仅 `tag=combo_push` 生效：+N 的 N ∈ {1,2}【细化补充：收敛 §2.1"N 可配 1-2"为显式字段，对齐 1c2】 | L44 |

编辑器：技能卡标签下拉（combo/preserve/push/interrupt/armor）（L371）。

---

## ③ 清零规则（连段生命周期）

### 3.1 清零事件全集（触发即归零）

| # | 事件 | 规则 | 引用 |
|---|------|------|------|
| 1 | **死亡 / 胜利 / 失败 / 战斗结束** | **一律清零**（连段是战斗内资源，不跨战斗残留） | L59 / L182 |
| 2 | 战斗结束（任何原因：胜利/失败/死亡/逃跑成功/怪物丢失/中断回收） | 一律清零 | L182 |
| 3 | 逃跑成功 | 连段清空 | L57 |
| 4 | 使用无标签普通技能成功结算 | 归零（打断自己），提示"连段中断！（原因：使用了普通技能）" | L45 / L53 / L143 |
| 5 | 被对手打断技命中（非霸体/非 hold 到顶态） | 目标连段清零，提示"⚡ 盾击打断连斩！连段中断" | L112 / L141 / L385 |
| 6 | 复活 | 复活不携带连段（combo=0） | L184 |
| 7 | 跨战斗（默认） | 战斗结束清零；可配"跨战斗蓄力"开关（默认关，内容包可开） | L213 |

**不清零豁免清单（对照）：** 防御（L54）｜道具（L55）｜指令被拒（L56）｜逃跑失败（L57）｜被控制 skip_turn（L58）
｜喘息技 combo_preserve/combo_push（L43-44）｜hold 到顶普通技能（L157）｜霸体期间被打断（L116）｜hold 到顶被打断（L163）。

### 3.2 消耗 ≠ 清零

- 派生技 `consume > 0`：连段**减 N**（非归零）——hold 大招循环的出口（L74、L157）；消耗后 `combo < max`
  立即退出 hold 免疫（L164）。
- 目标死亡时连段未消耗（大招溢出）：按配置转少量经验 / 下场起始 +1（可配项，默认不启用）（L238）。

### 3.3 实现落点与恢复

- `combo_state` 入战斗快照（3.18），**随战斗结束清零**（L382）；清零动作挂在战斗结算收尾（回合结束 tick 后）。
- 战斗中断恢复：combo_state 从快照还原（"连段 3/4 保持中"）（L183、L332-342）。
- 链被删除：进行中战斗按"无链=普通技能"降级，不报错（L343）。
- 清零是单向事件：清零后重新积累需从 0 开始（reset 归零重打即清零语义的节奏化复用，L154）。

---

## ④ 验收测试用例（19 条）

### 4.1 到顶 reset vs hold（7 条）

| ID | 名称 | 前置 | 步骤 | 期望 | 引用 |
|----|------|------|------|------|------|
| TC-TOP-01 | reset 到顶归零重打 | 剑士链 reset/4，连斩×4 达顶（combo=4） | 第 5 次使用连段技 | combo 归零后从 0→1（归零重打，节奏循环成立），非 4→5 | L154 / L170 |
| TC-TOP-02 | reset 到顶当回合判定顺序 | reset/4，连段 3，存在 eq=4 派生 | 第 4 次连斩（达顶当回合） | 先判定并执行 eq=4 派生（本次替换为派生形态），**再**归零——顺序反则派生永不触发 | L158 / L357 |
| TC-TOP-03 | reset+eq=max 死配置提示 | 加载 reset 链，step 条件 `eq:4` 且 `max_combo:4` | 校验器加载 | 出死配置提示（建议改 hold 或 min-max），不拒绝加载 | L357 |
| TC-TOP-04 | hold 到顶保持不增长 | 法师链 hold/5，combo=5 | 再使用连段技 | combo 保持 5，不再 +1 | L156 / L171 |
| TC-TOP-05 | hold 到顶普通技能不清零 | hold/5，combo=5 | 使用无标签普通技能 | combo 保持 5（到顶蓄力保持；到顶态豁免普通技能归零）【细化补充：L157 特例】 | L157 / L53 |
| TC-TOP-06 | hold 免疫进出（被动态） | 盾卫 hold/3，combo=3（==max） | ① 对手打断 → ② 大招消耗 3 段 → ③ 对手再打断 | ① 免疫（combo==max 被动态）；② combo=0<max 立即退出免疫；③ 打断成功清零 | L163-165 / L399 |
| TC-TOP-07 | 职业预置差异生效 | 加载剑士4/reset、法师5/hold、刺客2/reset、盾卫3/hold 四条预置链 | 各自连段至顶 | 各链 max_combo 与到顶行为严格按配置生效（节奏差异成立） | L168-174 |

### 4.2 防御续链（5 条）

| ID | 名称 | 前置 | 步骤 | 期望 | 引用 |
|----|------|------|------|------|------|
| TC-DEF-01 | 防御不清零可续链（攒大） | 盾卫 hold/3，combo=2 | 使用【防御】→ 下回合连段技 | combo 保持 2/3；连段技后 3/3 达顶（防御攒大成立） | L54 / L173 |
| TC-DEF-02 | combo_preserve 喘息 +0 不清零 | 连段 2/4 | 使用 combo_preserve 喘息技（回蓝/辅助）→ 下回合连段技 | combo 保持 2/4；可正常续链（+1 → 3/4） | L43 / L52 |
| TC-DEF-03 | combo_push 推进 +N | 连段 2/4，combo_push_amount=2 | 使用 combo_push 推进技 | combo 2→4 达顶，不清零（+N 推进成立） | L44 / L52 |
| TC-DEF-04 | 指令被拒不清零 | 连段 2/4，MP 不足（或冷却中/条件不满足） | 尝试使用被拒技能 | 不耗回合、combo 保持 2/4、可反复尝试；冷却中派生降级为原技能+连段保留 | L56 / L203-204 |
| TC-DEF-05 | 逃跑失败/被控制保留 | 连段 2/3 | ① 逃跑失败 ② 被控制 skip_turn | ① combo 保持 2/3 ② combo 保持 2/3（断节奏不断积累） | L57-58 |

### 4.3 战斗结束清零（7 条）

| ID | 名称 | 前置 | 步骤 | 期望 | 引用 |
|----|------|------|------|------|------|
| TC-END-01 | 胜利清零 | 连段 4/4 | 战斗胜利 | 战斗结束 → combo 一律清零（战斗内资源，不跨战斗残留） | L59 / L182 |
| TC-END-02 | 失败/死亡清零 | 连段 4/4 | 战斗失败 / 角色死亡 | combo 清零（两种结局均触发） | L59 / L182 |
| TC-END-03 | 复活不携带连段 | 角色死亡（combo 已清零） | 复活 | 复活后 combo=0，不携带死亡前连段 | L184 |
| TC-END-04 | 逃跑成功清空 | 连段 3/3 | 逃跑成功 | 连段清空；逃跑失败则保留（见 TC-DEF-05） | L57 / L182 |
| TC-END-05 | 中断恢复还原 | 连段 3/4 战斗中断 | 恢复该战斗 | combo_state 从快照还原（"连段 3/4 保持中"）；combo=3 | L183 / L332-342 |
| TC-END-06 | 跨战斗默认清零+可配开关 | 胜利后开启新战斗 | ① 默认配置 ② 开启"跨战斗蓄力"开关 | ① 新战斗 combo=0 ② 按配置保留连段（默认关，内容包可开） | L213 |
| TC-END-07 | 大招溢出补偿（可配） | 连段 4 大招击杀目标，连段未消耗 | 按配置开启补偿 | 连段转少量经验 / 下场起始 +1；默认不启用 | L238 |

---

*统计：TC-TOP-01~07 + TC-DEF-01~05 + TC-END-01~07 = 19 条；覆盖定稿 L16-L399 到顶/喘息/清零全部规则行，无跨章节自造规则。*
