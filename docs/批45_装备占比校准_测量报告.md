# 批45 · 装备占比校准（40% → 60%）· 测量与对照报告

> 依据：`docs/深度打造_决策记录.md` **§三 补充 1**（用户 2026-09-19 原话 + 主 agent 执行口径，本批唯一决策依据）；
> 参考：`/root/deliverables/打造系统_B_数值与经验规划.md` §四/§5.5、`docs/veinborn_阶段一_数值模型_1-35_v2.md`（7:8:5 出处）、
> `docs/审查参考/战斗数值层设计定稿.md`（三档推演口径）。
> 复跑脚本：`scripts/batch45_measure.py`（同一脚本、同一内容包、同一配置读取路径；修前/修后跑同一份代码）。

---

## 〇、落点核查（先核查再说改动）

| 项 | 落点 | 证据 |
|---|---|---|
| 面板 7:8:5 预算出处 | 规划文档 | `docs/veinborn_阶段一_数值模型_1-35_v2.md:32`（v1 `:20-33` 有逐份数值表） |
| 执行口径 | 决策记录 | `docs/深度打造_决策记录.md:37-45`（装备 8→18 ×2.25；白值 7/buff 5 不动；面板总功率 20→30） |
| 装备数值「总额度/系数」**聚合唯一落点** | `EquipmentEngine.aggregate_bonus` | `qbot_rpg/core/equipment.py:789`（各穿戴件 `stats_bonus` 同层求和写 `attributes.bonus`；`_recalc` `:567` 调用） |
| 装备 def 数值字段 → 实例 `stats_bonus` | `extract_bonus` / `route_bonus_into` | `qbot_rpg/data/gear_stats.py:166` / `:180`（键空间唯一源；强化/打造/商店均走此转实例） |
| 怪物 combatant 构造**唯一落点** | `_enemy_combatant` | `qbot_rpg/commands/battle_launch_commands.py:145`（`str→atk / con→dfn / agi→spd`；战力入口 `launch_pve_battle:307` 调用于 `:400`） |
| 真实白值/装备/怪物数据 | content | `content/veinborn/{stats,jobs,items,enemies}.json` |
| 战斗伤害公式（斩回口径） | `core.damage` | `defense_factor` `damage.py:470`、`channel_phys` `:524`；引擎消费 `battle.py:4498/4562` |

**结论**：仓库内装备数值是**逐件硬值**（`items.json` / `forge.json`），不存在已实现的「装备预算系数」；
当前唯一的**装备总额度可参数化落点** = `EquipmentEngine.aggregate_bonus`（读时聚合，不改任何落档数据 → 天然可回滚）。
怪物侧唯一落点 = `_enemy_combatant`（读 `enemies.json` → combatant）。

---

## 一、第一步 · 修前测量（基线；本报告的对照基准）

### 1.1 方法与种子（可复跑）

- **玩家**：35 级脊剑士（`content/veinborn/jobs.json` → `ridge_blade`.growth = atk 10 / dfn 6.5 / hp 12 / foc 6 / mp 1）；
  白值 = `stats.json` base + 职业 growth×(35−1)。毕业装取真实内容 **7 槽**：`guji_blade_d`（atk 396）＋`haozhu_{helm,body,hand,leg,foot}`（atk 16×5、dfn 276）＋`night_king_seal`（atk 10、crit 4）。
- **面板三源**：白值 = `PlayerAttributes.base`；装备 = `aggregate_bonus` 后的 `bonus.flat/pct`；buff = 战斗 `temp` 层。
- **buff 三档**：模型「5 份」按 lv35 白值等比折算 `unit = 白atk / 白值份 = 360/7 = 51.43` → 满 buff = 257.14、半档 = 128.57、无 buff = 0。
- **斩杀回合口径**：`core.damage` 执行级公式（与 `battle.py:4562` 同源）
  `D = max(1, floor(atk × skill_mult × weak × crit × K_def/(eff_con+K_def) × mdr))`，`turns = ceil(hp/D)`。
  确定性档 = 单发普攻（skill_mult=1.0）、不利用弱点（1.0）、不暴击（1.0，取下限）、乱数 1.0。
  蒙特卡洛 = **N=20000，种子 20260919**，命中/会心/格挡/乱数全部按引擎公式抽样。
- **三只代表怪**（真实 `enemies.json`，同属 D 区终局）：

| 类 | id | 名称 | hp | str | con | agi | foc |
|---|---|---|---|---|---|---|---|
| 普通 | `vein_black_bear` | 黑脉熊 | 2524 | 1106 | 154 | 10 | 28 |
| 精英 | `black_crystal_troll` | 黑晶巨魔 | 4285 | 1445 | 344 | 8 | 25 |
| Boss | `mountain_howl_lord` | D主1·群山嚎主 | 4733 | 1735 | 376 | 16 | 45 |

### 1.2 修前面板构成（实测数字）

| 源 | atk | dfn | hp | 说明 |
|---|---|---|---|---|
| 白值（7 份） | **360** | **251** | **808** | `stats.json` 20+10×34 / 30+6.5×34 / 400+12×34 |
| 装备（8 份 × 区段完成度） | **486** | **276** | 0 | 毕业 7 槽（原始值；`equip_stat_mult=1.0`） |
| buff（5 份 × 半档） | **128.57** | — | — | temp 层，半档 |
| **中值面板** | **974** | **527** | **808** | 360+486+128.57 = 974.57 → 管线 floor 974 |

- **预算占比（装备）= `8 / (7+8+5)` = 40.00%** ✅（决策记录口径：装备 8 份 / 合计 20 单位）
- 装备 `equip_stat_mult = 1.0`（未配置 → 缺省现状，回归基准）。

### 1.3 修前斩杀回合（3 只怪 × 三档）

| 怪物 | 类 | 无buff下限 | 半buff中值 | 满buff上限 |
|---|---|---|---|---|
| 黑脉熊 | 普通 | 8（MC 4.48） | **7**（MC 3.96） | 6（MC 3.59） |
| 黑晶巨魔 | 精英 | 23（MC 12.16） | **20**（MC 10.61） | 18（MC 9.42） |
| D主1·群山嚎主 | Boss | 27（MC 15.33） | **24**（MC 13.37） | 21（MC 11.87） |

> 括号 = 蒙特卡洛均值回合（含会心 1.3~2.2× / 格挡 / 乱数，故低于确定性下限口径）。
> 确定性口径为**主对照口径**（可比、可反解）；MC 作同种子的独立复核。

**修前基线 JSON**：`/tmp/batch45_before.json`（本报告 1.2/1.3 全部数字由该文件产生）。

---

## 二、第二步 · 改动（参数化 + 可回滚）

> 见 commit #2。全部新增参数缺省值 = 现状（`equip_stat_mult=1.0` / `hp_mult=atk_mult=1.0` / 不注入 `def_factor`），
> **不配置 = 与本批引入前逐字段一致**（回归对拍见 §3.3）。

### 2.1 参数与落点

| 参数（中文名） | 键 | 旧值 | 新值（veinborn） | 落点 |
|---|---|---|---|---|
| 装备面板倍率 | `settings.panel_budget.equip_stat_mult` | 1.0 | **2.25** | `core/equipment.py` `aggregate_bonus`（`:789`） |
| 面板预算·白值份 | `settings.panel_budget.white` | 7 | **7（不动）** | 占比口径（编辑器可见） |
| 面板预算·装备基准份 | `settings.panel_budget.equip` | 8 | **8（不动）**；有效 = 8×2.25 = 18 | 同上 |
| 面板预算·buff份 | `settings.panel_budget.buff` | 5 | **5（不动）** | 同上 |
| 怪物生命倍率 | `settings.monster_scaling.hp_mult` | 1.0 | **1.5** | `battle_launch_commands._enemy_combatant`（`:145`） |
| 怪物攻击倍率 | `settings.monster_scaling.atk_mult` | 1.0 | **1.5** | 同上 |
| 怪物防御系数补偿 | `settings.monster_scaling.def_factor` | （不注入=1.0） | **1.0828** | 同上（`con' = (con+K)×def_factor − K`，K=`formula.defense.k`） |

**依据比例**：
1. 装备 8→18（×2.25）→ 预算占比 `18/(7+18+5)` = **60.0%**；白值 7 / buff 5 份冻结 → **白值、buff 绝对数值不变**；
2. 面板总功率 20→30（**+50%**）⇒ 怪物 **HP ×1.5、攻击 ×1.5**（同一 +50% 比例）；
3. 防御 `con` 不按 ×1.5 硬缩——防御系数 `K/(con+K)` **非线性**，等比缩放会使中值斩回偏离（如 Boss 24→33）；
   按「斩杀回合不变式」反解 `def_factor = R / 1.5`（R = 中值面板 atk 实测比值 ≈ 1.6242），得 **1.0828**，使中值档斩回**逐只完全一致**。

### 2.2 未改动（硬约束）

- 白值 7（`stats.json` base/growth 未动）、buff 5（temp 层语义未动）——**绝对数值不变**；
- **品质概率表**（`settings.alchemy.quality_coef` / `forge` 概率阶梯）未动；
- **暴击参数**（`formula.crit.*` / `data/gear_stats` COMBAT 键）未动——装备倍率**只作用于面板轴 atk/dfn/hp**，
  不作用于 `crit / earplug / super_crit_lv / elem_crit_lv / 吸免穿` 等战斗词条，故会心/命中/格挡参数逐字段不变；
- **既有战斗公式结构**（判定顺序/双通道/防御系数/取整）未动。

---

## 三、第三步 · 修后对照

> 待第二步落地后由同一 `scripts/batch45_measure.py` 生成，见下方 §3。
