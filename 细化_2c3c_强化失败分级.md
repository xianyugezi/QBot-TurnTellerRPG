# 细化规划 · 2c3c 强化失败分级与转移（QBot-TurnTellerRPG）

> 性质：实现层细化规划（强化系统的失败分级三档结果 / 保护石 / 幸运修正对成功率与失败判定的影响 / 强化等级转移（内容包选项，默认关）/ 达顶零消耗 / 验收测试用例）
> 依据：《强化系统设计定稿》v1.0（`/root/docs_archive/RPG框架项目/强化系统设计定稿.md`，共 235 行）→ 简称【强化定稿】；本文每项定义均引用定稿原文并标注行号，可逐条回查。
> 接缝关系：与兄弟文档《细化_2c3a_强化数值曲线》§4（失败分级）同源；本文为失败分级 + 保护石 + 幸运联动 + 转移机制的**专项深化**，2c3a 已覆盖的成功率/消耗/上限数值不重复展开，仅保留本文所需判定链引用。
> 范围：失败档位 3（不变 / 掉级 / 碎）、失败分段按**当前等级**两段（≤split 低段不变 / >split 高段掉级，L42 铁剑+5→+4 示例口径）；强化石三档 +1~+3 / +4~+6 / +7+ 为**消耗档位**（按目标等级取石），与失败段无绑定（见 §1.1 裁定）、保护石触发与获取、幸运 √差公式与显示拆分、转移开启语义与校验一致性、达顶零消耗、24 条可脚本验证验收用例。

---

## 0. 核心判据（一句话 + 键值索引）

> 【强化定稿】L6 用户拍板：`失败惩罚分级（低不变/高掉级/碎=内容包选项）/ 素材=炼金产出+金币 / 转移=内容包选项我们不用 / 幸运修正 / 无等级限制默认`
> 【强化定稿】L15-16：`强化 = 给装备堆数值（+N）的系统…低等级失败无惩罚，高等级失败掉级`

| 键值 | 默认 | 出处 | 说明 |
|---|---|---|---|
| 失败结果档位 | 不变 / 掉级 / 碎（碎默认关） | L53-54 / L60-61 | 三档失败结果；前两档由 fail_tier_split 划分 |
| 等级区间 | 低段 +1~+3 / 高段 +4+（中[4-6]高[7+]同属高段） | L53-55 / L155 | 区间边界与强化石三档对齐（L179-181） |
| 保护石 | item_protect_stone（失败时扣 1 颗免掉级） | L68 / L188 | 可空；仅高段失败触发 |
| 幸运修正 | 实际率 = 基础率 +（√我幸 − √敌幸）% | L87 | luck_affects 默认开可关（L89 / L159） |
| 转移 | transfer_allowed: false（我们不开） | L127 / L160 | 同部位新装备 + 转移石/金币（可配） |
| 达顶 | /强化 提示"已达强化上限" | L115 | 零消耗、零随机 |

---

## 1. 失败分级（三档失败结果 × 三段等级区间）

### 1.1 段位区间与失败结果对照（含"三档"口径裁定）

> 【强化定稿】L53-55：`低等级段（+0 → 低段上限，默认 +3）：失败 = 装备不变（无惩罚）；高等级段（低段上限以上，默认 +4+）：失败 = 强化等级 -1（掉 1 级）；分级阈值可配（fail_tier_split）`
> 【强化定稿】L155：`"fail_tier_split": 3`
> 【强化定稿】L179-181：`"stone_tiers": [ {"tier":"low","levels":[1,2,3]}, {"tier":"mid","levels":[4,5,6]}, {"tier":"high","levels":[7,8,9,10]} ]`

**裁定（基于行号的权威阅读）**：任务口径"低[1-3]不变 / 中[4-6] / 高[7+]掉级"与定稿两段制对照如下——按定稿 L53-54，**默认只有两段**：**当前等级** ≤ fail_tier_split（默认 3，即 +0~+3）为低段（失败不变），当前等级 > split（+4 起）为高段（失败掉 1 级，参照 L42「铁剑+5 → +4」= 当前等级 −1）。"中[4-6]"与"高[7+]"在定稿中同属高等级段，失败效果完全一致（掉 1 级）；该区间边界只是与强化石三档（L179-181，按目标等级取石）对齐的**挫败感预期分段**，不引入第三个失败档，**与失败分段判定无绑定**。碎装备为第三个失败结果档，但属内容包选项且默认关（L60-62）。据此实现按定稿两段制落码，"三档"仅作为回执文案与策划沟通口径。

**段位区间表（失败判定按当前等级；区间列为当前等级，强化石档位 L179-181 按目标等级取石、与失败段无关）**：

| 区间 | 定稿段位 | 失败结果 | 默认是否触发保护石 | 引用 |
|---|---|---|---|---|
| +0 ~ +3（低） | 低等级段（当前 ≤ split） | 不变（无惩罚） | 否（无掉级可免） | L53 / L155 |
| +4 ~ +6（中） | 高等级段（当前 > split） | 强化等级 -1（当前−1） | 是 | L54 |
| +7 ~ +12（高） | 高等级段（当前 > split） | 强化等级 -1（当前−1；碎装备模式开启后另有小概率碎） | 是 | L54 / L61 |

**三档失败结果表**：

| 档位 | 触发条件 | 结果 | 默认 | 引用 |
|---|---|---|---|---|
| 不变 | 高段以外，且未碎 | 装备等级不变，提示"强化失败，装备保持不变" | 低段恒触发 | L38 / L53 |
| 掉级 | 当前等级 > fail_tier_split（高段） | 强化等级 -1（当前 −1，一次 1 级） | 高段恒触发 | L42 / L54 |
| 碎装备 | 内容包开启 shatter_mode 且高段失败掷中碎率 | 装备碎（归宿内容包自定，默认不碎） | 关（默认 0%） | L60-62 / L156-157 |

### 1.2 掉级语义

> 【强化定稿】L42：`❌ 失败！强化等级 -1（铁剑+5 → +4）`
> 【强化定稿】L38 / L45：`（失败：+1~+3 阶段失败不变 → "强化失败，装备保持不变"）` / `强化成功提示数值变化；失败提示惩罚类型`

- 高段失败：当前 +N → +(N−1)（一次 1 级，L42/L54「掉 1 级」）。
- 失败仍消耗已扣材料（强化石 + 金币），不退还（L38/L42 示例语义；2c3a §1.5 同口径）。
- 等级下限护栏：默认 split=3 时低段永不掉级，无负数边界；若内容包将 split 配为 0，则 +1 失败即掉级——实现须钳制等级下限 0（+0 不再递减），【工程补白】。
- 掉级只减强化等级；装备白值/孔位/品质等本体属性不受影响（三系统边界：强化只管"加数字"，L16/L26）。

### 1.3 碎装备（内容包选项，默认关）

> 【强化定稿】L60-63：`碎装备模式：内容包可配（default: off）；开启后：高等级段失败有小概率碎装备（概率可配，默认 0%）；我们自己的数据包：不开（用户拍板"我们不要"）；校验器：碎装备模式提示（碎率超默认 5% → 警告"碎率高挫败感强，确认？"）`
> 【强化定稿】L156-158：`"shatter_mode": false, "shatter_chance": 0, "shatter_max": 5`

| 字段 | 默认 | 语义 | 引用 |
|---|---|---|---|
| `shatter_mode` | false | 关；内容包显式配 true 才开启 | L60 / L156 |
| `shatter_chance` | 0 | 碎率（%）；仅 shatter_mode 开启时生效 | L61 / L157 |
| `shatter_max` | 5 | 碎率参考上限；超出仅**警告不拦截**（可配 0=不限） | L63 / L158 |

- 判定序（高段失败后）：先掷碎（shatter_mode=true 且 shatter_chance>0）→ 未碎再执行掉级；碎 = 装备消失（归宿由内容包定义，默认不碎）。
- 我们自己的数据包：不开（L62）。校验器 V3：碎率超 shatter_max → 软警告"碎率高挫败感强，确认？"，不拦截（L63 / L200）。
- V3 附则：碎率 0 时无需开启 shatter_mode（L200"碎率 0 时无需开启"）。

### 1.4 失败回执文案（统一口径）

| 场景 | 回执示例 | 引用 |
|---|---|---|
| 低段失败 | `❌ 强化失败，装备保持不变（铁剑+3）` | L38 |
| 高段失败 | `❌ 失败！强化等级 -1（铁剑+5 → +4）` | L42 |
| 高段失败·保护石 | `❌ 失败！保护石发动，等级不变（铁剑+5，消耗 1 颗保护石）` | L68（文案细化） |
| 碎装备（内容包开启） | 内容包自定义，须明示"装备已碎" | L61（归宿自定） |
| 失败回执均须提示惩罚类型（不变/掉级/碎/保护） | — | L45 |

---

## 2. 保护石机制

### 2.1 触发规则

> 【强化定稿】L68-70：`保护石：强化失败不掉级（消耗 1 颗，失败时消耗）；来源：炼金产出 / 商店高价 / 活动奖励（可配）；作用：高等级段堆装备的保险`

- 触发条件：**高段失败**（当前等级 > fail_tier_split）且走 `/强化保护` 分支（L215）→ 消耗 **1 颗**保护石，等级不掉。
- 成功时不消耗（无失败即无"失败时消耗"）。
- 低段失败不消耗——低段本无掉级（L53），保护石无可免；【裁定】
- 判定序：失败已被判定为掉级档 → 检查保护石数量 → 足够则扣 1 颗免掉级 → 不足则拒绝本次强化（不扣石不扣金币不掷随机，原子失败，L44 精神延伸）【工程补白】。

### 2.2 获取途径（可配）

> 【强化定稿】L69：`来源：炼金产出 / 商店高价 / 活动奖励（可配）`

- 三渠道：炼金产出（消耗品合成链路，配合 recipe.json）、商店高价（金币回收口）、活动奖励。
- 与强化石同为消耗品体系：保护石物品 ID 由 `enhance.json.protect_stone` 指向 items.json（D-13，L188），存在性校验。

### 2.3 指令与存在性

> 【强化定稿】L215：`/强化保护 <装备>  # 用保护石强化（可选）`
> 【强化定稿】L188：`"protect_stone": "item_protect_stone"  // 保护石物品 ID（可空）`

- `protect_stone` 配置为空 → `/强化保护` 指令不可用，回执提示"未开放"（软降级，不阻断 `/强化` 普通路径）【工程补白】。
- 加载时校验物品 ID 存在于 items.json；缺失同上软降级（2c3a §7-S4 同款）。

### 2.4 边界汇总

| 场景 | 行为 | 出处/依据 |
|---|---|---|
| 高段失败 + 携带保护石 | 扣 1 颗，等级不变 | L68 |
| 强化成功 | 不消耗保护石 | L68（失败时消耗反推） |
| 低段失败/u003c=split | 保护石不消耗、不提示触发 | L53 / L68 裁定 |
| 保护石数量不足 | 拒绝本次强化，提示缺 N 颗；零消耗零随机 | 【工程补白】（L44 原子失败精神） |
| protect_stone 未配置 | /强化保护 提示"未开放" | 【工程补白】（L188 可空 + S4） |
| 达顶后 /强化保护 | 同达顶零消耗（见 §5） | L115 精神延伸 |

---

## 3. 幸运修正对成功率的影响

### 3.1 √差公式（字段级）

> 【强化定稿】L87：`实际成功率 = 基础成功率 +（√我方幸运 - √对方幸运）%（对方=0 时即 √我方幸运%）`
> 【强化定稿】L88：`幸运来自角色属性（stats.json 自定义 luck 属性，内容包配）`
> 【强化定稿】L89：`幸运修正可选开关（luck_affects: true 默认开，内容包可关）`

```
base        = success_curve[to].rate            # 基础率（%），2c3a §2.1
luck_luckpp = round(√my_luck − √opp_luck, 1)    # 幸运修正（百分点，保留 1 位小数）
rate        = base + (luck_affects ? luck_luckpp : 0)
final_rate  = clamp(rate, 0, 100)               # 边界钳制
success     = rand(0, 100) < final_rate          # 判定（含下限边界，2c3a §2.4 同款）
```

- PvE 无对手：`√opp_luck = 0`，即修正 = `√我方幸运%`（L87 括号内原文）。
- stats.json 未配置 luck / 读不到 → 按 0 处理并黄提示【工程补白】（2c3a §7-S3 同款）。
- 开关关闭（luck_affects=false）：修正恒 0，仅基础率（L89）。

### 3.2 显示拆分（透明度）

> 【强化定稿】L90：`显示："成功率 60%（基础 55% + 幸运 5%）"`
> 【强化定稿】L36 / L41：`成功率 90%｜幸运修正 +2` / `成功率 50%｜幸运修正 -1`

- 强化回执成功率按三段展示：`成功率 X%（基础 A ± 幸运 B）`；正修正用 `+`，负修正用 `−`（B 为百分点的绝对值）。
- 幸运修正为 0 或 luck_affects=false 时，仅显示 `成功率 X%（基础）`。

### 3.3 与失败分级的联动

| 场景 | 行为 | 依据 |
|---|---|---|
| 幸运 + 率钳制到 100 | 必然成功 → 无失败分级 | L87 / 2c3a §2.4 |
| 幸运使率钳制到 0 | 必然失败 → **仍按 §1 分级**（高段掉级、低段不变） | L53-54 + 钳制 |
| 幸运只改成功率，不改失败结果档位 | 掉级/不变/碎完全由当前等级与配置决定 | L53-54 / L61 |
| luck_affects=false | 修正恒 0，显示无幸运段 | L89 / L90 |

---

## 4. 转移机制（内容包选项，默认关）

### 4.1 默认关闭语义（我们不开）

> 【强化定稿】L127：`转移机制：内容包可配（transfer_allowed: false 默认关）`
> 【强化定稿】L160：`"transfer_allowed": false     // 强化转移（我们关）`
> 【强化定稿】L129-130：`我们自己的数据包：不开放（用户拍板"我们不可以"）→ 装备淘汰 = 重新强化（强化石消耗循环 = 经济出口）`
> 【强化定稿】L229：`换装浪费（无转移） → 我们故意设计（强化石循环=经济出口，用户拍板）`

- transfer_allowed=false：**无任何转移指令/入口/UI 注册**（2c3a TC-24 同口径）。
- 装备淘汰 → 重新强化；强化石消耗循环 = 经济出口（L130 / L229）。
- 转移石等配套物品 ID 在关闭态不校验、不加载（【工程补白】：仅开启时纳入校验，避免关闭态误报）。

### 4.2 开启后规则（内容包启用时的完整语义）

> 【强化定稿】L128：`开启后：强化等级可转移到同部位新装备（消耗转移石/金币，可配）`

| 规则 | 语义 | 出处/依据 |
|---|---|---|
| 部位校验 | 目标装备必须与源装备**同部位**（如 剑→剑 可，剑→盾 拒绝） | L128（"同部位"） |
| 消耗 | 转移石（可配物品，数量可配）+ 金币（可配） | L128（"消耗转移石/金币，可配"） |
| 目标状态 | 目标必须为 +0（未强化）【工程补白】 | 未定义 → 建议拦截，防高等级覆盖歧义 |
| 源装备 | 转移后源装备归 +0，数值随等级一并卸载【工程补白】 | 未定义 → 等级不复制，天然防刷 |
| 目标品质/白值 | 不校验（只要求同部位 + 目标 +0）【工程补白】 | 未定义 → 不引入额外门槛 |
| 数值挂载 | 转移后目标装备按 2c3a §3.4 values 重新计算累计数值 | L116-119 精神延伸 |
| 失败路径 | 转移无概率判定（确定性操作，非强化），不参与成功/失败随机 | L44 原子精神延伸 |

### 4.3 转移字段 schema（【工程补白】，定稿仅留开关位）

定稿 enhance.json 未定义转移消耗字段（仅 L160 开关与 L128"可配"字样），以下为开启态最小字段建议：

```json
{
  "settings": { "transfer_allowed": true },
  "transfer": {
    "stone_item": "item_transfer_stone",   // 转移石物品 ID（items.json 必存在）
    "stone_cost": 1,                        // 转移石消耗数（可配）
    "coin_cost": 500,                       // 金币消耗（可配）
    "require_target_zero": true             // 目标必须 +0（建议恒 true）
  }
}
```

### 4.4 校验一致性

> 【强化定稿】L131：`校验器：转移开关 + 转移消耗配置一致性`
> 【强化定稿】L202：`转移/等级门槛开关与配置一致性`（校验器 5）

| 校验项 | 规则 | 拦截级别 |
|---|---|---|
| 开关+配置一致性 | transfer_allowed=true 时：必须配置 transfer 块（石 ID + 费用），否则拒绝加载（V5） | 硬拦截 |
| 石 ID 存在性 | transfer.stone_item ∈ items.json | 硬拦截（L201 精神类推） |
| 费用合法 | stone_cost/coin_cost 为 0 或正整数 | 硬拦截（L128 可配） |
| 关闭态 | transfer_allowed=false 时 transfer 块可缺省，不校验不加载 | — |

### 4.5 指令（【工程补白】，默认关无入口）

- 建议指令：`/强化转移 <源装备> <目标装备>`（定稿指令清单 L214-216 未列转移指令，名称属实现建议）。
- 仅 transfer_allowed=true 时注册；关闭态输入该指令 → "未开放（本服未启用强化转移）"。

---

## 5. 到达上限行为（提示零消耗）

> 【强化定稿】L115：`到达上限：/强化 提示"已达强化上限"`

- 当前等级 = max_by_rarity[品质]（普通 +5 / 精良 +8 / 史诗 +10 / 传说 +12，L113-114）时：
  - 任意 `/强化`（含 `/强化保护`）：直接回执「已达强化上限」；
  - **不扣强化石、不扣金币、不扣保护石、不掷随机**（零消耗零随机，L115 + 2c3a §3.2 同口径）。
- `/强化信息`（L216）照常展示：当前 +N / 成功率 / 上限（TC-23）。

---

## 6. 字段级 schema（enhance.json 相关切片）

> 配置落点：enhance.json（定稿 §九，L147-190）；校验器 7 条（L198-204）。本文仅列失败/保护/幸运/转移相关字段；消耗/曲线/上限见 2c3a §5。

**我们数据包（默认关闭态）JSON**：

```json
{
  "settings": {
    "fail_tier_split": 3,      // L155
    "shatter_mode": false,     // L156
    "shatter_chance": 0,       // L157
    "shatter_max": 5,          // L158
    "luck_affects": true,      // L159
    "transfer_allowed": false  // L160
  },
  "protect_stone": "item_protect_stone"  // L188
}
```

**字段表**：

| 序号 | 字段 | 默认值 | 类型 | 语义 | 引用 | 备注 |
|---|---|---|---|---|---|---|
| F-1 | `settings.fail_tier_split` | 3 | int | 当前等级 ≤ split 失败不变，> split 掉级 | L53-55 / L155 | 可配；配 0 时 +1 失败即掉级，等级下限钳 0（补白） |
| F-2 | `settings.shatter_mode` | false | bool | 碎装备开关（内容包选项） | L60 / L156 | 我们关；V3 软警告关联（L200） |
| F-3 | `settings.shatter_chance` | 0 | int(%) | 碎率；仅开启时生效 | L61 / L157 | 0 时无需开启（L200） |
| F-4 | `settings.shatter_max` | 5 | int(%) | 碎率参考上限；超出仅警告 | L63 / L158 | 0=不限 |
| F-5 | `settings.luck_affects` | true | bool | 幸运修正开关 | L89 / L159 | 关闭时修正恒 0 |
| F-6 | `settings.transfer_allowed` | false | bool | 转移开关（我们关） | L127 / L160 | V5 一致性（L202） |
| F-7 | `protect_stone` | item_protect_stone | string | 保护石物品 ID，可空 | L68 / L188 | 空 → /强化保护 未开放（补白） |
| F-8* | `transfer.stone_item` | — | string | 转移石物品 ID（开态必配） | L128（可配） | 【工程补白】V5 硬拦截 |
| F-9* | `transfer.stone_cost` / `coin_cost` | 1 / 500 | int | 转移消耗 | L128（可配） | 【工程补白】非负整数 |
| F-10* | `transfer.require_target_zero` | true | bool | 目标 +0 要求 | 未定义 | 【工程补白】建议恒 true |

\* F-8~F-10 为【工程补白】字段（定稿仅 L160 开关 + L128"可配"字样，无具体 schema）；标记 * 便于审查者与定稿引用区分。

---

## 7. 联动与判定链

**单次 /强化（含 /强化保护）结算判定链**（原子消息内完成，L44）：

```
1 达顶检查     当前等级 == 上限？ → 回执「已达强化上限」，零消耗零随机（§5 / L115）
2 材料检查     石、金币（+保护石走 /强化保护 时）是否足够？ → 不足：拒绝，零消耗零随机（2c3a §1.5）
3 成功判定     幸运修正结算（§3.1）→ rand < final_rate？
   ├─ 成功 → 扣材料，等级 +1，回执数值变化（L37 / L45）
   └─ 失败 → 进入失败分级（§1）
4 失败分级     当前等级 ≤ split？ → 不变（L53）
              否则（高段）→ 先掷碎（仅 shatter_mode=true 且 chance>0，L61）
                 ├─ 碎 → 装备碎（内容包归宿，默认不碎）
                 └─ 未碎 → 有保护石（/强化保护 分支）→ 扣 1 颗，等级不变（L68）
                             无保护石 → 等级 -1（L42 / L54）
5 回执         按 §1.4 文案提示惩罚类型（L45）
```

**正交关系**：幸运只改第 3 步概率，不改变第 4 步档位（§3.3）；碎与掉级互斥（碎则不掉级，判序 §1.3）；保护石只作用于"掉级"结果，"不变"与"碎"均不消耗保护石（§2.1）。

---

## 8. 验收测试用例（编号 TC-x · 24 条）

> 均为脚本可验证断言；输入 = enhance.json 配置 + 玩家/装备状态，输出 = 强化结算结果与回执文本。

### 8.1 失败分级（TC-01 ~ TC-07）

| 编号 | 用例 | 期望 |
|---|---|---|
| TC-01 | 低段失败不变（split=3） | 当前 +0/+1/+2（目标 +1~+3）失败 → 等级不变；边界：当前 +3（目标 +4）失败 → **仍不变**（当前 ≤ split），回执含「强化失败，装备保持不变」（L38 / L53） |
| TC-02 | 高段失败掉级 | 当前 +3（目标 +4）失败 → **维持 +3**（当前 +3 ≤ split，低段不变）；当前 +4（目标 +5）失败 → 掉到 +3（当前−1）；参照 L42：当前 +5（目标 +6）失败 → 掉到 +4 |
| TC-03 | 中/高区间同规则 | 当前 +5（目标 +6）失败与当前 +8（目标 +9）失败均掉 1 级（当前 > split 即高段，验证"中[4-6]高[7+]同属高段"，§1.1 裁定） |
| TC-04 | 掉级下限护栏 | 内容包配 split=0：+1 失败 → 等级 +0，不为负（§1.2 补白） |
| TC-05 | 失败材料不退还 | 高段失败 → 已扣石/金币不返还（§1.2） |
| TC-06 | 碎装备默认关 | shatter_mode=false：任意次数失败永不碎（L60 / L156） |
| TC-07 | 碎装备开启判序 + 警告 | shatter_mode=true、chance=8>5：加载软警告「碎率高挫败感强，确认？」不拦截；高段失败先掷碎，未碎再掉级；碎率 0 时无需开启（L61-63 / L200） |

### 8.2 保护石（TC-08 ~ TC-11）

| 编号 | 用例 | 期望 |
|---|---|---|
| TC-08 | 高段失败免掉级 | /强化保护 当前 +4（目标 +5）失败 → 扣 1 颗保护石，等级不变（L68）；当前 +3（目标 +4）为低段无惩罚，不消耗（§2.1 裁定） |
| TC-09 | 成功不消耗 | /强化保护 成功 → 不扣保护石（L68 反推） |
| TC-10 | 低段失败不消耗 | /强化保护 当前 +2（目标 +3）失败 → 不扣保护石、等级不变；边界：当前 +3（目标 +4）失败 → 低段不变、不消耗（§2.1 裁定） |
| TC-11 | 不足拒绝 + 空配置降级 | 保护石不足 → 拒绝本次强化、零消耗零随机；protect_stone 空 → /强化保护 回执「未开放」（§2.4） |

### 8.3 幸运修正（TC-12 ~ TC-15）

| 编号 | 用例 | 期望 |
|---|---|---|
| TC-12 | √差公式数值 | 我幸 36/敌 0：+6.0pp；我幸 4/敌幸 16（√2−√4=−2）：−2.0pp（L87） |
| TC-13 | 开关关闭 + 钳制 | luck_affects=false → 修正恒 0；基础 10%−15pp → clamp 0；基础 95%+12pp → clamp 100（L87-89） |
| TC-14 | 显示拆分 | 回执「成功率 60%（基础 55% + 幸运 5%）」；负修正「基础 50% - 幸运 3%」（L90） |
| TC-15 | 钳 0 必然失败仍分级 | 率钳制 0 → 必然失败 → 高段仍掉级、低段仍不变（§3.3 联动） |

### 8.4 转移（TC-16 ~ TC-20）

| 编号 | 用例 | 期望 |
|---|---|---|
| TC-16 | 转移默认关 | transfer_allowed=false → 无入口/无指令；输入 /强化转移 → 「未开放」（L127 / L160） |
| TC-17 | 同部位校验（开） | 剑(+5) → 剑 可；剑(+5) → 盾 拒绝（L128「同部位」） |
| TC-18 | 目标 +0 与源归零（开） | 目标已 +2 → 拒绝；成功转移后目标 +5、源回 +0（F-10 / §4.2 补白） |
| TC-19 | 转移消耗（开） | 成功转移 → 扣转移石 1 + 金币 500；目标 +5 数值按 values 重新累计（L128） |
| TC-20 | 配置一致性校验 | transfer_allowed=true 但缺 transfer.stone_item → 拒绝加载（V5，L131 / L202） |

### 8.5 达顶与系统级（TC-21 ~ TC-24）

| 编号 | 用例 | 期望 |
|---|---|---|
| TC-21 | 达顶零消耗 | 普通 +5 上再 /强化 → 「已达强化上限」，不扣石/金币/随机（L115） |
| TC-22 | 达顶后 /强化保护 | 已满装备走 /强化保护 → 同样零消耗零扣石（L115 精神延伸） |
| TC-23 | /强化信息 | 展示 当前 +N / 成功率（含幸运拆分）/ 上限 / 本次消耗（L216） |
| TC-24 | 原子性（失败路径） | 失败/保护石/达顶各路径均在单条消息内结算回执，无跨消息会话（L44） |

---

## 9. 行号回查表（【强化定稿】）

| 本文章节 | 引用行号 |
|---|---|
| §0 核心判据 | L6 / L15-16 |
| §1 失败分级 | L38 / L42 / L45 / L53-55 / L60-63 / L155-158 / L179-181 / L200 |
| §2 保护石 | L44 / L68-70 / L188 / L215 |
| §3 幸运修正 | L36 / L41 / L87-90 / L159 |
| §4 转移 | L127-131 / L160 / L201-202 / L214-216 / L229 |
| §5 达顶 | L113-115 |
| §6 字段 schema | L147-190 / L198-204 |
| §7 判定链 | L37 / L42 / L44-45 / L53-54 / L61 / L68 / L115 |
| §8 用例 | L38 / L42 / L53-54 / L60-63 / L68 / L87-90 / L115 / L127-131 / L160 / L200 / L202 / L216 |

---

## 附录 A：不实现、仅记录（内容包开启时才生效的延伸项）

| 项 | 记录内容 | 定稿依据 | 本包处置 |
|---|---|---|---|
| 碎装备细则 | 碎率掷判、碎后归宿（内容包自定）、碎率软警告 | L60-63 / L156-158 | 不实现（L62 我们不开） |
| 转移开启语义 | 同部位/目标+0/源归0/转移石金币消耗/校验一致性 | L127-131 / L202 | 不实现（L129 我们不开） |
| 对方幸运 | PvE 对手幸运恒 0；PvP 对手值来自对手 stats | L87-88 | 记录（PvE 恒 0 已固化） |
| 等级门槛 level_gated | 开启后 +N 需装备等级 ≥ 门槛 | L141-142 / L161 | 不实现（默认关，2c3a §3.3 同款） |

## 附录 B：工程补白清单（与定稿引用严格分离）

| 编号 | 补白内容 | 依据缺口 |
|---|---|---|
| P-1 | 掉级等级下限钳 0（split=0 时 +1 失败掉到 +0 不再递减） | 定稿未定义负数边界 |
| P-2 | 保护石低段失败不消耗 | L68 只定义"失败时消耗 1 颗免掉级"，低段无掉级 → 不触发 |
| P-3 | 保护石不足 → 拒绝本次强化、零消耗零随机 | 定稿未定义不足行为（L44 原子精神延伸） |
| P-4 | protect_stone 为空 / ID 缺失 → /强化保护 软降级「未开放」 | L188"可空" |
| P-5 | luck 属性缺失按 0 + 黄提示；PvE 对手幸运恒 0 固化 | L87-88 接口未定义读取失败路径（2c3a S3 同款） |
| P-6 | 转移消耗字段 schema（stone_item / stone_cost / coin_cost / require_target_zero） | L128 仅"可配"字样、无字段；L131 要求一致性校验 |
| P-7 | 转移目标须 +0、源归 +0、不校验品质/白值、转移无概率判定、指令名 /强化转移 | L128 未定义细节，均取不复制、防刷、确定性口径 |
| P-8 | 转移关闭态不校验 transfer 块 | 关闭态无入口，避免误报 |

---

*细化依据：《强化系统设计定稿》v1.0（235 行）；与《细化_2c3a_强化数值曲线》（成功率/消耗/上限、S1-S4）、《细化_2c2a_锻造派生树schema》（品质/部位来源）、《细化_2c2b_锻造流程契约》（同谱指令风格）对接；转移与碎装备保持关闭（L127 / L160 / L62），本包如实记录用户拍板"转移我们不用"（L6 / L129）。*