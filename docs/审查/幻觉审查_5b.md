# 幻觉审查报告：《细化_5b_GM指令契约.md》 vs 《RPG回合制框架设计文档.md》+《指令分隔符统一规范.md》

> 审查方法：通读细化全文（346 行）→ 抽取全部行号引用与规则声明（120+ 处）→ 回定稿逐一核对（①行号引对 ②内容一致 ③补白显式标注 ④数值有依据）。
> 核验对象：《RPG回合制框架设计文档.md》（v2.19，共 2794 行，2026-08-16）、《指令分隔符统一规范.md》（v1.1，共 246 行，2026-08-15）、《实现层规划文档.md》（共 676 行）、《细化_0_仲裁决议汇总.md》（R-02）。
> 审查日期：2026-08-18

---

## 结论：P0 = 0 · P1 = 3 · P2 = 4 · 评分 B+

**一句话**：14 条 GM 指令全部真实存在于定稿（重载/备份/恢复/存档导出/调试/日志/封禁/解封/封禁列表/编辑）或显式标注【细化指令入口】（测试/广播/玩家查询），静默语义/封禁闭环/审计留痕/快捷禁绑等核心声明与定稿逐行对上，且 R-02 口径（`sys_log.default_show=20` / `max_entries=50`）已正确落地、内部一致；唯 G14 依据【框架】L3185 超文档范围（文档实为 2794 行）、4 处【规划】A3/L3203/L2477 引用指向不存在的小节/行号、G4 与 TC-14 两处将 CSV 防公式注入错引至 L1156（实为 L1155），另有 4 处标注口径小瑕，评为 **B+**。

---

## P0（严重幻觉：内容不存在/规则与定稿直接冲突/行号指向无关内容）

**无。**
14 条 GM 指令无一虚构：重载（L256/L1300）、备份（L256/L1485）、恢复（L256/L1629）、存档导出（L256/L1129）、调试（L256）、日志（L1302/L2772/L2787）、封禁（L1135/L1301）、解封（L1136）、封禁列表（L1136）、编辑（L1299）均在定稿原文；测试（G6）、广播（G7）、玩家查询（G9）三个入口均以【细化指令入口】显式标注，未冒充定稿。三级权限、静默语义、封禁提示差异、权限存储不进玩家存档等核心主张均与 L254-261/L1135-1140/L255/L1310/L1355 逐行一致。

---

## P1（行号引错或引用指向不存在的内容/章节，但语义由其他行/文档承载，不构成内容虚构）

### P1-1 G14 `/设置` 依据【框架】L3185——超文档范围，该行不存在（框架文档实际仅 2794 行）
- **出错位置**：2.1 总表 G14 行（L102）「【规范】L97、L96；**【框架】L3185**」；2.2 G14 契约（L170-173）亦承载此依据。
- **核对**：当前定稿《RPG回合制框架设计文档.md》v2.19 共 **2794 行**，L3185 不存在（文档在 L2794 结束）。`/设置` 的指令模式/群级配置切换语义实际由【规范】6.1 L92/L94/L96-97（三种前缀模式 +「机主 /设置 切换」）完整承载——引错来源，但**内容不虚构，语义在【规范】有据**。G14 是 14 条指令中唯一直接依据落空的定稿行号。计 1 条 P1。

### P1-2 4 处【规划】A3 / L3203 / L2477 / L3201-3204 引用全部指向不存在的章节与行号
- **出错位置**：① 文首头注（L8）「【规划】=《实现层规划文档.md》A3 commands 壳层」；② 决策链 ⑥（L25）「message_id 幂等键防重复结算；per-player 串行队列…【规划 A3 L3203】」；③ G1 执行（L111）「对齐【规划】L2477 验收口径」；④ 跨指令约束（L177-178）「【规划】A3 L3203」×2；⑤ 4.3 幂等行（L263）「【规划】A3 事务口径」；⑥ 文末设计依据（L347）「【规划】A3 commands 壳层（L3201-3204）」。
- **核对**：存档中《实现层规划文档.md》共 **676 行**，结构为「一~七 + 附」，**全篇无「A3」小节、无「commands 壳层」表述**（目录骨架 1.3 L59-60 为 `commands/` 目录提及，非 A3 壳层），L2477 / L3201-3204 / L3203 均不存在。所声称内容的实质承载：message_id 幂等键 + per-player 串行队列在规划 L244/L480/L620 及框架 L1606/L1609（「per-player 串行队列（同一玩家指令按到达顺序处理）」+「指令处理以 message_id 做幂等键（防重复结算）」）完整存在；「坏配置拒绝加载并列出错误项」验收口径在规划 M2 validator 任务（L460-497 区间）有对应语义。即**内容非虚构，全部是章节/行号定位失效**。合并计 1 条 P1（列全 6 处位置）。

### P1-3 G4 与 TC-14 两处将「CSV 双向防公式注入」错引至 L1156（实际在 L1155）
- **出错位置**：2.1 总表 G4 行（L92）「【框架】L256、L1129、**L1156**」；2.2 G4 契约（L124）「`=` `+` `-` `@` 前缀单元格转义或拒绝（【框架】**L1156** 双向防公式注入）」；TC-14（L308）「【框架】L1129、**L1156**、L1154」。
- **核对**：定稿 L1154「SQLite 存档/备份/.bak 目录权限 600/700」；**L1155**「CSV 双向防公式注入（= + - @ 前缀单元格转义或拒绝）」；**L1156**「玩家输入限制：角色名 ≤20 字，过滤控制字符与超长 emoji」。所引 L1156 实为「玩家输入限制」，与「防公式注入」无关——行号错位一位，引到了无关内容（内容本体在相邻 L1155，识别无歧义）。两处同错，计 1 条 P1。

---

## P2（补白/数值/标注口径不完整或版本标注滞后，不构成实质失实）

### P2-1 1.1 权限表 P0 行「权限授予/撤销」无定稿明文且未标【细化】
- **位置**：§1.1 表（L39）「P0 机主（owner）= 全部指令 + **权限授予/撤销** + 数据库最高权限 | 依据【框架】L258」。
- **核对**：L258 原文仅「机主（数据库最高权限）= 全部」；「授予/撤销」权限的操作语义是 L254「机主在编辑器/数据库设置」的推演（合理，且 1.1.1 L49 已作为【细化】裁决覆盖 per-command 授予），但 P0 行表格内未标注【细化】，读者易误认为 L258 原文。计 P2。

### P2-2 1.1.1「默认授予集 = 全部指令表标注 GM 的指令」归纳口径与括号清单不符
- **位置**：§1.1.1（L49）「默认授予集 = 全部指令表标注 GM 的指令（重载/备份/恢复/日志/编辑/封禁/解封/封禁列表）」。
- **核对**：指令表（L1299-1302）标注 (GM) 的仅重载（L1300）、封禁（L1301）、日志（L1302）、编辑（L1299 机主/GM）4 条；备份/恢复的 GM 标注在 L259（权限分级）而非指令表；封禁列表 (GM) 在 L1136；解封在 L1136 无 (GM) 标注（与封禁列表并列）。即「全部指令表标注 GM 的指令」这一归纳不产出括号内的备份/恢复/解封/封禁列表。裁决本身显式标注【细化】且由 L259+L1136 承载，结论成立，仅归纳口径表述不严谨。计 P2。

### P2-3 白名单层「默认清单 L117-121 无 GM 指令」范围标注微偏
- **位置**：§3.2 白名单层行（L199）「不入 command_shortcut_whitelist（默认清单 **L117-121** 无 GM 指令）」。
- **核对**：L117-119 为白名单默认清单（攻击/防御/…/地图），确无 GM 指令；但 L120 原文为「⚠️ 保留前缀推荐：**开始 / 重载 / 封禁 / 编辑**（创建/GM 类低频指令）」——范围内 L120 实含 GM 指令字样（以「保留前缀推荐」形式出现），「L117-121 无 GM 指令」表述不精确；规则本体（L121「GM 指令永不快捷」、L128「GM 指令…永不快捷（强制 / 前缀）」）完全支持结论。计 P2。

### P2-4 文首版本标注滞后：标「框架（v2.8）」而当前定稿为 v2.19
- **位置**：L3「QQ 回合制 JRPG 框架（v2.8）GM 指令与权限系统的实现层细化契约」。
- **核对**：当前定稿为 v2.19（2026-08-16）；L5 头注「v2.7+，2026-08」范围表述可接受，L3 的「v2.8」系早期版本残留。全部行号引用与 v2.19 文档核对一致（内容未脱节），仅版本标注滞后。计 P2。

---

## 核对通过项（抽样清单，全部 ✓）

- **任务复核项 R-02 确认（按 R-02 改过 GM 日志配置键）**：G8（L146）「默认 20（`sys_log.default_show=20` 可配）、保留窗口上限 50（`sys_log.max_entries=50`，细化_0 R-02）」与《细化_0_仲裁决议汇总.md》R-02 定案（L48-49：`sys_log.default_show=20` / `sys_log.page_size=20` / `sys_log.max_entries=50`）完全一致；细化_0 L50 记载「细化_5b（G8 L96 补配置键与『上限=保留窗口 50』口径）✅」——5b 已落地，与 3f 口径（默认 20/窗口 50）统一 ✅。
- **三级权限表（§1.1，L252-261 全段精确）**：P0=全部（L258）、P1=重载/备份/恢复（L259）、P2=仅游玩指令（L260）；「不自动授予群管理」（L254）、静默语义三连引 L255/L1310/L1355 逐字对上；判定优先级/权限缓存/存档不含权限字段均显式标【细化】。
- **14 条指令依据逐条核对**：G1（L256/L1300/L1039 单数据包机制/L42 保存即生效/规范 L58 命名铁律）✓；G2（L256/L1485-1487 一键备份 zip/L1500/L1615 VACUUM INTO/L1166 默认 30 天）✓；G3（L256/L1485/L1629 校验→自动备份→原子替换→回滚）✓；G4（L256/L1129 脱敏/L1154 权限 600——除 L1156 错号外）✓；G5（L256 原文含「/调试 等」）✓；G8（L1302/L2772/L2787）✓；G9（L1129 导出字段集/L1130 封禁名单管理）✓；G10（L1135 默认永久/L1301/L1140 留痕三要素）✓；G11（L1136）✓；G12（L1136/L1334-1346 7.4 分页 5 条/页）/L1346 超长截断 ✓；G13（L1299 机主/GM 原文一致/L1124-1131 运营页）✓；G14（规范 L96/L97/L160——除 L3185 错号外）✓。
- **快键禁绑全链路（§3.2，规范逐行核对）**：绑定层拒绝文案 L160-161 与规范原文逐字一致（「『重载』是 GM 指令，不可绑定为快捷」（防权限绕过））；白名单 L121/L128；解析顺序 L149-152（快捷表→别名→白名单→忽略）；执行层 L169-171（完整指令管线 + 权限二次检查）；prefix_only 下 `/1` L177；别名精神 L189-191 正确标注为【细化】引用。
- **封禁提示 vs 静默（§3.3）**：无权限→静默 L255/L1310/L1355；被封玩家→人话提示 L1137-1138；区分口径 L1139；被封 GM 仍可发 GM 指令标【细化】——口径与定稿无冲突。
- **审计（§4）**：E1-E6 事件类依据 L256/L1129/L1125-1126/L1140/L1145（踢群）/L1168（磁盘预警）全部实存；表结构/字段/轮转/不可删/幂等均显式标【细化】；轮转依据 L1164、权限 600/700 依据 L1154、/日志 展示源 L2787 全部正确。
- **验收用例（TC-01~34，34 条计数自洽 8+12+6+5+3）**：26 条带定稿依据的用例逐一回查——TC-01/02/03/04/05/06（L255/L258/L259/L254）、TC-07（L1137-1139）、TC-09（L1300/L42）、TC-10（L1351-1353 三要素）、TC-11（L1351）、TC-12（L1485-1487）、TC-13（L1629）、TC-15（L1125-1126/L1128）、TC-17（L1302/L2787）、TC-21/22（L2772/L2787）、TC-23（规范 L160-161）、TC-24（规范 L169-171）、TC-25（规范 L177/L169）、TC-27（L1140 四要素）、TC-30（L1164）、TC-32/33（L1311+规范 L129）、TC-34（L1154）——零虚构行号；其余 8 条（TC-08/16/18/19/20/26/28/29/31）全部显式标【细化】并给出锚点（L1129 字段集、L256、规范 6.7 精神、L1140 精神）。
- **补白标注纪律（口径声明 L10 践行）**：测试/广播/玩家查询三入口标【细化指令入口】；参数语法延伸（≤200 字、定时=、群=、条数=N、分页）全部标【细化】；权限裁决（默认授予集/机主专属）标【细化】裁决（1.1.1）；审计表结构/字段/生命周期标【细化】——无一处将推断冒充定稿原文。
- **设计依据汇总（L347）**：框架 9 段（3.9 L252-261 / 2.1 L51 / 7.1 L1299-1302 / 7.5 L1349-1357 / 封禁闭环 L1133-1141 / 运营页 L1122-1131 / 安全补强 L1152-1158 / 资源治理 L1160-1170 / 备份恢复 L1485-1629 / L.2/L.4 L2772/L2787）与规范 7 段（6.1 L90-97 / 6.4-6.5 L117-129 / 6.6 L132-187 / 7 兼容 L235-242）全部精确——仅【规划】A3（L3201-3204）段失效（见 P1-2）。