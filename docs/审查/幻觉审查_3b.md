# 幻觉审查报告：《细化_3b_玩家属性三层.md》

- 审查对象：`/root/QBot-TurnTellerRPG/docs/细化/细化_3b_玩家属性三层.md`（389 行）
- 定稿基准：`/root/docs_archive/RPG框架项目/玩家属性方案定稿.md`（244 行，v1.2，2026-08-15）
- 审查方法：通读细化全文 → 抽取全部行号引用（约 70 处）→ 回定稿逐行核对
  - ① 行号是否引对 ② 引用内容与定稿原文是否一致 ③ 补白是否显式标注（ADR） ④ 数值（公式/基数/成长/矩阵/K/cap）是否有依据
- 审查日期：2026-08-18

---

## 总体结论

细化文档对定稿的**忠实度很高**：全部 6 段定稿原文引用（§1 三层结构、§2 计算模型、§2.2 药剂、§4 stats.json 九属性、§二会心、§三命中/格挡）**逐字核对无误**；九属性 base/growth/role/mh_map 全部数值与定稿 L198-206 完全一致；会心矩阵（Lv0-3）、K 值（100/1）、cap 95%、判定顺序 L100 等全部有依据。**未发现虚构规则或虚构数值（P0=0）**；ADRs 全部为显式标注的收敛性决策，符合文档自设的变更纪律。

主要问题集中在：① TC-06 的"L52 示例对齐 / L117-118 口径"两处行号引用指向无关内容（P1）；② §5.2 命中率边界表 4 行+注释的行号系统性错位（P1）；③ 若干未标注的补白与内部口径不一致（P2）。

---

## P0 清单（内容虚构 / 定稿不存在此规则数值）—— 0 条

无。核心规则、公式、数值、JSON 键值均能在定稿中找到原文或直接依据。

---

## P1 清单（关键行号引用错误 / 引用内容与定稿实质不符）—— 2 条

### P1-1｜TC-06 覆盖行引用"L52 示例对齐"与"L117-118 口径"——两处均指向无关内容（虚构引用）

- 细化 L335：`TC-06 ... lv=50，str 白值=15+1.5×49=88.5（L52 示例对齐）... | L168 / L174 / L117-118 口径`
- 核对结果：
  - 定稿 **L52** = `r ≤ P → 高级会心：2.2 + 0.05×超会心等级`——会心倍率判定行，**与"白值=15+1.5×49 示例"完全无关**，定稿中不存在"lv=50 str 白值 88.5"这一示例。"L52 示例对齐"为无中生有的引用。
  - 定稿 **L117** = 代码围栏 ```，**L118** = `元素伤害 = floor(装备元素攻击 × ...)`——元素伤害公式，"L117-118 口径"与白值/换职业口径无关。
  - 主张本身（白值 = base + growth×(lv−1)，换职业不重算）可在 L136-137 / L168 / L174 成立，数学 15+1.5×49=88.5 也正确，**但标注的行号支撑不了主张**，逐条回查必然落空。
- 处置建议：删除"（L52 示例对齐）"，覆盖行改为 `L136-137 / L168 / L174`。

### P1-2｜§5.2 命中率边界表 4 行行号系统性错位，且指向的行内容不符（含 L287 注释）

- 细化 L281-285 表格（覆盖行）与定稿实际行号对比如下：

| 细化声明 | 细化标注行号 | 定稿实际内容 | 应为 |
|---|---|---|---|
| K 可配默认 1 | L82 | `K 可配（默认 1）；对方敏捷 0 → 100% 命中` | L82 ✅ 正确 |
| 对方敏捷 = 0 → 100% | **L83** | L83 = `双方都 0 → 100% 命中（0/0 兜底）` | **L82** ❌ |
| 双方都 0 → 100% | **L84** | L84 = `怪物敏捷缺省 → 按等级换算兜底` | **L83** ❌ |
| 怪物敏捷缺省按等级兜底 | **L85** | L85 = `战斗提示显示当前命中率（"命中率 72%"）` | **L84** ❌ |
| 战斗提示显示命中率 | **L86** | L86 = 代码围栏 ``` | **L85** ❌ |

- 细化 L287 注释：`L83「对方敏捷 0 → 100%」先于 clamp 判定`——同样错位，应为 L82。
- 影响：5 处错位引用导致逐条回查时看到的都是**相邻行的其他规则**（如查 L83 会看到"双方都 0"而非"对方敏捷 0"），破坏文档"严格引用行号、可逐条回查"的承诺。内容本身全部真实存在于 L81-85（TC-14 的覆盖行"L81-85"范围正确），但表格逐行标注错误。
- 处置建议：表格逐行改为 L82/L82（对方敏捷0）/L83（双方0）/L84（怪物缺省）/L85（提示），注释改为 L82。

---

## P2 清单（轻微行号偏移 / 补白未显式标注 / 内部口径不一）—— 8 条

### P2-1｜白值/加成层行号整体偏移 1 行（未影响内容真实性）
- 细化 L18 术语表「白值层」引 **L136**（实为 L137）；L19「加成层」引 **L137-140**（应为 L138-140）；L48/57「对应 L136 / L137-140」；L63 ① 行引 L136；L64 ② flat 行引 L137-139（应为 L138-139）；L336 TC-07 覆盖行引 L137（应为 L138）。
- 定稿 L136 为"玩家属性 = 三层："块头行，L137 才是"① 白值"。内容全部在位，仅行号偏移。建议统一核对修正。

### P2-2｜§1.2「同层同槽位（装备）后装备覆盖先装备」——无依据无 ADR 的补白
- 细化 L72：`替换规则：同类药剂不叠加，新覆盖旧（L159）；同层同槽位（装备）后装备覆盖先装备。`
- L159 只覆盖了同类药剂；装备槽位替换规则定稿全文未定义，该半句属于未标注的补白，且与 ADR-01~05 均不挂钩。建议显式标为补白或并入 ADR。

### P2-3｜§5.4 魔法减伤公式"最终精神 同构除算"——推断公式未标注
- 细化 L307：`魔法减伤 | 最终精神 同构除算 | 双轨防御侧 | L21 / L31`
- 定稿 L32 仅给出**体质**减伤公式 `体质/(体质+K)`；L31 只有"精神(spr) → 魔法减伤"的职责描述，无公式。"同构除算"是合理推断，但未标注为补白（ADR-03 只声明了"输入口径统一"，未声明公式形态）。建议标注或补 ADR。

### P2-4｜§4.2 字段级校验规则（空串红/负数黄等）与 role 必填——未标注的 schema 强加
- 细化 L200-208 表：`name 空串→红`、`base 负数→黄（运行期按 0）`、`role 必填=是（枚举含 resource）`。
- 定稿 L220/L222 仅提供"注册制（引用存在性校验）"和"校验器提示改名"，未定义逐字段红/黄语义；且定稿 JSON 中 **hp/mp 并无 role 字段**，"role 必填"与定稿 L198-199 数据冲突（需为 hp/mp 补 role:"resource"才能自洽）。整组校验语义为补白，未入 ADR。建议补 ADR 或改为 role 对 hp/mp 非必填。L204 的 base="lck 10" 值本身无误（√ 与 L206 一致）。

### P2-5｜§4.4 注释"growth×lv"与 §2.1 伪代码"growth×(lv−1)"内部不一致
- 细化 L227 注释：`// ① 白值（base+growth×lv+加点...）；L92/L94 伪代码为 `growth × (lv - 1)`；TC-06 也按 15+1.5×49（=lv−1）计算。
- 两处口径自相矛盾，正式公式中"lv−1"是多数约定，建议统一并注明取整/升级瞬间口径。

### P2-6｜TC-13 断言含草稿残留文字，断言不完整
- 细化 L352：`r=20（≤15？否，≤15 之外且 >15）→ 检查档位边界`——"≤15？否"为思考残留，且未给出 r=20 的期望倍率（按 L52-54 应为低级 1.3），断言不可直接执行。建议改写为明确的期望值。

### P2-7｜§3.2 条件规则"依赖图环 → 红色拦截"引用 ADR-05/L220 牵强
- 细化 L161：`依赖图环（A→B→A）→ 红色拦截拒绝加载（ADR-05 + L220 引用存在性校验）`
- L220 的注册制针对 stats.json 自定义键的存在性校验，并未授权对条件规则做依赖图环检测；L148 仅有"防循环"意图。将该语义扩展为红拦截缺少定稿锚点，且未单独开 ADR（ADR-05 理由未涉及条件规则）。建议补 1 条 ADR 或降级为提示。

### P2-8｜§1.1 ③ 行"超会心 Lv1-3 / crit_bonus 由效果系统在此层（临时层）挂载"过度简化
- 细化 L66：`超会心 Lv1-3 / crit_bonus 由效果系统在此层挂载`。
- 定稿 L43：`crit_bonus：装备/技能/效果提供的会心加成`、L188：`装备/效果系统：crit_bonus（会心加成）`——crit_bonus 来源**含装备**（装备属加成层），全部归入临时层与定稿表述不符；超会心由效果系统提供（L56/L188 ✅）则无问题。建议改为"crit_bonus 按来源分层（装备→加成层/效果→临时层），超会心由效果系统挂载"。

---

## 核对通过的主要引用（抽样证明）

| 细化声明 | 定稿核对 | 结果 |
|---|---|---|
| §1 三层结构引用 L133-142，引文逐字 | L136-141 逐字一致 | ✅ |
| §2 计算模型引用 L144-151，五步公式逐字 | L146-150 逐字一致 | ✅ |
| §2.2 药剂引用 L153-161（items.json/战斗外→加成层/战斗内→临时层/不叠加/3.14 冷却） | L155-160 逐字一致 | ✅ |
| 九属性 JSON 引用 L194-208，9 条键值 | L198-206 逐字一致（base/growth/role/mh_map/note 全对） | ✅ |
| 会心率 L41 / 倍率 L52-54 / 矩阵 L60-63 / 示例 L65 / 负会心 L66 / 幸运职责 L71 / L183-184 | 逐字一致 | ✅ |
| 格挡率 L90 / 减半特色 L92-94 / 判定串行 L95 / 快照 L218 | 逐字一致 | ✅ |
| 判定顺序 L100 | 逐字一致 | ✅ |
| 元素管线 L112（白值0→加成flat→×(1+临时%)） | 逐字一致 | ✅ |
| 元素伤害 L118-119（含 floor 独立取整） | 逐字一致 | ✅ |
| 命中率公式 L81 / K 默认 1 L82 / 双方 0 兜底 L83 | 一致（行号标注错位见 P1-2） | ✅* |
| 减伤 K=100 L32 / 会心 cap 95% L217 / 注册制 L220 / 管线写死 L221 / 映射迁移 L222 | 一致 | ✅ |
| 白值成长 L168-174（加点=白值层/换职业不重算/职业默认成长率数值） | 一致（行号偏移见 P2-1） | ✅* |
| 术语表 基础合计 L146/148 / 加成后 L147/149 / 最终 L150 / 临时 L141/157 | 一致 | ✅ |
| 逃跑成功率 L185（agi/(agi+敌agi)）、元素减伤 L126-127 | 一致 | ✅ |
| ADR-01~05 的缺口声明（L150 无临时flat / resource-百分比未定义 / 属性管线取整未定义 / 自定义属性注册） | 与定稿空白核对属实，且均显式标注 | ✅ |
| TC-01~TC-18 全部断言数值（含 floor 结果）复算 | 数学全部正确，与 L146-150 公式一致 | ✅ |

（*为行号偏移项，见 P2-1/P1-2）

---

## 评分：B

**一句话结论**：细化文档无虚构规则与数值、六段定稿引文与九属性数据逐字准确、TC 断言全部复算正确，属高可信细化；需优先修复 2 处 P1 行号错误（TC-06 虚构引用、§5.2 命中边界表系统性错位）并补齐 8 处未标注补白/偏移后即可用于实现。

---

## 统计

- **P0：0 条**
- **P1：2 条**（P1-1 TC-06 行号虚构引用；P1-2 §5.2 命中边界表 5 处行号错位）
- **P2：8 条**（行号偏移簇、装备槽规则补白、魔法减伤公式补白、字段校验/role 必填补白、growth×lv 内部矛盾、TC-13 断言残稿、依赖图拦截引用牵强、crit_bonus 分层简化）