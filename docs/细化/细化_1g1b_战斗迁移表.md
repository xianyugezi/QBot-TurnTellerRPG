# 细化 · 1g1b 战斗主循环状态机迁移表

> 依据定稿（下文简写，行号引用格式 `L{n}`）：
> - **战斗数值层** `战斗数值层设计定稿.md` v1.1.1（2026-08-16）——《§一 回合死亡判定顺序》L40–68 为本表时序唯一权威
> - **效果系统** `效果系统设计定稿.md` v1.1（2026-08-14）——《§3.4 伤害拦截链》L133–149 衔接扣血→死亡判定
> 配套文档：`细化_1g1a_战斗状态集.md`（8 态定义）· `细化_1g2_回合时序与拦截链.md` · `细化_1g3_快照续战与测试.md` · `细化_1c1b_连段迁移表.md`
> 作用域：1g1b = QQ 回合制 RPG **战斗主循环**的全状态迁移（预备/行动/结算/死亡判定/胜负/逃跑/快照中断）。

---

## 一、状态集总览（8 态）

状态变量（随战斗实例持有，快照随 3.18 battle snapshot 持久化，战斗数值层 L333）：

```
battle_state  = { phase, turn_no, flags, combatants, rng_seed, collector }
turn_flags    = { player_death_mark, monster_death_mark, mutual_kill_mark, flee_pending }
```

| 状态枚举 | 名称 | 语义 | 来源/引用 |
|---|---|---|---|
| S0 `PREP` | 战前准备 | 进入首个回合前的初始化 + 战后/恢复重入入口；确立敌方档位、配置源、随机种子、伤害构成收集器 | 战斗数值层 L7、L112–123、L132–137、L139–151、L311–333 |
| S1 `ACT` | 行动选择 | 当前行动权侧选定动作（玩家先手 L49 / 怪物后手反击 L54） | L49、L54–57、L164 |
| S2 `RES` | 结算中 | 把动作喂入定稿固定判定管线逐环逐段执行（L16 判定顺序） | L16、L21–38、L49–51、L314、L317–329 |
| S3 `DTH` | 死亡判定 | 死亡/灭团与胜负标记唯一判定点（回合开始即死 L46 + 每次扣血后 L50） | L45–50 |
| S4 `WIN` | 胜利 | 玩家胜利收束；⑧ 当轮末尾统一结算 | L48、L53、L64–65、L167、L332–352 |
| S5 `LOSE` | 失败 | 玩家失败收束（含互杀可配为玩家败） | L47、L59、L64、L236 |
| S6 `FLY` | 逃跑 | 玩家主动退出战斗（定稿未定义专属规则，标待补） | L7、L56–57、L66–67、L298 |
| S7 `SNP` | 快照中断 | 战斗持久化/碎片化续玩；只落回合边界 | L66–67、L298、L333–334 |

> **平局（DRAW）不是独立状态**：它是 ⑧ 收尾的第三出口（互杀 ⑦，L59–63），由 S4/S5 的结束事件表驱动，不占状态数。以下表内以「结束=平局」标注。

---

## 二、主迁移表（8 条，覆盖任务要求 ①–⑧）

每行字段：`# | 当前状态 | 触发事件 | 条件（守卫） | 动作 | 目标状态 | 定稿引用`。

| # | 当前状态 | 触发事件 | 条件（守卫） | 动作 | 目标状态 | 定稿引用 |
|---|---|---|---|---|---|---|
| **① 战前→行动** | S0 `PREP`（含快照恢复重入） | 战斗发起 / 读档续玩 / 木桩入口，初始化完成 | 进入主循环 ①（L45）；且 ② 回合开始即死判定通过——双方在回合开始结算后均存活（L46–48） | ① 回合开始 dot/持续伤害结算（L45，dot tick 语义连效果系统）→ ② 即死判定（L46）→ 存活则进入 ③ 先手行动（L49）；初始化项：双方 9 属性 base+成长（L112–123）、三档难度基线（L132–137）、formula.json 唯一配置源（L172–242）、随机种子与 per-action 收集器（L311–333） | S1 `ACT`（先手） | 战斗数值层 L7、L45–49、L112–123、L132–137、L172–242、L298、L311–333；效果系统 L285 |
| **② 行动→结算** | S1 `ACT` | 当前侧选定并提交本回合动作（玩家：技能/普攻/防御指令/派生链；怪物：AI 反击） | 资源校验通过——MP 按倍率足够（普攻0/小技5–10/大招15–25，L164）；技能非冷却/条件满足（派生链条件评估走技能派生定稿）；目标存活或存在剩余目标（L56，否则转辅助 A3 无目标兜底） | 进入固定判定管线逐环执行：命中→会心→格挡→双通道（物理/元素独立取整相加）→防御率→伤害拦截链→扣血（L16、L21–38）；连段/多段按段执行（L49–51），每段写伤害构成记录（L314、L317–329）；拦截链顺序取效果系统 §3.4（L36、L231–234） | S2 `RES` | 战斗数值层 L16、L21–38、L49、L56、L164、L231–234、L314；效果系统 L133–144 |
| **③ 结算→死亡判定** | S2 `RES` | 拦截链执行至 **⑦扣血 → ⑧死亡判定**（每次扣血后立即检查，非等整回合） | 每段伤害扣血完成后必触发一次检查（L50）；拦截链走到 ⑦→⑧ 环节（效果系统 L142–143） | 检查目标 HP≤0：是→转 S3 死亡判定，出胜负分支（T4/T5）；否→继续剩余段数（套内击杀后续段照常，L51–52）或转入 ⑤ 后手（L54–57） | S3 `DTH` →（未死）回 S2/转后手 | 战斗数值层 L36、L50–52；效果系统 L142–143 |
| **④ 玩家死→失败** | S3 `DTH` | 回合开始即死（②，L46）或行动结算中死亡（④，L50） | 玩家阵营 HP≤0（L47）；**或**互杀按 `mutual_kill_result=player_loss` 判玩家败（L59、L236） | 打「战斗失败标记」（L47，标记随 ⑧ 在当轮事件末尾统一结算 L64）；收尾：不发放奖励/掉落，快照清理走统一收尾（L64、L334）；高难碎片化续玩→读档/快照恢复再战（L298） | S5 `LOSE` | 战斗数值层 L46–47、L50、L59、L64、L236、L298、L334 |
| **⑤ 怪物死→胜利** | S3 `DTH` | 怪物 HP≤0（L48）或 BOSS/最后目标死亡（L53、L65） | 怪物阵营 ≤0；BOSS/最后目标死亡→`boss_end_immediate=true`（L239）**战斗立刻结束、不清算后续段数（不鞭尸）**（L53、L65） | 打「战斗胜利标记」（L48）；⑧ 统一结算一次：奖励/掉落（金币=怪物等级×基准、素材保底 L167）、dummy_log 木桩战报（L345–352）、伤害构成统计战后汇总按来源+占比（L332、L339）、快照清理（L64、L334）；普通怪在连段套内被击杀且非最后目标→后续段数照常执行（鞭尸浪费，套内稳定 L51），套完结后目标退出战场（L52） | S4 `WIN` | 战斗数值层 L48、L51–53、L64–65、L167、L239、L332–352 |
| **⑥ 逃跑→回地图** | S6 `FLY` | 玩家在回合边界发起逃跑 | **仅限回合边界**（回合开始前/回合结束 tick 后，L66–67），不打断回合内结算；定稿未定义成功率/回合数/代价/BOSS 禁逃（**待补**，归流程/怪物定稿，L7 转引） | 逃跑 = 手动中断 + 立即结算（语义即 ⑤ 后手无目标兜底的玩家侧版本 L56–57）；成功后清空战斗内资源（连段为战斗内资源 L59，对齐 1c1b T7 清零），退出战场回地图/战前入口（L298） | 退出战斗（回地图，⟂ 回 `PREP` 语义入口） | 战斗数值层 L7、L56–57、L59、L66–67、L298 |
| **⑦ 快照中断→还原** | S7 `SNP` | 战斗恢复（框架 3.17/3.18 battle snapshot 存在，L66–67、L333） | 中断快照**只落回合边界**（回合开始前/回合结束 tick 后），回合内不落快照（L66–67）→ 死亡结算天然先于快照写入，恢复后状态=最近回合边界，**无「恢复在死亡判定前/后」歧义**（L67） | 还原最近回合边界状态：双方 HP/MP、回合序号、dot/冷却/效果驻留（效果系统 §8.3 快照扩展：status_state/marks_state/resist_table/effect_triggers/effect_cooldowns，L425–434）、连段/资源轴（L147）、伤害构成统计（L333，持久化登记统一收尾 L334）；恢复后回 ① 回合开始（L45）继续 | S0 `PREP`（经 T1 进入主循环） | 战斗数值层 L45、L66–67、L147、L298、L333–334；效果系统 L425–434 |
| **⑧ 回合结束 tick→下一回合** | S2/S3（后手结算完毕） | ⑤ 后手行动判定完成（L54–57）→ 触发射 **⑥→⑦→⑧→⑨** 收尾链 | 未触发战斗结束：⑥ 执行且 ⑦ 互杀/⑧ 结束判定均**未**命中结束条件（若已结束则不再进入下一回合，L64–65）；后手分支按 `no_target_action` 兜底（fallback=普攻打剩余目标 / skip=直接跳过；无剩余目标→空挥/待机；被先手击杀的怪物不执行反击，写死，L56–57、L238） | ⑥ 回合结束 tick：持续回合扣减/冷却递减/效果衰减（L58；效果系统：冷却回合单位 L96–97、持续回合回合结束 tick 扣减 L176–177）；⑦ 互杀判定（L59–63，基准 `order`=先判定后手→先手击杀死→先手胜、反伤双死→平局、回合开始 dot 双杀（无先后）→平局；`hp_ratio`=剩余 HP 比、相同→平局，可配 L236–237）；⑧ 战斗结束判定（有结束→收尾 L64；无结束→⑨ 快照落回合边界 L66–67）；进入下一回合→回 ① 回合开始 dot（L45） | 下一回合开始（=T1 的 ① 阶段，经 T1 回 S1） | 战斗数值层 L45、L54–68、L236–238；效果系统 L96–97、L176–177 |

---

## 三、辅助迁移（附录，非 8 条主迁移计数）

主表之外、主循环内的分支/兜底/内循环迁移（A0–A6），保证主表每条在实现时无悬空出口。

| # | 当前状态 | 触发事件 | 条件 | 动作 | 目标状态 | 定稿引用 |
|---|---|---|---|---|---|---|
| **A1 回合开始即死直出** | S0/S1 边界（②） | ① 回合开始 dot 结算完成（L45） | 即死判定（L46）命中 | 玩家死→直接打失败标记（L47）；怪物死→直接打胜利标记（L48）——**不必等到行动阶段** | 直出 S5 `LOSE` / S4 `WIN` | 战斗数值层 L45–48 |
| **A2 互杀→平局** | S2/S3 收尾 | ⑦ 互杀判定（L59） | 双方死亡标记同轮并存（互杀标记） | 战斗结束=平局（可配 `mutual_kill_result=player_loss` 改为玩家败，L236）；判定基准二选一：`order`（先判定后手，L60–62）/ `hp_ratio`（剩余 HP 比，L63），默认 order（L237） | 结束=平局（⑧ 统一收尾） | 战斗数值层 L59–63、L236–237 |
| **A3 后手无目标兜底** | S2 后手（⑤） | 先手结算后进入后手 | 目标已死/剩余目标为空（L56–57） | `fallback`=默认普攻打剩余目标；无剩余目标→空挥/待机；可配 `skip`=直接跳过（L238）；**被先手击杀的怪物本身不执行反击（写死，L57）** | 回 S2 结算或直接走 ⑥ 回合结束 tick | 战斗数值层 L54–57、L238 |
| **A4 套内击杀续段** | S2 `RES`（④） | 击杀发生在连段套中 | 目标非 BOSS/非最后目标（L51、L53） | 后续段数照常执行（鞭尸浪费，套内稳定哲学）；套完结后目标退出战场、下一回合自动消失（不结算其行动，L52） | 回 S2 `RES` 继续剩余段数 | 战斗数值层 L50–52 |
| **A5 BOSS 即时结束** | 任意结算点（④ 落下即触发） | BOSS/最后目标死亡（L53、L65） | `boss_end_immediate=true`（L239） | **战斗立刻结束（⑧）**，不清算后续段数（不鞭尸），未执行的后续事件作废（L65） | 直出 S4 `WIN` | 战斗数值层 L53、L64–65、L239 |
| **A6 逃跑判定（待补挂接）** | S6 `FLY` | 逃跑结算 | 定稿未定义（成功率/回合数/代价/禁逃清单——待补） | 成功→清空连段等战斗内资源退出（L59）;失败→保留战斗与连段（对齐 1c1b L57 逃跑失败同款保留） | 成功→退战斗回地图；失败→回 S1 `ACT`（自环） | 战斗数值层 L7、L56–57、L59、L66–67；技能派生 L57 |

---

## 四、状态机不变量（实现期强制约束）

1. **时序写死不可配**：L15、L40–42、L264 —— 判定顺序与 `死亡判定顺序` 写死；formula.json `death_check` 段仅「结果/基准/兜底」可配（`mutual_kill_result` L236 / `mutual_kill_basis` L237 / `no_target_action` L238 / `boss_end_immediate` L239），配置顺序字段→黄提示（L264）。
2. **死亡判定唯一的两个触发点**：回合开始 ②（L46，dot 结算后即查）与行动结算 ④（L50，每次扣血后立即查）——两处之外不经 `DTH`。
3. **打死不鞭尸只在 BOSS/最后目标**：连段套内普通击杀→续段照常（L51–52）；BOSS/最后目标→立即结束（L53、L65、L239）。
4. **快照只落回合边界**：回合开始前/回合结束 tick 后（L66–67）→ 死亡结算天然先于快照写入，恢复无歧义；逃跑同样只在边界发起（L66–67）。
5. **结算收尾单点**：胜负/奖励/掉落/快照清理统一在当轮事件末尾结算一次（L64）；BOSS/最后目标死亡例外——立刻结束（L65）。
6. **拦截链衔接点**：伤害管线 ⑦扣血→⑧死亡判定（效果系统 L142–143）即 `RES→DTH`（T3）的唯一入口；拦截链顺序以效果系统 §3.4 为准（战斗数值层 L36、L231–234）。

---

## 五、引用行号索引

**战斗数值层设计定稿.md**：L7（入口转引/新功能5）· L15–16（判定顺序写死）· L21–38（公式总览）· L40–68（**回合死亡判定顺序：45 dot、46 即死、47–48 胜负标记、49 先手、50–53 击杀判定、54–57 后手/兜底、58 tick、59–63 互杀、64–65 结束时机、66–67 快照全边界**）· L112–137（属性/难度基线）· L139–151（难度分层/BOSS 阶段）· L164–167（MP/掉落经济）· L172–242（formula.json 含 death_check L235–239）· L236–239（可配四项）· L264（校验器时序写死）· L298（碎片化续玩）· L311–334（§8 收集器/快照）

**效果系统设计定稿.md**：L96–97（冷却回合单位/入快照）· L133–149（§3.4 拦截链：142 扣血→143 死亡判定）· L176–177（持续回合回合结束 tick 扣减）· L285（dot tick 回合开始/结束）· L425–434（§8.3 快照扩展）

---

## 六、落地核对清单（写码自检）

- [ ] 主循环每轮严格按 `① dot → ② 即死 → ③ 先手 → ④ 击杀判定 → ⑤ 后手 → ⑥ tick → ⑦ 互杀 → ⑧ 结束 → ⑨ 快照` 顺序推进（L45–67）。
- [ ] `DTH` 只在 A1/T3 两处进入；胜负标记只在 `DTH` 内打（L46–48、L50）。
- [ ] `death_check` 四项可配均落在 formula.json，代码无硬编码顺序（L235–239、L264）。
- [ ] 快照落点白名单 = 回合开始前 ∪ 回合结束 tick 后（L66–67）；逃跑入口同样受此约束。
- [ ] 收尾（奖励/掉落/战报/快照清理）单点执行一次（L64、L334）；BOSS/最后目标死亡走 A5 立即结束例外（L65、L239）。
- [ ] 伤害构成收集器随快照保存，持久化登记统一收尾（L333–334）。

*本文档由 1g1a（8 态定义）× 1c1b（连段迁移表范式）派生，行号以定稿最新版为准。*
