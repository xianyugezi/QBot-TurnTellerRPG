# 细化规划 · 1a 伤害公式数值（QBot-TurnTellerRPG）

> 性质：实现层细化规划（伤害公式各乘区的字段级定义 / 数值表 / 边界异常 / 验收测试用例）
> 依据：《战斗数值层设计定稿》v1.1.1（`/root/docs_archive/RPG框架项目/战斗数值层设计定稿.md`，共 375 行）→ 简称【数值层】；本文每条定义均引用定稿原文并标注行号，可逐条回查。
> 门控记录：j-space full 档（单交付物，一次通读可验证）；接缝审计执行于两处——①写文件前（§5 定稿内部一致性审计）②写文件后复核（§6 附录回查）。
> 范围：伤害公式 7 个主乘区 + 3 个补充乘区 + 1 个开放项；数值表；边界与异常；38 条可脚本验证的验收断言。

---

## 0. 乘区总览

### 0.1 判定顺序（写死，不可配）

> 【数值层】L16：`命中 → 会心 → 格挡 → 双通道（物理/元素独立取整后相加）→ 防御率 → 伤害拦截链 → 扣血`

### 0.2 乘法链（执行级）

> 【数值层】L26-27：`④ 物理通道 = max(1, floor(攻击值 × 技能倍率 × 类型克制 × 会心倍率 × 防御系数 × 怪物防御率))`
> 【数值层】L31-32：`⑤ 元素通道 = max(1, floor(装备元素攻击 × 技能元素倍率 × 元素弱点 × 会心倍率 × 元素减伤系数 × 怪物防御率))`
> 【数值层】L34：`⑥ 总伤害   = max(1, floor((物理 + 元素) × 格挡(×0.5) × 防御指令(×0.5) × 乱数))`

### 0.3 乘区清单（10 乘区 + 1 开放项）

| ID | 乘区 | 所在环节 | 默认值 | 定稿来源 |
|---|---|---|---|---|
| M1 | 基础攻击值 | ④物理通道首因子（⑤为装备元素攻击） | 力量/智力 15（+1.5/级） | L28 / L117-118 |
| M2 | 攻击倍率（技能倍率/技能元素倍率） | ④⑤ | 普攻 1.0；小技 1.5-2.0；大招 2.5-4.0；派生累计封顶 1.5 | L127 / L129 / L229 |
| M3 | 弱点倍率（类型克制/元素弱点） | ④⑤ | ×1.3 | L102-103 / L206-207 |
| M4 | 会心倍率（暴击倍率） | ④⑤（物理+元素共用一次判定） | 2.2/1.7/1.3 + 0.05×超会心Lv | L24 / L191-193 |
| M5 | 会心频率 | ②判定 | P=√幸运÷2（%），cap 95，档位 r≤P / r≤3P | L23 / L189-192 |
| M6 | 防御减伤（防御系数/元素减伤系数） | ④⑤ | K_def=100 除算，打类型破防 20% | L29-30 / L33 / L200-203 |
| M7 | 格挡减半 | ⑥ | 成功率 min(40%, 专注/(专注+150))；成功 ×0.5 | L25 / L195-198 |
| M8 | 防御指令 | ⑥ | ×0.5 | L34 |
| M9 | 乱数 | ⑥ | [0.9, 1.1]（闭区间，一次判定双通道共用） | L35 / L180 |
| M10 | 武器类型倍率 | （公式未挂入，预留） | 斩/打/突/魔 均 1.0 | L84 / L209-211 |
| O1 | 怪物防御率 | ④⑤ | **未定义（开放项）** | L27 / L32 |

**乘区数 = 10**（M1-M10：7 个主乘区 M1-M7 + 3 个补充乘区 M8-M10）；开放项 1 个（O1 怪物防御率，见 §1.11 与 §5 接缝审计-①）。

---

## 1. ① 伤害公式各乘区字段级定义

> 字段命名对齐 formula.json 落点（【数值层】§5.1，L175-242）；「可配」标注均来自定稿原文。

### 1.1 M1 基础攻击值（base_attack）

**定义**：物理通道首因子。攻击值按武器类型取力量或智力；元素通道首因子为装备元素攻击。

> 【数值层】L28：`攻击值 = 力量（斩/打/突）｜智力（魔，防御对位=精神）`
> 【数值层】L31：`装备元素攻击 × 技能元素倍率 × 元素弱点 × 会心倍率`
> 【数值层】L117：`| 力量 | 15 | +1.5 | 88.5 |`（玩家 50 级范围）
> 【数值层】L118：`| 智力 | 15 | +1.5 | 88.5 |`

| 字段 | 类型 | 说明 | 默认值 | 可配 |
|---|---|---|---|---|
| `attacker.stat_power` | float ≥0 | 物理通道攻击值（斩/打/突取力量） | 15（+1.5/级） | 是（属性成长） |
| `attacker.stat_intel` | float ≥0 | 物理通道攻击值（魔取智力，防御对位=精神） | 15（+1.5/级） | 是 |
| `equipment.weapon_atk` | float ≥0 | 武器白值（T1-5：7-11） | 7-11 | 是 |
| `equipment.enchant_atk` | float ≥0 | 强化加成（每级 +5） | +5/级 | 是 |
| `equipment.element_atk[]` | float ≥0 ×8 | 各元素装备攻击（元素通道首因子，8 元素 ID 见 §2 表） | 0 | 是 |

> 【数值层】L165：`强化 +N：每级攻击 +5（可配）`；L166：`装备白值：T1-5 武器攻击 7-11（可配）`；L220-221：8 元素注册表 `"earth": "地", "fire": "火", "water": "水", "wind": "风", "thunder": "雷", "crystal": "晶", "moon": "月", "void": "无"`。

**校验器**：非负校验；负数 → 黄提示并运行期按 0 处理（细化建议，见 §3-C；定稿仅规定"其余数值全部提示不拦截" L263）。

### 1.2 M2 攻击倍率（skill_mult）

**定义**：技能倍率（物理通道）与技能元素倍率（元素通道）为攻击值上的乘区；派生累计受封顶约束。

> 【数值层】L127：`普攻 100% ｜ 小技 150-200% ｜ 大招 250-400%`
> 【数值层】L129：`倍率封顶：派生累计 ≤1.5×（技能派生定稿）`
> 【数值层】L179：`"base_attack_mult": 1.0,`
> 【数值层】L229：`"max_total_mult": 1.5       // 技能派生倍率封顶`

| 字段 | 类型 | 说明 | 默认值 | 可配 |
|---|---|---|---|---|
| `damage.base_attack_mult` | float | 全局攻击倍率基线 | 1.0 | 是 |
| `skill.mult` | float | 技能倍率（普攻/小技/大招） | 普攻 1.0 / 小技 1.5-2.0 / 大招 2.5-4.0 | 是 |
| `skill.element_mult` | float | 技能元素倍率（元素通道） | 随技能配置 | 是 |
| `derived.max_total_mult` | float | 派生累计倍率封顶 | 1.5 | 是 |

**校验器**：派生累计超过 `max_total_mult` 时封顶（L129/L229 语义）；多段攻击每段独立取整（L128）。

### 1.3 M3 弱点倍率（weakness）

**定义**：怪物配置制弱点（非全局克制表）——类型弱点作用于物理通道，元素弱点作用于元素通道。

> 【数值层】L102：`- 类型弱点（怪物配置）：斩/打/突/魔 弱点 → ×1.3（可配）`
> 【数值层】L103：`- 元素弱点（怪物配置）：8 元素弱点表 → ×1.3（可配）`
> 【数值层】L206-207：`"type_mult": 1.3,` / `"element_mult": 1.3`
> 【数值层】L249：`enemies.json → 弱点表只写 {元素ID: 倍率}，元素 ID 由 formula.json 注册表约束`

| 字段 | 类型 | 说明 | 默认值 | 可配 |
|---|---|---|---|---|
| `weakness.type_mult` | float | 类型弱点倍率（斩/打/突/魔） | 1.3 | 是 |
| `weakness.element_mult` | float | 元素弱点倍率（8 元素） | 1.3 | 是 |
| `enemies.json[*].weakness` | map | 每怪弱点表 `{元素ID: 倍率}` | 每怪 ≥1 弱点元素（见 §5-⑤） | 是 |

**校验器**：`type_mult/weakness` 超 2.0 → 警告"注意克制收益"（L257）；元素 ID ∈ 注册表（L258）。

### 1.4 M4 会心倍率（暴击倍率，crit_mult）

**定义**：三档会心倍率 + 超会心等级加成；物理+元素共用一次判定。

> 【数值层】L24：`倍率 2.2/1.7/1.3 + 0.05×超会心Lv（物理+元素共用一次判定）`
> 【数值层】L191：`"tiers": {"high": 2.2, "mid": 1.7, "low": 1.3},`
> 【数值层】L193：`"crit_mult_up": {"lv1": 0.05, "lv2": 0.10, "lv3": 0.15}`

| 字段 | 类型 | 说明 | 默认值 | 可配 |
|---|---|---|---|---|
| `crit.tiers.high` | float | 高级会心倍率 | 2.2 | 是 |
| `crit.tiers.mid` | float | 中级会心倍率 | 1.7 | 是 |
| `crit.tiers.low` | float | 低级会心倍率 | 1.3 | 是 |
| `crit.crit_mult_up.lv1..3` | float | 超会心 Lv1/2/3 加成 | 0.05 / 0.10 / 0.15 | 是 |

**校验器**：`crit.tiers` 范围 1.0-3.0（L256）；交叉一致性 低<中<高、超会心递增（L260）。

### 1.5 M5 会心频率（crit_chance）

**定义**：会心判定概率 P 由幸运导出，档位按随机数 r 划分；P 封顶 95%。

> 【数值层】L23：`② 会心     = P=√幸运÷2，r≤P 高级 / r≤P×3 中级 / 其余低级`
> 【数值层】L189：`"p_coef": 0.5,               // P = √幸运 × p_coef ÷ 100（幸运 100 → P=5）`
> 【数值层】L190：`"cap": 95,`
> 【数值层】L192：`"tier_p": [1, 3],            // r≤P 高级 / r≤P×3 中级`
> 【数值层】L92：`- 斩击类型：会心 +5%（对高 HP 怪收益）`；L216：`"slash_crit": 0.05,`

| 字段 | 类型 | 说明 | 默认值 | 可配 |
|---|---|---|---|---|
| `crit.p_coef` | float | P = √幸运 × p_coef ÷ 100（P 以 % 计） | 0.5 | 是 |
| `crit.cap` | float | P 封顶 | 95（%） | 是 |
| `crit.tier_p` | int[2] | 档位阈值倍数：r≤P×1 高级 / r≤P×3 中级 | [1, 3] | 是 |
| `type_affinity.slash_crit` | float | 斩击类型会心加成（加入 P 后统一 cap） | 0.05 | 是（`type_affinity_enabled` 总开关 L94） |

**一致性**：L23「P=√幸运÷2」与 L189「√幸运 × p_coef ÷ 100」等价（p_coef=0.5 时均为 %）：幸运 100 → P=5%。
**细化建议（§5-⑦）**：L23 未写 clamp；判定前统一 `P = min(P + 斩击加成, 95)`。

### 1.6 M6 防御减伤（defense_reduction）

**定义**：除算减伤（ratio 模式），防御系数作用于物理通道、元素减伤系数作用于元素通道；破防降低有效体质。

> 【数值层】L29：`防御系数 = K_def / (有效体质 + K_def)   K_def = 100`
> 【数值层】L30：`有效体质 = 体质 × (1 − 破防%)（打类型内置破防 20%）`
> 【数值层】L33：`元素减伤系数 = K_def / (元素抗性 + K_def)`
> 【数值层】L201-203：`"mode": "ratio",` / `"k": 100,` / `"pierce_types": {"blunt": 0.2}`

| 字段 | 类型 | 说明 | 默认值 | 可配 |
|---|---|---|---|---|
| `defense.mode` | enum | 减伤模式（ratio=除算，覆盖框架减法） | "ratio" | 是 |
| `defense.k` | float >0 | 减伤常数 K_def | 100 | 是 |
| `defense.pierce_types.blunt` | float | 打类型内置破防率 | 0.2 | 是 |
| `type_affinity.blunt_pierce` | float | 同上（打类型倾向，双源见 §5-③） | 0.2 | 是 |
| `target.effective_con` | float | 有效体质 = 体质 × (1 − 破防%) | 随目标 | — |

**校验器**：K/破防超范围提示不拦截（L263）；破防可被效果叠加（§8.1 示例 `"pierce": 0.2`，L324）。

### 1.7 M7 格挡减半（block_halve）

**定义**：格挡判定成功 → 总伤害 ×0.5；魔攻击无视格挡。

> 【数值层】L25：`③ 格挡     = min(40%, 专注/(专注+150))；成功 → 总伤害 ×0.5；魔攻击无视格挡`
> 【数值层】L196-198：`"k": 150, "cap": 40,` / `"magic_ignores": true,       // 魔攻击无视格挡` / `"halve_after_block": true    // 格挡减半（组队多段预留）`

| 字段 | 类型 | 说明 | 默认值 | 可配 |
|---|---|---|---|---|
| `block.k` | float >0 | 格挡率公式常数 | 150 | 是 |
| `block.cap` | float | 格挡率封顶 | 40（%） | 是 |
| `block.magic_ignores` | bool | 魔攻击无视格挡 | true | 是 |
| `block.halve_after_block` | bool | 格挡成功 ×0.5 | true | 是 |

**校验器**：同 §5.3 8-14（默认值可配、超范围警告，L263）。

### 1.8 M8 防御指令（guard_cmd）

**定义**：总伤害环节的防御指令减半乘区。

> 【数值层】L34：`⑥ 总伤害   = max(1, floor((物理 + 元素) × 格挡(×0.5) × 防御指令(×0.5) × 乱数))`

| 字段 | 类型 | 说明 | 默认值 | 可配 |
|---|---|---|---|---|
| `guard.mult` | float | 防御指令减半 | 0.5（写死于公式，无独立配置字段） | 否 |

### 1.9 M9 乱数（rng）

**定义**：总伤害末段乘区；闭区间；一次判定双通道共用。

> 【数值层】L35：`乱数 ∈ [0.9, 1.1]（一次判定，双通道共用）`
> 【数值层】L180：`"rng": [0.9, 1.1],`

| 字段 | 类型 | 说明 | 默认值 | 可配 |
|---|---|---|---|---|
| `damage.rng` | float[2] | 乱数区间（闭区间含端点） | [0.9, 1.1] | 是 |

### 1.10 M10 武器类型倍率（weapon_type_mult，预留）

**定义**：四类武器倍率表；类型差异由 type_affinity 承担，倍率表预留，未挂入 ④⑤ 乘法链（见 §5-②）。

> 【数值层】L84：`斩击 ×1.0 ｜ 打击 ×1.0 ｜ 突击 ×1.0 ｜ 魔 ×1.0`
> 【数值层】L85：`（类型差异用 type_affinity 倾向替代，见 2.3；倍率表预留）`
> 【数值层】L210：`"slash": 1.0, "blunt": 1.0, "thrust": 1.0, "magic": 1.0`

| 字段 | 类型 | 说明 | 默认值 | 可配 |
|---|---|---|---|---|
| `weapon_type_mult.slash/blunt/thrust/magic` | float | 武器类型倍率 | 均 1.0 | 是（预留） |

### 1.11 O1 怪物防御率（monster_def_rate，开放项）

**出现**：④⑤ 通道公式末因子。

> 【数值层】L27：`× 防御系数 × 怪物防御率))`
> 【数值层】L32：`× 元素减伤系数 × 怪物防御率))`

**定稿未定义**：全文无算法、formula.json 无字段（§5-①）。
**【仲裁 R-09 · 用户拍板 2026-08-18】怪物防御率 = 每怪物可配字段**：
- enemies.json 每怪可配 `monster_def_rate`（≥0，默认 1.0 = 普通同玩家不减免/不增伤）；
- 战斗引擎取**目标 combatant 配置**的 monster_def_rate 乘入 ④⑤ 通道末因子（回退 DamageFormulaParams.monster_def_rate=1.0）；
- 与 §4.3 怪物三档防御倍率（普通 ×1.0 / 精英 ×1.3 / BOSS ×1.5，L135-137）语义一致——内容包作者按档位直接配对应值。
- 校验器：monster_def_rate 非负；负值 → 黄提示 + 运行期按 0（护栏统一）。

---

## 2. ② 数值表（默认值 / 可配范围 / 校验器规则）

> 校验器规则出处：【数值层】§5.3 十五条（L254-265）+ §8.5（L369）。

### 2.1 formula.json 字段表

| 域 | 字段 | 默认值 | 可配范围 | 校验器规则 |
|---|---|---|---|---|
| damage | `schema_version` | 1 | 整数 ≥1 | 类型校验（§5.3-8~14） |
| damage | `base_attack_mult` | 1.0 | ≥0（建议 ≤10） | 提示不拦截 |
| damage | `rng` | [0.9, 1.1] | 0<min≤max（建议 max≤2） | 提示不拦截 |
| damage | `floor_mode` | "channel_end" | channel_end / deep | 枚举校验；deep_floor 开关保留（L38/L182） |
| damage | `deep_floor` | false | bool | 布尔校验 |
| hit | `k` | 0.2 | **0.05-1** | **范围校验（L256）** |
| hit | `cap_min` / `cap_max` | 10 / 95 | **10-100** | **范围校验（L256）** |
| crit | `p_coef` | 0.5 | >0（建议 ≤2） | 提示不拦截 |
| crit | `cap` | 95 | 0-100 | 概率合法（L259） |
| crit | `tiers` | high 2.2 / mid 1.7 / low 1.3 | **1.0-3.0** | **范围 + 低<中<高（L256/L260）** |
| crit | `tier_p` | [1, 3] | 整数递增 | 交叉一致性（L260） |
| crit | `crit_mult_up` | lv1 0.05 / lv2 0.10 / lv3 0.15 | ≥0（建议 ≤0.5） | 递增校验（L260） |
| block | `k` | 150 | >0 | 提示不拦截 |
| block | `cap` | 40 | 0-100 | 概率合法 |
| block | `magic_ignores` | true | bool | 布尔校验（双源见 §5-③） |
| block | `halve_after_block` | true | bool | 布尔校验 |
| defense | `mode` | "ratio" | ratio / subtract | 枚举校验 |
| defense | `k` | 100 | >0（建议 50-200） | 提示不拦截 |
| defense | `pierce_types.blunt` | 0.2 | 0-1 | 概率合法 |
| weakness | `type_mult` | 1.3 | **≤2.0** | **超 2.0 → 警告"注意克制收益"（L257）** |
| weakness | `element_mult` | 1.3 | **≤2.0** | 同上 |
| weapon_type_mult | `slash/blunt/thrust/magic` | 均 1.0 | ≥0 | 提示不拦截（预留，未挂链） |
| type_affinity | `enabled` | true | bool | 布尔校验 |
| type_affinity | `blunt_pierce` | 0.2 | 0-1 | 概率合法 |
| type_affinity | `thrust_hit` | 0.05 | 0-1 | 概率合法（命中 +5%） |
| type_affinity | `slash_crit` | 0.05 | 0-1 | 概率合法（会心 +5%） |
| type_affinity | `magic_ignore_block` | true | bool | 布尔校验（双源见 §5-③） |
| elements | 注册表 | 地火水风雷晶月无 | 元素 ID 唯一 | **引用存在校验（L258）** |
| derived | `max_total_mult` | 1.5 | ≥1.0 | 派生封顶（L129/L229） |
| effects_link | `intercept_order` | [shield, reduce, immune, revive, reflect] | 见 §5-④ | 顺序与 §一⑦ 冲突待对齐（L36 vs L232） |
| effects_link | `pierce_cap` | 0.6 | 0-1 | 概率合法 |
| death_check | `mutual_kill_result` | "draw" | draw / player_loss | 枚举；**仅结果/基准/兜底可配（L264）** |
| death_check | `mutual_kill_basis` | "order" | order / hp_ratio | 枚举 |
| death_check | `no_target_action` | "fallback" | fallback / skip | 枚举 |
| death_check | `boss_end_immediate` | true | bool | 布尔校验 |
| stats_collector | `enabled` / `dummy_log_size` / `dummy_realtime` | true / 5 / false | log_size **1-20** | **类型校验；1-20 超范围黄提示（L369）** |

### 2.2 技能倍率与经济数值表

| 数值 | 默认值 | 可配 | 来源 |
|---|---|---|---|
| 普攻倍率 | 100%（1.0） | 是 | L127 |
| 小技倍率 | 150-200% | 是 | L127 |
| 大招倍率 | 250-400% | 是 | L127 |
| 派生累计封顶 | ≤1.5× | 是 | L129 |
| 多段攻击取整 | 每段独立取整 | 写死 | L128 |
| 药剂回复 | 普通 30% / 中 50% / 大 80% HP | 是 | L163 |
| 技能 MP 消耗 | 普攻 0 / 小技 5-10 / 大招 15-25 | 是 | L164 |
| 强化 +N | 每级攻击 +5 | 是 | L165 |
| 装备白值 | T1-5 武器攻击 7-11 | 是 | L166 |
| 玩家初始/50 级 | 生命 100/590、力量 15/88.5、体质 10/59、幸运 10/49.2 等 | 是（属性定稿） | L113-123 |
| 怪物三档 | 普通 100-380 HP 3-8 回合；精英 800-1500 攻 ×1.2 防 ×1.3；BOSS 3000-8000 攻 ×1.3 防 ×1.5 40-60 回合 | 是 | L135-137 |
| 命中同级区间 | 75-95% | — | L299 |

---

## 3. ③ 边界与异常

> 每条含：场景 → 定稿依据 → 行为定义 → 可断言结果。运行期护栏统一为「clamp 优先、max(1) 兜底、负值按 0」三级（细化建议，定稿未全部写明，标 ⚑）。

### A. 0 防御（有效体质 = 0）

- 防御系数 = 100/(0+100) = **1.0**：不减免、不增伤、**无除零**（K_def 作分子，L29）。
- 断言：`defense_factor ∈ (0, 1]` 恒成立，且体质 0 时恰为 1.0；无 NaN/Inf。

### B. 0 攻击值 / 0 元素攻击

- 物理通道 = max(1, floor(0 × …)) = **1**（L26）；元素通道同（L31）。
- 双通道均 0 → 总伤害 = max(1, floor(0 × 0.5 × 0.5 × 乱数)) = **1**（L34）。**最低伤害恒 ≥1，无 0 伤害（未命中除外，L22）。**

### C. 负数（攻击值 / 体质 / 倍率等 ⚑）

- 定稿未定义负数输入（属性 base 均 >0，L115-123）。细化建议：校验器对负值黄提示（§5.3-8~14 语义），运行期按 0 处理；任何乘区结果不允许为负。

### D. 超上限

| 项 | 护栏 | 依据 |
|---|---|---|
| 命中率 | clamp [10%, 95%] | L21 / L185-186 |
| 会心 P | cap 95% | L190（clamp 位置 ⚑，§5-⑦） |
| 格挡率 | min(40%, …) | L25 / L196 |
| 弱点倍率 | >2.0 → 警告 | L257 |
| 派生累计 | ≤1.5× | L129 / L229 |
| 50 级尖峰 | ≈3374，不破万 | L157 |
| 元素减伤 | 抗性 ≥0 时系数 ∈ (0, 1] | L33 |

### E. 判定边界（等号归属）

- r == P → **高级**（r≤P 含等，L23/L192）；r == 3P → **中级**（r≤P×3 含等）；r > 3P → 低级。
- 乱数区间闭区间：[0.9, 1.1] 两端合法（L35/L180）。
- 幸运 0 → P=0 → 恒低级 1.3；幸运 100 → P=5%（L189 注释）。
- 专注 → ∞ → 格挡率 → 40% 封顶（不超）。

### F. 顺序与取整边界

- 通道末 floor 一次（deep_floor=false），双通道独立取整后相加（L16/L38）。
- 格挡、防御指令、乱数在总伤害层相乘后**最后**一次 floor（L34）；拦截链后为最终扣血（L36）。

### G. 未命中

- miss → 伤害 0 + 提示"未命中"（L22）；命中判定在会心之前（L16）。

---

## 4. ④ 验收测试用例清单（可脚本验证断言，38 条）

> 约定：全部断言以 formula.json 默认值（§2.1）+ 固定种子随机数为输入；`hit_rate/ crit_prob/ block_rate/ defense_factor/ channel_phys/ channel_elem/ total_damage` 为管线函数，Python/JS 可直接执行；涉及随机环节固定种子保证可复现。状态列「P0=必过 / P1=调优期对齐」。

### A 命中与会心频率（7）

| ID | 名称 | 输入（固定参数） | 断言表达式 | 依据 |
|---|---|---|---|---|
| T01 | 命中下限 clamp | 专注=10，对方敏捷=1000 | `hit_rate(10,1000) == 0.10`（10/(10+200)=0.0476 → clamp 10%） | L21 |
| T02 | 命中上限 clamp | 专注=1000，对方敏捷=1 | `hit_rate(1000,1) == 0.95`（0.9998 → clamp 95%） | L21 |
| T03 | 同级命中区间 | 专注=50，对方敏捷=50 | `0.75 ≤ hit_rate(50,50) ≤ 0.95`（50/60=0.833） | L21/L299 |
| T04 | 未命中 → 0 伤害 | 命中判定失败 | `damage == 0 and 提示 == "未命中"` | L22 |
| T05 | 会心频率公式 | 幸运=100 / 幸运=0 | `crit_prob(100) == 0.05; crit_prob(0) == 0.0` | L23/L189 |
| T06 | 会心档位边界 | r=P；r=3P；r=3P+ε | `tier(r==P)=="high"; tier(r==3P)=="mid"; tier(r>3P)=="low"` | L23/L192 |
| T07 | 斩击会心加成 | 斩击武器，幸运=0 | `crit_prob == 0.05`（P=0+5%，cap 95） | L92/L216 |

### B 会心倍率（3）

| ID | 名称 | 输入 | 断言表达式 | 依据 |
|---|---|---|---|---|
| T08 | 三档倍率 | 高级/中级/低级各一次 | `mult == 2.2 / 1.7 / 1.3`，且物理与元素通道共用同一档 | L24/L191 |
| T09 | 超会心递增 | 超会心 Lv1/2/3 | `high + lv == 2.25 / 2.30 / 2.35` | L24/L193 |
| T10 | 会心 P 封顶 | 配置 P=120（越界） | `min(P, 95) 生效，never > 0.95` | L190 |

### C 格挡（3）

| ID | 名称 | 输入 | 断言表达式 | 依据 |
|---|---|---|---|---|
| T11 | 格挡率公式与封顶 | 专注=150；专注=0 | `block_rate(150) == 0.40`（0.5 → cap 40%）；`block_rate(0) == 0.0` | L25/L196 |
| T12 | 格挡减半 | 格挡成功 | `total_damage == floor(双通道和 × 0.5 × …)`（恰一次 ×0.5） | L25/L198 |
| T13 | 魔攻击无视格挡 | 魔通道攻击 | `block 环节跳过，倍率不乘 0.5` | L25/L197 |

### D 防御减伤（5）

| ID | 名称 | 输入 | 断言表达式 | 依据 |
|---|---|---|---|---|
| T14 | 防御系数 | 有效体质=100 | `defense_factor(100) == 0.5`（100/200） | L29 |
| T15 | 0 体质 | 有效体质=0 | `defense_factor(0) == 1.0`，无除零/NaN/增伤 | L29 |
| T16 | 破防公式 | 体质=100，破防 20% | `effective_con == 80; defense_factor == 100/180` | L30 |
| T17 | 打类型内置破防 | 打击武器 | `pierce == 0.2 自动生效` | L30/L90/L203 |
| T18 | 元素减伤 | 元素抗性=0；=100 | `elem_factor(0) == 1.0; elem_factor(100) == 0.5` | L33 |

### E 弱点（3）

| ID | 名称 | 输入 | 断言表达式 | 依据 |
|---|---|---|---|---|
| T19 | 类型弱点 | 怪物斩弱点 | `channel_phys ×= 1.3` | L102 |
| T20 | 元素弱点 | 怪物火弱点 | `channel_elem ×= 1.3` | L103 |
| T21 | 双弱点叠加 | 斩+火双弱点 | `总增益 == 1.3 × 1.3 = 1.69` | L102-103 |

### F 通道与取整（5）

| ID | 名称 | 输入 | 断言表达式 | 依据 |
|---|---|---|---|---|
| T22 | 通道末 floor | deep_floor=false | `floor 仅在通道末一次，无逐段 floor` | L38/L182 |
| T23 | 通道下限 | 攻击值=0 | `channel_phys == 1` | L26 |
| T24 | 总伤害下限 | 双通道均 0 | `total_damage == 1`（0×0.5×0.5×rng → max(1)） | L34 |
| T25 | 乱数闭区间共用 | 固定种子 | `0.9 ≤ rng ≤ 1.1`，物理/元素共用同一值 | L35/L180 |
| T26 | 防御指令 | 防御指令生效 | `total_damage == floor(… × 0.5)` | L34 |

### G 管线（3）

| ID | 名称 | 输入 | 断言表达式 | 依据 |
|---|---|---|---|---|
| T27 | 双通道独立取整相加 | 复现 §8.1 样例 | `ch_phys=214, ch_elem=96 → 拦截链前和 == 310`（乱数=1.0、无格挡/防御指令） | L16/L34/L320-321/L328 |
| T28 | 判定顺序不可配 | 探测管线入口 | `顺序固定：命中→会心→格挡→通道→防御率→拦截链→扣血；formula.json 无顺序字段` | L16/L262 |
| T29 | 拦截链必经 | 任意伤害 | `任何伤害路径都经 减伤→护盾→反弹→吸收→免疫→续行→扣血 后扣血` | L36 |

### H 经济基准护栏（3）

| ID | 名称 | 输入 | 断言表达式 | 依据 |
|---|---|---|---|---|
| T30 | 1 级普攻 10-15 | 无武器白值、同级普通怪（体质 10）、乱数 [0.9,1.0]、无会心 | `10 ≤ 普攻 ≤ 15`（P1：与公式推算 15.9-19.5 不吻合，见 §5-⑥） | L156 |
| T31 | 50 级尖峰 ≈3374 不破万 | 默认值全乘区最大组合 | `peak ≈ 3374（±容差）且 < 10000`，以 §4.5 验证脚本输出为准 | L157 |
| T32 | 派生累计封顶 | 派生链理论累计 >1.5 | `实际倍率 == 1.5（封顶生效）` | L129/L229 |

### I 校验器（6）

| ID | 名称 | 输入 | 断言表达式 | 依据 |
|---|---|---|---|---|
| T33 | tiers 交叉一致性 | 配置 low≥mid 或超会心递减 | `校验失败（低<中<高、递增）` | L260 |
| T34 | 倍率超限警告 | type_mult=2.1 | `警告"注意克制收益"，不拦截` | L257 |
| T35 | hit 参数范围 | hit.k=0.01 或 cap=200 | `校验失败（0.05-1 / 10-100）` | L256 |
| T36 | 无弱点怪 | 每怪弱点元素=0 | `黄警告"该怪无弱点，元素武器无发挥空间"，允许配置` | L261 |
| T37 | 管线顺序不可改 | 尝试配置顺序 | `formula.json 无顺序字段可写，写入被拒/忽略` | L262 |
| T38 | death_check 可配白名单 | 配置顺序字段 | `黄提示；仅 mutual_kill_result/basis/no_target_action/boss_end_immediate 可配` | L264 |

---

## 5. 接缝审计结论（定稿内部一致性，7 项）

> 审计范围：§一公式总览 ↔ §五 formula.json ↔ §二武器 ↔ §三弱点 ↔ §四经济 ↔ §八收集器。

| # | 严重度 | 接缝发现 | 证据行 | 建议处置 |
|---|---|---|---|---|
| ① | 高 | **怪物防御率出现于公式却无定义**：④⑤ 通道末因子，全文无算法、formula.json 无字段 | L27/L32 | 实现前裁决：默认 1.0 或挂 §4.3 档位防御倍率；本细化按 O1 开放项处理 |
| ② | 中 | **weapon_type_mult 未挂入乘法链**：配置表存在（均 1.0），但 ④⑤ 公式无此项；定稿自注"倍率表预留" | L84/L85/L209-211 | 保持预留不接入；实现时校验恒 1.0，避免"配置了但不生效" |
| ③ | 中 | **magic_ignore 双源重复**：block.magic_ignores 与 type_affinity.magic_ignore_block 同值并存，违反 §5.2「引用不复制数值」精神 | L197/L217 | 收敛为单一数据源（建议 block.magic_ignores 为准，type_affinity 引用之） |
| ④ | 高 | **拦截链顺序双源不一致**：§一⑦ 为 7 项「减伤→护盾→反弹→吸收→免疫→续行→扣血」；formula.json 仅 5 项且次序不同（shield→reduce→immune→revive→reflect），缺反弹/吸收/续行 | L36/L232 | 以效果系统 §3.4 为准（L36 自指），修订 effects_link.intercept_order |
| ⑤ | 高 | **弱点校验器语义冲突**：§三「每怪 ≥1 弱点元素（防白板怪）」vs §5.3 第 6 条「每怪弱点为 0 → 警告（允许无弱点）」 | L105/L261 | 二选一裁决；建议以 §5.3 黄警告（允许无弱点）为准并修订 §三措辞 |
| ⑥ | 中 | **「1 级普攻 10-15」与公式默认链不吻合**：力量 15 × 低级会心 1.3 × 防御系数 100/110 × 乱数 [0.9,1.1] ≈ 15.9-19.5 | L156 vs L23/L29/L35 | 数值调优期对齐（T30 标 P1）；需明确「低级会心 1.3 是否恒生效」 |
| ⑦ | 低 | **crit.cap=95 无 clamp 落点**：L23 会心公式未写 P 封顶，仅配置表有 cap | L190 vs L23 | 细化采纳：判定前 P=min(P, 95)（含斩击 +5% 后） |

---

## 6. 附录：引用索引（回查清单）

| 定稿行号 | 内容要点 |
|---|---|
| L16 | 判定顺序（写死） |
| L21-22 | 命中率公式 / miss → 0 |
| L23-24 | 会心频率 / 倍率 |
| L25 | 格挡 |
| L26-30 | 物理通道 / 攻击值 / 防御系数 / 有效体质 |
| L31-33 | 元素通道 / 元素减伤 |
| L34-35 | 总伤害 / 乱数 |
| L38 | 取整策略 |
| L84-85 | 武器类型倍率（预留） |
| L90-94 | type_affinity 四倾向 |
| L102-105 | 弱点与校验器 |
| L113-123 | 玩家属性表 |
| L127-129 | 技能倍率 / 派生封顶 |
| L135-137 | 怪物三档 |
| L156-158 | 伤害规模 / 验证 |
| L163-166 | 药剂 / MP / 强化 / 白值 |
| L175-242 | formula.json 默认值 |
| L254-265 | 校验器 15 条 |
| L299 | 命中挫败对策 |
| L306-371 | §八 伤害构成统计 |
| L320-328 | §8.1 记录字段样例（214/96/310） |
| L369 | §8.5 校验器 |

---

*细化规划 1a 完 · 乘区 10（7 主 + 3 补充 + 1 开放项）· 验收用例 38 条 · 接缝审计 7 项 · 全部引用可回查定稿行号*
