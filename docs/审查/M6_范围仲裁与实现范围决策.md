# M6 范围仲裁与实现范围决策（2026-08-27 · 用户授权 Hermes 拍板）

> 依据：M6 设计审查 6 批 12 组 dsh 总汇总（/root/deliverables/m6_design_review_summary.md，P0×12 + P1×60 + P2×62）。
> 用户授权"你看着办"，本决策作为 M6 细化与实现的唯一权威。

## 一、M6 范围仲裁（解决四源冲突）

**裁定：M6 = 接线闭环（F5/F6/G1-G4，实现层规划文档 §8 L3481）为主，verify_m6 为两段式。**

- 原冲突：规划文档（M6=接线闭环 F5/F6/G1-G4）｜细化_5d L95（verify_m6=数据 114TC 3e/3e2/5a/5b/5c）｜启动手册+run_all_tests（M6=数据框架 5a/5b/4c/4d/4e/6）
- 归属调整：
  - **5a/5b（编辑器/GM 后端）→ 归 M12 支撑工具**（用户拍板 8 步规划）
  - **4c/4d/4e/6（成就/图鉴/PVP/职业引擎）→ 归 M11 横切系统 / M13 职业引擎**
  - **3e/3e2（loader 校验接线/热重载契约）→ 已有实现（M2），并入 M6 verify 校验段**（不做新功能，只验证）
- **verify_m6 = 两段式**：① 接线闭环门禁（F5 热重载回退 + F6 冒烟闭环 + G2 测试体系 + G3 故障注入 + G4 质量门禁）② 承接 M2-M5 DELAYED 项（见三）
- 同步修订：实现层规划文档 §8 M6 行、细化_5d §2.1 M6 计数、run_all_tests.py MILESTONES["m6"] 注释、启动手册 M6 段——四份文档统一为"接线闭环"

## 二、M6 实现范围（8 大项）

1. **F5 热重载接线**：watcher 启动装载 + 调度驱动 + /重载 真实后端 + ReloadResult→人话提示翻译；**P0-1 续战旧配置修复**（战斗快照带 registry_generation 世代 + from_snapshot 支持 registry 注入/按世代重绑定旧快照 + 补 F5 验收③旧局快照冗余断言）
2. **F6 内容包冒烟**：删除"battle 留桩"过时前提，模拟一局直接用真实 BattleEngine（legal 包数据驱动）+ 注册→锁定→攻击→结算逐步断言（e2e_m4 约定：零 NoneBot/固定种子/两次重放一致）；四类样例包 + 五档数据包（content/demo_blank~demo_full，与 tests/fixtures/packs 四件套分离）；冒烟接入 run_all_tests + verify_m6
3. **G1 verify 门禁**：run_all_tests 接入 m6（未实现不假绿：--only m6 exit≠0；全量跳过 m6 置 fail）；t_coverage_self_consistent 升级 DELAYED 到期扫描（verify_m4 批次7-01 立即翻转）；M6 行补"承接 DELAYED 清单"段
4. **G2 测试体系**：性质用例（随机 1000 组不变量：伤害≥1/乱数区间/会心率≤95%）+ conftest 固定随机种子 fixture（seed 参数化）+ 公式参数独立 fixture 包（formula.json 段级参数 + 测试禁硬编码 DamageFormulaParams 默认值）+ 必测表补齐（levelup/inventory/equipment/storage 往返/版本迁移 round-trip）；删除"battle 留后续路"陈旧口径
5. **G3 故障注入**：tests/fault/ 六脚本（crash/save/reload/formula/doublepay/netdrop）+ run_all_tests --only fault 分支 + **幂等事务三件套前置**（message_id 幂等键入指令入口与业务写同事务 IDEM-2 / 购买结算搬入 repo.tx() BEGIN IMMEDIATE / per-player 串行队列）+ 并发双扣用例（限购/货币两路径断言）
6. **G4 质量门禁**：覆盖率实算（coverage 依赖 + 阈值断言 ≥80% + 报表归档 docs/verify/；口径定死 = qbot_rpg/core + engine + content 各自 ≥80%）+ ruff/mypy 配置依赖执行（pyproject [tool.ruff]/[tool.mypy] + run_all_tests 快速门）+ CHANGELOG.md 建档（Keep a Changelog，回填 M0-M5 六条目）+ CI workflow（.github/workflows/ci.yml）+ PR 评审清单模板（.github/pull_request_template.md A-F 六类）；修正伪托登记（细化_5d §7.4 补 D5：M0-M5 覆盖率简版/M6 恢复，verify_m0/记录.md/run_all_tests 改引真实 D5）
7. **承接 M2-M5 DELAYED**：图鉴分级（1f-TC-11/12）、防嵌套红拦（2a1c-TC-22）、锻造 TPL-10/11（3d TC-16/17）、/注册（4f TC-01~04/06）、/状态（4f TC-07/09/10）、快捷解绑/列表（4f TC-17/22/23）——各落 verify_m6 断言或显式残留登记
8. **levelup/inventory/equipment 三引擎实装**（审查批4 P1：核心空壳）：core/levelup.py（经验曲线边界：0 经验/满级不增长/升级回满）+ core/inventory.py（堆叠上限/绑定/同类型限次）+ core/equipment.py（部位占用/数量校验/运行时互斥）+ /装备 命令从 FakeEquipEngine 换真实引擎

## 三、M6 交付顺序（批次建议）

批1 基础指令+三引擎（/注册 /状态 /快捷 /levelup /inventory /equipment）→ 批2 幂等事务三件套（购买入事务/幂等键入指令/串行队列）→ 批3 热重载接线+P0-1 续战修复 → 批4 冒烟闭环+五档包 → 批5 故障注入六脚本 → 批6 G2 测试体系（性质/seed/fixture 包/往返/迁移）→ 批7 质量门禁（覆盖率/ruff-mypy/CHANGELOG/CI/评审模板）→ 批8 verify_m6 两段式 + run_all_tests 接入 + 承接 DELAYED 收口

## 四、其他决策

- **GitHub token**：`.git/config` 内嵌明文 token 先用着（项目进行中），项目结束统一清理轮换（用户拍板）
- 12 份审查报告 + 本决策 = M6 细化（细化_M6_接线闭环）的唯一依据；细化需求阶段逐条落地 P 项

*本决策已同步：实现层规划文档 §8 / 细化_5d / run_all_tests / 启动手册（M6 四源统一为接线闭环）。*
