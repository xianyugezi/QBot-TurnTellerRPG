# 批77 · X6 引擎/跨模块渲染文案迁表 · 收口报告（2026-09-21）

> 任务：`docs/消息模板重构/02_遗留登记.md` §A「引擎/跨模块渲染文案（待专批）」+ `docs/矛盾与待裁决登记.md` §A X6。
> 硬约束：**零行为变化**（迁表后渲染逐字符一致）、不改玩法/数值、不许顺手重构、发现新问题登记不动手。
> 基线：main `a40d755`（worktree 分支 `w-g38`）。

## 一、逐项处置清单

| # | 源 | 文案（迁表前） | 处置 | 证据 / 理由 |
|---|---|---|---|---|
| 1 | `core/checkin.py:729` | `第 {fallback} 天未配置，已按第 1 天奖励补全` | **迁表** `checkin_notes_day_fallback` | 对拍 diff 空；prose_keys（静态 33 半角） |
| 2 | `core/checkin.py:779` | `❌ 该表结算失败，已回滚` | **迁表** `checkin_settle_failed` | 对拍 diff 空（宽度 23 ok） |
| 3 | `core/checkin.py:808-814` | `{item}×{count}` 等 4 类 grant 标签 | **复用既有键** `checkin_grant_item|currency|exp|rep` | 同文字段；对拍 diff 空 |
| 4 | `core/checkin.py:850` | `签到汇总` | **迁表** `checkin_summary_header` | 对拍 diff 空 |
| 5 | `core/checkin.py:854` | `═══ {name}（{type}）═══` | **迁表** `checkin_summary_section` | 对拍 diff 空；prose_keys（自由文本） |
| 6 | `core/checkin.py:856` | `今天已签到（不重复发奖）` | **复用既有键** `checkin_already_signed_row` | 同文；对拍 diff 空 |
| 7 | `core/checkin.py:858,871` | `连签天数：{streak} 天 ｜ 进度 {cur}/{total}` | **迁表** `checkin_summary_progress` | 对拍 diff 空；prose_keys（数值占位） |
| 8 | `core/checkin.py:861` | `结算失败，已回滚` | **迁表** `checkin_summary_fail` | 对拍 diff 空 |
| 9 | `core/checkin.py:865` | `今日奖励：{grants}` | **复用既有键** `checkin_daily_reward` | 同文；对拍 diff 空 |
| 10 | `core/checkin.py:867` | `今日奖励：无` | **迁表** `checkin_summary_daily_none` | 对拍 diff 空 |
| 11 | `core/checkin.py:874` | `[连签里程碑达成] {grants}（连签 {days} 天）` | **迁表** `checkin_summary_streak_hit` | 对拍 diff 空；prose_keys（29 半角） |
| 12 | `core/checkin.py:877` | `[月度累计达成] {grants}（本月签满 {days} 天）` | **迁表** `checkin_summary_month_hit` | 对拍 diff 空；prose_keys（31 半角） |
| 13 | `core/checkin.py:889,932` | `❌ 结算上下文非法` | **迁表** `checkin_engine_invalid_ctx` | 对拍 diff 空 |
| 14 | `core/checkin.py:893` | `今天已签到（重复指令，未重复发放）` | **迁表** `checkin_engine_do_idempotent` | 对拍 diff 空；prose_keys（34 半角） |
| 15 | `core/checkin.py:935` | `已补签（重复指令，未重复扣费）` | **迁表** `checkin_makeup_idempotent` | 对拍 diff 空；prose_keys（30 半角） |
| 16 | `core/checkin.py:942` | `❌ 未配置签到表` | **迁表** `checkin_no_config_table` | 对拍 diff 空 |
| 17 | `core/checkin.py:945` | `❌ 签到表不存在` | **迁表** `checkin_table_missing` | 对拍 diff 空 |
| 18 | `core/checkin.py:947` | `❌ 该签到表当前未生效` | **迁表** `checkin_table_inactive` | 对拍 diff 空 |
| 19 | `core/checkin.py:951` | `❌ 当前未开启补签` | **迁表** `checkin_makeup_disabled` | 对拍 diff 空 |
| 20 | `core/checkin.py:961` | `今日已补过/已签到，无需重复补签` | **迁表** `checkin_makeup_already` | 对拍 diff 空；prose_keys（31 半角） |
| 21 | `core/checkin.py:972` | `❌ 本月补签已达上限 {max} 次` | **迁表** `checkin_makeup_limit` | 对拍 diff 空；prose_keys（数值占位） |
| 22 | `core/checkin.py:991` | `❌ 货币不足，补签失败` | **迁表** `checkin_makeup_insufficient` | 对拍 diff 空 |
| 23 | `core/checkin.py:1020` | `❌ 补签需要补签卡或货币，当前无可用通道` | **迁表** `checkin_makeup_no_channel` | 对拍 diff 空；prose_keys（39 半角） |
| 24 | `core/checkin.py:1021` | `❌ 补签失败，已回滚` | **迁表** `checkin_makeup_rollback` | 对拍 diff 空 |
| 25 | `core/checkin.py:1027` | `✅ 补签成功（{channel}）· 只计不补发` | **迁表** `checkin_makeup_ok` | 对拍 diff 空；prose_keys（静态 28） |
| 26 | `gm_commands.py:282` | `_EMPTY_LOG = "（暂无系统日志）"` | **迁表（复用）** `log_sys_empty` | `render_log_page(..., ctx=None)`；对拍（空页）一致 |
| 27 | `gm_commands.py:1506` | `render_log_line` 族（`[{ts}] {idx} /{cmd} …`） | **豁免** | 规范 §三 L50（GM/管理类可保留 `/`）+ §一.3；宽度由数据决定，`log_sys_line` 已在 WARN 冻结集 |
| 28 | `sender.format_tpl12` | TPL-12 | **已迁表（复核）** | 表键 `err_bad_command`（`commands/errors.py:29-30`）——M8 #72 |
| 29 | `world/movement.py:62-70` | `此方向没有通道` 等 | **豁免** | 原因码展示值，同值双源（`movement.py` ↔ `content/map_graph.py:40-41`）；壳模板 `explore_enter_fail` 已入表 |
| 30 | `_TAIL_TIP` 旧式 Tip | `发送'…'` | **已收口** | 尾行 Tip 专项 #60/#62；余 `forge_tree_tail_tip` 表键 |
| 31 | 数据层标签（`_TYPE_CN`/`CHECKIN_FIELDS`/`MAKEUP_CARD_ITEM`） | — | **豁免** | §B 数据映射豁免 |

## 二、零行为变化对拍证据

- 探针脚本：`/tmp/checkin_probe.py`；迁表前输出 `/tmp/checkin_golden_before.json`，迁表后
  `/tmp/checkin_golden_after.json`。
- 覆盖 14 组场景：`checkin_do` 首签 / 幂等 / 非法 ctx；补签 卡成功 / 货币成功 / 已签幂等 /
  月上限 / 货币不足 / 无通道 / 未开启 / 未配置 / 表缺失 / 非法 ctx；`_summary_lines` 全分支电池
  （already_signed / inactive / failed / 多奖励 / 空奖励 / notes / streak_hits / month_hits）；
  `_grant_label` item/currency/exp/rep/未知类型/未知物/None-ctx。
- `diff`：**空**（命令：`diff /tmp/checkin_golden_before.json /tmp/checkin_golden_after.json`）。
- 代码级护栏：`tests/unit/test_batch77_engine_template_migration.py`（6 条，含逐字保真锚点 +
  `tpl_of` 覆盖生效断言）。

## 三、门禁

- `scripts/check_template_width.py`：
  ```
  [check_template_width] 表内 924 条：FAIL 0 / WARN 14 （预算 28 半角=14 全角）
  ```
- 占位符冻结集：新增 `channel` 登记 `tests/unit/_template_frozen_sets.FROZEN_PLACEHOLDERS`
  （`test_template_table_final::test_table_placeholders_covered_by_whitelist` 双向相等通过）。
- 指令/模板一致性（`assembly.router_setup.check_consistency`）：
  ```
  registered_not_whitelisted = []
  whitelist_not_registered  = []
  registered count: 119 | subset of whitelist: True
  ```
- 宽度豁免：11 键（旧排版保真、零行为变化禁止改文案）登记 `template_table.json → meta.prose_keys`，
  理由「逐字保真迁表豁免（批77 · X6）」，对齐批71 `battle_lock_has_other_session` 先例；
  `WIDTH_WARN_ALLOWLIST` 冻结集**未改**。
- 全量回归：`pytest tests/ -q -o addopts=""` → **9073 passed / 0 failed / 10 skipped**
  （基线 9067 passed + 本批新增 `test_batch77` 6 条）。
- 迁移对拍门禁：模板表纯新增 22 键致派生计数位移（`硬差异` 仅 20 条计数位移、删除项 = 0），
  按既有口径重定基线 `47f443f → 1d1fa91`（`test(editor)` 提交），重定后逐字段 diff=0。

## 四、未完成 / 存疑 / 登记不动手

1. **全仓引擎硬编码 `❌/✅` 中文字面量**（批77 扫描，正则命中 ≈288 行；粗筛含注释/docstring，
   未逐条分类）：`core/upgrade.py`（合成/符文/配方/
   特性族）、`core/equipment.py:355,359`、`core/quest_board.py:158,214,216`、`core/craft_paths.py:100-102`、
   `core/combo.py:1426` 等 → 转「引擎文案专项·第三批」，**本批不动手**。
2. **§A #7/#8 forge 残余**：`forge_king` `未获铸造王` / `forge_progress` 拼接串 = reason 片段，
   经表内壳模板 `forge_king_gate` / `forge_material_shortfall` 组装 → 同 #4 口径豁免；
   `forge_material.py:205/214` 两条守卫 message **当前无生产调用点**（仅 `test_forge_material.py` 消费），
   登记待接线或清理。
3. **`checkin_summary_*` 引擎 `message` 路径**：命令层 P0-1 已从 `tables` 重建正文、不消费该 `message`
   （仅 `ok=False` / 补签透传路径使用）；本批仍逐字迁表以闭合 §A #1，**不改命令层行为**。
4. **旧排版保真与规范宽度的张力**：11 个迁表键超 28 半角。若后续用户批准按新规范重排（拆行/精简），
   属**行为变化**，需单独批次 + 用户拍板后再解除 `meta.prose_keys` 登记。
5. **批67 护栏**：`tests/unit/test_editor_batch66_fixes.py` 未改动，全绿。

## 五、变更文件

- `qbot_rpg/core/checkin.py`（迁表渲染 + 新增 import）
- `qbot_rpg/commands/gm_commands.py`（`_EMPTY_LOG` 迁表 + `render_log_page` ctx 形参）
- `qbot_rpg/core/templates/template_table.json`（+22 模板键 / +11 prose_keys）
- `tests/unit/test_batch77_engine_template_migration.py`（新增）
- `tests/unit/test_checkin_commands.py`（checkin_* 键数断言 23→45）
- `tests/unit/_template_frozen_sets.py`（+`channel`）
- `scripts/compare_field_meta_migration.py` + `tests/unit/test_editor_pack_field_meta_migration.py`
  （迁移对拍基线重定 `47f443f → 1d1fa91`）
- `qbot_rpg/web/static/index.html` + 16 个批次串断言测试（页脚 →「批77 · 引擎文案迁表」）
- `CHANGELOG.md`、`docs/消息模板重构/02_遗留登记.md`、`docs/矛盾与待裁决登记.md`、本报告
