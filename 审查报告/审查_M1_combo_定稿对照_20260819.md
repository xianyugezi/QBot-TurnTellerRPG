# 审查报告：M1-批3 连段引擎（combo.py）幻觉与设计缺漏专项·定稿对照

- 审查日期：2026-08-19
- 审查角色：QBot-TurnTellerRPG 代码审查 Agent（按《审查角色_初始化.md》固定人格与纪律，加载 1c1a/1c1b/1c1c/1c2/1c3 + 技能派生定稿）
- 审查方式：read/grep/glob 纯静态只读，零命令执行；运行行为结论均标注「静态推导」
- 审查对象：`qbot_rpg/core/combo.py`（1399 行：六态状态机 / 迁移表 apply_action / 到顶清零 / 配置 ChainConfig / 打断霸体 apply_interrupt / 条件 evaluate_condition）

---

## 一、审查对象与对照基准

| 项 | 内容 |
|---|---|
| 主文件 | `qbot_rpg/core/combo.py`（1399 行） |
| 唯一权威原文 | `/root/docs_archive/RPG框架项目/技能派生系统设计定稿.md`（v1.0，408 行） |
| 细化契约 | `细化_1c1a_连段状态集.md`（126 行）· `1c1b_连段迁移表.md`（38 行）· `1c1c_到顶与清零.md`（182 行）· `1c2_combo配置与打断霸体.md`（166 行）· `1c3_连段测试集.md`（124 行） |
| 质量审计（借鉴） | `幻觉审查_1c1a/1c1b/1c1c/1c2/1c3.md`（细化行号 100% 命中，P0=0，仅 P2 级瑕疵） |
| 关联接线 | `qbot_rpg/core/battle.py`（`_resolve_combo_action`）、`qbot_rpg/core/effects.py`、`qbot_rpg/content/validator.py`、`contract_deviations.md` |

> 前批复核口径：`审查报告/审查_M1_batch3_20260818.md` 的 P0-1/P1-1..5 在 `contract_deviations.md` 已登记修复（2026-08-18）；本报告确认：P0-1（未知键安全失败）已收口（combo.py L474-480）、step_index 回读已修（L196）、battle 打断/霸体/effects 已接线（battle L1155-1202）。本报告不重复计数已修复项，只计**当前仍存在**的问题。

---

## 二、结果总览

| 级别 | 数量 | 说明 |
|---|---|---|
| 🔴 P0（拦截·必改） | 0 | 无伪造机制/规则，六态/迁移表/到顶/打断/霸体主体语义与定稿一致 |
| 🟡 P1（应改） | 6 | 幻觉 2 + 缺漏 4（含 1 项定稿语义冲突需上报） |
| 🟢 P2（建议） | 11 | 幻觉 4 + 缺漏 7（多数已登记递延/随 M5） |
| **① 幻觉/编造项小计** | **6** | H1（P1）H2（P1）H3-H6（P2×4） |
| **② 设计缺漏项小计** | **11** | D1-D4（P1×4）D5-D11（P2×7） |
| ✅ 无问题维度确认 | 13 | 见第八节 |

**结论：条件可合入（P0=0，主体引擎与定稿一致）；P1 清单须本批或随 M5 收口；D2（TC-04 vs TC-32 冲突）与 D11（R-5 环检 vs TC-16）为文档层冲突，按纪律上报不裁决。**

---

## 三、① 幻觉/编造项（combo.py docstring/注释 声称 vs 定稿/细化 实际）

### H1【P1】定稿行号交串错标：eq>max 与 count min/max 的矛盾提示互换了 L355/L356
- **位置**：`combo.py:651` 与 `combo.py:663-666`
- **声称（实际代码文案）**：
  - L651：`eq={cc['eq']} > max_combo（死配置，L355）`（eq>max_combo 标 L355）
  - L664/L666：`count.min/max > max_combo（死配置，L356）`（min/max 与 max_combo 矛盾标 L356）
- **实际（定稿 L355-357）**：
  - **L355** = 「条件 min/max 与 max_combo 矛盾（min>max 永不触发）」
  - **L356** = 「eq 段数 > max_combo」
  - **L357** = 「reset 模式 eq=max（…提示改用 hold 或 min-max）」
- **证据**：定稿 L354-357；`幻觉审查_1c1c.md` L47 逐条确认 L355/L356/L357 语义；`细化_1c1c` L62 亦按 L355-356 分列。
- **定性**：两处标注交叉污染（非凭空编造行号，但**引错的规则**）——审查可追溯铁律（禁止编造/引错行号）被打破；校验逻辑本身正确，仅审计文案错标，误导复核。

### H2【P1】工程收敛②b 声称「cast to 条件不满足 → 降级为源技能 step.from 结算（TC-04）」，实际返回 rejected=True（指令被拒，不结算）
- **位置**：模块 docstring `combo.py:33-34`；函数内注释+返回 `combo.py:1380-1384`
  - L1380 注释：`# 路径 b：cast to 但条件不满足 → 降级源技能（TC-04 强行使用按原技能）`
  - L1382-1384 实际返回：`{"derived": False, "step": None, "auto_replaced": False, "rejected": **True**, ...}`
- **实际行为（静态推导）**：`rejected=True` → `apply_action` L1170-1179 直接返回 `ok=False, rejected=True`（指令被拒、不改连段、不消耗），**原技能并不结算**。
- **声称 vs 实际**：docstring/注释宣称“降级结算原技能”，代码实现“整体拒绝”。函数内注释与返回矛盾，误导所有读码者。
- **连带缺漏**：见 D2（TC-04 的“强行使用按原技能结算”分支未实现；1c3 内部 TC-04 与 TC-32 语义冲突）。

### H3【P2】工程收敛③声称「step.tag 缺省 → 继承源技能 step.from 的 tag」，实际派生缺省分支恒 +0，无源技能标签查询
- **位置**：模块 docstring `combo.py:39-45` 声称；`apply_action` 派生缺省分支 `combo.py:1209-1211`
  - 实际：`elif step is not None: new_state = state`（count 不变），分支内无 `skill_tag(source)` 回查。
- **声称 vs 实际**：①docstring 声称“继承源技能 tag”的行为在代码（静态推导）不存在；②定稿 L75「派生技标签独立配置，**缺省继承原技能标签**」的默认场景未落地（派生步无显式 tag 时恒按 L73“count 不变”处理，不继承源码 combo 标签）。
- **注**：按细化 1c1b 主迁移③ / 1c3 TC-03（“连段保留为 2”）收敛为 +0 亦可接受——但 docstring 描述与代码不一致必须修正，L75 语义何取待主 Agent/策划确认。

### H4【P2】is_armored docstring 声称“两个来源任一为真即霸体”（armor_active + 效果系统免疫矩阵），实现仅查 armor_active
- **位置**：`combo.py:948-953`（docstring）vs `combo.py:954-956`（实现）
- **声称**：数据源 = 战斗层瞬态 armor_active + 「效果系统免疫矩阵 immune_vs:interrupt（1c2 §1.3 字段 19 归口）。两个来源任一为真即霸体」。
- **实际**：实现仅 `if armor_active and armor_active.get(side): return True; return False`，从未读取 `status_state`/免疫矩阵（静态推导）。
- **影响**：若某单位通过免疫状态（immune_vs:interrupt）获得打断豁免，`is_armored` 不会识别；battle 侧 `_armor_active` 仅由 `result.armor`（技能/步 armor 布尔）置位（battle L1190/1202），故免疫矩阵来源实际未接线。请确认归口口径（字段 19 本“为该维度简配布尔”——若约定统一由布尔表达则实现成立，docstring 仍夸大）。

### H5【P2】「hold 到顶普通技能不清零」引定稿 L157，语义真身在 L156（邻位错标，承接细化既有）
- **位置**：模块 docstring `combo.py:55`；apply_action 注释 `combo.py:1222-1223`（“TC-TOP-05 / L157 特例”）
- **实际**：定稿 **L156** = 「hold 蓄力型：到顶后保持最大（不再增长，**普通技能不打断**）」；**L157** = 「→ 法师/盾卫（攒满连段→大招消耗→重新攒）」（职业映射行）。
- **证据**：`幻觉审查_1c1b.md` P2-1 已记录同一处邻位错标（L157 实为 L156 内容）；`细化_1c1c` TC-TOP-05/§1.2 L43-44 亦沿用 L157 引。属承接既有偏移，非本文件自造，但 combo.py 内再次引用时未校正。
- **注**：语义由相邻行兜底，无实质错误；建议后续在细化层统一校正为 L156-157。

### H6【P2】validate_chain docstring 引「§10.5」，定稿无该小节（item 5=倍率提示，非霸体代价）
- **位置**：`combo.py:601`（“armor 步骤无任何代价配置 → 「霸体无代价」提示（1c2 §2.5/§10.5）”）
- **实际**：定稿「十、校验器」为编号 1-8 条目（L348-363），**无 "10.5" 小节**；编号 5 = 「理论最高倍率提示」（L359），与霸体代价无关。
- **证据**：定稿通读；`幻觉审查_1c2.md` 对该引用标注 ✅ 放行（其把「§10.5」视为提示制模式引用）——故本项标 P2 弱疑点：规则语义由 1c2 §2.5 兜底成立，但「§10.5」在定稿无对位小节，属跨文档弱引用，建议改为「1c2 §2.5（对齐「提示不拒绝」模式）」。

---

## 四、② 设计缺漏项（定稿/细化要求但实现没有）

### D1【P1】冷却降级分支缺失：定稿 L203「冷却中派生技自动降级为原技能+连段保留」无实现
- **实现**：`should_reject` `combo.py:879-881` 对 `cooldown_remaining>0` 一律返回「冷却中」被拒；`apply_action` L1117-1126 / `cast_info` L1056-1059 随之拒绝。**无任何“冷却中派生技→降级为原技能（连段保留）”分支**（静态推导）。
- **应有**：定稿 **L203**；细化_1c2 §2.4（L115「特例：冷却中派生技：自动降级…」）；细化_1c1b 主迁移③/⑥；1c3 TC-31/TC-DEF-04/TC-50（TC-50 明确「区别于冷却降级为原技能」）。
- **现状**：`contract_deviations.md` P2 清单已登记（“冷却降级分支…随 M5 或数据包”）。但当前引擎把“直接拒”当终态——与 L203 语义（降级不是拒绝）相悖，须确保 M5 冷却体系落地时实现降级分支，勿保留直接拒。

### D2【P1】TC-04「强行使用按原技能结算」未实现（实现选择“整体被拒”）；1c3 内部 TC-04 vs TC-32 语义冲突，未经仲裁
- **实现**：`combo.py:1380-1384`（cast to 条件不满足 → rejected=True）+ apply_action L1170-1179（整体拒绝，不降级、不结算）。
- **应有**：1c3 **TC-04**（L32 该行）：“连段 1 时选择需'连段 2'的派生技…**强行使用按原技能结算**”；1c3 **TC-32**（L84）同一场景期望“**指令被拒，连段不变**”。
- **定性**：同一场景（连段 1、强行使用需连段 2 的派生技）在 1c3 内两条 TC 期望直接冲突（降级结算 vs 整体被拒）。实现自选 TC-32/TC-50/TC-51 的“原子拒绝”侧，**TC-04 的降级结算分支未实现**，且该冲突未在 仲裁/契约偏差 中登记。→ **按纪律上报不裁决**（主 Agent/策划拍板）。
- **关联**：H2（同根：docstring 声称降级、代码拒绝）。

### D3【P1】combo 校验器空转：validate_chain 全库零调用方 → 定稿 §10 派生专项校验全部不生效
- **实现**：`combo.py:593-673` 定义 `validate_chain`（置顶比特性：死配置 reset eq=max/eq>max/min·max>max_combo/可达性/armor 无代价提示），全库 grep **除定义与 __all__ 导出外零调用**（loader/validator/battle 均不触发）。
- **应有**：定稿「十、校验器」L348-363（死配置 L354-357、可达性 L358、出路 L360）；细化_1c1c §1.3 校验器要点；1c3 TC-16/TC-17/TC-18（环=合法/不可达报错/reset eq=max 提示）。
- **影响**：死配置互拦截（本专项重点核查项）逻辑存在但**不运行**（旧教训 #3“校验器校验空转”重演）→ TC-17/TC-18 在加载路径不可满足。另见 D11（loader 的 skill_chains schema 与 combo 链 schema 也不一致，双重脱节）。

### D4【P1】step_index 写侧缺口：apply_action 命中派生步后从不写 step_index=step.index（前 P1-1 仅修回读半）
- **实现**：`combo.py:1261-1267` 构造 post_state 仅 `count=after`、`step_index=new_state.step_index`（原样保留）；`_apply_delta` L909/915/925 同样保留；全引擎**无任何 `step_index=步索引(≥0)` 赋值**（grep 实证 L179/196/202/909/915/925/1142/1266）。
- **应有**：1c1a §2.1 字段 5「step_index=最近执行步骤索引（≥0），支撑循环互派生形态机（L97-103/L340）」；1c3 TC-15（形态机进度需可持久化/恢复，TC-43 快照含 combo_state）。
- **现状**：`contract_deviations.md` P1-1 仅登记修复 `0 or -1` 回读（已修 L196）；**写侧从未推进**——互派生形态机进度（L340）仍无载体。

### D5【P2】「每节点 ≥2 出路」校验缺失（定稿 §10.6 / L360；1c2 §1.1 字段 6）
- `validate_chain` 无“连段技续攒 + 派生技”两出路检查；1c2 §1.1 字段 6 steps 语义明确“≥1；校验器'每节点 ≥2 出路'”。

### D6【P2】「大招消耗量必填」校验缺失（L399；1c1c §1.3 字段 3）
- 校验器未对“派生步 consume>0/大招消耗量必填”出提示（1c1c L49「大招消耗量必填（校验/内容包约束），杜绝无代价循环」）；consume 字段仅被运行时消费，无配置校验。

### D7【P2】count 条件「min>max 永不触发」死配置未检测（定稿 L355 首条）
- `validate_chain` L654-668 仅比较 `min/max vs max_combo`，**未检测同一条件内 `min>max` 的内部矛盾**（L355“条件 min/max 与 max_combo 矛盾（**min>max 永不触发**）”含该语义）。

### D8【P2】定稿 L75「派生缺省继承原技能标签」未落地（与 H3 同源）
- 派生步无显式 tag 时恒按“count 不变”（+0）处理，从不查询源技能 tag。细化/TC 收敛为 +0 可接受，但 L75 默认继承语义未在代码体现，docstring 又声称已实现（见 H3）——需主 Agent/策划确认取哪侧。

### D9【P2】overflow_compensation 配置无任何消费方（TC-END-07 / L238 大招溢出补偿未接线）
- `combo.py:150-152` 定义 overflow 三键（off/exp_scale/next_start_plus1），全库 grep **仅 combo.py 出现**（无 battle/经验/下场接线消费）。定稿 L238「目标死亡时连段未消耗 → 转少量经验/下场起始 +1（可配）」默认关，但“可配”分支无实现载体，且未在 contract_deviations 登记。

### D10【P2】hold「退出免疫」反馈死代码：L1277 条件恒假
- `combo.py:1277` `if post_state.count >= chain.max_combo and ... consumed > 0`——consume 路径 `_apply_delta(-N)` 后 count 必 <max，故恒假（静态推导）→ “连段消耗 N 段，退出 hold 免疫”消息永不触发（前批 P2-5 已登记，仍存在）。应改为“结算前 count==max 且 consumed>0”。

### D11【P2】连段链 loader schema 与 combo ChainConfig 脱节 + R-5 环检与 TC-16「环=特性」冲突（上报不裁决）
- `content/field_meta.py:114-121` 的 skill_chains schema = `id/name/type/next/actions/effects`（chain_field="next"）；与 `combo.py:256-285` ChainConfig（1c2 §1.1：`id/name/trigger_skill/max_combo/max_combo_behavior/steps`）**字段不一致**——loader 校验无法识别 trigger_skill/max_combo/steps（未知字段/缺字段），且不以 `steps` 建图。
- `content/validator.py:573` 对连段链做 R-5 **环必错**（“连段链成环 A→B→A → R-5”）；但定稿 **L103/L361** 与 1c3 **TC-16** 明确「环=特性，互派生合法，不做无环校验」——若 loader 对组合链图适配环检，将红拦合法互派生。**跨细化冲突，上报不裁决**。

---

## 五、核查项结论（用户点名候选——逐条核实）

| 候选 | 核实结果 | 结论 |
|---|---|---|
| 派生进深上限 | 定稿 L103/L361 明确「配置链任意长度/任意拓扑（环=特性）」，无进深上限要求；`细化_1b` 的 chain_depth=3 系**特效链**深度上限（另一系统，§2.4/§3.5/§8.1），不适用于连段派生 | ✅ **不适用**（无此类定稿要求，避免误报） |
| 到顶先判 eq=max 派生 | `_at_max_derivation`（combo.py:1241-1258/1295-1322）按“达顶后新快照 count==max”先判派生（replace 步）再归零（derived_count_override=0），与 L158/TC-TOP-02/TC-21 一致 | ✅ **已实现** |
| 连段计数跨技能派生保留 | 派生默认 count 不变（L73），TC-03「连段保留 2」语义成立（非派生按技能 tag） | ✅ **已实现**（L75 默认继承未落地见 H3/D8） |
| 打断技自身以无标签处理 | TAG_INTERRUPT/TAG_ARMOR 在 apply_action 走“无标签/普通技能”自断分支（combo.py:1220-1239），TC-INT-04 引擎语义 | ✅ **已实现**（battle 已接线 apply_interrupt，L1188） |
| 冷却降级分支（1c2 §2.4/L203/TC-31） | 无降级分支，should_reject 一律拒 | 🔴 **缺漏**（D1，P1） |
| consume 消耗 | step.consume>0 → -N + 立即退出 hold 免疫（combo.py:1200-1201，L74/L164 落地） | ✅ **已实现** |
| MP 门槛（1a §2.2） | enforce_mp 引擎实现（combo.py:870-878），1a §2.2 数值（普攻0/小技5-10/大招15-25）有据；F-24 已登记递延 M5（contract_deviations L52） | ✅ **已实现+已登记** |
| 条件简写语义（target_hp_pct） | `_slot_bound`（combo.py:428-437）裸数字一律转 eq；1c2 §1.4 字段 27 仅 min/max（无 eq）——简写未定义/语义歧义，已登记 P2（contract_deviations L110） | ⚠️ **P2 已知**（建议显式禁止或按 max 收敛） |
| validate_chain 死配置互拦截 | 检查逻辑存在但**零调用方**（D3）；reset eq=max（L357）✓、eq>max 与 min/max 标注互串（H1）、内部 min>max 未检（D7） | 🔴 **缺漏+错标**（D3/D7/H1） |

---

## 六、P0/P1/P2 清单汇总

### 🔴 P0（拦截·必改）：0
无。

### 🟡 P1（应改）：6
1. **H1** combo.py:651/664/666 定稿行号交串错标（L355↔L356），审计文案误导复核 → 改正确 L 标注。
2. **H2 + D2** 路径 b“降级为源技能结算”声称与实现（rejected）相悖；TC-04 vs TC-32 冲突未仲裁 → 修正 docstring/注释 + **上报仲裁**，明确“强行使用派生技”终态语义。
3. **D1** 冷却降级分支缺失（L203/TC-31/TC-50）→ M5 冷却落地时实现降级，勿以“直接拒”为终态。
4. **D3** validate_chain 零调用 → 接入 loader/校验链路（连带 D5/D6/D7 随之生效）。
5. **D4** step_index 写侧缺口 → apply_action 命中步写入 step_index=step.index（TC-15/TC-43）。
6. **D11**（上报）连段链 loader schema 与 combo ChainConfig 脱节、R-5 环检 vs TC-16 「环=特性」冲突 → 上报不裁决，定稿/L56 口径对齐。

### 🟢 P2（建议）：11
H3 · H4 · H5 · H6 · D5 · D6 · D7 · D8 · D9 · D10 · （target_hp_pct 简写，已登记）。多数已随 M5/数据包登记（contract_deviations L110），建议本批至少修 H3/H4/H5/H6 文案与 D10 条件。

---

## 七、静态推导说明
本报告全部运行行为结论（派生分支、rejected 短路、consume 后 count 必<max、step_index 从不写入、overflow 无消费等）均为**阅读代码路径的静态推导**，未运行任何脚本（环境禁止）。涉及跨模块（battle/effects/content/loader）调用关系以 grep/read 实证为准。

---

## 八、无问题维度确认（✅ 通过 / 核实为不适用）

1. **六态状态机**：idle/in_combo/derivable/deriving/at_max_reset/at_max_hold 枚举与 derive_state 推导序（deriving→idle→at_max→derivable→in_combo）符合 1c1a §1.0-§1.7；六态自身为细化收敛（1c1a §1.0 表，幻觉审查_1c1a 全量引对）✅
2. **迁移表 ①-⑧ + A1-A5**：①普攻+1 ②连段技+1 ③派生 ④到顶reset ⑤到顶hold ⑥被拒 ⑦战清 ⑧防御/道具不打断（battle 短路）均按 1c1b 落实（①的“普攻=连段技”为细化补白，幻觉审查_1c1b P1-1 已标注，属承接非本文件编造）✅
3. **到顶 reset/hold + 先判 eq=max**：见核查表 ✅
4. **派生四模式 replace/enhance/append/unlock**：与定稿 §3.2 四模式对位（1c2 §1.2 字段 12）✅
5. **打断三条件窗口**：正在连段 + 非霸体 + 非 hold 免疫（1c2 §2.2）；引擎级 apply_interrupt 已实现，battle 已接线（L1188）✅
6. **霸体 armor**：技能/步 armor → armor_active 使用期间免疫（battle L1190/1202），TC-ARM-04（喘息技不免疫）语义成立（H4 仅指免疫矩阵来源未读，布尔口径下成立）✅
7. **清零事件全集/不清零豁免**：1c1c §3.1 全集 + 豁免清单（防御/道具/被拒/逃跑失败/被控/hold 到顶）全部对齐 ✅
8. **consume 消耗 + 退出 hold 免疫**：落地且语义正确（D10 仅反馈消息死代码）✅
9. **职业预置**：剑士4/reset、刺客2/reset、法师5/hold、盾卫3/hold 与定稿 L170-173 逐字一致 ✅
10. **MP 门槛**：enforce_mp 引擎实现 + F-24 登记递延（1a §2.2 数值有据）✅
11. **条件对象六字段 + 未知键安全失败 + 复合 + marks 接线**：evaluate_condition 未知键→False（TC-13）、and/or/not 任意拓扑、印记子句经 battle marks_lookup 接线（前 P0-1 已收口）✅
12. **打断技自身以无标签处理**：见核查表 ✅
13. **细化节号引用真实性（非行号级）**：1c1a §1.0-1.7/§2.1-2.3、1c1b 主迁移+A、1c2 §1.1-1.5/§2.2-2.5、1c3 覆盖矩阵与 TC-01..52 均在对应细化文档真实存在；细化→定稿行号经 5 份幻觉审查（1c1a 90 处/1c1b 60 处/1c1c 60 处/1c2 40 处/1c3 90 处）全量命中无编造 ✅（H1/H5/H6 为 combo.py 自身 L 级引用瑕疵，见上）

---

## 九、结论
- **状态：条件可合入（需随批收口 P1）**——P0=0、机制与定稿主体一致；六态/迁移表/到顶/打断/霸体/consume/MP 门槛等核心判定经静态推导可追溯且与定稿对位。
- **P1 收口清单**：H1 行号标注纠正；H2/D2 语义统一 + 上报仲裁（TC-04 vs TC-32）；D1 冷却降级随 M5；D3 validate_chain 接线；D4 step_index 写侧；D11 loader schema/R-5 上报仲裁。
- **P2**：H3-H6 文案/引用、D5-D10 补齐校验与反馈、target_hp_pct 简写收敛（已登记）。
- **上报项（不裁决）**：D2（1c3 TC-04 vs TC-32 冲突）、D11（R-5 环检 vs 定稿 L103/L361+TC-16 环=特性）——交主 Agent/策划拍板。

---
*审查完毕：逐条回查 定稿 408 行 + 细化 1c1a/1c1b/1c1c/1c2/1c3 全量；幻觉项 6（P1×2/P2×4），缺漏项 11（P1×4/P2×7），P0=0。*
