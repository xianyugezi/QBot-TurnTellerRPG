# M13 批0 路0B · 6b 职业库摸底报告

> 依据：docs/细化/细化_6b_职业库与变换引擎.md（409 行，v1.0）
> 现状核对：field_meta / validator / loader / 4f 注册契约 / battle 快照 / core.battle / 装配层 / content/test_demo（全部行号经 grep/read 验证在文件范围内）

## 〇、结论摘要
- 已就绪：4f RUL-03/04+B7 注册契约全套；快照回合边界机制（SN-1）；世代重绑定（SN-2）；dispel 通道（F2 路C）；stat_modifier 通道（TRF-6）；combo_preserve tag（TRF-4）；growth 升级消费管线；专项校验器+死配置红拦模式；指令注册+白名单模式；loader 三处同步门禁。
- 核心缺口：39 配置字段零登记；V1~V8 全缺；5 态状态机与 F1/F2/F3 引擎全缺；transform_state 7 字段未落快照；变身/还原指令未注册；ctx[jobs]/[skills] 未注入；berserker fixture 缺失。

## 一、契约 39 字段逐项核对（总：0 已实现 / 3 部分 / 36 缺口）
**顶层 11（#1~11）**：全部缺口。recommended_newbie 消费侧已实现（register_commands L218/L234/L338），但配置侧零登记；id/name 语义被 ctx job 解析间接消费（context._job_name L191-212）；resource_axes 依赖 6c；weapon_types 依赖 4b。
**growth 9（#12~20）**：部分实现（消费侧）：levelup.py L59-76 growth 注入、L173-176 LVL-06 逐级累加；配置侧零登记。
**transform 段 11（#21~31）+ state_policy 3（#32~34）**：全部缺口，零实现零登记。
**技能挂点 4（#35~38）**：job_restrict 部分实现（basic_commands._job_visible L1406-1412 装配过滤）；job_form/revert_form/derive_only 零实现。
**链挂点 1（#39 job_scope）**：缺口（field_meta skill_chains_fields L484-491 无此键）。

## 二、快照侧 transform_state 7 字段（T1~T7）
缺口。battle.py start L890-933 无 transform_state 段；to_snapshot L1748-1780 深拷贝不含；data/battle.py BattleSnapshot L46-75 dataclass 无字段（含既有登记注记 L13-20：dataclass 与 1g3 dict 快照双轨）；_settle L742+ 无 SN-4 清零。
挂载点建议：start() 初始化建段；to_snapshot 深拷贝自动携带；from_snapshot L1804-1864 透传恢复；_settle 增清零。

## 三、5 态状态机（S1~S5）
缺口（新建单位子状态机，区别于 battle 主 8 态 L123-130）。接入点：end_turn L1653-1693 ⑥ tick（effects.tick_turn_end L1209-1265）之后挂 S3 递减/S5 冷却递减/F2 自然+驱散还原；do_action L1034-1086 前挂 C1~C4 触发闸。

## 四、3 流程
- F1：缺口（触发技效果先结算 TRF-1 可走现有技能结算通道；四动作需新引擎）。
- F2：部分基础（路C 被驱散通道 effects.py L1520-1535 已实现；路B revert_form 钩子与路A tick 还原需新实现）。
- F3：部分基础（快照回合边界机制 to_snapshot L1757-1761、世代重绑定 snapshot_resume L178-262 就绪；transform_state 读写本身缺口）。

## 五、V1~V8 校验
全部缺口。可复用模式：V1/V2 引用存在性仿泛型 R-4（_check_ref L1699）；V3 形态环仿 _check_chain_cycle L1720；V4 battle+revert 矛盾仿 _check_dead_config L1570 红拦；V5 state_policy 枚举仿 FieldMeta enum 类型；V7 黄提示仿 Y 系；专项整体仿 _check_enemies L609+ 模式。

## 六、TC-01~18
全部缺口（无测试载体；test_demo manifest L6-31 未声明 jobs/skills/skill_chains，全目录 grep 零命中 6b 相关字段）。

## 七、ADR 核对（5 条，任务描述称 6 条——文档 §0.3 实际登记 D-01~D-05 共 5 行，以文档为准）
D-01 单例 transform 段：无实现，无冲突。D-02 双轨：status 通道现成（effects.apply_status L451+），联动缺口。D-03 计时挂回合 tick：tick_turn_end 现成挂点契合。D-04 battle+revert 红拦：死配置红拦模式现成可复用。D-05 被驱散结算时点：dispel 立即清状态已实现（L1530-1535），还原延迟到下一回合结束 tick 的钩子需新实现。

## 八、批4~批7 实施切法建议
**批4 路4A·数据层（无依赖）**：field_meta 新增 jobs 模块（39 字段全登记）+ NAMESPACES job_lib + loader FIXED_REGISTER_ORDER（L46）/ _KIND_FOR_MODULE（L150-186）增 jobs/skills；validator 新增 _check_jobs 专项（V1~V8 红/黄二分）；test_demo 增 jobs.json/skills.json/skill_chains.json 最小 fixture；TC-18 坏包 9 断言（复用 _check_dead_config/环检测模式）。
**批5 路5A·指令与 ctx 接线（依赖批4）**：context.py make_context 注入 ctx["jobs"]（_table_from_registry kind='job' / _module_raw_of 模式 L470）+ 修复 ctx["skills"] 空表问题；register/技能指令消费 transform 展示；parsers.py 默认白名单（L106-146）补变身/还原指令名。
**批6 路6A·transform 引擎核心（依赖批4/5）**：新建 core/transform.py——5 态子状态机 + F1 四动作（施加形态状态/切 job_form/技能位重排 SH-1~5/state_policy 三键）+ F2 三路归一 + cooldown tick；battle.py do_action 前 C1~C4 闸、end_turn ⑥ 后挂 transform tick、_resolve_combo_action L1191+ 识别触发技/revert_form 技；battle_commands 按 CommandSpec 模式注册 /狂暴 /平息战意。
**批7 路7A·快照与内容包（依赖批6）**：battle.py start/to_snapshot/from_snapshot/_settle 落 transform_state 7 字段（写/恢复/SN-4 清零）；装配战斗接线（battle_commands L928-956 注入 transform 引擎）；berserker 完整六文件 fixture（skill_set/derive_chains/job_form/revert_form）；TC-01~17 模拟器用例（含 TC-13 round-trip、TC-16 热重载旧局旧配置走 RSM-04）。
**交叉依赖**：6c 资源轴（怒气 energy_cost）为 F1 ② 资源校验依赖；批4~7 期间 fixture 暂以 mp-only + energy_cost 字段预留放行，不做硬拦（对齐 6b 附·未定稿依赖登记第 2 条）。

## 九、contract_deviations（核对口径差异）
① 任务描述称 6 条 ADR，细化_6b §0.3 实际登记 5 条（D-01~D-05，L55-59）——以文档为准。
② data/battle.py BattleSnapshot dataclass 与 1g3 dict 快照双轨为既有登记差异（L13-20 审计注记）——transform_state 新增须同时落 dataclass 与 dict 或登记双轨保留，不可只改一处。
③ ctx["jobs"] 从未注入（context.py 零命中），无 jobs 包时注册兜底「新手」（register_commands L469）与 RUL-04 缺省职业意图一致降级，无冲突。
④ loader 无 skills/jobs kind，context.py L1279 注入 skills 恒空表——6a/6b 落地前 /技能 展示口径为空列表，属既有缺失非契约偏离。
