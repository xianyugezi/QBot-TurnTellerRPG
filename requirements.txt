# QBot-TurnTellerRPG 依赖清单（规则 8：锁定版本号）
# 生成自当前 venv（pip freeze），2026-08-25
# M0 核心为纯逻辑包（qbot_rpg），仅 storage 层依赖 aiosqlite；
# NoneBot 壳层依赖随 M4 壳层接线补充。

# ---- 运行时 ----
aiosqlite==0.22.1
# M12 编辑器（细化_5a ED-01：Web 内容编辑器宿主；api.py 惰性 import 缺省可导入）
fastapi==0.115.14
uvicorn==0.34.3

# ---- 开发/测试 ----
pytest==9.1.1
pytest-asyncio==1.4.0
# M6 批7·路B（细化_M6_质量门禁 D7 LNT-03）：ruff/mypy 锁版本（与 pyproject dev 组宽松范围并存）
ruff==0.16.5
mypy==2.3.1
# M6 批7·路A（D7 细化_M6_质量门禁 COV-01）：覆盖率真实核算依赖，锁版本（对齐规则 8）；
# 口径 = qbot_rpg/core + engine + content 各自 ≥80% 行覆盖（总纲 ADR-04），见 docs/verify/coverage_latest.txt
coverage==7.15.4
