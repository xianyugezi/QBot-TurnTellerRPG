# 记录.md

> 规则 1 通用工作记录：记录每次完成的工作、下一步工作、未完成工作。
> 最后更新：2026-08-27

---

## 一、已完成（最近优先）

### 2026-08-27 · M6 实现层批1 完成（三引擎 + 基础指令，commit 720d7ee）
- **路A 三引擎实装**（35 规则全落地，空壳消除）：core/levelup.py（LVL-01~12，经验入账/跨级/回满/SP 联动/自由加点）、inventory.py（INV-01~11，堆叠合并/拆行/绑定拒移/数量截断/格数上限/到期惰性移除）、equipment.py（EQP-01~12，穿戴校验链/互斥/后装覆盖/卸装回包/聚合重算/互斥环加载拦截/战斗内禁穿脱）——操作对象=玩家状态 dict（对齐 checkin/quest 既有引擎模式），配置构造器注入+默认值兜底（exp_curve=100×lv/level_cap=45/sp_per_level=1）
- **路B 基础指令**：register_commands.py（/注册 REG-01~06，名字校验/缺省职业/重名/幂等/建号数据生成）+ status_commands.py（/状态 STT-01~05 面板五区）+ shortcut_commands.py（/快捷解绑 /快捷列表 SHC-01~05）+ parsers 白名单补「注册」+ basic_commands /装备 换真实引擎适配层 EquipmentEngineAdapter（EQP-12）+ cmd_help 别名显示替换（SHC-04/TC-17）
- **主 agent 收口修复**：status_commands 3 bug（_player_fields 忽略 dict 形态 ctx["player"] / _final_attrs bonus 恒非空吞 resolver 兜底 / prefix_line 空称号格式 - -）；补 test_shortcut_commands.py + test_equip_adapter.py（TC-SHC/TC-EQP 适配层）
- **verify_m5 翻转 11 条 DELAYED**（4f TC-01/02/03/04/06/07/09/10/17/22/23 → pytest 承载；剩 2 条=3d TC-16/17 锻造）——踩坑：execute_code 批量替换 read_file(limit=500) 截断 verify_m5 670→500 行（丢 main 块），git checkout 恢复后重新安全替换
- **验收**：全量 pytest 全绿（1 skip）+ verify_m5 全绿（79 承载+2 DELAYED）+ G0 架构门禁绿
- **下一步**：批2 D2 幂等事务三件套（message_id 幂等键/购买入 repo.tx()/per-player 串行队列）

### 2026-08-27 · M6 批1 dsh 审查 + 修复（commit 86f829e）
- **批1A 三引擎审查**（审查_M6实现_批1A_三引擎_jspace.md）：P0=0 / P1=2 / P2=9，35 规则+18 TC 全落地确认
- **批1B 三指令审查**（审查_M6实现_批1B_三指令_jspace.md）：P0=0 / P1=3 / P2=11，REG/STT/SHC 全部落地确认
- **P1 修复（3）**：①equipment aggregate_bonus 同 item_id 多件双计 → 穿戴行引用机制（_worn_refs/_resolve_worn_row，equip 登记/unequip 移除，精确聚合）②穿装序号按存储序 vs /背包 时间倒序错位 → 适配层 _sorted_inventory 按 acquired_at 倒序 ③/注册 固定子词吞名（/注册 自动 战士 → 注册名变战士）→ fixed_subword 非空一律 TPL-12
- **P2 修复（9）**：印记区 imprints_line（RUL-13/STT-01⑤）/ /帮助 引导版按 4f B6 改注册·状态·背包+豁免前置 / shortcut_max=0 显示不限 / /快捷列表 固定子词 TPL-12 / 命令壳裸吞异常加日志 / _slot_name 支持 slots.json 包装形态 / target 字段缺省整行降级 / exp 归一 / add_item new_rows 键 / D1 TC-LVL-04+TC-INV-06 数值勘误（P2-09）
- **登记项**（潜伏/依赖后续）：P1-2 强化字段丢失（M9 前置数据模型解决）、P1-3 适配层 dict 行接缝（装配层批次7 归一）、P2-01/02/03/04/05/06/07/10/11
- **回归**：全量 pytest 全绿 + verify_m5 全绿（79 承载+2 DELAYED）

### 2026-08-27 · M6 实现层批2 完成（幂等事务三件套，commit 13eb2f8）
- **processing.py**（D2 IDEM+POOL 件套，281 行）：指令处理入口 process_message（idem_claim 只查不插 → 未命中入队 → 队列内单事务 tx.idem_exists 权威判定 + 业务写 + write_idem_key 同事务 → COMMIT → 发送；命中幂等返回不入队；失败整单回滚无孤儿键；私聊 group_id "dm" 哨兵）+ PerPlayerQueue（per-player asyncio.Queue + 单消费者，同玩家 FIFO 按序、异玩家并行、队列空消费者自行退出、任务异常捕获不阻塞后续、close 取消）
- **shop_tx.py**（D2 SEG 件套，367 行）：buy_in_tx/buy_in_open_tx 购买结算整体入 repo.tx()（事务内读玩家行/库存 → 6 步校验链 → 扣减 → tx.upsert_player 写回 + 库存 CAS + ok 才 write_idem_key → COMMIT，失败整单回滚）；world_stock 扁平键 {shop:item: n} 双向转换
- **battle_boundary.py settle_exit_idempotent 实装**（IDEM-8 / 批5A P1-1 收口）：幂等键 (player_qid, "settle:{kind}" 入 command, message_id) + group_id 哨兵兜底；idem_claim 查重 → 单事务 delete_session（连段/战斗资源随会话删除清零）+ write_idem_key → 已结算 False
- **测试**：test_idem_processing（TC-IDEM-01~06 + 补充）+ test_per_player_queue（TC-POOL-01~04 + 排队窗口）+ test_shop_double_pay（TC-SEG-01~05 + 边界 4 例 + 幂等重发）= 26 例全绿
- **主 agent 收口**：修 test_per_player_queue 的 flakiness（「for _ in range(2000): sleep(0)」限次忙等在全量负载下偶发失败 → 改 _wait_until 带超时持续等待，5 处）；多次全量回归全绿
- **验收**：全量 pytest 全绿 + verify_m5 全绿 + G0 架构门禁绿
- **下一步**：批2 dsh 审查 → 批3 D3 热重载接线（P0-1 续战世代绑定修复）

### 2026-08-27 · M6 批2 dsh 审查 + 修复（审查_M6实现_批2_幂等三件套_jspace.md）
- **审查结果**：P0=0 / P1=2 / P2=12；三件套结构性要求全对齐（幂等载体=入口/只查不插/同事务写键/事务内读/双扣两路径）
- **P1-1 修复**：shop_tx `_ctx_inventory_to_player` 同 item_id 多实例回写计数膨胀（A2+B3 合并 5 再留 B3 = 8）→ 合并总量归并首实例、其余移除（总量守恒）；补回归测试
- **P2 修复（5）**：P2-2 幂等重放 ok 语义统一（shop_tx 命中 ok=False+idempotent=True，对齐 processing）+ P2-5 消费者异常/取消 future 兜底 resolve（防 await fut 永久挂起）+ P2-7 测试 sleep→事件同步 + P2-12 补纯货币路径并发用例（无限购商品+余额仅够一次 → 货币恰扣一份）
- **登记批次6/7 必办接缝（9）**：P1-2 线上 /购买 未接事务（cmd_buy 闭包替换 buy_in_open_tx 或走 process_message）+ P2-1 settle 嵌套张力（/逃跑 接线决策二选一）+ P2-3 cleanup_idem_keys 无生产调度 + P2-4 队列无超时（压测补 wait_for）+ P2-6 handler ok=False 须零写 + P2-8 close 未消费队列 + P2-9 dm 哨兵收敛一处 + P2-10 world_sold_out 持久化 + P2-11 world_stock CAS 版本交互
- **回归**：批2 测试 28 例全绿 + 全量回归全绿

### 2026-08-27 · M6 实现层批3 完成（热重载接线 + 续战世代绑定，commit 3abbac2 + a957873）
- **路A WIR 件套**（热重载四项接线）：GmBackend.reload_content（WIR-07 = watcher.reload 同一管线，{ok,summary,failures}）+ 权限分级（WIR-06 机主全部/GM 重载备份恢复/无权限静默）+ ReloadResult 人话翻译（reload_result.py TPL-15~18 四模板逐字 + 禁词扫描）+ ValidationReport 红黄计数（count_errors/group_by_module）+ backup_snapshot 激活（RSM-05）+ resolve_or_degrade 统一降级入口（WIR-14）+ schedule_polling 调度包装（WIR-04，零 apscheduler）
- **路B RSM 件套**（P0-1 续战世代绑定）：battle 快照增 registry_generation（RSM-02/F-RSM-01）+ from_snapshot 增 registry 参数透传（RSM-03）+ 续战入口世代重绑定（snapshot_resume pick_registry_snapshot/rebind_registry_for_snapshot，watcher N=2+backup 双口取档，fallback 降级 + RSM-09 自检告警）+ 存储 payload 内嵌世代往返（RSM-06，无新列）
- **主 agent 收口**（两路迭代上限截断）：补 4 个测试文件（battle 世代/重绑定/翻译/wiring = 45 例）+ 修 M43 threading 违规（gm_commands 线程适配 → 返回 None 由上层转提示）+ 修 reload_content 返回值契约 bug（dict vs ReloadResult）+ no_change 视为成功 + 文档回填（router WIR-18 / m5 WIR-20 / 细化_3d TPL-15~18）
- **验收**：全量 pytest 全绿 + verify_m5 全绿 + G0 架构门禁绿 + M43 全绿
- **下一步**：批3A/3B dsh 审查 → 批4 D4 内容包冒烟

### 2026-08-27 · M6 批3A dsh 审查 + 修复（审查_M6实现_批3A_wir_jspace.md）
- **审查结果**：P0=0 / P1=2 / P2=8；四项接线组件级齐全/权限矩阵一致/四模板逐字/红黄计数出口落地
- **P1 修复（2）**：F2 TPL-17 连续失败 N 恒 1 → reload_content 透传 consecutive_failures；F1 同步桥接 asyncio.run 与 watcher asyncio.Lock 跨循环绑定冲突（3.9 必失败、3.11 恰能跑）→ except RuntimeError 收窄只吞事件循环冲突、其余 re-raise；批次7 装配改 await 为正解
- **P2 修复（4）**：F3 手动 /重载 无变更「0 个模块变更生效」误导 → _commit_success 空变更置 no_change=True 走 TPL-18；F4 红黄计数日志三处格式不统一 → 统一收敛 _log_report_counts；F5 补 caplog 红黄计数断言测试（WIR-12/F4 验收③）；F7 resolve_or_degrade Mapping 分支畸形防护（(None,True) 不抛）
- **登记批次6/7（4）**：F6 WIR-13 对照公式补 FIXED_REGISTER_ORDER+manifest 两段 + verify_m6.py 建（D8 执行）+ F8 run() while+sleep 标注 deprecated（仅用 schedule_polling/poll_once）+ F9 四项接线①②壳层注册（启动调 start/apscheduler 注册）+ F10 TPL-18 自动提示发送出口（+限频不占玩法配额）
- **回归**：批3A 相关测试 90 例全绿 + 全量回归全绿

### 2026-08-27 · M6 批3B dsh 审查 + 修复（审查_M6实现_批3B_rsm_jspace.md）
- **审查结果**：P0=0 / P1=2 / P2=6；RSM 三件套主体落地无缺失、半套禁绝四路解析绑定完整（effects/marks/combo/defenses 均绑 rebind registry）
- **P1 修复（2）**：RSM-01 世代重绑定端到端验收无测试（resume_from_snapshot watcher 主链路/半套禁绝断言缺失）→ 补 4 例端到端测试（exact/none/fallback/skipped 四态 + registry 透传工厂）；RSM-02 none 降级路径（世代滚出 N=2 窗口=半套复发最高频）不告警 + degraded=False → 补告警 + degraded=True（与 fallback 可观测性对齐），skipped 分支保持不告警零影响
- **P2 修复（4）**：RSM-03 Registry.from_snapshot 畸形档 try/except 降级 + RSM-04 snapshot_registry_generation Mapping 防护 + RSM-05 battle start 世代类型防护回落 0 + RSM-07 backup_snapshot 异常补 debug 日志
- **登记批次6/7（2）**：RSM-06 resume 重建引擎不注入 enemy_ai → AI 侧半套登记（装配层按 rebind 档重建 ai_action_lib）+ RSM-08 测试质量小项（turn_boundary 名实相符补 interrupt 路径 / 事件循环模式）
- **回归**：重绑定测试 25 例全绿 + 全量回归全绿 + verify_m5 + G0 全绿

### 2026-08-27 · M6 实现层批4 完成 + 审查修复（内容包冒烟，commit f21de92 + ca44444）
- **路A 冒烟脚本**：scripts/e2e_m6_smoke.py（四步断言矩阵注册门槛→锁定→攻击→结算 + validator 四件套 + 确定性重放 33 断言 + 零 NoneBot）+ test_e2e_m6_smoke.py pytest 包装（子进程 exit0/确定性/ast 零 NoneBot）
- **路B 收尾**：verify_m0 missing_mod 空断言→软放行真实断言（SMK-13，Y-6 statuses + 未声明 npc 不加载 + potion 挂载）+ test_pack_fixtures_matrix（四件套矩阵 TC-SMK-08~12）+ content/ 五档包（demo_blank/lv15/lv30/lv45/full，PCK-01~12）+ docs/verify/m6_smoke.md 归档模板（F-01~05）+ run_all_tests m6 接入声明（置位归 D8）
- **主 agent 收口**（路A 迭代截断）：亲自实现冒烟脚本，修 5 个引擎调用问题（parse_command/回合时序/终局判定/命中 rng/溢出快照）
- **审查修复**（P0=1/P1=2/P2=10）：P0-1 攻击一轮一条死断言（BattlePipeline 缺 sender + 传 dict + except 全吞）→ 按 verify_m5 ④b 重写（start→player_act→dispatch_round，mock.send==1，独立新引擎防状态错乱）；P1-1 missing_mod Y-6 断言补消费；P1-2 五档包 demo_lv15 装配战斗（一处覆盖 SMK-10+PCK-10）；P2-4~7 结算 victory 标志/锁定数值 hp=120/注册后四指令统一/重放差异诊断
- **验收**：冒烟 33 断言全绿 + 全量 pytest 全绿 + verify_m0 + verify_m5 全绿
- **下一步**：批5 D5 故障注入（46 规则 / 26 TC）



### 2026-08-27 · M6 细化需求完成（总纲 + 8 份子细化文档，12 份审查报告 P 项全量落地）
- **产出 9 份 细化_M6_*.md**（docs/细化/）：①接线闭环总纲（38 规则/13 用例，M6 唯一范围+verify 两段式+8 份子文档注册+P 项映射表+ADR-01~13）②D1 三引擎与基础指令（51/29）③D2 幂等事务三件套（23/15）④D3 热重载接线含 P0-1 续战世代绑定（31/21）⑤D4 内容包冒烟（29/21）⑥D5 故障注入（46/26）⑦D6 测试体系强化（40/30）⑧D7 质量门禁（38/30）⑨D8 verify门禁与承接（34/21，聚合 172 TC）
- **12 份 dsh 审查报告 P 项全量归口**：P0×12 全承接、P1×60 归口 8 子文档、P2×62 逐批收纳；4 份子 agent 自报均经主 agent 独立复核（自检脚本 + 代码行号抽查命中）
- **四源仲裁落地**：M6=接线闭环（F5/F6/G1-G4）+verify_m6 两段式；5a/5b 归 M12、4c/4d/4e/6 归 M11/M13
- **跨档共享决策 ADR-01~13**：内容包冒烟≠服务器冒烟/旧 schema=容忍加载/五档包位置/覆盖率口径 core+engine+content 各自≥80%/幂等载体=指令入口/购买并发不依赖世界锁/伪托 D5 修正等
- **D4 曾一次子 agent 基础设施中断**（会话存储写入失败未落盘），重派成功
- **验收**：9 份全部过 refinement-doc-selfcheck.py（规则/用例/字段声明对账 OK）+ emoji 纪律（仅 ✅❌）
- **下一步**：M6 实现层（按总纲 §六 8 批：批1 三引擎+基础指令 → 批8 verify_m6 收口），每批 2 路并行

### 2026-08-27 · M5 实现层完成（6 批 12 任务全落地，六门禁全绿）
- **6 批 12 任务全部实现完成**：批0 M5-01 前缀接线+ M5-02 校验器 / 批1 M5-03 骨架+玩家行动 01-06 + M5-04 技能状态行 07-09 / 批2 M5-05 怪物行动 10-14 + M5-06 结算连段 15-22 / 批3 M5-07 开始结束明细 23-25 + M5-10 全仓 emoji 降级 / 批4 M5-08 battle_commands 一轮1条接线 + M5-09 背包筛选链+探索壳 / 批5 M5-11 emoji 登记表 + M5-12 verify_m5 门禁
- **新增实现**：qbot_rpg/commands/prefix_wiring.py（前缀消费接线 220 行）+ battle_commands.py（BattlePipeline 一轮1条 795 行）+ explore_commands.py（/进入 /休息 壳）+ basic_commands cmd_bag_filter（筛选链）+ data/emoji_sanitize.py（strip_icon_emoji）+ message_prefix 校验器 MP-1/MP-2 + verify_m5.py（658 行，81 TC=68 承载+13 DELAYED）+ 全局图标登记表.md
- **M5-08/11 曾两次 502 重派**（scnet 瞬时故障，确认未落盘后重派成功）
- **实现中排掉的坑**：G0 架构门禁（strip_icon_emoji engine→core 违规 → 移 data/ 层）；并行写 battle_render.py 冲突（patch-only 约定 + 主 agent 收口对齐）；weather 空模板断言/dialog 漏同步断言/页脚单页不输出断言
- **验收**：全量 run_all_tests 六门禁全绿（M0-M5 + G0 ARCH-OK，2138+ passed）
- **M5 实现完成，进入 dsh 审实现**（用户要求"实现后记得检查"）

### 2026-08-27 · M5 细化规划（shared_contract + batch_plan）+ 细化审查 3 批 + emoji 裁决
- **产出 docs/m5_shared_contract.md**（D1-D5 契约：铁律 11 条 / message_prefix 7 字段 / TPL+BREP 模板注册表 / 已实现-待实现缺口矩阵 / 81 TC 矩阵）+ **docs/m5_batch_plan.md**（4 批 9 任务 M5-01~09）
- **M5 实现缺口分析**：battle_render.py 是最大缺口（BREP-01~25 骨架未实装）；prefix_render/list_render/sender/panel_render/cmd_view M4 已实装勿重做；message_prefix show_on_system/per_channel 消费、筛选链、emoji 静态检查、verify_m5 待做
- **细化审查 3 批**（dsh，报告落盘 审查_M5细化_批次1-3_jspace.md）：批1 D1-D2（P0×0+P1×2+P2×7）/批2 D3-D4（P0×0+P1×1+P2×7）/批3 D5（P0×2+P1×4+P2×5）——共 19 项全处理
- **细化审查抓到的重要契约缺陷**：IF01 render_prefix 契约签名与实现不符（format/group_name/job 错）；ActionOutcome 字段编造（无 rating/damage/outcomes，实际 final_damage/raw_damage/target_hp）；BREP-02 丢 HP 后缀/BREP-09 丢 /最大（P0×2）；校验器规则误标 3h V9~V12（实为【前缀】§九）；emoji 白名单三方冲突
- **用户拍板「不用 emoji」**：除 ✅/❌ 功能性标记 + 排版符号（| → × / 「」【】）外，渲染输出不含任何 emoji——数据型功能图标（物品/NPC/商店/天气/印记 icon、GM 🚫、单机 ⛩️）一律降级纯文本；m4_shared_contract §2.2 豁免条款作废；规划_路3 L171 登记表作废；M5-08 含全仓渲染输出 emoji 降级 + 静态检查（命中=0）
- **契约修正全部落地**：m5_shared_contract/m5_batch_plan 按 3 批报告修（真实签名/真实字段/模板文本/军规5/16 行折叠锚点/TC 映射表/筛选分页断言/空列表/✅❌ 位置约束）+ 5e 行号 L171→L184（模板规范上下文）
- **M5 细化完成，可进实现层**（按 m5_batch_plan 4 批 9 任务派工）

### 2026-08-27 · M5 设计审查（3 批 dsh）+ 用户 3 项拍板 + 全量修复
- **M5 设计审查 3 批**（dsh，报告落盘 审查_M5设计_批次1-3_jspace.md）：
  - 批次1 细化_3d 消息模板规范（P0×1+P1×3+P2×7）：页码越界文档自相矛盾（正文报错 vs M4 裁决夹取）/§8.3 合并策略只承接 1/4/L25「汇总语义」错引 5 处/L46「费用语义」编造 4 处+签到引用链不实
  - 批次2 细化_5e 战斗战报格式（P1×1+P2×9）：【数值层】行号系统性 -2~-5 偏移约 40 处（R-09 后插导致，主 agent 独立验证属实）/「幻觉审查_5e」编造来源/TC-11 MP 区间自相矛盾/先手行降级跨文档冲突
  - 批次3 3h settings + 3b 玩家属性 + 4f 基础指令（P1×3+P2×12）：3h message_prefix 三字段错值（show_on_system/per_channel/empty_title_text）/K 默认冲突//角色 三层契约缺失
- **用户 3 项拍板**：① 属性面板指令名改回 **/角色**（VIEW_CMD=角色）② **K=1 按 2026-08-24 拍板回写**玩家属性方案定稿 L86（0.2→1.0，仓库副本+原始两处）③ /角色 三层展示**无条件全层**（战斗外也显示临时层，B）
- **dsh 误判纠正**：批次3 判「3b K=1 与定稿冲突、代码注释编造仲裁权威」方向反了——K=1 是用户真实拍板（contract_deviations P1-1），实际是**定稿 L86 没回写拍板**；dsh 只 grep docs/ 漏了仓库根 contract_deviations.md
- **修复落地**：主 agent 修 3d（页码夹取回改+TC/合并承接表/L25-L46 错引/发送出口四项/状态行/折叠注记/粘合语法/5条可配/TPL-02-04 澄清）+ 3h（三字段+指针行号）+ 定稿 L86 + /角色 代码更名（basic_commands/parsers/测试/m4 登记）；3 路子 agent 并行修 5e（40+ 处行号实核+9 P2）/3b（ADR-06 K=1+70+ 处行号重对）/4f（补 /角色 指令契约 CMD-09 RUL-35~40 + /查看目标 挂靠）
- **回归：run_all_tests 全绿（exit 0）+ M4 门禁 verify_m4 全绿 + test_basic_commands/test_router 全绿**
- **M5 设计层确认完毕**：3 份细化裁决注记（3d 页码/4f 更名+全层/m4 §2.3 更名），可进 M5 实现层（D1-D5 消息模板渲染 + verify_m5）

### 2026-08-26 · M4 实现层审查（5 批）+ 修复 + 双审查收官
- **实现层审查 5 批**（dsh）：批次1 公共基础+解析（P1×2+P2×10）/批次2 模板+指令接线（P0×1+P1×1）/批次3 NPC（P1×3）/批次4 商店任务（P1×1）/批次5 签到指令组（P1×3）——报告落盘 审查_M4实现_批次1-5_jspace.md
- **修复**（5 组子代理 + 主 agent 收尾）：reward 静默丢奖→skip(item_add_failed)+幂等不封口（P1-1）/parsers-router 双管线 3 处漂移+对拍测试（P1-2）/dialog reply 误判信息类+text[]空白屏+快照 exec_index（P1-3×3）/黑市 listing_count 引擎消费+N 统一（P1-1）/checkin 指令层对齐真实引擎+跨月补签+表 id 限定+bonus mult 键（P0-1+P1-3）/dialog 修复时 e2e 冒烟对齐（npc.json info:true）
- **回归：run_all_tests 1981 passed / 0 failed；M0/M1/M2/M3/M4 五门禁全绿；G0 ARCH-OK；verify_m4 83 覆盖点 82 承载 + 1 DELAYED**
- **M4 正式收官：设计审查（4 批 + 用户 8 项拍板）+ 实现审查（5 批）+ 全部修复落地——交互系统（NPC/商店/任务/签到/指令解析/基础指令/GM）完整可用，可进 M5**

### 2026-08-26 · M4 实现层全部完成（8 批次 25 路，五门禁全绿）
- 批次0 公共基础：reward 发放器（355 行）/统一条件引擎（744 行）/日界（339 行）——唯一实现三铁律
- 批次1 指令解析：parsers（803 行，分隔符 7 种+快捷三模式+裁决①）/sender+list_render（TPL-08 页脚+裁决②夹取）/router（860 行，路由管线+会话路由+GM 禁绑）
- 批次2 NPC：npc_models（1185 行）/发牌员（751 行，裁决④策略+10 类动作+一次一物）/dialog（935 行，裁决③深度可配）
- 批次3 商店：shop_models（1137 行，裁决⑤并存+裁决⑥永不刷新）/shop 引擎（1286 行，原子防双扣+period 驱动）/shop 指令
- 批次4 任务：quest_models（1131 行）/quest 引擎（952 行，三原语+主线置顶）/quest 指令
- 批次5 签到：checkin_models（1163 行）/checkin 引擎（960 行，裁决⑦补签只计+裁决⑧三键表名限定）/checkin 指令
- 批次6 指令组：basic_commands（1109 行）/gm_commands（810 行，权限三级+静默+留痕+禁绑）/校验器接线+4 legal fixtures
- 批次7 集成：e2e_m4_smoke（154 断言六链路，NPC→商店→任务→签到→快捷→翻页夹取，确定性重放）/verify_m4（83 覆盖点 82+1 DELAYED）/run_all_tests 注册 M4 门禁
- **最终回归：run_all_tests 1946 passed / 0 failed；M0/M1/M2/M3/M4 五门禁全绿；G0 ARCH-OK**
- 下一步：M4 审实现（按流程实现后 dsh 审实现）

### 2026-08-27 · M4 设计审查（4 批）+ 用户 8 项拍板落地
- **M4 设计审查 4 批**（dsh）：批次1 指令解析（P0×2+P1×4）/批次2 NPC对话（P1×2）/批次3 商店（P1×4）/批次4 任务签到（P1×2）——报告落盘 审查_M4设计_批次1-4_jspace.md
- **幻觉设计实锤 3 处**：商店 D-07 编造"daily 4 点冲突"+"A3 日界裁决"（全文零匹配）；2b4 D-03 编造"商店 L339/L500 求值失败惯例"；2b1 把"深度可配"（用户拍板过）偷改"≤2 层硬约束"
- **用户 8 项拍板**：①战斗裸数字按定稿（归快捷表）②页码越界夹取最后一页 ③对话树深度恢复可配 max_dialog_depth ④发牌员策略保留细化版 rotate/random/condition ⑤商店库存+个人限购并存 ⑥商店不配 refresh=永不刷新 ⑦补签只计不补发奖励 ⑧[签到:*] 键加表名限定（[签到:loop.连续天数]）
- **回写**：7 份细化裁决注记（3c/3d/2b1/2b2/2b3/2b4/2b5）+ 发放器失败策略统一（条目失败黄字跳过，单事务=簿记原子性）
- **M4 设计层确认完毕，可进实现层**（路 2b 24 任务 + 3c/3d/4f/5b）

### 2026-08-26 · M3 实现层审查修复（4 批）+ 双审查收官
- **实现层审查 4 批**（dsh）：批次1 地图数据层 P0×2+P1×3 / 批次2 时间天气 P1×6 / 批次3 副本换区 P1×1 / 批次4 世界集成 P1×2——报告落盘 审查_M3_批次1-4_jspace.md
- **修复**（三组子代理 + 主 agent 补完）：校验器 _emit 适配（P0 空转/崩溃）、time_query 接 IF08 + 枚举可配适配、校验器声明枚举集、default_pool 链、enabled 门控、单值枚举黄通道、pursue 错失窗口只计偏离可达目标（深图追击可达）、PV 恢复落库+幂等（e2e 双触发暴露二次恢复 bug）、phases/zone_change 形态兼容、DungeonSession 追击字段（with_chase + round-trip）、max_alive 阻塞行计时冻结、虚弱 0 时长、dungeon_entrances 交叉校验、weather_weights 键、rest 默认值（冷却 3/限次 0）、version 防串档
- **fixtures 修正**：legal/maps.json 加世界图 volcanic_path（dungeon_entrances 合法承载）+ rubble_field 移除违规入口（R2 防嵌套）；测试同步（断言对齐新行为/结构）
- **回归：run_all_tests 988 passed / 0 failed；M0/M1/M2/M3 四门禁全绿；e2e 冒烟全绿；G0 ARCH-OK**
- **M3 正式收官：设计层审查（3 批 + 用户 3 项拍板）+ 实现层审查（4 批）+ 全部修复落地——G3 达成，可进 M4 数据框架**

### 2026-08-26 · M3 设计层 dsh 审查（3 批）+ 三项裁决落地（用户拍板）
- **流程纠偏**（用户要求）：以后先 dsh 审设计（缺漏/错误/幻觉）→ 确认 → 细化 → 实现。M3 补做设计审查：批次1 地图层（P0×2+P1×2+P2×12）/批次2 换区副本（P0×0+P1×1+P2×10）/批次3 时间天气（P0×0+P1×1+P2×7）
- **设计审查发现的关键幻觉**：① 2a1a 自造 `rank:gate`/`chance` 字段（定稿无）② exits 三套互斥 schema（2a1a 数组/2a1b 对象/2a1c 裸字符串）③ spawns/spawn/monsters 键名分裂 ④ 季节时段枚举「固定写死」篡改定稿「开放可配」⑤ 出没边界「仅非战斗」与 1g4 冲突 ⑥ PV 恢复满值口径与定稿锚点冲突
- **用户拍板 3 项**：① 键名统一 **monsters**（按定稿）② PV 恢复改**缺失量口径**（补已损失的一半，0=不恢复/1=全恢复）③ **枚举开放可配**（season.enum/period.enum，%len(enum)）
- **实现落地**：map_models 读 monsters 键 + fixtures/测试同步；chase_resume/dungeon_boss PV 缺失量口径（+未破防不降断言）；worldtime 枚举可配（构造解析 enum + cycle_tick %len + season_now 返回自定义键）+ time_validator V1b/V2b enum 校验 + weather_conditions ctx 枚举注入 + 4 个新测试
- **文档回写**：m3_shared_contract 4 处 + 7 份细化裁决注记（2a1a/2a1b/2a1c/2a2/2a4a/2a4b/2a4c）；审查报告 3 份落盘（审查_M3设计_批次1/2/3_jspace.md）
- **实现层审查批次1**（地图数据层）：P0×2（validate_maps _emit 适配断裂生产空转 / time_validator _emit 缺失崩溃）已修 + 回归测试 2 条；P1-3 isdigit 陷阱已修；P1-1/P1-2 待设计裁决对齐后修
- **回归：tests/unit 939 passed / 9 skipped + e2e 冒烟 126 断言全绿 + G0 ARCH-OK**

### 2026-08-26 · M3 批次 8 集成回归 + M3 完整收官（G3 门禁全绿）
- 路V：scripts/verify/verify_m3.py（G3 门禁：81 TC 诚实化覆盖声明 75 承载 + 6 DELAYED 注明理由；关键模块/函数断言；子进程 pytest）+ 自测 exit 0
- 路W：scripts/e2e_m3_smoke.py（M42 端到端冒烟 658 行 126 断言：探索版 S0→S1→S5→S7 41 条 + BOSS 版 S0→S3→S4→S5 83 条（入场限制/守门怪/三阶段/换区追击/续战标记/通关奖励）+ 运行级 2 条（固定 now 确定性重放一致、24 步时间天气钩子））
- 路X：tests/unit/test_m43_regression.py（M43 三回归探针 33 断言：全仓零定时器扫描（threading.Lock 允许清单自证）+ 确定性抽签跨实例同值 + 快照完整性 ai_state/combo_state/换区上下文逐字段一致）
- **G0 架构门禁修复**：收口接线暴露 content→engine 反向依赖（validator→worldtime、weather_validator→engine/weather_conditions）+ world→core（map_graph）——校验函数迁移到 content/time_validator.py、weather_validator 常量本地化、map_graph 移 content 层、check_architecture 矩阵登记 engine 层；ARCH-OK 4 项全绿
- **最终门禁：run_all_tests 全量 951 passed / 0 failed；M0/M1/M2/M3 四门禁全绿（G3 verify_m3：81 TC 中 75 承载 + 6 DELAYED）；M3 完整收官，G3 达成可进 M4 数据框架**

### 2026-08-26 · M3 批次 7 安全区/快照/死亡（M24-M30，3 路并行；首轮 scnet 503 瞬时故障重派成功）
- 路S：world/rest.py（504 行：is_safe_zone 入口区/safe_zone 配置/maps 标记并集；rest_in_dungeon HP/MP 恢复+冷却缩减+次数限制；rest_is_not_exit 休息≠离开保留全部上下文）+ 26 测试 62 断言
- 路T：world/snapshot_resume.py（333 行：resume_from_snapshot 快照续玩 ai_state/combo_state/换区上下文逐字段保留，真实 BattleEngine.from_snapshot 端到端；non_combat_exit 非战斗离开重置+明示文案「战斗中断可续玩，离开副本将重置」）+ 19 测试 107 断言
- 路U：world/dungeon_persist.py（775 行：on_dungeon_death 复活点优先级+虚弱禁入+BOSS 残血保留（死亡≠离开）；save/load/clear_dungeon_session 会话持久化 content_pack 防串档，checkpoint 驱动已清区域回退）+ 28 测试 156 断言
- **回归：tests/unit 923 passed / 9 skipped**（记录：scnet 端点 10:02-10:07 瞬时 503，批次 7 首轮三路全挂，curl 验证恢复后重派成功）
- **M3 功能批次全部完成（0-7）**，剩批次 8（端到端 M42 + 三回归 M43 + verify_m3 81 TC）

### 2026-08-26 · M3 批次 6 换区追击（M12-M15，2 路并行）
- 路Q：world/chase.py（477 行：chase_trigger 触发装配+候选区 sha256 确定性选一、begin_chase 提示「BOSS 逃向了 XX」、pursue 追击走错计数+错失窗口超限 BOSS 回满/离开）+ 26 测试 75 断言
- 路R：world/chase_resume.py（347 行：prepare_resume_battle 续战装配残血保持+PV 半恢复 floor+开场技标记+战斗快照续接；exit_dungeon_reset 离开副本全清重置，战斗中拒绝）+ 13 测试 62 断言（两路已自行对齐续战契约 continue_data）

### 2026-08-26 · M3 批次 5 副本两型+子任务（M17-M23，3 路并行）
- 路N：core/dungeon.py（672 行：DungeonStateMachine 全量 M1-M15 迁移 + DungeonSession 持久化形态（content_pack 防串档）+ explore_run 探索版流程（入口校验先于消耗/走图/通关 CLEARED/离开重置回外部锚点））+ 26 测试 90 断言
- 路O：core/dungeon_boss.py（415 行：BossFlow 入场守门怪检查/phase_for 三阶段阈值可配/应该换区触发链（enabled+threshold+timing 衔接 PhaseTable）/on_chase_continue 续战标记残血保持+PV 半值+开场技）+ 20 测试 50 断言
- 路P：core/dungeon_subquest.py（336 行：五形式判定 ProgressTracker/奖励发放防重复/可选性不阻塞 BOSS）+ 16 测试 59 断言
- **回归：tests/unit 811 passed / 9 skipped**

### 2026-08-26 · M3 批次 4 刷怪（M08-M10，2 路并行）
- 路L：world/spawn.py 实装 SpawnManager（410 行：initial_spawn/refresh 懒补刷——last_kill_time+respawn_minutes 时间差纯计算零定时器、weather 变速/0 不刷、filter_eligible seasons/periods/active_time AND、zone_expire_removal 时段结束移除「对方逃跑了」）+ 40 测试 65 断言
- 路M：world/spawn_weather.py（284 行：weather_weighted_refresh 变速补刷/在场不驱逐、max_alive_guard 聚合上限）+ GameWorld.monster_pool/get_boss 实装（读 spawn 表过滤 + gate_guard）+ 36 测试 74 断言
- **回归：tests/unit 749 passed / 9 skipped**

### 2026-08-26 · M3 批次 3 地图行走（M02-M06，2 路并行）
- 路J：core/map_graph.py（326 行：can_move 双向/单向拦截/隐藏条件门/死路 + bidirectional_consistent 不对称黄提示 + path_exists BFS 捷径寻路）+ 26 断言
- 路K：world/movement.py（430 行：resolve_move 隐藏通道门 + move_to_map 地图切换钩子（map_weather_seen 记录 + R22 双钩子）+ enter_context_route /进入 路由（方向行走/入口名/序号匹配，副本进入批次 5 接线））+ 33 测试 89 断言
- **回归：tests/unit 673 passed / 9 skipped**（记录：test_formula_whitelist_boolean_and_string 偶发失败——JS formula runner 瞬时态，隔离证明与批次无关）

### 2026-08-26 · M3 批次 2 天气引擎（M37-M41，3 路并行）
- 路G：worldtime 追加 IF05/IF08/IF04（map_pool 地图池覆盖/回退默认池、map_weather sha256 确定性抽签 同 tick 跨群重启同值、weather_now）+ 27 用例
- 路H：engine/weather_conditions.py（M40 三键注册：SEASON_KEYS/PERIOD_KEYS 复用 worldtime 单口、eval_condition fail-safe、REGISTERED_KEYS 供 V6）+ engine/weather_consumers.py（M39 联动：weather_mods rate_mult/rarity_shift clamp 4 档/0 不出、combat_weather_mult 默认关、lore 天气判定）
- 路I：content/weather_validator.py（M41 V5-V8 硬校验：地图池引用/消费方枚举/combat mults 键/模板占位符 + 黄提示六条）+ 26 用例 70 断言
- **收口**：validate_weather 接入 check_pack settings 分支；M40 三键由 weather_conditions 承载（任务系统 M5 接线时用 REGISTERED_KEYS/eval_condition）
- **回归：tests/unit 614 passed / 9 skipped**

### 2026-08-26 · M3 批次 1 时间引擎（M32-M36，3 路并行）
- 路D：worldtime.py 追加 IF09/IF10（check_changes 变化检测钩子——顺序固定季节→时段→天气 ≤3 条、离线跨周期只报最新；maybe_broadcast 懒广播——enabled 默认 false、占位符模板 {type,name,emoji,map}、跨群去重 seen 预留）+ 15 用例
- 路E：data/world_time_persist.py（IF11 time_state 存档契约：DEFAULT_TIME_STATE 四字段、load 缺补默认多忽略迁移、save 非原地、mark_map_seen 惰性、cache_indexes_equal 去重）+ 24 用例
- 路F：engine/time_query.py（M36 查询：season_status 天折算/period_status 分钟/weather_status pool_label 覆盖池标注）+ M43 探针 test_time_regression（零定时器源码扫描 + cycle_tick 确定性跨年）
- **回归：tests/unit 506 passed / 9 skipped**

### 2026-08-26 · M3 细化 + 批次 0 数据骨架（地图/副本/时间配置装载）
- **M3 细化交付**：docs/m3_shared_contract.md（9 批次 43 任务权威契约：地图层 8 字段/exits 通道/spawn 行/换区追击/副本 S0-S7 状态机/时间引擎 IF01-IF12/天气引擎/verify_m3 81 TC 矩阵/十条铁律）+ docs/m3_batch_plan.md（批次派工单 0-8）
- **批次 0（3 路并行）**：路A maps 装载（map_models.py 520 行 MapDef/SpawnDef/ExitDef + validate_maps + maps.json fixtures + 68 断言）/ 路B dungeon 两型（dungeon_models.py 523 行 DungeonDef + validate_dungeons + zone_change 校验 + dungeon.json fixtures + 50 断言）/ 路C time_cycle 配置（engine/worldtime.py 272 行 ANCHOR + IF01-07 纯函数 + validate_time_cycle V1-V3 + 71 断言，手算验证 2026-08-16=夏）
- **主 agent 收口**：三 validate 接入 check_pack（maps 专项+泛型并行防误拦/恢复旧 dead_range 检测、dungeon ValidationReport 桥接回填 _Checker、settings 追加 time_cycle）；field_meta 注册 dungeon 模块；loader _KIND_FOR_MODULE 加 dungeon；manifest 补 dungeon；清调试脚本；修并发 id 不对齐（dungeon fixtures molten_* → 实际地图 id rubble_field/crag_den/lava_tunnel + 测试同步）
- **回归**：tests/unit **440 passed / 8 skipped**，legal 包零红拦

### 2026-08-26 · M2 怪物体系全里程碑完成（多批多路，G3 门禁全绿）
- **批次1**（3 路并行）：A1 enemies 八段 schema+Def（models/field_meta，M0 简档兼容）+ A2 校验器 R1-R15+难度模板补全+木桩特例（validator 双轨）+ B1 AI 决策管线（core/monster_ai.py：两级状态机+L0-L7 套间评估+随机池归一化+hungry/cooldown）
- **批次2**（3 路并行）：A3 fixtures 八段化+action.json+1e TC-01~13 转 pytest + B2 条件行动 13 类触发+chain C 连招链（monster_conditions/monster_chains）+ B3 意图预告三级+阶段表（monster_intent/monster_phases）
- **批次3**（2 路并行）：C1 battle.py 怪物行动挂接（enemy_act None→MonsterAI.decide、ai_state 快照往返、打断=套完结、tick 单点递减）+ C2 1g4 战斗世界边界（world/battle_boundary.py：丢失挂起/脱战回血/死亡惩罚/跨群锁/时间线）
- **批次4**（2 路并行）：smoke→pytest 固化（test_monster_ai 76/test_monster_intent_phases 57/test_battle_boundary 24/test_monster_conditions_chains 44）+ verify_m2 门禁（34 TC 诚实化覆盖：29 承载 + 5 DELAYED）
- **主 agent 收口修复**：B1 L5 权重 0 落空（_weighted_pick zero_as_one）；三桩接线→新模块；battle 0 倍率 max(1,) 保底误伤短路（蓄力/起身/吼叫）；派生 cap 只对 _derived 生效（单技能 power 1.6 不封顶）
- **回归**：tests/unit 337 passed / 8 skipped；run_all_tests 全量 356 passed / 0 failed；verify_m0/m1/m2 门禁全绿
- 记录：偶发 1 failed 复跑消失（测试确定性待 dsh 审查重点盯）

### 2026-08-26 · M2 dsh 三批审查（P0=0 / P1=6 / P2 关键项修复）
- 批次1 数据层：P1-1 R15 接续概率 <60% 黄提示实现 + P2-1 pv_recover 枚举 + P2-2 NaN/Infinity 八段零拦截 + P2-4 action probability 去旗标（Y-2 误报）+ P2-7 weight 和=0 黄提示 + P2-9 condition obj 放宽（合法配置误拦）
- 批次2 AI 引擎：P1-1 **phase 写端**（HP→phase 换算接进 decide，phase_changed 联动闭环 TC-04）+ P1-2 **断链冷却 max 保留+起算偏移**（防永久锁链）+ P1-3 **蓄力 intent 统一走 build_intent**（图鉴分级不再被绕过）+ P1-4 chain_queue 剩余段语义登记 + P2-3 enqueue 委托唯一实现 + P2-4 __get_up__ 哨兵 + P2-5 快照残留 + P2-1 docstring
- 批次3 战斗+边界：P1-1 **combo_broken 一次性标记**（决策后清除，防无限触发）+ P2-1 decide 异常回落普攻 + P2-2 打断判定以 chain_queue 为真 + P2-3 lost_pending 快照预留 + P2-4 attack_type 全通道归一化 + P2-5 verify_m2 注解刷新 + P2-6 ratio clamp 防负货币
- 契约登记：m2_shared_contract 补 ai/phases/basic_action/chains.cooldown 键 + enemy.luk→player.lck 桥接 + enforce_mp 边界
- 补回归：tests/unit/test_m2_review_fixes.py（5 用例：R15<60%/phase 写端/冷却不重置/蓄力 intent/combo_broken 清除）
- 最终回归：**361 passed / 0 failed**，verify_m0/m1/m2 三门禁全绿，G3 达成

### 2026-08-25 · 通用规则 ⑱~㉒ 落地核查（架构与代码规范）
- ⑱ 先定架构 ✅：全仓有细化文档 + check_architecture.py 依赖矩阵门禁（TC-03）
- ⑲ 模块化 ✅：五层（core/world/storage/content/data）+ commands/web 高度模块化，职责单一
- ⑳ 文件元信息 ⚠️ 部分符合：每文件 docstring 已有功能描述（快速识别已满足）；标准「文件名/创建时间/作者」块仅**新文件**强制（api_response.py 已带头示范），存量 38 文件不做大规模回填（避免破坏稳定代码，登记待办）
- ㉑ 规范注释 ✅：函数/类均有 docstring（入参/出参/核心逻辑，M0 复查已确认）
- ㉒ 统一返回格式 ✅ **新增** `qbot_rpg/data/api_response.py`：对外接口（web/commands 壳层）统一 {code,msg,data} + ok()/error() 工厂 + to_dict()；内部纯逻辑层按架构定稿保持「领域对象+异常」不强制改造 → test_api_response.py（5 用例）

### 2026-08-25 · 通用规则 ⑭⑮⑯⑰ 落地核查（开发流程与版本控制）
- 规则 ⑭ 分块编写：项目天然按「里程碑 → 模块 → 功能」分块（M0 骨架/M1 战斗/…），无不一次性写完 ✓
- 规则 ⑮ 测试驱动：**已实现模块 100% 有配套测试**（content/storage/data/core 全部 + test_logging_utils 新增）；骨架占位模块（levelup/equipment/worldtime/battle_render/commands 等标注「M1/M3/M4 实装·仅骨架」）无测试为合理豁免，实装时补
- 规则 ⑯ 测验后提交：历次 commit 均在回归全绿后执行 ✓
- 规则 ⑰ 规范提交：提交信息统一 `feat:/fix:/chore:/docs:` 前缀 + 中文说明 ✓

### 2026-08-25 · 通用规则 ⑪⑫⑬ 落地（日志与异常处理）
- 新增 `qbot_rpg/data/logging_utils.py`：统一日志工具（stdlib logging、幂等初始化、RotatingFileHandler 1MB×3、QRP_LOG_DIR 可配、日志落 logs/qbot_rpg.log）
- 接入关键异常路径：loader 红拦阻断前记日志、hot_reload 加载失败/意外异常记日志（快照回退兜底）、storage integrity_check 失败记日志
- 新增 tests/unit/test_logging_utils.py（日志真实落文件 + 幂等）
- 依赖矩阵合规：logging_utils 放 data 层（storage/content 只允许依赖 data）
- 回归：234 passed / 0 failed 全绿

### 2026-08-25 · 通用规则 1~10 落地
- 规则 1：`记录.md` 创建（本文件）
- 规则 8：`requirements.txt` 锁版本（aiosqlite 0.22.1 / pytest 9.1.1 / pytest-asyncio 1.4.0）
- 规则 9：`.env.example` 模板（敏感信息不入库）
- 规则 10：`.gitignore` 补 .env/日志/覆盖率杂项

### 2026-08-25 · M0 复查收官（三批全部完成）
- **M0 复查全貌**：批1（storage+data+架构）P0×0/P1×11 ✅ + 批2（content 六件套）P0×1/P1×12 ✅ + 批3（core+测试）P0×1/P1×8 ✅ —— **合计 P0×2 / P1×31 全部修复**
- **批3 修复**（用户拍板 K=1 + 派生收敛）：
  - 命中率 K 统一 0.2→1.0（HitParams/damage.hit_rate 默认值，3b/1a 同口径）
  - 派生属性 7 函数收敛为委托 damage 参数化版（单一实现，常数可配置）
  - 面板 render_panel 改消费 calc_all_final_attributes（显示值=战斗最终值）
  - verify_m0 门禁诚实化（覆盖率简版标注、MIN_PASS_COUNT=100、_validate_fixtures 带断言）
  - worldtime 骨架按 2a4c IF01~IF12 重建 + M1→M3 修正
  - test_message_format 空洞断言（`or True`）修复
- **回归**：232 passed / 0 failed 全绿（G1/G2）
- **3 个 commit 已生成，未 push**（批1/批2/批3）

### 2026-08-24 · 批1+批2 修复
- 批1：只读池令牌泄漏/坏连接复用/回收丢数据/缓存竞态/迁移链+备份兜底/stack_max/data双轨标注/架构门禁 ImportFrom —— 11 项 P1 全修
- 批2：formula 安全绕过封堵（Unicode 转义 + 方括号构造器链）/monster_def_rate 负值黄提示/type 错注册/map Def.id/integrity 自检 A/BLK-5 暂停 —— 13 项 P0/P1 全修
- run_all_tests.py cwd 路径 bug 顺带修复

### 更早
- M1 战斗核心已完结（verify_m1 G2 全绿）
- 效果系统/炼金系统/NPC 系统等策划案定稿 39 份归档 /root/docs_archive/RPG框架项目/

---

## 二、下一步

1. **push 待推送的 3 个 commit**（需用户提供 GitHub token，走 mihomo 代理 7890）
2. M1 复查（战斗核心）或数据包阶段（数值验证）

---

## 三、未完成 / 待办

| 事项 | 状态 | 备注 |
|---|---|---|
| GitHub push（count 更新：7 commit） | ⏳ 待 token | M0 三批修复 + 规则落地 |
| 覆盖率 ≥80% 硬门禁 | ⏳ M1 恢复 | verify_m0 当前简版口径（5d §7.4 登记） |
| 3d 模板注册表 TPL-07~11 | ⏳ M5 D3/D4 | 页脚/折叠/✅❌ 标记 |
| worldtime 引擎实装 | ⏳ M3 | 骨架占位已对齐 IF01~IF12 |
| 时间天气/图鉴/NPC 等 | ⏳ M3/M4/M6 | 3f TC-01~20 defer |
| 存量 38 文件元信息块回填（规则 ⑳） | ⏳ 可选 | 现 docstring 已有功能描述；标准块仅新文件强制 |

---

## 四、项目通用规则速查

见用户通用工作规则 10 条（规则 1~10）：记录.md / 下载不等待 / 联网搜索 / 全中文 / 国内镜像 / 保护环境 / 写前检查 / 依赖锁定 / 配置分离 / .gitignore。
