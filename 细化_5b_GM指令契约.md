# 细化规划 5b · GM 指令与权限契约（权限模型 / 指令集 / 权限分支 / 审计 / 验收测试）

> 定位：QQ 回合制 JRPG 框架（v2.8）GM 指令与权限系统的实现层细化契约——权限模型、GM 指令集、权限分支、操作审计、验收测试五件套。
> 依据文档（唯一权威，本文所有条目均逐条引用行号）：
> - 【框架】= 《RPG回合制框架设计文档.md》（v2.7+，2026-08）
> - 【规范】= 《指令分隔符统一规范.md》（v1.1，2026-08-15）
> - 【规划】= 《实现层规划文档.md》A3 commands 壳层
>
> 口径声明：
> 1. 凡【框架】【规范】原文已给出者（权限分级、静默语义、禁绑规则、留痕要求）直接引用并标注行号；原文未显式定义处（权限存储表结构、审计字段、GM 指令参数语法、/广播 /玩家查询 /测试 指令入口）一律标注 **【细化】**，属接口化推断，不冒充定稿原文。
> 2. 权限判定哲学：**静默是安全边界，留痕是信任边界**——无权限者只见「无」，有权限者的每一步都进审计（【框架】L254-255、L1140）。
> 3. 本文所有 GM 指令参数语法遵循【规范】分隔符总表（空格切参数、`*` 数量、`,` 列表、`=` 键值；【规范】L13-21）。

---

## 0. 总览：指令处理决策链

```
玩家消息进入
  → ① 频率控制（全局每指令 ≥1s / 单群每分钟 ≤20 条 / 单日总量上限）【框架 L1311】
  → ② 触发器匹配（@机器人 / 前缀 / 直接触发：如全局快捷模式"攻击2"）【框架 7.2】【规范 6.3】
  → ③ 快捷表 → 指令别名 → 指令白名单 解析顺序【规范 L149-152】
  → ④ 权限判定（机主 > GM > 普通玩家；GM 指令无权限 → 静默丢弃，不写审计）【框架 L257-260、L255、L1355】
  → ⑤ 参数解析 + 统一错误模板（有权限但参数错误 → 三要素提示）【框架 7.5 L1351-1355】
  → ⑥ 执行（message_id 幂等键防重复结算；per-player 串行队列按到达顺序处理）【规划 A3 L3203】
  → ⑦ GM 操作审计留痕（追加写，不可删）【框架 L1140】【细化】
```

**职责分层**：权限/路由/解析在指令层 commands.py（【框架】L51）；权限判定数据在数据库系统配置（【框架】L254），GM 指令不注册进快捷白名单、不参与别名（【规范】L121、L160）。

---

## 1. 权限模型

### 1.1 三级权限与判定（【框架】3.9 L252-261）

| 级别 | 名称 | 默认权限 | 依据 |
|---|---|---|---|
| P0 | 机主（owner） | 全部指令 + 权限授予/撤销 + 数据库最高权限 | 【框架】L258 |
| P1 | GM（机主授予） | 默认授予集：/重载 /备份 /恢复 /日志 /编辑 /封禁 /解封 /封禁列表；可另行下授扩展集 | 【框架】L259 + 【细化】裁决（见 1.1.1） |
| P2 | 普通玩家 | 仅游玩指令（含 /日志 玩家版=冒险日志分支） | 【框架】L260、L2772 |

- 判定优先级：**机主 > GM > 普通玩家**；同人命中高级别以高级别为准，不做多角色并存。【细化】
- GM 权限**不自动授予群管理**：群主/群管理员默认按普通玩家处理（【框架】L254），映射策略见 1.3。
- 静默语义：无 GM 权限的玩家使用 GM 指令 → **直接无视（不报错不提示不写审计）**，避免暴露指令存在（【框架】L255、L1310、L1355）；与「被封玩家触发游玩指令 → 人话提示」严格区分（【框架】L1137-1139，见 3.3）。

**1.1.1 权限分级冲突仲裁（默认授予集）**：
- 冲突：3.9 权限分级（【框架】L259）写 GM = 重载/备份/恢复；指令表（【框架】L1301 /封禁（GM）、L1300 /重载（GM）、L1302 /日志（GM）、L1299 编辑（机主/GM））对个别指令标 (GM)。
- 裁决（本细化）：指令表 (GM) 标注 = 该类指令 GM 可使用；L259 = 最小默认集。**默认授予集 = 全部指令表标注 GM 的指令**（重载/备份/恢复/日志/编辑/封禁/解封/封禁列表）；**机主专属（可另行下授）= 存档导出/调试/测试/广播/玩家查询/设置**。授予以 per-command 粒度存数据库（granted_commands 表），机主可在编辑器/数据库设置调整。【细化】
- 依据优先级：L259 为权限分级定义（权威），指令表标注为可授予范围声明；本裁决为两者并集的最小合理默认。

### 1.2 存储与玩家存档标记（【细化】）

| 存储项 | 位置 | 语义 | 依据 |
|---|---|---|---|
| 权限主表 admin_users | 数据库系统配置区（非 content/ 内容包、非玩家存档） | {qq_id, role: owner\|gm, granted_commands[], granted_by, granted_at, revoke_log} | 【框架】L254（数据库配置）|
| 权限缓存 | 进程内存 {qq_id: role} | 启动时全量加载；热重载**不**重建（权限不受内容包热重载影响）| 【细化】 |
| 玩家存档 players | 不含任何权限字段 | **防拷贝存档伪造权限**：权限永远不在玩家存档内联 | 【细化】 |

- 机主初始写入：安装向导把首个绑定 QQ 写为 owner（数据库直接写入，不经过任何指令）【细化】；机主身份变更仅可在编辑器/数据库操作，不走聊天指令（防聊天劫持【细化】）。
- 玩家存档标记（任务口径）：框架存档中**不设** GM 标记位；「该玩家是 GM」的唯一事实来源 = admin_users 表。任何「存档内标记权限」的设计一律拒绝，理由：存档可导出可旁路（【框架】L1154 安全补强精神）。
- 权限变更即时生效（内存缓存失效重载），无需重启【细化】；变更本身写审计（见 §4）。

### 1.3 群管理员映射（【框架】L254）

- 策略：**默认不映射**——群主/群管理员 ≠ GM；其发 GM 指令同样静默无视（【框架】L254「不自动授予群管理」）。
- 可配项（【细化】，默认关）：`allow_group_admin_map: false`。开启后可将指定群的「群主」自动映射为受限 GM（仅可授予集内指令），映射关系在数据库配置且随权限缓存加载；开启时群管理员加入即时生效（监听群成员事件），退出/被撤即时失效。
- 设计理由：简单优先（默认关）+ 防"拉群自封管理刷权限"的权限绕过面；映射仅作机主可选的运维便利。
- 边界：映射获得的 GM 权限**不可再授予他人**（授权操作仅机主可执行）【细化】。

### 1.4 校验时机：绑定层 + 执行层双检查

- 快捷绑定层：绑定目标为 GM 指令 → 绑定时拒绝（【规范】L160-161），且 GM 指令永不入快捷白名单（【规范】L121、L128）。
- 执行层：即便快捷表被写入脏数据（绕过绑定层），执行 GM 指令时**权限二次检查照常拦截**（【规范】L169-171）。
- 内容包注册层：GM 指令由框架注册，内容包不可注册同名指令覆盖（优先权归框架层）【细化】；指令别名指向 GM 指令的配置由校验器拒绝（【细化】，依据【规范】6.7 别名面向世界观定制游玩指令的 L189-191 精神，GM 指令不参与别名）。

---

## 2. GM 指令集（14 条）

> 参数语法遵循【规范】总表（L13-21）：`空格` 切参数（≤2 位置参数，L46-51）、`=` 键值修饰、`,` 列表、`*` 数量。GM 指令全部**保留 / 前缀**（永不快捷，【规范】L120-121、L128）。
> 权限列含义：**机主**=仅机主；**GM**=默认授予集；**机主→GM**=机主专属、可 per-command 下授。
> 所有 GM 指令执行成功/失败均写审计（§4）；无权限调用不写（防探测）。

### 2.1 总表

| # | 指令 | 权限 | 参数（【规范】语法） | 返回要点 | 依据 |
|---|---|---|---|---|---|
| G1 | `/重载 <内容包>` | GM | 1 位置参数：内容包名 | 热重载结果摘要 + 失败项清单 | 【框架】L256、L1300 |
| G2 | `/备份` | GM | 无参 | 备份文件名/路径 + 大小 | 【框架】L256、L1485-1487、L1500 |
| G3 | `/恢复` | GM | 无参（编辑器导入走 UI；指令版恢复最近备份） | 校验→原子替换→回滚结果 | 【框架】L256、L1485、L1629 |
| G4 | `/存档导出` | 机主 | 无参 | CSV 产物路径（脱敏开关）| 【框架】L256、L1129、L1156 |
| G5 | `/调试` | 机主→GM | 无参 | 调试模式开/关 + 当前状态 | 【框架】L256 |
| G6 | `/测试` | 机主→GM | 无参 | 只读冒烟测试通过/失败摘要 | 【框架】L256（/调试 等扩展）【细化】 |
| G7 | `/广播 <消息>` | 机主→GM | 1 位置参数：消息文本（≤200 字）；可选 `定时=HH:MM` 键值 | 推送目标群数 + 私聊数 + 完成确认 | 【框架】L1125-1126、L1128、L1168 |
| G8 | `/日志` | GM | 无参（可选 `条数=N` 键值，默认 20，上限 50【细化】） | 系统日志最近事件（GM 版分支） | 【框架】L1302、L2772、L2787 |
| G9 | `/玩家查询 <QQ号>` | 机主→GM | 1 位置参数：目标 QQ 号 | 脱敏玩家摘要（见 2.2-G9） | 【框架】L1129、L1130 |
| G10 | `/封禁 <QQ号> [时长] [原因]` | GM | 1 位置参数 QQ 号；可选 2 位置参数 时长；可选 `原因=` 键值（默认永久） | 封禁确认 + 到期时间 | 【框架】L1135、L1301、L1140 |
| G11 | `/解封 <QQ号>` | GM | 1 位置参数：目标 QQ 号 | 解封确认；未在名单 → 错误模板 | 【框架】L1136 |
| G12 | `/封禁列表` | GM | 无参（分页语法 7.4：`/封禁列表 <页>`【细化】） | 封禁名单分页（原因/时长/到期） | 【框架】L1136、L1130 |
| G13 | `/编辑` | 机主/GM | 无参 | 返回编辑器链接 | 【框架】L1299 |
| G14 | `/设置` | 机主 | 参数见 2.2-G14 | 指令模式/群级配置切换结果 | 【规范】L97、L96；【框架】L3185 |

**指令数 = 14**（G1-G14）。

### 2.2 逐条契约（权限校验 / 参数 / 返回）

**G1 `/重载 <内容包>`**（【框架】L256、L1300；单数据包机制 L1039）
- 权限校验：权级 GM；无权限 → 静默无视。
- 参数：内容包名（1 位置参数，符合【规范】命名铁律 L58——名称禁空格）；缺参 → 统一错误模板三要素（原因/正确用法/下一步，【框架】L1351）。
- 执行：校验加载 → 热重载生效（编辑器保存即生效精神 L42）；失败项逐条列出（坏配置拒绝加载并列出错误项，对齐【规划】L2477 验收口径）。
- 返回：`✅ 已重载【内容包X】：技能 12 条 / 怪物 8 条 / 地图 5 张；失败 1 项：maps.json 第3张缺 safe_zone → 已拒绝`。

**G2 `/备份`**（【框架】L256；L1485-1487 一键备份=存档+内容包+配置 zip）
- 执行：VACUUM INTO / checkpoint 后复制（【框架】L1615），产 zip；保留份数/目录按资源治理配置（默认保留 30 天，L1166）。
- 返回：`✅ 备份完成：QQRPG备份/2026-08-18_1200.zip（2.4 MB）`。

**G3 `/恢复`**（【框架】L256；备份恢复事务化 L1629：校验完整性 → 自动备份当前档 → 原子替换 → 失败自动回滚）
- 执行：取最近一份备份（或编辑器导入文件）；恢复期间写恢复中标记，完成后玩家恢复正常游玩（对齐【框架】L1195 关闭保护/发布精神）。
- 返回：`✅ 恢复完成：rollback 就绪（当前档已备份 backup_pre_restore.zip）`。

**G4 `/存档导出`**（【框架】L256；数据导出 L1129 等级/货币/活跃/最近在线，可脱敏；CSV 防公式注入 L1156）
- 权限：机主（可下授）。
- 执行：导出 CSV（默认脱敏）；`=` `+` `-` `@` 前缀单元格转义或拒绝（【框架】L1156 双向防公式注入）；产物文件权限 600【细化】（对齐 L1154）。
- 返回：`✅ 已导出 128 名玩家：exports/players_20260818.csv（脱敏开启）`。

**G5 `/调试`**（【框架】L256）
- 权限：机主（可下授）。
- 执行：切换调试模式（详细日志/性能计时开关）；仅影响日志详略，不改游玩数据。
- 返回：`🔧 调试模式：开（日志级别=DEBUG，性能计时=开）`。

**G6 `/测试`**【细化】（锚点【框架】L256 /调试 扩展）
- 权限：机主（可下授）。
- 执行：只读冒烟测试集——配置校验（三表 schema/引用完整性）+ 指令路由连通性检查；**不产生任何写操作**（简单优先、防生产误伤）。
- 返回：`🧪 冒烟通过：配置 3 包校验 OK / 指令路由 42 条连通 / 耗时 1.2s`；失败 → 错误模板列出首条失败项。

**G7 `/广播 <消息>`**【细化指令入口】（能力依据【框架】L1125-1126 公告一键推送全部群+私聊、定时 L1128、复用公告通道 L1168）
- 权限：机主（可下授）。
- 参数：1 位置参数=消息文本（≤200 字【细化】）；可选 `定时=HH:MM` 键值（【规范】`=` 键值修饰 L18）走 apscheduler 定时推送（L1128）；键值 `群=群号,群号` 可限定群列表【细化】（【规范】`,` 列表 L17）。
- 执行：逐群逐私聊推送（跨群去重保证一次一条），复用公告通道。
- 返回（即时）：`📢 已推送 5 群 + 12 私聊（定时 20:00 再推一次）`。
- 频率：受单群每分钟 ≤20 条约束（L1311），连续广播超限走统一错误模板（有权限超频 ≠ 静默）。

**G8 `/日志`（GM 版）**（【框架】L1302；分支见 §3.1）
- 权限：GM；普通玩家发 /日志 = 玩家版冒险日志（另一分支，L2772）。
可选 `条数=N` 键值，**默认 20**（`sys_log.default_show=20` 可配），保留窗口上限 50（`sys_log.max_entries=50`，细化_0 R-02）
- 返回：系统日志最近事件（来源见 §4），分行展示：`[12:00:01] G1 /重载 内容包X 成功 by 123456`。

**G9 `/玩家查询 <QQ号>`**【细化指令入口】（能力依据【框架】L1129 玩家数据导出 CSV 字段：等级/货币/活跃/最近在线；L1130 封禁名单管理）
- 权限：机主（可下授）。
- 参数：1 位置参数=QQ 号（纯数字校验）。
- 返回（脱敏，不返回背包/收益/聊天气泡等敏感明细）【细化】：`👤 玩家 123456：Lv.32｜金币 12,450｜最近在线 2 小时前｜封禁：无`。
- 未注册玩家 → 统一错误模板（目标不存在是参数级错误，与第 1 节静默语义不冲突——静默只针对「无权限」）。

**G10 `/封禁 <QQ号> [时长] [原因]`**（【框架】L1135-1140、L1301）
- 权限：GM（默认授予集）。
- 参数：QQ 号；时长（默认永久；支持 `30天`/`7天` 等，不含 `*`（数量符仅游戏内物品用，【规范】L31-36））；`原因=` 键值。
- 执行：写封禁名单（原因/时长/时间，L1130）；封禁留痕（谁封的/何时/何时到期，L1140）。
- 返回：`🚫 已封禁 123456（永久）原因：恶意刷屏`；被封玩家触发游玩指令 → 人话提示（L1137-1138）而非静默（分支见 §3.3）。

**G11 `/解封 <QQ号>`**（【框架】L1136）
- 返回：`✅ 已解封 123456`；不在名单 → `该玩家不在封禁名单 → /封禁列表 确认`（三要素模板）。

**G12 `/封禁列表`**（【框架】L1136；分页语法【框架】7.4 L1334-1346）
- 返回：分页 5 条/页：`封禁列表 第1/3页｜123456 永久（08-18 12:00 到期…）`；超长自动截断分两条（L1346）。

**G13 `/编辑`**（【框架】L1299，机主/GM）
- 返回：编辑器链接 + 权限级提示（机主=全功能，GM=只读预览【细化】，对齐编辑器 L1124-1131 运营页能力分层）。

**G14 `/设置`**（【规范】6.1 L90-97：三种前缀模式 settings.json；L96 默认 global_shortcut；L97 机主 /设置 切换）
- 权限：机主（可在 6.6 禁绑名单中 L160）。
- 参数：【细化】`/设置 command_mode=global_shortcut`（`=` 键值语法）等；切换后对该群即时生效。
- 返回：`⚙️ 本群指令模式：global_shortcut（无需 / 前缀）`。

**跨指令约束（全部 GM 指令生效）**：
- 频率控制照常：全局每指令 ≥1s / 单群每分钟 ≤20 条 / 单日总量上限（【框架】L1311；【规范】L129 频率控制照常——GM 不豁免）。
- message_id 幂等：同一条消息重复投递不重复执行（【规划】A3 L3203）。
- per-player 串行队列：同一 GM 的指令按到达顺序处理（【规划】A3 L3203）。

---

## 3. 权限分支（同指令双语义 + 快捷禁绑）

### 3.1 /日志 双分支（【框架】L2772、L2787）

| 分支 | 触发条件 | 数据源 | 展示 | 依据 |
|---|---|---|---|---|
| 玩家版 | 普通玩家（含 GM 玩家本人？——否，GM 身份优先） | 冒险日志 = longline_counters 展示层 + 环境快照 | 6 类自动记录分组展示（首杀/首钓冠级/剧情节点/隐藏发现/里程碑/图鉴新增）；传记 50 段环形 300 条 | 【框架】L2772、L2787 |
| GM 版 | 发令者权限 ≥ GM | 系统日志（§4 审计记录） | 最近事件 + 操作者 + 结果 | 【框架】L1302、L2787 |

- 分支判定在权限校验之后：权限 ≥ GM → 系统日志；否则 → 冒险日志【细化】（依据 L2787「权限分支」一词，判定键=权限，非群/非指令来源）。
- GM 玩家想看自己的冒险日志不冲突：冒险日志另有展示途径（传记入口），/日志 指令语义对 GM 固定为系统日志（L2787 口径）。

### 3.2 快捷绑定 GM 禁绑（防权限绕过）

| 层 | 规则 | 依据 |
|---|---|---|
| 绑定层 | 绑定目标为 GM 指令（重载/封禁/日志/编辑/设置...）→ 拒绝：`『重载』是 GM 指令，不可绑定为快捷`（防权限绕过） | 【规范】L160-161 |
| 白名单层 | GM 指令永不快捷（强制 / 前缀）：不入 command_shortcut_whitelist（默认清单 L117-121 无 GM 指令） | 【规范】L120-121、L128 |
| 解析顺序 | 玩家消息 → ①快捷表 → ②指令别名 → ③指令白名单 → ④忽略；快捷执行走【完整指令管线】 | 【规范】L149-152、L169 |
| 执行层 | 快捷只是别名：物品校验/冷却/MP/权限/频率/会话路由全部照常，不绕过任何校验；**GM 指令即使被意外绑定也不执行**（绑定层拦截 + 执行层权限二次检查） | 【规范】L169-171 |
| 别名层 | GM 指令不可别名化：别名配置指向 GM 指令 → 校验器拒绝【细化】（依据【规范】6.7 L189-191 别名面向世界观定制游玩指令）；keep_original 机制不适用于 GM 指令 | 【细化】 |
| 触发层 | prefix_only 模式（L92）下快捷名也要 / 前缀（`/1`）；GM 指令天然带 /，双保险 | 【规范】L177 |

**防绕过攻击面核对**（为什么禁绑能成立）：
1. 绑「快捷名=重载」→ 绑定层拒绝（L160-161）。✅
2. 绑「快捷名=/重载」（带斜杠完整串）→ 触发解析按完整消息匹配快捷名，命中后走完整指令管线 → 执行层权限校验，玩家无权限 → 静默（L169-171）。✅
3. 内容包注册别名/同名指令指向 GM 语义 → 框架指令优先权 + 别名校验器拒绝（1.4）。✅
4. 直写存档快捷表脏数据 → 执行层二次检查兜底（TC-24 验证）。✅

### 3.3 封禁提示 vs 静默无视（【框架】L1137-1139）

| 场景 | 反馈 | 依据 |
|---|---|---|
| 无 GM 权限玩家发 GM 指令 | **静默无视**（不报错不提示不写审计） | 【框架】L255、L1310、L1355 |
| 被封玩家发**游玩**指令 | 人话提示：`你已被禁止使用本游戏（原因：XXX），如有疑问请联系管理员` | 【框架】L1137-1138 |
| 被封玩家发 GM 指令 | 仍按权限判定（若其恰为 GM，GM 指令照常；封禁只禁游玩指令）【细化】 | 【框架】L1139 |

- 提示差异必须严格实现：静默分支不产生任何出站消息、不写审计（防探测）；提示分支写提示事件审计（§4 事件类 E6）。

---

## 4. 审计（GM 操作留痕）

### 4.1 审计事件分类【细化】

| 事件类 | 触发 | 依据 | 必填附加字段 |
|---|---|---|---|
| E1 内容操作 | G1 重载 / G2 备份 / G3 恢复 | 【框架】L256 指令集 | 内容包名/备份文件名/恢复回滚标记 |
| E2 数据导出 | G4 存档导出 | 【框架】L1129 | 产物路径/脱敏开关 |
| E3 广播 | G7 广播 | 【框架】L1125-1126 | 目标群数/私聊数/定时标记 |
| E4 封禁闭环 | G10 封禁 / G11 解封 | 【框架】L1140（谁封的/何时/何时到期） | 目标 QQ/时长/原因/到期时间 |
| E5 权限变更 | 机主授予/撤销 GM、per-command 授权调整 | 【细化】（对齐 L1140 留痕精神） | grantor/grantee/变更前后角色清单 |
| E6 系统事件 | 踢群（L1145）/ 磁盘预警（L1168）/ 封禁提示触发 / 备份失败 | 【框架】L1145、L1168 | 事件名/摘要 |

### 4.2 审计记录字段【细化】

```
audit_log 表（追加写，主键自增）：
  id            UUID
  ts            时间戳（服务端）
  qq           操作者 QQ（E6 无操作者=系统）
  group_id     发起群（私聊=0）
  command      指令名（G1-G14 或事件名）
  params       参数摘要（截断 200 字；不含 QQ 令牌类明文——GM 指令无密文参数，仍按最小化原则截断）
  target_qq    操作目标 QQ（E4/G9 有值）
  result        success | failed | rejected
  detail       错误模板文本摘要 / 成功摘要
  ref          关联引用（备份文件名 / 导出路径 / 快照版本号）
  audit_ts_hmac 审计行校验值【细化】（防篡改可选开关，默认开）
```

- 入源：GM 指令执行管线统一在步骤 ⑥ 后写审计（§0 决策链），成功与失败**都写**（result 区分）；**无权限调用不写**（防探测，与 L255 静默一致）。
- /日志（GM 版）的展示源 = 本表（【框架】L2787 GM=系统日志）。

### 4.3 生命周期与安全约束【细化】

| 约束 | 规则 | 依据 |
|---|---|---|
| 不可删 | 任何 GM 指令（含机主）**不可删除/清空**审计表；清理仅编辑器运维页手动操作 | 【细化】（审计可信前提） |
| 轮转 | 大小+保留份数轮转（默认保留 N 份，随磁盘水位预警 L1168 联动） | 【框架】L1164 |
| 权限 | 审计文件随 SQLite 存档目录权限 600/700（防旁路读取/篡改） | 【框架】L1154 |
| 幂等 | 审计写入与业务写入同事务（重载/恢复类先写审计事件再执行，防"没留痕的操作"）【细化】 | 【规划】A3 事务口径 |
| 展示 | GM /日志 展示本表；普通玩家永远看不到本表（对照 3.1 分支） | 【框架】L2787 |

---

## 5. 验收测试用例（TC-01 ~ TC-34，共 34 条）

> 编号规则：按 5 组需求分组连续编号；「依据」列 = 定稿行号（Lxx，可逐条回查）或本细化标注。
> 断言风格：每条均为可自动化断言的可观察状态（出站消息有无/权限判定结果/审计表行/返回文本）。

### 5.1 覆盖矩阵

| 需求组 | 覆盖点 | 用例区间 | 条数 |
|---|---|---|---|
| ① 权限模型与判定 | 三级判定/存档标记/群管理映射/静默语义 | TC-01 ~ TC-08 | 8 |
| ② GM 指令集 | 14 条指令成功路径 + 参数/越权/错误模板 | TC-09 ~ TC-20 | 12 |
| ③ 权限分支 | /日志 双分支 / 快捷禁绑 / 封禁提示差异 | TC-21 ~ TC-26 | 6 |
| ④ 审计 | 留痕字段/成败皆痕/不可删/轮转/权限变更 | TC-27 ~ TC-31 | 5 |
| ⑤ 频率与安全 | GM 频率不豁免/幂等/文件权限 | TC-32 ~ TC-34 | 3 |
| **合计** | — | TC-01 ~ TC-34 | **34** |

### 5.2 用例表

**① 权限模型与判定（TC-01 ~ TC-08）**

| 编号 | 场景 / 输入 | 期望断言 | 依据 |
|---|---|---|---|
| TC-01 | 普通玩家发 `/重载 内容包X` | 零出站消息、零审计行（静默无视，不暴露存在） | 【框架】L255、L1310、L1355 |
| TC-02 | 机主发 `/重载 内容包X` | 执行成功返回摘要；审计新增 E1 行 result=success | 【框架】L258、L1300 |
| TC-03 | GM 发 `/封禁 123456 7天 原因=刷屏` | 执行成功（默认授予集含封禁）；审计 E4 行含 target/时长/到期 | 【框架】L259、L1135、L1140 |
| TC-04 | GM 发 `/存档导出`（未下授） | 静默无视（默认授予集不含，L259 最小集） | 【框架】L259 |
| TC-05 | 群管理员（非 GM）发 `/封禁 123456` | 静默无视（不自动授予群管理）；allow_group_admin_map=false 时同断言 | 【框架】L254 |
| TC-06 | 授予 GM 后重启进程，再发 `/重载` | 权限持久化（数据库配置），重启后仍可执行 | 【框架】L254 |
| TC-07 | 被封玩家发 `/状态` | 出站人话提示含原因 `你已被禁止使用本游戏（原因：XXX）...`；非静默 | 【框架】L1137-1139 |
| TC-08 | 被封的 GM 玩家发 `/重载` | 按权限判定照常执行（封禁不禁 GM 指令）；发 `/状态` 仍被封 | 【细化】 |

**② GM 指令集（TC-09 ~ TC-20）**

| 编号 | 场景 / 输入 | 期望断言 | 依据 |
|---|---|---|---|
| TC-09 | G1 `/重载 内容包X`（配置合法） | 热重载生效：`✅ 已重载...` 摘要含各类条数与失败项 | 【框架】L1300、L42 |
| TC-10 | G1 缺参 `/重载` | 统一错误模板三要素（原因/正确用法/下一步）；审计 result=failed | 【框架】L1351-1353 |
| TC-11 | G1 `/重载 不存在包` | 错误模板列出失败项并拒绝；游玩数据不受影响 | 【框架】L1351 |
| TC-12 | G2 `/备份` | 产出 zip（存档+内容包+配置）且路径/大小返回；审计 E1 含文件名 | 【框架】L1485-1487 |
| TC-13 | G3 `/恢复`（最近备份） | 校验→自动备份当前档→原子替换；失败自动回滚无半状态 | 【框架】L1629 |
| TC-14 | G4 `/存档导出`（机主） | CSV 产物权限 600；`= + - @` 前缀单元格被转义/拒绝（防公式注入） | 【框架】L1129、L1156、L1154 |
| TC-15 | G7 `/广播 维护公告 20:00 开服` | 全部群+私聊推送，返回群数/私聊数确认；定时键值进 apscheduler | 【框架】L1125-1126、L1128 |
| TC-16 | G7 `/广播` + 消息 201 字 | 错误模板（超长拒发），不计入群推送 | 【细化】（≤200 字） |
| TC-17 | G8 GM 版 `/日志` | 展示系统日志最近 20 条（操作者/指令/结果字段） | 【框架】L1302、L2787、L1302 |
| TC-18 | G9 `/玩家查询 123456`（已注册） | 脱敏摘要：等级/货币/最近在线/封禁状态；无背包收益明细 | 【细化】（L1129 字段集） |
| TC-19 | G9 `/玩家查询 999999`（未注册） | 错误模板 `未找到该玩家`（目标不存在=参数错误，非静默） | 【细化】 |
| TC-20 | G6 `/测试` | 只读冒烟摘要返回；过程中零写操作（DB 行数/文件 mtime 探针不变） | 【细化】（锚点 L256） |

**③ 权限分支（TC-21 ~ TC-26）**

| 编号 | 场景 / 输入 | 期望断言 | 依据 |
|---|---|---|---|
| TC-21 | 普通玩家发 `/日志` | 玩家版=冒险日志：6 类分组展示、传记 50 段环形 300 条口径 | 【框架】L2772、L2787 |
| TC-22 | GM 发 `/日志` | GM 版=系统日志（数据源/格式与玩家版完全不同） | 【框架】L2787 |
| TC-23 | 玩家执行 `快捷绑定 R 重载` | 绑定层拒绝：出站 `『重载』是 GM 指令，不可绑定为快捷`；快捷表无新增 | 【规范】L160-161 |
| TC-24 | 绕过模拟：直写存档快捷表 `{"r": "/重载 内容包X"}` 后发 `r` | 执行层权限二次检查拦截：零出站、零审计、零重载副作用 | 【规范】L169-171 |
| TC-25 | GM 玩家快捷绑定 `奶 使用治疗药水*2` 并发送 | 正常生效（快捷只对本人游玩指令）；其发 `/重载` 仍需 / 前缀 | 【规范】L177、L169 |
| TC-26 | 内容包配置别名 `{"重载": {...}}` 指向 GM 指令 | 校验器拒绝加载（GM 指令不可别名化）；指令表无别名项 | 【细化】（规范 6.7 精神） |

**④ 审计（TC-27 ~ TC-31）**

| 编号 | 场景 / 输入 | 期望断言 | 依据 |
|---|---|---|---|
| TC-27 | G10 封禁后查 audit_log | 新增 E4 行：qq（谁封的）/ts（何时）/到期（何时到期）/原因 四要素齐全 | 【框架】L1140 |
| TC-28 | G1/G2/G3/G7 各执行一次（含一次失败） | 成败皆留痕，result 正确区分 success/failed | 【细化】 |
| TC-29 | 机主执行任意 GM 指令尝试清空审计表 | 不存在可删审计的 GM 指令；表数据完整（仅编辑器运维页可清理） | 【细化】 |
| TC-30 | 审计日志超大小阈值 | 按资源治理配置轮转（大小+保留份数），旧行归档不丢最新 | 【框架】L1164 |
| TC-31 | 机主授予 A 为 GM、再撤销 | E5 行两条：grantor/grantee/变更前后角色清单齐全；撤销后 A 发 GM 指令静默 | 【细化】（L1140 精神） |

**⑤ 频率与安全（TC-32 ~ TC-34）**

| 编号 | 场景 / 输入 | 期望断言 | 依据 |
|---|---|---|---|
| TC-32 | GM 2 秒内连发两次 `/重载` | 第二次触发 1s 间隔限制 → 统一错误模板（有权限超频=提示，非静默） | 【框架】L1311；【规范】L129 |
| TC-33 | 单群 1 分钟内 GM+玩家合计 21 条指令 | 第 21 条被拒（单群每分钟 ≤20 条，GM 不豁免） | 【框架】L1311 |
| TC-34 | 检查存档目录/备份/.bak 权限 | 600/700；`/存档导出` 产物 600（防旁路读取玩家数据） | 【框架】L1154 |

---

*设计依据：【框架】3.9 GM 权限系统（L252-261）、2.1 分层（L51）、7.1 指令表 GM 行（L1299-1302）、7.5 统一错误模板（L1349-1357）、封禁闭环（L1133-1141）、运营页（L1122-1131）、安全补强（L1152-1158）、资源治理（L1160-1170）、备份恢复（L1485-1487、L1500、L1615、L1629）、L.2/L.4 日志权限分支（L2772、L2787）；【规范】6.1（L90-97）、6.4-6.5 快捷清单与 GM 永不快捷（L117-129）、6.6 快捷绑定禁绑与执行铁律（L132-187）、7 兼容策略（L235-242）；【规划】A3 commands 壳层（L3201-3204）；用户设计哲学（简单优先/小白友好/不写死推测值/工程补白显式标注）。*