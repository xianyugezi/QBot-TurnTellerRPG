# 深度打造 · 实现说明（内容作者向）

> 范围：**批42 · C 节**（批41 的 C 节补做）——图纸固定/随机属性、套装词条、装备被动、
> 相性池抽取。上游依据：`docs/深度打造_决策记录.md`（§二 N1~N6、§三 补充 2、§六）、
> `docs/打造系统_原案_20260919.md` §3/§4/§8/§11/§12、`/root/deliverables/装备被动_实现口径.md`。
> 引擎落点：`qbot_rpg/core/deep_craft.py`（纯函数）；相性池唯一入口
> `qbot_rpg/core/affinity.py::resolve_available_entries`；随机源 = 玩家级随机流
> （`ctx["rng"]`，批40）。**框架不内置任何相性/池/词条名**——全部由内容包声明。

---

## 一、一句话说明

打造一次装备时：**固定项按图纸照抄**；**随机项条数由图纸/包声明**，**内容从相性池抽**
（池 = 通用池 ∪ 主相性专属池；主+副命中联动时专属池被特殊池覆盖）；**装备被动按相性
变体求值**；结果（相性值、属性、套装词条、被动）**打造时冻结进装备实例**，同模板多件互不
相同、以 `uid` 精确区分。

---

## 二、声明形状（内容包）

### 2.1 图纸（`items`/`equipment` 条目，图纸是物品的一种）

| 字段 | 类型 | 说明 |
|---|---|---|
| `blueprint_fixed_stats` | `list<obj>` | 固定属性，每行 `{stat, value, value_rule?}`；**`value` 照抄**（`value_rule` 缩放见 §六 待裁决） |
| `blueprint_random_stat_count` | `obj` | 随机属性条数 `{min, max}`，`0~2`（可配）；缺省回落 `settings.deep_craft.craft_rules.random_stat_count` |
| `blueprint_fixed_set_affix` | `str` | 固定套装词条 id（照抄） |
| `blueprint_random_set_affix_count` | `obj` | 随机套装词条条数 `{min, max}`，`0~2`；缺省回落 `craft_rules.random_set_affix_count` |
| `blueprint_random_set_affix_pool` | `str` | 可选的候选池收窄（空 = 用相性池解析结果；见 §四 边界 6） |
| `blueprint_passive` | `str` | 固定装备被动 id（引用 `traits.json`）；受相性影响时可变更为其他被动 |
| `affinities` | `obj` | 图纸相性 `{相性id: 数值}`（作为相性累计的**前置贡献**） |

### 2.2 相性四段（`settings` 顶层，打造与深度炼金共用的通用层）

```
settings.affinities[]        {id, name, desc, exclusive_pool}      相性定义 + 专属池引用
settings.affinity_pools[]    {id, kind, desc, entries[]}           kind ∈ common/exclusive/linkage
settings.affinity_linkage[]  {main, sub, override_pool}            主+副联动 → 专属池被覆盖
settings.affinity_reactions[] {kind, from, to, pair, value}        材料互动（冲突/增幅/反转）
```

**池词条行（`entries[]`）**——一个池同时服务随机属性 / 随机套装词条：

| 键 | 类型 | 作用 |
|---|---|---|
| `stat` | `str` | **随机属性候选**：命中该键即属性词条，属性键 = 该值 |
| `value` | `number` | 随机属性的数值（缺省 0；品质/等级缩放见 §六 待裁决） |
| `set_affix` | `str` | **随机套装词条候选**：值 = 套装词条 id |
| `weight` | `number ≥0` | 抽取权重（全 0/缺失 → 取候选首项） |
| `min_level` | `int ≥1` | 装备等级下限（低于则排除） |
| `requires_affinity` | `str`（单相性） | 该词条要求相性（**必须是 `{主, 副}` 的子集**，否则抽不出） |
| `effect_ref` / `value_rule` | `str` | 保留键（`effect_ref` 供后续被动/强化词条；本批消费 `stat`/`set_affix`） |

> `requires_affinity` 的**多相性列表**由引擎 `affinity.affinity_requires` 支持，但字段元数据
> 当前登记为 `str`（门禁更严，保持不变）→ 内容包请用「主专属池 + 联动覆盖池」表达
> 「主+副同时要求」，不要用列表（见 §六 待裁决）。

### 2.3 装备被动（复用 `traits.json`，不新建注册表）

```jsonc
// traits.json 条目（含相性变体的“部分被动”）
{ "id": "<被动id>", "name": "…", "effects": ["<effect id>"],
  "affinity_variants": { "<主相性id>": "<变体被动id>",
                         "<主相性id>|<副相性id>": "<变体被动id>" } }
```

- 有 `affinity_variants` = 可变更；无 = 固定被动（表达原案 §11「部分」）。
- 求值顺序：`"<main>|<sub>"` 优先 → 退 `"<main>"` → 无命中保留原被动（复用
  `affinity.resolve_affinity_effect`，不本地匹配）。

### 2.4 产物实例字段（`ItemInstance`）

| 字段 | 内容 |
|---|---|
| `stats_bonus` | 固定 + 随机属性合并后的 `{属性键: 数值}` |
| `set_affixes` | 套装词条 id（固定 1 + 随机 0~2，去重） |
| `passives` | 本次求值后的被动 id（固定 + 相性变更结果） |
| `affinities` | 本次相性结算值 `{相性id: 数值}`（主/副从中判定；供后续强化/附魔复用） |

> 随机属性的**逐条 provenance（`stat` / `value` / 来源池）**落在打造快照
> `persistent_state.deep_craft_last.random_stats`；实例本体只留合并后的 `stats_bonus`。

---

## 三、抽取顺序（一次打造内的确定性步骤）

```
1. 材料归一 → 等级 / 品质经验 / 品质等级
2. 品质抽取          ← 注入随机流第 1 个随机数（批41 语义不变）
3. cost 约束
4. 相性累计          图纸相性（前置）+ 材料相性（投入顺序）→ accumulate_affinity
5. 主/副判定         rank_affinities（最高=主、第二=副；并列按 affinities[] 声明序）
6. 池解析            resolve_available_entries(config, main, sub, level)
                     = 通用池 ∪ 主专属池；联动命中 → 主专属池被 override_pool 覆盖；
                       requires_affinity ⊆ {main,sub} 过滤；min_level 过滤
7. 条数抽取          随机属性条数（图纸声明优先，回落 craft_rules）→ 1 个随机数
                     随机套装词条条数（同上）→ 1 个随机数
8. 逐条抽取          按 weight **不放回**抽（同载荷键去重）；随机套装词条排除固定项
9. 被动求值          blueprint_passive → affinity_variants → resolve_affinity_effect
10. 冻结进实例       固定+随机属性 / 套装词条 / 被动 / 相性值
```

随机数消费顺序固定（品质 → 属性条数 → 套装条数 → 属性抽取 → 套装抽取），同种子必同结果。

---

## 四、边界行为（明确口径）

| # | 情形 | 行为 |
|---|---|---|
| 1 | **无相性**（`main=None`） | `requires_affinity` 非空的词条**全部抽不出**（原案 §12「本质一个池子，没有对应相性就抽不出来」）；通用池中不要求相性的词条仍可抽 |
| 2 | 主/副**并列** | 按内容包 `affinities[]` 声明顺序破平；无声明序则按 id 升序（确定性） |
| 3 | **副相性缺失** | `sub=None`；联动不命中 → 回主专属池；要求副相性的词条抽不出 |
| 4 | 联动命中 | 主专属池**整体被 `override_pool` 覆盖**（原案 §8「特殊联动池也是专属池的一种，触发会覆盖掉专属池」） |
| 5 | 候选不足声明条数 | 实际条数 = `min(声明条数, 候选数)`，**不补空**、不降级到其他池 |
| 6 | `blueprint_random_set_affix_pool` 声明 | 在**已解析候选**中再按 `_pool` 收窄；声明池不在「通用 ∪ 专属/联动」可达集内 → 无候选 = 抽不出（不外开第二条池查询） |
| 7 | 权重全 0/缺失 | 取候选首项（确定性；**不引入未声明概率**），仍逐项去重 |
| 8 | 固定套装词条 | 随机抽取时**排除**固定项，避免重复 |
| 9 | 无 `traits` 定义 / 被动无变体 | 被动 id 原样保留（框架不臆造） |
| 10 | 注入随机源缺失/非法 | 条数取 `min`、抽取退化取首项（确定性，不报错） |

---

## 五、与既有 `forge_sets`（套装档位/激活）的关系

**核查结论：产物上的套装词条目前无法被既有套装结算识别——如实登记为缺口。**

- 既有套装结算（`core/forge_sets.py` + `core/forge_set_skills.py`）的件数来源是
  **锻造节点 id**：`player["set_tracker"]`（族 id→件数）优先，否则从
  `equipped`/`equip_nodes`/`equip_snapshot` 取条目的 `node_id`/`id`（`forge_sets.py:116-118,687-713,730-739`），
  与 `forge.json sets[].pieces` 求交。
- 深度打造产物的身份是 `ItemInstance`（`item_id` + `slot` + `uid` + `set_affixes`），
  **没有锻造节点 id**；`EquipmentSlot` 只存 `item_id/name/slot_level/locked/gems/uid`，
  也不携带套装词条。因此「同名套装词条件数 → 激活」这条链**当前未接通**。
- 文档化的 `INS-07 set_tag` 消费方**未实现**（全仓 `grep set_tag` 仅命中注释）。
- **建议复用路径（待裁决后再接线）**：在唯一已穿戴件枚举点（`equip_mods._worn_defs`，
  与副手失活同源）按穿戴实例的 `set_affixes` 归并族件数写 `set_tracker`，即可让既有
  `set_lookup`/`resolve_set_skills` 零改动识别打造套装。**前置待裁决**：套装词条 id 与
  `forge.json` 套装族 id 的对应关系（原案未定义；是否要求同名，或需一处映射声明）。

---

## 六、待裁决（原案/决策记录未写清；本批取工程补白并显式标注）

| # | 项 | 本批口径 | 影响 |
|---|---|---|---|
| C-1 | 图纸相性在相性累计中的位置 | 作为**前置贡献**（其后按材料投入顺序） | 影响互动触发顺序；参数化不需改码 |
| C-2 | `blueprint_random_set_affix_pool` 语义 | 在 `resolve_available_entries` 结果上**收窄**（不外开第二条池查询，守「池查询唯一入口」红线） | 声明池不可达 → 抽不出 |
| C-3 | 固定/随机属性的**数值缩放** | `value` **照抄**（`value_rule` 暂不消费） | 原案 §3「基础数值由品质、等级决定」未落公式 |
| C-4 | `requires_affinity` 多相性列表 | 引擎支持，字段元数据限 `str`（门禁不减）；「主+副同时要求」用联动覆盖池表达 | 若要包内直接写列表需重定字段门禁 |
| C-5 | 套装词条 ↔ `forge_sets` 族 id 的映射 | **未接**（见 §五） | 产物套装词条暂不计入套装激活 |
| C-6 | 随机属性数值的来源池字段 | 新增池词条键 `value`（随机属性值） | 若改由公式生成，需替换该键消费 |

---

## 七、`ItemInstance` 构造点清单（批42 新字段透传核对）

`ItemInstance(` 在框架内共 **6 处**非测试构造点；新字段 `affinities`/`set_affixes`/`passives`
必须逐处透传，漏一处即在落档/穿脱/装备流转中丢数据：

| # | 位置 | 角色 | 批42 处理 |
|---|---|---|---|
| 1 | `qbot_rpg/storage/repository.py::_item_from_dict` | 存档 JSON → 实例（**读档**） | ✅ 逐字段读回（缺省空） |
| 2 | `qbot_rpg/assembly/runner.py`（ctx `inventory_instances` → `Player.inventory`） | 打造/背包落地 | ✅ 原样带过 |
| 3 | `qbot_rpg/commands/basic_commands.py`（`_equip` 的 Mapping 归一） | asdict dict 行 → 引擎实例（**穿戴**） | ✅ 原样带过 |
| 4 | `qbot_rpg/core/equipment.py:754`（`unequip` 回包兜底重建行，uid 命中分支） | 槽实例 → 背包行 | ⚪ 走 `dataclasses.replace` 保留；本处仅在**背包行缺失**时重建，无来源（既有口径：槽不含词条） |
| 5 | `qbot_rpg/core/equipment.py:760`（同上 `TypeError` 兜底） | 兼容旧实例 | ⚪ 同上（空缺省） |
| 6 | `qbot_rpg/commands/shop_tx.py::_new_default_instance` | 商店购买新建（无实例来源） | ⚪ 空缺省（商店货无打造随机内容） |

> 序列化侧 `dataclasses.asdict`（`repository._asdict_row`）自动包含新字段，无需改。
> 安装/卸下时穿戴行保留在背包（`_worn_refs` 进程态引用 + 槽 `uid` 回指），走
> `replace` 保留字段，故 #4/#5 只在「背包行丢失」的降级路径触发。

---

## 八、复跑与证据

```bash
# 核心单测（相性池 / 无相性抽不出 / 联动覆盖 / 条数边界 / 固定照抄 / 确定性 / 被动变体）
pytest tests/unit/test_batch42_craft_affixes.py -q -o addopts=""

# 端到端（临时内容根 /精造 产词条装备 + 同 id 互不污染 + 构造点往返）
pytest tests/unit/test_batch42_craft_affixes_e2e.py -q -o addopts="" -s   # 打印 PRODUCT_JSON

# 批41 回归对拍（等级/品质/概率表/cost/防退化不受影响）
pytest tests/unit/test_batch41_deep_craft.py tests/unit/test_batch41_deep_craft_e2e.py -q -o addopts=""
```

实测产物摘要（e2e，`PRODUCT_JSON`）：

```json
{"stats_bonus": {"基础攻": 20.0, "联动甲": 91.0, "联动丙": 93.0},
 "random_stats": [{"stat": "联动甲", "value": 91.0, "pool": "池联动"},
                  {"stat": "联动丙", "value": 93.0, "pool": "池联动"}],
 "set_affixes": ["套·核心", "套·联动丙", "套·联动乙"],
 "passives": ["被动·联动"],
 "affinities": {"测试火": 10.0, "测试冰": 2.0}, "quality": "红"}
```

（测试自造 id，非任何真实内容包。）

---

## 九、批 43：强化六档上限 + 每 N 级特殊词条（构造点与证据）

### 9.1 `ItemInstance` 构造点清单（批43 新字段 `quality_level` / `enhance_affixes`）

| # | 位置 | 角色 | 批43 处理 |
|---|---|---|---|
| 1 | `qbot_rpg/storage/repository.py::_item_from_dict` | 存档 JSON → 实例（**读档**） | ✅ 逐字段读回（缺省 0/空） |
| 2 | `qbot_rpg/assembly/runner.py`（ctx `inventory_instances` → `Player.inventory`） | 打造/强化落地 | ✅ 原样带过 |
| 3 | `qbot_rpg/commands/basic_commands.py`（`_equip` 的 Mapping 归一） | asdict dict 行 → 引擎实例（**穿戴**） | ✅ 原样带过 |
| 4 | `qbot_rpg/core/equipment.py:754`（`unequip` 回包兜底重建行，uid 命中分支） | 槽实例 → 背包行 | ⚪ 走 `dataclasses.replace` 保留；仅在**背包行缺失**时重建，无来源（既有口径：槽不含词条/等级） |
| 5 | `qbot_rpg/core/equipment.py:760`（同上 `TypeError` 兜底） | 兼容旧实例 | ⚪ 同上（空缺省） |
| 6 | `qbot_rpg/commands/shop_tx.py::_new_default_instance` | 商店购买新建（无实例来源） | ⚪ 空缺省（商店货无强化词条/品质等级） |

> `dataclasses.asdict` 自动包含新字段；深度打造产物在
> `commands/deep_craft_commands.py::_land` 写 `quality_level`（强化上限依据），
> `enhance_affixes` 初始空。

### 9.2 复跑与证据

```bash
# 批43 核心单测（逐档上限 / 旧档兼容 / 每 4 级 / 跨度可配 / 无相性抽不出 / 占位不接引擎）
pytest tests/unit/test_batch43_enhance_tiers.py -q -o addopts=""

# 既有强化回归（成功率/失败段/档位行为）
pytest tests/unit/test_enhance_commands.py -q -o addopts=""
```

实测摘要（合成样本）：
- 品质等级 1~6 → 上限 `+3/+6/+9/+12/+15/+18`（逐档断言，`_CAP` 合成表）。
- 旧档普通 +5：`/强化信息` 显示 `强化上限 +6`，`/强化 铁剑+5` → `铁剑+6`（不降级）。
- 跨度 4：`+3→+4` 得 1 条、`+7→+8` 得第 2 条、`+11→+12` 得第 3 条；跨度改 3：`+2→+3` 即得 1 条。
- 无相性且池词条均 `requires_affinity` → `enhance_affixes == []`（负向同批42）。
- `cooldown_reduction_pct`：`extract_bonus` 保留、详情面板显示「冷却缩减」、
  `route_bonus_into` 不拆进 pct 层（占位不接引擎）。

---

## 十、批 44：投入概率暴击（内容作者向 · 彩档品质 10 可达性）

> 上游：`docs/深度打造_决策记录.md` §七（用户裁定「投入概率暴击」解彩档 q10 可达性，
> **不**拉高 cap、**不**强制扩材料表）；`打造_材料表与图纸表_草案v2.md` §3。
> 引擎落点：`qbot_rpg/core/deep_craft.py::quality_exp`（判定期在该函数内，品质等级
> 判定之前）；随机源 = 批40 玩家级随机流（`ctx["rng"]`，可注入确定性 RNG 复现）。

### 10.1 一句话

投料提交时按**图纸档**掷一次概率，命中则给**额外品质经验**（倍率可配），使高档图纸有机会
冲破常规经验上限达到**品质 10**。全部参数包声明、可调；**缺省关闭**——不声明时引擎不掷、
不消耗随机数，行为与既有**逐字段一致**。

### 10.2 参数表（`settings.deep_craft.craft_rules.quality_exp_crit`，R5 落点裁定）

| 键 | 类型 | 默认 | 范围 | 说明 |
|---|---|---|---|---|
| `enabled` | bool | `false` | — | 总开关；**缺省关**（不声明 = 零变化，且不消耗随机数） |
| `grades` | `list<str>` | `[]` | 图纸档 id | 适用档；**空 = 全档**，可只开高档（如 `["金","彩"]`） |
| `chance_by_grade` | `obj` | 铜 .15 / 银 .10 / 金 .07 / 彩 .05 | 每值 0~1 | **逐档暴击概率**（低档更高、高档更低，§七建议值，待实测校准） |
| `chance_default` | number | `0.05` | 0~1 | 档未在 `chance_by_grade` 声明时的概率 |
| `mult_by_grade` | `obj` | 铜/银/金/彩 = `2.0` | 每值 ≥0 | **逐档暴击倍率** |
| `mult_default` | number | `1.0` | ≥0 | 档未声明时的倍率（1.0 = 不加成，只记暴击标记） |
| `additive_exp` | number | `0.0` | 任意 | 命中时在倍率之外追加的**固定**品质经验 |
| `applies_to` | enum | `quality_exp` | `quality_exp` | 作用目标（当前仅品质经验） |
| `affects_quality_level` | bool | `true` | — | `false` = 暴击经验只展示，**品质等级仍按暴击前经验**判定 |
| `rolls_per_craft` | int | `1` | 1~2 | 每次打造掷数；任一命中即暴击（>1 显著抬高期望） |
| `exp_cap` | str/num | `last_threshold` | `none`/`last_threshold`/数值 | 生效档位最终经验封顶；`last_threshold` = 取阈值表末项 |
| `rng_stream` | enum | `player` | `player` | 随机流标记（引擎只走注入的玩家级随机流） |

> **档 id 必须与 `blueprint_grades[].id` 一致**（引擎按图纸 `blueprint_grade` 查表，不认
> 任何固定档名）。若内容包用自有档位 id，请在自己的 `chance_by_grade` / `mult_by_grade`
> 里声明，或设 `chance_default` / `mult_default`（**默认 `mult_default=1.0` 不会加成**）。

### 10.3 声明示例

```jsonc
// settings.deep_craft.craft_rules
"quality_exp_crit": {
  "enabled": true,
  "grades": ["彩"],                       // 只开彩档；[] = 全档
  "chance_by_grade": { "铜": 0.15, "银": 0.10, "金": 0.07, "彩": 0.05 },
  "chance_default": 0.05,
  "mult_by_grade": { "铜": 2.0, "银": 2.0, "金": 2.0, "彩": 2.0 },
  "mult_default": 1.0,
  "additive_exp": 0.0,
  "applies_to": "quality_exp",
  "affects_quality_level": true,
  "rolls_per_craft": 1,
  "exp_cap": "last_threshold",
  "rng_stream": "player"
}
```

### 10.4 语义与随机消费顺序

```
1. 材料归一 → 等级 → 暴击前品质经验 QE0 = Σ(品阶基数 × 相性 × 件数边际) × 种类奖励 × 互动
2. 投入暴击判定        ← 仅当 enabled 且档位适用且概率 >0 时消费随机数（rolls_per_craft 个）
                        命中：QE = QE0 × mult(+档) + additive_exp；未命中：QE = QE0
3. 经验封顶            生效档位：QE = min(QE, exp_cap)（last_threshold → 阈值末项）
4. 品质等级            按 exp_for_level（affects_quality_level=false → 用 QE0）判级
5. 品质抽取            注入随机流（批41 首位随机语义在**未启用暴击时不变**）
```

- **不生效/未命中不改变既有随机序列**：`enabled=false`、档位不适用、概率 ≤0、`rng` 缺失
  时**一次随机数都不消费** → 品质抽取与后续词条抽取与现状一致。
- 启用后掷数**全部消费**（`rolls_per_craft` 个），任一命中即暴击 → 消费次数确定，同种子同结果。
- 暴击只加品质经验：**不改等级、不改 cost、不改相性累计**。

### 10.5 边界行为

| # | 情形 | 行为 |
|---|---|---|
| 1 | `enabled=false` / 未声明 | 不掷、不消费 → 计划逐字段等于现状（对拍断言：显式关 == 缺省） |
| 2 | 档位不在 `grades` | 不生效、不消费 |
| 3 | 概率 ≤0 | 不掷、不消费 |
| 4 | `applies_to ≠ quality_exp` | 不生效（保守，不外溢到其他计算） |
| 5 | `mult_default=1.0` 且档未声明倍率 | 记为暴击但经验不加成（作者须显式声明倍率） |
| 6 | `exp_cap=last_threshold` 而阈值表为空 | 不封顶 |
| 7 | `affects_quality_level=false` | `quality_exp` 为暴击后值，品质等级按暴击前值 |
| 8 | `rng` 缺失/非法 | 不掷（退化为不暴击），不报错 |
| 9 | `rolls_per_craft>2` | 校验器红拦（R-2，范围 1~2） |

### 10.6 文案（模板表）

暴击命中时在成功文案后**追加一行**（未命中不追加 → 既有输出形态零变化）：

```jsonc
"deep_craft_crit_gain": "品质经验暴击！本次 ×{crit}"
```

- 占位符 `{crit}` = 实际倍率（如 `2`）；已在 `tests/unit/_template_frozen_sets.py`
  `FROZEN_PLACEHOLDERS` 登记。
- **禁 emoji**（emoji 由前端/渲染层加）；内容包可在 `templates.json` 覆盖该键。

### 10.7 复跑与证据

```bash
# 核心单测（两条路径 / 概率统计 / 按档不同 / 回归对拍 / 编辑器可见 / 校验器）
pytest tests/unit/test_batch44_crit.py -q -o addopts=""

# 端到端（临时内容根：彩档暴击达品质 10 / 不暴击达不到）
pytest tests/unit/test_batch44_crit_e2e.py -q -o addopts=""

# 批41/42/43 回归（暴击缺省关 → 既有验收不受影响）
pytest tests/unit/test_batch41_deep_craft.py tests/unit/test_batch41_deep_craft_e2e.py \
       tests/unit/test_batch42_craft_affixes.py tests/unit/test_batch42_craft_affixes_e2e.py \
       tests/unit/test_batch43_enhance_tiers.py -q -o addopts=""
```

实测摘要（E2E 彩档：4 种红料 2/2/1/1，同相性 ×1.5，种类奖励 1.03³）：

| 路径 | 暴击前 QE | 暴击 | 最终 QE | 品质等级 |
|---|---|---|---|---|
| 强制暴击（RNG 0.0 < 5%） | 991.65 | 是（×2） | 1080（封顶 last_threshold） | **10** |
| 强制不暴击（RNG 0.999） | 991.65 | 否 | 991.65 | 9（**达不到 10**） |

（概率统计：逐档 1000 次实际触发率与声明值偏差 ≤0.02（实测 ≤0.005）；测试用固定种子，不 flaky。）

### 10.8 与「拉高 cap / 扩材料表」的关系

本机制**不改变** `blueprint_grades[].cost_cap` 默认值、**不扩充**材料表、**不改**品质概率
阶梯 13 行——彩档 q10 由「5% 概率 ×2 品质经验」实现（期望约 20 次打造一次）。若后续需要
校准，只改包声明的 `chance_by_grade` / `mult_by_grade` / `exp_cap`，**引擎零改动**。

---

## 十一、批 45：装备占比校准（内容作者向 · 面板预算与怪物数值倍率）

> 上游：`docs/深度打造_决策记录.md` §三 补充 1（用户 2026-09-19：装备占比 → 60%，其他数值不动）
> + §十 落地登记。引擎落点：`core/panel_budget.py`（纯参数/纯函数）、
> `core/equipment.py::aggregate_bonus`（装备侧）、
> `commands/battle_launch_commands.py::_enemy_combatant`（怪物侧）。
> 对照报告：`docs/批45_装备占比校准_测量报告.md`。**缺省全部 1.0 → 不配置零变化。**

### 11.1 一句话

把「装备这一份预算」乘以可配倍率（veinborn 校准 ×2.25 → 装备份额 8→18、占比 40%→60%、
面板总功率 ×1.5），白值与 buff 绝对数值不动；怪物侧按同比例重校（hp/atk ×1.5），
防御按斩杀回合不变式补偿，**中值档斩回逐只不变**。

### 11.2 参数表（`settings` 顶层）

| 键 | 类型 | 默认 | 范围 | 说明 |
|---|---|---|---|---|
| `panel_budget.white` | number | 7 | ≥0 | 面板预算·白值份（占比分母；**冻结**） |
| `panel_budget.equip` | number | 8 | ≥0 | 面板预算·装备基准份；有效份 = ×倍率 |
| `panel_budget.buff` | number | 5 | ≥0 | 面板预算·buff份（**冻结**） |
| `panel_budget.equip_stat_mult` | number | 1.0 | ≥0 | 装备面板倍率（veinborn **2.25**） |
| `monster_scaling.hp_mult` | number | 1.0 | ≥0 | 怪物 hp/max_hp 倍率（veinborn **1.5**） |
| `monster_scaling.atk_mult` | number | 1.0 | ≥0 | 怪物攻击（str→atk）倍率（veinborn **1.5**） |
| `monster_scaling.def_factor` | number | 1.0 | ≥0（建议 0.5–2.0） | 防御仿射补偿 `(con+K)×f−K`（veinborn **1.0828**） |
| `monster_scaling.def_k` | number | 100 | ≥0 | 防御系数常数 K（同 `defense.k`） |

**装备占比公式**：`equip×equip_stat_mult ÷ (white + equip×equip_stat_mult + buff)`。
`7:8:5` + 1.0 → 40%；+2.25 → `18/(7+18+5)` = **60%**。

### 11.3 引擎语义（内容作者须知）

- **装备倍率只作用「面板轴」`atk/dfn/hp`**（flat 与对应 `_pct` stem）；会心/耳栓/超会心/属性会心/
  吸血/免伤/物法穿（`data.gear_stats.GEAR_COMBAT_KEYS`）与其余属性（力量/敏捷/专注…）**不缩放** ——
  故暴击、命中、格挡参数不变。
- **读时聚合**：倍率不改背包实例的 `stats_bonus`，也不改 `items.json`/`forge.json` 的原始数值；
  把 `equip_stat_mult` 改回 `1.0` 即**完全回滚**。
- **既有存档**：倍率在「穿戴/重算」时写入加成层；已穿戴角色需再穿脱一件（或重建）才反映新倍率；
  新建/新穿戴直接生效。
- **怪物侧**：只缩放 hp/atk 与防御补偿；mp/mag/spd/foc/lck/agi 与战斗词条保持原值
  （命中/闪避/格挡/会心参数不变）。`def_factor` 使防御系数与攻击的**比**可控：
  `def_factor = R / 比例`（R = 面板 atk 实测比值，比例 = hp 倍率）。

### 11.4 校验器 / 编辑器

- 校验器：`settings.panel_budget` / `settings.monster_scaling` 段结构、类型、负值红拦；
  三份全 0 → 黄提示 **Y-16**；`def_factor` 超 [0.5, 2.0] → 黄提示 **Y-17**（只提示不拦）。
- 编辑器：两个段的中文名 + 逐字段说明卡可见（`content/field_meta.py` 登记）。

### 11.5 复跑与证据

```bash
# 单测（占比 / 只作用面板轴 / 缺省回归对拍 / 怪物倍率 / 斩回不变式 / 编辑器 / 校验器）
pytest tests/unit/test_batch45_panel_calibration.py -q -o addopts=""

# 修前/修后测量（读 content/veinborn 真实数据；种子 20260919）
python3 scripts/batch45_measure.py
python3 scripts/batch45_measure.py --json
```

实测摘要（见对照报告 §3）：中值档斩回 7/20/24 → 7/20/24（**完全一致**）；
预算占比 40.00% → **60.00%**；白值 360/251/808 与 buff 128.57 **未变**。

---

## 十二、批 46~48：符文附魔（内容作者向 · 三阶 / 跨装备类型 / 2·3 阶效果 / 3 合 1）

> 上游：原案 `打造系统_原案_20260919.md` §9；口径 `/root/deliverables/符文系统_实现口径.md`
> §〇 结论 7/8、§三、§五 43-A~D；决策 `docs/深度打造_决策记录.md` H1/H2 + §十一~§十三。
> 数据模块：`content/<包>/runes.json`（list）。引擎：`core/runes.py`（纯解析）、
> `core/jewel.py`（孔位/镶嵌）、`core/equipment.py`（数值聚合）、`core/upgrade.py`（3 合 1）、
> `core/battle.py`（2/3 阶战斗接线）。**总闸 = `settings.deep_craft.enabled`（默认关）。**

### 12.1 一条符文 = 一个条目（`runes.json`）

```jsonc
{
  "id": "rune_x",            // 必填、模块内唯一（命名空间独立于 items.id）
  "name": "…", "icon": "…", "desc": "…",
  "tier": 2,                 // 必填 1/2/3（独立刻度，禁止借用 quality 四档）
  "family": "…",             // 必填；同族才允许互为 3 合 1 输入
  "by_equip_type": {         // 差异表：default 必填；键 = items.type
    "default": { "stats": { "atk": 6 } },
    "weapon":  { "stats": { "atk": 9 } }   // 命中类型条目「顶层浅覆盖」default（stats 整块替换）
  },
  "effects": [ { "effect": "…", "trigger": "…" } ],  // 2/3 阶效果（见 12.3）
  "bias": { "affinity": "…", "bonus_pct": 0 }        // 3 阶偏向（见 12.4）
}
```

- `stats` 键**只能**取 `data/gear_stats.py` 的 `GEAR_NUMERIC_KEYS`（`_pct` 后缀 = 百分点）；
  自造键由校验器 **RUNE-05** 红拦。
- 一阶符文：`default.stats` 必须非空（**RUNE-05**）；数值经 `aggregate_bonus` 与普通装备词条
  **同层** flat/pct 汇入面板与战斗（副手折算件按副手失活 → 符文不生效）。
- 类型键取 `items.type`（Q5 默认项①）；未声明的类型 → 回落 `default`。

### 12.2 3 阶偏向性（`bias`，默认口径 = 偏向某相性）

```jsonc
"bias": { "affinity": "fire", "bonus_pct": 50 }
```

- 宿主装备的**主/副相性**（`items.affinities` → 相性通用层 `core/affinity.rank_affinities`）
  命中 `affinity` → 该符文数值贡献 **× (1 + bonus_pct/100)**；未命中 / 无 `bias` → 原值。
- `bonus_pct` 可 0（等价未声明）/可负（偏科惩罚）。
- 校验：`affinity` 必填非空（**RUNE-07 红拦**）；`bonus_pct` 非数红拦；低阶声明 `bias`、
  未知相性 id → 黄提示（`settings.affinities` 已接线时才查引用）。

### 12.3 2/3 阶特殊效果（`effects` + `trigger`）——两条铁律

```jsonc
// runes.json 里的符文条目：声明「在什么时点、引用哪个效果」
"effects": [ { "effect": "fx_grievous_on_hit", "trigger": "on_hit" } ]

// effects.json 里被引用的「效果容器」：只放 actions，**不要写 trigger**
{ "id": "fx_grievous_on_hit", "class": "special",
  "actions": [ { "type": "status_apply", "status": "s_grievous", "target": "enemy" } ] }
```

1. **`trigger` 只写在符文引用条目上**，取 `core/event_dispatcher.EVENT_POINTS`
   （`battle_start` / `action_start` / `on_hit` / `turn_end` …）。效果容器**不带 `trigger`** →
   才不会被全局事件分派器对所有战斗单位触发（否则敌方也会获得该符文效果）。
2. 效果内容仍由 `effects.json` 定义（口径 §三.2：符文不得内联执行语义）；
   `overrides` 可按符文覆盖参数；`target`（`self`/`enemy`）可写在引用条目上。
3. 只有**符文穿戴者一侧**在这些时点触发；副手折算件经 `active_rune_sockets` → 不触发。
4. 触发计数沿用既有上限（每次行动 10 / 每场 99，`EffectRuntime.config` 可配）。

**例 ①「重伤」（治疗削减）** —— 给目标挂一个带 `heal_taken` 修正的状态：

```jsonc
// statuses.json
{ "id": "s_grievous", "name": "重伤", "category": "harm",
  "stack_frame": "single", "duration": { "turns": 3, "charges": 0 },
  "actions": [ { "type": "stat_modifier", "stat": "heal_taken", "value": -40 } ] }
```

被该状态覆盖的目标受到治疗时，治疗量 **×(1 + heal_taken/100)**（-40 → 治疗仅 60%）；
下限 0（不反向扣血），封顶复用 S6 `max_boost_pct`（默认 ±100，可配）。该键为引擎语义键
（`core/effects.HEAL_TAKEN_STAT`），正=受治疗增幅、负=削减，数值全由内容声明。

**例 ②「每次攻击获得物攻加成，上限 n」** —— 叠层状态 + `action_start` 引用：

```jsonc
// statuses.json：层数上限 n = max_stack（包声明；不写则回落引擎 stack_default_max）
{ "id": "s_atk_build", "name": "蓄势", "category": "buff",
  "stack_frame": "stack", "max_stack": 5, "duration": { "turns": 3, "charges": 0 },
  "actions": [ { "type": "stat_modifier", "stat": "atk", "value": 4 } ] }

// effects.json
{ "id": "fx_atk_build", "class": "special",
  "actions": [ { "type": "status_apply", "status": "s_atk_build", "target": "self" } ] }

// runes.json
"effects": [ { "effect": "fx_atk_build", "trigger": "action_start" } ]
```

每次攻击 +1 层 → 加成 = 层数 × `value`（百分点，先按层求和再走 S6/S7 封顶）；
达 `max_stack` 后不再增。**上限一律包声明**（`max_stack`）。

> 实现注记（内容作者无须关心）：既有引擎 S5「同来源同侧只保留一个」下同来源不会叠层，
> 故符文接线侧对每次施加使用区分来源 `侧@行动序号`，让 S3 stack 真正逐次累层；
> `_aggregate_boost` 已按 `stacks` 乘算（批48 修正，含逐字段回归对拍）。

### 12.4 3 合 1 进阶（`recipe.json`，`kind=upgrade`）

```jsonc
{ "kind": "upgrade", "subtype": "rune_upgrade", "id": "rcp_rune_1_2",
  "inputs": [ { "item": "r_t1", "count": 3 } ],
  "output": { "item": "r_t2", "count": 1 },   // 可加 "rune_tier": 2 显式声明产出阶（回退）
  "cost": { "gem": 10 },
  "synth_allowed": true }                      // false = 深度未解锁 → 拒绝
```

- 3×同阶同 id → +1 阶；**禁跳级**（产出阶必须 == 输入阶+1）、**满阶拒绝**、**必成**。
- 闸：`synth_allowed=false` → `runes_deep_locked`；`settings.deep_craft.enabled` 非 true
  （且 ctx 带该段）→ `runes_disabled`；非法/不足/产出 hook 失败 → **零副作用**（原子回滚）。
- 越阶/跳级/不同 id/件数不足一律拒绝（`rune_skip_tier` / `rune_input_mismatch` / …）。
- 1→2→3 逐阶：3×一阶 → 二阶；3×二阶 → 三阶（链终点，无更高阶）。

### 12.5 复跑与证据

```bash
# 批48 单测（重伤/层数/2 阶接线/3 阶偏向/3 合 1 端到端/回归对拍）
pytest tests/unit/test_batch48_runes_effects.py -q -o addopts=""
# 批46/47 地基与数值（不受影响）
pytest tests/unit/test_batch46_runes.py tests/unit/test_batch47_runes_numeric.py -q -o addopts=""
```

实测摘要（详见批48 报告）：重伤 100 → 60；每次攻击 5/10/15/20/20/20（上限 4 层，可配）；
3 阶偏向 fire 主相性 atk 10 → 15；3 合 1 逐阶产物 r_t1×3→r_t2、r_t2×3→r_t3；回归对拍 0 差异。

---

## 十三、批 50：特效轴地基（内容作者向 · 一条轴怎么写、两个方向怎么填）

> 口径：`特效整理设计_1_修正轴全集.md`（轴全集）+ `特效整理设计_3_落点与分期.md`
> §1.0/§1.2 + 决策记录 §十三「双向修正轴优先」。
> **本批只登记：键族 + 声明段 + 校验 + 编辑器可见，不接任何消费点**——作者现在可以
> 写这些轴、编辑器看得见、校验器会拦错，但**引擎还没读**（接线批次在后续批）。
> 这就是为什么每条轴都注明「唯一消费点（待接）」：**没写消费点的轴不登记**，
> 免得出现「作者写了不生效」的陷阱键（`cooldown_reduction_pct` 的教训）。

### 13.1 一句话

**一条轴覆盖双向玩法**：减疗与增疗不是两条轴，是**同一个轴的两个取值**。

### 13.2 作者怎么写（装备/物品词条）

在 `items.json` / `equipment.json` 的条目顶层直接写轴键即可（与 `atk`/`dfn_pct` 同处）：

```jsonc
{ "id": "heavy_gauntlet", "name": "重伤护手", "slot": "weapon",
  "healing_received_pct": -50 }        // 受疗 ×0.5：被治疗打五折
```

**书写口径 = 百分点增量**（不是乘数、不是百分比正数）：

```
有效值 = 基准值 × (1 + 轴值 / 100)
```

| 想表达 | 写法 | 等价的乘数说法 |
|---|---|---|
| 受疗打五折（重伤） | `healing_received_pct: -50` | ×0.5 |
| 原样（默认） | 不写 / `0` | ×1.0 |
| 受疗加半（增疗） | `healing_received_pct: +50` | ×1.5 |
| 禁疗 | `healing_received_pct: -100` | ×0 |

**加算轴**（层数上限、行动条推动）不是百分比，直接写差值、键名**没有** `_pct` 后缀：
`stack_cap_delta: 2` = 层数上限 +2 层。

### 13.3 一条轴的两种填法 = 两个方向（不要建两条 trait）

`重伤` 与 `强化治疗` 是**同一条轴**的两个取值。内容侧**不得为两个方向各建一条 trait**
（否则相性池与图鉴会重复计数）：

| 预设 | 填法 |
|---|---|
| 重伤 | `healing_received_pct: -50` |
| 禁疗 | `healing_received_pct: -100` |
| 强化治疗 | `healing_received_pct: +50` |
| 易伤 | `damage_taken_pct: +30` |
| 铁壁 | `damage_taken_pct: -30` |

### 13.4 本批登记的 15 条轴（P0 8 / P1 7）与「唯一消费点（待接）」

| 轴键 | 轴全集 | 级 | 建议范围 | 计量 | 唯一消费点（本批待接） |
|---|---|---|---|---|---|
| `healing_received_pct` | X17 | P0 | -200 ~ +300 | mult | `effects.heal_apply`（受疗侧 target） |
| `healing_done_pct` | X16 | P0 | -100 ~ +300 | mult | `effects.heal_apply`（施疗侧 source） |
| `damage_taken_pct` | X02 | P0 | -100 ~ +300 | mult | `battle.damage_taken_mult`（终伤乘区） |
| `damage_dealt_pct` | X01 | P0 | -100 ~ +1000 | mult | `damage.total_damage` |
| `cooldown_pct` | X27 | P0 | -80 ~ +200 | mult | `battle.skill_cooldown` |
| `status_chance_pct` | X21 | P0 | -100 ~ 不限 | mult | `effects.status_apply_chance` |
| `stack_gain_pct` | X23 | P0 | -100 ~ 不限 | mult | `effects.stack_gain` |
| `stack_cap_delta` | X24 | P0 | 不限（加算） | delta | `effects.stack_cap` |
| `status_duration_pct` | X20 | P1 | -80 ~ +300 | mult | `effects.apply_status_duration`（source） |
| `status_duration_taken_pct` | X20 | P1 | -100 ~ +300 | mult | `effects.apply_status_duration`（target） |
| `status_resist_pct` | X22 | P1 | -100 ~ 不限 | mult | `effects.resist_roll` |
| `action_bar_shift` | X30 | P1 | 不限（加算） | delta | `ctb_scheduler.action_bar_shift` |
| `resource_cost_pct` | X34 | P1 | -100 ~ +200 | mult | `resource_axis.pay_cost` |
| `resource_gain_pct` | X35 | P1 | -100 ~ 不限 | mult | `resource_axis.apply_gain` |
| `crit_damage_pct` | X03 | P1 | -100 ~ +300 | mult | `damage.crit_multiplier` |

**唯一源**：`qbot_rpg/data/gear_stats.py` 的 `EFFECT_AXIS_SPECS`（新增轴 = 这张表加一行，
路由/桥/展示/编辑器/校验自动跟进）。

### 13.5 范围与默认值是可配的（不写死）

在 `settings.json` 加 `effect_axes` 段即可覆盖**建议缺省**（不改引擎代码）：

```jsonc
{ "effect_axes": {
    "healing_received_pct": { "min": -300, "max": 500, "default": 0,
                              "display": { "mode": "mult", "label": "受疗修正" },
                              "stack": "add" } } }
```

- **不写该段 = 用框架建议缺省**（增量轴默认 0 = ×1.0 恒等 → 引擎零变化）；
- 范围**只是声明与校验依据**，消费点读它、**不许**在引擎里硬编码钳制；
- `display.mode`：`mult` = 编辑器按乘数理解（`<1` 减弱 / `>1` 增强）；`delta` = 加算差值。
- `legacy_alias`：旧键 → 本轴的换算声明（`本轴值 = sign × 旧键值`）。旧键**自身链路不动**，
  例如 `immune_dmg`（免伤 30%）⇔ `damage_taken_pct: -30`。

### 13.6 校验器会拦什么（越界红拦 / 未知轴黄提示 / 撞名红拦）

| 情形 | 结果 |
|---|---|
| 声明或内容的轴取值超出范围 | **红拦**（R-2） |
| 声明的 `default` 不在 `[min,max]` 内、或 `min > max` | **红拦**（R-2） |
| 轴名与面板三轴 stem 撞名（如 `atk_pct` → `atk`） | **红拦**（R-4） |
| 声明了框架没登记的轴（自造轴） | **黄提示**（Y-18，不阻断） |
| 段结构/类型错误、NaN/Inf | **红拦**（R-1 / R-3） |

内容侧取值检查覆盖：装备/物品条目顶层词条键、符文 `by_equip_type[*].stats`。

### 13.7 本批**未登记**的轴及原因（诚实登记）

| 未登记项 | 原因 |
|---|---|
| `lifesteal_pct` / `drain_to_resource`（X12，P1） | 吸血有两条并存路径（`battle.absorb_hp` + `effects.lifesteal`）须先归并 → **无唯一消费点**；归并即改动既有吸血行为 |
| `reward_mult{scope}`（X43，P1） | 一条轴多 scope，声明形状待裁决；消费点分散三处 → 无唯一收口 |
| `stack_potency_pct`（X25，P1） | 消费点 `_aggregate_boost` 现按实例求和、不乘层数，接轴须先改聚合本身（M） |
| `buildup_rate_pct`（X26，P1） | **缺消费点**：异常累积段尚不存在 → 按「无消费点的轴不得登记」不登记 |
| `action_recovery_pct`（A-2 / X29） | 轴全集 §3 定级 **P2**（超出本批 P0/P1 范围）+ 须归并状态硬编码倍率 |
| `action_speed_pct`（X28） | 设计裁定与 X29 二选一（并存 → 指数级速度），**只留 A-2** → 不作独立轴 |
| `roll_mode` / `pierce{target}` / `stack_mode` | 聚合规则与方向参数，**不是数值轴**；穿透是既有键扩展 |
| I01 触发归属 / I02 条件变量 / I04 套装阈值 | 接线类基础设施，非数值轴 |

### 13.8 复跑与证据

```bash
# 批50 单测（登记完备 / 零变化对拍 / 校验三类 / 编辑器可见）
pytest tests/unit/test_batch50_effect_axes.py -q -o addopts=""
# 装备键族与面板校准回归（唯一源与桥接）
pytest tests/unit/test_gear_wiring.py tests/unit/test_batch22_gear_combat.py \
       tests/unit/test_batch45_panel_calibration.py -q -o addopts=""
# 字段元数据迁移门禁（新增 = soft，允许；删改 = 0）
PYTHONPATH= python scripts/compare_field_meta_migration.py
```

---

## 十四、批 51：触发归属与事件补点（内容作者向 · 触发型效果怎么声明）

> 口径：`特效整理设计_3_落点与分期.md` §二「批 49」（旧编号 = 本批，本批叫**批51**）
> + `特效整理设计_1_修正轴全集.md` §1.8 **I01**（触发时点 ＋ 持有人归属过滤，P0 第 1 项）
> + `装备被动_实现口径.md` §3.3（归属过滤方案 A）。
> 本批是**装备被动系统的前置**：只补「触发能不能按宿主正确生效」这块地基，
> **不实现**被动身份解析 / 相性变体 / 生效集缓存 / 展示（那些属后续批，见 §14.6）。

### 14.1 一句话

**带 `trigger` 的效果不再对所有单位全局误触发**：装配层把「本侧拥有哪些效果」
写进战斗单位，引擎只在**其宿主侧**的那个时点触发它。

### 14.2 触发型效果怎么写（三段：时点 → 归属 → 条件）

```jsonc
// ① effects.json：效果本体（class=special + actions 容器；既有机制，不新开）
{ "id": "blood_feast", "name": "饮血", "type": "special",
  "trigger": "on_kill",                 // ← 时点（见 14.3 清单）
  "chance": { "mode": -1 },             // ← 概率（可省；三态复用既有 chance 口径）
  "actions": [ { "type": "heal", "value": 30, "target": "self" } ],
  "condition": { "self_hp_pct": { "<": 50 } } }   // ← 可选条件门控（复用 condition_engine）

// ② traits.json：命名被动 = effects 引用（既有注册表）
{ "id": "passive_blood_feast", "name": "饮血", "effects": ["blood_feast"] }

// ③ items.json / equipment.json：装备带哪些被动（实例 passives 由打造写入）
{ "id": "crimson_blade", "name": "赤刃", "slot": "weapon" }
```

**归属**由「这件装备实例带哪些被动」决定，不需要在效果条目里写任何归属字段：
装配层按**已穿戴件实例的 `passives`**（批40 `uid` 精确定位）→ `traits.effects`
→ 效果 id 集，写进该侧战斗单位；引擎据此把 `trigger` 限制在宿主侧。

### 14.3 事件时点清单（`EVENT_POINTS`，唯一源 `data/event_points.py`）

| 时点 | 触发侧 | 何时派发 |
|---|---|---|
| `battle_start` / `battle_end` | 两侧各一次 | 开战 / 收尾 |
| `action_start` / `action_end` | 行动者 | 每次行动开始 / 收尾 |
| `turn_start` | 行动者 | 行动者回合开始 |
| `turn_end` | — | **CTB 已删除回合单位 → 不再派发**（写法保留仅为兼容） |
| `status_gain` / `status_lose` | 状态持有侧 | 状态获得 / 消失（含过期） |
| `mark_gain` / `mark_lose` | 印记持有侧 | 印记获得 / 消失 |
| `death` | **死者侧** | 任一侧死亡 |
| `on_kill` | **击杀者侧** | 我击杀敌（**批51 新增**） |
| `revive` | 复活侧 | 复活 |
| `on_attack` / `on_hit` / `on_skill` | 攻击方 | 普攻 / 命中 / 技能 |
| `season_change` | 两侧 | 换季 |

**`death` 与 `on_kill` 不得混用**（本批最重要的语义区分）：

| 想表达 | 写 | 派发给谁 |
|---|---|---|
| 我击杀敌人时回血 / 叠层 / 免冷却 | `trigger: "on_kill"` | **击杀者**（1v1 的另一侧） |
| 我死亡时爆炸 / 给对手挂状态 | `trigger: "death"` | **死者自己**（动作 `target` 相对死者侧） |

两者都在**唯一死亡判定点**（`battle._death_check_side`）派发，由死亡标记门控 →
**每次击杀恰好一次**；同归于尽（击杀者也已死）或战斗已结束 → 不派发 `on_kill`。

### 14.4 归属规则（什么时候过滤、什么时候不过滤）

| 本场战斗 | 引擎行为 |
|---|---|
| **两侧都没声明归属集**（既有内容） | **全库扫描** = 本机制引入前的旧行为，**逐字段零变化** |
| **任一侧声明了归属集** | 该侧候选 = **自己拥有的** ∪ **未被任何一侧认领的**（全局效果） |

- 第二行里「自己拥有的」= 该侧装备被动展开的效果；「未被认领的」= 没有任何战斗单位
  声明拥有的效果，仍按**全局效果**对任意侧触发（避免归属过滤把既有全局触发**静默吞掉**）。
- 因此：**A 装备的触发只在 A 身上生效**，B（哪怕带同类效果）不会串到 A 的触发上；
  而没有被任何装备认领的全局事件效果（如季节/环境类）**照常对双方触发**。
- 旧内容（无 `passives`）→ 战斗单位不带归属键 → 走第一行，**回归逐字段一致**。

### 14.5 校验器会拦什么

| 情形 | 结果 |
|---|---|
| `trigger` 写了框架**未登记**的时点（拼错 / 自造 / 未来时点） | **黄提示**（Y-19，不阻断）——该效果**永不触发**，提示里带已知时点全集 |
| `trigger` 不是字符串 | **红拦**（R-1） |
| 缺 `trigger` / 空串 | 放行（非事件型效果） |

覆盖两处声明面：`effects.<idx>.trigger` 与 `runes.<idx>.effects.<j>.trigger`。

### 14.6 本批**做到哪**、**留什么给后续批**（与装备被动系统的边界）

**本批做**（P0 缺口收口）：
1. 归属过滤（`owner_effect_ids` / `claimed_effect_ids`）；
2. 事件补点 `on_kill`（击杀者侧）；
3. **最小接线**：装备实例 `passives` → traits `effects` → 战斗单位归属集 → 按宿主触发。

**本批不做**（属装备被动系统的后续批，别在本批找）：
- 装备/模板的**被动声明字段**（`items`/`equipment` 的 `passives` 内容字段与校验）；
- 被动身份解析：打造时 `resolve_passives` 已可产出实例被动，但**相性变体求值 / 池抽取 /
  冻结口径**的完整链路与校验；
- 生效集缓存（`persistent_state.equip_passives` / `recompute_equip_passives`）与装配镜像
  （`ctx["equip_passives"]`）——本批直接从实例 `passives` 现取，**不落缓存**；
- 被动展示（名称/描述/变体对照）与 `traits` 的战斗/效果层消费点（N-1 仍成立）；
- `on_struck` / `on_block` / `on_crit` / `on_interrupt` / `on_cc` / `on_synergy` 等其余缺失
  时点：**登记为后续批**（它们各自需要新的派发点，而派发点=行为改动，须单独批次测量）。
- 条件变量扩展（`self_hp_pct` / `is_kill` 等）：本批不新增；条件门控仍用既有
  `condition_engine` 变量集。

### 14.7 复跑与证据

```bash
# 批51 单测（归属不串 / on_kill 两侧 / 装备端到端 / 校验器 / 既有 16 时点对拍）
pytest tests/unit/test_batch51_trigger_ownership.py -q -o addopts=""
# 事件分派与战斗接线回归（无归属键 → 旧行为）
pytest tests/unit/test_event_dispatcher.py tests/unit/test_event_dispatcher_battle.py \
       tests/unit/test_batch48_runes_effects.py -q -o addopts=""
# 分层契约（EVENT_POINTS 落 data 层、content 不反向依赖 core）
python scripts/check_architecture.py --path .
```

---

## 十五、批 52：治疗与承伤双向轴（内容作者向 · 三条轴怎么真生效）

> 口径：`特效整理设计_1_修正轴全集.md` §5-D2（X17/X16 治疗双向）+ §5-D3（X02 承伤收敛）
> + `特效整理设计_3_落点与分期.md` §二「批 50」（旧编号 = 本批，本批叫**批52**）
> + `特效强度预算_设计.md` §四（红线守护复算清单）。
> 批50 把三条轴**只登记、不接引擎**（作者写了不生效）；本批**接线**——现在写了真生效。
> 同一批把 `heal` 的**唯一收口**建起来（批48 的重伤通道并入），并放开**负治疗**。

### 15.1 一句话

**治疗和承伤各是一条双向轴**：同一条轴，`<0` 是减半/减伤、`>0` 是增幅/易伤；
不配置 = `0` = ×1.0，引擎逐字段零变化。

### 15.2 治疗怎么写（受疗 / 出疗是两个实体上的字段，不合并）

```
治疗量 = 基础治疗 × (1 + 受疗修正/100) × (1 + 施疗修正/100)
        受疗 = 我受到的治疗（被治疗方身上的 `healing_received_pct`）
        施疗 = 我造成的治疗（施疗方身上的 `healing_done_pct`）
```

| 想表达 | 写在哪 | 值 | 效果 |
|---|---|---|---|
| 重伤 | 受疗方 | `healing_received_pct: -50` | 受疗 ×0.5 |
| 禁疗 | 受疗方 | `healing_received_pct: -100` | 受疗 ×0（治疗归零） |
| **负治疗（反转）** | 受疗方 | `healing_received_pct: -200` | 受疗 ×(-1)：**治疗转伤害** |
| 强化治疗 | 受疗方 | `healing_received_pct: +50` | 受疗 ×1.5 |
| 施疗削弱/增幅 | 施疗方 | `healing_done_pct: -50 / +50` | 我造成的治疗 ×0.5 / ×1.5 |

**反转开关 = 声明下界**（不另设布尔）：包在 `settings.effect_axes.healing_received_pct.min`
声明下界；默认 `-200` = 允许反转，声明 `min: -100` = 关反转（治疗到 0 为止）。

**旧键 `heal_amp_pct` 已归并**（批43 悬空键 → 现在真生效）：它在 pct 层等价换算为新轴
`healing_done_pct`（`本轴值 = +1 × 旧值`），旧键自身路由不动（`pct["heal_amp"]` 仍在）。

**重伤（批48 状态通道）与新轴同轴相加**，不是两次乘：
`受疗 = clamp(状态 heal_taken 合计(经 S6 封顶) + healing_received_pct, 声明 min, 声明 max)`。
例：状态 −40 + 轴 +50 = +10 → 治疗 ×1.1（不是 ×0.6×1.5 = ×0.9）。

**多源一次改全**（唯一收口 `effects.heal_apply`，`effects.py:1760`）：heal 原子
（`:2050`）/ absorb_heal（`:1303`）/ regen（`:1350`）/ lifesteal（`:1994`）/
`battle.absorb_hp` 吸血（`battle.py:4852`）——**只改一处必漏**，本批五处全接。

### 15.3 承伤怎么写

`damage_taken_pct` 是**主乘区**（`<0` 减伤 / `>0` 易伤），与免伤旧键 `immune_dmg`
**归并到同一处求值**（`battle._damage_taken_mult`，`battle.py:4120`）：

```
承伤乘区 = [1 + (damage_taken_pct − clamp(immune_dmg, 0, 100)) / 100] × status_damage_mult?
```

- `immune_dmg: 25` ⇔ `damage_taken_pct: -25`，**只乘一次**（不双计）；
- 两者并存时**同轴相加**（`-25` 与 `immune_dmg 25` → ×0.5），绝不叠乘成 ×0.5625；
- 读时按 `settings.effect_axes.damage_taken_pct` 的 `min/max` 钳制（不写死）；
- status `damage_mult`（倒地增伤）在**同一函数内**并入（`include_status=broken_hit` 门控沿用
  既有破位窗口）→ 三路来源一处求值、破位路径一次整型截断。

**收敛边界（如实登记）**：`effects` 的 `mitigation` 阶段按批50 `consumer_note` 明文
**保持独立**（它只做减伤、与护盾/保底伤害交织，混入易伤会错）。**唯一受影响的既有组合 =
「破位 + 非零 `immune_dmg`」**：截断序由「先破位截断、再免疫四舍五入」并为一次截断，
可能相差 ±1 点（实测 |差| = 1）；无 `immune_dmg` 且无轴时与旧序**逐位一致**、实机逐字段零变化
（`docs/深度打造_决策记录.md` §十八.2 对拍表 + §18.3 斩回复算）。

### 15.4 范围与默认值是包声明的（不写死）

`settings.json` 的 `effect_axes` 段覆盖缺省区间（同批50；本批开始被引擎**消费**）：

```jsonc
{ "effect_axes": {
    "healing_received_pct": { "min": -100, "max": 300 },   // 关反转（下界收到 -100）
    "damage_taken_pct": { "min": -50, "max": 100 } } }
```

未声明该段 → 用登记表建议区间（`healing_received_pct` −200~+300、`damage_taken_pct`
−100~+300）→ 未配置轴 = 0 = 恒等。声明段经 `assembly/context.py:1858` →
`commands/battle_launch_commands.py:509` → 引擎配置 → `EffectRuntime.config`。

### 15.5 红线复算（`特效强度预算_设计.md` §四清单）

工具：`scripts/batch45_measure.py`（同种子 20260919）。**修前 = main eb21d7d，修后 = 本批**：

| 怪物（档位红线） | 档 | atk | D | 斩回（确定性） | 修前→修后 |
|---|---|---|---|---|---|
| 黑脉熊 普通（3-8） | 无buff / 半buff / 满buff | 1453 / 1582 / 1710 | 528 / 575 / 621 | 8 / 7 / 7 | **8 / 7 / 7（0 变化）** |
| 黑晶巨魔 精英（10-15） | 同上 | 同上 | 302 / 329 / 355 | 22 / 20 / 19 | **22 / 20 / 19（0）** |
| 群山嚎主 Boss（40-60） | 同上 | 同上 | 281 / 306 / 331 | 26 / 24 / 22 | **26 / 24 / 22（0）** |

- C2 装备面板占比 = **60.00%**（不变）；C4/C5 中值档 7/20/24 逐只一致；C8 等效输出变化 **0%**
  （三条轴**全是生存/受疗侧，不改变玩家输出**）→ 无需重解 `def_factor`；C9/C10/C11/C12 见单测。
- C6/C7（档位红线 / 生存轴专项）：batch45 工具口径**不含生存轴**，且**全仓内容包对特效键
  使用量为 0**（`特效强度预算_设计.md` §〇-1）→ 实机档位不变、Boss ≥40 回合不被压缩。
- **潜伏风险（不擅自调数值，仅报告 + 建议）**：`特效强度预算_设计.md` §1.6 的「典型生存
  build（减伤 25% + 吸疗 15%）」在**实机整场口径**下把 Boss 回合压到 ≈39–41（贴死 40 下沿）。
  本批只接线、不填预设；缓解建议 = 批53（数值终校）启用 `effect_budget` 上限（默认
  `cap_equiv_pct=8` / `tier_mult 12%/8%/5%` / `gate_mode="warn"`）或方案 C 怪物侧上浮，
  **在内容真正开始使用特效词条之前**先立闸。

### 15.6 复跑与证据

```bash
# 批52 单测（受疗双向三例 / 负治疗反转 / 出疗归并 / 承伤双向 / immune 不双计 / 零变化对拍）
pytest tests/unit/test_batch52_heal_damage_axes.py -q -o addopts=""
# 批48 重伤通道回归 + 批50 轴地基回归 + 批22 免伤/吸血回归
pytest tests/unit/test_batch48_runes_effects.py tests/unit/test_batch50_effect_axes.py \
       tests/unit/test_batch22_gear_combat.py tests/unit/test_batch22_effect_switches.py -q -o addopts=""
# 红线复算（修前 vs 修后；中值档 7/20/24 不变）
python scripts/batch45_measure.py --json > /tmp/fx_after.json
# 换包验收
python scripts/editor_verify_packs.py
```

---

## 十六、批 53：时序与资源轴（内容作者向 · 冷却/状态/层数/行动条/资源/会心怎么真生效）

> 口径：`特效整理设计_1_修正轴全集.md` X27 / X21 / X22 / X23 / X24 / X30 / X34 / X35 / X03；
> `特效整理设计_3_落点与分期.md` §二「批 51」（旧编号 = 本批批53）；
> `特效强度预算_设计.md` §四（红线守护）。批50 只登记、批52 接治疗承伤；本批把**时序 / 资源 / 结算**
> 余下轴接上既有消费点。**明确排除** `action_speed_mult` / `action_recovery_mult`（数学互为倒数、
> 必须二选一，用户未裁决 D2）——**不登记、不接线、不写测试**。相关决策记录见 §十九。

### 16.1 一句话

**不新增任何乘区**：七条轴全部接到**既有唯一消费点**上（冷却 α3 / `apply_status` / `_after_actor_action`
的 CTB 原语 / `resource_axis` / 会心乘区）；未配置任一轴 → 逐字段零变化。

### 16.2 逐轴落点（file:line 为实现处）

| 轴 | 语义 | 唯一消费点（实现） | 双向口径 |
|---|---|---|---|
| `cooldown_pct` (X27) | 冷却时长倍率 | `core/battle.py:3348` 技能冷却起算（α3 同乘区**相加后一次缩放**） | `+100`×2 / `−50`×0.5 / 下钳 `max(0,…)` |
| `status_chance_pct` (X21) | 施加概率 | `core/effects.py:584` `apply_status` R1 命中判定（**source 侧**） | `×(1+pct/100)`；`>100%` 溢出按每满 100% 折 1 层（仅 stack 框架） |
| `status_resist_pct` (X22) | 状态抵抗 | `core/effects.py:586` resist 判定（**target 侧**） | **加算百分点**到既有 `resist_table`，0..100 钳制 |
| `stack_gain_pct` (X23) | 层数获取 | `core/effects.py:692` stack 框架增量（**source 侧**） | `round(1×(1+pct/100))`，四舍五入 |
| `stack_cap_delta` (X24) | 层数上限 | `core/effects.py:519` `max_stack` 计算（**target 侧**） | **加算** `+Δ`，下钳 ≥1 |
| `action_bar_shift` (X30) | 行动条推动 | `core/battle.py:_after_actor_action` 收尾（`delay_actor`/`hasten_actor`） | 正 = hasten（提前）/ 负 = delay（延后） |
| `resource_cost_pct` (X34) | 资源消耗 | `core/battle.py:_apply_skill_energy` → `resource_axis.scale_amount_map` | 同一倍率同时作用于 check 与 pay，下钳 0 |
| `resource_gain_pct` (X35) | 资源获取 | 同上 → `gain_energy`/`apply_gain` 的 `mult` | 同一倍率作用于入账 |
| `crit_damage_pct` (X03) | 会心倍率 | `core/battle.py:4605` crit_roll 之后、入 rating 之前 | 会心三档等比缩放；与超会心/属性会心叠乘 |

- **旧占位键 `cooldown_reduction_pct` 归并（旧键仍可用 · 不双计）**：`route_bonus_into` 契约
  **不变**（占位键仍不进 flat/pct，批43/批50 的回归断言继续成立）；新增 `data/gear_stats
  .route_legacy_aliases_into_flat` 在**聚合入口** `core/equipment.aggregate_bonus` 逐件调用一次
  → `flat["cooldown_pct"] += −旧值`（`reduction 30 ⇔ cooldown_pct −30`）。同一旧键只被换算一次，
  α3 与新轴在**同一乘区相加** → 不双计。**与 §3 C-05 的偏差（用户口径优先）**：C-05 建议旧键
  「未声明 alias 即不生效」，本批按用户明确要求「旧键仍可用」→ **激活**；已核实全仓内容包对
  `cooldown_reduction_pct` 使用量为 0（仅测试构造），故现网内容零突变。
- **`debuff_chance_pct` / `buff_chance_pct` 归并**：经属性 pct 层 `combatant_updates(flat, pct)`
  各折一次进 `status_chance_pct`（沿用批52 别名机制）；两键**不设 scope**（口径未写分治需求，
  登记待裁决）。
- **`stack_cap_delta` 的 `marks.max_stack_of` 半边未接**：印记与状态是两套容器，口径只点名
  状态；登记待裁决，本批不动（诚实登记）。

### 16.3 排除项（待裁决）

- **`action_speed_mult` / `action_recovery_mult`**：二者数学互为倒数，**必须二选一**；用户未裁决
  → **本批不登记、不接线、不写测试**（决策记录 §十九「待裁决 D2」）。`cooldown_pct`（冷却时长）
  与「行动频率」不是一回事，不构成并存冲突。
- `counter_refund` 收编为 `action_bar_shift` 的一个实例：会改变「防反/闪反返还」的既有隐性口径
  → 本批只接轴、不动 `counter_refund`（登记待裁决）。
- `status_chance_pct` 溢出**换算率**（每满 100% 折 1 层）与 `stack_gain_pct` 是否「向上取整可配」
  属口径未写 → 本批取保守确定值（`//100`、四舍五入），登记待裁决。

### 16.4 红线复算（`特效强度预算_设计.md` §四清单）

工具：`scripts/batch45_measure.py`（同种子 20260919）。**修前 = main d5a4ea7，修后 = 本批**：

| 怪物（档位红线） | 档 | atk | D | 斩回（确定性） | 修前→修后 |
|---|---|---|---|---|---|
| 黑脉熊 普通（3-8） | 无 / 半 / 满 | 1453 / 1582 / 1710 | 528 / 575 / 621 | 8 / 7 / 7 | **8 / 7 / 7（0 变化）** |
| 黑晶巨魔 精英（10-15） | 同上 | 同上 | 302 / 329 / 355 | 22 / 20 / 19 | **22 / 20 / 19（0）** |
| 群山嚎主 Boss（40-60） | 同上 | 同上 | 281 / 306 / 331 | 26 / 24 / 22 | **26 / 24 / 22（0）** |

- `--json` 输出**修前/修后逐字节一致**（`diff` 为空）；C2 装备面板占比 = **60.00%**（不变）；
  C4/C5 中值档 7/20/24 逐只一致；C8 等效输出变化 **0%** → 无需重解 `def_factor`。
- C9/C10/C11/C12：见 `tests/unit/test_batch53_timing_resource_axes.py`（特效键不进面板轴 /
  区间钳制 / 未配置零变化 / 占位旧键归并不双计）。
- C6/C7（档位红线 / 生存轴）：全仓内容包对特效键使用量 = 0（本批亦不填预设）→ 实机档位不变。
- **潜伏风险（只报告 + 建议，不擅自调数值）**：`cooldown_pct` / `action_bar_shift` 是**行动次数轴**，
  本批只是接线、缺省为 0；一旦内容开始填，实机斩杀回合会被乘性压缩。建议同 §15.5——先立
  `effect_budget` 闸再让内容使用。

### 16.5 复跑与证据

```bash
# 批53 单测（A 冷却 · B 状态概率/抵抗 · C 层数 · D 行动条 · E 资源 · F 会心 · G 零变化）
pytest tests/unit/test_batch53_timing_resource_axes.py -q -o addopts=""
# 既有回归（α3 / 批43/47/50/52 / CTB 反击返还 / 资源）
pytest tests/unit/test_batch26_alpha3_skill_amp.py tests/unit/test_batch43_enhance_tiers.py \
       tests/unit/test_batch50_effect_axes.py tests/unit/test_batch52_heal_damage_axes.py \
       tests/unit/test_counter_refund.py tests/unit/test_combo_battle_link.py -q -o addopts=""
# 红线复算（修前 vs 修后；本批逐字节一致）
python scripts/batch45_measure.py --json > /tmp/fx_after.json
# 换包验收 / 字段元数据迁移门禁
python scripts/editor_verify_packs.py
PYTHONPATH= python scripts/compare_field_meta_migration.py
```


---

## 十七、批 54：预设集与结算轴（内容作者向 · **怎么用预设**）

> 口径：`特效整理设计_2_框架预设包.md` §1（预设词条集）/ §2（成套预设）/ §4（速览）；
> `特效整理设计_1_修正轴全集.md` §3 P1 第 16 项（X43 奖励倍率）；
> **复用既有机制** `content/entry_presets.py`（批17 物品通用预设）——**不新造第二套**。
> 相关决策记录见 §二十。

### 17.1 一句话

**作者不用背轴名**：在编辑器「新建条目」选一个预设（如「重伤」），轴与档位自动填好；
同一条轴的**两个方向 = 两条预设**（同族互斥），不是两条轴。

### 17.2 怎么用预设（三步，编辑器）

1. 打开内容包 → 模块 **`equipment`**（装备/物品注册表）→「新建条目」；
2. 「条目预设」下拉选一条（如「重伤」）→ 主区只显示该预设关心的字段：
   名称 / 类型 / 说明 / 部位 / 价格 / **该预设的轴**；
3. 改名称 → 创建。`defaults`（轴取值）由**服务端按生效预设**落档，不依赖前端提交。

- **不选预设**（默认「（不用预设 · 显示全部字段）」）→ 与批17 现状**逐字段一致**；
- 选预设后未包含的字段进「其他字段」折叠区，**仍可编辑**（一号原则：预设只收窄显示面，
  不做永久隐藏）。
- **模块边界（如实说明）**：特效预设声明在 **`equipment`** 模块（装备注册表，含 `slot`）；
  `items` 模块保留批17 的**六种通用品类预设**（逐字段不变）。若某内容包把装备登记在
  `items` 模块（本仓库 veinborn 的武器/防具即为 `items` 条目），可在包 `field_meta.json`
  的 `entry_presets.items` 里**追加**同名预设，或作者直接按 §17.6 写轴键——两条路都不需要
  改框架（复用既有机制）。

### 17.3 同族两方向 = 两条预设（不是两条轴）

设计最高原则**双向修正轴优先**：同一族只落一条轴，两个方向各给一条预设，共用
`conflict_group` → **互斥**（同一件装备只能取一端）。执行仍走**既有装备互斥链**
（`excludes` / `mutual_exclusions`）——预设只给作者判断依据，**不新增互斥引擎**。

| 族（`family`） | 轴 | 负向（down） | 正向（up） |
|---|---|---|---|
| `healing_received` | `healing_received_pct` | `armor_heal_down` 重伤 | `armor_heal_up` 强疗 |
| `damage_taken` | `damage_taken_pct` | `armor_taken_down` 坚壁 | `armor_taken_up` 破绽之甲 |
| `crit_damage` | `crit_damage_pct` | `weapon_crit_down` 钝锋 | `weapon_crit_up` 会心锋刃 |
| `cooldown` | `cooldown_pct` | `weapon_cd_haste` 循环加速 | `weapon_cd_seal` 封印回响 |
| `stack_gain` | `stack_gain_pct` | `accessory_stack_damp` 抑层 | `weapon_stack_gain` 涌流之刃 |
| `stack_cap` | `stack_cap_delta` | `accessory_stack_cap_down` 缚层 | `weapon_stack_cap` 拓层之刃 |
| `status_resist` | `status_resist_pct` | `armor_ctrl_vuln` 失神 | `armor_ctrl_res` 定神 |
| `status_duration_taken` | `status_duration_taken_pct` | `armor_dur_down` 涤净之躯 | `armor_dur_up` 缠身之甲 |
| `resource_cost` | `resource_cost_pct` | `accessory_cost_down` 节流 | `accessory_cost_up` 挥霍 |
| `action_bar` | `action_bar_shift` | `accessory_delay` 迟滞符 | `accessory_first_strike` 先手符 |

> 任务书范例：**重伤 = `healing_received_pct: -15` / 强疗 = 同轴 `+15`**（编辑器落普通档；
> 稀有档为 `-50 / +50`）。**不要**为两个方向各建一条 trait / 两个轴键。

### 17.4 每条预设给你的六个信息

| 信息 | 声明键 | 说明 |
|---|---|---|
| 稳定 id | `id` | 如 `armor_heal_down`；包可用同 id 整体覆盖 |
| 中文名 | `label` | 编辑器下拉显示 |
| 一句话说明 | `help` | **≤ 60 字**（一号原则：Tip/引导，忌长文） |
| 使用的轴与取值 | `axis` + `tiers` | 轴键 + 普通/进阶/稀有三角档取值 |
| 适用部位或档位 | `slot` / `tiers` | 武器 / 护甲 / 饰品 / 通用；档位见 §17.7 |
| 叠加与互斥 | `stack` / `conflict_group` | 多来源叠加口径；同组 + 反方向 = 互斥 |

**唯一源**：`qbot_rpg/content/effect_presets.py` 的 `FRAMEWORK_EFFECT_PRESETS`
（新增预设 = 这张表加一条，投影 / 编辑器 / 校验自动跟进）。

### 17.5 框架预设清单（26 条，设计 §1.1~§1.3；数值 = 设计档位示意）

**武器（weapon）**

| id | 中文名 | 轴 | 普通/进阶/稀有 | 部位 | 互斥组 | 一句话 |
|---|---|---|---|---|---|---|
| `weapon_dmg` | 破甲锋刃 | `damage_dealt_pct` | 10 / 25 / 45 | weapon | damage_dealt | 造成伤害提升：终伤乘区 ×(1+值/100)（>0 增伤）。 |
| `weapon_crit_up` | 会心锋刃 | `crit_damage_pct` | 10 / 25 / 40 | weapon | crit_damage | 会心倍率提升：暴击伤害 ×(1+值/100)（>0 强化）。 |
| `weapon_crit_down` | 钝锋 | `crit_damage_pct` | -10 / -20 / -30 | weapon | crit_damage | 会心倍率下降：暴伤 ×(1+值/100)（<0 削弱，换更高白值的代价型）。 |
| `weapon_cd_haste` | 循环加速 | `cooldown_pct` | -10 / -20 / -30 | weapon | cooldown | 冷却缩短：技能冷却 ×(1+值/100)（<0 更快；结果下钳 0）。 |
| `weapon_cd_seal` | 封印回响 | `cooldown_pct` | 15 / 30 / 50 | weapon | cooldown | 冷却延长：技能冷却 ×(1+值/100)（>0 更慢，换高额伤害的代价型）。 |
| `weapon_stack_gain` | 涌流之刃 | `stack_gain_pct` | 25 / 50 / 100 | weapon | stack_gain | 层数获取提升：每次叠层量 ×(1+值/100)（>0 叠得快）。 |
| `weapon_stack_cap` | 拓层之刃 | `stack_cap_delta` | 1 / 2 / 3 | weapon | stack_cap | 层数上限加算（+1 层）：不是百分比，直接抬高层数上限。 |
| `weapon_lifesteal` | 汲血 | `absorb_hp` | 3 / 6 / 10 | weapon | lifesteal | 吸血：造成伤害后按该比例回复自身生命（单位 %，0-100）。 |
| `weapon_pierce` | 破防 | `pierce_pct` | 10 / 20 / 30 | weapon | pierce | 物穿比：按比例无视目标物理防御（单位 %，0-100，既有封顶不放宽）。 |

**护甲（armor）**

| id | 中文名 | 轴 | 普通/进阶/稀有 | 部位 | 互斥组 | 一句话 |
|---|---|---|---|---|---|---|
| `armor_taken_down` | 坚壁 | `damage_taken_pct` | -8 / -15 / -25 | armor | damage_taken | 减伤：受到的伤害 ×(1+值/100)（<0 减伤）。 |
| `armor_taken_up` | 破绽之甲 | `damage_taken_pct` | 8 / 15 / 25 | armor | damage_taken | 易伤：受到的伤害 ×(1+值/100)（>0 受伤增加，换输出）。 |
| `armor_heal_up` | 强疗 | `healing_received_pct` | 15 / 30 / 50 | armor | healing_received | 受疗提升：受到的治疗 ×(1+值/100)（>0 增疗）。与『重伤』同族互斥。 |
| `armor_heal_down` | 重伤 | `healing_received_pct` | -15 / -30 / -50 | armor | healing_received | 受疗下降：受到的治疗 ×(1+值/100)（<0 减疗）。与『强疗』同族互斥。 |
| `armor_ctrl_res` | 定神 | `status_resist_pct` | 20 / 35 / 50 | armor | status_resist | 状态抵抗：更难被挂上控制/减益（加算百分点，>0 更抗）。 |
| `armor_ctrl_vuln` | 失神 | `status_resist_pct` | -20 / -40 / -60 | armor | status_resist | 状态抵抗下降：更易被挂上控制/减益（<0 易感，换收益）。 |
| `armor_dur_down` | 涤净之躯 | `status_duration_taken_pct` | -15 / -30 / -50 | armor | status_duration_taken | 受状态时长缩短：挂上的状态更快结束（<0 抗控）。 |
| `armor_dur_up` | 缠身之甲 | `status_duration_taken_pct` | 15 / 30 / 50 | armor | status_duration_taken | 受状态时长延长：挂上的状态持续更久（>0 代价型）。 |

**饰品（accessory）**

| id | 中文名 | 轴 | 普通/进阶/稀有 | 部位 | 互斥组 | 一句话 |
|---|---|---|---|---|---|---|
| `accessory_cost_down` | 节流 | `resource_cost_pct` | -10 / -20 / -35 | accessory | resource_cost | 省耗：技能资源消耗 ×(1+值/100)（<0 更省；下钳 0，-100 = 免费）。 |
| `accessory_cost_up` | 挥霍 | `resource_cost_pct` | 10 / 25 / 40 | accessory | resource_cost | 更耗：技能资源消耗 ×(1+值/100)（>0 更贵，换收益）。 |
| `accessory_gain_up` | 灵泉 | `resource_gain_pct` | 15 / 30 / 50 | accessory | resource_gain | 资源获取提升：资源获得量 ×(1+值/100)（>0 回得快）。 |
| `accessory_first_strike` | 先手符 | `action_bar_shift` | 3 / 6 / 10 | accessory | action_bar | 行动条提前（加算 +N）：正 = 提前行动。与『迟滞符』同族互斥。 |
| `accessory_delay` | 迟滞符 | `action_bar_shift` | -3 / -6 / -10 | accessory | action_bar | 行动条延后（加算 -N）：负 = 延后行动，代价型。与『先手符』同族互斥。 |
| `accessory_stack_gain` | 涌流 | `stack_gain_pct` | 25 / 50 / 100 | accessory | stack_gain | 层数获取提升：每次叠层量 ×(1+值/100)（>0 叠得快）。 |
| `accessory_stack_damp` | 抑层 | `stack_gain_pct` | -25 / -50 / -75 | accessory | stack_gain | 层数获取下降：每次叠层量 ×(1+值/100)（<0 叠得慢，换收益）。 |
| `accessory_stack_cap_down` | 缚层 | `stack_cap_delta` | -1 / -2 / -3 | accessory | stack_cap | 层数上限加算（-1 层）：压低上限，对抗叠层型敌人或作代价。 |
| `accessory_bewitch` | 惑心 | `status_chance_pct` | 15 / 30 / 50 | accessory | status_chance | 状态命中提升：施加状态的基础命中 ×(1+值/100)（>0 更易挂上）。 |

### 17.6 落位示例（预设落档 = 作者手写，同形）

选「重伤」预设并创建后，`equipment.json` 里的条目就是**既有扁平字段**（与 `atk` / `dfn_pct`
同处），完全走既有 `extract_bonus → route_bonus_into → combatant_updates` 链路：

```jsonc
{
  "id": "eq_001",
  "name": "裂伤护胸",
  "type": "armor",
  "slot": "armor",
  "price": 200,
  "healing_received_pct": -15   // ← 「重伤」预设普通档落档（受疗 ×0.85）；稀有档 -50（×0.5）
}
```

**书写口径 = 百分点增量**（不是乘数）：`有效值 = 基准 × (1 + 轴值/100)`；
`-15` ⇔ ×0.85、`0` ⇔ ×1.0、`+15` ⇔ ×1.15、`-100` ⇔ 禁疗。加算轴（层数上限、行动条推动）
直接写差值、键名**没有** `_pct` 后缀（如 `stack_cap_delta: 2`）。

### 17.7 档位（普通 / 进阶 / 稀有）

- 编辑器「新建条目」落 **`normal`（普通）** 档（= 预设 `defaults`）；
  `advanced` / `rare` 只在预设声明里（`tiers`），**本批不接「掷档」**——掉落/强化系统如何选档
  属后续批次，登记待裁决。
- 档位**只改数值**，不改轴与触发方式（设计 §1.0）；乘法轴的偏离幅度约 `1 : 2 : 3.4`。
- 表内数值为**设计档位示意**，**不是平衡终值**；本批**不调任何数值平衡**（红线）。

### 17.8 结算 / 奖励轴核查结论（**如实报告，不登记、不接线**）

设计项 **X43 `reward_mult{scope}`**（`特效整理设计_1_修正轴全集.md` §3 P1 第 16 项，
`特效整理设计_3_落点与分期.md` §1.2 F5 `L-1`）要求「一条轴 + `scope ∈ {exp, coins, gem,
rep, drop_chance, drop_count, drop_rarity}` 覆盖多类，**勿造三键**」。核查既有消费点后：

| 奖励类别 | 现状消费点 | 能否作「唯一收口」 |
|---|---|---|
| `coins` / `gem` / `rep`（标量） | `core/reward.py` `_SCALAR_KEYS`（`dispatch_reward` 唯一发放器） | **否**：任务/签到/NPC/钓鱼/成就/PvP **共用**该发放器 → 在此加装备倍率会外溢到非战斗奖励 |
| `exp` | `core/battle_reward.py:settle_battle_rewards` 内经 `LevelUpEngine.gain_exp`（**不走** `dispatch_reward`） | **否**：与上面的币不是同一点 |
| 掉落 `drop_*` | `core/battle_reward.py:roll_death_drops` 的 `chance` roll | **否**：第三处 |

**结论（与批50 §13.7 的登记一致）**：三个类别分散在**两处函数 + 一个被非战斗系统共用
的发放器**，且框架的**扁平数值键空间**无法在不「造多键」（违反设计明文）或新造「对象型
轴」形状（违反「复用既有机制」）的前提下承载 `scope` → **本批不登记该轴、不接线、不写预设**，
理由如实登记（**不为交差硬造**）。若后续要落地，须先裁决：
`scope` 是「多键」还是「对象型轴」，以及装备奖励倍率是否应只作用于**战斗结算**。

> **红线守护**：`tests/unit/test_batch54_effect_presets.py`
> `test_reward_axis_not_registered_no_single_consumer` 把「未登记 + 三处分散」钉死，
> 防后续误以为「已接线」。

### 17.9 本批**未纳入**的预设族（诚实登记）

| 未纳入（设计条目） | 原因 |
|---|---|
| `weapon_wound`（命中降目标受疗）、`weapon_dmg_elem/skill/dot/weakpoint`（定向增伤） | 需 **on_hit 效果条目 + 条件/scope**（契约 B），非扁平装备字段 |
| `weapon_leech_res` / `weapon_convert` / `weapon_crush` / `weapon_pierce_elem` | 轴 `drain_to_resource` / `dmg_convert` / `damage_basis` / `pierce{target}` **未登记**（无唯一消费点或需参数化） |
| `weapon_speed_up/down`（X28/X29） | 二选一**待裁决 D2**（数学互为倒数）→ 不登记、不接线、不写测试 |
| `armor_flat_red` / `armor_thorns` / `armor_counter_amp` / `armor_fatal_guard` / `armor_shield_bloom` / `armor_status_ward` / `armor_immunity_charges` / `armor_evade` | 走**效果条目 / 未登记轴 / 布尔枚举**（`dmg_reduction_flat` / `reflect_pct` / `counter_damage_mult` / `fatal_guard_max` / `evasion_mult` …） |
| `armor_lowhp_ward` | 需**条件门控**（I02 条件变量注册表）→ 非扁平字段 |
| `accessory_regen` / `accessory_initiative` / `accessory_accuracy` / `accessory_mirage` / `accessory_overheal` / `accessory_dot_brand` | 轴 `recovery_rate_mult` / `initiative_add` / `hit_mult` / `evasion_mult` **未登记**；`overheal` 是布尔开关；`dot_brand` 走效果引用 |
| `accessory_greed` / `accessory_ascetic` | 见 §17.8（`reward_mult` 无唯一收口） |
| `common_*`（proc / 叠层 / 套装 / 持久成长 / 异常积蓄 / 属性转化 / 授技 / 时序校准） | **触发 / 结构 / 效果引用类**，不是扁平数值轴（走 `effects` / `traits` 契约，非本预设集） |
| `healing_done_pct`（施疗侧，X16） | 轴**已登记**且已接线；设计 §1 **未给具名预设** → 不为它臆造预设名 |

### 17.10 校验与纪律

- **越界红拦 / 引用轴存在**：`effect_preset_errors()` 逐条校验——轴须在宿主模块元数据登记，
  三档取值须落在该轴**有效区间**（特效轴取 `settings.effect_axes` 声明，既有键取字段元数据
  区间），另查 id 唯一 / 部位与方向合法 / `help` 忌长文 / `defaults` 取普通档。
- **框架预设 ≠ 内容数据**：预设只声明在框架代码，**不写进任何内容包**；包可**同 id 覆盖 /
  追加 / 关闭**（`entry_presets_disable`），沿用批17 口径。
- **不配置 = 零变化**：不选预设 → 新建条目逐字段与批17 一致；`items` 六种通用品类
  **逐字段不变**；框架新增预设不影响任何既有条目 / 战斗结算。
- **不写死模块名 / 字段名 / 数值**：宿主模块、轴键、档位取值全在声明表；逻辑层不分支。

### 17.11 复跑与证据

```bash
# 批54 单测（A 可枚举 · B 可校验/越界红拦 · C 同族两方向 · D 复用 entry_presets ·
#            E 编辑器可见 · F 落进产物实例 · G 零变化 · H 结算轴核查结论）
pytest tests/unit/test_batch54_effect_presets.py -q -o addopts=""
# 既有回归（批16/17/18 预设链路 + 批50 轴地基 + 装备接线）
pytest tests/unit/test_editor_batch16_presets.py tests/unit/test_editor_batch17_framework_presets.py \
       tests/unit/test_editor_batch18_preset_refine.py tests/unit/test_batch50_effect_axes.py \
       tests/unit/test_gear_wiring.py -q -o addopts=""
# 红线复算（本批不改战斗/数值 → 修前/修后逐字节一致）
python scripts/batch45_measure.py --json > /tmp/fx_after.json
# 换包验收 / 字段元数据迁移门禁
python scripts/editor_verify_packs.py
PYTHONPATH= python scripts/compare_field_meta_migration.py
```
