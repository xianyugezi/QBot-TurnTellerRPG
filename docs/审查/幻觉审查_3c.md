# 幻觉审查报告：《细化_3c_指令解析契约.md》

- **审查对象**：`/root/QBot-TurnTellerRPG/docs/细化/细化_3c_指令解析契约.md`（421 行）
- **定稿依据**：`/root/docs_archive/RPG框架项目/指令分隔符统一规范.md`（246 行，v1.1）
- **配套核验**：`细化_2b2_对话会话状态机.md`（245 行，文件存在；R1–R5 位于 L120–124）、`细化_1g1a_战斗状态集.md`、`细化_1g2_回合时序与拦截链.md`（均存在）
- **审查方法**：通读细化全文 → 抽取全部 `[Lxx]`/`[2b2·...]` 声明 → 回定稿/2b2 逐一核对（①行号引对 ②内容一致 ③补白显式标注 ④数值有依据）

---

## 一、总体结论

| 项 | 结果 |
|---|---|
| 规范行号引用总数 | 约 150 处 `[Lxx]`，逐条核对 **全部指向真实存在且内容相符的行**，无一条指向空行/错位行 |
| 跨文档引用（2b2） | 文件存在；R1–R5 行号（L120–124）、L96（路由层二选一）、L135（会话激活区间）**全部核对一致** |
| 补白标注纪律 | 会话路由泛化至战斗（S4、§五、5.2、TC-39）、快捷展开层级≤1（S7、P04）、快捷名冲突文案（C03）等均显式标注【工程补白】 |
| 数值核对 | 20 条上限（L172）、50 条别名上限（L223）、99 上限（L33/L73）、≤4 字符（L186）、1-4 字符（L162）、7 种分隔符（L6/L13-21）、TC 计数 14+2+6+1+9+3+6+4=45——**全部有据** |
| 关键引语 | 「动态指令注册表：框架指令+内容包注册指令+固定子词」（L158）、四类错误模板（L72）、固定子词表（L71）、/雇工 键值示例（L49）、N.离开固定末项（2b2·L63）等——**逐字一致** |

**未发现虚构行号、张冠李戴、关键数值捏造或"无中生有"式规范内容。**

---

## 二、问题清单

### P0（0 条）——引用不存在的行号 / 内容与定稿实质冲突 / 虚构规范内容

无。

### P1（0 条）——行号指向存在但内容严重不符 / 关键规则张冠李戴

无。

### P2（4 条）——表述瑕疵 / 引用区间略宽 / 自述不一致，不影响语义正确性

| # | 位置 | 问题 | 证据 | 建议 |
|---|---|---|---|---|
| P2-1 | 文档头 L6 覆盖声明 | 把「子解析实现顺序」列为"规范未显式定义处（需补白）"，但规范 L69 已**显式**写明"参数内解析顺序固定：先按 , 拆列表 → 再按 * 拆数量 → 再按 = 拆键值"；且本文 S3 接缝裁决自己又按 L69 落实（"按 L69 字面顺序实现"）。**自述矛盾**：前后两处对同一事项的定性不一致，可能误导读者以为子解析顺序是补白。 | 规范 L69 vs 细化 L6、L34（S3） | 将 L6 覆盖声明中的「子解析实现顺序」一项删除，或改为「后置修饰（+/-/>）检测时机」 |
| P2-2 | TC-02（L344） | 断言回显文案"下次可以这样写：/使用 药水*10"，而定稿 L239 原文示例为"下次可以这样写：/合成 药水*10"。模板句一致、示例指令被替换（输入用例本身是 `/使用 药水 10`，回显 /使用 属合理应用），但非定稿原文逐字。 | 规范 L238-239 vs 细化 L344 | 属示例适配，可接受；如求严格可注明"示例指令按用例替换" |
| P2-3 | 接缝 S4（L35） | 声称"会话路由语义在 NPC 定稿 L58-70"，而配套 2b2 头注（L5）声明同一节为"§2.1 会话路由规则（L55–71）"。行号区间不同（58-70 ⊂ 55-71），未直接核对 NPC 定稿原文，区间系 3c 自估。 | 细化 3c L35 vs 细化_2b2 L5 | 建议统一为 2b2 声明的 L55-71 或标注"区间采自 2b2" |
| P2-4 | 覆盖矩阵 ①（L329） | "§一/§二（L13-51）"区间含 L24-28 空白/分隔与 L52-54 分隔行，行号区间偏宽（§一实为 L11-23、§二实为 L27-51）。属区间标注略宽松，所覆盖内容无误。 | 定稿 L11-51 | 可收窄为"L13-23、L29-51"提升严谨性 |

**说明（非问题）**：S0–S8 状态机整体为细化工程化建模（规范无此图），但每个状态行的规则均带真实规范行号背书，且头注已声明"仅对规范已有语义做工程化定型"，不属于幻觉内容。S1-S7 接缝裁决中"未登记符号按未知分隔符处理""快捷展开深度=1"等派生规则均已显式标注依据或【工程补白】。

---

## 三、评分

**评分：A**

- 全部约 150 处规范行号引用真实且内容一致（含逐字引语 10+ 处）；
- 跨文档引用（2b2 R1-R5、L96、L135）逐一核实无误；补白标注纪律良好；关键数值全部有据；
- 仅 4 处 P2 级表述瑕疵（自述不一致 1、示例替换 1、区间略偏 2），无任何规则层面的虚构或错引。

**一句话**：细化_3c 是一份可高度信赖的工程化契约文档——行号全真、引语逐字、补白显式、数值有据，仅存 4 处 P2 级表述瑕疵，无需返工。